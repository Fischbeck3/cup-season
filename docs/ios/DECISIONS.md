# iOS decision log

Sequential IDs, never reused. IOS-001…005 are the five founding artifacts (documents); decisions proper start at IOS-006. A decision is never overwritten — new information gets a new entry that references the old one. Product-mechanic decisions still ALSO go in `spec/decision-log.md` (CLAUDE.md rule 5); this log is the iOS build's own record and cross-references D-numbers.

Priority: **P0** must decide, work stops · **P1** recommendation given, pauses if it substantially changes the experience · **P2** documented, proceeding.

Format per entry: ID · date · priority · decision · options · recommendation · final choice · rationale · files/features affected.

---

## Founding artifacts

| ID | Date | Artifact | File |
|---|---|---|---|
| IOS-001 | 2026-08-27 | Full Product & Feature Audit (+ parity matrix) | `IOS-001-audit.md`, evidence in `audit/` |
| IOS-002 | 2026-08-27 | Native iOS Architecture | `IOS-002-architecture.md` |
| IOS-003 | 2026-08-27 | iOS Design Direction | `IOS-003-design-direction.md` |
| IOS-004 | 2026-08-27 | Web → iOS Opportunity Map | `IOS-004-opportunity-map.md` |
| IOS-005 | 2026-08-27 | Implementation Roadmap | `IOS-005-roadmap.md` |

---

## IOS-006 · The phone's stack: SwiftUI/Xcode or Expo/React Native — **P0 · DECIDED 2026-08-27: SwiftUI**

**Final choice (owner, 2026-08-27):** Option 1, SwiftUI in Xcode. Logged upstream as D99 in `spec/decision-log.md`. Consequences taken: `apps/ios/` is the phone; `apps/mobile/` (Expo B1) is retired in M0 per IOS-017; Android is not from this codebase (D98 Phase C is re-opened, not scheduled); `packages/` stays the shared source and gains Swift emitters.

*2026-08-27. Reopens the implementation half of D98; does not touch its product half.*

**Decision.** Build the phone in Swift/SwiftUI (Xcode), or continue the Expo/React Native app D98 chose on 2026-08-26.

**Context.** D98 picked Expo for "Android from the same codebase" (Phase C) and "one language family across three surfaces" (Phase D's React desktop). The 2026-08-27 directive asks for a native iOS app in Xcode with Live Activities, widgets, Dynamic Type, haptics and Swift concurrency. The B1 Expo scaffold exists (uncommitted, ~600 lines, preflight-clean, SDK 56 pinned to the phone's Expo Go, a Hermes memory regression pending SDK 57) but its gate — a real sign-in on the owner's iPhone — is not evidenced; the one attempt to build to the phone failed on code signing, not on the app. Full analysis: `audit/09-ios-state-and-canon.md` §9.

**Current web behaviour.** N/A (the web client is unaffected either way).

**Options.**
1. **SwiftUI / Xcode.** One toolchain (Xcode + SPM + supabase-swift). Live Activities, widgets, Watch, App Intents are first-class and share one `RoundState` model with no JS↔Swift bridge. Dynamic Type and haptics are the real system, not approximations. Costs: Android is *not* from this codebase (a Year-2 decision, or a separate RN/Kotlin app); the desktop React rewrite shares only `packages/` (tokens, contract), not components; two small generators (Tokens.swift, Rpc.swift) and ~100 lines of re-encoded rules; the Expo scaffold is retired.
2. **Expo / React Native** (D98 as decided). Reuses `packages/db` and `theme.ts` directly; Android nearly free; possible component sharing with the React desktop. Costs: Live Activities/widgets/Watch still require Swift extension targets inside an Expo-managed Xcode project plus an App Group bridge for round state — the part most likely to break on each SDK upgrade, on a project already downgraded to SDK 56; a dev client + signing for push anyway; Dynamic Type is scaling, not text-style semantics.
3. **Hybrid: Expo now, Swift for extensions later.** This is option 2 with the cost deferred, not removed.

**Recommendation.** Option 1, SwiftUI — *if* the reasons for a native app are the ones D98 itself gave (a live match on the lock screen, scoring on a watch) and Android is a Year-2 question. Option 2 — *if* Android in Phase C and shared React UI with the desktop rewrite are near-term commitments. The repo's evidence leans to 1: D98's justification is native-only surface area; `gtm-year1.md`, the App Store runbook and CLAUDE.md plan only for iPhones; the phone's scope is deliberately ~10–15 screens; the Expo hours so far went to toolchain friction.

**Tradeoffs.** Gain (1): the headline features without a bridge; fewer moving parts for a solo builder; the signing step becomes a dropdown. Give up (1): D98's Android benefit; the ~600-line scaffold (the ideas survive — Keychain sessions are built in, `theme.ts` becomes generated Swift).

**Reversibility.** HARD after M1 (either way). EASY today.

**Action.** Waiting for the owner. IOS-002 is written for option 1 with option 2 mapped in its §13; IOS-005's sequencing holds under both. **Independent of the answer, the first engineering task is identical: set the team in Xcode and get a signed build onto the owner's iPhone.**

**Files/features affected.** Everything under `apps/`; `tools/build-tokens.mjs`, `tools/build-db.mjs` (new emitters); `tests/preflight.mjs` checks 12–14; `spec/native-arc.md` Phase B/C wording; D98 needs a follow-up entry in `spec/decision-log.md` whichever way this goes.

---

## IOS-007 · What the phone owns: the D98 split vs the directive's definition of done — **P0 · DECIDED 2026-08-27: Option 2**

**Final choice (owner, 2026-08-27):** Option 2 as recommended — the golfer's whole life plus the Pro's pocket tools (quick-start creation, draw, start, mark-paid, bye, starter index, endgame dial, announce, invite, D71 vote); authoring (full wizard dials, lock, assign, overrides, founder dashboard) stays on the desk. Logged upstream in D99.

*2026-08-27.*

**Decision.** How much of the league-running surface lives on the phone.

**Context.** D98 says the phone is for the tee box (scoring, posting, board, push, standings read) and the desk owns the wizard, draft, roster, ledger, month closes, receipts and founder desk. The directive's Definition of Done says a real user must be able to *create or join a competition, track the pot, manage participation* and lists admin/moderation among audited features. Audit 06 §10 evaluated D98's "desk work" claim screen by screen: the wizard (twelve dials) and the draft pick clock are genuinely desk; the **pot read and mark-paid are phone-first** (money changes hands in the parking lot); the ceremony is phone-first; bye/starter-index are phone-worthy; overrides, report resolution, `app_flags`, the sandbox are desk.

**Current web behaviour.** Everything is on one surface; the wizard is a 3-step form with a "Customize" disclosure holding ~12 dials; the pot pane is one number, three tiles and a tap-to-mark list.

**Options.**
1. **D98 verbatim.** Phone never creates a league, never shows the pot pane, no Pro tools. Fails the directive's DoD on "create a competition" and "track the pot".
2. **Golfer's whole life + Pro's pocket tools (recommended).** Phone: join (covenant), **quick-start** league creation (name · preset · stake — three fields; the "Customize" dials stay desk with a hand-off link), create Ryder/Major (their sheets are small), **the blind draw and "Start the season"** (single RPCs with server validation; audit 02 §8 calls the draw "the league's one appointment moment" — a phone moment by D54's own reasoning), pot summary + Pro mark-paid + forfeits + ceremony from `season_payouts` + D71 vote, Pro pocket tools (announce, starter index, bye, endgame dial, make Pro, invite), draft **reveal** read-only. Desk: full wizard dials, the lock sequence (five client writes today — should become one RPC before any phone touches it), Pro assign (desk-first, phone fallback), the pick clock, ledger overrides, report resolution, flags, sandbox, founder dashboard (field note stays on phone). Audit 02's summary of the honest split: *authoring* (wizard, lock, assign, overrides) on the desk; *operating* (draw, start, bye, buy-in, finish dial) plus *reading everything* on the phone.
3. **Full parity.** Port the wizard dials and draft board to the phone. Contradicts D98's principle argument ("a draft on a 6" screen is friction we invented") and Persona 1 being a desktop user.

**Recommendation.** Option 2. It meets every line of the DoD with real backend data and keeps D98's principle intact.

**Tradeoffs.** Gain: the phone is complete for Persona 2 and useful for Persona 1 between sessions. Give up: a phone Pro cannot customise bylaws beyond the preset without the desk.

**Reversibility.** EASY (additive screens).

**Action.** Waiting for the owner. The uncontested core (M0–M3) does not depend on this; M5 does.

**Affects.** IOS-005 M5; `spec/native-arc.md` "Owns" table; `spec/decision-log.md` (D98 follow-up).

---

## IOS-008 · What "match" and "next opponent" mean on the phone — **P1 · PROCEEDING WITH RECOMMENDATION**

*2026-08-27.*

**Decision.** How Home treats "next match / opponent / match approaching" language from the directive.

**Context.** Cup Season's season has **no fixture list**: squads accumulate points from any posted round; Format B (monthly H2H) was retired by D48; even the Cup Final pairs nobody. Exactly four objects can honestly be called a match (audit 04 §1): the tee-sheet game (created on the first tee, never scheduled), the **Ryder duel** (the only scheduled head-to-head, and Home never shows it today), the Major (no opponent), and the retroactive weekly clash. D12 fixes the nouns.

**Options.** (1) Add a generic "next opponent" slot — empty for ~90% of users, invents a mechanic the product rejected. (2) **Home leads with the live-round banner, then the standing, then "your match this week" *only* when a Ryder session is open, then "your next round" (scheduled rounds).** (3) Build D52's weekly clash spotlight (owner-approved, unbuilt) to give every league member a weekly "vs" — needs a table, posts and settle logic.

**Recommendation.** (2). (3) is a real product option and is parked as a question, not designed around.

**Reversibility.** EASY.

**Action.** Proceeding with (2). Flag if the owner wants (3) in scope.

**Affects.** IOS-002 §4, IOS-004 #1, M1/M5.

---

## IOS-009 · Backend additions for the native client — **P1 · PROCEEDING (additive, owner runs `db push`)**

*2026-08-27.*

**Decision.** Ship eleven small, additive backend changes the phone needs (IOS-002 §14): `close_month` revoke (security), `min_ios_build` flag, `native_home()`, `post_round()`, `round_holes_of()`, `handle_available()`, APNs payload routing + categories, `invite_golfer` push fan-out, mute-aware push recipients, `event_players.notify_board`, `live_set_scores` batch + idempotent finish, `device_tokens.platform` widening.

**Context.** None changes web behaviour; each fixes a debt the audit found (the direct `rounds` insert; unreadable `round_holes`; `url:'/'` on every push; a promised invite notification that never sends; a month close any member can trigger).

**Options.** Ship in two batches ahead of M0 and M4 (recommended) · ship lazily per milestone · never (phone mirrors the web's client-side fan-out).

**Recommendation.** Two batches. Batch 1 (before M0): items 10, 6, 1, 3, 8. Batch 2 (before M4): 4, 5, 9, 7. Item 11 before Android.

**Reversibility.** EASY (new migrations; D37 grant discipline; `contract.psv` refreshed after each push).

**Action.** Batch 1 WRITTEN 2026-08-27 (`supabase/migrations/20260827130000…130400`, libpg-query-parsed, `tests/db-checks.sql` check 12 added); **PUSHED 2026-08-27** (owner-authorised, run from the local session); snapshot refreshed; `Rpc.swift` carries the three new functions. The `close_month` revoke should go first regardless of iOS.

**Affects.** `supabase/migrations/`, `supabase/functions/push`, `packages/db/contract.psv`, `tests/db-checks.sql`.

---

## IOS-010 · Sign in with Apple — **P2 · DOCUMENTED, NOT BUILDING**

*2026-08-27.* Email-OTP-only keeps SIWA optional under App Store 4.8 (D98 guardrail; `spec/native-arc.md`); adding any third-party login makes SIWA mandatory. The phone keeps the reviewer door (`reviewer@cupseason.app` password path) for App Review. Reconsider only if a social login is ever added.

## IOS-011 · Navigation contract — **P1 · PROCEEDING**

*2026-08-27.* Four tabs Home · Clubhouse · ⊕ · You (D82 canon, unchanged); `NavigationStack` per tab; ⊕ is a full-screen cover opening **on the post form**; objects pushed, actions as sheets with detents; the Board is its own screen. Rejected: a fifth tab; the ⊕ as a menu page; the six-segment Clubhouse control. Details IOS-002 §2.

**CORRECTED 2026-09-05 (D227, wave 2).** "⊕ opens **on the post form**" has been stale since D110's addendum (2026-09-01) put the three-door cover back on the ⊕, and the two lines sat in the canon contradicting each other for four days. The ⊕ opens the **cover**, with live first and in ember (L-40); the composer is reached by a **long-press** on the ⊕, by Home's first foot door, and by every explicit "Add a round" CTA; and with a round live the ⊕ opens the **round**. The four tabs become five in wave 3 under D222/IOS-028, which is the other half of this entry that no longer holds.

## IOS-012 · Home slot order on the phone — **P1 · PROCEEDING**

*2026-08-27.* Hero first; the "make something" doors leave the lane for a `+` in the toolbar (reverses D94's placement for the phone only; D94's own tradeoff line predicted this). Web Home is untouched.

## IOS-013 · `dim` text contrast — **P2 · PROCEEDING**

*2026-08-27.* Text the web sets in `dim` (≈3.1:1, fails AA) renders in `mut` on iOS; `dim` stays for hairlines and dots. No token change; preflight check 10 untouched. Owner may instead lift the token itself (affects web) — flagged in IOS-003 §2.2.

## IOS-014 · iPad — **P1 · PROCEEDING UNLESS OVERRULED**

*2026-08-27.* Out of scope for v1 (`supportsTablet: false`); the desktop surface is the big-screen product (D98).

## IOS-015 · Founder/QA surfaces on the phone — **P2 · PROCEEDING**

*2026-08-27.* Phone keeps: feedback, report, mute, delete account, founder **field note**. Phone drops: founder dashboard, report resolution, sandbox, `?debug`/`?exit`/`?forge` (a build-flagged Debug menu replaces them). `test-seed` is not founder-gated (audit 06 §9.9) — fix before a public build (folded into IOS-009 batch 2).

## IOS-016 · "Best" means the lowest differential — **P2 · PROCEEDING**

*2026-08-27.* The web says "best" for max PvI on You and min differential on the Tour Card. iOS uses one definition (lowest differential, matching `tour_card`) and labels PvI figures as "vs your number".

*2026-09-01, CONFLICT (named) — brand canon §3 forbids "PvI"/"differential" on any user surface, and this entry used the word as the definition with no CONFLICT line.* The definition stands as an engine fact; the word never appears on a surface — D210. On screen the figure is *"vs your playing number"* (D209: every You figure uses the allowance PvI from `v_rounds_ranked.pvi`), a bare float is always labelled, and `RoundCopy` reads its band edges from `CSBands`.

## IOS-017 · The Expo B1 scaffold — **P2 · PROCEEDING**

*2026-08-27.* Commit the scaffold on `native/b1-scaffold` as the record of B1 (it passes preflight 14/14) regardless of IOS-006; if IOS-006 = SwiftUI, retire `apps/mobile/` in M0 when its replacement boots. `ios-wrapper/` (Capacitor, dead per D98) is removed in M0. **Status:** the replacement boots (`apps/ios`, 2026-08-27); removal of `apps/mobile/` and `ios-wrapper/` waits on the owner's commit so the record exists first.

---

## Open questions surfaced by the audit (not yet decisions)

Owner-facing questions that fell out of the nine slices, collected so nothing is lost. Each becomes an `IOS-0xx` when it is picked up.

- `transfer_pro` never updates `leagues.commissioner_id`; `delete_account` and `leagues_read` key on it — the original creator can never leave, the new Pro can leave and strand the league (audit 02 §7.2). Server fix; decide which column is "the Pro".
- Two lifecycle columns with no lockstep: a league stuck in `draft` still gets month closes, a Cup Final and a crown (audit 02 §7.3). Gate the engine on `phase`, or make `start_season` the thing that activates the season?
- No server event at first tee (`kicked_off` is dead, audit 02 §7.4) — restore the post + push in the tick, or local notification only? **Closed by D204 (2026-09-01):** the tick's kickoff branch is restored (it was deleted by the `20260716170000` rewrite, not by a ruling); "The season is live. Week 1 — counting rounds start now." rides the existing `season` push kind, and `lock_league` posts a line in every structure.
- Solo leagues are offered floors that never fire (audit 02 §7.12) — assess them or hide the dial? **D205 (2026-09-01) makes the copy structure-aware** — the guide's floor paragraph stops promising a penalty that cannot happen, and the D204 horn appends the floor sentence only for squads; **the floor itself stays open (D140)** — a solo floor tracks a habit and never assesses.
- Late joiners score retroactively and `join_league` has no phase gate (audit 02 §7.5) — keep, or build §14.1 proration?
- Weekly clash spotlight (D52) — build it, or leave the phone without a season-side weekly "vs"? (audit 04 Q2)
- `played_on` for live finishes is stamped in UTC (`current_date`) — Saturday-evening rounds can land on Sunday and miss a Ryder/Major window (audit 04 §7.4). Server fix.
- Clinch orphans later Ryder sessions (audit 04 §7.1) — is "remaining duels resolve for the record" still wanted?
- Ryder buy-in/settlement thin version (§R12.4) — build or keep the Ryder $0?
- Pot arithmetic: `stake × roster` (what is owed, incl. unpaid and tombstoned) vs `sum(paid)` — needs a decision-log line before it goes into a store listing (audit 06 §9.3/§9.7).
- Points override tool with mandatory reason (spec §9 promise) vs revoking `adj_write` (audit 06 §9.10).
- Server-side seen cursor (cross-device unread) vs per-device (audit 05 Q8).
- Band edge at PvI = −1.0: server 6, client 7 (audit 03 §5.2) — pick the server's rule.
- `set_profile` cannot clear city/home course (audit 01 Q2) — bug or intended.
- Guest identity on native: App Clip for the account-less pencil (IOS-004 #12) — scope.

---

## IOS-018 · Full web parity on the phone first; the web is rethought after — **P0 · DECIDED 2026-08-27 (owner)**

**Decision.** Supersedes the scope half of IOS-007. The phone builds *everything the web has* — including the wizard's dials and the lock, Pro-assign and the draft board, ledger and pot in full, the founder desk, feedback, reports, the sandbox controls — walked row by row from the IOS-001 parity matrix, in the web's own copy and behaviour. Only after parity is real does the ecosystem question get asked: what the web should be as a standalone product next to the phone (a later decision, not this one).

**Context.** The owner's read of the M0 build: "still like 25% of what it should be." The audit's opinionated "do differently" items (IOS-004) remain valid but are *sequenced after* parity — a native improvement is measured against a working port, not a plan.

**Consequences.** IOS-005's milestones are re-cut as parity waves (see the roadmap amendment); D99's "operating vs authoring" split no longer bounds the phone; `spec/native-arc.md`'s "Owns" table is historical. Nothing in D98/D99 about the backend, the guardrails (no purchase UI, email-OTP-only, immutable rounds, D37 grants) changes.

**Reversibility.** EASY — screens are additive.

**Action.** Proceeding. Logged upstream as D100.

## IOS-019 · The visual pass — depth from ground, not from borders — **P2 · PROCEEDING 2026-08-27**

**Decision.** The parity build (waves 1–6) transplanted the web's card grammar one-to-one and the phone reads flat: every surface is a `bg1` box with a 1px `line`, the hero and a footnote carry the same weight, and iOS 26's glass toolbar clips the wordmark into a "— C" pill. The pass keeps the identity contract (IOS-003 §1: palette, the two metals, three type voices, the spine, the roll) and changes **how surfaces sit**, on four rules:

1. **One hero, one lane, per screen** (IOS-003 §4 "no dashboard grid"). The hero gets **the wash** — a radial of its spine colour at ≤14% opacity from the top-leading corner (ember = live, gold = earned, `pos` = on the tee, dusk = ceremony) — and nothing else on the screen gets one. The Clubhouse's 2×2 stat grid becomes one season strip inside the hero.
2. **Borders retreat, hierarchy comes from ground.** Cards keep `bg1`; the 1px `line` stays on interactive cards only; rows inside a section separate with a hairline, never nest cards in cards. Section heads are the eyebrow plus a hairline rule.
3. **The page header lives in the scroll, not the toolbar.** Home opens on the gradient tick + the serif wordmark + a mono date eyebrow ("THU · AUG 27"); the toolbar keeps only the `+`. Clubhouse and You get the same header shape with the league name / "You".
4. **Panes are a tab strip, not pills.** Mono uppercase labels with an ember underline that slides on the roll; the pills read as filter chips and they are navigation.

The ⊕'s composer is its own decision (IOS-020). Ceremonies, the finish, the settlement and share cards are untouched (they already sit on dusk).

**Context.** Owner, 2026-08-27, on the wave 1–6 build: "improve upon the visuals." Screens read from the simulator signed in to the owner's account.

**Options.** (a) keep the web grammar verbatim — rejected, the phone must not inherit "boxes all the way down" (IOS-004 §"web behaviours the phone must not inherit"); (b) a full re-skin with new colours — rejected, it stops being Cup Season and preflight 15 (palette purity) forbids invented hex; (c) **the four rules above** — chosen: every colour still comes from tokens.json; the wash is a token colour at an opacity.

**Reversibility.** EASY — layout and modifiers, no data, no copy.

**Action.** Proceeding. `CSDesign/Surfaces.swift` adds `CSWash`, `CSPageHeader`, `CSSectionHead`, `CSTabStrip`, `CSHairline`; the screens adopt them.

## IOS-020 · The card — the composer as a scorecard, not a form — **P2 · PROCEEDING 2026-08-27**

**Decision.** "Post a round" keeps every mechanic and every line of web copy (D32 two boxes default, D34 grid opt-in, the even-par guard, `touched`, half-value nines, rating/slope always editable, course memory, scan with the exact soft-failure paths, draft autosave, ceremony → epilogue → partners) and changes its **shape**:

- **The gross is the hero and it is live.** The serif figure (Charter, 64) sits at the top on a dusk wash with the band phrase under it ("beat your number by 2") and a points chip through the open league's lens; it updates as you type. "How this round scores" stops trailing the form (IOS-004 §2).
- **Where** — remembered courses are rows (course · tee · rating/slope), not clipped chips; search sits above them; rating/slope collapse to one mono line ("72.1 / 128") that expands to two fields on tap.
- **Your card** — the 18/9 seg; front and back as two large mono figures with the sum between them; "Enter your card" opens **the scorecard strip**: a real card — Out and In rows of nine cells (hole, par small, score large, coloured by result), tap a cell to select, one big − / + stepper under the strip that advances to the next hole. Eighteen tiny ± pairs go away.
- **Details** — date, photo, scan as pills in one row; the photo preview under them.
- **The bottom bar** — "Post round" pinned in a safe-area inset with the gross line above it, so the tap is always one thumb away on a long card.
- **Point bands** — a "How points work" disclosure, not a table on every open.

**Context.** Owner, 2026-08-27: "create the post score section." The wave-3 composer is a faithful port of `#view-post` and reads as a long web form on the phone (simulator screenshot: memory chips overflow and clip, the preview is below the fold).

**Reversibility.** EASY — same model (`PostRoundModel`, `PostCard`), same RPCs; the view is replaced.

**Action.** Proceeding in `Post/`. The scan confirm surface (the D34 grid's job) is the same strip.

## IOS-021 · Pay-to-play on the phone — the visible model ships, checkout stays on the web — **P1 · RECOMMENDED 2026-08-27**

**Decision (recommended).** Integrate D56 exactly as decided — **visible model, no checkout** — on the phone now: `app_flags.pricing` (the kill switch + bands), the wizard's pot-step pass card, the You-tab membership card (Founding / free season / paid states), the League Room pot pane's Pro card, and the store metadata line "there are no in-app purchases." Close the two open items with a recommendation: **$79 anchor, banded flat $49 / $79 / $99 at ≤9 / 10–13 / 14+, fixed at roster lock; Founding cap 10, numbered.** Checkout, when it opens at the first season-2 "run it back," is **Stripe on cupseason.app** with the phone showing pass status and (while US rules allow) a "Renew at cupseason.app" link — never IAP-first (discovery §6.3).

**Why P1 not P2.** The price point was left to the focus-group script (D56); shipping a number on the phone closes it by default. The owner should confirm the $79 anchor or say "range" (the deck's fallback framing) before the surfaces go live — the flag makes either a one-row change, not a rebuild.

**Context.** Owner, 2026-08-27: "finalize pay to play plan and integrate." No pricing surface was ever built on the web (grep: 0 hits; no `pricing` flag in prod as of today).

**Options.** (a) IAP now — rejected: 15–30% on a $79 desk purchase, App Review exposure, and D56 said checkout waits for season 2; (b) Stripe checkout on the phone now — rejected: same timing argument, plus link-out rules are before SCOTUS; (c) **visible model now, Stripe on the web at season 2** — chosen.

**Reversibility.** EASY — one flag write hides every surface.

**Action.** Plan + surfaces built behind `pricing.visible` (seeded `false` in the migration so nothing shows until the owner flips it). Owner decides: the anchor, and the flip.

**Amended 2026-08-27 (evening) — owner: "build the annual recommendation; look at competitors and reduce 25%."** The unit is now the **league-year** (one pass, every season the league runs in twelve months; first year free), and the bands are set 25% under the per-league comps — Golf League Tracker $119/season (≤365 days), Fantrax premium league $130, MyFantasyLeague ~$110, LeagueLobster $228/yr, League Golfer $10–20/player/yr — giving **$59 ≤9 · $89 10–13 · $109 14+ a year** (≈ $6.50–$7.80 a player a year at every banded roster). Logged upstream as **D101**. The anchor question is closed; the flip stays the owner's.

## IOS-022 · The polish list — **P2 · PROCEEDING 2026-08-27 (owner: "follow your rec for all")**

**Decision.** From the professional review: (1) the empty navigation bar above Home's wordmark goes — `+` moves into the header row; (2) the You hero's marker watermark drops to 10% or gains a label; (3) the ⊕ presents with a rise on the roll and a haptic; (4) the composer's rating/slope row expands only on tap (a picked tee fills it); (5) copy density — every fine-print line appears once per screen; (6) a haptics vocabulary via `sensoryFeedback` on post, tee-off, lead change; (7) the Major's door is hidden behind a flag for v1 (the code stays); (8) the Founder's Desk, feedback ledger and QA surfaces leave the You tab and live behind a long-press on the build line in Settings; (9) a Dynamic Type / VoiceOver pass on the season strip, the scorecard strip and the tab strip; (10) a real light-theme pass. **Reversibility.** EASY. **Action.** Proceeding.

## IOS-023 · Sign in with Apple beside the code — **P1 · DECIDED 2026-08-27 (owner)**

**Decision.** Supersedes the email-OTP-only guardrail (IOS-018's list) for the door only: Apple sign-in is offered as a second door, code-only email stays the first. Client: `SupabaseService.signInWithApple(idToken:nonce:)` through the service (preflight 16 still forbids direct auth calls and any redirect URL); the button renders only when `app_flags.ios.apple_sign_in` is true, so the owner enables it after the Apple portal + Supabase provider are configured. **Why P1.** Apple requires it if any other third-party login ever ships, and the OTP delay is a measured drop-off. **Owner owes:** the Sign in with Apple capability on the App ID, the Services ID/key in Supabase Auth → Providers → Apple, then the flag. **Reversibility.** EASY (flag).

## IOS-024 · Reliability floor — MetricKit, five events, `test-seed` gated — **P2 · PROCEEDING 2026-08-27**

**Decision.** (1) MetricKit crash/hang diagnostics land in `client_events` (fire-and-forget, 4-frame stack kept — the ghost lesson); (2) five product events: `signed_in`, `card_set`, `league_created`, `league_locked`, `round_posted`; (3) `test-seed` requires the caller to be the founder (`profiles.is_founder`) — SEC-H2 closes. **Reversibility.** EASY.

## IOS-025 · Looks on the phone — the resolver, the two pickers, the surfaces — **P2 · PROCEEDING 2026-08-27 (D103a)**

**Decision.** `CSLooks` (generated) + a `LookResolver` in the Kit: `resolve(date:, leaguePhase:, leagueLook:, personal:)` with the D103a precedence. Personal dial in Settings → Appearance ("Palette": Follow the calendar · Fescue only · each look, with its dates). The Pro's dial on the League pane ("Dress the room": Follow the calendar · each look) writing `set_league_look()`; `league_looks()` read once per session. Surfaces: the Home hero and the Clubhouse hero take the resolved look's accent as spine + wash; the ⊕ halo tints; the occasion card and empty states take the motif and eyebrow. Ceremonies, the pot, share cards, the founding tag ignore looks. **Reversibility.** EASY — `none` on both dials is homebase. **Action.** Proceeding.

## IOS-026 · Wave 7 — push that means something — **P2 · PROCEEDING 2026-08-27 (D104)**

**Decision.** The contract is `docs/ios/push-contract.md` (payload keys, categories, badge rule, deep-link table). Backend: the `push` function sends routed APNs payloads with categories and a per-recipient badge, filters mutes and the Pro's `notify_system` curation, and `invite_golfer` fans into `push_nudges`. Phone: `UNNotificationCategory` registration with the three actions, a `PushRouter` (cold start + foreground) landing on the right screen through the `Presenter`, the contextual permission ask with a pre-permission explainer, badge discipline, and the local Ryder reminder. **Owner:** the APNs key (.p8) → `supabase secrets set APNS_P8 APNS_KEY_ID APNS_TEAM_ID` (+ `APNS_SANDBOX=1` only while a tethered dev build is under test — runbook D5), `supabase functions deploy push`, `supabase db push`. **Reversibility.** EASY.

## IOS-027 · TestFlight prep — the four blockers closed, the manifest, the pipeline — **P1 · RECOMMENDED 2026-08-28 (owner: "let's get started on the next item")**

**Decisions (recommended, from the runbook's D1/D3/D4/D6/D9).** D1 iPhone-only — already the build setting (IOS-014). D3 pre-empt the pot — the paragraph is in `docs/ios/app-review-notes.md`. D4 the reviewer lands in a seeded sandbox — `test-seed` gains `target_email` so the founder seeds the reviewer account (`{"action":"seed","target_email":"reviewer@cupseason.app"}`), and the walkthrough is in the notes. D6 money on screen — status only, no link-out; `pricing.visible` stays false for the review build. D9 privacy — `CupSeason/PrivacyInfo.xcprivacy` (no tracking; contact, user content, device id, crash and usage data linked to identity; required-reason APIs: UserDefaults, file timestamps, boot time) and the matching nutrition-label answers.

**Pipeline.** `tools/ios-archive.sh [--upload]` — archive (Release, build number = commit count), export with `apps/ios/ExportOptions.plist` (app-store-connect, automatic signing, team `3F7BK4WVH8`), upload with `altool` and an App Store Connect API key from the environment. Screenshots: the 6.9" set captured from the simulator via the dev hatches.

**Owner owes.** App Store Connect: the app record (name, bundle id `app.cupseason.ios`, SKU) and an API key (Users and Access → Integrations, role App Manager; `ASC_KEY_ID`, `ASC_ISSUER_ID`, the `.p8` under `~/.appstoreconnect/private_keys/`). `supabase functions deploy test-seed` (the target_email change). Unset `APNS_SANDBOX` before the first upload. Then `tools/ios-archive.sh --upload`, internal testing to PIGL.

**Reversibility.** EASY. **Why P1.** The four are the owner's calls; each is closed here with a recommendation and nothing ships until they run the upload.

## IOS-029a · The ME strip, on today's Home — **P1 · PROCEEDING 2026-09-05 (D226, D236, D246; owner-authorised with the UX overhaul)**

**Authorised 2026-09-05** with the UX overhaul (`docs/ux-overhaul-2026-09-04/OWNER_RULINGS.md`). **This is the FIRST HALF of IOS-029, split deliberately**, and the split is the mitigation rather than a scheduling convenience: the ranked list and the retirement of the three producers it replaces must not ship in one push. **029a (this entry, BUILT 2026-09-05, wave 1a)** delivers R3's additive payload keys, the **ME strip**, and the **four foot doors** — all mounted **on today's `HomeMode` Home, which is still there**, so there is no window in which Home has no defined renderer. **029b** adds R1/R2, the ranked list, `HomeFallbackItems`, the dispatch fixture and the decode suite, **and only then** retires `HomeLead`, `HomeHeroCopy`, `HomeMode` and `HomeLeagueRow`.

**Decision.** `MeStripCopy` (`…/CupSeasonKit/Home/MeStripCopy.swift`) is the producer: four facts in a fixed order (`YOUR NUMBER · LAST · NEXT · STILL OWE`), four doors, one season context row, one honest empty state per slot, and a `suppress: Set<Fact>` the strip publishes so **L-34 is a producer rule rather than a per-screen judgement**. `MeStrip.swift` renders it as a **line of type on the page's own ground** — no `bg1`, no border, no radius, no `CSStat`, which is the tile the AP-3 anti-pattern names; a tile has a box, this has a baseline. It is mounted above `HomeHero`, and `HomeFootDoors` puts **ADD MY ROUND · START SOMETHING · JOIN WITH A CODE · FIND GOLFERS** at the foot of every Home in every state.

**What the suppress set actually turns off, named so it is not mistaken for dead code.** `HomeHero` drops D129's owe line when the strip carries `.myMoney` (both fire on the same predicate — `buy_in.paid == false` at a live stake outside `setup` — so the two can never both be right and both be shown), and `UpNext.chips` drops the "Next round" chip when the strip carries `.myNextRound`. On the web the same set stands down the D94 Next tile **and** the Up-Next chip, which together made three renders of one plan on one screen.

**Three engineering facts, not aspirations.** (i) **Every new payload key is optional and every absent key renders NOTHING** — `NativeHomeV3DecodeTests` asserts the payload decodes with and without all of them, in both deploy orders, and `MeStripTests` asserts that nine posted rounds with no `last_round_on` produce **no LAST slot at all** rather than "NO ROUNDS YET" (a lie) or "—" (a guess). (ii) **The `STARTER` label is reserved and not issued**: nothing writes `index_source = 'starter'` until D247's onboarding ships, and per §4 of the build plan it ships in its declined form — client-side only, never reaching the engine. The label exists so that the day a starter figure appears it is never dressed as an established index (L-14). (iii) **`CSTelemetry.Metric.homeStateSeen` and `.ctaTapped` get their first callers here**, so the state-matrix funnel is measurable the day the strip ships; `platform` is stamped centrally, so the rows split by client without anyone typing it.

**Acceptance.** The AX3 pass on the strip at the **rank-≥3 season row**, which is the longest string the strip can produce (`DESERT DOGS · 3RD OF 8 · TOMMY LEADS BY 12 · 4 BACK OF DRE`, with the endgame clause dropped rather than wrapped) — pinned as a test, not left to a screenshot. Plus the $0 case (no pot surface anywhere) and the self-only case (the payload carries `paid_count: 6` of `players: 8` and the strip says nothing about the other two).

**Reversibility.** MEDIUM — additive, on today's Home, and every new payload key is optional in both directions. **Owner owes:** `supabase db push` for `20260906090000_the_strip_has_its_own_facts.sql`. Until it lands both clients run the declared fallbacks: no LAST slot, no named gap, and the local week formula.

## IOS-034 · The glanceable surface — two home-screen widgets off an App Group snapshot — **P2 · PROCEEDING 2026-09-05 (D234, D252; owner-authorised with the UX overhaul)**

**Authorised 2026-09-05** with the UX overhaul (`docs/ux-overhaul-2026-09-04/OWNER_RULINGS.md`). **Steps 2, 3 and the wiring landed 2026-09-05 in wave 0** — `com.apple.security.application-groups` on `CupSeason.entitlements`, a new `CupSeasonWidgets.entitlements`, and both declared in `project.yml` so XcodeGen sets `CODE_SIGN_ENTITLEMENTS` for the extension. **Steps 1 and 4 are the owner’s (P-12) and are named in wave 0’s commit:** register `group.app.cupseason.shared` in the developer portal, then regenerate **both** provisioning profiles — TestFlight 669’s will not cover it. The widget itself, and `DispatchSnapshot.swift` compiled into both targets, stay in the last wave; nothing reads or writes the shared container yet, so the entitlement is inert until it does.

**Decision.** The only widget today is the live round's Live Activity (D155), so the product has no out-of-app surface between rounds — the cheapest NOW surface it could have, and no Phase-2 proposal designed it. Two static-timeline widgets: **small** (the season row — `FELLAS · WEEK 7 / 2ND OF 8 · 4 BACK` plus the lead's one line) and **medium** (the ME strip's four facts plus the lead's headline). **The extension keeps no network client** — `CupSeasonWidgets` depends on CSDesign only, deliberately (`CupSeasonWidgets.swift:10-12`), and that stays true: the **app** writes a small `DispatchSnapshot` (the ME facts plus the lead's dateline, headline and route) into the shared App Group container on every successful `home_dispatch` load, and the widget's timeline reads it. **It never lies about time** (L-44): it stamps its own `AS OF`, and past 24 hours it reads `AS OF SAT · OPEN TO REFRESH` and **drops the lead's verb** rather than offering a stale action — the same honesty rule as the Live Activity going stale at 45 minutes. Tap → `cupseason://home`, or the lead's own route when the snapshot carries one. **Money never appears on it** — the owe line is self-only and a lock screen is not private (L-10). **The App Group does not exist, and creating one is owner work that gates the wave rather than a polish item.** `apps/ios/CupSeason/CupSeason.entitlements` carries only `aps-environment`, `applesignin` and `applinks:cupseason.app`, and the `CupSeasonWidgets` target (`project.yml:117-140`) has **no entitlements file at all** — only an `Info.plist`. Five steps, named so none is discovered late: **(1)** register the App Group id **`group.app.cupseason.shared`** in the developer portal; **(2)** add `com.apple.security.application-groups` to `CupSeason.entitlements`; **(3)** create `CupSeasonWidgets.entitlements` and wire it through `CODE_SIGN_ENTITLEMENTS` in `project.yml`; **(4)** **regenerate both provisioning profiles** — TestFlight 669's will not cover it; **(5)** compile `DispatchSnapshot.swift` into both targets the way `CSRoundActivity.swift` already is (`project.yml:122`). **Steps 1 and 4 are P-12 owner work on the owner's machine, so they move to Wave 0 beside the APNs gate they resemble**; the widget itself stays in the last wave. **Reversibility.** EASY once the group exists (a target, no server work).


## IOS-035 · The App Store listing is the first screen — **P1 · PROCEEDING 2026-09-05 (owner-authorised with the UX overhaul)**

**Authorised 2026-09-05** with the UX overhaul (`docs/ux-overhaul-2026-09-04/OWNER_RULINGS.md`). **BUILT 2026-09-05, wave 1a** — `docs/ios/app-store-listing.md` is rewritten (subtitle, promotional text, description, keywords, What's New) and `apps/ios/Screenshots/SHOT-LIST.md` carries the reordered shot list. **The owner owes the metadata edit and the new captures at the next submission**; nothing in this entry ships without them, which is why it sits in wave 1 rather than at the end.

**Decision.** The listing is the true first screen of the brief's "first seconds" and it currently sells the engine: subtitle "Run your golf season"; promo "Season three of the founding league is under way"; description "Retire the spreadsheet. Run your golf season in your pocket. … **Captains draft squads** … The Pro sets the bylaws once — squads or solo, the handicap allowance, how many rounds count a month, the endgame — and locks them at first tee". A stranger meets league, season, commissioner, bylaw, allowance and endgame before they meet golf. Rewritten: subtitle **"Golf with your friends"** (22 of Apple's 30 characters); first line **"Cup Season turns the golf you already play into a season with your friends."**; second paragraph the product paragraph (`INFORMATION_ARCHITECTURE.md` §1); promo text stops naming a founding league nobody outside it can join; **"Captains draft squads" is deleted** — all 13 prod leagues draw at random and no captain drafts anything, the same defect the 2026-08-29 audit filed as TM-22, and a store listing that describes an unbuilt mechanic is the listing lying. **The shot list is reordered**: the composer first, Home second, the season's story third, the head-to-head fourth, the ceremony fifth — today it leads with Standings and the wizard and the composer is shot 3 (`spec/appstore-launch-kit.md:104-113`).

**What was NOT touched, and why.** §5 (URLs, copyright), §6 (age rating — rewritten 2026-09-01 against Apple's current questionnaire), §7 (the privacy label, which must stay byte-aligned with `PrivacyInfo.xcprivacy` and `legal.html`) and §9 (the paste order). The posture rule holds over every changed field: store copy is legal copy, the D39 canon appears verbatim, and bet / betting / gambling / wager / odds / action / units / "takes no cut" / "never held" appear nowhere, keywords included. **One flag for the next submission:** shots 3 and 4 of the new list — the season's story and the head-to-head — are surfaces waves 4 and 5 build. Until they exist the list is the order to shoot in, not a set of files, and a submission before then uses shots 1, 2 and 5 plus the survivors of the old list.

**Reversibility.** EASY (App Store Connect; the subtitle and promo text are editable without review). **Owner owes:** the metadata edit and new screenshots at the next submission.


## IOS-029b · The desk — Home is one read, one ranked dispatch, and only then do the old producers retire — **P1 · PROCEEDING 2026-09-05 (D228, D229, D231, D236; owner-authorised with the UX overhaul)**

**Authorised 2026-09-05** with the UX overhaul (`docs/ux-overhaul-2026-09-04/OWNER_RULINGS.md`); **R-C, as revised**, makes it a two-client entry and **R-H** decides what the ranker does on a quiet day. **BUILT 2026-09-05, wave 1b.** **Owner owes:** `supabase db push` for `20260908090000_home_is_one_read.sql` (**R1** `home_dispatch`, **R2** `home_stories`). Until it lands, both clients run the declared fallback and Home is a shorter, honest screen rather than a broken one.

**Decision.** `HomeView` is rebuilt as **masthead → the lead → the ME strip → ≤4 ranked items → the wire → the four doors**. **One read:** `home_dispatch(p_days default 21)` returns `{me{…}, items[…], generated_at}`, so the ME facts and the ranked list arrive together and nothing is composed twice on an open — Home makes one call where it made two. The `me` it returns is `native_home()`'s own payload and Home draws the strip from **it**, not from the session's cached copy, so the strip and the items describe one instant. It is deliberately NOT written back into `SessionStore`: `generated_at` is `now()`, it is what `.task(id:)` watches, and adopting it on every load would reload Home for ever. The session's own payload is refreshed by the pull, as it was. Every item carries **`tier`, `rank`, `rank_reason`, `route` and `suppress`**; the lead's `suppress` set is unioned with the ME strip's, never replaced, so D129's owe line and D219's next-round chip stay down where wave 1a put them. **What retires, and only after the harness is green:** `HomeMode` and `HomeMode.pool` (`Models.swift`), `HomeLead` and its five-rung ladder, `HomeHeroCopy`, `HomeLeagueRow` and the D121 compact rows, and the `+` menu that hid three of the four doors. **What survives:** `HomeStream`, `HomeDigest`, `HomeFeedFold`, `HomeSocial` and `Occasion` — the wire is unchanged, D217's fold and D218's head are kept, and D252's Major gate is carried across intact (`HomeModel.majorOpen` is still cached for the model's life, so the four gated cards light the day wave 3 flips the flag).

**The rulings the retiring producers carried are relocated, not deleted.** `HomeHeroCopy`'s and `HomeLeagueRow`'s pure functions move into **`SeasonFacts`** (`CupSeasonKit/League/SeasonFacts.swift`), which is where the season page will read them in wave 4 — D129's owe matrix, D106/D70's pot lines, D140's cap foot, D14's floor sentence verbatim, D138's finalist seed and its Cup-Final clock (§14.3), D207's n=2 sentences, D126's endgame foot and D26/D130's "back of"/"clear of". **`HomeCopyContractTests.swift` carries the assertions off ruling by ruling** and is the reason the deletion is allowed to happen: a ruling with no assertion in that file has not been migrated.

**Three engineering gates, not aspirations.** (i) **The declared fallback is a producer, not "as today"** — this entry deletes the three producers that *are* today, so `HomeFallbackItems` composes items from `native_home` + `home_feed` + `my_invites` + the inlined clash, in the static order **CLOSING → CHANGED → COMING → CIRCLE**, **with no lead card**, and the ME strip is drawn by `MeStripCopy` exactly as it is on the served path. **Preflight check 23 fails the push if `HomeFallbackItems` is absent from the Home target.** (ii) **Every new SQL argument is defaulted** and every payload key is optional in both directions; `HomeDispatchDecodeTests` drives a checked-in fixture per state and a payload with every optional key missing. (iii) **`-cs_dev_dispatch`** (DEBUG only, the `MainTabView.swift:9-12` hatch pattern) dumps tier, rank and `rank_reason` per item, so the ranking can be argued with on a real account rather than in a table.

**The gates are re-applied on the client, deliberately.** `HomeRank.arrange` re-runs G1 (the fence — an item with no door does not render), G2 (the veto — the lead must have a human subject) and G5 (the cap — one lead and four items) over whatever the server sends. It is the same rule twice on purpose: a ranker bug then produces a shorter screen instead of a standing in the lead slot, and the veto becomes a unit test rather than a SQL assertion.

**Acceptance.** The week-in-the-life on a seeded season and on a leagueless seed; the **multi-league walk on the App Review seed**, which is where `native_home`'s per-membership loop at four seasons is finally timed; and the AX3 pass on the lead card beside the strip's rank-≥3 season row. **The first two are release gates on the owner's own machine and are named in the commit, not claimed here.**

**Reversibility.** MEDIUM — the fallback ships with it, and every retired producer's rulings are asserted in `HomeCopyContractTests` before the file is deleted.


## IOS-030 · The composer is one box, and the direct write becomes an RPC — **P1 · PROCEEDING 2026-09-05 (D227, D229, D239, and A's B-13; owner-authorised with the UX overhaul)**

**Authorised 2026-09-05** with the UX overhaul (`docs/ux-overhaul-2026-09-04/OWNER_RULINGS.md`); **R-B** settles the ⊕ (live keeps the lead, the 90 % case is served by the long-press) and **R-C, as revised**, makes it a two-client entry — the phone gets one box above a fold, the desk gets the same box in a reading column with the points rail beside it. **BUILT 2026-09-05, wave 2.** **Owner owes:** `supabase db push` for `20260910090000_a_round_is_posted_by_the_server.sql` (**R11** `post_round`, **R7** `round_epilogue` extended) and `20260910093000_who_was_out_there.sql` (**C-2** `round_players`). Until they land, both clients post through the **declared fallback** — the direct insert, with the season the client can see — and the epilogue is the one it always was.

**Decision.** The ⊕ cover keeps **Score it live** first and in ember (L-40), tightened to three rows with a one-line gloss each — the ~1,000 px void closes. **Long-press on the ⊕ opens the composer** with the gross focused; every explicit "Add a round" CTA still jumps past the cover (D110's addendum, already built); with a live round open the ⊕ **opens the round**, agreeing with `LiveNowBar` (`:134`). The composer becomes one box over an inherited, editable line (course · tee · rating/slope · date), with an optional "Who was out there" row (D239). **`PostService`'s raw `db.from("rounds").insert(…)` becomes `post_round()`** — the one consequential direct write left on the phone, and L-03 says a direct write is a defect awaiting an RPC. **Three clauses make that buildable rather than aspirational.** (i) **Its signature is the real payload**: the insert it replaces writes eleven columns (`PostCard.swift:280-307`) — gross, rating, nine_rating, slope, holes_played, source, played_on, course_label, api_course_id, season_id, photo_path. Without rating/slope there is no differential; without holes_played/nine_rating the D72 nine-versus-eighteen branch is gone. `p_tee_id` is **not** in the signature — it has no counterpart in `PostPayload` or in the insert. **As built the RPC takes ten arguments and the eleventh column, `season_id`, is the one the server derives** — the payload is whole, and the only column the client stopped sending is the one it should never have chosen. (ii) **`season_id` is derived server-side**, from `p_played_on` against every membership's window at that membership's own allowance (D123/L-13) — which is what replaces the `preferredLeague` coupling D229 deletes, and which also fixes the multi-league case one `season_id` cannot express. **What that stamp is, precisely, because it matters:** `v_rounds_ranked` never read `rounds.season_id` and never will — it fans every round into every membership whose window holds it, at that league's own allowance, which is why a golfer in two seasons already scored in both. The stamp decides which season the receipt and the epilogue *talk about*. Both call sites D229 named are closed: the composer's allowance preview now runs through `PostSeasonRule.membership(playedOn:memberships:)`, the same date rule the server uses, and the ceremony's league name and squad come back **with the round**. (iii) **`PostService.insert` is kept as the declared fallback** and fires on a function-missing error (PGRST202), with the epilogue then fetched via the existing `round_epilogue(p_round)`: CLAUDE.md's skew rule covers new **args** and **columns**, not a function that does not exist yet, and this is the only post path in the rebuilt composer. `PostService.fallbackFires(on:)` is that condition as a value a test can hold — a refused rating or a dead network is a real error and reaches the golfer, never a quiet second attempt down the old path. The two skew retries (`PostService.swift:70-80` — drop `api_course_id`, then `photo_path`) move **inside** the RPC, which nulls an unknown course id or a photo path that is not the caller's own and posts anyway; neither failure mode may cost a golfer their score. Three defects on the same path are fixed: the rating guard (a blank rating never posts 0 — `PostCalc.blocked` names the missing field and `preview` stops scoring a card against a rating of zero, which is the phone twin of the sentence the web was given in wave 0), the placeholders that read as values (`PostRoundScreen.swift:194-195` → `—`, now in the inherited line), and the hand-typed course, which posts and carries its label. The epilogue becomes a page with **one ranked next act** — `PostNextAct`, eight rungs, eight distinct sentences, one door each, with the share artifacts demoted below it — and **honest movement**: `EpilogueMovement` renders a sentence only when **R7**'s `rank_before`/`rank_after` are both there and the round actually climbed (L-44; A-4, since `prev_rank` is a Sunday figure and could never answer what a Tuesday round did).

**What the one box did NOT delete (P-6).** The 18/9 segment, front and back, the pars sheet, the scorecard scan, the photo, the date sheet and "How points work" are all still there, complete, behind the inherited line's `edit`. `PostCard.entry` is the one place that decides what a card with one number in it means — the box wins while the card is closed and on eighteen, the nines answer otherwise — so the preview, the payload and the receipt cannot disagree, and D72's rule (one nine filled is a nine; a real 9-hole tee sends its own rating) is unchanged.

**Reversibility.** EASY on the cover and the composer; MEDIUM on `post_round` (it ships with the client that calls it, and the client falls back to the insert until it is pushed).


## IOS-028 · The five slots on the phone — the bar, the routes, the Presenter, the push table — **P1 · PROCEEDING 2026-09-05 (D222, D223, D229, D230; owner-authorised with the UX overhaul)**

**Authorised 2026-09-05** with the UX overhaul (`docs/ux-overhaul-2026-09-04/OWNER_RULINGS.md`); **R-A** settles the fifth slot and **R-D** the two names, **Compete** and **Golfers**. **BUILT 2026-09-05, wave 3 — in the half this wave owns.** The decision below is the whole of IOS-028; what shipped in wave 3 and what a later wave owes are split out under it, because appending the entry after the code it governs is what CLAUDE.md rule 5 forbids and appending a promise as a fact is what L-44 forbids.

**Decision.** `enum Tab { home, clubhouse, post, you }` (`MainTabView.swift:128`) → `{ home, compete, play, golfers, you }`, with a fourth `NavigationPath`. Route enums: `HomeRoute { schedule }` (`.people` becomes `openGolfers()`; `.league`/`.pot` retarget), **new** `CompeteRoute { season(UUID, pane: Pane = .story), event(UUID), weekend(UUID), draw(UUID), finished }` with `Pane { story, table, board, schedule, pot, album, rules }`, **new** `GolfersRoute { root, requests, person(UUID), headToHead(UUID), board }`, `YouRoute { settings, addGhin, record }` (`.people` retires). `openLeague(_:)` → `openCompetition(_:)` — one environment key, same shape, resolving a season or a moment. `store.preferredLeague` is re-scoped to navigation memory. Presenter: every sheet survives; `showEventPicker` retires into `intent`; `draft` becomes `CompeteRoute.draw`; **new** `intent`, `pair(UUID)`, `leaveSeason(UUID)`; `dismissAll()` and `anythingUp` gain the new cases so the push-ask drain (`:329-335`) keeps working. `apply(_:)` gains `.season(UUID)`, `.headToHead(UUID)`, `.weekend(UUID)`, `.desk`, and `friend_round` → `.receipt`; the founder-report nudge finally lands on the desk (N13). **The ⊕ keeps its snap-back and haptic verbatim** (`:336-339`). **Reversibility.** HARD — it is the whole nav. **Owner owes:** the D222 ruling first; then `docs/ios/app-review-notes.md` and the Guide are rewritten in the same commit, because they both walk a nav that would no longer exist.

**What shipped in wave 3.** The five slots and the five tab items, with the ⊕ still a verb in ember, its snap-back, its haptic, its live branch and its long-press all verbatim (`CSTabBarLongPress.plusIndex(of:)` already derived the middle slot from the item count, so five slots moved nothing). Four `NavigationPath`s. `HomeRoute` is `{ schedule }`; `CompeteRoute { season(UUID, pane: RoomPane), event(UUID), weekend(UUID), finished }`; `GolfersRoute { requests, person(UUID) }`; `YouRoute { settings, addGhin }`. `openLeague(_:)` is **`openCompetition(_:pane:)`** at all eight call sites, and `\.openGolfers` is the second environment key that replaced the cross-stack `HomeRoute.people` push — which is the whole of the D178 class of bug, since a link can no longer be declared in a stack that does not resolve it. **`NavSlot` (Kit) is the one producer that answers "which slot does this land in"** — over every `PushRoute`, every deep-link path and every shell door — and `MainTabView.apply(_:)` derives its tab from it rather than from a second switch, so `RouteMapTests` is load-bearing rather than decorative. `CompeteScreen` and `GolfersScreen` are the two new tab roots, each with its **empty root** ending in a next move (L-32) and each distinguishing a failed read from an empty one. The four Major surfaces got their words (D252) and the flag's migration.

**What a later wave owes, and where.** `CompeteRoute.season`'s pane is `RoomPane` and lands on the room that exists today; **wave 4** (D223 / IOS-031) replaces the landing with the season page and the `Pane { story, table, board, schedule, pot, album, rules }` vocabulary, and takes `draw(UUID)` and `leaveSeason(UUID)` with it. **Wave 5** (IOS-032) owes `GolfersRoute.headToHead` and `.board` — neither is added here, because a route to a page that does not exist is a door that does not open (L-32/L-44). **Wave 7** (D225) owes `intent`, into which `showEventPicker` retires; until then the picker is the door, with the Major's row now open. **The five new push routes wait on the sender**: `season`, `headToHead`, `weekend`, `desk` and `friend_round` need `PushKind` values that `docs/ios/push-contract.md` does not define and no server sends, and minting a client route for a payload nobody can send is dead code. What wave 3 did instead is retarget where the nine existing routes **land** — `chat`/`announce`/`moment`/`system` now land Compete → the season → the board, `request` lands the Golfers tab, and the contract's rule is unchanged: an unknown `v`, an unknown kind or a missing id lands Home, never a blank.

## IOS-031 · The season page replaces `LeagueRoomScreen` — **P2 · PROCEEDING 2026-09-05 (D223, D226, D235, D244; owner-authorised with the UX overhaul)**

**Authorised 2026-09-05** with the UX overhaul (`docs/ux-overhaul-2026-09-04/OWNER_RULINGS.md`); no ruling was needed on this entry itself, and **R-C (revised)** decides that its web half ships in the same wave, in the desk's own shape. **BUILT 2026-09-05, wave 4 — in the half this wave owns.** What shipped and what a later wave owes are split out under the decision, because appending a promise as a fact is what L-44 forbids.

**Decision.** `ClubhouseView` → `CompeteScreen` (a peer list; its paged `TabView` becomes paging between season pages; its `preferredLeague` write is deleted; `LeaguelessDoors` is replaced by the list's own empty state). `LeagueRoomScreen` + `RoomBits` → `SeasonPage`: story line → THIS WEEK → THE TABLE (with the endgame permanently beneath it) → THE POT → doors (Board · Schedule · Album · Rules) → the Pro's verb row. `StandingsPane` becomes the table section **with the climb intact**; `PotPane` becomes the pot section and `pane: .pot`; `LeaguePane` **splits** into a Rules page (everyone) and the Pro's verb row (Pro only) — two doors, not one pane with a role branch; `RoomAlbumPane` becomes a row inside the Board. `ClimbView`, `IndividualRaceView`, `CupFinalRaceView`, `StandingsTableView`, `ReceiptSheets`, `MembersSheet`, `SheetFrame` and `SeasonCeremonyView` are **kept verbatim**; the ceremony gains one caller (Home's state H). `DraftNightScreen` becomes a **pushed page with two seats** — the Pro's and the member's are different screens, not one screen with a swapped verb — and the `assign` and `snake` branches (`:126-262`) are **deleted** unless the wizard offers them (all 13 prod leagues are `random`). The cancel vote becomes a Tier-1 Home item. **Leave the season** appears at the foot of the Rules page, member-only, two-tap. **Reversibility.** HARD. **Sequencing:** this is the largest single lift and it ships **after** the season page has proven itself as a pushed destination.

**What shipped in wave 4.** `SeasonPage.swift` is `CompeteRoute.season`'s landing, in the order above, and `LeagueRoomScreen.swift`, `ClubhouseView.swift`, `RoomAlbumPane.swift` and `BylawsCard.swift` are **deleted**. `RoomPane` is **`SeasonPane { story, table, board, schedule, pot, album, rules }`** — the vocabulary IOS-028 promised — with `named(_:)` still answering the server's free-string pane and still landing an unknown name on the table. `StandingsPane.swift` is `Season/SeasonPhases.swift`: the three phase screens (`SeasonSetupChecklist`, `SeasonDraftHero`, `SeasonWrappedHero`), `ClashCard`, `NextCard` and `PressMeter` survive; **`RoomSeasonStrip` and `PolishWrapRow` are deleted**, because all four of the strip's figures are second renders of facts the page or the ME strip already carries (D223's own note 1). `LeaguePane` **split as specified** — `SeasonRulesPage.swift` (everyone: the rules as sentences, who's in, the look, the notices line, and **Leave the season**) and `ProVerbRow.swift` (the Pro: seven verbs, each already a definer RPC) — and the file that held it keeps only the sheets the rest of the season still calls (`SeasonAdminSheets.swift`). `SeasonStoryPane.swift` is the arc at length and the archive. The Kit gained `SeasonStory.swift` (the R6 payload, the seven-rung ladder, the rules producer), `LeaveSeason.swift` (the exit's copy and its gate) and `SeasonVote` in `StandingsMath.swift`. New tests: `SeasonStoryTests` (the ladder as a table, rung 7's reach and its fence, rung 7b), `EndgameCopyTests` (both grains, one date) and `LeaveSeasonTests` (forward-only, in the words a golfer reads).

**What a later wave owes, and where.** **`DraftNightScreen`'s two seats and the deletion of its `assign`/`snake` branches did not ship** — the draw is reached from the page's own door and is unchanged; it is a screen with its own three-way branch and its own tests, and folding it into the wave that deleted the room would have put two hard changes in one push. **The cancellation vote is an item on the SEASON PAGE, not yet on Home**: `SeasonVote.item` produces the Tier-1 copy IA §7.5 specifies (the eyebrow, the sentence, the two facts, the act this viewer has) and `SeasonVoteBanner` renders it at the top of the page; Home's placement needs the ranker to read `league_cancel_status`, which is a change to `home_dispatch` and belongs in the wave that touches it rather than in a second copy of the sentence. **The ceremony's Home caller** landed in wave 1b. **Compete's paging between season pages** is still a gesture nobody has built; `CompeteScreen` is the list and the page is a push.

**Reversibility.** HARD, as filed. The route is the seam: `competeDestination` is eleven lines and one `switch`, and every caller funnels through `openCompetition(_:pane:)`.

## IOS-032 · The Golfers tab, the person page, the head-to-head — **P2 · PROCEEDING 2026-09-05 (D222, D239, D241, D245; owner-authorised with the UX overhaul)**

**Authorised 2026-09-05** with the UX overhaul (`docs/ux-overhaul-2026-09-04/OWNER_RULINGS.md`); **R-D** settles the tab's name and **R-C (revised)** decides that its web half ships in the same wave, in the desk's own shape. **BUILT 2026-09-05, wave 5 — in the half this wave owns.** What shipped and what a later wave owes are split out under the decision, because appending a promise as a fact is what L-44 forbids.

**Decision.** `PeopleScreen` → `GolfersScreen`, promoted to a tab and widened: requests at the head · the board (form lens by default) · **PLAYING SOON with "Ask for a seat"** (which writes an `rsvp` `push_nudges` row to the host — a request, never a write to the tee sheet, so D69 is untouched) · buddies · **YOU PLAY WITH (not buddies yet)** · rivalries · league mates · **Invite someone who isn't here**. `TourCardSheet` becomes a **page** at `GolfersRoute.person`, with the sheet kept for the in-context peek from a round card; its footer carries **the three lengths (This Saturday · One week · A season)** per R-F's ruling — asked as one step, never guessed, all three always offered — plus **Put something on it** where the two share a season, a moment or a plan. **And the page carries P-17.** `TourCardSheet` holds mute (`:134-137`, with its VoiceOver label) and the two-step report (`:151-169`) today; promoting it to a page without them drops report/block/hide from the surface a golfer most often reaches a person on, which **L-38** forbids and App Store Guideline 1.2 rejects. P-17 mounts on the person page, the head-to-head page, every wire row and every moment page, **and the peek sheet keeps its copy**. Two rows on the page wait on **R21** (`tour_card` + `case[]` + `career.best_round`) and do not render until it exists: `trophies` is RLS self-only (`20260713200000:31-34`), `my_trophies()` takes no `p_profile`, and `career.best` is `min(differential)` with no gross, course or date. `RivalriesSection` moves to Golfers and to the record, and the head-to-head page is new. The privacy gate is **unchanged** (L-37/D150) and a card the viewer may not see says so plainly and offers the buddy request. `tour_card`'s `vs_you`, `courses` and `shared_courses` — already returned and discarded (`TourCard.swift:93-126`) — are finally rendered. **Reversibility.** MEDIUM.

**What shipped in wave 5.** `GolfersRoute { requests, person(UUID), headToHead(UUID), board }` — the two routes IOS-028 deliberately withheld now land on pages that exist. `PersonPage.swift` is the person page: the credential, the narrative sentence, YOU AND HIM (opening the head-to-head), THIS SEASON, LAST FIVE, BEST, TROPHIES, the courses, the recent rounds, the three lengths, and **P-17 in the toolbar**. `HeadToHeadPage.swift` is the record between two golfers — the summary sentence, LAST FIVE as dots, the six facets, the naming door and **P-17**. `FriendsBoard.swift` is the board with its two lenses. The Kit gained `People/HeadToHead.swift` (the R4 payload, the six facets and their copy), `People/FriendsBoard.swift` (the ranking rule and its two lenses) and `You/RecordPage`-facing narrowing in `CareerRecord.swift`. **`CSSafetyMenu` is P-17 built once** and mounted on both new pages plus the peek sheet; preflight check 25 fails the push if a surface that renders another golfer's content cannot reach it.

**What the pattern had to settle, because the design named a control the product does not have.** P-17's anatomy lists **report · block · hide · mute**. The app has `report_content`, `set_mute` and a device-local hide; it has **no block mechanic**, and `hide_content` is a *moderator* verb (a Pro takes a post down), not "hide this from me". **L-38's own wording is the answer** — *"Report, block (mute), hide/unhide, suspend, delete account"* — and `docs/ios/app-review-notes.md:105` already tells App Review that **Mute is the block**. So `CSSafetyMenu` mounts **Mute · Hide this · Report**, the mute item names what it delivers ("hide their rounds from your feeds"), and no item named Block is drawn over a mechanic that does not exist (L-32/L-44). A real block — mutual invisibility, with the read policies to enforce it — is a class-C mechanic with its own entry and it is not smuggled in under a menu label.

**Six defects found by looking, not by asserting.** Every one came from a real screenshot — three from the simulator against the owner's own account, three from the desk walk — and none of them was a logic error a test would have caught. **(1) A pushed page with `toolbar(.hidden)` has no back button**: the first shot of the person page was a golfer's card with no way off it. The bar stays now, and P-17 rides its trailing edge, which is where the design put it. **(2) "Couldn't load this" over a buddy the app simply has no history with** — the fallback's answer is THREE-valued (the read failed · the read answered with no record · a record) and collapsing the middle into the first is the exact L-32 confusion this build exists to end. **(3) "The board didn't load." over an account whose board is not deployed yet** — same shape, so `PeopleService.BoardRead` names all three and a function-missing error renders nothing rather than an apology. **(4) A golfer with no rounds got three em dashes in a column**; they get one true sentence. **(5) "Galen has won one title. Galen has beaten you five times"** — the same golfer opening two consecutive clauses reads as two people; the producer joins them. **(6) The board told me Tash had beaten MY number** — `CSBands` is written from the golfer's own point of view, which is right on a receipt and wrong on a list of other people, so the possessive turns on somebody else's row and the band table stays the one band table.

**What a later wave owes, and where.** **"Put something on it"** is the forfeit past the league (**C-5 / D242**) and is not drawn on the person page: `create_forfeit` is crew-only today and the widening is its own migration. The three lengths are offered and the two that land on an object that exists (This Saturday → the tee sheet with the person tagged; A season → the intent sheet) are wired; **One week** is the callout (**R19 / D237**), which is **wave 7**, and it is not sold before it opens. **YOU PLAY WITH** reads `recent_partners`, which ships; **LEAGUE MATES** does not render, because no read returns a per-season roster to this surface without a second query per membership. The **contacts door** is **R-G / D251**, wave 8.

## IOS-033 · Onboarding, and the link that survives the boot — **P1 · PROCEEDING 2026-09-05 (D224, D233, D247; owner-authorised with the UX overhaul)**

**Authorised 2026-09-05** with the UX overhaul (`docs/ux-overhaul-2026-09-04/OWNER_RULINGS.md`); **R-G** turns the crew step's fourth route into a built one (D251) and **R-C (revised)** puts the web's own card gate and crew step in the same wave, in the desk's own shape. **BUILT 2026-09-05, wave 8 — in the half this wave owns.** What shipped and what the owner still owes are split out under the decision, because appending a promise as a fact is what L-44 forbids.

**Decision.** `OrientationScreen.swift` is **deleted** — **and so are its callers**, which the file list must name or the build does not compile: `RootView.swift:16` (the `orienting` state), `:37-41` (the branch), `:57` (its animation) and `:62-70` (the decide-once block, which calls `OrientedFlag` at `:69` and `OrientationDev` at `:67`, both defined in the deleted file). Two telemetry events retire with it — `orientation_shown` (`:153`) and `orientation_done` (`:212`) — the pair the deleted screen wrote; D224 retires the screen on the brief's ban on explainer slides, not on their counts. `CardGateView`'s three steps merge to one scrolling frame: name, **"What do you usually shoot?"** in bands, the handle auto-filled and the marker **defaulted** with a footnote (L-08 needs them *set*, not *chosen*; L-24 is satisfied and no silhouette state is created); GHIN moves to the card. A third frame is the crew step (search, the person link, skip). `DoorView` **reads `JoinIntent` and names the invite above the email field**, and gains **"I have a code"** validated by `league_by_code` before the email round trip. **The link survives the *boot*, and the cold case gets a door — the title is corrected because iOS has no deferred deep linking.** After an App Store install the system passes nothing, so `.onOpenURL` (`CupSeasonApp.swift:29-37`) never fires and `JoinIntent.store` is never reached; the four parts below only help once an intent has been stored. **Four parts:** `JoinIntent.pending()` read on `DoorView` **and** `CardGateView`; the Smart App Banner `app-argument` (delivered by Safari on a later visit, never by the App Store hop); an `NSUserActivity` handler; and `JoinIntent.clear()` moved to **after** the covenant resolves (today `RootView.swift:43` clears it on appear). **The cold recovery is the door's "I have a code"** — which is why that control is in this entry and not a later one — with **re-tapping the original link** as the second path, which does work once installed. A genuine deferred link would be a first-launch clipboard read, and that is **its own entry with its own privacy note**, not a clause here. `JoinLeagueFlow`: the covenant fires on **every** path including $0 and the in-app invite, shows **WHO before the money** with the roster named, and the welcome gains a **Done** button. The flow ends on **"Say you're in"** to the Pro's next round, not on a solo post. The push ask's **timing** moves (D247); its copy and mechanism are untouched. **Reversibility.** EASY per part. **Owner owes:** the AASA already claims `?join=`; the Smart App Banner is a web change.

**What shipped in wave 8.** `OrientationScreen.swift` is **deleted with all four callers**, `OrientedFlag` and `OrientationDev`; `RootView`'s `.ready` branch now shows `CrewStep` where the orientation stood, decided by `CrewFlag` on the way in (the flag written on the DECISION, an invited golfer skipping it per D116). `CardGateView` is **one scrolling frame**: the name, the five bands, the marker DEFAULTED behind a "Change" row with its footnote, the handle DEFAULTED and shown, Save, and GHIN's forwarding address. `CrewStep.swift` is the third frame with D233's four routes. The Kit gained `Onboarding/OnboardingCopy.swift` (the bands, every string, the gate, the deterministic marker default and the device-held starter), `People/ContactHash.swift` (normalisation, a plain SHA-256, and `match_contacts`) and `Nav/PendingLink.swift`. New tests: `OnboardingTests`, `ContactHashTests`, `PushKindTests`.

**THE LINK THAT SURVIVES THE BOOT — what was actually wrong, and what fixed it.** The defect was not that the token was lost on disk; all four (`?join=`, `?claim=`, `?p=`, `?plan=`) are stored by `.onOpenURL` and three of them were already drained correctly. It was that **`RootView` cleared the join code on the tabs' `.onAppear`, BEFORE the covenant sheet resolved** — so a golfer who backgrounded the app on the covenant, or whose join failed, lost the code they had been sent and had no way back to it. `PendingLink` is the producer that answers "is anything waiting", in one order, with **one door to a clear** — named `spend`, because a token is retired when it has been ANSWERED and never merely because a screen appeared. The join is spent when `JoinLeagueFlow` completes or is dismissed, not on appear. `PendingLink.doorLine` is the sentence the door owes a stranger who tapped a friend's link, and `PendingLink.invited` is D116's rule, which outlived the screen that wrote it.

**What a later wave owes, and where.** **`DoorView` does not yet read `PendingLink.doorLine` and does not yet carry "I have a code"** — the web's door has both, and the phone's door is a screen this wave did not otherwise touch; it is one `if let` and one control, and it is named here rather than claimed. **The Smart App Banner and the `NSUserActivity` handler** are unbuilt; the banner is a web change and is the owner's. `JoinLeagueFlow`'s own clauses (the Done button, ending on "Say you're in") shipped in wave 7 with the covenant work and are unchanged here.

**Two DEBUG hatches, and why they exist.** `-cs_dev_open card` puts the card gate over a signed-in simulator as a NEW golfer meets it (the band question is otherwise invisible on an account that already has a number), and `-cs_dev_open crew` does the same for the crew step. Both were load-bearing: the crew step's first cut drew **two controls for one act** — its own "Text an invite" door and `PersonInviteLink`'s row underneath it — and that is only visible in a photograph. A wave that builds a frame and does not photograph it has not looked at it.

---

## IOS-036 · One producer per law, one preflight check per law — **P2 · BUILT 2026-09-05, wave 9 (D246, D249; owner-authorised with the UX overhaul)**

**Decision.** Extend the D120 pattern (`LeagueCopy.Stage` + `STAGE_LABEL` + preflight check 20) to every law the audit found produced in more than one place. Producers already landed by earlier waves: the week (`LeagueDates.week` / `csWeek`, D246, wave 1a), the head-to-head record (R4's one read, wave 5), the honest movement label (`StandingsMath.movement` / `csMovement`). This wave lands the last one and the checks that hold all of them: **the band** — `band_name(p_pvi)` beside `cup_points`, returned on `round_card` beside the `pvi` it names, with both clients preferring the server's word and falling back to their own producer so either deploy order renders the same five words.

**Two checks, and each was proven to FAIL before it was left green.** **Check 27 — the vocabulary suite:** twenty-nine laws from `TERMINOLOGY.md` §4, over `apps/ios/CupSeason/**`, the Kit's `Sources/**`, `index.html`, the store subset of `docs/ios/app-store-listing.md`, and the live definitions of the board/push/ledger generators in SQL. It reads **prose**, not bytes: `tools/extract-strings.mjs` (NEW) classifies string literals by CALL SITE, which is §4's own rule and the thing a grep cannot do — `.select("id,differential,…")` is a column list, `class="pvi …"` is a class name, `case "duel"` is a server kind, and a lint that cries wolf on those gets switched off within a week. Proven to fail on a Swift offender, a web offender, a SQL offender and its own three self-test probes. **Check 28 — the band-name parity fixture:** the table is re-derived FROM THE MIGRATION'S SQL on every push and compared against `tests/fixtures/bands.json`, `CSBands.bandName` and `index.html`'s `bandName()`. Without it R13 is a *third* band table rather than one rule with three renderings — which §4's own check 27 forbids. Proven to fail by bending the web's −1.0 edge, the exact drift Q-20 ruled back in August.

**The widget ships here, and it knows only what Home just read.** `DispatchSnapshot.swift` is compiled into **both** targets the way `CSRoundActivity.swift` is (`project.yml`), so the extension still depends on CSDesign alone and holds no client: the app writes the ME strip's facts and the lead's own sentence into the App Group after every successful `home_dispatch` (`DispatchSnapshotFeed`), and the widget draws them. Three rules are values rather than habits, each with a test: **money can never reach it** (`sanitised` drops the owe fact whoever passes it — L-10, and a home screen is not private), **it never lies about time** (`AS OF 14:22` while fresh; past 24 h `AS OF SAT · OPEN TO REFRESH` and `verb` returns **nil**, so a day-old door is not offered — L-44), and **a route it cannot open is not written** (`DeepLink` resolves exactly one custom-scheme URL today, so anything else links Home rather than selling a door that does not open — L-32).

**What this wave learned about its own tools, kept because the next lint will hit it.** A `/'/g` regex literal inside a template expression opened a phantom string and desynchronised nine hundred lines of `index.html`; an HTML comment quoting the retired words counted as six offences; `\u2713` came back as the letter u; and a `Me.Membership` JSON fixture and an auth-error regex both read as prose. Every one was found by RUNNING the extractor over the tree, not by reasoning about it, and each is now a rule in the scanner. **The measured cost:** ~420 strings moved across both clients (the design's own estimate of ~172 covered 12 of the 27 string patterns and no web half), seven SQL generators copied forward, and four pinned Kit assertions retargeted onto the new sentences.

**Reversibility.** EASY. **Gate:** the suite reads 0 at ship or the ship stops — it does, at 29 laws over 253 Swift files, `index.html` and 230 live SQL definitions.

## IOS-037 · The repair pass on the phone — the body writes, the shed arguments, the metals and the fold — **P1 · BUILT 2026-09-05 (D254; owner-authorised with the UX overhaul)**

**Decision.** Fix what the four-lens review of waves 0–9 found on the phone, in the order it matters: the two writes made from inside a SwiftUI body, the five `RpcCall`s that shed consequential arguments on a blind retry, the ruled words with no lint behind them, and the four screens a photograph found wrong.

**The body writes.** `HomeModel.ranked(stripSuppress:)` assigned `HomeLeadFlag.shared.liveIsLead` and `HomeView.body` called `DispatchSnapshotFeed.publish(...)` — an `@Observable` mutation and an App-Group `UserDefaults` write, both on every body evaluation, both read by an ancestor. `@Observable`'s generated setter calls `withMutation` on **every** assignment, equal value or not, so each Home body invalidated `MainTabView`, which re-rendered Home. Both move into `run(...)` after `guard live(gen)`, in one `publishOutwards(me:)` — and the widget is handed a LEAD only when the ranker actually served one, because a sentence composed by the declared fallback is honest on Home, where the screen says what it is, and is not a sentence to put on a home screen as though the server said it.

**Nothing consequential is droppable.** `SupabaseService.call(_:)` retries on ANY first error by removing EVERY droppable key at once. `PostRoundCall` listed seven, so one 500 re-dated the round to `current_date` (changing which season window scores it, L-13), posted a nine as an eighteen, and dropped the partners and the scorecard photo — on a `volatile` function with no idempotency key. `ConfirmPartnerCall` listed `p_confirm`, whose server default is `true`, so a retried DECLINE was written as a CONFIRMATION. `RunItBackCall`, `CreateForfeitCall` and `DeclarePlanCall` were the same shape with smaller blast radii. All five are `optionalArgs = []`; where a real missing-function skew existed (`declare_round`'s two new arguments) it becomes a **declared fallback** at the call site, firing on PGRST202/42883 alone. `create_forfeit` needed none: a season's forfeit already encodes the six keys the deployed function has, and `ForfeitHomeTests` now asserts that rather than the shed list.

**Four screens a photograph found wrong.** The You hero and the person page rendered the **handicap index in gold**, which `UX_PRINCIPLES` §4 forbids in those exact words — it is ink now, and the hero's gold spine is keyed on an actual trophy rather than on the index being established. The person page's 1:1 credential panel pushed its only actions under the tab bar on the largest iPhone — it takes a 16:10 panel there and the three lengths are hoisted above the record, because that page is where a callout push lands and it may not ask for a scroll to answer. The Golfers tab printed `BUDDIES · 7` over seven visible rows from a pre-overhaul call site while `GolfersRoot.Section.head` — the producer R-D's three tiers were written into — had **zero call sites**; the heads read from the producer now and the count is gone. A wire row's display name rendered as orphaned glyph fragments behind the ✦ FOUNDER capsule; the name takes the layout priority and truncates.

**A-6's mechanism, widened so it can express what A-6 forbids.** The design's answer to "the same fact twice on one screen" is *"the lead hands the deck a suppress"* — typed `Set<MeStripCopy.Fact>`, four ME facts, which cannot name a round. So A-6's own worked example reproduced on Home: a buddy's personal best in the ranked deck and again in the wire one scroll below. `HomeRank.Ranked` gains `spentRounds`, derived from what each item already carries (its `receipt` door and the `story:<round>` key both producers mint, so nothing new travels on the wire and the SERVED path gets the de-dupe for free), and `HomeFeedFold.fold` takes it. The web does the same with `csSpentRound`.

**R-06, settled the way the doc reads.** `HomeRank.arrange` gains `allowLead`, separate from `useServerRank`: the first chooses whether there is a lead card at all, the second chooses the ORDER. They had been one flag, and the tests used it for the ordering meaning while §5.4 rule 2 means the other one — so the phone drew a lead card off the declared fallback where the web drew none, which on an un-migrated database is every day. `noLeadCard` now asserts the CARD, not just the absence of a server rank.

**Two producers that were built and never rendered.** The live dispatch item had ONE face — *"You are on the card right now."* — for a read that returns a round the caller is **seated in**, which includes one somebody else started and I have never opened, where both sentences are false and the verb JOIN is missing. It has two faces again, on the server ranker and in the client fallback, and `native_home` v3 carries the host's name so the invitation has a subject. The **month minimum** — the one item with a hard deadline and a real penalty — was composed by the server ranker and by neither client fallback, so until the migrations land it never appeared at all; `HomeFallbackItems.floorItem` ports the retired hero rung's guards verbatim, D140's solo exclusion included, and the web's `csFallbackItems` gained the same item.

**Reversibility.** EASY per fix; none introduces a schema object. **Gate:** preflight 36 checks 0/0, sunningdale 27, CupSeasonKit 775 · CupSeason 28 · CSDesign 14.

**Not built here, and why.** `-cs_dev_home_state <letter>` and `-cs_dev_look dark` are the Prove phase's two cheapest asks and belong with the pass that photographs the twelve unreachable Home states, not with a repair commit. `-cs_dev_push` still does not route a `callout` payload (`PushDev.payload` → `router.pending` is the gap, not `PushRoute.from`), so the head-to-head landing page remains unwalkable; it is named in the ship report.

## IOS-038 · The ruled words and the worth of a round — the fourth intent, and what the schedule is worth — **P1 · BUILT 2026-09-05, wave A (R-J, R-K; D256, D257; owner-ruled 2026-09-05, second sitting)**

**Decision.** Retire `I want to beat one guy` on both clients and give the schedule the arithmetic the season page has had since QB-12.

**R-J · the fourth intent reads "Go head to head".** `StartIntent.beatOneGuy` becomes `StartIntent.headToHead`, which changes the rendered line, the telemetry label (`door: "intent:headToHead"`) and every header the flow reaches. It was the only one of the four intents that began "I want to", it read as cringe, and it was the one sentence not addressed to every golfer in a mixed league — so the gloss goes with it: *"you and him, whatever length you like"* becomes *"the two of you, at whatever length you like"*, and `IntentSheetTests` now asserts that **no string the sheet renders carries a gendered pronoun**, which is R-J's own reason turned into a test rather than a promise. **"Head to head" is already this product's noun** for the record between two golfers (`head_to_head`, the page, the rivalry line), so the door and the record it builds finally share a word. The three lengths under it are untouched. Preflight 27 gains **§4.34** and its self-test probe, so the retired phrasing cannot come back quietly — D249's thesis, applied again.

**R-K · what a round is worth, and where it is said.** `RoundWorth` (Kit) and `csRoundWorth` (web) are verbatim ports of the new `public.round_worth(p_cap, p_used, p_worst)`; `ClimbMath.closer` calls the port instead of doing the multiplication a second time, so the phone holds exactly ONE copy of the sum and preflight 37 fails the push on a second. `round_detail` v2 carries the cap and the month's counters per season the round counts in, and `ScheduledRoundSheet` prints at most two lines under the course block — *"This round is worth up to 12. Your best 4 count and you have 2."* The subject is "This round" and not the day and the course because the sheet's header already carries both: **DEF-2 is the defect filed the last time a card said its course twice**, and the producer keeps a subject slot so a surface without that context can pass the fuller form.

**The plan you NEED** is `home_dispatch`'s new `need:<league_id>` item (D257), in the existing COMING band, fenced by L-21 so hard that it is silent in most states by construction: active season, solo structure, a field of two, a counting slot still open this month, and an arithmetic that actually closes. Its door is the declare sheet, which both clients already route.

**Skew, both directions.** `round_detail`'s `worth` is an added key: a phone built against this renders no worth line against today's database, and a phone built before it ignores the key. `home_dispatch`'s new item is one more element in an array the client already ranks and fences. Neither client gained an RPC, so no grant moved.

**Not built here, and why.** The worth line is **not** on Home's plan card and **not** in the composer: the plan card's standfirst is spent on who is in (L-34) and the composer's fold already explains the bands. The dispatch item is **solo-only** — a squads member's gap is not closed by one golfer's round, and the sentence would be a promise the engine cannot keep. Neither half can be verified against real data until the owner pushes; both were judged from code, the tests and a rolled-back dry-run against production, and that is said plainly in the wave report.

**Reversibility.** EASY. No schema object is created; the two new SQL functions are additive and the two re-created ones are `create or replace`.

**Gate:** preflight 37 checks 0/0, sunningdale 27, CupSeasonKit · CupSeason · CSDesign all green (counts in the wave report), migration `20261001090000` dry-run clean against production inside `begin … rollback`.

## IOS-039 · The accessibility sizes, the record voice, and one easing — **P1 · BUILT 2026-09-05, wave B (D258; owner-authorised with the UX overhaul)**

**Decision.** Make the AX sizes a layout the app *decides* rather than a size it survives, restore the mono voice, and give L-30 a mechanism instead of a habit.

**The strip reflows by arithmetic, not by hope.** `MeStripLayout` (Kit) takes two rows of two while every pair's widest unbreakable WORD fits the column, and one fact per row when it does not. It can do that because the strip is set entirely in IBM Plex Mono, whose advance is 600/1000 of the em in all three bundled weights — read from the files, not remembered. The view hands it a unit **measured** off a ten-character probe set in the real face at the real size: `UIFontMetrics(forTextStyle:).scaledValue(for:)` and SwiftUI's `Font.custom(_:size:relativeTo:)` disagree by about five points at AX5, and the computed version drew `NUMBE` / `R`. `MeStripLayoutTests` holds the reflow at AX3, AX4-equivalent and AX5 with the app's own reported units, and holds a **copy-length budget** on every label and fixed value the producer can print. The season row's two-line cap is gone: it wraps.

**A pinned sheet is a reading-size decision.** `.csFittedSheet(_:large:)` (CSDesign) keeps `.height(...)` at the reading sizes and gives the whole page at the accessibility sizes. The intent sheet showed two of four intents at AX3; the length sheet drew its three rows **on top of each other** and ellipsised two of three glosses. Three sheets also gained the scroller they never had. Preflight 38 fails the push on a bare `.height(` detent anywhere else.

**`CSFont.monoRegular` was `"IBMPlexMono"` and nothing carried that name.** The face is `IBMPlexMono-Regular`; `Font.custom` resolved nothing and fell back to the system sans, so `label`, `mono` and `monoSmall` — the labels, the eyebrows' small sibling, the record voice — had never rendered in Plex. It was invisible because the fallback is legible. Preflight 38 now reads every bundled file's `name` table and fails on a name no face carries.

**The 11pt floor, and the names that were being cut.** Ten sites scaled a role below 11pt (`CSFont.label` × 0.7 = 7.7); each is now at or above the floor or wraps instead. The table's name column and the climb's rung were truncating `Jerecho Fisch…` and `You · Jere…` at the DEFAULT size; both wrap. The table's rank and its movement chip get a gap that scales with the type they separate — at AX3 the row read `01HELD SINCE SUN`.

**L-30 gets a mechanism.** `CSMotion.run`, `.csAnimation(_:value:)` and `CSMotion.breath(_:)` resolve to nothing under reduce motion, and every animating call site in the app and in CSDesign now goes through them; the roll is the only curve, at four named durations. Preflight 39 fails the push on a second easing, a raw `withAnimation`, or an unguarded `.animation(`.

**The web half.** A single global `prefers-reduced-motion` backstop rests every animation and transition on its end frame — the thirty per-surface opt-outs above it stay, but the ones nobody wrote (`csStamp`, `csPutt`, `csDrop`, the split-flap) are covered now. The one bouncing bezier is the roll. The climb's rung stops clipping its name and its sentence, and its three sub-11px rules are paid off (the web's floor debt ratchets 86 → 83). `tools/web-verify.mjs` gains a **permanent horizontal-overflow assertion** and the walk runs at **1440, 390 and 320** — 320 being the web's exact twin of "nothing scrolls horizontally".

**Skew.** None. No RPC, no payload key, no migration: this wave is client-only in both clients, and the 24 migrations the owner owes are unchanged.

**Reversibility.** EASY, and the font-name fix is the one to think about before reverting — it changes the metrics of every mono line in the app, which is a visible change and the correct one.

**Gate:** preflight **39** checks, 0 failures 0 warnings; sunningdale 27; CupSeasonKit · CupSeason · CSDesign green (counts in the wave report); `web-verify` clean at 1440/390/320 with no document overflow. No migration was written, so there was none to dry-run.

---

## IOS-040 · The hatches the next audit needs — thirteen Home states, a callout that routes, and three sheets nobody had photographed — **P1 · BUILT 2026-09-05, wave C (D259; owner-authorised with the UX overhaul)**

**Decision.** Make every screen the phone can draw a screen a simulator can open, and make the two clients' five nav words hold each other.

**`-cs_dev_home_state <id>` substitutes ONE READ.** `HomeModel.run` takes `HomeStateFixtures.payload(id)` in place of `home_dispatch`, and nothing else changes — the same `HomeRank.arrange`, the same `MeStripCopy`, the same six slots. It never writes to the server. The four sub-views that make their own reads (`InvitesBanner`, `BuddyRequests`, `UpNextChips`, `UpcomingRoundsSection`) stand down under `CSDevHatch.fixtureHome`, because a screenshot that is half a fixture and half the signed-in account is evidence of neither; that flag is `homeState != nil` and `homeState` is `nil` in Release by construction, so the shipped build has no branch to take.

**The fixtures are generated, and they are also the test suite.** `tests/fixtures/home-states.json` is the source; `tools/build-home-states.mjs` writes `Generated/HomeStateFixtures.swift` inside `#if DEBUG`; `HomeStateFixtureTests` runs the fence, the veto, the cap, the suppress union and the determinism of the arrangement over the same thirteen payloads the screenshots use — plus the copy laws they must keep (no count of the golfer's absence, no retired R-J phrasing, the typographic apostrophe). Dates are **tokens** resolved at load, so a fixture never reads stale.

**`-cs_dev_push <kind>` fills the ids the route needs.** `PushRoute.from` was always right; `PushDev.payload` is a pure parse and `-cs_dev_push_ids` printed **no person's id at all**, so no callout payload could carry the `profile_id` its landing requires and every attempt fell through the contract's "a missing id lands Home" clause. `PushDev.resolved(me:preferred:)` takes a bare `PushKind` name, fills only what that kind's landing needs from the session already in memory, logs `kind → route`, and leaves a hand-written payload untouched. The dump prints buddies, clash opponents and the caller.

**`-cs_dev_open declare | whenfork | forfeit`.** Three sheets that set private `Presenter` state with no case here. `-cs_dev_open forfeit` opens D242's no-container shape (the point of that entry); `-cs_dev_open forfeit league` opens it inside a season.

**What the hatches found, in one pass.** The phone drops every `invite:` item because it answers invitations in a banner that reads a different source, so a golfer with an invitation in the ranked payload and nothing in `my_invites` sees nothing on Home — while the desk leads with it. A wrapped season loses its ME-strip row, which `HOME_STATE_MATRIX.md` §4 specifies twice. The wire's leagueless empty state breaks its own sentence across two lines at the DEFAULT size. `YOUR NUMBER` wraps in the four-fact strip when the owe line is present. `HomeDispatch.Route` has no event case, so an event never leads Home on either client. All of it is in `HOME_STATES.md` with the picture beside it; none of it is fixed here, because a wave that instruments and repairs in one commit cannot say which it was.

**The web half.** `?cs_home_state=<id>` fetches **the same fixture file** and drives the desk's lead and deck — including over a signed-out boot, which is the only web session a build machine has, and which is why the last re-audit had no signed-in web screenshot at all. It cannot exist in production: `stamp-version.sh` publishes an allowlist, `tests/` is not on it, the fetch 404s and the hatch declines out loud. It does **not** fake the desk's ME strip, which reads five sources rather than one.

**Preflight 40 · the two clients' nav labels, slot by slot.** `NavSlot.label` against both of the web's shapes — the sidebar and the bar — with the slot→`data-v` map declared and reasoned in the check, and a failure on a sixth destination in either. The word `Record` in place of `Play` now fails the push; it shipped twice inside this overhaul. **Preflight 41 · the fixture set**: the generated twin is fresh, the phone's copy is `#if DEBUG`, every fixture item carries a door both clients resolve, and `tests/` is absent from the dist allowlist and from `sw.js`. Both were made to fail on purpose before being trusted.

**Skew.** None. No RPC, no payload key, no migration and no grant: this wave is client-only on both clients, and the 24 migrations the owner owes are unchanged.

**Reversibility.** EASY. Every line is DEBUG-only on the phone or query-gated on the web.

**Gate:** preflight **41** checks, 0 failures 0 warnings; sunningdale 27; CupSeasonKit 829 · CupSeason 28 · CSDesign 14; `web-verify` clean at 1440/390/320 with no document overflow. No migration was written, so there was none to dry-run.

---

## IOS-041 · The playing-HCP gloss and the offline course book — **P1 · BUILT 2026-09-05, wave D (R-M, R-N; D260, D261; owner-ruled 2026-09-05)**

**Decision.** Say what the figure a round is measured against actually is, and stop being dead without a signal.

**The gloss says `playing HCP`; the five bands say what they always said.** `CSBands.vsPhrase` and `CSBands.pointsFor` are the only two producers that had to change on the phone — *"beat your playing HCP by 2.4"*, *"played to your playing HCP"*, *"1.3 over your playing HCP"* — and everything downstream follows: the composer's chip, the receipt, the epilogue and its share caption, the feed card, the friends board's form line, the rivalry captions, the You and Tour Card labels, the head-to-head basis, the individual race's column head. `bandName` is untouched: *Torched it · **Beat your number** · Played to it · A little loose · Posted anyway*, exactly as spec §2.2 has them, because the owner ruled they stay. A card can therefore show the chip **Beat your number** over the line **2.1 under your playing HCP** — recorded in R-M, deliberate, not a defect.

**The two nouns are distinguished once, at first contact.** The scoring guide's own section is now `Your index` and the one under it defines the other in a worked example; the receipt shows one becoming the other in adjacent rows (**Your index that day 14.2** → **Playing HCP 13.5**); the rules page reads *"Scored at 95% of your index"*. The INDEX sense of `your number` is deliberately untouched everywhere else — R-M retired the word for the comparison, and sweeping both would have been a ruling nobody made.

**Two producers were destroying the acronym and neither had a test.** `PostEpilogue.caption` lower-cased the whole share line — the ONE string that leaves the app and lands in a group chat — and Home's feed card lower-cased the same gloss for both the card and its VoiceOver label. Both now keep the case their producer gave them; the caption's test pins it.

**`CourseDisk` is `LiveDisk`'s twin, plus a cap.** Actor, JSON files in Application Support, atomic writes, every read tolerant of a missing or corrupt file — and 40 books with least-recently-used eviction, where "used" means READ (`book(_:)` touches on the way out, which is what makes the order mean anything). A course id arrives off the network, so it is never spliced into a path raw.

**`my_course_books(p_limit)` fills it in one round trip**, hand-declared as an `RpcCall` until the owner's push refreshes the contract, with the argument defaulted on both sides. It runs **detached** after a boot that already succeeded and is silent on failure, because a failure means the phone keeps the books it has. Sign-out clears them.

**Three surfaces work with no signal, and the fourth says why it cannot.** `CourseCardSheet` draws the picked tee's rating, slope, par and yards, then *"1st: par 5 · stroke index 7"*, then the card in two rows of nine — the escalation's own question, at the top, above the list of every other tee. `LiveRepository.courseHoles` asks the server, writes the answer through, and falls back to the book, so a tee sheet can score in a canyon. All three course pickers (composer, plan, live setup) consult the book before the network through ONE producer, `CourseBookStore.searchCourses`, and say *"No signal — searching the courses on your phone"* when nothing on the network answered. Searching the whole catalogue offline is out of scope and the same line says so.

**Honesty is in the type.** `CourseAnswer` cannot hand over a book without saying where it came from, and `savedLine` is the only sanctioned rendering of provenance — so there is no path that draws a cached rating without the line above it. The plan sheet now says when its read failed instead of quietly drawing the thinner row it already had. A tee with no cached card says so; a partial card is not a card; `pars` and `card` return nil rather than a guess.

**`-cs_dev_open coursecard`** opens the first book the phone holds, and with an empty store opens the never-kept state — the other one worth photographing. Nothing is seeded: a fabricated book would make the screenshot a lie about what the store does.

**The web half, in its own shape (R-C).** The desk gets the ARCHIVE — every course you have played or planned, its tees, ratings, slopes and cards, browsable under YOUR COURSES on the You page — backed by the same store in `localStorage`, the same cap, the same LRU, and the same strings with ONE word changed — the phone says *"your phone"* and the desk says *"this device"*, because a golfer reading this on a MacBook is not reading it on a phone and L-32 is about telling the truth. The three pickers fall back the same way, and `searchCache` now returns null on a failed read so the dropdown can tell "no such course" from "no signal".

**Skew.** Two migrations owed. `my_course_books` is called by both clients through a guarded read that returns null — NOT zero — when it is not there, so a client ahead of the database shows the books it already has and never narrates "no courses". `settle_week_clash`'s rename changes no signature and no payload key; either deploy order renders an honest board post, in one noun or the other.

**Reversibility.** EASY for the gloss (four string literals per client, plus the tests and one lint). MEDIUM for the store, only because deleting it means deleting a directory of files on every device that has one — nothing depends on it, and every read falls back to the network path it had before.

**Gate:** preflight **42** checks, 0 failures 0 warnings; sunningdale 27; CupSeasonKit 841 · CupSeason 28 · CSDesign 14; `web-verify` clean at 1440/390 with no document overflow. Both migrations dry-ran clean against production inside `begin … rollback`.

## IOS-042 · What's in the bag — fourteen clubs, the sideline, and the change that is a post — **P1 · BUILT 2026-09-05, wave E (R-O; D262; owner-ruled 2026-09-05 and ordered built tonight)**

**Decision.** Give the card the thing golfers actually talk about, and make the CHANGE the feature rather than the list.

**Fourteen clubs, free text, and no equipment database.** `Bag.Item` is a slot and a label, both typed by the golfer (*"Driver" · "TSR3 9° · Ventus Blue"*). There is no picker, no catalogue, no autocomplete against a brand list on either client — R-O's written refusal, restated at the top of `Bag.swift` and in the migration so the next session meets it before it re-proposes one. `slot` exists for exactly one reason: *"Galen put a new driver in the bag"* is not sayable without it, and inferring "driver" from a label would be the fabrication the laws forbid. Blank degrades to *"a new club"* everywhere.

**Every item is a spell.** `added_on` / `removed_on` are the model and `in_bag` is a GENERATED column over them, so no write can set one without the other. That is what makes the sentence below computable, and it is why R-O asks for the dates by name.

**`BagCopy.sinceLine` is the line the wave exists for.** *"Since the new driver went in: four rounds, two beat your playing HCP."* The arithmetic is the server's (`bag_of` counts against the allowance lens, one row per round, `beat` at the engine's own `pvi >= 1`), because a second implementation would drift from `v_rounds_ranked` the way D209's did. Two fences: it must be a CHANGE (a club older than nothing in the bag is an initial fill, not a new driver) and it must have rounds behind it. `beat` arrives NULL, never zero, when no season has scored those rounds, and the sentence then stops after the rounds (L-44). One word differs from R-O's illustration — *beat* rather than *under* — because "under" at face value would count rounds whose own band chip says *Played to it*; the reason is in D262 §7.

**`BagSheet` is the only place a bag is edited**, and it is reached by a door on You that is drawn only once `bag_of` has ANSWERED. A read that did not happen draws NO ROW — no dead door, which is the whole of L-32 here — and the sheet itself, opened by the hatch before the push, says *"Could not open your bag. Nothing has been changed."* rather than an empty bag a golfer would fill in twice. Photographed in exactly that state tonight (`scratchpad/ux/shots/v4-waveE-bag-skew.png`).

**The diff is the DATABASE'S, and neither client decides what is worth posting.** The sheet sends the whole bag; `save_bag` compares it with what is stored and writes ONE person-homed post only when a club entered or left the bag or the ball changed. A re-order posts nothing, a typo fix posts nothing, a save that moved nothing posts nothing (L-20/21/22). Exercised against real production schema inside a rolled-back transaction: a fill of three clubs and a ball → *"Diego made four changes to the bag."*; the identical bag saved again → **0 changes, no post**; a club in → *"Diego put a new 8-iron in the bag."*; a club to the sideline → *"Diego took the driver out of the bag."*; a club deleted outright → the same sentence, still naming the driver (the slot is snapshotted before the row is deleted — the first run of that path said *"took the club out of the bag"* about a driver); a typo fixed → **0 changes, no post**; the bag emptied → one post; a fifteenth club refused by name.

**`kind = 'bag'` is a sixth kind because the PUSH is curated by kind.** `moment` — the kind a bag change would otherwise borrow — rings a lock screen through D238's person-homed branch, and a bag change is a feed item, not an interruption (L-22). `push/index.ts` declines it by name in the same commit. **Ordering matters and is stated rather than left to be found: a database that carries `save_bag` before that function is deployed WILL ring buddies' phones on a bag change.**

**The person rail gets its first reader.** D238 gave a post the right to be homed on a golfer instead of a league and nothing has ever read one — `HomeStream.loadPosts` filtered by `league_id`, so every profile-homed post written since (a leagueless golfer's milestones among them) went nowhere. `loadPersonPosts()` reads them under `posts_profile_read`'s own predicate — no id list, because the policy IS the circle — excluding `round` (the circle's rounds already arrive through `home_feed`) and `chat`, deduped by id, and empty on any error.

**The person's page and the card.** `PersonPage` renders the bag under the card, in the third person, and only when there is something in it — an empty "In the bag" head on somebody else's page is a sentence about them they did not write. Visibility is `can_see_profile_board`, the Tour Card's own predicate, so a bag is never on a page the card is not, and the MARKER is still the identity floor (L-24).

**The web half, in its own shape (R-C).** The desk gets the bag and the sideline **side by side in two columns** on a 1440 — the sideline's whole question is a comparison and a comparison down one column is not one — stacking under 900px, with the four move buttons wrapping under the two boxes at 390 rather than crushing the label to one character (measured, then fixed, at 390). The read-only block also rides a golfer's Tour Card sheet. `csBagSince` / `csBagWord` / `csBagSummary` are the verbatim twins of `BagCopy`'s, and **preflight 43** compares the two clients' fragments and their fourteen so they cannot drift the way D259 caught the nav labels drifting.

**One R-M miss fixed in passing.** The desk's Tour Card built three career labels by interpolation — `Career · vs ${whose} number`, `Avg vs ${whose} number`, and the signs line — which wave D's rename missed because check 42's web scan reads PROSE and these are template literals. All three now say *playing HCP*, matching `TourCard.careerEyebrow` / `avgLabel` / `careerSignsLine` on the phone, and check 42 part C pins that block by name so the blind spot cannot swallow them twice.

**Skew.** One migration owed (`20261006090000`), and one Edge Function deploy. Client ahead of the database: no bag anywhere, no door, no error — the state photographed tonight. Database ahead of the client: the table sits unread. Every argument on both new functions is defaulted; NEITHER call declares a droppable argument, deliberately — dropping `p_profile` would read MY bag under somebody else's name and dropping `p_ball_set` would silently discard a ball, and a function that does not exist is not helped by a second attempt.

**Deferred, and named rather than half-built.** (a) The ranked dispatch does not carry a bag change — only the feed does. `home_dispatch`'s live definition is ~900 lines and re-minting it verbatim to add one rung, the night before the owner archives to a phone, is the risk the ship rule forbids. (b) A photo of the bag: R-O permits it and explicitly allows deferring it; the `media` bucket and `photo_path` pattern are already shipped, and it is the obvious next addition.

**Reversibility.** EASY. One migration (a new table and five new functions — nothing existing is re-created but `posts_kind_check`, which is widened by one word), one sheet, one door, one section on a page, one panel on the desk, and one guard in the push function. Nothing scores, counts or settles anything.

**Gate:** preflight **43** checks, 0 failures 0 warnings; sunningdale 27; CupSeasonKit **854** · CupSeason 28 · CSDesign 14; `web-verify` clean at 1440/390, 0 console errors, no document overflow. The migration dry-ran clean against production inside `begin … rollback`, and the rollback was VERIFIED by a `pg_proc` count afterwards rather than trusted to a clean envelope.

## IOS-043 · The morning repair — the sentence that scrambled, the name the table used, and a boot that failed still knowing things — **P1 · BUILT 2026-09-06 (D263, D264; the overnight stress pass's fix list — six lenses, 21 P1s, no P0)**

**Decision.** Fix every P1 the stress pass raised before the owner archives, and fix nothing else. Twenty-one findings, all reachable by a beta tester, none of them requiring a new object.

**The sentence that read out of order (SB-1 / OE-5).** Home's wire-empty line is ONE sentence split into a head and a door, laid out as an `A11yStack` — an `HStack` at every reading size, baseline-aligned to the FIRST line. The no-season branch is a 44-character head and an 18-character door and they cannot share one 350pt line, so a brand-new golfer at the DEFAULT type size on the widest phone read *"No rounds from your buddies yet. Post   add some buddies. / one, or"*. Every walked account had a season, which is why it survived six audits. Both branches — the empty wire and the failed read — are now one composed `Text` inside the Button, so the sentence wraps as prose, stays one tap target, and VoiceOver speaks it as one element. Same class as DEF-1/DEF-2: geometry no test can see.

**DEF-3, closed on both sites.** The clash card said `you`, the story line said `Jerecho Fischbeck`, and the table row under it said it again — one screen, three names for one person. `StandingsStory.displayName` / `.rowName` / `.leads(_:viewer:)` resolve the viewer's own SOLO rung to **You** in the sentence and **You · &lt;name&gt;** in the row, the form `ClimbView` has always used; a squad keeps its name. Second person takes its copula. The web takes the same edit against `window.CS.member.id`, and the row's `data-squad` attribute keeps the raw name so the click handler that looks a golfer up by it is untouched.

**A cold boot with no signal is no longer the whole app (OE-1/OE-2).** `BootFailedView` now renders the App Group `DispatchSnapshot` — the last Home this phone actually loaded — under its own `AS OF` line, and a door onto `KeptCoursesList`, which was `private` inside the settings screen and therefore behind the very boot that failed. `Sign out` asks first and names what is lost, and `CourseBookStore.forget()` has moved off sign-out and onto `claim(_:)` — a DIFFERENT golfer signing in, which is the event D261 actually cares about. Photographable only by code trace: no sudo on this Mac, the simulator shares the host network, and no Network Link Conditioner profile is installed. **Stated as code-verified, not seen — and it is the first thing to try on the physical device in the morning.**

**Nothing set a request timeout (OE-4).** No `URLSessionConfiguration`, no `timeoutIntervalForRequest`, no reachability monitor anywhere in `apps/ios`, so every call inherited the 60-second default and the app only learned it was offline when something failed. The main client gets 12 seconds — an INACTIVITY timer, so an upload that keeps sending is unaffected. The realtime client keeps `URLSession.shared` on purpose: an inactivity timeout on a websocket closes a quiet channel. And `applyTee` reads the phone's own card FIRST, before `cacheCourse` / `api_course_tees` / `api_course_holes`, so a golfer on one bar stops scoring against `LiveCourseCard.postParStd`, a fabricated par-72 template, while three network calls time out.

**Seven of seven course cards opened on a women's tee (OE-3 / NW-2).** `defaultTee` was *the first 18-hole tee* and both fill paths sort by `course_rating desc`; a women's rating off the same tee is higher. Measured on the owner's real phone: Palo Verde Back 58.0, Gold Canyon Blue 74.5, Biltmore White 72.7, Raven White 74.2, Hunter Ranch White 76.6, Papago Blue 78.5, Troon North Silver 75.3. Nothing on the golfer card carries a gender, so the book takes the LONGEST — a fact about the course, not a guess about the golfer — and `keep()` stops writing `nil` over a yardage a `my_course_books` fill had already learned.

**A tee-name miss returned another tee's card (NW-3).** `tee(named:)` ended `?? defaultTee`, so a renamed, hand-typed or nil tee handed back the default tee's pars and stroke indexes as if they were the picked tee's — silently, with `CourseBookCopy.noCard` never firing because a card *was* found. Stroke indexes allocate strokes in Match Play, Skins and Wolf. Strict now, narrowed by rating so Palo Verde's men's `Back` and women's `Back` are told apart, and `CourseBook.swift:28`'s own law is finally true of every path.

**Money (M1, M2, M4).** One pot split, in cents, and it is the settlement's own — see D263. The phone's Other stake clamps to `WizardDials.maxStake` = 200, the database's own ceiling, and says so.

**R-M's rename reached fifteen more sentences (NW-4/NW-5).** The Ryder explainer, the Major's fine print, the join covenant's scoring rule, the season story, `head_to_head()` itself and four share captions that LEAVE the app. Check 42's frames now tolerate an adjective between the determiner and the noun; the opponent's-score sense (*the number to beat*) is kept and pinned by name. And the composer's eyebrow says **your index**, produced rather than typed — R-M's accepted cost is that the two nouns be distinguished once, and the composer is the screen that shows both.

**Skew (S-1).** `post_kudos_home()` derives EITHER key from the other now. Without it, one reaction from a new client on a league post would have made build 669's league board render *"Could not load the board."* — every post, round and announcement gone, permanently, on every load — and silently emptied Home's reaction strips.

**Gate:** preflight 0 failures 0 warnings (39 checks reported, numbered through 43); sunningdale 27; CupSeasonKit **854** · CupSeason **28** · CSDesign **14**, all passing; `web-verify` clean at 1440/390, 0 console errors, no document overflow, and the pot split asserted in the running browser over 828 combinations with 0 mismatches. Three migrations edited and each dry-ran clean against production inside `begin … rollback`, with the rollback VERIFIED by `pg_proc` afterwards rather than trusted to a clean envelope.

**Not fixed, and named rather than buried:** the P2s and P3s of all six stress reports, and the two owner actions (`my_course_books` is live in production while its migration is unapplied — `db push` heals it; and the push function must be deployed BEFORE the database). Both are in `MORNING.md`.

---

## IOS-044 · The tokens, the emitter, the two bundled cuts, and the app icon that finally ships — **P1 · BUILT 2026-09-06, Wave 0a (D268, D270, D274; owner-authorised "build it", 2026-09-06)**

**Decision.** Lay the ground the whole overhaul stands on, change no surface's composition, and prove each piece rather than assert it.

**THE EMITTER CHANGED FIRST, AND IT HAD TO.** `tools/build-tokens.mjs:117` did an unguarded `entries.find(([, n]) => n === 'glow')[2].dark`, and `:118` the same for `grad` — two tokens D270 deletes. The very first command of the overhaul threw `TypeError: Cannot read properties of undefined`, nothing regenerated, and preflight 10 would then have failed permanently on "generated tokens are stale". The block is **deleted rather than guarded**: a token that does not exist should not have a line waiting for it. In its place the emitter learned three groups — `CSTokens.Space`, `.Alpha`, `.Track` — and the reason they were missing is worth keeping: **the Swift emitter walks colours plus four groups BY NAME**, so a group added to `tokens.json` reaches `tokens.css` and `tokens.ts` for free and is *silently absent from the phone*. That is exactly how `space`, `alpha` and `track` came to be cited by every spec in the design and present in neither client. `TokenTests.theGeometryGroupsReachThePhone` is now the thing that says so out loud.

**THE BOARD FACE IS BUNDLED, AND ITS POSTSCRIPT NAMES ARE NOT THE ONES THE BRIEF PREDICTED.** `BUILD_BRIEF` §2.6 names `IBMPlexSansCondensed-SemiBold` / `-Bold`. The files' own `name` table (id 6) says **`IBMPlexSansCond-SmBld`** and **`IBMPlexSansCond-Bold`**, and the SemiBold file's family (id 1) is `IBM Plex Sans Cond SmBld` with a subfamily of `Regular`. Typing the brief's names would have resolved nothing and rendered 46% of the product's type in SF Pro — no crash, no log, D258 on a bigger surface. So: read from the file, and then held there by three things that fail rather than remember. **Preflight 38(b)** now checks `board*` beside `mono*` against the bundled files' name tables, and additionally refuses any TTF that sits in `Resources/Fonts` but is missing from `project.yml`'s **or** `CupSeason/Info.plist`'s `UIAppFonts` — one without the other is D258's other half. **`CupSeasonTests.BundledFaceTests`** resolves all five faces through CoreText inside the app host, asserts CoreText hands back the face that was asked for, asserts the board cut is measurably narrower than the system sans at the same size (a right name on the wrong cut passes every string check and fails here), and asserts the mono cut still advances uniformly. Both new preflight assertions were self-tested by breaking them.

**THE APP ICON.** The shipped build carried Xcode's blue placeholder while `brand/appstore-1024.png` — the ember flag — sat finished in the repo. `AppIcon.appiconset` now carries it as the universal 1024, plus the two iOS 18 appearances declared in `Contents.json`: **dark**, the flag in ember on transparency so the system's dark material shows through, and **tinted**, the same shape in grayscale on transparency so the system's tint takes. Both are derived from the shipped mark by an alpha mask off its own antialiasing, so the silhouette is the brand's, to the pixel. Verified by the asset catalogue compiling into the installed app and by reading all three PNGs; SpringBoard itself is not scriptable from here.

**Reversibility.** MEDIUM. The token diff is one commit and reverts as one, but ~180 Swift sites and ~270 web sites moved colour token inside it. The mapping is in D270 and is mechanical.

**Gate:** `node tools/build-tokens.mjs` clean; preflight **PASS — 0 failures, 0 warnings** (40 checks, numbered through 44); sunningdale 27; CupSeason **32** · CSDesign **16** · CupSeasonKit **887**, all passing (up from 28 / 14 / 887 — no test was deleted, two were migrated); `web-verify` clean at 1440/390 with 0 console errors and no document overflow. No migration was written, so there was none to dry-run.

---

## IOS-051 · The two capture hatches — appearance and content size, at launch — **P1 · BUILT 2026-09-06, Wave 0a (owner-authorised "build it", 2026-09-06)**

**Decision.** Two DEBUG launch arguments, and they are the smallest entry of the overhaul and the one to write first.

**Why they are not a convenience.** Every light-theme and AX3 statement in `UI_SYSTEM.md`, in all seven surface specs and in both columns of `UI_SCORECARD.md` was **computed, never seen** — and not for want of trying. `CupSeasonApp` applies `preferredColorScheme` from `UserDefaults`, so `xcrun simctl ui <udid> appearance light` changes the simulator and **never reaches this app's UI**; and `-UIPreferredContentSizeCategoryName` does not take on a SwiftUI app launched by `simctl`. Without these two arguments the Phase-7 blind review is blind on two of its ten axes, exactly as Phase 1's was.

**`-cs_dev_appearance <light|dark|auto>`** overrides the stored choice for one launch, and it wins at `@State private var appearance` rather than at `.preferredColorScheme` — so the Appearance selector in Settings shows the same answer the screen is actually rendering, and there is no second source of truth to drift. **`-cs_dev_text_size <category>`** pins Dynamic Type through `.dynamicTypeSize(_:)`, applied as a `@ViewBuilder` modifier so the `nil` case adds nothing to the view tree at all. It takes a UIKit category name (`UICTContentSizeCategoryAccessibilityL`), a SwiftUI name (`accessibility3`), or the shorthand the reviews actually use (`AX3`), because a hatch nobody can spell is a hatch nobody uses. Both are `#if DEBUG` and return `nil`/`nil` in Release by construction.

**Proved, not asserted.** Home was photographed three ways off one build in one minute: `w0a-home-dark.png`, `w0a-home-light.png` — genuinely the warm almanac stock, not a dimmer — and `w0a-home-ax3.png`, where the masthead's dateline drops to its own line and the ME strip's two labels wrap, which is the layout the design says it is and which nobody had seen.

**Reversibility.** TRIVIAL — two computed properties and one modifier, none of which exists in Release.

**Gate:** as IOS-044, same commit.

---

## IOS-045 · `CSDesign` becomes the product's only container and control vocabulary — **P1 · BUILT 2026-09-06, Wave 0b (D265, D266, D267, D269, D271; owner-authorised "build it", 2026-09-06)**

**Decision.** Build the whole vocabulary before any surface is rewritten, prove it on a screen rather than in a changelog, and name the wave that removes every shim it leaves behind.

**THE THREE CONTAINERS, AND A DEFECT THEY FOUND IN THEMSELVES.** `CSBand` · `CSRule` · `CSPanel` · `CSLeaf` · `CSPlate` · `CSObject` replace the card. The first build drew **one leaf per paragraph** — the scorecard grid on one sheet of paper and its legend on a second, 12pt below it — because a SwiftUI `@ViewBuilder` that yields more than one child is a `TupleView`, and a modifier applied to a `TupleView` applies to **each child**. `CSBand` and `CSObject` had the same bug. No unit test would have caught it and the first screenshot could not miss it; every container now composes its children into a stack before it draws anything. This is the harness earning its keep on its first run.

**THE THREE TIERS ARE `ButtonStyle`s AT LAST.** `CSPrimaryStyle` / `CSSecondaryStyle` / `CSTertiaryStyle(.live|.content|.toolbar)` / `CSDestructiveStyle`, plus `.csPrimary` / `.csSecondary` / `.csTertiary(_:)` / `.csDestructive`. `configuration.isPressed` and `isEnabled` are read **zero times** in the shipped product; that is the finding, and this is the fix. A pressed state cannot be photographed by `simctl` — there is no finger — so each style takes a `held:` flag **used by the developer harness and by nothing else**, documented as such, rather than the specimen sheet hand-copying a pressed fill, which is the exact duplication `Controls.swift` exists to remove.

**THE TYPE ROLES READ THE ENVIRONMENT'S SIZE, NOT THE DEVICE'S.** `figure`, `display` and `agate` are capped (×1.45 – ×2.2) and `Font.custom(_:size:relativeTo:)` offers no ceiling, so a capped role computes `min(UIFontMetrics.scaledValue(for:), base × cap)` itself. **`UIFontMetrics.scaledValue(for:)` with no trait collection reads the SYSTEM content-size category**, and `-cs_dev_text_size AX3` (IOS-051) pins `\.dynamicTypeSize` and never touches the system's — so every capped role would have photographed at the default size while every uncapped one grew, and the AX3 evidence would have been a lie in the direction that flatters. `CSType.renderedSize` passes a `UITraitCollection` built from the environment value; a test asserts every capped role grows between Large and AX3.

**AND THE AX3 SHOT FOUND A REAL DEFECT IN THE LINK.** The tertiary's label was `lineLimit(1)` + `fixedSize`, which is right at the reading sizes and at AX3 pushes 300pt of label off a 362pt measure — taking the page's whole left edge with it, so the specimen photographed as a screen sheared down its left side. The label now takes two lines and the rule spans the block. That is one of the two things IOS-051's hatches exist for, catching something on its first use.

**THE FROZEN PAIR, AND THE LINE THAT WOULD HAVE BROKEN IT.** `UI_SYSTEM` §6.2a specifies `pig[hash(id) % 6]`. Swift's `Hashable.hashValue` is **seeded per process**, so writing that line literally would have reseated every golfer on every cold launch — the precise drift the clause forbids, arriving through the one line that looks most like the spec. It is FNV-1a over the UUID's sixteen bytes, and the test pins the seat for a fixed id.

**WHAT IS KEPT, AND UNTIL WHEN.** `CSCard` (20 sites), `CSStat` (12), `CSEmptyState` (6), `CSButton` (90) and `CSHairline` (34) keep their declarations, unchanged, and each names the wave that removes it: **Waves 1–7 take the seven surfaces, Wave 8 the remainder, Wave 9 deletes the five declarations.** A shim with no named removal is not a shim; it is a second system. `LINT-30` counts them at **157** and fails on a rise, so nothing new can be written against them while they wait. `CSFont`'s 857 sites are the same story and are migrated role by role with the surfaces.

**THE HARNESS IS THE WAVE'S ONLY ACCEPTANCE EVIDENCE.** `-cs_dev_developer <a|a2|b|c>` renders every component in every declared state, arranged as the three system specimens are so a shot lands beside `cs-system-{a,b,c}.png` and reads row for row. It takes a page argument because a simulator driven by `simctl` has no finger and cannot scroll. Shot nine ways — four pages dark, four light, three at AX3 — and every one was looked at.

**Reversibility.** MEDIUM. Ten new files in `CSDesign` and four changed ones; the eleven `CSFace` call sites and the two Home face sizes are the only app-side edits, and both are mechanical. Nothing in `CupSeasonKit` moves.

**Gate:** `node tools/build-tokens.mjs` reports `same` on every artefact; preflight **PASS — 0 failures, 0 warnings** (45 checks, 14 of them `LINT-nn` with baselines); sunningdale 27; **CupSeason 35 · CSDesign 49 · CupSeasonKit 887 = 971 tests in 166 suites**, up from 935 / 157 at Wave 0a and 929 / 156 at the design commit. No test was deleted; the two `CSFace` assertions were migrated to the frozen-pair API. No migration was written, so there was none to dry-run.

---

## IOS-046 · Home is an edition — a masthead, five wire weights, and a face in every row — **P1 · BUILT 2026-09-06, Wave 1 (owner-authorised "build it", 2026-09-06)**

**Decision.** Replace Home's view body and its seven private row types with an edition: a masthead, one weight-1 lead, a numeral rail, a wire that runs at five deliberately unequal weights, and a floor of four doors. **The load is untouched** — `HomeModel`, `LoadKey`, `ranked()`, `feed(upcoming:spent:)`, `toggle(round:emoji:)`, the lead → deck → strip precedence, the `suppress` union, the failed-vs-empty branch and the widget snapshot are the same code they were this morning.

**THE WAVE IS ONE SENTENCE: DO NOT MAKE EVERY FEED ITEM VISUALLY EQUAL.** The audit's finding is that ceremony night and a brand-new account render as the same card with a different eyebrow word, and that half a premium feed's slot is four `N league notes` rows with chevrons — a database's `GROUP BY` drawn as a feed. So the deck is deleted as a visual object: ranked items 2–5 no longer render as four smaller copies of the lead, they enter the wire at the weight their kind earns. `HomePage` is where that decision lives, it is pure, and `HomePageTests` argues with it in clauses rather than in screenshots. Ceremony night is now a full-bleed band on the pinned `ceremony` ground; a brand-new account is a drawn scorecard, a serif sentence and one primary. Photographed side by side they share a masthead and nothing else.

**THE WORDMARK CHANGED FACE, AND THAT IS A BRAND DECISION MADE IN THE OPEN.** Wave 0b cut `CSMasthead` in Plex Mono 600 at 0.32em — the setting `brand/README.md` states, and the two lockups and the og-image are generated from it — reasoning that re-cutting it would ship two wordmarks. `surfaces/home.md` §1.1 and all five artboards set it in the board face, tight, because a 0.32em mono lockup reads as *a logo pasted onto a screen* where the board cut reads as a masthead. The artboards won. The generated lockups are untouched, so the trade is real and stated: **the wordmark on Home is now set differently from the wordmark in an email.** The pennant went with it — `LINT-28` reserves the flag to the tab band and the app icon, and the masthead was quietly the third site.

**THE TAB BAND IS THE PRODUCT'S OWN NOW, AND FOUR PIECES OF MACHINERY DIED WITH THE PILL.** The system bar had to be measured at runtime by walking the view hierarchy for a `UITabBar` (`CSTabBarProbe`), its ⊕ had to be a tinted `UIImage` because a `tabItem` cannot be coloured, its long press had to be attached to that live bar by sorting subviews on a class-name substring (`CSTabBarLongPress`), and every page had to be inset by the shortfall so the floating pill did not guillotine it. `CSTabBand` is a sibling in the stack: nothing floats, so nothing is guillotined, the ⊕ is a drawn ember glyph with no fill and no disc, and the long press is a gesture on a button. `csTabBarRoom` / `csTabBarEdge` / the probe / the recogniser are unused and named for Wave 8.

**AND `ranked()` LOST TWO LINES, ON PURPOSE.** It dropped every `invite:` route and every `friend:` key because the phone answered both IN PLACE, through `InvitesBanner` and `BuddyRequests` — the r10 boxes with 60%-ember borders the design deletes. With the banners gone the items have to render, or **an invitation to a season is the one Home state that shows a golfer nothing at all**: under `-cs_dev_home_state invited` the rank-1 item was filtered out and the banners, which make their own reads, stood down for the fixture. It rendered `find_golfers`. It now renders the invitation, and the two clients draw the same Home from the same payload, which is what D234 asked for.

**SIX DEFECTS THE SCREENSHOTS FOUND AND NO TEST COULD HAVE.** `.frame(maxWidth:)` EXPANDS, so a `CSPanel` asked for 84 and then handed a 96 ceiling drew at 96 — the lead chip was 14% wider than the design. The tertiary's underline is drawn by a `Rectangle` in a `VStack`, which is proposed the whole column, so the rule ran the full measure and read as a divider under a paragraph rather than as an underline under a link. The live dot is the glyph family's one filled mark at 0.3 of its box, so `size: .inline` drew it at 3.9pt against a specified 7. And at AX3 the band's five agate labels broke in half and printed `COMP/ETE` and `GOLFE/RS` under their own glyphs — §16.3's answer is that the labels drop and the glyphs grow to 28, and it is built.

**The AX3 shots then found two more, and the second is the serious one.** The chip's own frame pinned its CONTENT to 84 while its ground grew, so `OF EIGHT` wrapped by character and printed `OF / EIG / HT` under a `4TH` — a panel that breaks its one label has stopped being a panel, and at the accessibility sizes it takes the measure. And **the serif did not scale at all**: `Font.system(size:weight:design:)` has no `relativeTo:`, and neither does any other initialiser that takes a point size AND a design, so `lead` and `story` stood still at every content size and the product's one serif sentence — the thing the screen slows a golfer down for — rendered **smaller than the sans standfirst under it** at AX3. It is scaled through the same `UIFontMetrics` path the capped roles use, read against the environment's size so `-cs_dev_text_size` and the roles agree, and uncapped, because a sentence is read for meaning. That defect shipped in Wave 0b's `Type.swift` and had never been photographed.

**ONE EMBER OBJECT PER HOME, BY CONSTRUCTION.** `MeStrip`'s shipped `emberKey` knew only what the LEAD offered. It is now `HomePage.offered`, which is the union of what the lead, the empty block and the wire's empty offer, keyed on the ROUTE and never on the prose — and `.declare` and `.composer` are ONE door, because "Add my round" beside "Put a round on the schedule" is the same act twice at two weights. If anything above the floor carries the live metal the floor lights nothing; if the page's own primary is `ADD MY ROUND` the floor collapses to `SOMETHING ELSE`. That closes H-06 without a review step.

**WHAT DEGRADES, NAMED RATHER THAN FAKED.** Weight 3, the course discovery, has **no producer** — `home_dispatch` emits no `course` kind and the product has no ratings table at all (`home.md` §5.1–5.2) — so the row type exists and never renders, and Home runs on four weights. The takeover band's course contour is `D272` rung 3 and Wave 4's renderer, so the band is the ground and the words; **a gradient wash is not one of the three legal images** and does not stand in for it. The desk's membership payload carries no `standing`, so the web's lead has no chip — and §1.2's own rule is that a chip is never invented to fill the column.

**Reversibility.** MEDIUM. Three Home files replaced and two deleted, four `CSDesign` components re-cut, and the tab shell re-hung. The load, the producers and every RPC are untouched, so a revert is a view revert.

**Gate:** `node tools/build-tokens.mjs` reports `same` on every artefact; preflight **PASS — 0 failures, 0 warnings** (45 checks, 15 `LINT-nn`, **nine baselines lowered** as Home's seven row types, two banners, deck card, ME strip and the system tab bar left the codebase); sunningdale 27; **CupSeason 35 · CSDesign 49 · CupSeasonKit 901 = 985 tests in 167 suites**, up from 971 / 166 at Wave 0b — no test deleted, fourteen added. No migration was written, so there was none to dry-run.

---

## IOS-047a · The card is one object, pushed, at 362 × 312 in every theme — **P1 · BUILT 2026-09-06, Wave 2 (D272; owner-authorised "build it", 2026-09-06)**

**Decision.** One object, one chrome, one ratio, one meta string. `CredentialCard`, `CredentialFace`, `TourCardSheet` and `FoundingTag` are **deleted**; `CSCredential` is the card, `PersonPage` is the surface, and the peek from a round card presents that same surface rather than a second one.

**GP-16 IS A DEFECT YOU COULD ONLY SEE BY OPENING THE SAME GOLFER TWICE.** A name tapped on the board opened a 16:10 panel with `@handle · city · est. Aug 2026` under it; the same name tapped from a round card opened a 1:1 panel with `@handle · city · home course`; the You tab drew a third with a `CSHero` wash, a gold spine and a scrolling row of emoji milestone chips. Three chromes, three ratios, three meta strings, one golfer. There is now **one** `CSCredentialGolfer`, built by **one** producer (`CredentialCopy.identity`), drawn by **one** view, at **362 × 312 measure-relative** — 335 × 289 on an SE — on the `ceremony` ground in both printings, because a physical card does not change colour when the room does (audit D8, answered on a device rather than in arithmetic).

**THE MARKER FLOOR IS A DESIGN, NOT A DEGRADE, AND THAT COST A NEW COMPONENT.** Most golfers have no photograph, so the state most of them are in is the one the old card handled worst: a 28% ember radial wash with the marker blurred behind the record. `CSCrestPlate` draws their **own mark at 190pt in `crest`, bleeding off the plate's right edge**, over **their home course's contour** — and the contour had to be built, because D272 rung 3 did not exist and a gradient is the one image state the ladder bans by name. `CSContour` is a seeded value-noise field sampled at six isolevels by marching squares over a 32 × 32 grid, **not nested ellipses**, which is the difference between two courses looking like two places and looking like two radar sweeps. `csContourSvg` is the same algorithm in the browser, seeded by the same FNV-1a constant, verified by value in `ContourTests` and held together by preflight 46.

**THE SCREENSHOTS FOUND THREE DEFECTS THAT NO TEST WOULD HAVE.** (1) A `scaledToFill` image has **no intrinsic ceiling**, so the plate's `ZStack` sized to the image and the name, the identity line and the gold slot were laid out hundreds of points below the card's own foot — present, addressable by VoiceOver, and invisible. The first photograph card had no name on it. (2) `frame(maxWidth: .infinity)` inside an `HStack` **does not make equal columns**: it hands each child its ideal width and redistributes the remainder, so the second figure's label started 16pt left of its own numeral. The strip measures the object once and gives each cell a third of it. (3) **At AX3 the copy band exceeded 45% of the plate** and printed a 60pt name straight through the credit line and the slot — which is exactly the case §6.7 predicts, and the fix is the shipped `CredentialFace.riding` behaviour kept verbatim: the identity block drops off the photograph onto the card's own ground beneath it. Legibility beats composition, and it is now a rendered fact rather than a claim.

**WHAT THE PAGE GAINED BY LOSING ITS FORM.** `PersonPage` was a credential and then eleven settings-list rows — `YouDoorRow` for the head-to-head, for THIS SEASON, for each of R-F's three lengths; `MathRow` for the career table. It is now the object, one sentence, **one primary**, and a printed record: FORM as five grosses on one rule with the best in gold, the league slat, the head-to-head as an overlapping pair, the courses, and D150's overlap sentence — which `tour_card` has returned since D150 and both clients threw away. **R-F survives in full**: all three lengths are still always offered, asked as one step, from the page's one primary through `LengthStep`, which is where they should have been. P-17 survives too, moved off the chrome to the page's foot so the toolbar can carry the one trailing action the design gives it — `SHARE`, spelled, in both themes, because a `…` on the most shareable screen in the app hides the only verb that matters.

**AND ONE RULE WAS SUPERSEDED IN THE OPEN.** `UI_SYSTEM` §6.5 row 3 says the medallion is present only over a photograph — "the crest, or the corner, never both". The blind review overturned it (§6.2a's revision: *"on Galen it is a small gold-ringed medallion in the corner; on Tash it is a giant flat glyph filling a third of the card"*), §19(i) accepts the crest and the medallion as **the same glyph at two scales**, and `player-card-marker.png` draws both. The mockups and the later ruling win; the older sentence in the anatomy table does not, and this is where that is written down. The consequence is stated rather than buried: **a card with nothing earned still carries one gold ring**, so "no gold anywhere on the card" is now "no gold FIELD anywhere on the card".

**Reversibility.** MEDIUM. Four app files deleted and four rewritten, `CSCredential` re-cut, one new component (`CSContour`) and two new producers. `PersonModel.load`'s card → bag → head-to-head cascade, every RPC and every copy producer it reads are untouched, so a revert is a view revert.

**Gate:** `node tools/build-tokens.mjs` reports `same` on every artefact; preflight **PASS — 0 failures, 0 warnings** (46 checks, one new — *one credential, two clients* — and seven `LINT-nn` baselines lowered as the four deleted files and the old board row left the codebase); sunningdale 27; **CupSeason 35 · CSDesign 58 · CupSeasonKit 914 = 1,007 tests in 171 suites**, up from 985 / 167 at Wave 1. No test was deleted. No migration was written, so there was none to dry-run.

## IOS-047b · Wave 3 — the identity page, not an account screen — **P1 · BUILT 2026-09-06 (owner-authorised "build it", 2026-09-06)**

*IOS-047 is the entry the build brief scopes to Waves 2–3. Wave 2 built the object; this is the surface it sits on, and it is recorded here rather than as a new number because it is the same decision — one golfer, one anatomy, one page — carried through to the tab root, the archive and the rivalry.*

**THE YOU TAB WAS AN ACCOUNT SCREEN WITH GOLF NUMBERS IN IT.** Eight `YouStatRow`s — label left, value right, in the grammar of Settings — carried the rounds, the best, the average and the money. `LifetimeTiles` put a grid of tiles inside a card. `SeasonStatsStrip` repeated three of the same figures under a different head. **Where a golfer sat in their season was not on the page at all**, and their recent rounds were a list with a delete × on every row, which is an admin tool on an identity page (YRS-01, YRS-02, YRS-07, YRS-22). It is now the credential, the season, the rivals, the form, the courses and the record — §D-2's order, which the surface spec argues for by name because `BRIEF` §10 puts COMPETITION second and `UI_SYSTEM` §15.2 leaves it out entirely.

**THE COURSES WERE ALWAYS IN THE PAYLOAD.** `tour_card` has returned `courses[]` and `shared_courses[]` since **D150** and the phone has discarded them on every surface ever since. The You root reads the SAME producer the person page already calls — a re-use, not an endpoint — so eleven courses a golfer kept are finally on their own page, under an `RDS` head, because an unlabelled number column is a defect and not a minimalism (§16A.3).

**THE FINISH IS FIELDS NOW, AND NOT ONE READ WAS ADDED TO GET THEM.** `LeagueRecordRow` carried one pre-formatted string — `"SEASON II · 3RD OF 12 · 41 PTS"` — and a leaf cannot set a figure with an ordinal rider, or hang an earned rule, off a string. `LeagueRecord.finish(...)` returns `(finish, of, won)` from the **same standings `LeagueRecord.line` already ranks and threw away**. `won` is true only on a **complete** season: leading in week nine is not a title, and a gold rule under a live table would be the product telling a golfer they had won something they had not.

**THE EARNED MARK, TIGHTENED, AND GOLD ON PAPER GOT ITS OWN TOKEN.** §9.8 gave a gold rule to a win *or* a podium; 2nd of 8 is not silverware, so a **win takes gold and a podium takes a 2pt ink rule** (D-5). And the gold under a won season is `object.leaf-gold` — the light theme's `#795912`, **pinned in both printings**, because the leaf does not invert and dark gold at 2pt on bone is a pale smear (D-6). It is a token rather than `CSTokens.light.gold` reached across the palette in Swift and a hex hard-coded in CSS: one value, one name, both clients, and the single-source check covers it. `CSTokens.tokenNames` moves 80 → 81 and the canary test says why.

**TWO ACHIEVEMENTS MAY NEVER SHARE A GLYPH — AND EIGHT OF THEM DID.** The display case drew 🏆 for a Cup, a major, an event and a points crown; 🎯 for both *Broke 100* and *Broke 90*; 📈 for both *4-week streak* and *8-week streak* — each pair under a subtitle that differs by one word. `CSTrophyMark` draws them in the family every other glyph in the product is drawn in, and where two are the same SHAPE at different values the **numeral inside the mark is the difference**. `TrophyMeta.glyph` names the mark by string, because the Kit sits below `CSDesign`; `TrophyMarkTests` is hosted by the app — the one target that can see both packages — and fails if a key the Kit emits has no mark, which is D258's failure mode on a new vocabulary. **None of them is the pennant** (`LINT-28`): the first draft put the flag on LOW ROUND OF THE SEASON with the Compete tab's identical flag 400pt below it.

**THE HEAD-TO-HEAD HAD NEITHER GOLFER ON IT.** GP-20, the audit's biggest personality opportunity in the social slice, was one sentence long. The page now opens on the christened name in gold, `YOU AND GALEN` in `display` 34, and **the graphic**: two 64pt faces at the ends of the measure with `6–5` between them as `figXL` 56 on a 2pt rule. Under it **the meeting tape** — every meeting a tick, above the rule if you won it and below if they did, oldest first, yours filled and theirs outlined, a halved meeting a flat bar on the rule because a half belongs to neither side. **No legend**, which §9.10 bans in the same breath as an axis; the row names ride the graphic's own right margin and one line under it reads *One square is one win.* The six facets moved **behind a door**: the hero, the tape and a leaf whose rows sum to 6–5 is the same fact three ways in one viewport.

**AND THE PAGE HEADER STOPPED BEING A GRADIENT.** `CSPageHeader` drew a 28 × 3 two-stop gradient tick over a serif title on every pushed screen. A gradient is the one image state D272's ladder bans by name, and it was the only thing on a header that changed colour with a LOOK — so a page's name meant "which league you last opened" rather than "which page this is". It is `display` 34 with an `agateS` dateline flush right (§12.2), and the AX3 stacking branch survives verbatim because it is a "what works".

**Reversibility.** MEDIUM. Three app files deleted (`YouSections`, `YouHero`, `TrophyCaseView`), four rewritten, one added (`ProfileBlocks` — the six blocks the You root and the person page now share rather than each cutting their own), two components added to `CSDesign` (`CSRecordLeaf`, `CSTrophyMark`) and two re-cut (`CSTape`, `CSPageHeader`). Every RPC, every copy producer and the Y-17 partial-load contract are untouched, so a revert is a view revert plus one token.

**Gate:** `node tools/build-tokens.mjs` reports `same` on a clean tree; preflight **PASS — 0 failures, 0 warnings** (nine `LINT-nn` baselines lowered as the three deleted files and eleven emoji left the codebase); sunningdale 27; **CupSeason 39 · CSDesign 64 · CupSeasonKit 919 = 1,022 tests in 175 suites**, up from 1,007 / 171 at Wave 2. No test was deleted — `credentialLines` was MIGRATED to the thing that replaced the chips it asserted. No migration was written, so there was none to dry-run.

## IOS-048 · Wave 4 — the course is an editorial object, and the rating is content — **P1 · BUILT 2026-09-06 (owner-authorised "build it", 2026-09-06)**

*The wave the build brief scopes to the course. It carries D272's imagery ladder to the surface the ladder was written for, and D275's rating to the client, with the server half written and unpushed.*

**THIS IS THE SURFACE THE BLIND REVIEW FAILED, 0/3 ON PROUD-TO-POST AND 0/3 ON BELONGS-IN-THE-CATEGORY**, and all three reviewers gave the same reason: *there is no photograph of a golf course anywhere in five course renders*. What a golfer actually got was worse than the renders — a `CSSheetHeader`, a cache disclaimer as the first paragraph, a 2×2 `LazyVGrid` of four `CSStat` tiles (the audit's named "canonical SaaS dashboard pattern", scoring **brand 3 / emotion 2**, the two lowest cells on the board), a tee picker, and a `Done` in ember. It is now a **pushed screen**: the plate, the facts as ONE LINE OF TYPE, the rating in `ink`, the golfers who have played it named in a sentence, the front nine printed on a leaf, and the rounds posted here.

**THE LADDER IS BUILT, AND RUNG 1 IS REAL.** `CoursePageRepository` reads the rounds posted at a course straight off `rounds` — `api_course_id` has been there since `20260714050000`, `photo_path` since `20260718045514`, and `rounds_read` has always been *mine plus anyone I share a league with* — so the hero photograph, the faces, the sentence that names them, your best here and the rounds list are **one filtered select under the RLS this database already enforces**. `course.md` §7.3 asks for a `course_page` RPC and says of these facts that "none of it is currently reachable for a course other than the viewer's own"; the second half of that sentence turned out not to be true, and the wave stayed client-side because of it. What it costs is stated in the file: the set is league mates and me, **not** accepted buddies (who share no `league_members` row) and not event field mates — widening it is the RPC, on the server's own predicate.

**AND THE DRAWN CARD IS REAL PAR AND REAL STROKE INDEX, OR IT IS NOT DRAWN.** *Fake data as ornament is less premium than a plain colour* — three reviewers, three sentences. `CSDrawnCard` takes a **required** par per hole: a hole with no par is not a hole it can draw, and a defaulted 4 would be the invention the component exists to refuse. Width by par, height by yardage where the book carries one and **by par where it does not** (D-2, and `my_course_books` does not select `api_course_holes.yardage`, so that is the state the phone is actually in), **gold on the #1 stroke hole**, numbered 1–18 beneath — and the sentence that explains the gold bar is the page's own facts line, *"the 6th plays hardest"*, twenty points below it. At ≤64pt it renders the **front nine only**, nine bars at three heights by par, because eighteen 3pt bars in a 44 × 26 box are three near-identical grey combs. A course with no cached card at all shows **no thumbnail**: the row's left column collapses and the name sets flush.

**A DRAWN PLATE STANDS ON THE CEREMONY GROUND IN BOTH PRINTINGS, AND THE FIRST BUILD PROVED WHY.** §4 says the drawn card fills the plate with the name reversed out *exactly as over a photograph* — and a name cannot be reversed out of the light theme's paper. Drawn on `bg1` in light, the `.title` scrim turned the whole plate into a white-to-black ramp: **a gradient wash**, the one image state D272 bans by name, arrived at by accident and visible only in a screenshot. The plate is pinned the way the credential is.

**THE RATING IS `ink`, IN A CONTROL AND OUT OF ONE, AND THERE IS NO GOLD ON THE PAGE BUT THE ONE BAR.** D275 records the argument: nobody earns a course's rating. `RateCourseSheet` is the fitted sheet — a continuous drag rail mapping *x* to the nearest half, a **−½ / +½ stepper pair at 44pt** beside it because five stars × two halves is ten targets and 10 × 44 = 440pt against a 362pt measure, `.accessibilityAdjustable` at 0.5 increments, and the figure at `figXL` on a 2pt `ink` rule. **The migration is written and not run**, so every read returns `unavailable`, both clients draw the full-size unfilled rail and `NOT RATED · THE FIRST RATING SETS THE NUMBER`, and the sheet says plainly that a rating cannot be saved yet rather than letting a golfer drag a rail into an error.

**A FILLED STAR IS FILLED.** `CSStarRail` masked a *stroked* glyph, so "not rated" and "four and a half" drew the same picture in two greys. The star is the one mark in the drawn family with a filled state, because filling it is what the rating means; `CSStarShape` is the same path `CSGlyph.Name.star` draws, closed so a fill is legal.

**THE COURSES LIST STOPPED BEING A SETTINGS PANE.** `KeptCoursesList` was a section head and a stack of 44pt rows with an ember `SEE` label on a row that was already the door, embedded in Card & settings and mounted again in a sheet on the boot-failed screen. It is `CoursesScreen` — `display` 34, one agate line, one chip row, a drawn thumbnail on every row, the name wrapping to two lines before it truncates, and **your best gross in the trailing column under a head that appears only once a row carries a figure**. Settings keeps one row that pushes it. The boot-failed path carries its own stack and still needs no session.

**Reversibility.** MEDIUM. Two app files deleted (`CourseCardSheet`, `KeptCoursesList`), four added (`CourseScreen`, `CoursesScreen`, `CourseCardLeaf`, `RateCourseSheet`), one component file added to `CSDesign` (`Course.swift` — the drawn card, the star rail, the rating, the plate, the facts line and the quote, five of them moved out of `Chrome.swift` and built out), one producer added to the Kit (`CoursePage.swift`), and the course's presentation changed from a sheet to a push on five stacks. Every RPC, every copy producer, `CourseBookStore` and `CourseDisk` are untouched, so a revert is a view revert plus one route.

**Gate:** `node tools/build-tokens.mjs` reports `same` on a clean tree; preflight **PASS — 0 failures, 0 warnings** (three `LINT-nn` baselines lowered as the two deleted files took a `Done`-in-ember toolbar, four `CSStat` tiles, a `CSFine` spinner and an off-token radius out of the codebase); sunningdale 27; **CupSeason 39 · CSDesign 72 · CupSeasonKit 930 = 1,041 tests in 180 suites**, up from 1,022 / 175 at Wave 3. No test was deleted — neither deleted view had one. The migration `20261007090000_what_your_golfers_think_of_a_course.sql` is **written and NOT RUN**, and there is no local Postgres in this session to dry-run it against.

## IOS-049a · Wave 5 — a season is a story with a leaderboard — **P1 · BUILT 2026-09-06 (owner-authorised "build it", 2026-09-06)**

*The wave the build brief scopes to the season. It carries D273's ink laws to the surface money actually lives on, and D267's rank rail and rule-and-figure to the surface they were designed for.*

**ONE STANDINGS OBJECT, NOT THREE.** The shipped page rendered `StandingsTableView`, then `ClimbView`, then `IndividualRaceView` — **three tables for a field of two** (CS-21, a P0) — under column heads whose swallowed `.frame(maxWidth:.infinity, alignment:.leading)` sat them 140–200px left of the columns they named (CS-18). All of it is one `CSStandingsBoard` of `CSSlat`s now, with the header row `POS · GOLFER · GAP · PTS` at **every** field size, because one blind reviewer called the unlabelled `+4 / +9 / +12` column "the single most confusing element in the set". The climb's *window* survives as a behaviour — over ten in the field the board renders the leader, the cut neighbours and you ±1 with an ellipsis rung between and a tertiary `Every golfer` — and `ClimbView.swift`, `IndividualRaceView.swift` and `ProVerbRow.swift` are deleted.

**THE TWO DEVICES THE REVIEWERS NAMED BOTH SHIPPED.** The **cut rule** — `CUT · TOP TWO PLAY THE CUP FINAL` in agate over a 2pt `ink` rule running to the margin — replaces a gold band reading "Cut line · top 2 advance", which spent the surface's one earned metal on a thing nobody has won yet and used a verb (`advance`) for a thing that is a seat. And the **month clock** replaces `PressMeter`, a 6pt three-stop `warm→hot→fire` capsule with no head that the audit read as a warning bar: `CSSeasonCalendar` is one 20 × 8 bar per season week, grouped by the calendar month that holds most of it, played `mut`, ahead `rule`, and **one live cell in `brand` at 12pt**, with the live month named and its days left beside it. Countable, unmistakably a scorecard rather than a health bar, and one object answering three questions.

**A WEEK BELONGS TO THE MONTH THAT HOLDS MOST OF IT, AND THE FIRST BUILD PROVED WHY THAT MATTERS.** Grouping by the week's FIRST day — which is defensible, and was the first cut — put the live tick at the end of the August group while the label under September was the one wearing the ember. The graphic pointed at two different places at once, and only a screenshot showed it. `SeasonCalendarMath.months` now samples each week's **median day**, the fourth of the seven, and `csSeasonMonths`' twin on the desk does the same.

**GOLD APPEARS EXACTLY TWICE AND THEY ARE THE SAME FACT** — the leader's rail field and the pot — which is `LINT-17`'s one sanctioned exception, whitelisted by name (D-6). The leader's **points** are `ink`, not gold: the rail is the metal, and §1.4's stray "gold ink only for the leader" loses to the budget table and to all six artboards. Ember appears twice and they are one clock: the live eyebrow's dot and the current week's tick.

**THE LEADER'S SLAT IS THE BOARD'S HERO, AND IT IS NOT A CARD.** `CSSlat` gains one second geometry, used once per table and only on rank 1: 74pt against 50, a 38pt face against 30, and the total at `figure` 40. No radius, no border, no fill, same rail, same columns — which is what "the current leader should visually matter" asks for without a container (D-5).

**THE RAIL CAN STAND ITS NUMERAL DOWN, AND THAT IS A BUG THIS WAVE FOUND IN ITS OWN SCREENSHOT.** `RankFlipText` (SF-6's split-flap, kept verbatim — the audit calls it "a real ceremony") is drawn by the SURFACE over the rail; the rail's own numeral stayed underneath, aligned invisibly at the reading sizes and **stacked visibly at AX3**, which is how `02 / 02` reached a photograph. `CSRankRail(_:field:hidesNumeral:)` paints the field and draws nothing.

**THE POT IS A PRINTED LEDGER.** D273's leaf, with the sign as a word and no colour on it at all. The gold-spined card, the serif trio in gold, the `✓` at 50% opacity, the handshake emoji in a stroked circle and the `✕`-as-a-button are all gone, and `PotMath.trioCents`, `SeasonFacts.owe` (built, tested and called from **nowhere** in the shipped UI until now), `PotPassCard` and `PricingPotFinePrint` are consumed unchanged.

**A CAPTURE FIXTURE, NAMED, BECAUSE THE ACCOUNT CANNOT SHOW THE WAVE.** The signed-in simulator plays a **two-golfer free league**: no cut (it needs a field over two), no pot (L-10 makes the section absent at `$0`, correctly), no squad table. `-cs_dev_season_fixture [squads]` seeds the Fellas — eight in at $60 → $480 → $288 / $120 / $72, the blind review's own arithmetic — or the Dew Sweepers at twelve in four squads. DEBUG only, never written to the server, same posture as `-cs_dev_home_state` and `-cs_dev_h2h_fixture`, and every shot taken through it is a fixture rather than anybody's real season.

**Reversibility.** MEDIUM. Three app files deleted (`ClimbView`, `IndividualRaceView`, `ProVerbRow`), one added (`SeasonFixture`, DEBUG-only), five rebuilt (`SeasonPage`, `SeasonPhases`, `StandingsTableView`, `PotPane`, `SeasonStoryPane`), one producer added to the Kit (`SeasonBoard.swift` — the month grouping, the clause, the gap, the count slots, the countdown and the pot's caption, all pure and all tested), and `CSSectionHead`, `CSSeasonCalendar`, `CSSlat`, `CSRankRail`, `CSCut` and `CSStandingsBoard` changed in `CSDesign`. Every RPC, every mechanic and every copy producer this surface reads are untouched, so a revert is a view revert.

## IOS-049b · Wave 6 — the event is a title card, and the side is carried by the roster — **P1 · BUILT 2026-09-06 (owner-authorised "build it", 2026-09-06)**

*The second half of the build brief's IOS-049. It carries the ceremony ground and the rule-and-figure to the one surface whose character is a moment, and it closes the single defect two of three blind reviewers filed against this deck.*

**§15.5a IS THE WAVE.** *"A Ryder screen where you cannot tell teams apart has failed at its one job."* Sides were two 14 × 4pt colour ticks that also collided with the dateline, over six roster discs coloured by **personal marker** — identity, not side. `sq0` and `sq3` are **1.58:1** apart, so on a bright screen, through a greyscale filter, or with achromatopsia the two teams were one team. The roster is now **two named groups**, each under a full-width 3pt rule in its squad colour with the squad's name in `agateS` beneath it, and **every disc carries a 2.5pt ring in its side's colour while keeping its own pigment and glyph**. Identity and side are two channels and stay so: re-tinting a marker by side would trade one legibility failure for a worse one. One change, three problems — the key goes, the collision goes, and a stranger can name the sides in a second.

**A COUNTDOWN IS NOT A SCORE.** `3½`, `2½` and `2 DAYS LEFT` sat under one ember rule "as if they were one class of number". `CSScoreRail` carries the two side scores; the deadline moved into the live eyebrow — `Live · week 2 of 3 · 2 days left` — where the other time facts already are. A callout keeps its clock in the rail, because a callout has no week structure to put it in and its three numbers are *his, mine, and how long*.

**THE HEAD IS AN OBJECT, NOT A CARD.** `EventHeaderRow` set the event's NAME as a 12pt tracked-caps eyebrow with a **gold** status chip beside it (gold on a status is not "earned"), over a `CSCard` holding `A 6½ – 4½ B` inside a border, over 9pt `RoundedRectangle` duel rows — box inside box inside box, and the one screen in the product where the score of a live competition was smaller than the label above it. It is one full-bleed `ceremony` plate now, running under the status bar, with the title at `display` 34, one dateline, and the whole field on it. `EventHeaderRow`, `EventFineCard` and `EventSeg` are deleted; `EventTeamSwatch` survives as the 3pt squad **rule**, drawn under a name and never beside one.

**A CALLOUT HAS ITS OWN ROOM AT LAST.** `CalloutShape.isCallout(sessionCount:leagueId:field:)` has been in the Kit since D237 and was called **nowhere on the phone**, so two buddies who bet on Saturday were told they were in "WEEK 1 OF 1" of a series they are not in — the exact failure `Callout.swift`'s own header names. `EventRoomScreen` routes on it in one line, and `CalloutRoomView` is the same objects for two people: the two golfers ARE the title (a 56pt face and `display` 34 each, with a 2pt `brand` rule between them), three figures on one rule, the forfeit as the surface's one serif sentence, and the record beneath.

**CASE BELONGS TO THE ROLE, ON BOTH CLIENTS.** `statusChip` returned `"Live · wk 2/3"` beside `"NAME TAKES THE CUP"` — one string shouted by the copy, the other by the view — so neither client could set them the same way. `statusChip`, `clinchLine`, `sessionHeader` and `seriesLine` all return sentences now and the `agate` role uppercases; `Weather.line` and `.glance` drop the literal `☀` and the mark is drawn from `icon` by the icon family. Every one of those has a twin in `index.html` and both moved together.

**TWO `LINT-11` FAILURES ON ONE SHEET.** The plan sheet's **gold-tinted weather chip** and its **gold middle RSVP button** are gone: gold may never touch a control. Three equal RSVP buttons become one primary (**I'm in**), one secondary (**Maybe**) and *"Can't make it"* as a `mut` text link with no rule — a decline is not a control the sheet should advertise. The four facts — the countdown, the tee time, the field and the weather — are one `CSScoreRail` on an **`ink`** rule, because a plan six days out is not live, and a cell with no fact is REMOVED rather than dashed.

**THE INTENT SHEET WAS THE LEAST DESIGNED SCREEN IN THE DECK AND IT IS THE FRONT DOOR.** The question is the serif now (`lead` 28, not `CSFont.sentenceBold` — the audit's finding 7 in one line); **nothing is pre-tinted**, because two reviewers read the `brand` first option as already selected; every option is a two-line row with a chevron at 44pt+; and `I have a code` — the smallest thing on the sheet while being the highest-frequency act an invited golfer performs — is a full row at option weight with its own sub-line. Not one of `StartIntent`'s sentences, glosses or orders is touched.

**THE PAGE-SHEAR BUG HAS A NAME.** Wave 5 filed it as unfinished: a vertical `ScrollView` sizes its content box to its widest child and CENTRES a box wider than itself, so ONE row that refuses to compress slides every block on the screen left. This wave found **two** causes. `CSTertiaryStyle` hugs its label by design (the rule must be as wide as the words) — so a 57-character taunt label measured ~490pt on a 362pt page. And `CSSectionHead`'s title carried `fixedSize(horizontal: true)` at every size, so `WEEK 2 · SEP 5 – SEP 8` at agate ×2.2 measured ~400pt at AX3. Both are fixed at the source, the event room additionally pins its content with `containerRelativeFrame(.horizontal)`, and the season page's AX3 shear should be gone with them.

**A `fullScreenCover` DOES NOT INHERIT `.dynamicTypeSize`.** IOS-051's capture hatch is applied at the app root and reaches every pushed screen; it does not cross a cover's presentation host, so the first AX3 shot of this room was pixel-identical to the reading-size one — evidence that flattered, which is the exact failure the hatch exists to prevent. It is re-applied on the cover.

**A CAPTURE FIXTURE, NAMED, BECAUSE THE ACCOUNT HAS NO EVENT AT ALL.** `native_home` returns an empty `events` array for the signed-in simulator, so `-cs_dev_open ryder` set `presenter.event = nil` and fell through to Home — which is what `SHOTS.md` caveat 1 already recorded about the shipped `dark-ryder` capture. `-cs_dev_event_fixture [complete|callout|major]` seeds the Dew Sweepers Cup — six in the field, 3½–2½, first to 5, $480 — DEBUG only, never written to the server, same posture as `-cs_dev_home_state`, `-cs_dev_h2h_fixture` and `-cs_dev_season_fixture`. Its ids are FIXED: `CSFace` keys its pigment to the profile id, and a fixture minting a fresh `UUID()` per launch gave Galen a different coin in every screenshot — §6.2a's drift, arriving through the capture harness. `-cs_dev_open plan` opens the plan sheet, which had no door at all and is one of this surface's five artboards.

**ONE MIGRATION, WRITTEN AND NOT RUN.** `20261008090000_an_event_knows_its_course.sql` adds `events.course_id` and `events.course_label` — nullable, additive, FK-by-convention to `api_courses` exactly as `scheduled_rounds` carries them, with the two column grants D37's seal requires. The client decodes both as optional, so a phone ahead of its database draws the bare ceremony ground — which `event-callout` is the rendered proof is not a degraded state.

> **CORRECTED 2026-09-07 (the repair pass).** The paragraph above originally said the migration "buys the dateline's first line and the contour's seed", and the migration's own closing comment said the course "is set at setup by `create_event`". **Neither is true today.** `create_event` is not changed by that file and its signature carries no course argument; neither client writes the columns. Pushing the migration is safe and additive and buys **nothing a golfer can see** — the columns are GROUNDWORK, and a second change (an RPC signature, a contract refresh, and the setup sheet asking for the course) is what turns them into the dateline and the contour seed. The migration's header now says so; the owner should not push it expecting a feature.

**Reversibility.** MEDIUM. Two app files added (`EventTitleCard`, `CalloutRoomView`) plus one DEBUG-only (`EventFixture`); five rebuilt (`EventRoomScreen`, `RyderRoomView`, `MajorRoomView`, `IntentSheet`, `ScheduledRoundSheet`); four re-clothed (`EventBits`, `EventChips`, `EventPickerSheet`, `EventStagePicker`, and both setup sheets); one file added to `CSDesign` (`Event.swift` — `CSSideRoster`, `CSScoreRail`, `CSClashRow`), with `CSGlyph`, `CSSectionHead` and `CSTertiaryStyle`'s call sites touched. `EventRoomModel` is untouched, every RPC and every hand is unchanged, and the producer edits are re-casings of strings both clients already shared — so a revert is a view revert plus four `toUpperCase()`s.

## IOS-050 · Wave 7 — one standings object, and a score that looks like a score — **P1 · BUILT 2026-09-07 (D276; owner-authorised "build it", 2026-09-06)**

*The wave the build brief scopes to leaderboards and score displays. It carries D267's rank rail and rule-and-figure to the two surfaces they were designed for — the boards and the live sheet — and D276's one-meaning-per-mark to the producers underneath them.*

**THE ONE-SECOND TEST IS THE WHOLE SPEC.** Open a board, look for one second, look away: you should be able to say who leads, where you are, and which way you are moving. Everything below is subordinate to that.

**THE MOVEMENT CLOCK RIDES THE SECTION HEAD.** `THE TABLE · MOVED SINCE SUN`, once, for the whole table — against the shipped `HELD SINCE SUN` printed beside eleven two-digit ranks at 11pt. `StandingsMath.movedSince` is the producer and its refusal is absolute: no snapshot, no clause in the head AND no column in the rows. That single move frees the movement column to be a **drawn mark** rather than a sentence to parse, which is what D276 is for.

**A TIE IS DRAWN AS A TIE — 04 · 04 · 06.** Both renderers indexed the sorted array, so two golfers on 86 points read `04` and `05`: the same figure in the PTS column with two different positions beside it. `StandingsMath.competitionRanks` shares the numeral and skips the next, over the column the table actually prints, and the sub-line says `TIED`. The rail is gold on **rank 01**, not on row 0, so two golfers genuinely tied at the top both wear it — which puts that season's gold count at three against §15.4's whitelisted pair, and `CSBudgetProbe` reports it honestly rather than the code hiding a tie to make a budget.

**THE LIVE SHEET, REBUILT FROM THE STATUS BAND DOWN.** The most-looked-at screen in the product, and the one the audit scored 4.8 in portrait against 6.8 for its own landscape card. Deleted by name: the two capsule chips restating four rows twelve points above them; the **eighteen ember dots** that said how far the round was and never how it was going; the four 4 × 40 colour bars standing in for four people on a screen with four people on it; the stacked `55 / THRU / 14 / -1` column that broke `THRU` across two lines on an SE; the `− – +` tray whose unscored placeholder was an en dash 34pt from the decrement glyph; and the bordered side-game card. In their place: the **hole strip** (`CSHoleStrip`, chart 4) with the scorecard ink law and — filed three times by the blind reviewers — **a key**; a **`CSFace` in every golfer row**, which needed `profiles.marker` carried onto `LivePlayer` (nullable, so a snapshot written before today still decodes); the **score object**, a bare `figure` 27 over a 2pt rule between two 44pt targets, with **an em dash when nothing is in**, because rendering the hole's par in a real score's slot is the guess §9.9 forbids; `TO WIN THIS HOLE`, which is the question a foursome is actually asking while it stands there; and **one full-width ember primary**, because on the screen a golfer is holding while standing on a course the live thing they can do is finish the round.

**THE CARD IS KEPT — IT IS THE CEILING — AND IT TAKES FOUR FIXES, NOT THREE.** `LiveCardView`'s eleven `cs.dim` text sites move to `mut`; its birdie **stops being a colour** (it was `pos` green here and GOLD on the portrait sheet — one product, two colour languages for one fact) and takes the ink law's ring; its columns get per-column accessibility labels. The fourth is not in the surface spec and is named because a non-negotiable outranks a fix count: **OUT / IN / TOT and the stroke pips were gold**, and a column total is not a thing anybody won (D269).

**THE RECEIPT IS PRINTED ON A LEAF, AND IT ENDS IN A FIGURE.** `ReceiptRows.build` is untouched — nothing is invented, nothing reordered, and the `.note` case still stands where the verdict would. What changed is the paper: `body` labels at `leafInk` with the arithmetic and the two handicap nouns at `leafMut` because they are the working and not the answer, the leaf's own hairline rather than the page's `rule`, the verdict's band word hanging under its figure, and the total under a **2pt `leafInk` rule** at `figure` 20. Above it, the score as an object: the gross at 56 and the points at 40, each over its own rule, on one baseline — and the sentence beneath them with its numeral in the **board face at the sentence's own size** (`CSBands.vsPhraseMarked`, the producer marking its own run; there is no regex over prose).

**THE FORM BOARD SPENDS ZERO GOLD.** The Golfers tab's rail has two states — `panel` when it is yours, unpainted for everyone else — because leading a rolling 30-day window is not a thing that was *won* (D245 clause 5, `L-22`). The row carries the **figure** the band is a name for, with the band word beneath it on one line, and the sub-line drops to the count alone: `3 ROUNDS · BEAT IT ONCE` beside `−1.8` beside `A LITTLE LOOSE` was the same fact three times.

**AND THE ARTBOARD SAID THE COLUMN WAS UPSIDE DOWN.** The design writes `VS YOUR NUMBER · LOWER IS BETTER` over the form column. In this product **that is false**: `CSBands` is written so a POSITIVE figure means you beat your playing HCP by that much — `bandName(+3)` is *Torched it* and `bandName(−3)` is *Posted anyway*. The artboard was drawn in the golfer's other convention, a score under par, and shipping its words would have told every golfer on the board that the best round on it was the worst. The built line reads `VS PLAYING HCP · PLUS IS BETTER`, and it names the governing noun because R-M retired *number* as the comparison noun and preflight 42 fails the frame.

**Reversibility.** MEDIUM. No file is deleted; six are rebuilt (`LivePlayView`, `FriendsBoard`'s board section, `RoundReceiptSheet`, `CupFinalRaceView`, `PostRoundScreen`'s hero, `ReceiptSheets`' squad receipt), one is added (`ReceiptLeaf`), and `CSHoleStrip`, `CSSlat` and `CSFace.Model.seeded` change in `CSDesign`. Four producers change in the Kit and all four are pure and tested: `Movement`, `awards`, `competitionRanks`/`movedSince`, and `LiveCopy`'s row and `toWinThisHole`. One field joins a payload — `profiles.marker` on the live roster read — and it needs no migration, because the column is already in the frozen grant list. Every RPC and every mechanic is untouched, so a revert is a view revert plus four producer reverts.

---

## IOS-052 · Wave 8 — the system reaches the rest of the app, and the ceremony finally plays — **P0 · BUILT 2026-09-07 (D277; owner-authorised "build it", 2026-09-06)**

*The propagate wave. Everything Phase 3 did not name, brought onto the same components — and the audit's worst-scoring screen rebuilt from the ground up.*

**THE TAKEOVER, WHICH IS THE P0.** `SeasonCeremonyView` had **zero animation calls and zero haptics** and read as a dialog: a centred serif name, a `heroSmall` margin in gold, three grey label lines, a rounded tile per block. It is `CSTakeover` now — §11.3's biggest motion, in `CSDesign` so the live recap uses the same object: the plate wipes from the leading edge, the `display` lines arrive at a 60ms stagger, `CSTally` counts the figure over 340ms eased out, and the medallion seals last with `.posted`. **The rest frame is the finished state**: under `reduceMotion` nothing wipes, nothing staggers and nothing counts — never the same animation faster. Under the band the settlement is a printed ledger and `MoneyCopy.ledger` is printed once.

**AND IT CANNOT BE PHOTOGRAPHED ON REAL DATA, SO IT SHIPS WITH A FIXTURE AND SAYS SO.** No season on any device is `complete`. `-cs_dev_open ceremony` presents the same view against `CeremonyFixture` — four invented golfers on invented ids, DEBUG only, never written to the server — on the same posture as `-cs_dev_home_state`, `-cs_dev_h2h_fixture` and `-cs_dev_season_fixture`. `PotMath.Settlement` gained a public initialiser for it, and nothing else about the type moved. **Every claim about the ceremony in this entry is a claim about a fixture.**

**THE SETTLEMENT CARD IS RENDERED IN-APP AT THE GEOMETRY IT EXPORTS AT.** 1080 × 1350, scaled to the measure inside the recap. The audit calls it the most beautiful object in the product and the golfer who won it never saw it. **All four share artifacts were also printed in the palette D270 replaced** — `#F0F2F3` ink beside the app's `#F1F4EF`, `#E9BE62` gold beside `#D8B25A` — on both clients, because each typed its own hexes "verbatim from the web". They read the pinned `ceremony` ramp now, their faces are ROLES at a literal size (`CSType.fixed`, new — a PNG must not scale with a reading size), Charter is gone from all four, and **preflight 47** holds the two clients' canvases to `tokens.json` so they cannot drift apart again.

**FOUR LAWS MADE GENERAL, NOT SURFACE BY SURFACE.** One chip (twelve web families and every phone pill → §7.2's 3pt rectangle, selected inverting to the panel). One icon family (ten drawn glyphs added; every SF Symbol beside a drawn one deleted; `📌`/`📣`/`✦`/`◆` gone from the board on both clients). One dismiss verb (`SheetFrame`'s `✕`-in-a-circle → `Close`, a toolbar tertiary — fifteen sheets in one edit, plus eight more sites). And **every toast carries a kind**: 126 sites classified, a confirmation getting a `pos` rail and a check, a failure a `neg` rail and a cross.

**THE 44pt MISS THE AUDIT NAMES, AND ITS CAUSE.** The composer's `Start over` was ~20pt because `// F-13` ran straight into `.foregroundStyle(cs.mut).frame(…minHeight: 44)` — **both modifiers were inside the comment**, and nothing in Swift had anything to say about it. It is a tertiary now, which carries its target in the style rather than at the site. The composer's golfer chips (36pt, ember-tinted capsules) are the one chip at 44.

**AND THE CAPTURE HATCH DOES NOT CROSS A COVER EITHER.** IOS-050 recorded that `-cs_dev_text_size` crosses a `fullScreenCover` and not a `.sheet`. It crosses neither: the composer's first AX3 shot in this wave was pixel-identical to its reading-size one. `csSheet` and `csCover` wrap both presentations on the two hosts, so a new sheet cannot forget the line — and the AX3 evidence for this wave is real rather than flattering.

**EVERY FACE IN THE PRODUCT IS NOW KEYED TO A GOLFER (`LINT-31` = 0).** `CSCheckRow` and `RoomLineRow` took a marker STRING and seated the golfer on a pigment derived from the glyph; they take a `CSFace.Model` now, built by one producer (`Faces.of`) that keys to the profile id and seeds from the NAME when the payload has none. The scheduled round's comment list is seeded from its author. `UpNextChips` and `UpcomingRoundsSection` — unreferenced since Wave 1 — are deleted with the last two sites.

**Reversibility.** MEDIUM. Two files are deleted (both already dead), two are added (`CSDesign/Ceremony.swift`, the DEBUG fixture), and about seventy are edited. Two producers change: `WizardCopy.back`/`next` drop their typed arrows on both clients, and `PotMath.Settlement` gains an initialiser. No RPC, no migration, no mechanic. A revert is a view revert plus those two.

**Gate:** `build-tokens` clean · preflight PASS, 0 failures, 0 warnings, with twelve LINT baselines lowered and check 47 added · sunningdale PASS · 1109 tests in 196 suites, up from 1100 in 193.

## IOS-053 · Wave 9 — the lint runs all twenty-nine, and the retired vocabulary leaves the codebase — **BUILT 2026-09-07 (D278; owner-authorised "build it", 2026-09-06)**

*The consistency sweep. The wave that stops the system drifting back — and the wave that found out the drift-detector itself was broken.*

**THE TABLE SAID TWENTY-NINE AND THE FILE RAN THIRTEEN.** `UI_SYSTEM` §17 declares `LINT-01…29`; `tests/preflight.mjs` ran 01, 03, 04, 05, 09, 10, 12, 13, 14, 22, 25, 28, 29 plus this build's own 30 and 31. Sixteen were a sentence in a document, and a reader of §17 would have believed the product was linted for spacing, tracking, gold on a control, a panel holding a sentence, a leaf holding a grid, a pushed screen naming itself twice, a photograph cropped into an avatar, the ledger line and all five render-time budgets. **Ten greps and six test-carried checks were written here, and the count is now twenty-nine plus two.**

**AND CHECK 49 MAKES THE TABLE ITSELF CHECKABLE.** It reads §17's own markdown rows out of `UI_SYSTEM.md`, and fails the push if any declared id has neither a `lint('LINT-nn'` call here nor a named test file that mentions it — **and in the other direction too**, if this file runs an id §17 never declared. The specific failure this wave exists to prevent is a table and a codebase disagreeing about what is enforced; that failure is now a preflight failure.

**THE PROBE MECHANISM WAS BROKEN, IN THE ONE WAY NOBODY COULD SEE.** `CSBudgetTick` used `View.preference(key:value:)`. That modifier **SETS** the value for the modified view and **DISCARDS whatever the subtree below produced** — so `CSPanel`, `CSLeaf` and `CSPlate`, all three of which end their chain with `.csBudget(nested:)`, were erasing the budget of everything they held, and `LINT-08` — *a container inside a container* — could never fire, because the outer container overwrote the inner one's count with its own. Nothing said so because **nothing read the total**: every wave since 0b closed with "the counters are wired; nothing reads the total", and the moment `BudgetProbeTests` read it the nesting test failed on the first run. It is `transformPreference` now, which ADDS to what came from below — which is what a budget means.

**`BudgetProbeTests` HOSTS A VIEWPORT FOR REAL.** A `UIHostingController` in a window, laid out, with the main run loop pumped until the preference lands — `ImageRenderer` produces a bitmap without giving `onPreferenceChange` a turn to fire on, so the budget comes back empty and every assertion passes vacuously, which is worse than no suite. Ten tests, **each of the five budgets asserted in BOTH directions** (ten agate lines pass and eleven breach; two panels side by side pass and a panel inside a leaf breaches), plus two tests about the harness itself so a blind harness cannot make the rest green.

**AND THE PROBE IMMEDIATELY FOUND SOMETHING: the credential spends TWO gold marks.** The slot a golfer earned and the medallion §19(i) puts on every card. §17's `LINT-17` already provides for this — *"with the season's leader-rail-plus-pot pair whitelisted by name"* — so the whitelist is declared by the OBJECT rather than by a table somewhere else: `CSBudget.goldPaired`, passed at the medallion's own call site with the ruling written above it, and only when a slot is actually present. **A card with nothing earned spends exactly one gold mark, and that is asserted too**, so the whitelist cannot start firing unconditionally and hide a real second gold on every card in the product.

**THE RETIRED VOCABULARY IS DELETED.** `CSCard`, `CSStat`, `CSEmptyState` and `CSButton` were kept for eight waves with their removal wave named in the file — Waves 1–7 the surfaces, Wave 8 the remainder, **Wave 9 the declarations** — while `LINT-30` held the call sites to a number that could only fall: **157 → 111 → 14 → 0**. The last fourteen migrated here: eight `CSStat` tiles on the owner's desk became rule-and-figures, two `CSEmptyState`s became `CSEmpty` with a non-optional door, and four `CSCard`s (the live resume banner, the guide's band block, both pricing passes) became a rule and an eyebrow. `CSHairline`, `CSGroupHead`, `CSTabStrip`, `CSHero`, `CSWash` and `CSDuskCard` went with them. **A shim with no named removal is not a shim; it is a second system** — and that sentence is the only reason these were removable at all.

**D270'S LAST OWED CLAUSE: THE PRODUCT PAINTS NO ATMOSPHERE.** `CSLookSky` drew a band of the look's accent at 22% over the top 260pt of six surfaces, and `CSWash` a radial at 30% behind every hero. Both were named in D270's "still owed" on the day the palette was re-printed and deferred by Waves 0a, 0b and 8, because removing a wash changes six screens and none of those waves owned all six. A look now reaches **three channels — the tick, the eyebrow and the spine — all three of them a MARK**, and `washStrength`/`skyStrength` are deleted so there is no number waiting to be painted again. The scroll strip survives: it paints `bg0` under the clock so rows do not show through the status bar, which is the half of it that was ever doing work. **This is visible on the season page** and is the one place the sweep deliberately changed a screen.

**NINE BASELINES REACH ZERO AND ARE ZERO-TOLERANCE FROM HERE.** `LINT-01` (the finish ceremony's gross was the last `.custom("Charter-Bold"` in the product — D268's retirement, D258's mechanism) · `-11` (`CSButtonStyle.gold` left with the four declarations) · `-13` (twenty-two typed arrows on both clients) · `-22` (seven spinners; `CSDestructiveStyle` gained `busy:` so two admin sheets stopped hand-rolling a fourth button tier with a spinner in it) · `-23` · `-25` (nine dismiss verbs → `Close`; "Done" survives only where it was a COMMIT, and then it says what it commits — *Save the pars*) · `-30` · plus `-19`, `-20`, `-21`, `-24` and `-26`, which landed at zero.

**THREE BASELINES ROSE, AND IT IS SCOPE RATHER THAN DRIFT.** `LINT-06`, `-07` and `-10` were turned on for `index.html` — `UI_SYSTEM` §14.5's stated web half of this wave — adding the desk's **922 spacing literals, 173 off-scale tracking ratios and 158 boxed surfaces** to the phone's. 363 → 1238, 59 → 211, 109 → 265. The ratchet now holds both clients.

**Reversibility.** MEDIUM. Eleven public types are deleted from `CSDesign` (all with zero app call sites by the time they went), one file is added (`CupSeasonTests/BudgetProbeTests.swift`), `Probe.swift`'s accumulation is fixed, and about thirty files are edited. Three producers change: `RankMove.label` is deleted, `LeagueCopy.indexSub` and `SeasonStats.deltaText` stop printing a direction glyph — all three on both clients, all three with their tests moved rather than removed. No RPC, no migration, no mechanic.

**Gate:** `build-tokens` clean · preflight PASS with all 29 checks of §17 running plus checks 48 and 49 · sunningdale PASS · the suite up on the design commit, and the probe tests are new coverage rather than replaced coverage.

## IOS-054 · Wave 10 — one system, three phones, five switches, and a desk you can drive from a keyboard — **BUILT 2026-09-07 (D279; owner-authorised "build it", 2026-09-06)**

*Responsive and accessibility. The first wave that could SEE the two axes Wave 0 built the hatches for — and the first shots taken on the SE and the Max at all.*

**`ViewThatFits` WAS ZERO AND `horizontalSizeClass` MEANT "SIDEWAYS".** Three phones, one view tree. The remedy is not a breakpoint table — `UI_SYSTEM` §16.3 says so in as many words and names the model to copy: `MeStripLayout` reflows on the **measured advance of its own characters at the size the golfer is reading**. That is generalised here into `CSAdvance` (a string's width in the real face, at the real rendered size, with the role's tracking — Plex Mono could be multiplied out at 0.6 em and the board face cannot) and `CSClauseLine` (a `·`-separated agate line that sets on one line while it fits and stacks on its separators when it does not, decided by `ViewThatFits` so there is no arithmetic to be wrong about).

**THE AX5 SHEAR, FOUND WITH A PROBE AND FIXED IN TWO PLACES.** At AX5 the whole of Home slid ~32pt left and ran off both edges. A vertical `ScrollView` sizes its content box to its widest child and CENTRES a box wider than itself, so ONE row that will not shrink moves every block on the page. A temporary geometry probe measured each block at a proposal of 402: masthead 402, lead 402, ME strip 402, wire 402, **floor 465.7**. `HomeFloor`'s verb held `fixedSize(horizontal: true)` so its ember rule would stop at the end of the word, and at AX5 `Start something` in `nameS` is wider than the phone. It is an `A11yStack` now — at the accessibility sizes the row becomes a column, the verb takes the measure and the gloss sets under it.

**AND WAVE 5'S HALF-FIX WAS ONE ARGUMENT SHORT.** That wave clamped the season page with `containerRelativeFrame(.horizontal)` and reported that it fixed the wrap width and left a twenty-point offset it could not close. **`containerRelativeFrame` defaults to `.center`**: the frame was the right width and its content was still centred inside it. `csPage(_:)` is the general form — it measures the content's NATURAL width first, clamps with `alignment: .leading`, injects the page's own measure as `\.csMeasure`, and under DEBUG logs `CS-SHEAR <page> wants 465.7 in 402`. Nine page roots take it, `SheetFrame` among them, so fifteen sheets get it at once. **A mechanism nothing reads is a mechanism nobody can know is broken** — Wave 9's lesson, applied to the thing Wave 5 could not isolate.

**THE SLAT'S COLUMNS ARE DERIVED FROM THE MEASURE, AND THE DOCUMENTED ARITHMETIC WAS 12pt OPTIMISTIC.** `changeWidth` and `trailingWidth` are 14.4% and 12.4% of the measure, floored at what `+12` beside a triangle and a two-digit total actually need and capped at 58/50 so the Max's extra 38pt goes to the NAME. And the row pays `s3` **twice** — before the face and before the name block, which is what stops a disc touching a surname — so the real fixed total is 226 and the name column is **176 at 402 and 156 on an SE**, not the 188 and 161 six surface specs quote. `CSStandingsBoard` now takes the field's `names:` and abbreviates when the longest one does not fit the column *this phone* gives it; `count >= 10` stays as the floor so a big field keeps one grammar. Photographed: the same eight-golfer board reads `P. RAGHUNATHAN` on an SE and `PRIYA RAGHUNATHAN` on a Max.

**AT AX3 A BOARD ROW SAYS ITS OWN FACTS.** The column heads hide at the accessibility sizes — a head naming columns that are no longer on screen is worse than no head — and what that left behind was `+4 ▲1 15`: three numerals with nothing to attach them to. `CSSlat(axFacts:)` takes the row's clauses and prints them under the name on one `CSClauseLine`: `UP ONE SINCE SUN · 4 BACK · 15 POINTS`, from `Movement.long`, which Wave 7 produced and named as a deferral because "the reflow that consumes it does not exist". It exists.

**AND THE RAIL IS A SIBLING OF THE WHOLE ROW.** §16.3 says the rail keeps its 44pt width and the row grows its HEIGHT. The shipped `A11yStack` grew the row by stacking a second band *under* the rail, so at AX3 the leader's gold field stopped half-way down its own row and the viewer's `02` fell out of the bottom of its white panel into `panelInk` on a dark ground. One `HStack`, `.top` at the accessibility sizes and `.center` at the reading ones, and the rail paints the row it belongs to.

**REDUCE TRANSPARENCY, WHICH IS THE SWITCH THIS DESIGN IS MOST EXPOSED TO.** The folio's hairline at `a16`, the contour at `a24`/`a56` and the photo scrim are not decoration over a solid thing — they are the entire texture of the credential and the title card. Each resolves to its **composited opaque value** over the ceremony ground, through `CSOpaque`, whose arithmetic is asserted against `folioRule` `#8B8F8B` — the one composite the palette already computed by hand — so the two cannot drift. **The scrim is the stated exception**: flattening a `.title` ramp to its darkest stop paints the plate solid and erases the photograph, so it becomes a hard edge at the point the ramp crossed half its darkest value. Nothing fades into anything, the copy's band carries the ramp's own maximum, and the top of the picture survives.

**THE 44pt FLOOR, PUT IN THE COMPONENT RATHER THAN AT NINETEEN CALL SITES.** `CSChip` is a 28pt drawn shape — that is the design — and nineteen sites shipped it after Wave 8 with exactly **three** adding a target by hand. The live round's HOLE/CARD toggle (the audit's named ~22pt miss, 28 after the sweep), the composer's golfer chips and fourteen more were under the floor. `a11yHitSlop(vertical: 8)` sets the hit shape and hands the space straight back to the layout, so nothing moves by a point and every chip in the product is 44 tall. The other two named misses were already fixed: the composer's "Start over" is a tertiary and the star rail has its stepper pair.

**EIGHTEEN COLUMNS, EIGHTEEN LABELS.** §16.4's last line — *the landscape scorecard's single label for 18 columns is the one hole to fill*. The score row already named its holes; the PAR and SI rows a score is READ AGAINST did not, so VoiceOver could tell a golfer what they shot and not what the hole plays to. And the shot pips were `accessibilityHidden`, so the one thing they exist to say — **where you get a stroke** — was the one thing a screen reader could not hear. It is in the cell's own label now.

**THE TERTIARY HUGS OR WRAPS, DECIDED BY FIT.** Wave 0b pinned two lines and Wave 1 hugged, and each shipped the other's defect: `SETTINGS`, one word, drew a full-measure rule directly above the credential (Wave 3's own note), and `START OVER — CLEAR THIS ROUND` truncated at AX5. `ViewThatFits` takes the hugged one-line form while it fits and the wrapped full-measure form with **no line limit** when it does not.

**THE CONTRAST TABLE IS COMPUTED.** `ContrastTableTests` walks every pair §16.1 names, in both printings. Every text tier clears AA on every ground; `rule` is under 3 in both; the four ceremony objects clear their pinned ground; all six pigments carry an ink glyph; the four squad marks are 1.58:1 apart, which is asserted as a FAILURE so a later palette change cannot make the mandatory squad name look optional. Two of §16.1's own numbers are wrong and are named in the tests rather than repeated: `dim` in dark is **3.15:1**, not sub-3, so the fence is the ruling; and the `bg2` review list is stale in both directions.

**THE DESK, WHICH COULD NOT BE DRIVEN FROM A KEYBOARD AT ALL.** 40 hover rules, one `[disabled]` rule, and the standings slat and Golfers board row unreachable by Tab. They are `tabindex="0" role="button"` now, the standings row carrying an `aria-label` built from the same facts `CSSlat.spoken` reads. Hover: the slat's ground steps to `bg1` and its rail slot paints `bg2` — **never over a gold field**, because a hover state may not spend the earned metal. Focus: a 2px `brand` outline on the whole slat. Keyboard: `↑`/`↓` between the slats of the board the focus is IN, `→`/Enter/Space to open the receipt, `/` to the nearest search field, `g` then `t` to the table, and every one of them returns early on an editable target.

**Reversibility.** MEDIUM. Two files added to `CSDesign` (`Responsive.swift`, `ResponsiveTests.swift`), `CSSlat` restructured, `CSSlatMetrics` gains four measure-derived functions, `Theme.swift` gains the Reduce Transparency printing, and ~15 app files are edited. No producer changes its words, no RPC, no migration, no mechanic. The season fixture's ids are frozen so two captures of the same board wear the same pigments.

**Gate:** `build-tokens` clean · preflight PASS · sunningdale PASS · the suite up on Wave 9, with the responsive and contrast suites as new coverage and two pinned numbers corrected in place with the reason in the file.

## IOS-055 · The repair pass — the rail tells the truth, and one board head — **BUILT 2026-09-07 (D281; owner-authorised "build it", 2026-09-06)**

Three reviewers went over the finished overhaul. This is the first of the repair commits, and it is the boards.

**THE RAIL FOLLOWS THE FIGURE.** `FriendsBoard.ordered` sorted on the server's `rank_by_form`, which ranks on beats, while the column beside it prints the average against the golfer's own playing handicap — so the photographed board read −2.6, +1.0, +2.8, −3.8 down a 44pt gold-and-panel rail. Each lens now sorts on the figure IT prints, and `ranked(_:)` hands the view the position rather than a server column, so the two cannot diverge again. Asserted in both directions: a +2.8 row outranks a −2.6 row, a row with no figure takes the tail, and the rail is 1…n with no holes.

**ONE BOARD HEAD, AND IT KNOWS WHETHER ITS ROWS CARRY A FACE.** `CSSlat.faceAndName` pays `railGap` before the face and again before the name; `CSStandingsBoard.head` paid that arithmetic on every board, including a squads season's top table, whose rows carry **no** face — so `GOLFER` sat 42pt right of `MUDSHARKS`, and read `GOLFER` over four squads. `CSSlatMetrics.nameInset(hasFace:)` is now the one place that sum lives; the board takes `nameHead:` and `hasFaces:`; and `MajorRoomView`'s hand-rolled head — which paid the gap ONCE and sized `CARDS` and `NET` with the 58/50 constants while its rows used the measure-relative forms — reads the same function and the same `(at:)` widths. The product had two board heads twelve points apart and one of them was wrong on an SE as well.

**THE CUT KEEPS ITS RULE AT AX3.** `CSCut` dropped the 2pt ink rule entirely when the label wrapped, so at AX3 the cut degraded to a stray sentence between two rows — and the cut is the one element on the board whose meaning is carried by the rule. The label now wraps ABOVE a full-measure rule.

**ONE LONG HANDLE NO LONGER ABBREVIATES THE FIELD.** Abbreviation is decided per BOARD, so a golfer who never set a display name — `fedor.garcia-mendoza`, one unbroken token — collapsed `GALEN MARR`, `JADE OKONKWO` and `BLAKE RIDLEY` to first names while itself staying full, and bought nothing: a token with no space cannot be shortened. `CSSlatMetrics.isAbbreviable` excludes a spaceless token over eleven characters from the measurement, so the outlier truncates itself.

**Reversibility.** MEDIUM. `Board.swift` gains two parameters and one static; `FriendsBoard`'s ordering changes and its rank becomes a position; `StandingsTableView` reads `ClimbMath` for K; `LeagueRoomModel` gains `myCountingPoints`, derived from rows already on the device. No RPC, no migration, no mechanic.

**Gate:** `build-tokens` clean · preflight PASS · sunningdale PASS · 980 tests / 161 suites, four new assertions.

## IOS-056 · The repair pass — the card is an object again — **BUILT 2026-09-07 (D282; owner-authorised "build it", 2026-09-06)**

The second repair commit, and it is the credential — the object the whole design is judged on.

**THE NAME PRINTED THROUGH THE SLOT.** `head` bottom-anchors the identity block in a plate of FIXED height while the block GROWS, so `JERECHO FISCHBECK` at `display` 34 on two lines over a three-clause identity reached the gold `FOUNDER` slot at the plate's head. `CSCredential.PlateBand` is the arithmetic that was missing: the head band is reserved (`s4` + the 24pt field + `s2`), the copy band is measured against what is left, the name drops to `displayS` when it will not set on one line, and only an impossible band sends the identity off the photograph onto the card's own ground. `CredentialBandTests` pins the photographed case in both directions.

**THE RULE REACHES THE CARD'S EDGE.** Wave 10's measured columns fixed `HANDICAP IND…` and left the rule 55pt short of the card with the folio's own longer hairline directly beneath it — two rules, two lengths, on one object. Three cells now scale up to fill the inner measure; fewer keep the short rule at the 66%/33% the doc comment already claimed.

**AN OBJECT HAS AN EDGE.** `ceremony` #0A0E0C against `bg0` #0F1A15 is a 1.05:1 step, so in the dark printing the crest card had no boundary and `shadow-lift` had nothing to fall on. `CSObject` draws its own edge as a 1px rule at the `a16` `ceremonyInk` the folio inside it already uses.

**AND THE EMBER DOT IS NOT A DUST SPECK.** `CSContour`'s mark is placed in the field's own space; on the crest it landed inside the line of `GALEN MARR`. It takes a `markSafe` region now, and the credential passes the upper-right quadrant — where `player-card-marker.png` draws it, and the one quadrant the slot, the name and the medallion all leave alone. The course page passes nothing and keeps the free placement §7 gives it.

**THE BUILDING STATE IS ON YOUR OWN PAGE.** `YouScreen` printed `CredentialCopy.mine` unconditionally and never called `CredentialCopy.status` — the producer written for this — so a buddy's card said *One more round sets their number* and your own said nothing.

**Reversibility.** MEDIUM. `Person.swift`, `Structure.swift`, `Contour.swift`, `YouScreen.swift`. No producer changes its words; `CSContour` gains an optional argument that defaults to today's behaviour.

**Gate:** `build-tokens` clean · preflight PASS · sunningdale PASS · 980 tests / 161 suites plus a four-test `CredentialBandTests`.

## IOS-057 · The repair pass — the lints stop reporting on a codebase that does not exist — **BUILT 2026-09-07 (D283; owner-authorised "build it", 2026-09-06)**

The third repair commit. Four checks said **zero, and held there** while the defects they name shipped.

**`LINT-04` COULD NOT SEE THIS REPO'S OWN LITERAL.** Its regex was `Color(red:` / `Color(white:` / `Color(hue:`; every colour literal in this codebase is written `Color(hex: 0x…)`. Eight shipped. Five were the palette D270 deleted — Fairway `#2FA46A` and `pine` `#12271B` — on `FinishCeremonyView`, which Waves 8 and 9 both edited and both left. All eight are `ceremony` / `object.scrim-*` tokens now; the Share control is ember because it is the screen's one primary; the radial wash is gone on both clients.

**`LINT-11` WAS SCOPED TO A BUTTON AND THE GOLD WAS EVERYWHERE ELSE.** A WARNING paragraph about a failed push registration, a colour legend whose key was gold, the calendar dot that key names, a tee time, a membership tag, a rivalry tag and a gold SF Symbol trophy on a leagueless door — none of them earned. The check now sees a `legend(` and a gold `Image(systemName:`; the sites take `ink` and `mut`, and the trophy is `CSTrophyMark`.

**`LINT-23` CHECKED THE WORDS AND NOT THE FORM.** Eight ledger printings in three type roles reported clean, and the ceremony printed the sentence twice — once mid-page beside the figure you are owed and once at the foot — so which one a golfer read depended on whether they had won. The check now fails a standalone printing set in anything but `agateS`; the four standalone sites are normalised and the ceremony's two are one, at the foot.

**AND THE VOCABULARY CHECKS HAD THE HOLES THEIR OWN NOTES PREDICTED.** Check 7 carried three phrasings of the retired record sense of *your card*; `HomeWireCopy` — a file this overhaul ADDED — wrote a seventh. Check 18 greps `\bSI \d`, so a bare `SI` column head walked through on the course leaf, the live card, the scorecard sheet and three desk tables. And `snapshot` was on §3.1's never-print list with no ship-list row, so three live strings said it. Widening the three found **eleven** more hits than the review named; all eleven are fixed, and five tests that pinned the old strings were migrated.

**Reversibility.** LOW for the lints (they only widen), MEDIUM for the copy. No producer changes what it MEANS; five change a noun. No RPC, no migration, no mechanic.

**Gate:** `build-tokens` clean · preflight PASS · sunningdale PASS · 980 tests / 161 suites.

## IOS-058 · The repair pass — one back button, and nothing renders under the clock — **BUILT 2026-09-07 (owner-authorised "build it", 2026-09-06)**

The fourth repair commit: the chrome the system draws for us, and the two places it was wrong.

**TWO BACK BUTTONS IN ONE PRODUCT.** iOS 26 wraps every toolbar button in a glass capsule, so the season page, the record page and the fixture table drew their back chevron inside a translucent `bg2` disc while the course page and the event room — which hide the bar and draw their own — did not. No artboard draws either disc, and a boxed control is the first non-negotiable's own defect. **The first fix did not work and is recorded because it cost a build:** keeping the bar, hiding its background and hanging a `.plain` button in it still painted the capsule. A toolbar item cannot be talked out of its glass on this system, so the page draws the chevron: `CSBackChevron`, in the content, riding the head, exactly as the course page has since Wave 4. `csBareBar()` is the one modifier that empties the bar.

**THE CLOCK ON A NEAR-BLACK PLATE IN THE LIGHT PRINTING.** The course page and the event room pin their plate to `ceremony` in both printings and hide their bar, so nothing was left to hang `toolbarColorScheme` on and the light printing put its DARK glyph set on near-black — the time, the cellular dots and the battery, unreadable, over the top 60pt of two flagship surfaces. Three routes were measured and two failed: `toolbarColorScheme(.dark)` does not reach the status bar on a bar with no background, and a surface-level `preferredColorScheme(.dark)` loses to the App's own at the scene root. **So the plate bleeds in the dark printing and stops at the safe area in the light one** (`csPlateBleedsInDark`), and the clock reads dark on paper the way it does on every other light surface. `course-light.png` draws it bleeding with light glyphs; a mockup draws its own status bar and the app does not get to (§19).

**AND CONTENT SCROLLED UNDER THE CLOCK EVERYWHERE ELSE.** `csStatusCap` paints the page's own ground into the top safe area on the four tab roots and the two pushed roots that hide their bar. On the You page the shipped behaviour put the form row — five grosses on one rule — under the Dynamic Island.

**THE DRAWN CARD IS A COURSE, NOT A BAR CHART.** Eighteen neutral grey bars on near-black. `course-page-noimage.png` draws them at **#4A6155** — sampled off the artboard, and it is `ground.rule`'s own dark value, so the fix is a token rather than a choice. 2.7:1 on the pinned ground against the 1.3:1 the neutral gave.

**THE TAB BAND KEEPS ITS WORDS.** At AX3 it dropped all five destination labels, leaving unlabelled glyphs for the golfers most likely to need words. They stay and SHRINK rather than wrap: `COMPE/TE` is not a label. And the AX3 board line prints `4 back`, from a producer, rather than `+4` with `BACK` welded on.

**Also:** Home's floor drew its top rule full-measure and its three separators inset 20pt on one edge — an asymmetric hairline reads as a rendering fault. The season head gives ~28pt back to the table.

**Gate:** `build-tokens` clean · preflight PASS · sunningdale PASS · 980 tests / 161 suites.

## IOS-059 · The repair pass — the delete, the total, the heat ramp and the wire's echo — **BUILT 2026-09-07 (D284; owner-authorised "build it", 2026-09-06)**

The fifth repair commit.

**THE PHONE COULD NOT DELETE A ROUND.** `YouScreen`'s comment says deletion belongs on the receipt; the receipt never got one, so for four waves the capability existed only on the desk — which D234 forbids. It is on the receipt now, two steps, owner-gated, with `RoundCopy.deleteConsequence` read by both clients.

**THE RECEIPT ENDED IN PROSE.** `POINTS` is board caps beside a `figure` 27 now, with `THIS MONTH` / `COUNTING #1 OF 4` in agate beneath — and the denominator the leaf was already holding is printed, which is L-01 on the surface that exists to show its work.

**THE FORGE'S HEAT RAMP WAS ONE COLOUR.** `UI_AUDIT` §311–320 names warm → hot → fire → ink as one of the four things that must survive; Wave 0a's mechanical mapping sent three of the four stops to `brand`, so the door drew three indistinguishable ember tracers and one white, and the wordmark's "white-hot with an ember halo" was two layers of the same colour. The palette has one hot metal now, so heat is its intensity: an ember coming up to white, four stops, every one a token at a token alpha, no gold. `DoorTests` asserts the CLIMB — the old test asserted only the timing, which is why nothing caught it.

**A FACE STANDS ON THE GROUND IT IS DRAWN ON.** `CSFace` takes `over:` the way `CSRule` and `CSSlot` do, so a roster on a pinned `ceremony` plate resolves its pigment against that plate rather than against an ancestor's theme modifier.

**THE EVENT'S DATELINE IS ITS SUBTITLE AGAIN** — eyebrow → title → dateline → roster, not title → roster → dateline, which put six faces between an event's name and where it is played.

**AND HOME SAID ONE THING TWICE.** `HomeFeedFold` folded system notes across leagues and left moments alone, so a personal best fanned to two leagues printed two identical wire rows one row apart.

**Gate:** `build-tokens` clean · preflight PASS · sunningdale PASS · **982 tests / 162 suites** — up two, with a new `HomeFeedMomentFoldTests`, a `CredentialBandTests` and the heat-ramp assertion.

## IOS-060 · The repair pass — the live sheet's act comes back up the screen — **BUILT 2026-09-07 (owner-authorised "build it", 2026-09-06)**

`lb-live.png` sets the match block in **two** lines. The build set it in four — `MATCH PLAY · NET BEST BALL · YOU + DANNY VS CHUCK + GARY` wrapping to two on a 402pt measure, then the status, then `THRU 14 · STROKES OFF LOW MAN (CHUCK)` — which pushed `FINISH THE ROUND`, the surface's one primary, off the screen a golfer reads standing on a tee with one thumb.

Two lines now, and each clause moved to where it is READ rather than where it was produced. The pairing goes to the scorecard, beside the strokes it decides. `THRU n` rides the status, where the eye already is — and **not** on a closed match, because `WIN 3&2` already says how far it went and a test caught that the moment the clause moved. `STROKES OFF LOW MAN` is a rule about the scorecard and is read there. A stake and an estimated card are facts about this round and stay; an empty meta line draws nothing.

**And the live hole head said `SI`** — `PAR 4 · SI 14`, on a surface a golfer reads eighteen times a round, after the terminology sweep. TERMINOLOGY §3.1's ruled replacement is `HCP`.

**Not fully closed, and measured:** on a FOUR-player round the primary is still a short scroll away. Four 60pt score rows, the eighteen-cell hole strip and the hole header are ~640pt before the match block starts; the artboard's round is a pair. The block is the artboard's two lines and ~120pt came back, which is the half of this that was a layout defect rather than a content count.

## IOS-061 · The wire reads as an edition — datelines, one league-note line, and the viewer is "You" — **BUILT 2026-09-07 (D285; owner feedback from his own phone, 2026-09-07)**

*The owner installed the overhaul and filed three things. This is two of them: "Things look text heavy, sections arent differentiated."*

**WHAT HE WAS LOOKING AT, MEASURED RATHER THAN DESCRIBED.** His wire is **fifteen rows**, and the instrumented composition says what they are: one digest, one round, four ranked items, one board moment — and **eight `N league notes` counts**, three of which are the same two leagues at three different periods. Every one of the fourteen non-round rows is the same object: `bodyS` 15 in `mut`, a 34pt leading date column, a chevron. That is not a rendering accident. `surfaces/home.md` §1.4 rules the buckets into **row markers and not section heads**, and the build honours it exactly; four of the five designed weights have no data on any account this product has (`BUILD_REPORT` §3), so the fifth drew everything.

**THE DATE COLUMN IS THE MECHANISM, AND DELETING IT IS HALF THE FIX.** A 34pt leading column in front of every sentence pushes each headline off the margin that the masthead, the lead, the ME strip and the floor all align to — so the wire is the one block on Home that does not start where the page starts. And it repeats, because a period repeats: `Sun · Sun · Sun · Aug 31 · Aug 31`. The stamp moves to the **trailing** edge, a row under `TODAY` prints none at all, and `HomePage.sayItOnce` drops a stamp that matches the row above it **inside its own group** — so the first of a run carries the date and the rest carry the sentence.

**`HomeSectionRule` GAINS A SECOND WEIGHT RATHER THAN A THIRD COMPONENT.** `.section` is unchanged — `agate` 12 at `mut` with a rule to the margin, which is what `THE WIRE` still is. `.period` is the dateline: `displayS` 24 in `ink`, no rule and no box, with `s5` of air above it. It is `displayS` and **never** `display`: §1.5 allows one `display` per viewport and the masthead has it. It sets `fixedSize(horizontal: false)` deliberately — `CSSectionHead`'s own comment records the AX3 shear that `horizontal: true` caused on the season page, and a head that refuses to wrap is a row wider than the screen.

**THE RANKED ITEMS GET WEIGHT 3, WHICH HAD NO PRODUCER AND NOW HAS ONE.** A clash, a standing, a plan and a circle story are `body` 17 in `ink` on a 56pt row with the item's **own clock** — `HomeWireCopy.stamp` answers `Tomorrow` / `6 days` ahead of today and the day marker behind it, where the shipped row computed `Sun` for a clash that closes in six days and printed the one fact a golfer already had. **It does not wear ember.** §2.4 gives the live metal two seats per viewport and Home's lead holds both (its dot and its door), so a live thing on the wire reads as live through its clock and its weight. Three visible steps now: the round's slat (a 38pt face, a 17pt name and a 27pt rule-and-figure), the ranked item (17, `ink`), the quiet line (15, `mut`).

**AND A DEFECT THE SLAT HAD BEEN CARRYING SINCE WAVE 1.** `CSRule` is a bare `Rectangle`, so a `CSFigure`'s stack reads as FLEXIBLE inside an `HStack` and took an equal share of it: `frame(minWidth: 62)` capped nothing, the 2pt rule ran halfway across the page, and *"89 at UNM Championship — 1.4 over your playing HCP."* broke over four lines beside it. `fixedSize(horizontal: true)` proposes the column its own ideal width — which is the label's, and which is what §0.2 means by *a rule the width of its column*.

**DEF-3, ON THE SURFACE IT WAS WRITTEN FOR.** The wire printed *"Jerecho set a personal best. New number to chase."* to Jerecho. `HomeWireCopy.viewerVoice` resolves the viewer's own name to `You` in the subject seat and `you` in every other, and gives second person its copula (`has` → `have`, `is` → `are`, and six more). The post's `member_id` is the authority — `round_to_board()` stamps `lm.id` — which is what keeps two golfers who share a given name apart; D238's person-homed rail carries no member row, so there the name answers. **The desk takes the same rewrite** (`csYouVoice`, against `CS.memberships[].id`), because a copy law is produced once (D234).

**THE ROW COUNT, BEFORE AND AFTER, ON THE SAME ACCOUNT:** 15 → 8, under three datelines, with one league-note line at the foot.

**WHAT IS NOT CLOSED, AND IS NOT PRETENDED TO BE.** Weight 2's photograph and weight 4's ceremony band still have no data on this account and weight 3's *course discovery* still has no producer — this wave gave the ranked item the row that was empty, it did not invent a course. The AX3 evidence for the dateline head itself is the wrap-safe construction plus an unsheared AX3 page: at AX3 the wire is taller than the screen and the simulator has no scroll hatch, so the head is photographed at `xxxLarge` and the page at AX3. And the desk's Home feed still renders one row per post with **no fold at all** — D217 was only ever built on the phone, and that stays true.

---

## IOS-062 · Compete's sections have a size, and where you stand is a figure — **BUILT 2026-09-07 (D286; owner feedback from his own phone, 2026-09-07)**

**ONE SECTION HEAD IN THE PRODUCT, AND THIS IS THE WAVE THAT PROVES IT BY USING IT TWICE.** D285 built Home's dateline as `HomeSectionRule(weight: .period)` — a private object in `CupSeason/Home/`. It is now `CSSectionHead(_, weight: .display)` in `CSDesign`, and `HomeSectionRule` keeps only the agate block name it was documented to keep. **The default weight is `.label` and is byte-for-byte the shipped head**, which is what lets a type used on nine surfaces gain a second weight without restyling one of them; a test asserts the default rather than trusting it. `count:` and `trailing:` belong to `.label` — a 24pt line with an 11pt rider beside it is two heads.

**THE ROW IS THE PAGE'S SUBJECT AND IT SHIPPED AS THREE GREY LINES.** `CompeteRoot.Row.rank` (`place`, `of`) is the Kit's half; `CSFigure(_, size: .m, label: "of 8", ordinal: CSOrdinal.suffix(place))` is the phone's. The Kit carries **two numbers and nothing about how they are drawn** — the ordinal is a `CSDesign` symbol and the row builds it, which is the same division `HomeWireSlat` makes for a gross. The figure takes `fixedSize(horizontal: true)`, the line D285 had to find the hard way: `CSRule` is a bare `Rectangle`, so a figure's stack reads as FLEXIBLE inside an `HStack` and takes an equal share of it — the 2pt rule then runs half the page and the sentence beside it breaks over four lines.

**AND THE SENTENCE STOPS SAYING THE RANK.** `SeasonFacts.seasonLine` gains `rank:` beside `week:` — the same producer's third grain, not a second producer (D234). A test asserts the money clause survives, the race clause survives, and no orphan separator is left where the rank was.

**THE LOADING STATE WAS TWO ROUNDED RECTANGLES.** `.loading` drew two 68pt `bg1` cards — the audit's finding 1 (a card standing in for content) on the one frame nobody thinks to photograph. It is now the head and two rows the page is about to render, redacted (§13.2).

**AND THE MASTHEAD'S DOOR WAS BREAKING ITS OWN LINE.** `START SOMETHING ↗` measures wider than the 362pt measure leaves beside `COMPETE` and `MON · SEP 7`, so the glyph wrapped onto a second line under the words and sat beside the dateline. The arrow is deleted — it was the **last typed arrow on the phone**, which `LINT-13` forbids by name (*a link's arrow is absorbed into its underline*) — and the label holds one line.

**`CSDoorRow`, BECAUSE THE DRAWING LIVED THREE TIMES IN ONE FILE.** `HomeFloor.row`, `HomeFloor.elsewhere` and `HomeFirstRoundRows` each drew a verb-plus-gloss row, the third carrying the comment *"drawn in the floor's own grammar so the page has one row shape and not two"* — which is the argument for the type. It carries the AX5 shear note with it (the verb must not hold its line, or the whole page goes 466pt wide) and takes an optional action, so a row that STATES and a row that OPENS are the same object. Compete's foot is its fourth caller. Home's floor was re-photographed after the extraction and is unchanged.

**`-cs_dev_compete_fixture`, AND WHY A NEW HATCH.** Compete lives behind the tab bar and the tab bar lives behind a session. This wave opened with the simulator's refresh token expired (`/auth/v1/token` → 401) and the phone at the door, and **a wave that cannot photograph its surface has not looked at it**. The hatch substitutes ONE VALUE — the `Me` — decoded from `native_home`'s own JSON by the app's own decoder, so `CompeteRoot.make` is still the thing under the camera; it renders the real screen over the root, reads nothing, and writes nothing. Same posture as `-cs_dev_home_state`, `-cs_dev_season_fixture` and `-cs_dev_event_fixture`. Its five rows carry every branch the wave built: `2ND OF 2`, `1ST OF 8`, a preseason with **no** figure, a moment with none, and a wrapped season's finish. The session was recovered mid-wave (`-cs_dev_email` / `-cs_dev_code`), so the acceptance shots are the **owner's real account** and the fixture is the regression harness rather than the evidence.

**THE SWEEP, AND WHAT IT DELIBERATELY LEFT.** Changed: Compete (three heads), Home (its dateline is now the shared object). Photographed and LEFT: the **Golfers** tab — its `THE BOARD` head is correctly `.label` (a caption over a table that carries a rank rail, faces, names and figures), and its `RIVALS` / `BRING SOMEONE IN` / `FINDABLE BY` heads do have the defect, but the page carries **six sections and two rounded-rectangle cards on one scroll** and six 24pt heads would make it worse; the **schedule** — same, plus a card calendar and a four-line mono sentence. Both need their own pass. Left by the rule this wave wrote into `CSSectionHead.Weight`: settings, the sheets (post, live setup, plan, pot), the season page and the leaderboard — their rows carry their own weight, so the head is a caption and 24pt would shout.

**THE WEB HALF (D234):** none is owed in copy — nothing shared moved, because `seasonLine`'s new grain is only called where a figure is drawn and the desk draws none. What the desk **does** owe is the head weight: `.grouphead` is still mono 11.5px tracked caps over stat blocks, and that lands with the desk's own typography pass (§14.5 turns the roles on one at a time). Named here rather than half-done in a session that cannot drive a browser.

**EVIDENCE.** `-cs_dev_open compete` on the real account in dark, light and AX3; `-cs_dev_compete_fixture` in dark, light and AX3 for the branches the account has no data for; `-cs_dev_open golfers` and `-cs_dev_open schedule` for the sweep; `-cs_dev_bottom` for Home's floor after the `CSDoorRow` extraction. Every one was read.

## IOS-063 · A round is on the front page once, and the wire opens with its own dateline — **BUILT 2026-09-07 (D287; the review pass over IOS-061/IOS-062, owner feedback 2026-09-07)**

**THE OWNER'S HOME OPENED BY TELLING HIM ABOUT HIS 89 TWICE.** With IOS-061 shipped, `-cs_dev_bottom` and the top of Home together read: `THE WIRE` · `Fri, Sep 4 — You posted 89 at UNM Championship` · `COMING UP` · … · `THIS WEEK` · the 68pt slat with the face, `89`, `GROSS`. Same round, two weights, one screen. The loose row is `HomeDigest`'s **quiet frame** (D27) — and that file's own comment already rules that *the digest yields* when something else on the page is carrying the story. It was given `spent:` = the ranked deck's rounds; nobody gave it **the wire's**, and the wire is where rounds draw.

**SO THE PRODUCER DECIDES IT, ON THE ROUND ID.** `HomePage.make` gathers the buckets' `round_id`s before it appends the digest row and drops the quiet frame when its `roundId` is one of them. Narrow on purpose: `.since` is a real summary of what changed and is **never** dropped; a quiet frame whose round the wire is *not* drawing still renders, because that is the day the frame exists for. It is a test (`HomePageTests`) rather than a look, and it sits beside `wireEmptyItem`'s removal — the page already had one precedent for "something else spent this, so the wire does not draw it".

**AND IT WAS PUTTING A DATE BACK AT THE HEAD OF A SENTENCE.** The quiet frame's body is built `day — line`, so the first row of the wire read `Fri, Sep 4 — …`: the 34pt leading date column D285 deleted, in prose, in the one row that sat above every dateline.

**THE WIRE'S BLOCK NAME GIVES WAY TO ITS FIRST DATELINE.** `THE WIRE` is `agate` 11 caps in `mut` with a hairline; `COMING UP` is `displayS` 24 in `ink`. Stacked with 24pt between them they are two headers for one block **and the outer one is the quieter** — §27's *repetitive header* and §8's hierarchy upside down, on the page the owner was complaining about. `HomeSectionRule(page.wireTitle)` now draws on every branch that has **no** dateline under it — the first-round rows, the redacted skeleton, the failed read, the wire's empty block, the roster empty — and yields where `wireRows` will emit a `.display` head. Nothing else moved: the head is the same object with the same title, drawn or not drawn on one condition.

**WHAT THE REVIEW LOOKED AT AND LEFT ALONE**, so a later reader does not think it was missed:
- **Home's floor is four identical caps rows** — `ADD MY ROUND` · `START SOMETHING` · `JOIN WITH A CODE` · `FIND GOLFERS`, one weight, four rules, no primary, and on the owner's account it is the **loudest block on the bottom of the page**, louder than the news above it. `BRIEF` §18 names it (*avoid five equally prominent buttons*) and §8 wants a tier. It is not drawn that way by accident: L-25 rules all four doors onto every Home and the collapse to one `SOMETHING ELSE` row fires only on `pageHasPrimary`, which a live lead does not set. Changing that is a **ruling**, not a fix, and it is written up in `BUILD_REPORT` for the owner rather than decided here.
- **At the accessibility sizes the 1.6× head step collapses to about 1.05×.** `displayS` is capped at 1.8× (43.2pt) and `name` is **uncapped**, so at AX3 `YOUR SEASONS` and `WHO'S THE BITCH?` render at the same size and Compete's sections stop being differentiated exactly where a reader most needs them to be. IOS-062's `SystemTests` assert the step at the **default** size only — a test that passes while the thing it guards fails. The fix is a cap on `name`, which changes every row title in the product, so it belongs to a typography pass and not to a review.

**THE GATE.** `build-tokens` · `preflight` PASS 0/0 · `sunningdale` 27 assertions · **1,172 tests in 207 suites, 0 failures** (52 app + 120 `CSDesign` + 1,000 `CupSeasonKit`). Baseline `0b4217b` was 1,150 / 204; IOS-061 took it to 1,157, IOS-062 to 1,169, this to 1,172. **Read the count from THREE bundles.** `xcodebuild test` will silently skip the 52-test app bundle on a warm `derivedDataPath` and print only two `Test run with` lines — which reads exactly like 22 tests having vanished. Grep for every `Test run with` line and add them, and if only two appear, the run is short a bundle, not the suite.

## IOS-064 · The door has two registers, and floating chrome stops eating live content — **BUILT 2026-09-07 (the re-score's one BUG, `ROAD_TO_TEN` §3 rank 6 and §5.1)**

*`ROAD_TO_TEN` put one item ahead of everything else on its list and said why: "This is the only item on this entire list that is a bug rather than a score." It is the first screen of the product, and on a 375pt phone a new golfer could not complete it.*

**THE DOOR COULD NOT BE COMPLETED ON A 375pt PHONE, AND IT WAS WORSE THAN THE RE-SCORE PHOTOGRAPHED.** `se-door.png` caught the scrolled state: the mark cut in half by the status bar, the email field half under the keyboard, `CONTINUE WITH EMAIL` entirely off the bottom. The captures taken to check it (`se-door-BEFORE-dark.png`, `-light`) caught the state a golfer actually arrives in — the door auto-focuses email on the Forge's handoff, so the keyboard is up from the first second, and the field was not half covered, it was **entirely gone**, along with the action, the hint and the legal. At AX3 (`se-door-ax3-BEFORE-dark.png`) the mark was off the top of the screen and the paragraph was all there was.

**THE CAUSE IS NOT A CONSTANT THAT IS 20pt WRONG, SO THE FIX IS NOT A CONSTANT EITHER.** The door was drawn once, at one size, for a tall phone: a 260pt crest, a 48pt entrance pad, a four-line pitch, and only then the field. That column runs ~605pt to the bottom of its own primary; an SE with an email keyboard up has 417pt of viewport. **No keyboard-avoidance machinery makes 605 fit in 417 with the mark still whole** — the ceremony has to have a second register, and `DoorLayout` is it. The door reads its WINDOW once (a window height does not move when a keyboard rises, so nothing thrashes, and `-cs_dev_text_size` still flips it) and picks:

- **ceremony** — a tall phone at a reading size: the Forge at 260, the pad at 48, the pitch above the field. Byte-for-byte the shipped door. `max-door-AFTER-dark.png` is the proof and it is the strongest shot in the set: **keyboard up, the full crest, the pitch, the field AND the primary, all on screen, zero scroll.**
- **working** — a window under 700pt, or ANY device at an accessibility size: the crest keeps every part it has and draws the cup at 168 instead of 260, the pads give 44pt back, and the marketing paragraph rides BELOW the action instead of between the mark and it. The invited stranger's sentence — the one that names the season the email is being handed to — never moves, because it is the answer to "what am I joining" and it is owed before the field, not after it.

`crestWorking` was 132 first. It fitted and it looked wrong — a small icon over a full-size wordmark — so it is the LARGEST cup that still leaves the action clear, which is a different question from the smallest one that fits.

**AND THE ACTION, NOT THE FIELD, IS WHAT CLEARS THE KEYBOARD.** The system scrolls the first responder into view and stops there, which is why the button below it stayed hidden. Every stage of the door now tags its own primary `DoorLayout.action` and the door scrolls THAT to the bottom edge, 360ms after focus so the keyboard's inset has landed (a scroll in the same runloop measures the pre-keyboard viewport and undershoots by the keyboard's whole height). `scrollTo(_:anchor: .bottom)` is the MINIMUM scroll that reveals a target and it clamps at zero — so a phone where the column already fits does not move at all, which is what keeps the mark WHOLE in the working register rather than merely reachable. At AX3 it scrolls, because at AX3 the tagline alone is 260pt; there the mark goes and the action arrives, which is the right trade.

**THE DOOR ALSO HAD NO STATUS CAP.** It was the one scrolling surface DF-14 missed, which is the whole of "the app mark is cut in half by the status bar". `csStatusCap(cs.bg0)` now, like the other six.

**Not a constant, and asserted as such:** `DoorLayoutTests` adds eight tests. The register is a pure function of two numbers, so the device split, the accessibility override and the settle window are unit tests — and two of them do the ARITHMETIC: the working column must end above an SE keyboard, and the ceremony column must NOT, because if it ever fits, the second register is dead weight and should be deleted.

**THE SAME FAULT CLASS, SWEPT.** `ROAD_TO_TEN` §3 named four guillotines and three were invisible until the appearance and text-size hatches shipped:

- **Home's 28pt fade is gone, and its reason went first.** §1.6 ended Home in a fade to `bg0` so a FLOATING tab pill could not guillotine a row mid-glyph. Wave 8 stopped the band floating — `MainTabView` says so in its own comment: *"the band is a sibling in the stack above… a page simply ends where it ends."* There was nothing left to float over, and what the fade did instead was erase the bottom 28pt of live content: `light-home.png` caught `THIS WEEK` faded to a ghost, reading as **disabled** in the light printing while the dark one hid the same damage against its own near-black. A remedy that outlives its defect becomes one.
- **The composer's pinned foot at AX3 took roughly a third of the viewport.** A two-line readout, a wrapped ember primary and a second wrapped action, with the scroll content above reading four words a line through a slot the height of a business card (`composer-ax3-BEFORE.png`, cut mid-word at "season's own math"). Chrome that takes a third of a phone is not chrome. At an accessibility size the foot now carries only what has to be pinned — the live readout that answers the number being typed, and the one act the surface exists for — and `Start over` rides at the foot of the page, where a reader who wants to abandon can still reach it and every other reader gets ~250pt of their screen back.
- **`dark-schedule.png` is NOT one of these, and the re-score has it wrong.** It reads the cut ember button as "the page's own PINNED ember primary cut by the tab band". `ScheduleScreen` pins nothing: `Put a round on the schedule` is inline scroll content with a section head, a list and the weeks below it, and the shot is a scroll position, not a guillotine. Nothing is chrome-cut there and nothing is changed. Filed rather than quietly skipped, because a fix list that quietly drops an item is a fix list nobody can audit.

**The web half (D234).** The desk already gives the crest back on a short WIDE viewport (`@media(min-width:760px) and (max-height:820px)`); a short NARROW one had no rule at all, so a 375×667 browser drew the full crest and pushed the door's primary below the fold with the soft keyboard up. Same rule, other axis, one line.

**Gate:** `build-tokens` clean · preflight PASS 0/0 · sunningdale PASS 27 · **1,206 tests / 210 suites** (1,198 / 209 at HEAD, +8 in `DoorLayoutTests`).

## IOS-065 · A photo on a round you already posted — the receipt is where it goes — **BUILT 2026-09-07 (D293; owner from his own phone on build 733, 2026-09-07)**

*"I just posted a round at Dinosaur mountain but I cant go back to add a photo. I may have missed that option to."* — and the second sentence is the worse one, because he assumed the failure was his.

**HE MISSED NOTHING; THERE WAS NOTHING TO MISS.** `post_round`'s `p_photo_path` is the only path a photograph has ever taken to a round. `grep p_photo_path` over `Generated/Rpc.swift` returns `post_round` and `set_profile` and stops. No RPC anywhere attached, replaced or removed a photograph on a round that already existed — §16 says a posted round is not mutated and D37 killed `rounds_owner_update` by name, so the only write left on a posted round was `delete_round`. **The product asked for the photograph at the one moment a golfer is least able to supply it**: the score is typed in the car park; the photograph is on the phone from the 12th, or arrives from somebody else in the group on Tuesday. D293 argues the §16 scoping in full — a photograph is in none of the chain §16 protects — and the migration enforces the argument rather than trusting it.

**THE AFFORDANCE GOES ON THE ROUND'S OWN RECEIPT, IN THE PHOTOGRAPH'S OWN SLOT.** That is where a golfer looking at a round he posted already is: the wire row, the album cell, the achievement slat and the board card all open `RoundReceiptSheet`, and D284 put the delete there for the same reason ("deletion belongs on the round's own receipt"). His round with no photograph now draws `Add a photo` under the head where the plate would be; his round with one draws the plate and then `Replace photo` and a two-tap `Remove photo`. **A round that is not his draws nothing**, the same gate the delete applies. The composer's pills are untouched — a photograph in hand at post time still goes on at post time — and the album's empty line stops naming the composer as the only door, because it is no longer the only door.

**IT DEGRADES HONESTLY, AND IT DOES NOT LEAVE AN ORPHAN DOING IT.** The migration is written and unpushed, so today every attempt meets a database with no `set_round_photo`. The order is upload, then call; on a function-missing error (`RpcError.isMissingFunction` — PGRST202 / 42883) the just-uploaded object is **taken back out of the bucket** before the failure reaches the golfer, because an object nothing points at is exactly the orphan `delete_round` had to learn about on 2026-09-01. What he reads, in `neg` under his own thumb rather than in a toast that has already gone (L-32), is *"Not attached — a photo on a posted round needs the next database push"* — the sentence form `csRateCourse` set. Every other failure says *"That photo didn't attach. Check your signal and try again."*, and neither ever shows a code.

**ONE PRODUCER, TWO SHAPES (D234).** `RoundCopy` holds all six sentences and both clients read the same words; `RoundPhotoService` holds the two calls and the upload-then-attach order; the desk's `csAttachRoundPhoto` / `csRemoveRoundPhoto` are its twins, guarded by their own try/catch so preflight 11 reports them as a push that is owed rather than a 404 a user can meet. The phone's picker is the composer's — camera when `NSCameraUsageDescription` is present, library otherwise — so the two surfaces open the same door to the same camera roll.

**THE MIGRATION SEALS THE ARGUMENT INSTEAD OF RESTATING IT.** `20261010090000_a_photograph_is_not_a_score.sql` writes two SECURITY DEFINER functions, each fenced to `profile_id = auth.uid()`, each granted to `authenticated` and revoked from `public, anon`, each reclaiming the object it stops pointing at. Its self-check RAISES if a function writes anything but `update rounds set photo_path`; if it so much as **names** any other column of `rounds` — and the forbidden list is read from `information_schema.columns` at push time, not typed into the file, so a column added next season is sealed the day it exists; if any touch of `rounds` is unfenced; if the attach stops fencing the path to the caller's own storage prefix; if `authenticated` cannot execute; or if `anon` can. The three regexes and the column scan were run against the real function bodies through the linked database read-only before the file was committed: two writes, both `update rounds set photo_path`; two touches of `rounds` and two fences; zero of the twenty-one forbidden columns named.

**Tests:** `RoundPhotoTests` asserts what a screenshot cannot — the two call contracts (name and `optionalArgs` empty, so the skew retry can never drop the round or the path), the upload path shape the server's `^{uid}/` guard requires, the affordance state machine (`RoundPhotoSlot.for(isMine:photoPath:)` — absent, offered, present, and never offered on someone else's round), and that the pre-migration sentence names the push rather than a code.

**The capture hatch, and the one lint line it cost.** Two of the three states cannot be reached on this machine — `simctl` has no finger, so the PhotosPicker cannot be driven, and the migration is unpushed, so an attach against prod cannot succeed even with one. `-cs_dev_receipt_photo <none|on|skew>` draws all three over whatever round the receipt opened, moving the photograph's two facts and the one note line and nothing else; the stand-in photograph is DRAWN, greyscale and abstract — a sky, a horizon and a ground mass at 3:2 — under `CredentialDev.photo`'s own rule that no place and no face is fabricated. That put it in LINT-04's sights, so `ReceiptPhotoDev.swift` joins `CredentialDev.swift` and `DeveloperHarness.swift` on that check's **named** exemption rather than moving a baseline: a baseline on a zero-tolerance check is a hole with a number written on it, and the lint's own comment says so.

**The desk's evidence was shot through the shipped path, not around it.** No browser automation was available this session, so the desk was photographed in the iPad simulator's Safari against a harness copy of `index.html` — the file verbatim, plus a theme line above the pre-paint script and a block that stands in for the two NETWORK edges (the session and `sb`) and then calls the shipped `openRoundReceipt`. The pre-migration shot is the strongest of the twelve because nothing about it is staged: a real `File` goes into `csRoundPhotoInput`, `csAttachRoundPhoto` uploads it, `set_round_photo` answers PGRST202, the object is taken back out, and the sentence the golfer reads is the one the code produced. The harness pages were deleted before the commit.

**Gate:** `build-tokens` clean · preflight PASS 0/0 · sunningdale PASS 27 · **1,218 tests / 211 suites** (1,206 / 210 after IOS-064, +12 in `RoundPhotoTests`).

## IOS-066 · The camera stands beside the number — the composer's plate, and the `Details` bucket is gone — **BUILT 2026-09-07 (owner from his own phone on build 733, 2026-09-07; the audit's and the re-score's shared conclusion on §20)**

*"Our scorecard looks good lets show it off."*

**THE CAMERA WAS THE BEST VISUAL SOURCE THE PRODUCT HAS AND IT WAS BEHIND A SCROLL.** `Add a photo` and `Scan the card` were two `CSMini`s under a `Details` head, below the fold, beside a date chip — three tertiaries of identical weight in a bucket named after nothing. `before-se-top.png` is the whole argument: the composer OPENS focused on the gross (IOS-030), so on a 375pt phone the keypad is up from the first frame, and everything visible above it is the number, one sentence and a disclaimer. Not a pixel of camera. The audit and the re-score both put photography as the single biggest ceiling on emotional appeal and premium feel, and the surface that supplies it hid the supply.

**THE PLATE IS THE FIGURE'S TWIN, WHICH IS WHY IT COSTS NOTHING.** The gross is a rule-and-figure in a 150pt column with ~185pt of dead ground beside it on the narrowest phone the product supports. The photograph goes there: **150 wide — the figure column's own width — and 100 tall, which is 3:2**, `CSPlate(.inset32)`, the same object and the same ratio the round receipt draws a photograph in (D293). The hero becomes two columns of one size: the number, and the picture, ending on the same line. It takes no vertical space that was not already blank, so it is above the fold on both phones with the keypad up, and `PostCameraColumnTests` asserts that as a SUM rather than trusting the screenshot of the day: the plate plus the 8pt gap plus a 44pt scan target plus an eyebrow must clear an SE keypad, and the two 150pt columns plus their gutter must fit 375pt.

**BOTH COLUMNS SIT ON THEIR OWN MARGIN.** The plate was parked 12pt from the figure for one build and stopped 22pt short of the trailing edge, which reads as a mistake rather than as composition — every other trailing element on this page (the inherited row's `Edit`, the rating row's value, every band's points) is on that margin. A `Spacer` puts it there.

**AN EMPTY FRAME, NOT A FOURTH BUTTON.** §17 asks empty states to be invitations and §27 asks for fewer controls. A frame the exact size and shape of the picture it wants tells a golfer what will happen and how big it will be; a pill under a head does not. It is `CSPlateWell` — new in `CSDesign`, because a rounded rectangle drawn outside the package is what LINT-10 counts and the answer to a lint is the component it is asking for. One hairline in `rule` (the system's single hairline, bent to the plate's own radius), the drawn `.photo` glyph, one agate word from `RoundCopy.photoAdd`. It was a `bg2` fill first and the shot said no: a filled tile beside an EMPTY gross box outweighed the number the screen exists for (§18). The remove is a ✕ on the picture's own corner rather than a third control, so the column is the same height full or empty and nothing under it jumps when a photograph arrives. **One tap, not two** — nothing is posted yet, so this is "wrong picture", where the receipt's is a deletion.

**SCAN AND PHOTO ARE TWO OFFERS ON ONE INSTRUMENT, AND THEY STOPPED PRETENDING OTHERWISE.** Adjacent pills of identical weight read as one choice with two labels. `Add a photo` attaches a memory; `Scan the card` READS THE ROUND and fills the scores — it is an alternative to the keypad, not to the picture. So the plate is the photograph and the scan is a plain tertiary beneath it, in words. What ties them is real and already in the model: `PostRoundModel.apply(_:row:)` sets the scan's own shot as the round photo, so **a scan lands in this plate**. That is why they share a column and why they are not one control.

**AND THE EPILOGUE IS DELIBERATELY NOT TOUCHED.** The brief's own candidate — put the invitation after the round posts, where the funnel is warmest — was considered and rejected, for four reasons, all of them load-bearing. (i) **The photograph is an INPUT to the celebration, not an output of it**: `model.recapPhoto` is read by `FinishCeremonyView`, which fires BEFORE the epilogue, so a picture attached there arrives too late for the one moment it would have improved most. (ii) It would depend on `set_round_photo`, which is written and unpushed, so it would be dead on the owner's phone today — while the composer's plate works immediately through `post_round`'s existing `p_photo_path`. (iii) §18: the epilogue already carries one ranked act, a share, a link and a revoke, and the audit demoted the artifacts specifically to protect the act; a fifth control there is the "five equally prominent buttons" §18 bans by name. (iv) The after-the-fact path already exists, on the round's own receipt, one tap from the epilogue's act (D293 / IOS-065). Warmth is not possession: a golfer has the photograph from the moment he took it on the 12th, and the composer is where he is holding it.

**`DETAILS` IS DELETED, NOT EMPTIED.** With the photograph and the scan gone it held one date chip — a section head, a rule and 34pt of chrome for a single pill, which is §27's "repetitive headers" exactly. The day is one of the round's circumstances, so it is a `CSRow` in the fold that already lists them, in the same shape as rating and slope, under the inherited line that already SHOWS the day and says `EDIT`. One fewer section, one fewer head, one fewer floating pill, and the rating row gains the hairline it stopped needing when it stopped being last.

**ONE LINE OF VERTICAL WAS RECLAIMED HONESTLY.** `A preview — your season's own math scores it on the books.` now renders only when there IS a preview. With no number typed there is no calculation to disclaim, and on an SE it ran to two lines of the small face at the exact moment the screen has nothing to preview. That is the space the plate is paid for with, and §27's own list names it ("excessive labels").

**At an accessibility size the row becomes a column and the plate is capped.** 3:2 of a 362pt measure is 241pt, and an empty frame that tall — before a number is typed — owns the screen. The cap is **180**, the height the composer's own photo preview used before this change, so the number is the product's rather than one invented here. It is applied to the WIDTH (270) so `CSPlate`'s own ratio still does the arithmetic: capping the height instead does not work, because `.frame(height:)` does not clip and the plate simply overflowed and drew over the scan — caught in `ax3` before it shipped, and the test asserts the cap is both real and pointing the right way (bigger than the reading size, smaller than 3:2 of the narrowest measure).

**The web half (D234).** The desk was WORSE: `#postPhotoRow` lived inside `#postCardFold`, which is `display:none` by default, so the camera was behind a fold as well as a scroll. It is the gross box's neighbour now — `.posthero` splits the number and a 150×100 plate, the `.plate` button carries the frame, the glyph and the word, the ✕ is its corner overlay, and `Scan the card` is the mini beneath. The button no longer relabels itself `Change photo`, because a picture in the frame already says what tapping does; the accessible name still says it, which is what `aria-label` is for. The words come from `CS_PHOTO`, which is `RoundCopy`'s twin. Under 460px the column collapses under the number.

**LINT-10, accounted rather than baselined.** The desk's plate and its corner ✕ add two boxed surfaces and the old 180pt preview `<img>` took one away; the phone's empty frame moved INTO `CSDesign` and took one more. Net zero, so the baseline is untouched at 261 — which is the honest way to pay for a new object, and the reason `CSPlateWell` exists as a component rather than as a rounded rectangle at a call site.

**Tests:** `PostCameraColumnTests` — five, and all five are arithmetic a screenshot cannot hold: the 3:2 ratio in both registers, the two columns fitting 375pt, the whole camera column clearing an SE keypad, the accessibility cap being real and pointing the right way, and the invitation reading from the one producer.

**Gate:** `build-tokens` clean · preflight PASS 0/0 · sunningdale PASS 27 · **1,223 tests / 212 suites** (1,218 / 211 after IOS-065, +5 in `PostCameraColumnTests`).

## IOS-067 · The card a round actually has — the scorecard lands on the round's own page, and on the artifact after it — **BUILT 2026-09-07 (D294; owner from his own phone on build 733, 2026-09-07)**

*"Our scorecard looks good lets show it off."*

**IT DOES, AND THE PRODUCT DREW IT IN ONE PLACE.** `CourseCardLeaf` — the bone leaf with `HOLE · PAR · HCP` — lives inside the course page, where it describes a COURSE. A ROUND, which is the object every surface in this app opens, showed a gross, a points figure and a receipt of arithmetic. The parts to fix that were all already here and nothing joined them: `round_holes` has held the strokes since the baseline; `round_holes_of()` has read them under `round_card`'s own visibility rule since 20260827130300, is declared in `Generated/Rpc.swift`, and **was called by nothing on either client**; `api_course_holes` caches par, stroke index and yardage per tee — 14,130 rows over 87 courses. `round_scorecard()` (D294, written and unrun) is the join.

**THE AUDIT'S FINDING 9 IS THE TRAP THIS STEP WAS STANDING IN.** The settlement card — *"the product's most beautiful object"* — exists ONLY as a share PNG, so the golfer who won the match reads a bulleted list while the beautiful thing goes to everybody else. The easy build here was the same shape: render a card into an artifact and leave the receipt alone. So the order is deliberate and it is the whole point — **`CSScorecard` is drawn on his own receipt first**, on paper, in his own theme, at his own text size, and `RoundCardArtifact` renders the SAME component for people who are not holding his phone.

**THE CARD FOLDS, LIKE A REAL CARD, AND THAT IS WHY IT FITS.** Nine columns and a total, then nine more. Eighteen in a row needs 542pt and never fits a 375pt phone; two blocks of nine need 308pt against the 311 a leaf inside the page gutter leaves, and always do. `se-card-full-dark.png` is that claim photographed and `RoundCardTests` is it asserted — including the converse, that eighteen in a row must NOT fit, because if it ever did the fold would be decoration and should be deleted. The shipped `ScorecardSheet` scrolls sideways because it carries a row per player and cannot fold; a round has one golfer and can. That sheet is untouched and `See the scorecard` stays where it was: it is a different object, the whole group's card for a live round.

**WHICH ROWS EXIST IS L-44, AND IT IS A PURE FUNCTION.** `RoundCardBlocks.build` is where the law lands on the phone: `Hole` always, `Par` only when the server proved one, `HCP` only when the index was proved SEPARATELY, and the golfer's strokes only when the set is whole and sums to the gross above it. Asserting the row COUNT — four, three, two — is asserting that the product cannot draw a par it does not have, at every size and in both themes at once, which no screenshot can do. `17pro-card-nopar-dark.png` is the two-row printing, and it is also **the state build 733 is in today**: the migration is unrun, `round_holes_of` answers, and a round with strokes draws its card now, without par, and gains the par row the day the owner pushes.

**ONE METAL, NOT A RAINBOW (§33).** A cell under par is `leafGold` — the system's EARNED metal — and everything else is ink. The composer's own grid paints eagle, birdie and bogey in three colours; on a receipt that is a heat map with golf written on it, and §33 names covering real golf with decoration outright. A bogey does not need to be shamed in red: the number says it. `RoundCardTests` asserts the mark array cell by cell, and asserts that a strokes-only card carries **no** metal at all — with no par there is nothing to be under.

**AT AN ACCESSIBILITY SIZE THE COLUMNS ARE COMPUTED, NOT GUESSED — and the first build was a ruin.** Pinned at 33/42/46pt, AX3 photographed with every two-digit cell truncated to `…`, every row label to `H…`, and a horizontal scroller with nothing to scroll, because `.frame(width:)` CLIPS rather than overflows. The widths now come from the metric — `CSType.renderedSize` × the mono advance — so a column is always wide enough for the widest thing in it, `.fixedSize` guarantees no cell is ever cut, and the `minWidth` floors keep the grid square when content is narrower. The block then genuinely overflows and D-5's scroller works with the row labels pinned (`17pro-card-ax3-dark.png`), which is the one rule `CourseCardLeaf` already knew: the `HOLE` row is not a column head, it is the key the other rows are read against, and hiding it makes the card unreadable.

**A NINE NEVER TAKES PAR, AND ITS TENTH COLUMN IS NOT CALLED OUT.** `round_holes` numbers a nine 1–9 whichever nine was walked (`PostCard.inputs` sums `0..<9` for `side == 9`), and nothing in the schema records which — the owner's own Palo Verde round is labelled `· Back` and its strokes are stored as holes 1 through 9. So the server refuses a nine a par row, and the block's total reads `TOT`. Calling it `OUT` would be the product asserting the front nine on no evidence, which is the same failure one word smaller.

**THE ARTIFACT IS THE SAME OBJECT, NOT A SECOND DRAWING.** `RoundCardArtifact` is 1080×1350 on the ceremony ground with `RecapCardView`'s furniture and D2's law intact (gross, the named band in the third person, course and date — no differential, no index, no league name, D60a). `CSScorecard` takes `over: .ceremony` — the system's own `CSRule.Ground` vocabulary — so it reads the ceremony ramp with no palette rebuilt at the call site, and a `Fixed` geometry so a share PNG renders the same picture on every device. Two artifacts about one round because they are about two different things: the recap card's hero is the NUMBER, and a hole-by-hole grid under a 300pt numeral would have been one artifact good at neither. **The marker carries no ring** — `RecapCardView` circles its marker, but on a canvas whose subject is already a ruled grid a second outline is §27's unnecessary border, and it is also the one `.stroke(` LINT-09 would have counted. **The block gap is a token of the artifact's own scale**: at 20pt rows the page's `s3` says "two nines"; at the artifact's 62pt rows the same 12 disappears and the card reads as one eight-row table. Caught on the first render.

**The web half (D234).** `csRoundCardRead` runs the same two-step — `round_scorecard`, then `round_holes_of` on a missing function and on nothing else — and `csRoundCardFromHoles` carries the same gross seal, because the fallback returns rows and the rule has to live once per client. `csRoundCardHTML` folds the same way and prints on **`.rcpt-leaf`, the receipt's own paper**, rather than on a second bone surface built beside it: a duplicate leaf is exactly what LINT-09 and LINT-10 count, and the answer to a lint is the thing it is asking for (§26). `drawRoundCardArtifact` is the canvas twin, in the house pattern beside `drawRecapCard`/`drawSettlementCard`, sharing `recapText` so the caption that lands in a group thread has one producer whichever artifact carried it. The gold on paper is `--leaf-gold` (#795912) — **6.4:1 on bone**, AA as ink; the receipt's own "gold ink never touches a leaf (1.68:1)" is about the PAGE's gold, a different value, and the distinction is now written down beside the rule.

**One preflight edit, in the open.** LINT-20 (*a leaf always holds a grid*) looks for `Grid|GridRow|HStack|ForEach|CSLeafRule|rows:|.earnedRule` within twelve lines of a `CSLeaf {`. `CSScorecard` **is** a grid — it is the printed card that lint exists to protect, drawn as a component instead of at a call site — so its name joins the alternation. That is the honest fix; restructuring the leaf to contain an `HStack` would have been gaming the check.

**Tests:** `RoundScorecardTests` (15) and `RoundCardTests` (11) — 26, and every one of them is arithmetic or a row count, because that is what a screenshot cannot hold. The seal in both directions, the gap, the nine, the fold, the two par routes arriving independently, the one metal, the empty card, the dateline naming only what was proved, the SE fit and its converse, and the VoiceOver sentence saying its gaps out loud.

**Gate:** `build-tokens` clean · preflight PASS 0/0 · sunningdale PASS 27 · **1,249 tests** (1,223 at HEAD, +26).
