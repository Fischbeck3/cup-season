# Reader "home" — Home on the phone, exhaustively

Repo `/Users/fischbeck3/cup-season` at tip `3bba87e`, read 2026-09-04. Read-only. Line numbers are as of tip. **SAW** = read in code or returned by a read-only prod query; **INFER** = deduced from code paths without running the app (no simulator was driven for this pass; there are no Home screenshots newer than the 2026-09-02 hard-look in the repo).

Files read in full: `apps/ios/CupSeason/Home/HomeView.swift` (1113 lines), `Home/HomeLeadCard.swift`, `Packages/CupSeasonKit/Sources/CupSeasonKit/Home/{HomeHeroCopy,HomeLead,HomeStream,HomeFeedFold,HomeLeagueRow,HomeDigest,HomeSocial}.swift`, `Models.swift` (`SeasonPhase`, `HomeMode`), `Schedule/UpNextChips.swift`, `Schedule/UpcomingRoundsSection.swift`, `People/InvitesBanner.swift`, `People/BuddyRequests.swift`, `Main/MainTabView.swift`, `Main/Presenter.swift`, `RootView.swift`, `Live/LiveRoundHost.swift` (`LiveResumeBanner`), `Live/LiveNowBar.swift`, `Onboarding/OrientationScreen.swift`, `Wizard/LeaguelessDoors.swift`, the Kit's `ScheduleModels` (`UpNext.chips`, `CalendarBuilder.homeRounds`), `PeopleModels` (`Invite`), `LeagueCopy`, `LiveCopy.resumeBanner`, `CSBands`, `CSReactions`; SQL `20260827130400_native_home.sql`, `20260902200000_the_hero_names_the_leader.sql` (v2), `20260716050000_home_feed_order.sql`, `20260723090000_home_feed_photo.sql`, `20260831160000_home_lead_and_clash_beats.sql` (`home_clash`); web `index.html` `renderHomeHero` 11198–11475, `heroFloorFoot` 11165, the `#view-home` slot tree 2992–3028; canon: `spec/decision-log.md` D176, D121, D216–D221, `spec/voice-and-tone.md`, `spec/brand-canon.md`, `Cup-Season-Guide.md` ("The four places"), and the prior audit's `issues.csv` rows that name Home.

Prod, read-only via `supabase db query --linked` (SAW, 2026-09-04): 39 profiles; 14 with no league membership; 27 with no accepted buddy; 20 with fewer than 3 posted rounds; 212 rounds total, **14 in the last 21 days across everyone**; 12 buddy pairs; 1 event; 1 upcoming booking. Leagues: six `My Cup` in `setup` with 1 member each (created Jul 17–21), `Fellas` (solo, 2 members, $75, active Jul 20 → Jan 18), `Who's the bitch?` (solo, 2, $0, active Aug 3 → Nov 2), `Sandbox` (complete, 8, sandbox=true), and four leagues all created 2026-08-28 at $75 — `Ridgeline Cup` (squads2, 8), `Fairway Society` (solo, 5), `Winter Circuit` (squads4, 9, first tee Aug 30), `Sunset Match` (squads2, 6, **cup_final ending 2026-09-05**). INFER: the four 08-28 leagues are seeded/sim leagues. Every active league's last system post is "August is in the books. The ledger is posted." (WTB's is a booking line); 6 open week clashes exist.

---

## Section 1 — The map

Home is one `ScrollView` (`HomeView.swift:43-137`) with a fixed slot order. Every slot is listed, including the ones that render nothing on most days.

| # | Screen / slot / sheet | How reached | What it shows first | Primary action | Exits |
|---|---|---|---|---|---|
| 1 | **Home root** (`HomeView`) | Home tab; push routes `.home`, `.invites` land here (`MainTabView.swift:428-430, 436-438`) | Header: wordmark "Cup Season" + eyebrow "THU · SEP 4" (`CSHeaderDate`, `Surfaces.swift:133-138`) + `+` (`HomeView.swift:48`). Nav bar hidden (`:156`) | none on the header | tabs; every slot below |
| 2 | **`+` menu** (`plusMenu`, `:178-190`) | tap `+` top-right; a11y "Start or join" | Menu: "Start a league" (flag) · "Start an event" (trophy) · "Join with a code" (key) · "Your golf calendar" (calendar) · "Find golfers" (magnifyingglass) | — | wizard full-screen cover · `EventPickerSheet` ("Start an event · Short form · its own little trophy", `EventPickerSheet.swift:25`) · `JoinLeagueFlow` ("Join a league · I HAVE A LEAGUE CODE", `JoinLeagueFlow.swift:33`) · `ScheduleScreen` push · `PeopleScreen` push |
| 3 | **Invites banner** (`InvitesBanner`, `:50`) | pending `my_invites` rows (loaded by the banner itself, `InvitesBanner.swift:65`) | one bordered row per invite: "League invite · PIGL" / "from Jerecho" or "Ryder invite · Desert Ryder" / "from Galen · first tee 2026-09-12" (`PeopleModels.swift:129-135`) | `Accept` (in line) · `Details` | Details sheet (medium detent): "A season-long league. Invited by Jerecho" / "A Ryder event — two teams, vs-index duels. Invited by Galen. First tee 2026-09-12." → `Accept & join` / `Decline` (`InvitesBanner.swift:66-81`). Accept → toast "Joined ✓", `store.reload()`, `preferredLeague` = the league (`:84-90`) |
| 4 | **Buddy requests** (`BuddyRequests(head: true)`, `:56`) | pending friend requests (own load, `BuddyRequests.swift:91`) | head "Requests · N"; `PersonRow` "@galen · wants to be golf buddies" (`:76-79`) | `Accept` · `✕` (a11y "Decline") | toast "Golf buddies ✓" / "Request declined"; `store.reload()` |
| 5 | **Live resume banner** (`LiveResumeBanner`, `:58-60`) | a live round in `LiveRoundStore` where I'm seated, stage `.live`, not a guest (`LiveRoundHost.swift:90`, `LiveCopy.swift:269-277`) | card, green spine: "Continue your round" / "PAPAGO GC · MATCH PLAY" / "HOLE 5 · THRU 4" / "→"; or ember spine: "Galen put you on the tee sheet" / "… · JUST TEED OFF · NOTHING SCORED YET" / "JOIN" | tap card | `LiveRoundHost` full-screen cover |
| 5b | **LiveNowBar** (above the tabs, not in the scroll) | same store; live or "ON THE TEE" while an accepted invite waits (`LiveNowBar.swift:29-46`) | ember bar "LIVE · PAPAGO GC · MATCH PLAY →" | tap | same cover |
| 6 | **Lead card** (`HomeLeadCard`, `:70-72`) | `HomeLead.choose` ladder (`HomeLead.swift:159-184`), only while `vm.leadLeague == hero league` | ONE of: clash / floor / move / milestone (see matrix). Ember spine. | one text button "… →" | clash → composer cover (`postOnComposer = true`) or receipt sheet; floor → composer; move → Clubhouse STANDINGS push; milestone → receipt (`HomeView.swift:162-175`) |
| 7 | **Hero** (`HomeHero`, `:74-75, 793-1000`) | always; mode from `HomeMode.of(me, preferredLeague)` (`Models.swift:305-318`) | eyebrow · big figure (+ caption tail, + move chip) · one sentence · foot lines (rule · endgame · money · owe) · "See the table →" | whole card is a `Button` **only with a membership** (`:802-812`) | `HomeRoute.league(id)` → `ClubhouseView` on STANDINGS (`MainTabView.swift:160`); owe line → `.pot(id)` (`:831-838`). League-less: not a door at all (`:810-812`) |
| 8 | **Other-league rows** (`HomeLeagueRows`, `:79-82, 1015-1069`) | 2+ non-wrapped memberships (`HomeLeagueRow.rows`, `HomeMode.pool`) | up to 3 rows: name + `sub` line ("Week 5 of 13 · 3rd of 10, 12 back of Galen", "First tee Sat Sep 5 · 5 on the roster", "Cup Final · 2 weeks left", "Forming", "Squads drawing") + `›`; then "and N more → Clubhouse" | tap row → `store.preferredLeague = id`, Home re-renders around it, haptic (`:80-81`) | "and N more" → Clubhouse tab (`:1050`) |
| 9 | **Occasion card** (`OccasionCard`, `:84-94, 374-407`) | calendar window from `Occasion.all` (`HomeStream.swift:201-214`), not dismissed this year, `leaguelessOnly` honoured | eyebrow "The oldest one" · "Links weather is a state of mind." · "One window, every card on one board, one name on the jug." · "Name the jug →" · ✕ | the act link | `.event` → `EventPickerSheet`; `.league` → wizard cover; ✕ → dismissed for the year; telemetry `home_occasion_tap` (`:87, :91`) |
| 10 | **Up Next chips** (`UpNextChips`, `:96-103`) | any of: next round (mine or with me, not RSVP'd out), a buddy's plan, invites+requests > 0, month ≤10 days out **and any membership** (`ScheduleModels.swift:374-410`) | horizontal capsules "NEXT ROUND · Mon Sep 7 · Gold Canyon with Galen ›", "BUDDY'S PLAYING · Galen · sat ›", "NEEDS YOU · 2 invites ›", "MONTH CLOSES · in 3 days ›" (warm ≤3 days, `UpNextChips.swift:50-62`) | tap chip | scheduled-round sheet / Schedule push / People push / Clubhouse STANDINGS push (`:97-102`) |
| 11 | **Section head "Around your buddies"** (`:108-113`) | always | eyebrow + hairline + "YOUR BUDDIES ↗" | the ↗ | `HomeRoute.people` push |
| 12 | **Digest row** (`HomeDigestRow`, `:116, 412-441`) | second visit onward (`HomeDigest.make`, nil on first visit `HomeDigest.swift:76`) | "SINCE YOU WERE HERE" · "**3 rounds**, a personal best from Galen, and **Galen** 🔥'd your 83." or "QUIET SINCE YOUR LAST VISIT" · "Yesterday — **Galen** posted 83 at Cave Creek GC" (+44pt photo thumb) | quiet frame's thumb | receipt sheet |
| 13 | **Feed** (`FeedBucketView`, `:118-131, 445-490`) | `home_feed(21)` ∪ league posts (announce/system/moment, 20 newest), folded (`HomeFeedFold.fold`), 30 max, buckets Today / This week / Earlier, cap 8 per bucket | round cards (photo ground or plain), post rows (📣/🏁 disc, body, "Fellas · Sep 1 · THE ROUND ›"), folded notes ("Fellas · 2 league notes this week ⌄") | tap card → receipt; face → Tour Card; 🔥 chip; long-press → six reactions; "Show 3 more · this week"; "Show earlier · N" | receipt / scorecard / scheduled-round sheets; `ClubRoute.board(id)` push from an expanded fold ("THE BOARD ↗", `:767`) |
| 13a | **Feed loading** | first load with nothing in hand | three redacted 76pt rectangles (`:118-119, 192-195`) | — | — |
| 13b | **Feed empty** | no rows and not loading | "No rounds from your buddies yet. Post one, or **add some buddies.**" — only the last three words are a link (`:120-126`) | "add some buddies." | `HomeRoute.people` push |
| 14 | **Coming up** (`UpcomingRoundsSection`, `:133`) | always (D176: never vanishes) | head "Coming up" + "THE CALENDAR ↗"; up to 5 `HomeRoundCard`s ("TOMORROW · WITH YOU" / "Galen · GOLD CANYON · 7:40a" / "YOU'RE IN" / "3 in" / 💬2 / weather glance) or the empty row "Put a round on the calendar →" (`UpcomingRoundsSection.swift:30-61`) | tap card | scheduled-round sheet; empty row and ↗ → `ScheduleScreen` push |
| 15 | **Pull to refresh** (`:141-150`) | pull | spinner through `store.reload()` + stream load | — | — |
| 16 | **Orientation screen** (`OrientationScreen`) | once per device, before first Home, only with no league/round/event and no pending join (`OrientedFlag.take`, `OrientationScreen.swift:85-91`) | "Four places. / Two ways to play." · "Thirty seconds, then you're in." · four rows (Home "Everything you're in, one feed" …) · "The long game / A league" · "The short game / An event" · `LeaguelessDoors` (Join a league · Start a league · Start an event) · "Take me in" | "Take me in" | Home |
| 17 | **Post cover** (⊕ tab) | centre tab; selection snaps back (`MainTabView.swift:336-339`) | "Golf" · "Play one live, post one you just finished, or plan the next" · three rows (`PostCoverView.swift:90-102`) | "Post a round — after you play" | composer |

Not on Home although `native_home()` delivers it (SAW): `events` and `open_duels` (`native_home.sql:398-456`; decoded at `Models.swift:233-234`; consumed only by `OrientationScreen.swift:90`, `YouRepository.swift:141`, `PushDev.swift:40`), `upcoming_rounds` (Home's card re-reads `sched.watch()`), `invites` (the banner re-reads `people.invites()`), `live_round` (the banner reads `LiveRoundStore`).

### The HOME STATE MATRIX as it exists today

`HomeMode` distinguishes exactly six modes (`Models.swift:286-292`): `leagueless(rung 7 | rung 6)`, `forming`, `preseason`, `season`, `cupFinal`, `wrapped`. Everything else in the brief's list is either a cross-cutting slot or **not distinguished**.

| State | Hero eyebrow · figure · line (quoted) | Foot lines | Hero door | Lead card can be | Feed when empty | Up Next / Coming up | Primary action offered |
|---|---|---|---|---|---|---|---|
| **Rung 7** — no live league, `rounds_count < 3` (`Models.swift:309`) | "Your card" · "0 of 3" · "Three rounds and your index goes live. Nothing else needed." (`HomeView.swift:875, 895, 951`) | none | **none** (card is not a button, `:810-812`) | milestone only (a buddy's PB/first/sub-80 today; `loadLead` runs with `m == nil`, `:313-332`) | "No rounds from your buddies yet. Post one, or add some buddies." | no month chip (no memberships); "Put a round on the calendar →"; occasion "fresh" Dec 27–Jan 15 only | **nothing on the card**. The ⊕ tab or `+` menu. Web: "Post your first round" (`index.html:11448-11451`) |
| **Rung 6** — no live league, `rounds_count ≥ 3`, **buddies ignored** | "Your card" · index e.g. "12.4" (or "—") · "Established. Nobody's seen it yet — you haven't joined a league." (`:895, 952`) | none | none | milestone only | same sentence | same | nothing on the card. Web rung 6 (no buddies): "Find your buddies"; web rung 5 (buddies, no league): "Four makes a league. You have 3." + "Start a league" (`index.html:11453-11474`). **The phone has no rung 5.** |
| **Buddies but no league** | identical to rung 6/7 — the buddies are not acknowledged; the line "Nobody's seen it yet" is false when buddies exist (their feeds carry your rounds via `home_feed`'s friendship union, `20260723090000:24-27`) | — | — | milestone | buddies' rounds DO appear (so not empty) | "Buddy's playing" chip possible | none |
| **No buddies** (and no league) | rung 6/7 | — | — | none in practice (milestone needs a buddy's round) | the empty sentence; on visit 2+, "Quiet since your last visit · Yesterday — You posted 91 at Papago" resurfaces your own round (`HomeDigest.swift:52-63, 96-99`) | — | none |
| **Invited** (league or event) | whatever mode applies (usually rung 6/7) | — | — | — | — | "NEEDS YOU · 1 invite ›" | `Accept` in the banner (slot 3). `OrientedFlag` skips orientation for an invited golfer only when a **pending join/claim intent** exists (`OrientationScreen.swift:88-90`), not for a server-side invite |
| **Forming — setup** (no season row, or phase setup) | "My Cup · forming" · "—" (days to start only if a season exists) · Pro: "Your league is still forming. Lock the bylaws and the invite link is yours." / member: "Galen is setting the bylaws. You'll see them the moment they lock." (`:881-882, 897-898, 956-962`) | cap only: "Best 3 rounds a month count" (`HomeHeroCopy.swift:154-165`); endgame nil without a season; money nil in setup (`:200`) | → STANDINGS, which reads "NO ROUNDS YET. / SQUADS FORM WHEN THE PRO LOCKS — STANDINGS START AT THE FIRST POSTED ROUND." (`LeagueCopy.swift:374-377`); "See the table →" shown (`HomeView.swift:839-842`) | milestone only (clash null: no active season; floor: pulse nil; move: gated to `.season`) | usual | **"MONTH CLOSES · in N days ›" appears** (gate is `hasMemberships`, `ScheduleModels.swift:405-407`) | **none** — the sentence names the lock and the card goes to the empty table. Web: "Name your league" / "Lock it in and invite your crew" (`index.html:11388-11391`). Prod: six such Pros today |
| **Forming — draft** (phase draft) | "Fellas · squads drawing" · days or "—" · Pro: "Bylaws locked. Draw the squads when the crew is in." / member: "Galen draws the squads before first tee — it's random." (`:958-961`) | cap; endgame (season exists); **money**: "$150 on the books · $0 collected · 2 still owe" (draft is not setup, `HomeHeroCopy.swift:200`); owe: "You still owe $75 · ask the Pro how to pay — money moves between you" | → STANDINGS (empty) | milestone | usual | month chip | none. Web: "Share the invite link" / "Draw the squads" (`index.html:11392-11393`) |
| **Preseason** (phase season, `starts_on` > today) | "Fellas · before first tee" · "26d" · "First tee in 26 days. Rounds before it build your number." (`:883, 899-901, 965-968`) | cap; endgame ("The top 2 golfers seed into a four-week Cup Final from Sat Dec 19 — scored fresh, so the regular season sets the seeds, not the winner. Level on points? Months won breaks it.", `LeagueCopy.swift:398-424`); money; owe | → STANDINGS (empty); "See the table →" | milestone (no clash row before week 1) | usual | month chip (meaningless — the edge month is waived anyway) | none on the card. Web: "Plan a round" + "Post a round" (`index.html:11394-11408`). The D121 row for a *different* preseason league knows "First tee Sat Sep 5 · 5 on the roster" (`HomeLeagueRow.swift:71-84`); the hero for *this* one does not say the roster |
| **Season week 1** | "Winter Circuit · week 1 of 24" · rank ordinal + "of 4" (both at 0 pts tie at 1st) · "Level with Galen · 0 – 0." or, with no standing row, "Standings start at the first posted round." (`HomeHeroCopy.swift:47, 85-87`) | squads: "Partial month · floors waived" / solo: "Best 4 rounds a month count · 0 posted · 26 days left in September"; endgame; money; owe | → STANDINGS | **clash** (two-person league, idle, shows once: "The clash · 6 days left" / "It's the two of you — every week is the clash." / "You — Nothing posted" V "Galen — Nothing posted" / "Post a round", `HomeLead.swift:75-85, 204-206`); bigger league idle → yields; floor no (partial); move no (no `prev_rank`); milestone | usual | month chip ≤10 days | clash card's "Post a round" when it fires; else none |
| **Mid-season, up** | "Fellas · week 7 of 26" · "1st" "of 2" · chip "▲ up 1" (warm; ≥2 hot, `:928-931`) · "You lead Jade by 22 · 31 – 9" / "6 clear of Jade" (`HomeHeroCopy.swift:49-66`); gold wash at rank 1 (`:865-867`) | rule ("Month floor 2/4 · 2 more" / "Month floor met · 6/4" / solo cap+posted+clock); endgame; money; owe; "See the table →" | → STANDINGS | ladder: clash → floor (≤3 days, short, squads only) → **move** ("Up 1" / "1st of 2. A new leader." / "See the table") → milestone | usual | month chip ≤10; next round; buddy's playing | lead card's action (composer / receipt / table); else nothing but the table |
| **Mid-season, down** | chip "▼ down 1" (cool, `:931`) · "12 back of Galen · 9 – 21" (n=2) / "12 back of Galen · 3 back of 2nd" (rank>2) | same | same | move: "Down 1" / "3rd of 10, 12 back." / "See the table" | | | |
| **Mid-season, held** | chip "— held" (mut, `:932`) · same lines; "Level with Galen · 14 – 14." | same | same | no move card; clash / floor / milestone | | | |
| **Clash open, I'm in it** (`home_clash` non-null) | any season hero | | | "The clash · closes today" / "You v Galen. Best round of the week takes it." / "You — 83 · +2.4 · SAT" V "Galen — Nothing posted" / "See the receipt" · "Post a better one" · "Post a round" (`HomeLead.swift:195-227`). A named rivalry replaces "The clash" in the eyebrow | | | the card's verb |
| **Floor short, month closing** (squads, ≤3 days, not partial) | | "Month floor 2/4 · 2 more" | | "The month closes in 2 days" / "You're 2 short of the floor. One round covers it." / "Post a round" (`HomeLead.swift:229-235`) | | "MONTH CLOSES · in 2 days" (warm) | composer |
| **Cup final — finalist** | "Sunset Match · cup final" · "1st" "seed" (gold at seed 1, `:868`) · "Four weeks, scored fresh. Whoever's hottest takes the cup. 1 week left." (`HomeHeroCopy.swift:122-140`) | rule (cap/floor still apply, `:161-163`); endgame (still the "seed into…" sentence, now past); money; owe | → STANDINGS | clash / floor / milestone; move never (phase `.cupFinal`) | usual | month chip | none beyond the table |
| **Cup final — non-finalist** | "5th" "of 8" · "Galen v Jade for the cup. Your place on the table is still live — 12 back of Galen. 1 week left." (`:124-129`) | same | same | same | | | |
| **Wrapped** — shown **only when every membership has wrapped** (`HomeMode.pool`, `Models.swift:300-303`); a wrapped league beside a live one is invisible on Home | "Sandbox · season wrapped" · rank ordinal "3rd" "of 4" · squads champion: gold + "Your name goes on the cup."; **everyone else, including a solo champion**: "The cup's been lifted. Run it back." (`:869, 980-981`) | none (`:990, 997`) | → STANDINGS; "See the table →" | milestone only | usual | month chip still appears | **no run-back door on Home** (`RunItBackCard` lives in `LeaguelessDoors` → Orientation and the league-less Clubhouse, and `ClubhouseView.swift:172`). Web: "Season recap →" + "Run it back — Season 2" (`index.html:11251-11253`) |
| **Multiple leagues** | hero = `preferredLeague` if in the pool, else pool.first = server order (phase season first, then `joined_at desc`, `native_home.sql:145-146`) | | | lead card pinned to its league (`HomeView.swift:70`) | | | rows (slot 8) re-render Home; "and N more → Clubhouse" |
| **Live round open** | any | | | | | | slot 5 card + slot 5b bar, both to the same cover |
| **Events only** (in an event, no league) | **rung 6/7 hero** — nothing about the event, its duel, or its deadline (`me.events`/`open_duels` unused on Home) | | | milestone (event-mates' rounds are in `home_feed`'s circle, `20260723090000:32-34`) | event-mates' rounds appear | "NEEDS YOU" only for a pending invite | none |

---

## Section 2 — Flows

**F1 · First open, no league, no rounds (rung 7).** Goal: "what is this, and how do I add my round?" Friction: after "Take me in" the screen is a wordmark, a card that says "0 of 3 · Three rounds and your index goes live. Nothing else needed." with nothing to press (`HomeView.swift:810-812`), the sentence "No rounds from your buddies yet. Post one, or add some buddies." where "Post one" is plain text (`:123`), and "Put a round on the calendar →" which opens a calendar, not a post. The one way to post is the centre ⊕ → a cover titled "Golf" → "Post a round — after you play" → composer (`PostCoverView.swift:90-100`). Unnecessary complexity: three screens between Home and a score box. Terminology: "index goes live", "your card". Dead ends: the hero. Missing feedback: nothing says what happens when the round is posted. Delight opportunity: the hero could be the post button and the first receipt could be the aha.

**F2 · Returning, rounds posted, no league (rung 6).** Goal: "compete with someone." Friction: "Established. Nobody's seen it yet — you haven't joined a league." with no door; the doors are in `+` (a11y "Start or join", `:189`). Confusing: "Nobody's seen it yet" is false if buddies exist (rung 5 does not exist on the phone, `Models.swift:309`). Redundant: the Clubhouse tab carries a second, differently-worded set of doors ("Join a league · Start a league · Start an event" + "Add golfers", `ClubhouseView.swift:137-151`) versus Home's "Join with a code" / "Find golfers". Delight: name the buddy who just posted and offer "beat Galen this week".

**F3 · Pro of a forming league (the six `My Cup` accounts).** Goal: "get my crew in." Friction: hero says "Your league is still forming. Lock the bylaws and the invite link is yours." and the card opens an empty standings table (`:956-957, 803`); "See the table →" under it. The lock lives in the Clubhouse's League pane; nothing on Home points there. "MONTH CLOSES · in 26 days" chip appears for a league with no season (`ScheduleModels.swift:405-407`). Dead end: the hero. Missing feedback: no "1 on the roster / invite link" on the hero (the D121 row would say "on the roster" for a *different* league). This is D96's web finding ("seven pilots created a league and stopped, six are still alone in it") reproduced on the phone with the same six leagues still in prod.

**F4 · Member of a forming/preseason league.** Goal: "what do I do until first tee?" Friction: "Galen is setting the bylaws. You'll see them the moment they lock." — true, but the only action is "See the table →" to an empty table; preseason says "Rounds before it build your number." (jargon: number) and the foot already prints the endgame paragraph and the books ("$150 on the books · $0 collected · 2 still owe", `HomeHeroCopy.swift:196-214`) plus "You still owe $75 · ask the Pro how to pay — money moves between you" (`:225-237`) — money is the loudest line on a day nothing has happened. The web offers "Plan a round" and "Post a round" here (`index.html:11394-11408`); the phone offers nothing.

**F5 · In season, the daily open.** Goal: "where do I stand, what's today asking?" What works: eyebrow "Fellas · week 7 of 26", "2nd" "of 2", a chip, "12 back of Galen · 9 – 21" — the leader by name (`HomeHeroCopy.swift:46-80`). Friction: under that one good sentence sit up to four 13-pt mono lines (`Typography.swift:31`): "Best 4 rounds a month count · 2 posted · 26 days left in September" / the 40-word endgame sentence / "$150 on the books · $0 collected · 2 still owe" / "You still owe $75 · …" / "See the table →" (`HomeView.swift:827-845`). Confusing terminology (quoted): "— held", "Month floor 2/4 · 2 more", "Partial month · floors waived", "on the books", "scored fresh", "Months won breaks it", "seed". Redundant: when the move card fires, the move is stated three times on one screen (card eyebrow "Up 2", hero chip "▲ up 2", hero line). Missing: nothing personal about *my golf* (index, last round, streak) in season; nothing about *who* beyond one name; no "week closes Sun" anchor on the hero. Delight: the Gentleman Instigator has one line here and it is arithmetic.

**F6 · The weekly clash.** Goal: "beat Galen this week." What works: the card names the opponent, the deadline in days, both sides, and a verb that follows the state (`HomeLead.swift:195-227`); 0–0 mid-week yields (D216); the opener shows once. Friction: sides read "83 · +2.4 · SAT" — "+2.4" is an unlabeled band (`:210-217`); "V" as a separator. After settlement the result lives on the board as a system line ("All square in the clash. Nothing settled.", `20260831160000`), folded on Home into "Fellas · 1 league note today" — the *result* of the duel you were shown all week arrives as a folded footnote.

**F7 · Month floor.** Goal: "don't lose points." What works: the floor card fires at ≤3 days, short only, never for solo, never in a partial month (`HomeLead.swift:170-176`); the month chip turns warm at 3 days. Friction: "floor" is never defined on Home ("You're 2 short of the floor. One round covers it."); the foot's "Month floor 2/4 · 2 more" and the chip's "MONTH CLOSES" are the same fact twice with different nouns. Solo leagues get "Best 4 rounds a month count · 2 posted · 26 days left in September" — the clock was retired for solo on the web (D140, `index.html:11178-11190`) but the phone's solo foot still runs one.

**F8 · Multiple leagues.** Goal: "see both without leaving." What works: D121 rows with stage-appropriate sub lines; tap re-renders hero and lead card together (D218). Friction: the hero's default league is whatever the Clubhouse pager last landed on (`HomeView.swift:39, 45`; `MainTabView.swift:174`) — Home's lead can change because you swiped in another tab (INFER). A wrapped league beside a live one disappears from Home entirely (`Models.swift:300-303`) — the wrap is a story beat Home never tells to a golfer who also has a live league.

**F9 · Invited.** Goal: "say yes." What works: in-line Accept, a toast, Home re-renders around the league. Friction: "first tee 2026-09-12" is a raw ISO date in the banner and the sheet (`PeopleModels.swift:131-140`); every event invite is a "Ryder invite" because `my_invites` reports `kind = 'event'` for any event (`20260713180000_member_invites.sql:72`) (whether a Major ever sends a member invite: unknown, see §5); the Details sheet explains "vs-index duels" (jargon). "NEEDS YOU · 2 invites" counts buddy requests as invites (`ScheduleModels.swift:403-404`).

**F10 · Buddy request.** Goal: "accept a friend." Works in line ("@galen · wants to be golf buddies", `BuddyRequests.swift:79`), toast "Golf buddies ✓". Friction: on a pull-to-refresh the requests list does not reload (its `.task` runs once, `:91`); a new request appears only on re-entering the tab (INFER from the task modifiers).

**F11 · Live round.** Goal: "get back to my round / join the one I was put on." Works: two faces, one tap. Redundant: the same door is on screen twice (bar above the tabs + card in the scroll) while Home is the visible tab (`LiveNowBar.swift:34`, `HomeView.swift:58-60`).

**F12 · Reacting to a buddy's round.** Goal: "trash talk." Works: 🔥 one-thumb chip, six named reactions on long press ("heater", "the eagle", "dialed", "ice", "snake", "sandbagger"), optimistic flip with revert. Friction: the strip exists only when the round has a post in a league the viewer shares (`HomeSocial.swift:5-10, 30-33`; `HomeView.swift:502-511`) — a **buddy-only** round (no shared league) gets no reactions at all and no explanation. There are no comments on Home (only the digest counts them). Trash talk has no composer anywhere on Home.

**F13 · Planning.** Goal: "get a round on the books." Works: "Coming up" never vanishes; a booking with you says "WITH YOU" and "YOU'RE IN"; the feed hides the duplicate booking line (D219). Friction: the empty row "Put a round on the calendar →" opens `ScheduleScreen` rather than the declare sheet the app already has (`Presenter.swift:27`, `MainTabView.swift:499`), so the smallest planning action is two screens away; "Coming up" mixes league-mates' plans with yours, ordered by date only (`ScheduleModels.swift:307-315`).

**F14 · Events only.** Goal: "play my Ryder/Major." Friction: Home does not know it exists. No event card, no duel ("number to beat" is in the payload, `native_home.sql:424-456`), no session deadline, no "your team", no door to the event room. The hero says "you haven't joined a league". Dead end for the "short game" the orientation just taught.

**F15 · Season wrapped.** Goal: "celebrate, then run it back." Friction: a solo champion is not told "Your name goes on the cup." — the check is `champion_squad_id == squad.id` (`HomeView.swift:869, 980`); solo seasons carry `champion_member_id` (`Models.swift:50`) which Home never reads (the web does, `index.html:11242-11246`). Both of the owner's real leagues are solo. "Run it back." is a sentence with no door (`:981, 803`). No recap door either (the web has "Season recap →").

**F16 · Creation from Home.** Goal: "play with friends / run a season / this weekend / beat Jake / money on it." Friction: one `+` with five items in two registers (three makes, two go-somewheres — D176's own words at `decision-log.md:4900` complain about exactly this and it is still there). No intent-first door on Home; the occasion card is the only intent-shaped prompt and it speaks in riddles ("Put a jug up", "Name the jug", "Set the Major", "Azaleas are blooming somewhere.", `HomeStream.swift:201-214`).

**F17 · The occasion card.** Works: per-year dismiss, telemetry, the gold "earned" spine in the Major windows. Friction: oblique by design; "teams" (Sep 18–Oct 5, two weeks from today) says "Two teams. One cup. You know the one. / Run your own →" and lands on a generic picker titled "Start an event · Short form · its own little trophy".

**F18 · Loading, refresh, failure.** Works: skeleton; a failed read keeps what is on screen (D220, `HomeView.swift:283-288`); one load per key. Friction: with nothing in hand a network failure renders as "No rounds from your buddies yet." — a quiet week and a dead network are the same sentence (`HomeStream.swift:95-101` knows the difference; the view only uses it to keep old rows). Pull-to-refresh reloads the payload and the stream but **not** the chips (`.task(id: memberships.count)`, `UpNextChips.swift:42`), the calendar card (`.task {}` once, `UpcomingRoundsSection.swift:63`), the invites (`InvitesBanner.swift:65`) or the requests (`BuddyRequests.swift:91`) (INFER on runtime; SAW the modifiers). The lead card lands "a moment after the rest of Home" by design (`HomeView.swift:310-312`), so the top of the page shifts after paint.

---

## Section 3 — Findings

Severity: P0 blocks a goal · P1 major · P2 minor · P3 polish. "Q" = which of the brief's five questions is damaged: **Q1** what is happening · **Q2** why it matters to me · **Q3** what can I do right now · **Q4** who am I competing with · **Q5** what happens next.

**HM-01 · P0 · Home is an empty dashboard for the majority of real accounts.**
Observed: with no live league the hero is a non-tappable card (`HomeView.swift:810-812`), the feed is one sentence (`:120-126`), the calendar is one row (`UpcomingRoundsSection.swift:41-54`), and the chips are absent. Prod (SAW): 14 of 39 profiles have no league, 27 of 39 have no accepted buddy, and there were 14 rounds in the last 21 days across the whole product — so the "living feed" has, on average, fewer than one round a day for everyone combined. Q1, Q3, Q4, Q5. Recommend: Home must lead with *my* golf (last round, index, streak, next tee time) and with a concrete first move whenever the circle is quiet; treat "no rounds from buddies" as the default state to design for, not the edge.

**HM-02 · P0 · The league-less hero has no action.**
Observed: rung 7 "Your card · 0 of 3 · Three rounds and your index goes live. Nothing else needed." and rung 6 "Established. Nobody's seen it yet — you haven't joined a league." render in a card that is not a `Button` (`:802-812, 893-895, 950-952`). The web's same hero carries "Post your first round" / "Find your buddies" / "Start a league" (`index.html:11448-11474`). "Post one" in the empty-feed sentence is plain text (`:123`). Q3. Recommend: the hero *is* the button ("Add my round" → composer at rung 7; "Find your buddies" / "Start something with them" at rung 6/5).

**HM-03 · P0 · Events do not exist on Home.**
Observed: `native_home()` returns `events` (setup|live, with `my_team_slot`, `is_organizer`) and `open_duels` (opponent, `closes_on`, `my_pvi`/`their_pvi`) (`20260827130400_native_home.sql:398-456`); the phone decodes them (`Models.swift:233-234`) and Home never reads either (only `OrientationScreen.swift:90`, `YouRepository.swift:141`, `PushDev.swift:40` do). An event-only golfer sees the rung 6/7 hero ("you haven't joined a league"). Event posts are not in the feed (`loadPosts` filters by league ids, `HomeStream.swift:150-158`). Q1–Q5 for the "short game" the orientation just taught. Recommend: an event is a moment with identity — a hero/lead face ("Desert Ryder · session 2 closes Sun · you v Marcus · beat 84") and a door to the room.

**HM-04 · P1 · The forming hero names the Pro's next step and sends them to an empty table.**
Observed: "Your league is still forming. Lock the bylaws and the invite link is yours." (`HomeView.swift:956-957`); the card's only door is `push(.league)` → STANDINGS, which reads "NO ROUNDS YET. SQUADS FORM WHEN THE PRO LOCKS — STANDINGS START AT THE FIRST POSTED ROUND." (`LeagueCopy.swift:374-377`); "See the table →" (`:839-842`). The web's ladder sends the Pro to the wizard step that blocks ("Name your league" / "Lock it in and invite your crew" / "Share the invite link" / "Draw the squads", `index.html:11388-11393`). Prod: six `My Cup` leagues in setup with one member each — the D96 stall, on the phone. Q3, Q5. Recommend: the forming hero's door is the blocking step (wizard lock / share link / draft), with the roster count on the card.

**HM-05 · P1 · "See the table →" on every membership hero, including forming, preseason and wrapped.**
Observed: `if mode.membership != nil { … "See the table →" }` (`:839-842`); the door pushes STANDINGS in all six modes (`:803`). Before first tee the table is empty; after the wrap the sentence beside it says "Run it back." Q3, Q5. Recommend: the hero's door and its word follow the mode (lock / invite / plan a round / the table / the recap / run it back).

**HM-06 · P1 · A solo-league champion is never told their name goes on the cup.**
Observed: `earned` and the wrapped line test only `champion_squad_id == m.squad?.id` (`HomeView.swift:869, 980`); a solo season carries `champion_member_id` (`Models.swift:50`, `native_home.sql:164`) and Home never compares it to `m.member_id`. The web resolves both (`index.html:11242-11246`). Fellas and Who's the bitch? — the two real leagues — are solo (SAW). Q2, Q4. Recommend: `iWon = solo ? champion_member_id == member_id : champion_squad_id == squad.id`; gold wash and "Your name goes on the cup." for the solo champion.

**HM-07 · P1 · The wrapped hero says "Run it back." and has no run-back door.**
Observed: line "The cup's been lifted. Run it back." (`:981`); the card opens STANDINGS (`:803`). `RunItBackCard` exists only in `LeaguelessDoors` (Orientation, league-less Clubhouse) and `ClubhouseView.swift:172`. Web: "Season recap →" + "Run it back — Season 2" (`index.html:11251-11253`). Q3, Q5. Recommend: the wrapped hero's door is the recap; a "Run it back — Season 2" button sits on it (`presenter.runBack` already exists, `Presenter.swift:33`).

**HM-08 · P1 · The season hero's foot is a rules paragraph in 13-pt mono.**
Observed: rule · endgame · money · owe · door word, each `CSFont.monoSmall` (13 pt, `Typography.swift:31`) (`HomeView.swift:827-845`); the endgame sentence alone is ~40 words: "The top 2 golfers seed into a four-week Cup Final from Sat Dec 19 — scored fresh, so the regular season sets the seeds, not the winner. Level on points? Months won breaks it." (`LeagueCopy.swift:398-424`); then "$150 on the books · $0 collected · 2 still owe" and "You still owe $75 · ask the Pro how to pay — money moves between you" (`HomeHeroCopy.swift:196-237`). Q1 (buried under Q-nothing), and the brief's "no dense forms, tiny text". Recommend: the hero carries the standing and ONE next-thing; rules, endgame and books live one tap away on the table, surfacing on Home only as a lead-card face when they are time-bound.

**HM-09 · P1 · Creation on Home is a bare `+` with five mixed items; there is no intent-first door.**
Observed: `Menu` "Start a league / Start an event / Join with a code / Your golf calendar / Find golfers", a11y "Start or join" (`HomeView.swift:178-190`). D176 itself records that "a plus sign reads *make something*, not *go somewhere*" (`decision-log.md:4900`) and the two navigations are still in it. The Clubhouse tab carries a second set of doors with different words ("Join a league", "Add golfers", `ClubhouseView.swift:137-151`). Q3. Recommend: one intent-first "Compete" surface reachable from the hero and the tab ("Play with friends this weekend" / "Run a season" / "Beat Jake" / "Put money on it"), with Customize behind it; the calendar and people are destinations, not creations.

**HM-10 · P1 · "Buddies but no league" is not a state; rung 6 lies about it.**
Observed: `HomeMode.of` returns rung 7 or 6 by `rounds_count` only (`Models.swift:305-310`); the rung 6 sentence "Nobody's seen it yet — you haven't joined a league" (`HomeView.swift:952`) is false when buddies exist (their `home_feed` includes your rounds, `20260723090000:24-27`). The web has rung 5: "Four makes a league. You have 3." + "Start a league" (`index.html:11465-11474`). Q4, Q3. Recommend: a rung 5 hero that names the buddies and offers the smallest competition with them (a challenge, a weekend event), not only a league.

**HM-11 · P1 · Reactions are unavailable on buddy-only rounds, silently.**
Observed: a strip renders only when the round has a `posts` row in a league the viewer is in (`HomeSocial.swift:5-10, 65-71`; `state(for:)` nil → no strip, `HomeView.swift:502-511`). A golfer whose circle is "friends without a league" — the brief's funnel entry — can see friends' rounds and cannot react to any of them; nothing says why. Q4 (trash talk), Q3. Recommend: reactions keyed to the round, not a league post; or a friend-scope post.

**HM-12 · P1 · "Add my round" is not on Home in season either.**
Observed: no post affordance on the hero; the lead card reaches the composer only on the clash and floor faces (`HomeView.swift:162-169`); otherwise the path is ⊕ → "Golf" cover → "Post a round — after you play" → composer (`MainTabView.swift:336-339`, `PostCoverView.swift:90-100`). Q3. Recommend: a persistent one-tap "Add my round" on Home (hero foot or a fixed chip), with the cover's three doors reserved for the ⊕.

**HM-13 · P2 · The "Month closes" chip fires for leagues with no season, and after the wrap.**
Observed: gate is `hasMemberships` only (`ScheduleModels.swift:405-407`; `UpNextChips.swift:42-44` passes `leagueId != nil || !memberships.isEmpty`). Forming, preseason (edge month waived anyway) and wrapped leagues all get "MONTH CLOSES · in N days ›" → STANDINGS. Prior audit M-042 partially fixed. Q5 (false urgency). Recommend: gate on `SeasonPhase == .season/.cupFinal` of at least one membership.

**HM-14 · P2 · Pull-to-refresh does not refresh the chips, the calendar card, the invites or the requests.**
Observed: `.refreshable` reloads the payload and the stream (`HomeView.swift:141-150`); `UpNextChips` `.task(id: store.me?.memberships.count)` (`:42`), `UpcomingRoundsSection` `.task { }` (`:63`), `InvitesBanner` `.task { }` (`:65`), `BuddyRequests` `.task { if model == nil … }` (`:91`) — none keyed on `generated_at` (INFER on runtime). A new invite or a fresh booking appears only after leaving and re-entering the tab. Q1, missing feedback. Recommend: key every Home child load on `HomeModel.LoadKey` or fold them into one load (see HM-15).

**HM-15 · P2 · Home re-reads what `native_home()` already delivered, ~13–16 requests per open.**
Observed: `native_home` carries invites, live round, upcoming rounds (14 days), events, duels (`native_home.sql:324-456`); Home then calls `home_feed`, `posts` (twice: stream and social), storage signing, `post_kudos`, `post_comments`, `league_members`, `home_clash`, `sched.watch()` twice (`UpNextModel:89`, `UpcomingModel:133`), `people.invites()` twice (`InvitesCount:19`, `UpNextModel:90`), `people.friends()` twice (`BuddyRequestsModel:31`, `UpNextModel:91`), weather per card. Consequence: staggered paint (the lead card "appears a moment after the rest of Home", `HomeView.swift:310-312`), slots that can disagree, and the refresh gap in HM-14. Q1 (time-to-aha). Recommend: one Home read (extend `native_home` or a `native_home_stream`), one paint.

**HM-16 · P2 · "— held" is unexplained jargon on the most-seen chip.**
Observed: `("— held", cs.mut)` whenever `prev_rank == rank` (`HomeView.swift:932`) — most weeks. Prior audit M-059 logged "HELD" unexplained; still there. Q1. Recommend: drop the chip when nothing moved (the ladder already treats no-move as no news) or say "no change".

**HM-17 · P2 · The move is stated three times on one screen.**
Observed: move card "Up 2" / "3rd of 10, 12 back." (`HomeLeadCopy:237-245`), hero chip "▲ up 2" (`HomeView.swift:930`), hero line "12 back of Galen". Redundant. Recommend: the card carries the news, the hero the standing; one arrow.

**HM-18 · P2 · Digest says "You's first round".**
Observed: `bits.append("\(who(f))'s first round")` with `who` returning "You" (`HomeDigest.swift:42, 85`). Prior audit M-082 still open in the "since" frame (the quiet frame at `:69` handles your/their). Q2 (voice). Recommend: "your first round".

**HM-19 · P2 · The milestone lead card's eyebrow duplicates the section head.**
Observed: card eyebrow "Around your buddies" (`HomeLeadCard.swift:77`) above a hero, above a section head "Around your buddies" (`HomeView.swift:108`). Recommend: "Today" / the buddy's name as the eyebrow.

**HM-20 · P2 · Occasion copy is deliberately oblique and reads as riddles to a first-timer.**
Observed: "Azaleas are blooming somewhere." / "Put a jug up", "Somewhere out there, par is winning." / "Set the Major", "Links weather is a state of mind." / "Name the jug", "Two teams. One cup. You know the one." / "Run your own" (`HomeStream.swift:201-214`). Q3 ("don't make me understand"). Recommend: keep the wink in the eyebrow, make the act plain ("Start a weekend Major", "Start a two-team match").

**HM-21 · P2 · Invite copy prints raw ISO dates and calls every event a Ryder.**
Observed: "from Galen · first tee 2026-09-12" and "First tee 2026-09-12." (`PeopleModels.swift:131-140`); `title` is "Ryder invite" for `kind != league`, and `my_invites` emits `'event'` for any non-league invite (`20260713180000_member_invites.sql:72`). Q1. Recommend: `LeagueDates.dowMonDay`; name the event kind from the row.

**HM-22 · P2 · "Needs you · 2 invites" counts buddy requests as invites.**
Observed: `needs = invites + requests` → "\(needs) invites" (`ScheduleModels.swift:403-404`); `BuddyRequests.swift:11-12` names the problem. Recommend: "2 waiting on you".

**HM-23 · P2 · Rung 7 ignores a starter index typed on the card.**
Observed: the card gate offers "Optional. Your index builds itself at 3 posted rounds; a starter only helps before then." (`CardGateView.swift:88`) and prefills from `index_current` (`:199`); the hero at rung 7 shows "0 of 3" and "Three rounds and your index goes live" regardless (`HomeView.swift:895, 951`). Prior audit M-050 still applies (INFER that the starter writes `index_current`; SAW the copy and the prefill). Q2. Recommend: "Playing off 14.2 for now · 0 of 3 posted".

**HM-24 · P2 · The empty calendar's "Put a round on the calendar" opens a screen, not the declare sheet.**
Observed: `NavigationLink(value: HomeRoute.schedule)` (`UpcomingRoundsSection.swift:42`); `presenter.declare` with a `DeclarePrefill` exists and You uses it (`Presenter.swift:27`, `MainTabView.swift:499`). Q3. Recommend: open the declare sheet directly with today/Saturday prefilled.

**HM-25 · P2 · A network failure with nothing cached reads as a quiet week.**
Observed: `if r.failed && !items.isEmpty { return }` (`HomeView.swift:283-288`); with empty items the view falls through to "No rounds from your buddies yet." `Result.failed` (`HomeStream.swift:95-101`) is never shown. Missing feedback. Recommend: "Couldn't reach the clubhouse — pull to try again" when `failed`.

**HM-26 · P2 · The wrap disappears from Home for anyone who also has a live league.**
Observed: `HomeMode.pool` drops every wrapped membership when any live one exists (`Models.swift:300-303`); `HomeLeagueRow.rows` draws from the pool (`HomeLeagueRow.swift:29-34`). The most story-worthy day a league has is invisible on Home for exactly the multi-league golfer. Q1, Q2. Recommend: a wrapped league earns a lead-card face for N days ("Sandbox wrapped · Ray took it · see the recap") whatever else is live.

**HM-27 · P2 · Money is the loudest daily line under the standing.**
Observed: "You still owe $75 · ask the Pro how to pay — money moves between you" in `cs.warm` every open until paid (`HomeHeroCopy.swift:225-237`; `HomeView.swift:831-838`), plus "$150 on the books · $0 collected · 2 still owe" above it. The brief: anticipation, never pressure. Recommend: a one-time lead-card face at lock and at the due date; the books stay on the Pot pane.

**HM-28 · P2 · The clash card's sides speak in an unlabeled band ("+2.4").**
Observed: `sideLine` → "83 · +2.4 · SAT" (`HomeLead.swift:210-217`); nothing on the card says what +2.4 is. Q1. Recommend: "83 · beat his number by 2.4 · Sat" or drop the band and keep gross + day.

**HM-29 · P2 · Preseason/forming heroes know less than the D121 row does.**
Observed: the current-league preseason hero shows the countdown and "Rounds before it build your number." (`HomeView.swift:965-968`); the row for a *different* preseason league says "First tee Sat Sep 5 · 5 on the roster" (`HomeLeagueRow.swift:71-84`). The roster and the invite link never reach the hero. Q4, Q5. Recommend: the hero states the roster and offers the invite link before first tee.

**HM-30 · P2 · The solo foot runs a month clock the web retired.**
Observed: solo: "Best 4 rounds a month count · 2 posted · 26 days left in September" (`HomeHeroCopy.swift:166-171`); the web's D140 note says "Solo tracks the habit; it does not run a clock" (`index.html:11178-11190`). Terminology "floor"/"cap" is also unexplained in "Month floor 2/4 · 2 more" and "Partial month · floors waived" (`:173-177`) — prior M-057/M-059 still open on Home. Recommend: one plain sentence ("2 of your best 4 posted this month") and no countdown in solo.

**HM-31 · P2 · Home has no ME layer beyond the standing.**
Observed: the header is the wordmark and the date (`HomeView.swift:48`); in season nothing on Home says my index, my last round, my streak or my rounds this month (the leagueless hero shows the index only). The brief's hierarchy is ME → NOW. Q2. Recommend: a compact "you" strip under the header (index · last round · next tee time), stable across every mode.

**HM-32 · P2 · Home has no WHO layer beyond one name.**
Observed: the hero names the leader (`HomeHeroCopy.swift:46-80`); faces appear only on feed cards and the clash card (`themMarker`, `HomeLeadCard.swift:54`); no rival row, no table snippet, no "you v Galen" record. Q4. Recommend: a two-row "you and Galen" strip on the hero (faces, gap, last clash).

**HM-33 · P2 · The two-person league's caption reads "2nd of 2".**
Observed: `"\(ordinal(rank)) of \(of)"` (`HomeHeroCopy.swift:90-93`). It is "last", and the Gentleman Instigator has nothing to say about it. Q2 (voice). Recommend: at n=2 drop the caption and let the score line carry it, or let the voice in ("Chasing Galen").

**HM-34 · P2 · The clash's result reaches Home as a folded footnote.**
Observed: `settle_week_clash` posts a `system` line (`20260831160000:17-19`), which `HomeFeedFold` folds into "Fellas · 1 league note today ⌄" (`HomeFeedFold.swift:55-61, 138-163`); the lead card shows only open clashes (`home_clash` filters `settled_at is null`, `:78-83`). The duel you were shown all week ends off-stage. Q5, delight. Recommend: a settled clash is a lead-card face for a day ("You took the week · 83 to 88").

**HM-35 · P2 · Live round offered twice on the same screen.**
Observed: `LiveNowBar` above the tabs (`LiveNowBar.swift:34`) and `LiveResumeBanner` in the scroll (`HomeView.swift:58-60`) both open the cover. Recommend: the bar alone on Home; the card only when the bar is absent.

**HM-36 · P2 · Feed cards use "number"/"gross" vocabulary.**
Observed: "Beat their number by 2.4" (`HomeView.swift:522-528`), "gross" label under the score (`:582`), "beat your number by" bands (`CSBands.swift:50-56`). Q1 for a first-timer. Recommend: "83 · 2.4 better than usual" or plain score + one adjective.

**HM-37 · P2 · Sandbox leagues render as real on Home.**
Observed: `sandbox` decoded (`Models.swift:137`) and ignored by `HomeMode` (`:300-318`). Prod: `Sandbox` (complete, 8 members). A sandbox-only member gets a real "season wrapped" hero (INFER). Recommend: exclude sandbox from the pool, or label it.

**HM-38 · P2 · The hero's league follows the Clubhouse pager.**
Observed: `loadKey` and `HomeMode.of` read `store.preferredLeague` (`HomeView.swift:39, 45`), which the Clubhouse's paged view sets (`MainTabView.swift:174`; D121 note "the Clubhouse pager sets the preference too", `:151-153`). Home's lead league changes as a side effect of browsing elsewhere (INFER). Recommend: Home's lead is the most time-bound league (open clash, closest deadline), not the last swiped.

**HM-39 · P2 · "Coming up" mixes everyone's plans by date only.**
Observed: `homeRounds` sorts all watched rounds by date, five max (`ScheduleModels.swift:307-315`); a league-mate's Tuesday outranks your Saturday. Recommend: yours and with-you first, then the crew.

**HM-40 · P3 · "See the table →" is small mono text, not an affordance.**
Observed: right-aligned `monoSmall` in `cs.brand` (`HomeView.swift:840`); the whole card is the button. Recommend: a real button or a chevron in the card corner.

**HM-41 · P3 · The hero's VoiceOver label is a paragraph.**
Observed: eyebrow · caption · move · line · every foot · owe joined by ". " (`:852-860`). Recommend: label = eyebrow + figure + line; foots as a value.

**HM-42 · P3 · The "quiet" digest resurfaces your own round to you.**
Observed: `best(rounds)` ranks all circle rounds including `is_me` (`HomeDigest.swift:52-63, 96-99`) → "Yesterday — You posted 91 at Papago". Recommend: exclude `is_me` from the quiet frame.

**HM-43 · P3 · The "teams" occasion promises a team match and lands on a generic picker.**
Observed: "Run your own →" → `EventPickerSheet` "Start an event · Short form · its own little trophy" (`HomeView.swift:88`; `EventPickerSheet.swift:25`). Recommend: pass the intent (Ryder) into the picker.

**HM-44 · P3 · No telemetry for hero state or CTA taps on the phone.**
Observed: only `home_occasion_tap` (`HomeView.swift:87, 91`); the web logs `home_hero_state` per session and every hero tap (`index.html:11204-11205`). The brief's metrics (activation, time-to-aha, empty-state engagement) cannot be read from the shipping client. Recommend: mirror the web's two events.

**HM-45 · P3 · Web/phone divergences a separate reader should reconcile.** (SAW) Web hero CTAs on every non-season state; web rung 5; web solo-champion handling; web `homeTiles` row (league · next round · board) and `homeStart` doors row for members, both absent on the phone; web head door "THE BOARD ↗" (`index.html:3025`) vs phone "YOUR BUDDIES ↗" (D218 says the web is owed); web has no D121 rows (owed, `decision-log.md:4243`) and no D217 fold (the only "league note" string on the web is the digest's, `index.html:12097`); web `upcomingFromSchedule` keeps a declined booking and drops tagged rounds (`ScheduleModels.swift:385-388`); web forming hero shows a roster bar and "N in".

---

## Section 4 — What already serves the brief (keep, and cite)

- **The lead card ladder** — one slot, fixed order, first match wins, no card as the resting state, every face a door that never dead-ends (`HomeLead.swift:159-184`; `HomeView.swift:162-175`); 0–0 mid-week yields (D216, `:75-82`); the opener shows once with the board's own sentence (`:85, 205`); pinned to the league it was built for (`HomeView.swift:70`). Tested (`HomeLeadTests.swift`, 30 cases).
- **The hero names the leader by name and says the score at n=2** — "12 back of Galen · 9 – 21", "You lead Jade by 22 · 31 – 9", "Level with Galen · 14 – 14." (`HomeHeroCopy.swift:46-80`); the Final is honest to a non-finalist ("Galen v Jade for the cup. Your place on the table is still live — …", `:122-132`); gold is earned only (`HomeView.swift:865-872`). Pinned by `HomeHeroCopyTests` (the whole n × rank × structure × stake × skew matrix, `:212`).
- **Stage vocabulary is one set of six words** (`LeagueCopy.Stage`, `LeagueCopy.swift:175-187`) and the hero, the row and the Clubhouse share one week producer (`SeasonPhase.of`, `Models.swift:266-282`) — the prior audit's M-145 "week 4 of 14 vs 13" is closed.
- **D217 fold** — seven system lines over two leagues become one line per league per bucket, identical bodies across leagues collapse, the golf passes through (`HomeFeedFold.swift:111-164`; tests).
- **D219 doors** — a feed row is a door iff it knows its round (live → scorecard, round → receipt, booking → tee sheet), else a plain note with no dimmed button (`HomeFeedFold.swift:101-106`; `HomeView.swift:642-696`); a booking already on the calendar card is drawn once (`:113-116`).
- **The digest** — "Since you were here · 3 rounds, a personal best from Galen, and Galen 🔥'd your 83." and the quiet frame (`HomeDigest.swift:74-100`); the seen-mark is read once per load (`:28-33`).
- **Failed read ≠ empty feed** (D220, `HomeView.swift:283-288`; `HomeStream.swift:95-101`) and one load per key with a generation guard (`:255-303`).
- **Every Up Next chip is a door** (`ScheduleModels.swift:361-366`), amber only at ≤3 days (`UpNextChips.swift:47-51`), "today" never "tonight" (`HomeLead.swift:190-200`).
- **"Coming up" never vanishes** and the empty row is the owner's sentence "Put a round on the calendar" (`UpcomingRoundsSection.swift:25-54`); "WITH YOU" / "YOU'RE IN" on a booking that names you (`:89-90, 108`).
- **Buddy requests and invites answered in line** on Home (`BuddyRequests.swift:74-89`; `InvitesBanner.swift:43-63`), with toasts and a reload.
- **The live invitation face** — "Galen put you on the tee sheet · JUST TEED OFF · NOTHING SCORED YET · JOIN" (`LiveCopy.swift:269-277`) and the bar that follows you (`LiveNowBar.swift`).
- **Reaction vocabulary** — heater, the eagle, dialed, ice, snake, sandbagger; the bare 🔥 always present; long-press tray; optimistic flip with revert and a toast (`CSReactions`, `HomeView.swift:1074-1113`, `HomeModel.toggle` `:357-369`).
- **The photo card** — the photograph is the ground, the text sets the height, the milestone rides the meta (`HomeView.swift:532-561`); milestones "🔥 Personal best", "⛳ Broke 80 — first time", "🎉 First round on the card" computed server-side over the whole record (`20260723090000:40-55`).
- **D121 rows** — quiet, learnable order, a real door, a headcount noun the room agrees with ("5 on the roster", `HomeLeagueRow.swift:71-84`).
- **Accessibility work** — combined elements, rotor actions for the Tour Card and the six reactions, hit-slop on every eyebrow link, named glyphs ("Your buddies", not "north east arrow") (`HomeView.swift:109-112, 604-608, 620-636`).
- **Occasions dismiss per year** so the azaleas come back (`HomeStream.swift:222-234`).
- **Deploy-skew tolerance everywhere** — v1 payloads still render honest sentences ("Top of the table.", "back of the lead"), a missing column retries without it (`HomeStream.swift:149-158`; `HomeHeroCopy.swift:42-45`).

---

## Section 5 — What I could not determine from reading

1. **How it looks.** No simulator was driven; the 2026-09-02 hard-look screenshots are the newest visual record and predate D216–D221. Fold position of the hero foot at Dynamic Type sizes, and whether the lead card's late arrival visibly shifts the page, are unverified.
2. **Runtime refresh behaviour** (HM-14) is inferred from `.task` modifiers, not observed.
3. **Whether a Major ever creates a `member_invites` row** — if it does, "Ryder invite" (HM-21) is wrong for it; if not, the label is merely brittle.
4. **Whether the card gate's starter index writes `index_current`** (HM-23) — I read the copy and the prefill, not the write path (`set_index`, `20260716120000`).
5. **The initial value of `store.preferredLeague`** on a fresh install and after an invite accept elsewhere — the hero's default league on day one for a multi-league golfer.
6. **How often the lead card is nil on a real day** — there is no phone telemetry for hero/lead states (HM-44); the ladder's frequency can only be reasoned about.
7. **Push copy and timing** that lands on Home (`.invites`, `.requests`, `.home` routes) — a separate reader owns notifications.
8. **The Clubhouse's STANDINGS pane appearance** in setup/preseason — I quoted the empty-state strings (`LeagueCopy.swift:374-377`) but did not read the pane.
9. **Which of the four leagues created 2026-08-28 are seeded** (INFER: all) and therefore which "in-season" Home states have ever been seen by a real, non-owner person. The prod evidence suggests the only real in-season Homes today are the two two-person solo leagues.
10. **Whether `home_feed`'s 21-day window and 40-row cap are ever binding** at current volume (14 rounds in 21 days product-wide says no).
