# Reader report — create-and-join (finding prefix CJ-)

Repo `/Users/fischbeck3/cup-season` at tip `3bba87e` (2026-09-04). Read-only. Line numbers are as of tip. "SAW" = read in code or a rendered string; "INFER" = a consequence I reasoned to but did not run. Prod facts come from `supabase db query --linked` (read-only) run 2026-09-04 and are marked PROD.

Area: the funnel from "I want to play with these people" to "a thing exists that they are in" — the league wizard, the Ryder, the Major, the live side game from the tee sheet, the declared round, and every join path (link, code, in-app invite, event invite), on the phone first, with the web twin noted.

---

## 0. The headline numbers (PROD, 2026-09-04)

| Fact | Value | Source |
|---|---|---|
| Leagues by phase | setup 6 · season 6 · complete 1 | `leagues` join `league_members` |
| Setup leagues with exactly ONE member | **6 of 6** (avg roster 1.0) | same |
| Season leagues avg roster | 5.3 | same |
| Events ever | 1 Ryder (complete, 6 players) · 0 Majors | `events` |
| In-app invites ever | 2 (both league, both accepted) | `member_invites` |
| Board "JOINED THE LEAGUE" posts / "ADDED BY THE PRO" posts | 2 / 0 | `posts` |
| Client telemetry lifetime | `league_create` 2 · `lock_attempt` 1 · `lock_ok` 1 · `invite_open` 1 | `client_events` |
| Live rounds | 26 total · **21 abandoned (81%)** · 5 final · games: none 13, match 9, sunningdale 4, wolf 0, skins 0 | `live_rounds` |
| Declared rounds | 4 (3 with tags) | `scheduled_rounds` |
| Forfeits ("Post a stake") ever | 0 | `forfeits` |
| Profiles / carded (marker+handle) | 39 / 29 | `profiles` |
| `app_flags.ios` | `{min_build:0}` — no `major` key → the Major door is CLOSED on the phone; `app_flags.pricing.visible=false` | `app_flags` |

Reading: half of every league ever created is a founder sitting alone in an unlocked wizard. The one creation path that real people actually finish (the live match) is abandoned four times out of five. Nobody has ever posted a forfeit. This is the funnel the brief wants inverted, and the data says the current shape is the failure D96/D151 already named.

---

## 1. The map — every screen, sheet and state in the area

Reached = how a golfer arrives. First = what the eye lands on. Primary = the ember/primary action. Exits = every way out.

| # | Screen / sheet / state | Reached | What it shows first | Primary action | Exits |
|---|---|---|---|---|---|
| M1 | **OrientationScreen** (`Onboarding/OrientationScreen.swift:107`) | once, after the golfer card, only for a golfer with no league/round/event and no pending code (`OrientedFlag.take` :85) | "Four places. / Two ways to play." · "Thirty seconds, then you're in." · four tab rows · two cards "The long game · A league" / "The short game · An event" · the guide line | pinned "Take me in" (:180) | Take me in · the three doors (M2) · Start an event → M11 |
| M2 | **LeaguelessDoors** (`Wizard/LeaguelessDoors.swift:14`) — three pills "Join a league · Start a league · Start an event" (+ RunItBackCard when a membership is `complete`) | Orientation (:138) · league-less Clubhouse (`ClubhouseView.swift:140`) · Card & settings ×2 (`CardAndSettingsScreen.swift:412,529,633`) | the three nouns; fine "Post a round — it counts on your card. Leagues score it when you join one." (only when no memberships) | none primary — three equal quiet doors | Join → M20 sheet · Start a league → M3 fullScreenCover · Start an event → M11 |
| M2b | **Home "+" menu** (`Home/HomeView.swift:178-190`) | Home header trailing "+" (a11y "Start or join") | a system Menu | — | Start a league → M3 · Start an event → M11 · Join with a code → M20 · Your golf calendar · Find golfers |
| M2c | **OccasionCard** (`HomeView.swift:84-95`; copy `Kit/Home/HomeStream.swift:200-214`) | Home, in a calendar window, once per year | oblique eyebrow/headline ("The big team match" · "Two teams. One cup. You know the one.") | e.g. "Run your own" / "Name the jug" / "Start a league" | go → M3 or M11 · dismiss |
| M3 | **Wizard — name sheet** (`WizardScreen.swift:70-86`, `leagueId == nil`) | any Start-a-league door; Run it back (prefilled "· S2") | "Name your league" · "The banner everything hangs under" · field "The Big Slice, The Sunday Cup, Dew Sweepers…" · "You can rename it any time before the bylaws lock." | "Start the league" → **`create_league` mints the row** (:252-268) | Cancel (closes, nothing minted) |
| M4 | **Wizard step 0** (`WizardSteps.swift:11-41`) | after M3; also re-entry to a setup league (Clubhouse checklist "Continue", `StandingsPane.swift:35`) | eyebrow "Create your league — set the rules once, lock them in" · 3 dots · "LEAGUE NAME" field (again) · "PRO — THAT'S YOU" chip "@handle · you run this league · THE PRO" | "Next →" | Cancel (two-tap "Sure? Discard it" + "Cancel this league? It hasn't started, so this discards it completely." → `delete_league`) · Next |
| M5 | **Wizard step 1** (`WizardSteps.swift:45-183`) | Next from M4 | "How serious is your league?" (i) · 3 preset cards Casual/Standard/Cutthroat · summary line · "Verification is a norm the league holds, not a filter the engine applies." · **"Use these defaults →"** · "Customize" chevron · (collapsed dials) · "YOUR LEAGUE SO FAR" portrait card · (PricingPassCard, flag-hidden) | "Use these defaults →" (jumps to M7) | ← Back · Next → · Customize (M6 inline) |
| M6 | **Wizard dials** (`WizardSteps.swift:110-163`, inline in M5) | "Customize" | Buy-in stepper · Season length stepper · First tee date · TEAMS (i) seg Solo/2/3/4 Squads + fit line · HOW TEAMS FILL (i) Blind draw/Assign · HOW IT ENDS (i) Cup Final/Points table · THE POT SPLIT (i) Balanced/Winner-heavy/Spread it · Counting cap (i) stepper · Participation floor (i) stepper | — | Hide options |
| M7 | **Wizard step 2 — review** (`WizardSteps.swift:187-214`) | Next / fast path | "Review the bylaws, then lock it in" · 10–11 bylaw rows · "Lock opens the invite link — one link fills the league. The code works until first tee, or until you close the roster. Squads need four to tee off; solo tees off at two." | **"Lock the bylaws & form the squads"** (solo: "Lock the bylaws") → `lock_league` (18 args, one transaction) | ← Back · Lock |
| M8 | **WizardLockShareSheet** (`Wizard/WizardLockShareSheet.swift:29`) | after a successful lock (toast "Bylaws locked") | "Bylaws locked ⛳" · "One link fills the league" · seat-math line ("1 in so far — 3 more fills 2 squads, and the draw runs when the crew is in.") · card "You're invited to X on Cup Season · cupseason.app/?join=CODE" | "Share the invite link" (system ShareLink) | "Add golfers" → M22 · "Later — it lives in the league room" · dismiss → `onLocked` → Clubhouse tab |
| M9 | **Clubhouse, draft phase** (`League/StandingsPane.swift:53-71`) | after M8 for a squads league | PhaseHero "Squads are forming · The Pro has the list." · "N PLAYERS IN THE POOL · 3 SEATS OPEN" | Pro: "Form the squads" / member: "See the squads" → M10 | Share the invite link · room panes |
| M9b | **Clubhouse, setup phase** (`StandingsPane.swift:32-47`) | a league minted but not locked (6 in prod) | "League setup · three steps to first tee" · 1 Season settings [Continue] · 2 Invite the crew "At lock" · 3 Squad formation "Locked" · "JOINS OPEN AT THE LOCK · 1 OF 4 IN — 3 SEATS OPEN" | Continue → M4 (existing league) | room panes; header "Cancel & delete this league" (`LeagueRoomScreen.swift:189-197`) |
| M10 | **DraftNightScreen — formation** (`Draft/DraftNightScreen.swift:99-150`) | M9 button; `presenter.draft` | dusk ground · "Form squads · blind draw" · clock card "Form squads / N in the pool / THE HAT SHUFFLES SERVER-SIDE — NOBODY RIGS THE DRAW" · blocker "Minimum four to tee off — 1 in so far. Share the invite link." · squad cards "Empty" · the pool chips | Pro: "Draw squads" → `randomize_squads`; then "Start the season →" → `start_season` | Close · toolbar "Members & invites" (M23) · setup bounce "Lock settings first: the draft opens after setup" [Continue → wizard] |
| M10b | DraftNight — snake board (`:184-262`) | a league whose stored `draft_type` is snake/live (the wizard never offers it) | "On the clock" · picks · "Randomize order · Pick for captain · Undo last pick" | make_pick | Close |
| M11 | **EventPickerSheet** (`Events/EventPickerSheet.swift:10`) | Start an event (any door), Occasion cards, Home + | "Start an event" · "Short form · its own little trophy" · "⚔️ The Ryder — Two teams · weekly vs-index duels · first to the clinch — LIVE" · "🥊 Bracket — Knockout · seeded · last golfer standing — SOON" · (A Major, flag-gated: CLOSED in prod) · "Every event mints a trophy for your display case." | The Ryder → M12 | Close · Bracket → toast "Bracket isn't built yet" |
| M12 | **RyderSetupSheet** (`Events/RyderSetupSheet.swift:38`) | M11; Run it back from a complete Ryder room (prefilled) | "Start a Ryder" · "Two teams trade weekly duels against their own numbers — first to the clinch takes it" · Event name · Team A / Team B · Sessions 3/4/5/6 · Cadence Weekly/Every 2 wks · First tee (a Sunday) · Attach to a league (optional; DEFAULTS to `store.preferredLeague`) · Add players · "How it plays" card · fine "You captain Team A…" | "Create the event" → `create_event` → invites → room | Cancel · Add players → M13 |
| M13 | **EventStagePicker** (`Events/EventStagePicker.swift:12`) | "Search the app or tap a buddy" in M12/M14 | "Add players" · "They get an invite to accept once the event is created" · search "Find golfers by name or @handle" · buddies | Add (stages) | Done |
| M14 | **MajorSetupSheet** (`Events/MajorSetupSheet.swift:39`) | M11 only when `app_flags.ios.major` (never in prod); Run it back from a complete Major | "Start a Major" · "A championship window — every card on one board, one name on the jug" · Name the jug · The final day · Window 2/3/4 days · Buy-in per player ($0 = bragging rights) · The pot pays (only when >0) · Run it with a league (optional; defaults to preferred league) · Add golfers · How it plays | "Set the Major" → `create_major` | Cancel |
| M15 | **EventRoomScreen → RyderRoomView** (`Events/RyderRoomView.swift:10`) | after create; Clubhouse EventChips; push route; InvitesBanner accept does NOT open it | "NAME · FORMING" · scoreboard "Red 0 – 0 Blue" · "Add players to both teams to set the target." rule line · taunt toggle · organizer: "Invite players" · "Scrap" · Unassigned rows → A/→ B · sessions "Pairings not set." · rosters "No one assigned yet." · the board | organizer: Invite players → M22 (event mode); Generate pairings; Score this session | Close (fullScreenCover) |
| M16 | **MajorRoomView** (`Events/MajorRoomView.swift:11`) | as above for a Major | trophy card "A MAJOR · 4 DAYS / when · FIELD OF 1 / BRAGGING RIGHTS" · rule line · league member not in field: "Enter the field" · organizer: Invite golfers · "Needs 2 in the field to open." · Open the window now · Scrap · the clubhouse board | Enter the field / Open the window now | Close |
| M17 | **PostCoverView** (the ⊕) (`Post/PostCoverView.swift:24`) | centre tab (a verb, snaps back) | "Golf" · "Play one live, post one you just finished, or plan the next" · LIVE hero "Play now — score the group" · "Post a round — after you play" · "Plan a tee time — before" | Play now → M18 | Close · Post → composer · Plan → M19 |
| M18 | **LiveSetupView** (`Live/LiveSetupView.swift:9`) | Play now; LiveNowBar; nearby accept | "Set up the round" · (plan bridge "On your tee sheet today · Load it →") · Course card (course search · Tee/Rating/Slope · 18/9 · "Enter the pars") · Foursome card ("The foursome · 1 / 4" · 2v2/Everyone · slots "Open slot · TAP A PLAYER BELOW" · chips You/Nearby/You play with/League/Buddies/Guests · "Search the app — add any golfer" · Add a guest Name/Index · 40-word fine print) · Game card ("Game for this round · pick one" Just score/Match play/Wolf/Skins/Sunningdale Rules · note · stake field · strokes preview) · "Who's on this tee" toggle | "Tee off →" → `start_live_round` | Close (host) · Enter the pars sheet · roster picker sheet |
| M19 | **DeclareRoundSheet** (`Schedule/DeclareRoundSheet.swift:20`) | ⊕ Plan; calendar day; "I'm in" on a buddy's round (prefilled "Get in on it") | "Put a round on the tee sheet" · "BUDDIES & LEAGUE MATES SEE IT THE MOMENT YOU POST" · Day (next Saturday) · Tee time · optional · Course · Note · optional · Tag your group chips | "On the tee sheet" → `declare_round` | Cancel |
| M19b | **ScheduledRoundSheet** (`Schedule/ScheduledRoundSheet.swift:11`) | Home Up-next chip, calendar, push | course · weather chip · note · "Who's in" · RSVP I'm in/Maybe/Can't · "On the board" chat · host: Edit group / Cancel round | RSVP | Done |
| M20 | **JoinLeagueFlow** (`People/JoinLeagueFlow.swift:13`) | Universal Link `/?join=CODE` (after card gate, `RootView.swift:43-48`) · Home + "Join with a code" · doors "Join a league" | "Join a league" · "I HAVE A LEAGUE CODE" · field "League code" (or the pre-filled code in mono, and `go()` **auto-runs** on appear, :49) · after validate: "You're invited to <name>." | "Join" → `league_by_code` → `join_covenant_info` → (M21 if buy-in > 0) → `join_league` | Cancel · errors as CSNote ("No league with that code — check with your Pro"; server refusals verbatim) |
| M21 | **CovenantSheet** (`JoinLeagueFlow.swift:125-158`) | only when `buyin_cents > 0` | "Before you join <name>" · "THE FINE PRINT, UP FRONT" · BUY-IN "$50 / player · on the pot sheet" · PRESET "Standard" · PARTICIPATION FLOOR "2 rounds / mo" · FINISH "Cup Final · final 4 weeks" · "Joining puts you on the pot sheet for $50. Cup Season keeps the ledger; the money moves between friends." | "Join — I'm in for $50" | "Not now" (code is already cleared, `RootView.swift:43`) |
| M21b | **LeagueWelcomeSheet** (`JoinLeagueFlow.swift:173-220`) | after `join_league` | "Welcome to <name>" · "THREE THINGS TO KNOW" · (paid: "You're on the pot sheet: $50 buy-in. The Pro tracks who's paid.") · 3 rules · "How scoring works →" · "Who else plays with you? …any member's link works." · "Share the invite link" | (none; dismiss) → `onJoined` → Clubhouse tab | dismiss · How scoring works → ScoringHelpSheet |
| M22 | **PeoplePickerSheet, invite mode** (`People/PeoplePickerSheet.swift:16`) | M8 "Add golfers" · room "Add golfers" (`presenter.inviteTo`) · Ryder/Major room "Invite players/golfers" | "Add golfers" · "Invited golfers get a notification and choose to join" · search · buddies list · rows: Add / "Added ✓" / "In" · league mode: "Share an invite link instead" | Add → `invite_golfer` (Pro/organizer only) | Done |
| M23 | **MembersSheet** (`League/MembersSheet.swift:10`) | room header / DraftNight toolbar | "Members & invites" · "N PLAYERS · CODE X" · rows · Pro: "Add golfers" · "Share the invite link" | Share | dismiss |
| M24 | **InvitesBanner** (`People/InvitesBanner.swift:26`, mounted `HomeView.swift:50`) | Home top, when `my_invites` non-empty | "League invite · <name>" / "Ryder invite · <name>" · "from X · first tee 2026-09-12" | "Accept" → `respond_invite` (no covenant) | "Details" sheet: "A season-long league. Invited by X." · Accept & join / Decline |
| M25 | **EventChips** (`Events/EventChips.swift:13`) | Clubhouse room top | "NAME · RYDER · LIVE" / for an attached event you have not joined: "NAME · RYDER · ENTER THE FIELD" | tap → event room | — |
| M26 | **RunItBackCard** (`LeaguelessDoors.swift:67`; `presenter.runBack`) | doors when a membership is `complete`; room `runItBack` link | "Season wrapped · <name>" · "Run it back — Season 2" · "Same crew, same bylaws, fresh table — change anything in the wizard." | Run it back → M3 prefilled with "<name> · S2" + last bylaws → the whole wizard again → a NEW league + code | Close |
| M27 | **ForfeitCreateSheet** (`League/PotPane.swift:171`) | Pot pane "The other stakes · pride, on the books · Post a stake" | "Post a stake" · "Pride, on the books — never money" · Name it · The shape (Loser hosts / Winner picks the course / Strokes next time / Standing bounty / Name your own) · The terms · Against (The field / a member) · Rides on (optional) | "Put it on the books" → `create_forfeit` (league members only) | Cancel |

Screens mapped: 32 (27 rows, several with sub-states).

Not on the phone at all (SAW by absence): a code field on the signed-out door (`Door/DoorView.swift` has only email + OTP code; the web door has `#joinCode`, `index.html:2811`); D151's crew step (web `#obCrew`, `index.html:2859-2880`); any link or code for an event; any "Challenge" action on a Tour Card (`You/TourCardSheet.swift:134,176` — mute and Add buddy only); the Pro's buy-in payment note (`set_buy_in_terms` — no phone surface found).

---

## 2. The flows

### 2.1 League via the wizard (the Pro)

**Counts (SAW).** Screens to a locked league: 5 (M3 name sheet → M4 → M5 → M7 → M8), using "Use these defaults →". Screens to a *running* squads season: 8 (+ M9 Clubhouse → M10 draft → Draw → Start). A solo league goes straight to `season` at lock (`lock_league`, `20260902163000:207`).

Fields: name (asked twice: M3 and M4), then the dials: buy-in · season length · first tee · teams · how teams fill · how it ends · pot split · counting cap · participation floor = **9 dials + preset + name = 11 controls**. Decisions the user *must* make before anything exists: **1 (the name)** — a row is minted at M3. Decisions before a season exists: name + Lock (2), plus 3 "Next" taps. Decisions the full Customize path invites: 11, each with a paragraph of help.

Terms introduced per step (quoted from `WizardState.swift` / `WizardSteps.swift` / `LeagueCopy.swift`):

- M3: league · bylaws ("before the bylaws lock") · "Start the league" (before anything is decided).
- M4: "Create your league — set the rules once, lock them in" · Pro ("Pro — that's you", "THE PRO", "you run this league").
- M5 cards: "How serious is your league?" · Casual/Standard/Cutthroat · "100% hcp / 95% hcp / 90% hcp" · "honor scores" · "post what you'd post to GHIN" · "attested where you can" · "rated tees" · "unlimited counting" · "best 3 / mo count" · "best 2 / mo" · "2-round floor" / "3-round floor" · "no floor" · "beer-league" · "the Pro rules on the rest" · "receipts required" · "Verification is a norm the league holds, not a filter the engine applies." · "Use these defaults →" · "Customize".
- M6 dials: Buy-in ("Per player · $0 = bragging rights") · Season length ("Weeks or months · ends the same weekday") · First tee ("Pick any day") · Teams / Solo / 2 Squads / 3 Squads / 4 Squads · "Individual · every player for himself" · "top 2 players meet in the Cup Final in the final four weeks" · "+10 head start" · "Cut line after 2nd" · "the full cup experience" · "1 golfer staged — solo fits" · "How teams fill" / Blind draw / Assign / "Pro assign" / "the server shuffles" / "posts the reveal" / "Argument-proof" · "How it ends" / Cup Final / Points table / "top seeds" / "reset" · "The pot split" / Balanced / Winner-heavy / Spread it / champ / runner-up / "Points King" · "the ledger" line · Counting cap ("Best N rounds / month") / "counter" · Participation floor ("MIN ROUNDS / MONTH · −5 SQD PTS SHORT") / "bye month" / "anti-ghosting".
- M5 portrait: "Your league so far" · "Forming — the rules aren't locked in yet" · Squads · "2 SQUADS · BLIND DRAW" · Endgame · "Cup Final" chip / "Points table" chip · The pot · "Bragging rights · $0 STAKE" · "$75 / player · 1 in so far · 60/25/15" · Season · "13 wk" · "FINAL 4".
- M7 review rows: STRUCTURE ("Individual — no squads" / "2 squads") · Squad formation ("Blind draw" / "Pro assign") · PRESET · HANDICAP ALLOWANCE ("95%") · VERIFICATION ("Post what you'd post to GHIN") · COUNTING CAP ("Best 3 / mo") · PARTICIPATION FLOOR ("2 / mo · −5 sqd pts / round short") · BUY-IN ("None · bragging rights") · POT SPLIT ("60 / 25 / 15 · champ / 2nd / king") · SEASON ("13 wk · Sat Sep 12 – Sat Dec 12") · CUP FINAL ("Final 4 weeks · from … · scored fresh") or FINISH ("Points table crowns it · whole season, one race") · "Lock opens the invite link — one link fills the league. The code works until first tee, or until you close the roster. Squads need four to tee off; solo tees off at two." · "Lock the bylaws & form the squads".
- M8: "Bylaws locked ⛳" · "One link fills the league" · "fills 2 squads" · "the draw runs" / "you seat the squads" · invite link · "Add golfers" · "Later — it lives in the league room".
- M9/M10: "Squads are forming" · "The Pro has the list" · "in the pool" · "seats open" · "Form the squads" · "blind draw" · "the hat" · "Draw squads" · "Start the season →" · "Minimum four to tee off".

**Distinct terms a first-time Pro meets on the default path (M3→M4→M5→M7→M8): ~48. With Customize: ~75.**

**User goal.** "Get my Saturday group into a season with something on it."

**Current friction (SAW).** The row is minted before any decision (M3) and the invite is offered after the last one (M8, D40). Everything between is the rulebook. The name is typed twice (M3 field + M4 "LEAGUE NAME" — `WizardScreen.swift:74`, `WizardSteps.swift:20`). Step 0 carries zero decisions (the name is already saved; the Pro chip is not a control). The first invite surface is a system share sheet; the in-app "Add golfers" finds only buddies/@handles (`PeoplePickerSheet.swift:47,81`). The default first tee is next Saturday (`WizardState.swift:148-154`), so a locked league opens in a 1–7 day pre-season where posted rounds "build your number; no league points yet" (D122). A squads league then needs four bodies and two more Pro taps (Draw, Start) before anyone's round counts.

**Unnecessary complexity (SAW).** Nine dials for one decision the brief wants ("what are we playing for?"). Six (i) paragraphs of 60–90 words each (`WizardState.swift:387-407`). Three presets whose *lines* are the banned vocabulary ("95% hcp · post what you'd post to GHIN · best 3 / mo count · 2-round floor"). A portrait card (M5) restating the review card (M7) restating the share line (M8) — three tellings of the same bylaws (brand canon "one fact one place"). A pricing card lives inside step 1 (`WizardSteps.swift:77`; hidden by flag today).

**Confusing terminology (exact strings).** "Lock the bylaws & form the squads" (the squads are minted *empty* at lock; they *fill* at the draw — `lock_league` `:198-202` calls `form_squads`, and `DraftCopy.formK = "Form squads"` names the draw with the same verb). "Assign" (seg) vs "Pro assign" (portrait, review) vs "you seat the squads" (share line) vs "Form squads · Pro assign" (draft). "Solo" (seg) vs "Individual — no squads" (review) vs "SOLO · EVERY PLAYER" (portrait). "Cup Final" vs "FINAL 4" vs "Final 4 weeks · scored fresh". "1 golfer staged" (D97 removed staging). "Standard rules locked for the season" toast on a preset tap two screens before Lock (`WizardState.swift:116`). "Points King" (undefined until the (i)). "−5 SQD PTS SHORT". "HANDICAP ALLOWANCE 95%" on the review (Q-32 of the prior remediation said to withhold it, D2/D48 — unbuilt on the phone). The floor *penalty* (none / deduct / forfeit-the-month) is set silently by the preset (`WizardState.swift:318`, `penaltyKeys`) and only appears as a review value "Forfeit month" for Cutthroat.

**Dead ends (SAW).** The wizard is a `fullScreenCover` with no Close: step 0 has Cancel (which discards), steps 1–2 have only Back and Next/Lock (`WizardScreen.swift:118-130`). "I'll finish this later" = there is no gesture; the only non-destructive exit is to Lock. (Prior M-016, still open on the phone.) "Bracket" in the event picker is a dead row. `WizardSeg` dims but does not block a structure that doesn't fit (good) — but the default itself, "2 Squads", doesn't fit a roster of one (CJ-14).

**Redundant actions (SAW).** Name twice. Portrait + review + share line. Toast "Bylaws locked" + sheet "Bylaws locked ⛳" + board post "Bylaws locked. First tee …" (`20260902163000:217-223`) within one second. "Share the invite link" appears on M8, M9, M23, the welcome sheet and the room header — five placements (fine as redundancy of a good thing, but the *first* one is on screen 5).

**Missing feedback (SAW/INFER).** After "Start the league" (M3) the toast says "<name> is on the books — set the bylaws" — a league now exists and the Pro is not told a code exists, that it admits nobody yet, or that abandoning means deleting. After Lock, nothing says how many people were told; `invite_open` is logged with `sent: 0` unconditionally (`WizardScreen.swift:289`). When the Pro taps "Later", the league sits in the Clubhouse draft hero with "Joins have gone quiet" only after 48h (`StandingsPane.swift:73-78`) — no nudge before.

**Opportunities for delight (INFER).** The engine already knows everything needed to make creation a sentence: "The Sunday Cup · you + 3 · first tee Sat Sep 12 · 13 weeks · Standard · bragging rights" — one card, editable inline, Lock underneath. The blind-draw reveal ("The hat has spoken") is the one ceremonial beat and it is buried on a dusk screen the members never see live. The Pro chip could become the *first* question ("Who runs it? You.") instead of a non-decision.

### 2.2 The Ryder

**Counts (SAW).** Screens: 2 (M11 picker → M12 sheet) + M13 optional. Fields: 8 (name, Team A, Team B, sessions, cadence, first tee, league, players). Required decisions before the event exists: **1 (name)**; everything else is defaulted — Red/Blue, 3 sessions, weekly, next Sunday, **and the league = `store.preferredLeague`** (`EventPickerSheet.swift:40`, `RyderSetupSheet.swift:62`). Terms introduced: event · the Ryder · "Short form · its own little trophy" · "weekly vs-index duels" · "the clinch" · Bracket/"Knockout · seeded" · LIVE/SOON · "mints a trophy for your display case" · Team A/B · Sessions · Cadence · "First tee (a Sunday)" · "Attach to a league" · "Standalone — invite anyone" · staged players · "session" (bold) · "paired 1-on-1" · "your best round that week — scored by how far you beat your own index" · "Win a duel = 1 point, tie = ½" · "first team past half the points" · "You captain Team A" · "draw or assign teams from the scoreboard" — **~22**. In the room: "Forming" · "Generate pairings" · "Score this session" · "Pairings not set." · "No one assigned yet." · "W-L-H" · "Duel taunts" · "number to beat" · "Still to post" · "Scrap" · "Run it back — next year's benches".

**User goal.** "Two teams of my friends, a few weekends, bragging rights."

**Friction (SAW).** The Sunday rule is enforced by the server ("The Ryder starts on a Sunday — sessions run Sun to Sat", `20260830190000:43`) and warned about after the fact in the sheet (`RyderSetupSheet.swift:97-100`) rather than by a picker that only offers Sundays. Players are "staged" and invited *after* create with errors swallowed (`EventsRepository.swift:202-205`, `try?`), so "Event created — 3 invited" can be false (INFER). Invited players get a push "X put you on the tee sheet" (`invite_golfer`, `20260827210000`) and an InvitesBanner row titled "Ryder invite · from X · first tee 2026-09-12" (raw ISO). Accepting lands them nowhere (the banner reloads Home; the room is reached via the Clubhouse EventChips or a push). A Ryder attached to a league appears on every league mate's Clubhouse as "NAME · RYDER · ENTER THE FIELD" (`EventChips.swift:30`, `EventCopy.chipSub` `EventMath.swift:431-433`), and the Ryder room offers a non-member no way in (only the organizer's "Invite players", `RyderRoomView.swift:88`).

**Dead ends.** A room with one player: "Add players to both teams to set the target." (`EventMath.swift:183`) · "Pairings not set." · "No one assigned yet." — three empty states on one screen. The Bracket row.

**Terminology.** "Sessions" and "Cadence" are undefined at the field; "event" and "duel" reach the user (D12 said schema-only). "Attach to a league" says optional while the default is attached.

**Delight (INFER).** The Ryder *is* the "Challenge" engine the brief wants (event_duels + resolve_session + number-to-beat + taunts), shaped for two teams. The same machinery at n=2 is a duel with a window.

### 2.3 A Major

**Counts (SAW).** Screens: 2 (+ picker). Fields: 7 (name, final day, window, buy-in, split, league, golfers). Required: 1 (name). Door: CLOSED on the phone in prod (`EventFlags.majorEnabled` reads `app_flags.ios.major`; the prod `ios` row has only `min_build` — PROD), OPEN on the web (`index.html:17899` "A Major · LIVE"). Terms: jug · "The final day" · Window · "Buy-in per player ($0 = bragging rights)" · "The pot pays" · "Top 3 · 60/25/15" / "Winner takes all" · "Run it with a league" · "best 18-hole card" · "established number (3 posted rounds)" · "exhibition" · "countback" · "logged coin flip" · "Set the Major" · "build the field" — **~15**; room: "FORMING · OPENS JUL 10" · "Enter the field" · "Needs 2 in the field to open." · "Open the window now" · "Sound the horn — settle" · "AWAITING THE HORN" · "Yet to card" · "the clubhouse".

**Friction (SAW).** Three Home Occasion cards ("Put a jug up", "Set the Major", "Name the jug" — `HomeStream.swift:202-211`) route to `showEventPicker` where the Major is not offered on the phone — the nudge leads to a menu without the thing (CJ-16).

### 2.4 A live side game from the tee sheet ("Play now")

**Counts (SAW).** Screens: 2 (M17 cover → M18 setup) + optional pars sheet + optional roster picker. Fields: course · tee · rating · slope · holes · pars · mode (2v2/solo) · 4 slots · guest name · guest index · game · stake · nearby toggle = **14**. Required decisions before a round exists: **0** (defaults: "Just score", only you; `teeOffProblem` → "Pick at least yourself", `LiveModels.swift:87`; no course required — M-157 still true, `LiveSetupView.swift:79-110` has no required field). For "Challenge Jake": pick Match play (3rd card) · add Jake (a chip if league mate/buddy/nearby, else "Search the app" or a guest) · type a stake = **3 decisions across three cards in the order course → foursome → game**. Terms: tee sheet · "Set up the round" · "Tee & rating — off the scorecard" · "Enter the pars" · "The foursome" · "2v2 teams" / "Everyone for themselves" · "Open slot · TAP A PLAYER BELOW" · You / Nearby / You play with / League / Buddies / Guests · BUDDY / GUEST · ASK / ASKING… · "estimated 18" · "Just score" / "Match play" / "Wolf" / "Skins" / "Sunningdale Rules" · "net best ball" · "Low net takes the hole's skin; ties carry the pot" · "go 2 down and you get a stroke" · "bank a unit" · "Stake per side · $0 = bragging rights" / "Dollars per point" / "Bank unit" / "Dollars per skin" · "strokes off the low man" · "Who's on this tee" · "Tee off →" — **~30**.

**User goal.** "Score today's match against Jake and settle up."

**Friction (SAW/PROD).** 21 of 26 live rounds are abandoned (81%). The screen is one long scroll of three cards with the thing the intent is about (the game) last. The 40-word fine print under the foursome ("Pick who plays with who under the game — pairings, stakes, the lot. Every complete card posts to its golfer at the finish; account-less guests play every game, post nothing. Leave index blank for an estimated 18.") answers questions nobody asked yet. Money vocabulary differs per game. There is no "vs" framing anywhere: it is a foursome builder, not a challenge.

**Delight (SAW).** "Nearby · ASK" (a chip that asks the other phone — D158) is exactly the consent grammar a challenge wants. The plan bridge "On your tee sheet today · Load it →" is the right bridge, one day late (§2.5).

### 2.5 A declared / scheduled round ("Plan a tee time")

**Counts (SAW).** Screens: 2 (cover → M19). Fields: 5 (day, tee time, course, note, tags). Required: **0** (next Saturday, 7:40 hidden until "Set a tee time", no course, no note, nobody). Terms: tee sheet · "Put a round on the tee sheet" · "Tee time · optional" · "Tag your group" · "the boards" · "Scratch it" · calendar · "Get in on it" / "I'm in" · HOST · RSVP I'm in / Maybe / Can't · "On the board" — ~12.

**Friction (SAW).** The plan has no game, no stake, no "vs" — "make it count" cannot be said here. It becomes a live round only on the day via the plan bridge, which loads course + group but not a game (`LiveSetupView.swift:64-75`, `store.loadPlan()`). The board post exists again since `20260831170000` (the 6-arg `declare_round` was posting nothing — restored; body not re-read here, see §5).

### 2.6 Joining by link or code

**Path (SAW).** Universal Link `cupseason.app/?join=CODE` → `CupSeasonApp.onOpenURL` (:29-35) stores the code and logs `link_opened` → (signed out: Door → OTP → golfer card; orientation is skipped for the invited, `OrientedFlag.take` :88-89) → `RootView.swift:43` **clears** the pending code and presents M20 with it pre-filled → M20 `.task` **auto-runs `go()`** (:49) → `league_by_code` (name only) → `join_covenant_info` → if `buyin_cents > 0` M21, else **`join_league` immediately** (`JoinModel.go` :99-100) → toast "Joined <name>" → M21b welcome → Clubhouse tab.

A typed code: Home "+" → "Join with a code" (M2b), or the doors' "Join a league" (M2). There is **no code field on the signed-out phone door** (`DoorView.swift`; the web has one at `index.html:2811`). A texted code (not a link) to a new golfer therefore means: sign in → card → orientation → "Join a league" — four screens before the code has a home — or, for a golfer already oriented, find the "+" key icon.

**What the joiner learns before committing (SAW).**
- Always: the league's name ("You're invited to <name>.").
- Only if buy-in > 0: BUY-IN · PRESET (one word) · PARTICIPATION FLOOR · FINISH, and the ledger line.
- Never: who the Pro is, who else is in, how many, the dates / first tee, solo vs squads, whether it has started, how the money is paid. `join_covenant_info` **already returns** `structure` and `phase` (`20260830300000:242-253`), and the phone's `Covenant` init **drops both** (`Kit/People/JoinLeague.swift:67-74`). D115 ("the covenant is the decision sheet": Pro, roster, dates, structure, pot, "How scoring works") was ruled 2026-08-29 and is unbuilt on the phone (and on the web: `covenantGate` `index.html:17702-17716` renders the same four rows).
- A $0 league: **nothing** beyond the name, and no confirmation tap at all on the link path (CJ-05).

**Refusals (SAW).** Server sentences pass through verbatim: "<name> isn't open yet — the Pro is still locking in the rules." · "The season's underway — ask the Pro to add you. You're welcome on the tee sheet meanwhile." · "The roster's closed for this season…" (`20260831190000` `_join_gate`). Good — but they arrive *after* the Join tap, though `phase` is available before it.

**After joining (SAW).** The welcome's three rules are structure-aware (D205) and fear-killing. Then the joiner lands in the Clubhouse; in a `draft` league that is "Squads are forming · The Pro has the list." + "See the squads" — a wait. Home's hero line for a member says "Casey draws the squads before first tee — it's random." (`HomeView.swift:960`) — a sentence, not a move.

### 2.7 Joining by in-app invite (league or event)

**Path (SAW).** The Pro/organizer taps Add in M22 → `invite_golfer` → push "X put you on the tee sheet" → M24 InvitesBanner on Home → "Accept" → `respond_invite` → for a league, `league_members` insert (Pro's door, halfway-turn rule) → `onJoined`. **No covenant on this path**, paid or not (`InvitesBanner.swift:53,84-90`). "Details" shows one sentence. For events: title "Ryder invite" for any `kind == 'event'` (`PeopleModels.swift:129`) — a Major invite reads "Ryder invite" with detail "A Ryder event — two teams, vs-index duels." (:137-141); subline prints the raw ISO date.

### 2.8 Joining an event any other way

None. Events have no code, no link, no `?event=` route (`CupSeasonApp.swift:28` claims only `?join=` and `?claim=`). A league member sees an attached Major as "ENTER THE FIELD" and can `enter_major` from the room (`MajorRoomView.swift:59-63`); an attached Ryder shows the same chip and has no door (CJ-15).

### 2.9 Run it back

**Path (SAW).** A `complete` membership → RunItBackCard (M26) → the wizard with the old bylaws and "<name> · S2" → the whole flow again (name sheet → 0 → 1 → 2 → lock → share) → a **new league id and a new code**; the crew must join again by code (D41 "continuity by convention"). The Ryder and Major run-backs prefill the setup sheet with the invitees re-staged (`RyderPrefill`, `MajorPrefill`) — better: one sheet, players pre-added, though every staged row reads "Name @?" because the prefill builds `Person(handle: nil)` (`RyderSetupSheet.swift:33`, `MajorSetupSheet.swift:34`, `EventBits.swift:98`).

### 2.10 Intent → engine mapping (what the brief's sentences become today)

| Intent | Closest existing object | Screens / decisions today | What is missing |
|---|---|---|---|
| **"I want to beat Jake"** | (a) a live **Match play** round on one tee (M17→M18: pick Match play, add Jake, optional stake) — needs co-presence and hole-by-hole scoring; (b) a **2-player solo league** (D205 ratifies "a solo league is a season at two") — the full wizard + Jake joins by code + first tee next Saturday: 5 Pro screens + 3 joiner screens; (c) a **Rivalry** — a lifetime record that accrues from live matches/clashes (`my_rivalries`), christenable from the Tour Card, but never *started*; (d) the **weekly clash** — the engine picks one pairing per league week (D52/D108), not the golfer; (e) a **Forfeit** "Against: Jake · Rides on: Sunday's duel" — league-only (`create_forfeit` → 'crew only', `20260724120000:59`), pride-only ("never money"), and nobody has ever posted one (PROD 0); (f) the **Ryder** at 1v1 — its duel engine (`event_duels`, `resolve_session`, `event_session_targets`, taunts) is exactly a "beat Jake this week vs our own numbers" machine, wearing team-and-session clothing | (a) 3 decisions on the 3rd card; (b) 11 controls / 2 decisions + a wait; (e) 5 fields inside the Pot pane | A **Challenge** object: two golfers, a window (this weekend / this month), a rule (best card vs your number — the Ryder duel; or a live match), an optional stake, a scoreboard, a share link; startable from a Tour Card, a buddy row, a feed item, and by Universal Link; no league required. The Ryder duel engine and `week_clashes` are its parents. |
| **"We're playing Saturday, make it count"** | Declare a round (M19: day + tags) → the boards → on the day, Play now with the plan bridge → Match play / Skins with a stake → settlement card | 2 screens now + 2–3 on the day; "make it count" is not expressible at declare time | A game and a stake on the plan; the plan auto-opening as the live round for tagged players at tee time (D163's accept-and-land exists for nearby); the plan's board post saying what is on it |
| **"Put $20 on it"** | League buy-in ladder 0/25/50/75/100/150/200 (`WizardState.swift:17`) — $20 impossible; live "Stake per side / per skin / per point / bank unit" free text; Major buy-in free text; Forfeit = never money | one stepper / one field, four vocabularies (M-062 still open) | One stake grammar ("$20 on it · settles when the round posts") that rides on a challenge, a plan, a live round, or a league; the ledger line said once |
| **"Play with friends" / "we want money on it"** (recurring) | The league (only) | 5–8 screens, 48–75 terms | A creation that starts from *who* and *what for* and defaults the rest (see §4 for what already defaults well) |

### 2.11 Web twin — divergence (brief)

SAW in `index.html`:
- **The web has D151's crew step** (`#obCrew` `:2859-2880`: "Who are you playing with?" · code box first "Got a league code? … A buddy texted you one? This is where it goes." · "Find your buddies" · "Start a league instead" · "I'll do this later"); the phone has none — its Orientation carries the three doors under the two teachings instead (`OrientationScreen.swift:9-14` says so).
- **The web door has a code field** signed-out (`#joinCode` `:2811`, `#joinGo` `:17734`) that validates with `league_by_code` before the email round-trip; the phone door has none.
- The web wizard is a two-column grid with the portrait as an aside (`:3476-3601`); the phone inlines the portrait under the dials. The web markup pre-selects "4 Squads" and prints "4 squads · the full cup experience for 8+ players." (`:3535-3537`) while the state default is squads2 — `resetWizard` (`:16163`) presumably corrects it; not verified.
- The web share moment opens `openInviteSheet` ("Invite your crew · ONE LINK FILLS THE LEAGUE · Copy link · Copy a message to send · Share… · Or read them the league code"); the phone has ShareLink + "Add golfers" and does not show the code as a code.
- The web `covenantGate` **fails open** (`if(!info || !Number(info.buyin_cents)) return true;` `:17705`); the phone's `JoinService.covenant` fails closed (`JoinLeague.swift:99-106`).
- The web keeps a direct-write fallback lock path for deploy skew (`:17368-17400`); the phone has none (D206: nothing droppable).
- The web collapses the three doors to one quiet "Start something else…" for anyone in a league (D119(4), `:11116-11119`); the phone's Home "+" menu is five items for everyone.
- The web offers the Major (LIVE); the phone gates it off.
- The web's `openJoinSheet` fine print promises "or you'll see an invite here when someone adds you" (`:17917`) — the phone's InvitesBanner does that on Home.

---

## 3. Findings

Severity: P0 blocks a goal · P1 major · P2 minor · P3 polish. "Damages" names which of the brief's five questions (what is happening · why it matters to me · what can I do now · who am I competing with · what happens next) the item hurts.

**CJ-01 · Every creation door is a database noun, not an intent.** P0. SAW: the doors read "Join a league · Start a league · Start an event" (`Wizard/LeaguelessDoors.swift:28-30`; copy `Kit/Wizard/WizardState.swift:462-464`); Home's "+" adds "Join with a code · Your golf calendar · Find golfers" (`Home/HomeView.swift:180-184`); the ⊕ cover offers "Play now / Post a round / Plan a tee time" (`Post/PostCoverView.swift:95-102`); the event picker offers "The Ryder / Bracket (SOON) / A Major (closed)" (`Events/EventPickerSheet.swift:26-29`). Nowhere can a golfer say "play with friends", "beat Jake", "this Saturday", or "money on it"; the league-vs-event split is the `leagues`/`events` tables. Damages: what can I do now. Rec: one "Start something" surface that asks Who / When / What for, and routes to league · challenge · plan · live · event underneath.

**CJ-02 · "Challenge a friend" has no object.** P0. SAW: the head-to-head primitives are all league-bound or engine-picked — `week_clashes` pairs are chosen by the tick (D52/D108); a rivalry is a record you cannot start (`You/RivalriesSection.swift`, `my_rivalries`); a forfeit requires a shared league and is pride-only (`create_forfeit` 'crew only', `20260724120000:59`; sheet sub "Pride, on the books — never money", `League/PotPane.swift:189`); the Ryder is teams × sessions; the Tour Card's only actions are Add buddy and Mute (`You/TourCardSheet.swift:134,176`). The same-day option (a live match) needs both golfers on one tee. PROD: 0 forfeits ever; the Ryder's duel engine (`event_duels`, `resolve_session`, `event_session_targets`, taunts) already scores "your best round this week vs your number" for a pair. Damages: who am I competing with · what can I do now. Rec: a Challenge object built on the Ryder duel at n=2 (or the clash settle), startable from any person row, with a link.

**CJ-03 · The wizard produces leagues of one.** P0. PROD: 6 of 13 leagues are `setup` with exactly one member (every setup league); lifetime `league_create` 2 · `lock_ok` 1 · `invite_open` 1. SAW: the row is minted at the name sheet (`Wizard/WizardScreen.swift:252-268` → `create_league`), the first human contact is the share sheet on screen 5 after Lock (D40, `WizardLockShareSheet.swift`), and nothing between asks who is in. D151 named this ("six golfers pressed Start a league and are still sitting in a league of one") and ruled a crew step for the web; the phone did not get one (`OrientationScreen.swift:9-14`). Damages: who am I competing with · what happens next. Rec: ask "who do you play with" before anything is minted; mint on lock, or mint silently and never show a one-person league as a thing to "continue".

**CJ-04 · The joiner decides on four money rows or on nothing.** P0. SAW: `CovenantSheet` shows BUY-IN · PRESET · PARTICIPATION FLOOR · FINISH (`People/JoinLeagueFlow.swift:135-139`) and only when `buyin_cents > 0`; a $0 league shows "You're invited to <name>." and nothing else (:41). The Pro's name, the roster, the dates, solo-vs-squads and whether it has started are absent; `join_covenant_info` already returns `structure` and `phase` (`20260830300000:242-253`) and `Covenant.init` drops them (`Kit/People/JoinLeague.swift:67-74`). D115 (the decision sheet: WHO · WHEN · HOW · STAKE) is ruled and unbuilt on both clients. Damages: all five. Rec: build D115 as ruled; a $0 league gets the same sheet minus STAKE.

**CJ-05 · A $0-league link joins with no consent tap, and "Not now" loses the code.** P1. SAW: `JoinLeagueFlow.swift:49` `.task { if vm.presetCode != nil { await vm.go() } }` → `go()` → `covenant()` returns nil for $0 (`JoinLeague.swift:102-106`) → `join()` immediately (:99-100); `RootView.swift:43` clears `JoinIntent` on appear, before the sheet; nothing stores a declined invite. D116 ("every signed-in `join_league` is preceded by the covenant; a decline is a state") is unbuilt on the phone. Damages: what is happening. Rec: one confirm on every path; keep the code until the Join tap; "Invited · <name> · REVIEW" card on Home after "Not now".

**CJ-06 · A texted code has nowhere to go on the signed-out phone.** P1. SAW: `Door/DoorView.swift` is email + OTP only; the web door has a code box that validates before the email (`index.html:2811, 17734-17748`). On the phone a new golfer with a code must sign in, do the card, meet the orientation, then find "Join a league" — or, if already oriented, find the "+" key icon on Home. Damages: what can I do now. Rec: a code field on the door (validate with `league_by_code`, which is anon-callable), or accept a pasted code anywhere a link is accepted.

**CJ-07 · The name is asked twice and step 0 has no decision.** P1. SAW: name sheet field (`WizardScreen.swift:74`) mints the row; step 0 repeats "LEAGUE NAME" with the same binding (`WizardSteps.swift:19-22`) plus a Pro chip that is not a control (:23-38). A four-screen wizard has one screen that decides nothing. (Prior M-014 still open.) Damages: what is happening. Rec: fold the Pro chip into the name sheet or drop step 0.

**CJ-08 · No non-destructive exit from the wizard.** P1. SAW: `fullScreenCover` (`Main/MainTabView.swift:369-371`) with no Close; Cancel exists only at step 0 and discards (`WizardScreen.swift:118-130,174-193`, "Cancel this league? It hasn't started, so this discards it completely."); steps 1–2 have Back and Next/Lock. "I'll finish later" has no gesture; the league persists in `setup` either way and the Clubhouse checklist says "Continue" (`StandingsPane.swift:35-37`). (Prior M-016 still open on the phone.) Damages: what can I do now. Rec: a Close that keeps the draft, and a draft that never looks like a league.

**CJ-09 · Step 1 is the rulebook.** P1. SAW: three preset cards whose lines are "100% hcp · honor scores · any course · unlimited counting · no floor" / "95% hcp · post what you'd post to GHIN · best 3 / mo count · 2-round floor" / "90% hcp · attested where you can · rated tees · best 2 / mo · 3-round floor" (`WizardState.swift:66-73`); nine dials with six (i) paragraphs of 60–90 words (`WizardSteps.swift:110-163`; `WizardState.swift:387-407`); ~48 distinct terms on the default path, ~75 with Customize (§2.1). Even the fast path ("Use these defaults →") sits *under* the cards, so the vocabulary is met before the escape. Damages: what is happening · why it matters. Rec: the default path should never render a preset card; "what are we playing for?" (bragging rights / $) and "how long?" are the two dials a first-timer needs; everything else behind Customize with the *consequence* stated in golf ("your best 3 rounds a month count").

**CJ-10 · One preset tap silently sets five bylaws, including a penalty that is never a dial.** P1. SAW: `WizardLockCall` sends `p_handicap_allowance = Bylaws.allow[preset]`, `p_verification = verificationKeys[preset]`, `p_floor_penalty = penaltyKeys[preset]` (`WizardState.swift:313-318`); Cutthroat = `forfeit` = "Forfeit month" (`LeagueCopy.swift:25`), shown only as a review value nobody chose. The review row "HANDICAP ALLOWANCE 95%" (`LeagueCopy.swift:136`) is the number D2/D48 withheld (prior Q-32, unbuilt on the phone). Damages: why it matters · what happens next. Rec: state each preset as consequences ("miss the floor twice and your squad gives back 5 a round") and drop the allowance from user surfaces.

**CJ-11 · The share moment is easy to skip and the in-app invite can't reach a stranger.** P1. SAW: "Later — it lives in the league room" (`WizardLockShareSheet.swift:57`); "Add golfers" → `PeoplePickerSheet` that lists buddies and @handle/name search only, "No golfers found. Invite links still work for everyone else." (:81); `invite_golfer` is Pro-only and profile-only (`20260827210000`); no contacts, phone numbers or email (prior M-002 open on the phone). PROD: `invite_open` 1 for `lock_ok` 1; 2 in-app invites ever. Damages: who am I competing with. Rec: the share sheet is the *first* screen after lock, pre-filled with the message, and "Add golfers" accepts a contact/phone.

**CJ-12 · After lock, a squads league is three verbs and a wait.** P1. SAW: Clubhouse draft hero "Squads are forming · The Pro has the list." + "Form the squads" + "Share the invite link" (`StandingsPane.swift:53-71`); DraftNight adds "Draw squads", "Start the season →" and the blocker "Minimum four to tee off — 1 in so far. Share the invite link." (`DraftNightScreen.swift:109-122`; `DraftFormation.swift:132-146`); three server steps (lock → draw → start_season) and two more Pro taps. "Lock the bylaws & form the squads" says the squads form at lock; they form at the draw. Damages: what happens next. Rec: one state ("3 more and the draw runs itself") with the draw + start automatic at first tee when the roster fits (the server already validates).

**CJ-13 · Every new league opens in a dead week.** P1. SAW: `defaultStart()` = next Saturday, never today (`WizardState.swift:148-154`); rounds before first tee "build your number; no league points yet" (D122); the review's SEASON row prints dates only (`LeagueCopy.swift:148`) and the share line says "First tee Sat Sep 12" (P-11). The first week of every league is, by construction, a week in which the app answers "why it matters" with "not yet". Damages: why it matters · what happens next. Rec: default first tee = today (D180's 7-day grace already keeps the code alive), or make the pre-season week *do* something (a practice board, the clash preview).

**CJ-14 · The default structure doesn't fit the founder.** P1. SAW: default `structure = "squads2"` (`WizardState.swift:102`) while the fit line under it says "1 golfer staged — solo fits. Bigger squads open up as more join, by code or invite." (:171-179); D205 records that both real seasons are two-person solo leagues; "staged" survives D97. Damages: what is happening. Rec: default to solo (any size), promote squads when the roster reaches four.

**CJ-15 · An attached Ryder promises "Enter the field" to league mates who cannot enter.** P1. SAW: setup sheets default `league` to `store.preferredLeague` (`EventPickerSheet.swift:40,43`; `RyderSetupSheet.swift:62`; `MajorSetupSheet.swift:62`) under a label that says "(optional)"; attached events are readable by league members (`20260720193000:79-81`) and chip as "NAME · RYDER · ENTER THE FIELD" (`EventChips.swift:30`; `EventMath.swift:431-433`, `mine ? status : "Enter the field"`); the Ryder room has no join for a non-member (`RyderRoomView.swift:86-107` — organizer only). Damages: what can I do now. Rec: `chipSub` says "Invite only" for a Ryder; or give the Ryder a self-enter like the Major.

**CJ-16 · The Major is closed on the phone while three Home cards sell it.** P1. PROD: `app_flags.ios` = `{min_build:0}` → `EventFlags.majorEnabled()` false (`Kit/Events/EventFlags.swift:16-20`); SAW: Occasion cards "Put a jug up" / "Set the Major" / "Name the jug" (`HomeStream.swift:202-211`) route to `presenter.showEventPicker` (`HomeView.swift:88`) where the Major row does not render (`EventPickerSheet.swift:28-30`) and the only other row toasts "Bracket isn't built yet". The phone's event menu is one live thing and one dead thing. (IOS-022 item 7 chose the curtain; the cards were not re-checked against it.) Damages: what can I do now. Rec: hide the jug occasions while the flag is off, and drop the Bracket row.

**CJ-17 · Event invites misname the thing and print raw dates.** P1. SAW: `Invite.title` = `isLeague ? "League invite" : "Ryder invite"` for every `kind == 'event'` (`Kit/People/PeopleModels.swift:129`) — a Major invite reads "Ryder invite", detail "A Ryder event — two teams, vs-index duels." (:137-141); subline "from X · first tee 2026-09-12" (:131-135). D12 makes "duel" and "event" schema-only. Accepting lands on Home, not in the room. Damages: what is happening. Rec: name the kind from `events.kind`, format the date, land in the room.

**CJ-18 · "Play now" builds a foursome, not a match.** P1. SAW: three cards in the order course → foursome → game (`LiveSetupView.swift:24-35`); the game the intent is about is third; the default is "Just score" with only you; no "vs" anywhere; a 40-word fine print under the slots (:138-140). PROD: 81% of live rounds abandoned. Damages: what can I do now. Rec: lead with the game and the opponent ("Match play · you v Jake · $20 a side"), course last and optional.

**CJ-19 · "Make it count" cannot be said on a plan.** P1. SAW: `DeclareRoundSheet` fields are Day · Tee time · Course · Note · Tag (`Schedule/DeclareRoundSheet.swift:42-74`); `declare_round` takes no game or stake (`Generated/Rpc.swift:398-410`); the plan → live bridge fires only on the day and loads course + group, not a game (`LiveSetupView.swift:64-75`). Two objects, no continuity of intent. Damages: why it matters · what happens next. Rec: a game and a stake on the plan; the plan opens as the live round at tee time for the tagged.

**CJ-20 · "$20 on it" has four grammars and one ladder that skips it.** P1. SAW: league buy-in ladder `[0, 25, 50, 75, 100, 150, 200]` (`WizardState.swift:17`) — $20 impossible; "Buy-in · Per player · $0 = bragging rights" (`WizardState.swift:393`) vs "Stake per side · $0 = bragging rights" / "Dollars per point" / "Bank unit" / "Dollars per skin" (`Kit/Live/LiveModels.swift:71-78`) vs "Buy-in per player ($0 = bragging rights)" (`MajorSetupSheet.swift:86`) vs "Post a stake · Pride, on the books — never money" (`PotPane.swift:189`). Prior M-062 still open. Damages: what is happening. Rec: one stake grammar with a free amount, and the ledger line said once where money is.

**CJ-21 · The web got the crew step; the phone did not.** P1. SAW: `index.html:2859-2880` (`#obCrew`: "Who are you playing with?" · code box first · Find your buddies · Start a league instead · I'll do this later) per D151; the phone's `OrientationScreen.swift:9-14` substitutes the three doors under the teaching. The phone is the shipping product and lacks the one signup step the owner ruled to ask the retention question. Damages: who am I competing with. Rec: port D151 (or supersede it with the intent-first surface of CJ-01) on the phone first.

**CJ-22 · The in-app invite path bypasses the covenant entirely.** P2. SAW: `InvitesBanner.swift:53` "Accept" → `respond_invite` (`20260830300000:187-232`) inserts `league_members` with no `join_covenant_info` read; Details shows "A season-long league. Invited by X." Paid league or not, the joiner is on the pot sheet on one tap. Damages: what is happening. Rec: the same decision sheet on every path (D116).

**CJ-23 · The Ryder's Sunday rule is a warning, not a picker.** P2. SAW: server raises "The Ryder starts on a Sunday — sessions run Sun to Sat" (`20260830190000:43`); the sheet shows the warm line after any non-Sunday pick (`RyderSetupSheet.swift:97-100`); `DatePicker` has no constraint. Damages: what can I do now. Rec: a Sunday-only picker or a "next Sundays" segmented row.

**CJ-24 · Ryder setup: eight fields for one decision, and the invites are fire-and-forget.** P2. SAW: fields at `RyderSetupSheet.swift:78-105`; "You captain Team A" is a footnote under the button (:113); `repo.invite` swallows errors with `try?` (`Kit/Events/EventsRepository.swift:202-205`) so "Event created — 3 invited" can be false (INFER). Damages: what happens next. Rec: name + who; teams/sessions/cadence defaulted and editable in the room.

**CJ-25 · Eleven new nouns on the Major sheet.** P2. SAW: jug · the final day · Window · the pot pays · Winner takes all · Run it with a league · exhibition · established number · countback · logged coin flip · Set the Major (`MajorSetupSheet.swift:78-104`); the room adds "Sound the horn", "AWAITING THE HORN", "Yet to card", "the clubhouse" (`MajorRoomView.swift:77,124`; `EventMath.swift:333-345`). The voice is right; the count is not. Damages: what is happening. Rec: keep the jug and the horn; move countback/exhibition/coin flip into the fine print only.

**CJ-26 · The orientation teaches the database's two containers as "the two ways to play".** P2. SAW: "The long game · A league · Months. Every round counts toward a table." / "The short game · An event · A weekend or a few weeks. Its own little trophy." (`OrientationScreen.swift:55-56`); the tee sheet — the thing 26 real rounds used — is not a way to play; the doors follow. The brief: no explainer slides. Damages: what can I do now. Rec: replace with the three questions (handicap · who you play with · what golf you play) and land in a useful Home.

**CJ-27 · Term drift inside one flow.** P2. SAW: "Assign" (`WizardState.swift:33`) / "Pro assign" (:242; `LeagueCopy.swift:31`) / "you seat the squads" (:446) / "Form squads · Pro assign" (`DraftFormation.swift:75`); "Blind draw" / "Draw squads" / "the draw runs" / "THE HAT SHUFFLES"; "Solo" / "Individual — no squads" (`LeagueCopy.swift:29`) / "SOLO · EVERY PLAYER" (:245); "Cup Final" / "FINAL 4" (`WizardSteps.swift:421`) / "Final 4 weeks · scored fresh" (`LeagueCopy.swift:150`). Damages: what is happening. Rec: one word per mechanic (brand canon "one fact one place").

**CJ-28 · The review is an all-caps bylaws table the Pro didn't write.** P2. SAW: rows STRUCTURE · Squad formation · PRESET · HANDICAP ALLOWANCE · VERIFICATION · COUNTING CAP · PARTICIPATION FLOOR · BUY-IN · POT SPLIT · SEASON · CUP FINAL (`LeagueCopy.swift:131-153`); values like "Post what you'd post to GHIN" and "2 / mo · −5 sqd pts / round short". Damages: why it matters. Rec: a sentence per row in the Broadcaster voice ("Your best 3 rounds a month count.").

**CJ-29 · Help that explains the engine's absence and contradicts a ruling.** P2. SAW: "Verification is a norm the league holds, not a filter the engine applies." (`WizardState.swift:389`); `fillHelp` ends "Live draft night with picks and a clock isn't built yet." (:399); `floorHelp` says "One Pro-approved bye month per season" (:407) while D14 made the bye automatic and the web's own help says "your season bye covers it automatically" (`index.html:3567`) — prior Q-27 unbuilt on the phone. Damages: what happens next. Rec: delete the first two sentences; one `floorSentence` producer.

**CJ-30 · "Standard rules locked for the season" on a tap that locks nothing.** P2. SAW: preset tap toast (`WizardState.swift:116`; `WizardSteps.swift:88`) two screens before Lock; Lock later toasts "Bylaws locked" and the sheet says "Bylaws locked ⛳" and the board posts "Bylaws locked. First tee …" (`20260902163000:217-223`). Damages: what is happening. Rec: no toast on a preset; one "locked" moment.

**CJ-31 · The bylaws are told three times before the league has a second member.** P2. SAW: portrait card "YOUR LEAGUE SO FAR" (`WizardSteps.swift:324-381`) → review rows (M7) → share line (M8). Brand canon: one fact one place. Rec: the portrait *is* the review; drop the table.

**CJ-32 · A pricing card lives inside the creation flow.** P2. SAW: `PricingPassCard(flags:roster:buyInCents:)` under the portrait in step 1 (`WizardSteps.swift:77`), rendering "The league pass · One pass, the whole league, every season you run for a year — … split it on Venmo — less than a sleeve each" when `pricing.visible` flips (PROD false today). Damages: why it matters (to the wrong party). Rec: pricing belongs in the Pot pane/Settings, never between the dials and Next.

**CJ-33 · Home's "+" mixes creation with navigation.** P2. SAW: Start a league · Start an event · Join with a code · Your golf calendar · Find golfers (`HomeView.swift:178-190`), a11y "Start or join". Damages: what can I do now. Rec: the "+" is the intent surface (CJ-01); places live in tabs.

**CJ-34 · The one intent-flavoured entry is calendar-gated and event-only.** P2. SAW: six `Occasion`s (`HomeStream.swift:200-214`), five route to the event picker, one ("A fresh table", Dec 27–Jan 15) to the wizard and only when league-less. Good voice, narrow door. Rec: the occasion grammar ("Two teams. One cup. You know the one. → Run your own") is the model for every door, every day.

**CJ-35 · The primary button names a squad that will be empty.** P2. SAW: "Lock the bylaws & form the squads" (`WizardState.swift:416`; D161's "tee-it-up" rename deferred); squads are minted empty at lock (`lock_league :198-202`). Rec: "Lock it in" / "Open the invites".

**CJ-36 · The share sheet hides the code it exists to hand over.** P2. SAW: the card shows "cupseason.app/?join=CODE" as a URL (`WizardLockShareSheet.swift:40-47`); no "read them the code" affordance (the web's `openInviteSheet` has "Or read them the league code"); the code appears as "CODE X" only in MembersSheet. Rec: the code, big, plus the link.

**CJ-37 · The join sheet's header is wrong for the link path.** P2. SAW: "Join a league · I HAVE A LEAGUE CODE" (`JoinLeagueFlow.swift:33`) over a pre-filled code and an auto-running join (:39,49). Rec: "You're invited to <name>" is the header; the code is a footnote.

**CJ-38 · The welcome recruits the joiner and miscounts its own rules.** P2. SAW: "THREE THINGS TO KNOW" over four bold items when paid (`JoinLeagueFlow.swift:181-198`; prior M-155 open); the fourth, "Who else plays with you? Growing the league isn't the Pro's chore — any member's link works." asks the newest member to grow the league before seeing the room. Rec: the joiner's welcome ends in the joiner's first move.

**CJ-39 · A joiner's first screen is a wait.** P2. SAW: `onJoined` → `tab = .clubhouse` (`MainTabView.swift:475`); a `draft` league shows "Squads are forming · The Pro has the list." + "See the squads" (`StandingsPane.swift:53-58`); Home's hero line "Casey draws the squads before first tee — it's random." (`HomeView.swift:960`). No verb for the member (D119's member cells exist as sentences only). Rec: "Post a round — it builds your number" / "Plan Saturday" as the member's first move.

**CJ-40 · Six placements of the same three doors.** P2. SAW: Orientation (`OrientationScreen.swift:138`), league-less Clubhouse (`ClubhouseView.swift:137-155`), Card & settings ×2 (`CardAndSettingsScreen.swift:412,529`), Home "+", Occasion. Rec: one surface, one voice.

**CJ-41 · The wizard promises joins by code during setup, when the code admits nobody.** P2. SAW: `structToast` "plays best at 4+ — fine if more join by code" and `structFitLine` "…as more join, by code or invite." (`WizardState.swift:176-184`) while `_join_gate` refuses in `setup` ("<name> isn't open yet — the Pro is still locking in the rules.", `20260831190000`). Rec: "…as more join once you lock".

**CJ-42 · The stage picker and run-back rows show "@?".** P2. SAW: `EventStagedRow` prints `"@\(person.handle ?? "?")"` (`Events/EventBits.swift:98`); `RyderPrefill`/`MajorPrefill` build `Person(handle: nil)` (`RyderSetupSheet.swift:33`; `MajorSetupSheet.swift:34`). Every run-it-back invitee reads "Name @?". Rec: fetch the handle or omit the line.

**CJ-43 · The event picker sells the trophy and labels rows like a release train.** P2. SAW: "Every event mints a trophy for your display case." (`EventPickerSheet.swift:31`); "LIVE" / "SOON" chips (:62); prior M-149 open. Rec: only built things; no status words.

**CJ-44 · "Start an event" is offered to a golfer with nobody.** P2. SAW: the door appears in Orientation and the league-less Clubhouse for a golfer with zero buddies; the Ryder room then shows "Add players to both teams to set the target." · "Pairings not set." · "No one assigned yet." (`EventMath.swift:183`; `RyderRoomView.swift:121,165`). Rec: events unlock when there are people; the door leads with "bring your crew".

**CJ-45 · No link or code for an event.** P2. SAW: `CupSeasonApp.swift:28-36` claims only `?join=` and `?claim=`; the jug card says "START YOURS AT CUPSEASON.APP" (`MajorJugCard.swift:71`); invites are Pro-typed only. Rec: an event share link with a covenant-style card.

**CJ-46 · Run it back = do the whole wizard again, under a new code.** P2. SAW: `RunItBackCard` → `WizardScreen(runBack:)` (`LeaguelessDoors.swift:92-110`); a new league id and code (D41); the crew must re-join. Rec: "Same crew, same bylaws" should mean the roster carries over and the Pro confirms one date.

**CJ-47 · Two landings for one act.** P2. SAW: `MainTabView.wizardLinks.onLocked` → Clubhouse tab (`:472`); `OrientationScreen.doorLinks.onLocked` → `leave("league")` → the tabs rise on Home (`OrientationScreen.swift:198`); `SettingsLeaguelessDoors` → `openLeague` (`CardAndSettingsScreen.swift:635`). Rec: one landing (the share moment, then Home).

**CJ-48 · The plan bridge is a day late and course-only.** P2. SAW: `planBridge` shows only when `store.plan` is today's declared round (`LiveSetupView.swift:28,64-75`); "Load the course and your group into the tee sheet" — no game. Rec: the plan opens the round (CJ-19).

**CJ-49 · 81% of live rounds are abandoned and nothing records why.** P2. PROD: 21/26 `abandoned` (the 24h sweep in `daily_season_tick`, `20260902163000:40-45`). SAW: no telemetry on setup drop (`tee_off` etc. from Q-34 not found on the phone). INFER: setup friction or exploratory taps. Rec: log setup opened / teed off / abandoned-at-hole.

**CJ-50 · The web covenant fails open; the phone fails closed.** P2. SAW: `index.html:17705` `if(!info || !Number(info.buyin_cents)) return true;`; `Kit/People/JoinLeague.swift:99-106` throws. One product, two consent behaviours. Rec: fail closed on both.

**CJ-51 · The floor's abbreviation on the dial.** P3. SAW: "MIN ROUNDS / MONTH · −5 SQD PTS SHORT" (`WizardState.swift:406`). Rec: "at least N a month, or your squad gives back 5 a round".

**CJ-52 · Tool words reach the golfer.** P3. SAW: "Loading the wizard…" (`WizardScreen.swift:49`), "Could not open the wizard." (:248), "change anything in the wizard" (`WizardState.swift:468`), "Lock settings first: the draft opens after setup" (`DraftFormation.swift:98`). Rec: never "wizard".

**CJ-53 · Placeholders: the owner's league is the Major's example.** P3. SAW: "The PIGL Championship" (`MajorSetupSheet.swift:79`) vs "The Big Slice, The Sunday Cup, Dew Sweepers…" (good, `WizardState.swift:368`) and "The Grudge Match" (Ryder). Rec: invented names only.

**CJ-54 · The code format is hard to say out loud.** P3. SAW: four letters of the name + four base-36 characters (`WizardState.swift:266-277`, e.g. "DEWS7K2Q"). Rec: word-ish codes or the link as the only artifact.

**CJ-55 · The first-tee picker allows the past and the review does not warn.** P3. SAW: `DatePicker` with no range (`WizardSteps.swift:125-127`); `lock_league` accepts any date; D180's 7-day grace saves the code window (`20260831190000`). Rec: `in: today...`.

**CJ-56 · Season length as a 13-rung ladder.** P3. SAW: `durs = [2,3,4,5,6,8,10,13,17,22,26,39,52]` (`WizardState.swift:18`) stepped with −/+; a Pro cannot say "through Thanksgiving". Rec: pick the last day; derive the weeks.

**CJ-57 · "Standalone — invite anyone" describes the wrong difference.** P3. SAW: `EventLeaguePicker` (`Events/EventBits.swift:114`); an attached event can also invite anyone (`invite_golfer` is profile-based). INFER. Rec: "On its own" / "With <league>".

**CJ-58 · Sheet-over-sheet for the Ryder.** P3. SAW: `EventPickerSheet` (`.sheet`, detents medium/large) presents `RyderSetupSheet` as a nested `.sheet` (:39-44) — two drag indicators, the picker dismissed after create. Rec: push inside one sheet.

---

## 4. What already serves the brief (keep, and cite)

- **"Use these defaults →"** — one tap from step 1 to review (`WizardSteps.swift:61`; web A-W3). The escape hatch exists; it just sits under the rulebook.
- **A row is minted only after a name** (D5 "My Cup husks" lesson; `WizardScreen.swift:68-93`) — no scaffolds named "My Cup".
- **The lock is one server transaction, idempotent, and the server decides the phase** (`lock_league` `20260902163000:121-229`; `WizardLockCall.optionalArgs = []`, `WizardState.swift:292-330`) — a retap after an ambiguous failure returns the truth; nothing droppable can lock a league on defaults (D111/D206). Prior M-001 (P0) is closed on the phone.
- **The wizard's defaults are the row's defaults** (D206, `20260902160000`) — re-entry shows what the founder chose: points · squads2 · 13 wk · $0.
- **The lock ends on the share moment, not a Done toast** (D40; `WizardLockShareSheet.swift`), the seat-math line is honest and structure-aware, and **"First tee <date>" is said instead of "Season is live" until the date comes** (P-11, `WizardState.swift:441-453`).
- **Solo tees off at two and every minimum derives from one table** (D205; `Bylaws.structMin`, `WizardCopy.inviteNote`, `DraftCopy.startBlocker`).
- **Guidance never blocks**: a structure that doesn't fit is dimmed and toasted, not disabled (`WizardSeg` `WizardSteps.swift:272-299`, a11y hint "A stretch for your roster — still yours to pick").
- **The covenant names the stake BEFORE `join_league` and fails closed on the phone** (`JoinLeague.swift:99-106`); the sheet's two buttons are honest ("Join — I'm in for $50" / "Not now"); the ledger line is one constant (`MoneyCopy.ledger`).
- **The welcome's three rules kill the fear at the door** and are structure-aware: "You can't hurt your standing by playing badly. Only by not playing." (`JoinLeagueFlow.swift:192-195`).
- **Server refusals reach the golfer verbatim, with the league's name** ("<name> isn't open yet — the Pro is still locking in the rules.", `_join_gate`; `HumanError` pass-through) and **the pending code is removed before the attempt so a failure can't loop** (`JoinIntent`, `JoinLeague.swift:4-8`).
- **The invited skip the orientation** (D116 item 3; `OrientedFlag.take` :88-89).
- **The tee sheet is league-less by design** (D107): "Play now" works with guests and no account; "Nearby · ASK" is consent-shaped (D158, `LiveSetupView.swift:229-257`); the plan bridge exists (`:64-75`); accepted rounds land you in them (D163).
- **`LiveGame.note` is plain golf** ("Low net takes the hole's skin; ties carry the pot.", `LiveModels.swift:35-41`).
- **The Ryder sheet needs one field**; Red/Blue, 3 sessions, weekly, next Sunday are sensible defaults (`RyderSetupSheet.swift:44-70`); run-it-back prefills the benches (`RyderPrefill`).
- **The declare sheet needs zero fields** and has the "Get in on it / I'm in" twin (`DeclareRoundSheet.swift:38-40,76`).
- **The Occasion cards** are the right grammar for a door: an oblique golf moment, one verb, dismiss per year (`HomeStream.swift:200-214`).
- **The Pro chip replaced a dead email field** (D97; `WizardSteps.swift:24-37`) and the Pro is always the creator.
- **The blind draw's copy**: "Argument-proof." · "THE HAT SHUFFLES SERVER-SIDE — NOBODY RIGS THE DRAW" · "The hat has spoken" (`WizardState.swift:35`; `DraftFormation.swift:85-88`).
- **Any member's link works** (welcome, MembersSheet, draft hero) — recruiting is not the Pro's chore.
- **D119's member sentences name the Pro** ("Casey draws the squads before first tee — it's random.", `HomeView.swift:957-961`) rather than handing members the lock button; the wizard is reachable only through Pro-gated doors on the phone (`StandingsPane.swift:35`, `LeagueRoomLinks.openWizard`).
- **The Ryder duel engine** (`event_duels`, `resolve_session`, `event_session_targets`, opt-in taunts, W-L-H) — the challenge primitive the brief wants already exists; it is wearing the wrong clothes.

---

## 5. What I could not determine from reading

- Whether `resetWizard` on the web corrects the "4 Squads" pre-selected markup (`index.html:3535-3537`) before first paint — the state default is squads2.
- Whether a buddy (no shared league) actually sees a league-less golfer's declared round in their Home stream (the sheet promises "BUDDIES & LEAGUE MATES SEE IT"); `HomeStream` was not traced for scheduled rounds.
- The body of `20260831170000_declare_round_posts_and_guards.sql` beyond its header (it says the board post is restored on the 6-arg overload; not re-read).
- Fold positions at default type on a 5.4" phone (no simulator run): whether "Create the event" / "Set the Major" sit below the "How it plays" card, and whether "Use these defaults →" is above the fold on step 1.
- Why 21 of 26 live rounds are abandoned (no telemetry on the setup screen; could be exploratory taps or a real drop).
- What Universal Links do when the app is not installed (the web door path) — outside this area, but it is the joiner's first screen for most invitees.
- Whether `EventsRepository.invite`'s swallowed errors ever fire in practice (only 2 invites exist in prod).
- Whether the Pro's buy-in payment note (`set_buy_in_terms`, D129) has any phone surface — none found by grep; `join_covenant_info` returns `has_pay_note` and the phone ignores it.
- Whether the web's `crew_step_done` telemetry has ever fired (no rows in `client_events` for `crew_step_done` or `orientation_done` in the query above — likely never emitted, or the names differ).
- The exact pricing copy that will render inside step 1 when `pricing.visible` flips (read `PricingPassCard` fragments only).
