# Verify SP-6 · lens "is-it-structural" · tip 3bba87e · 2026-09-04

Verdict: **HOLDS** — narrowed. Roughly half of the statement's itemised defects ARE dissolvable by a copy, single-screen or default change and should be demoted to aggravations; the residue that survives every such fix is what makes the problem structural, and it is enough to leave persona A's Q2/Q4/Q5 and persona E's pre-commit Q4/Q5 unanswered. No fix required breaks a wall law; two of the fixes RESTORE one (L-12).

## 1 · The refutation I tried (SAW code at tip; the "fix" column is what I'd change)

| # | Item in the statement | Smallest fix that would dissolve it | Kind | Dissolves it? |
|---|---|---|---|---|
| 1 | Three card steps; required marker with no default; handle + 60-day line; index/GHIN copy | `CardGateView.swift:21` `marker: String? = nil` → a random `CSMarkers.all` key (L-24 keeps 14 named markers; "every skipper was a saguaro" `:3` is satisfied by a random default, not by forcing a choice); handle is already derived silently `:97`; step 3 copy → "What do you usually shoot?" | default + copy | **Yes.** L-08 (`Models.swift:246-249`, `CLAUDE.md:226-235`) constrains the STATE MACHINE (needsCard when either column is empty), not what the card must ASK. The root_cause's "L-08 … so signup asks the schema's required columns first" overstates the law. |
| 2 | Push ask over the first Home | `PushAskPolicy.shouldAsk` gains "no memberships, no buddies → defer `.cardSaved`" | default | **Yes.** `.cardSaved` is one of push-contract §6's three sanctioned moments (`PushAskPolicy.swift:1-6`), so the ask is L-20-compliant today; the fix is a policy line. |
| 3 | Texted code has no door on the signed-out phone | A "Got a code?" box on `DoorView.emailStage` (`:91-114`) calling `league_by_code` (anon: `contract.psv:169`, L-45) and `JoinIntent.store` — the web's `:2811-2814` | single screen | **Yes.** Also gives the App-Store-"Open" case a manual fallback (the persona re-typed the link; with a box she types the code). |
| 4 | Door does not name the league for a pending link | `DoorView` reads `JoinIntent.pending()` and renders "You're invited to X" (FR-07's fix) | single screen | **Yes** for the link-tapped-then-installed case where the code IS stored (`CupSeasonApp.swift:35`). **No** for the cold App Store "Open" — no `onOpenURL` fires (`JoinIntent.store` has exactly one call site, `CupSeasonApp.swift:35`), so there is nothing to read; only #3's manual box or a deferred-deep-link mechanism covers it. |
| 5 | $0 league gets no covenant | `JoinLeague.swift:104` drop `c.buyinCents > 0`; `CovenantSheet` hides BUY-IN at $0 (D115 §3's own wording) | default + UI | **Yes** — and it is a RESTORATION of L-12 ("every join passes the covenant", D116 `:4196`), not a design choice. |
| 6 | Home's one-tap Accept joins a stake league with no covenant | `InvitesBanner.swift:53,84-90` route Accept through `JoinLeagueFlow`'s covenant | single screen | **Yes** — same L-12 restoration. |
| 7 | "Not now" loses the invite | Move `JoinIntent.clear()` from `RootView.swift:43` to the Join tap (`JoinLeagueFlow.swift:109` already clears there) | default | **Partly.** Cancel/swipe stops losing the code — but the result is the web's loop (the sheet re-raises at every launch, `index.html:20026-20031`), which is exactly what D116 §2's "a decline is a STATE" was ruled to replace. A REVIEW state is a persisted key + a Home card + the doors' rendering + "Not interested" — cross-surface. (Note: at tip "Not now" does NOT lose the invite — `onNo` nils the covenant and returns to the Join sheet with the code preset, `JoinLeagueFlow.swift:51 → :34-42`; Cancel/swipe loses it.) |
| 8 | Welcome hides the roster behind a recruiting pitch; no Done | `LeagueWelcomeSheet` (`JoinLeagueFlow.swift:173-215`) lists the roster (a member may read it — `MembersSheet` exists) and gets a Done | single screen | **Yes, post-join.** |
| 9 | Orientation is an explainer slide | Replace `OrientationScreen`'s body with the web's `#obCrew` (`index.html:2859-2878`, D151/O-14) | single screen | **Asks the question — but see §2.** |

## 2 · What survives every fix above (the structural residue)

**R1 · The cold golfer's answer to "who do you play with?" has nowhere to go and nothing consumes it.**
- The only invite link on the phone is a LEAGUE's join code: `PeopleScreen.swift` inviteLink comment — "The link is the LEAGUE's join link, which is the only invite link that exists. A buddy-invite link is a different mechanic and would need a decision"; `shareables` is empty for a league-less golfer (empty-search sentence `:71-73`); grep for a personal/buddy link in both clients: none. Contact invites: D136 Q-07 "not scheduled" (`decision-log.md:4394-4443` block). `search_golfers` cannot find someone who is not on the app.
- The phone's Home never consumes the answer: `HomeMode.of` returns only rung 7 or 6 (`Models.swift:308-309` — "5: buddies, no league" is declared at `:287` and never produced), so "Four makes a league" / "Find your buddies" never renders; the empty feed is "No rounds from your buddies yet. Post one, or add some buddies." (`HomeView.swift:121-124`) whatever was answered.
- So for persona A (three friends off the app, wants one Saturday) the crew step (fix #9) is a question with no place to put the answer: "Find your buddies" finds nobody, "Start a league instead" is the wizard that made him the Pro of a 13-week two-squad league (persona A Part 7), and skip lands on the same Home. A's own five-question table at first Home: Q2 No · Q4 No · Q5 No. Answering them needs (i) a new mechanic — a buddy/personal invite or a contacts path (an L-03/L-04 RPC + a decision reversing D136 Q-07's "not scheduled") — and (ii) a Home that leads with the answer (IA; SP-1's territory). Neither is copy, one screen or a default.

**R2 · The joiner's decision surface cannot say WHO or WHEN without a new server read.**
- `join_covenant_info` (`20260830300000_join_window.sql:236-256`) returns name · buyin_cents · preset · floor · finish · structure · has_pay_note · buy_in_due_on · phase — no Pro, no roster, no dates. It is the ONLY pre-join read by code: the RPCs taking `p_code` are `create_league`, `join_covenant_info`, `join_league`, `league_by_code`, `sandbox_find` (`contract.psv:116,166,167,169,243`); no roster/member RPC is callable by a non-member (L-36/L-37 gate roster and Tour Card on membership). `Covenant.init` (`JoinLeague.swift:67-74`) could pick up `structure`/`phase`/`buy_in_due_on` client-side — that is a single-screen improvement — but "Casey runs it · 5 in · starts Saturday" is unreachable from the client.
- Persona E at the covenant: "Not on this screen and I wish it were: who else is in (Casey, and who?), when it starts (Casey told me Saturday; the app doesn't)". D115 named this sheet as the bail point. The brief's own progressive disclosure is "Who? When? What are we playing for?" — two of the three are unanswerable pre-commit without D115's RPC, which is a mechanics-level change (a migration; L-04 grants; L-36 fail-closed; roster names for signed-in callers only per D115 §1). Fix #8 answers Q4/Q5 only AFTER the $50 consent.

**R3 · The invited path and the cold path share one door that reads neither intent, and the link does not survive the App Store.** Fix #3/#4 patch the door; the cold-install case (`JoinIntent.store` only in `onOpenURL`, `CupSeasonApp.swift:35`; no Smart App Banner `app-argument`, no `NSUserActivity`) still delivers an invitee to a league-less Home with the code in a text message. A texted-code fallback is a screen; carrying the link is a mechanism.

**R4 · A decline state is cross-surface** (fix #7) — a key, a Home card, the doors, a clear path; D116 §2, unbuilt on both clients.

**Result of the lens test.** With every copy/screen/default fix applied, persona A's Q2/Q4/Q5 remain unanswered (R1) and persona E's Q4/Q5 remain unanswered at the moment she consents to $50 (R2). holds = true.

## 3 · Immutable-law check (reader-canon §3A)

No fix SP-6 needs breaks a wall law. Specifically:
- **L-12** ("every join passes the covenant", D116 `:4196`) is currently VIOLATED by the $0 path (`JoinLeague.swift:104`) and the banner path (`InvitesBanner.swift:84-90` → `respond_invite`, `20260830300000:187-205`). The statement should say so: items #5/#6 are restorations of a wall law, not proposals.
- **L-08** is the gate condition, not a mandate to ask. A one-step card with a default marker (L-24: 14 named markers, the marker is the floor) and a derived handle keeps the gate intact. The root_cause sentence should be corrected.
- **L-06** — the "every first run needs an email round-trip" evidence must not be read as licence for a magic link or another social login; the remedy is the owner's Apple flag (D186; `DoorFlags.swift`, `DoorView.swift:104-110`). State this so the design phase does not propose a banned door.
- **L-45 / L-36** — a code box on the door uses `league_by_code` (already anon); D115's enrichment keeps `join_covenant_info`'s signature (D115 §1 "one RPC, richer, same signature") with names for signed-in callers only — no new anon endpoint, no table grant.
- **L-20** — `.cardSaved` is a sanctioned moment; deferring it for the league-less is within the contract.
- **D132 (owner DECIDED)** — "the Pro" stays; the fix DEFINES it at first contact (the covenant's WHO row is one of D132's three named places), never renames it.
- **v23.55 "no foyer"** — the crew step is a signup step, not a room (D151's own resolution); R1's "Home leads with the answer" must be built as Home, not as a foyer.
- **CC-29** — "what golf you play" has no data home; a profiles column is a migration with the column-grant landmine (`CLAUDE.md:192-198`). Device-local is fine if the answer only orders doors; do not assume a column.

## 4 · Corrections to the statement / evidence

1. "eight surfaces and eight taps" — eight surfaces is the reader's count (FR §2) and matches the code path; "eight taps" is unsupported: persona A counts 10 taps to first Home (11 with the push sheet), persona E 17 (including the App Store). Say "eight surfaces; 10–11 taps cold".
2. "'Not now' loses the invite" → "Cancel or a swipe loses the invite (`RootView.swift:43` cleared it before the sheet); 'Not now' returns to the Join sheet with the code preset (`JoinLeagueFlow.swift:51, :34-42`) — a loop, not a state".
3. "contact invites declined by D136 Q-07" → "not scheduled — 'revisit only if a real Pro asks'".
4. "D116 §3 is half-built" → "built on the phone (`OrientationScreen.swift:88-89`), not on the web (`index.html:14980-14983` gates only on `cs_oriented`)".
5. Demote to aggravations (dissolvable by copy/default/one screen): the card's three steps and marker gate (#1), the push ask (#2), the texted-code door (#3), the welcome's roster/Done (#8), the $0 and banner covenant bypasses (#5/#6 — restorations of L-12). Keep as the structural core: R1 (no mechanic or Home rung for the crew answer), R2 (no pre-join WHO/WHEN read), R3 (one door, link lost on install), R4 (no decline state), plus the sequence itself (schema columns → containers taught → no people/golf question).
6. Add the two facts that make the crew question structural rather than a screen swap: `HomeMode.of` never produces rung 5 (`Models.swift:287, :308-309`); the only invite link is a league's (`PeopleScreen.swift` inviteLink comment; `shareables` empty for the league-less).
7. Add: `join_covenant_info` is the only pre-join read by code (`contract.psv:116-169, :243`) and no roster RPC is non-member-callable — the evidence that D115 is a mechanic, not a sheet.
8. Root cause, implementation line: replace "L-08 gates the app on marker AND handle, so signup asks the schema's required columns first" with "the card asks for what L-08 gates on instead of defaulting it; L-08 itself only requires the columns be set".
9. Personas hit: A and E carry the problem; D is hit lightly (one sentence about the doors on a screen she'll not see again; D's walk starts at the orientation with 0 taps and never met the door, code or card). List D as secondary.
10. The welcome's "Who else plays with you?" line is a UI-copy defect (L-33/L-34), fixable in place; the structural welcome defect is that the join ends on recruiting rather than the joiner's first move (CJ-38/CJ-39) — but that is SP-1/SP-2's Home, so cite it as a hand-off, not as SP-6's own.
11. Costs: add "friend connection is unmeasurable" — `crew_step_*` and `covenant_declined` have never recorded (FR-09; `index.html:6900`), so the redesign cannot A/B the one step built for it until the `qaEvent` guard is dropped.
