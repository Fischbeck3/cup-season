# Verify SP-6 · lens "already-ruled" · tip 3bba87e · 2026-09-04

Verdict: **HOLDS** — but as a *ruled-and-mostly-unbuilt* problem, with two of its named "defects" being rulings that were built exactly as written (D82's orientation, D104 §6's push ask). The design phase must cite each entry below by number as executed, amended or superseded (CC-30); nothing here may be re-proposed as new. All file:line as of `3bba87e`; prod facts re-read read-only via `supabase db query --linked`.

## 1 · Ruling-by-ruling status (SAW)

| Ruling | What it rules | Web at tip | Phone at tip | Status |
|---|---|---|---|---|
| **D82** `:2856-2876` | ONE skippable orientation after the golfer card, "exactly two things" (four places · two ways to play); seen-flag once per device | `#obOrient` `index.html:2905`; `showOrientation` `:14990-14994` (flag set on show) | `OrientationScreen.swift:46-58` copy verbatim; `OrientedFlag.take` `:85-91` (evidence-gated: no league, no event, no round); `RootView.swift:37-41, 62-70` | **RULED + BUILT on both.** The phone's screen landed in `92aeaf2` (2026-09-02, the D204–D214 commit) with **no decision-log line**; D185 `:5119` (08-31) still says "iOS keeps its own gap — no orientation and no crew step" — stale on the orientation half. |
| **D104 §6** `:3816-3819` | the push permission ask is contextual — "after the card is saved, the first round is posted, or a league is joined — with a one-screen explainer first, never on launch" | n/a (web push differs) | `CardGateView.swift:238` `PushAsk.shared.request(.cardSaved)`; `MainTabView.swift:329-335` drains once the tabs mount; `PushAskPolicy.swift:29-33` | **RULED + BUILT as ruled.** The sheet over the very first Home is the ruling's own placement, not drift. |
| **D115** `:4182-4190` | `join_covenant_info` gains pro_name · members · phase · dates · weeks · payout · pot; the door renders a league card above the email box when a code is pending; the covenant becomes WHO · WHEN · HOW · FLOOR · FINISH · STAKE; a $0 league gets the same sheet minus STAKE (D136 Q-08/09/11 `:4406-4409`) | RPC (prod, `pg_get_functiondef`) = name, buyin_cents, preset, floor, finish, structure, has_pay_note, buy_in_due_on, phase — **no pro_name / members / dates / pot** (`20260830300000_join_window.sql:236-256`; `grep pro_name supabase/migrations` = 0). `covenantGate` `index.html:17702-17712` renders four rows and returns `true` for $0 (`:17705`). Door: only T2-02's sentence "You're invited to X. Sign in to review…" `:20456` / `:17751-17753` — no card. | `Covenant.init` `JoinLeague.swift:67-74` reads five keys, drops structure/phase/has_pay_note/buy_in_due_on; `covenant()` `:104` returns nil for $0; `CovenantSheet` `JoinLeagueFlow.swift:133-139` four rows; door/card read nothing (`DoorView.swift:91-114`; grep `JoinIntent` in Door/Onboarding = only `OrientationScreen.swift:88`) | **RULED, UNBUILT on both** (T2-01/03/04 + T2-14 in the prior plan, never scheduled). Note: the pay-path boolean (D129) and `phase` (D161) ARE in the RPC and both clients discard them — a client gap on a built RPC. |
| **D116 §1** `:4192-4200` | every signed-in `join_league` is preceded by `covenantGate` | `boot()` `:20026-20040` and `resumeAfterProfile` `:20093-20111` both gate; `respondInvite` `:19044-19046` does not | `JoinModel.go()` gates; `InvitesBanner.swift:84-90` → `respond_invite` does not | **BUILT for `join_league` on both; `respond_invite` is outside D116's letter on both** (the ruling names `join_league`, so the Home Accept gap is an extension to rule, not a violation to cite). |
| **D116 §2** | "Not now" moves the code to a declined key; Home / the league-less doors render "Invited · {name} · REVIEW"; Review reopens the covenant | code stays in `cs_code` (`:20034`, `:20105-20110`), Home tile still "None yet · JOIN OR START" `:11044`; the kept code re-fires the covenant at next boot `:20026-20031` (a loop, not a state); no `cs_invite_declined` anywhere | `RootView.swift:43` clears the intent on appear; `onNo` `JoinLeagueFlow.swift:51` nils the covenant and returns to the join sheet (`:34-42`) with the code preset — Cancel/swipe then loses it; grep `Invited`/`declined` in `apps/ios` = 0 | **RULED, UNBUILT on both** (T2-06, T2-14). |
| **D116 §3** | the invitee skips the orientation | `showOrientation` gated on `cs_oriented` only `:14980-14983` (T2-07's `viaInvite` check never added) | `OrientedFlag.take` `:88-89` returns false when a join/claim intent is pending | **BUILT on the phone, UNBUILT on the web** — the SP's "half-built" is right; name the halves. |
| **D117** `:4202-4210` | one product sentence under the slogan on both doors; a "How it works" link on the Terms line; the long-game card gains "The Pro runs it. The cup is the season title." | sentence only in `<meta>` `:22`; `#obOrient` long-game card `:2905` unchanged; 0 hits for "runs the league"/"The Pro runs it" | `OrientationCopy.longGame` `:55` "Months. Every round counts toward a table."; `ForgeView.swift:161` slogan | **PROPOSED (owner's call), UNBUILT on both** (T2-11, T6-06). |
| **D132** `:4353-4362` RULING | keep "the Pro"; define it at first contact (orientation long-game card, covenant WHO row, Clubhouse chip) | none of the three | none of the three (`OrientationScreen.swift:55`; no WHO row `JoinLeagueFlow.swift:133-139`) | **RULED (owner), UNBUILT on both.** |
| **D151** `:4588-4602` | §1 crew step (code box → find buddies → start a league) once, cold arrivals; §2 home course + city INTO the card; §3 search row renders course/index; §4 doors reorder Join · Start a league · Start an event; §5 handle copy stops lying | §1 `#obCrew` `:2859-2878`, wired `continueAfterCard` `:15006-15015`; §2 `#pfHome` `:2830-2831`, `set_profile(p_home…)` `:14955-14965`; §4 built; §5 `:2828` "It can be changed later, but only once every 60 days, and your leagues are told" | §1 **absent** (`OrientationScreen.swift:9-13` "the phone has no crew step"); §2 **absent** — `CardGateView.swift:157` still defers city/home course to You; §4 built (`LeaguelessDoors.swift:4`); §5 `CardGateView.swift:106` "It changes once every 60 days." (the pre-D151 line) | **RULED; §1/§2/§5 web-only, §4 both.** The one question that starts the graph is ruled and unbuilt on the shipping client. |
| **D159** `:4763-4778` | formal handle change flow; "the phone gets a change path" | built | built (per D159) | irrelevant to the card gate except that the phone's card copy predates it. |
| **IOS-023** (DECISIONS.md:247) + **D186** `:5121-5140` | Sign in with Apple beside the code, flag-gated; Apple's one-shot name caught | n/a | `DoorView.swift:104-110` renders only if `flags.appleSignIn`; `DoorFlags.swift` fails closed; prod `app_flags.ios = {min_build:0, note:…}` — no `apple_sign_in` | **RULED + BUILT, flag-closed; D186 "Not built, owner's: flipping `apple_sign_in` on, and — before that — registering the sending domain with Apple's Private Email Relay".** An owner-owed deployment, not a design gap. |
| **D136 Q-07** `:4422-4424` | contact invites "**not scheduled**. The link plus a prewritten Copy message is the invite… Revisit only if a real Pro asks." | — | — | The SP says "declined by D136 Q-07"; the word is *not scheduled*. |
| **D3** `:59-70` | the welcome's three sentences ("You can't hurt your squad by playing badly…") | built | built (`JoinLeagueFlow.swift:187-193`, D205 solo variant) | The welcome's CONTENT is ruled; no ruling puts a roster or a Done on it — the SP's welcome finding (CJ-38) is unruled/new. |
| **D83** `:2878-2908` | the door sells with its own splash (demo retired) | built | built | The slogan door is an owner decision; D117 is its (proposed) amendment. |
| **CLAUDE.md:226-233** / **IOS-002 `:73`** | gate onboarding on `marker` AND `handle` (the m001 trigger auto-creates the row) | built | `Models.swift` `needsCard`, `RootView.swift:34-35` | This is the implementation invariant the SP calls "L-08" — **"L-08" is the reader's own id** (`reader-canon-and-constraints.md:129`), not a canon id. It is a working rule, not a decision-log ruling; no D-entry ties the *order* of the card's questions to it. |

**Never ruled anywhere (genuinely new under the brief):** asking "what golf you play" (no decision, no profile column — `set_profile` `contract.psv:274`; CC-29); a typed-code door on the phone's signed-out screen (IOS-004 `:68` designed the phone first run without one; D151 §1 put the code box in the crew step; D115 §2's door card is for a *pending link* only); a roster or a Done on the welcome; a link that survives an App Store "Open" (`JoinIntent.store` only in `onOpenURL` `CupSeasonApp.swift:35`; no Smart App Banner `app-argument` in `index.html`).

## 2 · Prior remediation plan (docs/audit/blind-ux-2026-08-29/plan)

- `t2-joining-and-consent.md`: T2-01 (RPC prospectus), T2-03 (door card), T2-04 (decision sheet), T2-06 (decline persists), T2-07 (invitee skips orientation, web), T2-11 (door sentence), T2-13 (share text carries the offer), **T2-14 "iOS parity for D111/D112 — L"** (`:185-192`: "do not `JoinIntent.clear()` in `.onAppear`… add `JoinIntent.decline()`… league-less doors show 'Invited · Review'") — none built; only T2-02 (landing copy `:20456`), T2-05 (consent on every `join_league` path) and T2-08 (one code noun) landed. T2-14 `:189` says "the phone has no D82 orientation screen… so D112 §3 needs nothing there" — overtaken: the phone now has one and does skip invitees.
- `t6-…-ios.md` T6-06 `:168-175` (door sentence + orientation definitions, web + iOS; verification "the orientation's league card contains 'the Pro runs it'") — not built.
- `reader-prior-audit-delta.md:14, 172, 187, 226` reaches the same status (TOP-2 "RULED-NOT-BUILT (both clients)").

## 3 · Prod (read-only, 2026-09-04)

- `client_events`: `orientation_shown` 5, `orientation_done` 0 (phone emits it at `OrientationScreen.swift:212`), `covenant_declined` 0 (web emits at `:20034`, `:20108` — but through `qaEvent` `:6898-6903`, which still carries `if(state.demo || !window.sb) return` — the same guard D185 removed from `growthEvent`; `state.demo` is cleared only in `showWelcome`/`resetToBlank`, which run AFTER the crew step and the signup covenant), `crew_step_*` 0. **The zeros are therefore not evidence that nobody took the crew step or declined a covenant; they are evidence the web's `qaEvent` guard swallows both.** (INFER from D185 `:5108-5113`'s own account of `state.demo`; not runtime-tested.)
- `join_covenant_info` in prod = the `20260830300000` body (nine keys; no pro/members/dates).
- `app_flags.ios` has no `apple_sign_in`.

## 4 · Why it still holds

Rewriting nothing in this table would produce the brief's onboarding (handicap · who you play with · what golf you play → a useful Home): the card's order is the schema's required columns first (CLAUDE.md:226-233), the orientation teaches containers by D82's own design, D151's question is unbuilt on the shipping client, D115/D116 §2/D117/D132 are ruled and unbuilt on both, and the two parts that ARE built as ruled (D82, D104 §6) are the parts the brief says to remove ("no explainer slides"; "notifications create anticipation, never manufacture engagement" — a permission sheet over an empty first Home).

## 5 · Corrections to the statement / evidence

1. Reframe the orientation and the push ask: **not defects but rulings built as written** — D82 (both clients; phone since `92aeaf2`, unlogged) and D104 §6 (phone). The overhaul's decision must retire/amend D82 and D104 §6 explicitly (O-03), not treat them as drift.
2. "'Not now' loses the invite" → on the phone "Not now" returns to the join sheet with the code preset (`JoinLeagueFlow.swift:51 → :34-42`); **Cancel/swipe** loses it because `RootView.swift:43` already cleared the intent. On the web the code is kept and the same covenant re-fires at every boot (`:20026-20031`) — a loop, not D116 §2's REVIEW state.
3. "D116 §3 is half-built" → built on the phone (`OrientationScreen.swift:88-89`), unbuilt on the web (`:14980-14983`).
4. The Home one-tap Accept (`respond_invite`) bypasses the stake on **both** clients (`index.html:19044-19046`; `InvitesBanner.swift:84-90`) and sits outside D116 §1's literal `join_league` rule — the design phase must extend D116, not cite it as violated.
5. D151: cite §1/§2/§5 as web-built / phone-unbuilt and §4 as built on both; the card's "changes once every 60 days" (`CardGateView.swift:106`) and "City and home course live on your card — add them any time from the You tab" (`:157`) are the pre-D151 lines the web already replaced (`index.html:2828-2831`).
6. `join_covenant_info` already returns `structure`, `has_pay_note`, `buy_in_due_on` (D129) and `phase` (D161); both clients discard them. "No pay path" is a client gap; D115 §1's unbuilt part is pro/roster/dates/pot.
7. D136 Q-07 says contact invites are "not scheduled… revisit only if a real Pro asks", not "declined".
8. Sign in with Apple (FR-20): IOS-023 decided, D186 built; the flag and the Private Email Relay registration are owner-owed — list under "owner owes", not under the structural problem.
9. "L-08" is a reader id; the canon source is CLAUDE.md:226-233 and IOS-002 `:73` (a working rule, not a D-entry).
10. The telemetry zeros (crew_step, covenant_declined) should be stated as "never recorded because `qaEvent` still has the demo guard (`:6900`)", not as behaviour counts; D185 fixed only `growthEvent`.
11. Add D3 (the welcome's three sentences) and D83 (the slogan door) to the rulings the design must cite; add D185 `:5119` as stale on the orientation half.
12. Evidence line "D115 unbuilt on both" should add the built exceptions: T2-02's door sentence (`:20456`), consent on every `join_league` path (web boot/resume; phone `go()`), and `phase` in the RPC.
