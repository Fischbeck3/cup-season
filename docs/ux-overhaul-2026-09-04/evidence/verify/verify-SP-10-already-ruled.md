# Verify SP-10 — lens "already-ruled" (2026-09-04, tip 3bba87e)

**Verdict: HOLDS.** Neither of SP-10's two central claims is ruled anywhere in D111–D221, `docs/ios/DECISIONS.md` (IOS-006..027), or the prior remediation plan: (a) which client is the behavioural reference after parity — IOS-018/D100 explicitly *deferred* it ("a later decision, not this one") and no later decision exists; (b) the metric events (app-open, view, empty-state, platform stamp) — never proposed in any D-entry. The prior plan's measurement theme (T7) planned a large part of the scoreboard and none of it was built or D-numbered. Several evidence lines need correction (§4), and one cited gap is ruled AND built (db-checks 15).

## 1. Ruled AND built — remove or soften in the statement

| Ruling | What is built | Evidence |
|---|---|---|
| D111 / prior-plan T1-16 + T7-04 "alert when lock_fail > lock_ok" | `tests/db-checks.sql` **check 15 "lock health"**: FAIL on any `lock_fail` carrying a JS reference error, or `fails > oks` with ≥ 3 attempts, over 7 days | `tests/db-checks.sql:326-355` |
| D185 §4 — the `state.demo` guard "is deleted, not narrowed" | Built for `growthEvent()` ONLY | `index.html:6915-6930` (the D185 comment + `if(!window.sb) return;`); `qaEvent` at `:6900-6904` still reads `if(state.demo \|\| !window.sb) return;` |
| Platform stamping — by hand at three phone call sites; `build` on the five IOS-024 product events | `home_occasion_tap` and `orientation_shown/done` carry `platform: "ios"`; `CSTelemetry.product` carries `build` | `apps/ios/CupSeason/Home/HomeView.swift:87,91`; `Onboarding/OrientationScreen.swift:153,212`; `Kit/Telemetry.swift:50-54` |
| D126 endgame sentence | ONE producer per client fed by ONE shared fixture — not "phone-first, web owed" | `LeagueCopy.endgame` (Kit), `endgameLine()` `index.html:6318`, `tests/fixtures/endgame.json`; D126 `:4294` says "ported from the web's `endgameLine()`, both read from the shared fixtures" |
| D120 producer + preflight 20 | the one cross-client string lint the statement names is real | `tests/preflight.mjs:635-655`; db-checks 17 gates band edges (`tests/db-checks.sql:393`) |

Notes on the first row: "the lock has fired zero telemetry since its fix" is still TRUE (prod 2026-09-04: `lock_attempt` 1 and `lock_ok` 1, both 2026-07-27; nothing after `lock_league` shipped 2026-08-29/30). The eleven historical `lock_fail` rows D111 cites are gone from prod — cascaded with the wiped audit profiles — so check 15 has never had anything to watch and has lost its own baseline. `v_lock_health` (T7-02) and the `deploy-status` funnel line (T1-16's second half) are NOT built: no migration, test or tool reads `lock_fail` except `db-checks.sql` (`grep -rln lock_fail supabase/migrations tests tools` → `20260829220000_lock_league.sql` comment, `app-tests.js:94` comment, `preflight.mjs:524` comment, `db-checks.sql`); `tools/deploy-status.mjs` has no `client_events` read.

## 2. Ruled, NOT built — holds; the design phase must cite these

- **IOS-018 / D100 — the reference question is ruled-as-deferred.** `DECISIONS.md:181`: "Only after parity is real does the ecosystem question get asked: what the web should be as a standalone product next to the phone (a later decision, not this one)." `decision-log.md:3645-3648` says the same. The later decision never came: `DECISIONS.md` is 271 lines and its last entry is IOS-027 (2026-08-28); no IOS-028 exists (the prior plan's T3-11 promised an "IOS-028 parity record"); no D-entry after D100 re-rules the reference (`grep -n D100` finds nothing after line 5000). `CLAUDE.md:301-303` still reads "The web client is the behavioural reference; the phone owns operating the league, the desk owns authoring it (IOS-007)" — the second clause is contradicted by IOS-018's own consequences line (`DECISIONS.md:185`: "D99's 'operating vs authoring' split no longer bounds the phone"). The flip was first WRITTEN inside a build note — D138 `:4456`: "the web is a deferred task under this entry, not the reference" — a sentence, not a ruling.
- **D136 Q-37 (`:4437-4439`) — telemetry names: "the WEB's names win where the clients differ (`league_create`, `lock_ok`) … the phone's `league_created` / `league_locked` retire."** NOT built: `Kit/Telemetry.swift:27-31` still defines both; `Wizard/WizardService.swift:62,138` still emits them; `TelemetryTests.swift:66-67` asserts the retired names; prod carries `league_create` AND `league_created` on 2026-08-30. This is the one telemetry-parity ruling in the log and it is unbuilt.
- **The four explicit "web owed" notes** — D121 `:4243` ("The web row is still owed"), D130 `:4334` ("the web hero still repeats it, `index.html:11520` — a web task, logged here so it is not lost"), D138 `:4456`, D219 `:5671` ("a web task under this entry"). Verified unbuilt at tip: no second-league row on the web and the boot toast still fires (`index.html:20066`); `:11520` still repeats the tiebreak; `upcomingFromSchedule` `:10985-10986` still drops `mine===false` and ignores `my_rsvp`.
- **Prior plan T7 (`docs/audit/blind-ux-2026-08-29/plan/t7-measurement-monetization-gate.md`) — planned, never D-numbered, never built.** T7-01's seven web events (`invite_shared`, `covenant_seen`, `covenant_skipped`, `joined`, `rules_open`, `endgame_seen`/`endgame_open`, `pot_mark`/`pot_mark_denied`): zero of the nine names exist in `index.html` (grep over every `qaEvent(`/`qaEvent?.(` site — the full web vocabulary is 28 names, none of these). T7-05 iOS parity: not built. T7-06 event registry `packages/telemetry/events.json` + preflight check 18: `packages/` holds only `db`, `tokens`, `README.md`; check 18 became the `staged` free-identifier lint (T7-07) instead (`preflight.mjs:519`). T7-02 `v_lock_health`: not built. The decision-log never mentions "T7-".
- **Prior plan T2-12 covenant breadcrumbs (`covenant_shown` / `covenant_join` / `covenant_decline` / `invite_resume`)** — half-built and swallowed: only `covenant_declined` exists (`index.html:20034` via `'link'`, `:20108` via `'signup'`) and BOTH sites run before `showWelcome()` clears `state.demo` (`:19847`; boot's site `:20034` precedes boot's `showWelcome()` at `:20056`; `resumeAfterProfile`'s `:20108` precedes its `showWelcome()` at `:20119`). `crew_step_shown/done` (`:15012`, `:15016`) fire from `continueAfterCard()`, called straight from the card save (`:14983`, `:14998`) before any `showWelcome`/`resetToBlank` (`:9024`, `:13581`, `:19847`). Prod 2026-09-04: zero rows of `crew_step_*`, `covenant_*`, `invite_resume`, ever. FR-09 CONFIRMED at code and prod; D185 is the precedent (same guard, same reasoning, applied to one of the two functions) — cite it, do not re-argue it.
- **D135 — the monetization gate** (`:4384-4393`) reads `v_lock_health`, `endgame_open`, `pot_mark_denied`, `v_preseason_exposure` — none exists. The one business-level ruling that depends on a scoreboard is unmeasurable for the same reason the brief's metrics are.
- **D131 retired-terms lint** (T6-11, `tests/retired-terms.json`) — not built; covered in verify-SP-8; relevant here only as "copy laws have two homes with one lint".

## 3. NOT ruled anywhere (D111–D221 / IOS-0xx / plan) — the design phase may propose, naming what it overrides

- **`app_open` / a view event / `home_empty_state_seen` / `empty_state_cta_tapped` (DL-30).** D33 defined four post-composer events (`:877-906`); IOS-024 five product events (`docs/ios/reliability.md:101-113`); T7-01 seven funnel moments — none is an open or view event. Nearest existing thing: the web's `home_hero_state` (`index.html:11205`, deduped once per page-session via `window.__heroSeen`, tags `complete | cup_final | season | forming | rung5 | rung6 | rung7`; a second producer at `:11435` with `{stage, role, cta}` for the forming hero) — a de facto per-session Home-view + hero-state event, WEB ONLY (prod: 90 rows all unstamped; 44 rows / 8 golfers in 30 days). The phone's entire `CSTelemetry.event` vocabulary is `push_opened`, `push_action`, `push_prompt_*`, `orientation_shown/done`, `home_occasion_tap`, plus the five product events and the wizard/post names — no hero state (HM-44 confirmed). `push_opened` (7 rows/30 d) is the only open-shaped row and only for push-launched opens.
- **A client column on `client_events` or `rounds`.** `rounds.source` exists (`'quick' | 'live' | 'scan_claim'`, `20260718172300:221-223`) — a posting mode, not a client. No migration or ruling adds a platform column anywhere (`grep -i platform supabase/migrations` → only push-token rows).
- **The screenshot set tied to the build.** IOS-027 (`DECISIONS.md:267`) says only "Screenshots: the 6.9" set captured from the simulator via the dev hatches". D195 (`:5322`) flattened the alpha channel of the same six files on 2026-09-01 (`apps/ios/Screenshots/6.9/01..06.png`, mtime Sep 1 04:33, last commit e4eb7f8) and did not reshoot. YP-41 (six retired strings) is unruled.
- **CC-33 (⊕ contract).** IOS-011 `DECISIONS.md:127`: "⊕ is a full-screen cover opening on the post form"; D110 addendum `:4112-4119`: "the ⊕ … now ALWAYS open the cover". Code follows D110: `MainTabView.swift:338` sets `postOnComposer = false; showPost = true` → `PostCoverView(startOnComposer: false)` at `:389-390`. IOS-011 is stale and unamended.
- **CC-34 (CLAUDE.md:301-303).** Stale; no entry corrects it.
- **CJ-50 (fail-open vs fail-closed covenant).** D116 rules "consent on every join path"; it never rules the failure mode. The web's `covenantGate` `:17701-17705` fails open by design ("Fails open to the old flow while the RPC isn't deployed (skew)"); the phone rejects that reasoning (`JoinLeague.swift:99-101`: "a stake that goes unnamed is exactly what S3-01 exists to prevent"). Preflight 19 (`preflight.mjs:620`, "every join passes the covenant") checks that the gate is CALLED, not what it does on failure. Partially ruled; the failure mode is the unruled half.
- **The web freeze trigger / "contract parity not IA parity" / shared producers as the copy deck's format (WB §6, CC-31).** No entry. D201 `:5449` is a diagnosis ("Where a producer exists and is bypassed, the copy is wrong. There is not one exception") plus a PROPOSED canon law (`:5453-5458`, "logged here, not yet written into the canon"); brand-canon §3 "One fact, one place" was later added (`spec/brand-canon.md:77`) — that is a copy law, not a client-architecture ruling.

## 4. Corrections to the statement / evidence

1. **"eight Home rulings … built phone-first with 'web owed' notes"** → four entries carry an explicit web-owed note (D121 `:4243`, D130 `:4334`, D138 `:4456`, D219 `:5671`). D216 / D217 / D218 are "BUILT 2026-09-02 — client only" with the web UNMENTIONED (silent divergence — arguably the sharper fact). D126 is built on BOTH clients from a shared fixture; its only owed piece is the hero's repeated tiebreak, recorded under D130. D129 is data (`native_home()` v2) both clients can read. Recast: "four rulings say the web is owed; three more diverged without saying so; D138 declares the web 'not the reference' inside a build note; the IOS log has had no entry since 2026-08-28."
2. **"no app-open or view event exists"** → "no app-open event on either client; the web's `home_hero_state` (`:11205`) is a per-session hero-state/view event with no phone counterpart and no platform stamp." WAU-shaped counts are possible on the web from it today (8 golfers / 30 d) and impossible on the phone.
3. **"nothing stamps the platform … `CSTelemetry.event` does not stamp platform (`Telemetry.swift:41-48`)"** → "neither writer stamps it (`Telemetry.swift:41-48`, `index.html:6900`); three phone call sites hand-stamp `platform:'ios'` (`HomeView.swift:87,91`, `OrientationScreen.swift:153,212`) and the five product events carry `build` (`Telemetry.swift:50-54`); the web stamps nothing. Prod 30 d: 131 rows / 8 golfers with neither, 24 rows / 5 golfers with `build`, 5 rows / 1 golfer with `platform`."
4. **"the lock has fired zero telemetry since its fix"** → keep, and add: the watcher IS built (db-checks 15) and has had nothing to watch; the eleven `lock_fail` rows that motivated it are gone from prod (profile cascade), so its baseline is gone too; `v_lock_health` and the `deploy-status` funnel line are unbuilt.
5. **Add D136 Q-37** as a ruled-unbuilt telemetry-parity item (the phone still emits `league_created` / `league_locked`; prod has both spellings on 2026-08-30).
6. **Add the prior plan's T7-01 / T7-05 / T7-06 / T7-02 as planned-never-built and never D-numbered**, and T2-12 as half-built-and-swallowed; name D185 as the precedent for the `qaEvent` fix.
7. **Add D135**: the monetization gate's eight evidenced items read views and events that do not exist — the same absence blocks a business ruling already on the books.
8. **"every copy law has two homes with one lint"** → "two homes with one string lint (preflight 20, stages), one numeric check (db-checks 17, band edges) and one shared fixture (`tests/fixtures/endgame.json`); no event registry (T7-06), no retired-terms lint (T6-11 / D131)."
9. **Root cause** — sharpen "flipped the reference in fact without an entry" to "flipped it in three 'client only' build notes and one sentence in D138's build note (`:4456`), while `DECISIONS.md` has had no entry since IOS-027 (2026-08-28)".
10. **CJ-50** — cite D116 as the consent ruling and state that the failure mode is the unruled half; the web's fail-open is a documented skew guard (`:17701-17704`), not an oversight.
11. **Prod figures re-verified 2026-09-04 (read-only):** 30-day `client_events` vocabulary = `post_open` 69/4 golfers, `home_hero_state` 44/8, `signed_in` 14/5, `client_error` 8/3, `post_submit` 7/3, `push_opened` 7/1, `orientation_shown` 5/1, `home_hero_tap` 2/1, `league_create` 1, `league_created` 1, `push_prompt_shown` 1, `round_posted` 1; zero `crew_step_*`, `covenant_*`, `invite_resume`, `app_open`, `wizard_gate_bounce`; `lock_*` last seen 2026-07-27. The "signup walk" set is the web (`docs/audit/signup-walk-2026-08-31/1-door.png … 11-guide-scoring.png`, including `7-crew-step.png`, a web-only screen). `apps/ios/Screenshots/6.9/*` are dated Sep 1 (commit e4eb7f8, D195) — before the D204–D214 You wave that retired their strings.
12. **Personas** — keep D/E/A as cited; note that E's "I HAVE A LEAGUE CODE" (`persona-E-invited-joiner.md:86`) and A's "No 'I have a league code' on the phone" (`persona-A-new-golfer.md:44`) are the same web-vs-phone door divergence, unruled (D117 is PROPOSED, `:4202`).

## 5. What the design phase must cite (CC-30) rather than re-propose

IOS-018 / D100 (supersede the deferral by name; correct `CLAUDE.md:301-303`; add the missing IOS-0xx entry) · D136 Q-37 (web names win — build it or reverse it) · D185 (extend the guard deletion to `qaEvent`) · D111 + db-checks 15 (the lock watcher exists) · D135 (its gate needs the same events) · D121 / D130 / D138 / D219 (each web-owed note becomes a shared-producer port or an explicit non-goal) · D116 (rule the covenant's failure mode) · D201 + brand-canon §3 (one fact, one place — extend from copy to producers) · prior plan T7-01 / T7-05 / T7-06 (reuse the event names and the registry design; do not invent a third vocabulary) · IOS-011 vs D110 addendum (amend IOS-011).
