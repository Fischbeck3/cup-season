# Verify · SP-3 · lens "already-ruled"

Verifier: adversarial, read-only. Tip 3bba87e. Prod read via `supabase db query --linked` on 2026-09-04.
Question: is "creation asks the organiser to configure the engine before anything exists; no intent can be said" already ruled (D111–D221, IOS-0xx, prior plan) — and if ruled, is it built?

## Verdict

**HOLDS.** No ruling in D111–D221, docs/ios/DECISIONS.md, or docs/audit/blind-ux-2026-08-29/plan/* rules an intent-first creation layer, a pre-mint Who/When/What-for step, one stake grammar, a lightweight outing/challenge object, or a roster-carrying run-it-back. Every ruling that touches SP-3's ground either **reaffirms the noun doors and the engine-first wizard** (D94, D119(4), D136, D151(4), IOS-011/012, IOS-018), **explicitly declines the change** (D136 Q-29 keeps next-Saturday; D151 "explicitly NOT done: a foyer"; T1-13 "the ladder stays"), **names the gap as a future decision and stops** (D41 linked multi-season; D180 "tee-it-up starts the season … worth its own decision"; D205 "stays deferred"), or **was the near-miss and was reversed** (IOS-007 Option 2's three-field quick-start, superseded the same day by IOS-018 full parity). Several sub-findings ARE ruled and partly built; the design phase must cite them by number rather than re-discover them (list below).

## What I SAW — the rulings, and their build state at tip

| Ruling | What it says about SP-3's ground | Built? (file:line) |
|---|---|---|
| **D40** `spec/decision-log.md:1139-1170` (2026-07-20) | Invites gate on lock; "a member must never see the Pro's configuration tool"; the post-lock share sheet is "the single share moment". | BUILT both clients: `WizardScreen.swift:289-292` → `WizardLockShareSheet.swift:42-56`; setup checklist "Invite the crew · At lock" `StandingsPane.swift:36-38`. This ruling is the *cause* of "first invite surface is screen 5", not a fix. |
| **D97** `:3461-3483` | The wizard's pre-lock "Add golfers"/staged roster was deleted 2026-07-21 (`5cf7a80`) and the dead JS removed; inviting lives after lock. | BUILT (removed). `WizardState.swift:169` "D97: staging is gone". The only "who is in" the wizard ever asked was removed by ruling. |
| **D41** `:1172-1203` | Run it back = the wizard prefilled, a NEW league id, "continuity by convention"; true linked continuity "is a … build with its own decision" — deferred. | BUILT as V1: `LeaguelessDoors.swift:66-110` (`WizardScreen(runBack:)`), `StandingsPane.swift:140`, `SeasonCeremonyView.swift:67`. The roster-carrying version is un-ruled. |
| **D94** `:3328` · **D119(4)** `:4226` · **D136** `:4404-4407` · **D151(4)** `:4596` · **IOS-012** `docs/ios/DECISIONS.md:129-131` | The doors are object names — Join a league · Start a league · Start an event; members get one quiet "Start something…"; the phone's doors move to a `+` in the toolbar. | BUILT: web `index.html:11117-11128` (member link / league-less row), phone `LeaguelessDoors.swift:28-30`, `HomeView.swift:178-190` (`+` menu, five items). These rulings *constitute* the noun-door layer SP-3 objects to. |
| **D151(1)** `:4593-4594` | A one-time crew step after the card: code box first, find buddies, "start a league" quiet. "Explicitly NOT done: a foyer." | Web BUILT `index.html:15000-15040` (`cs_crew`, `#obCrewJoin`, `#obCrewFind`, `#obCrewStart` "Start a league instead" `:2876`). Phone NOT built — `OrientationScreen.swift:9-12` "the phone has no crew step, so the league-less doors sit here". Prod: no `crew_step_shown` event exists in `client_events` at all. |
| **D113** `:4161-4170` | Buy-in $0 at every layer AND "the buy-in row moves from `#wizDials` to above 'Use these defaults →' with the D39 fine line". | Server half BUILT (D196, `20260901220000`; prod `league_settings.buyin_cents` default `0` verified today). Placement half NOT built on either client: web `index.html:3516` (`#wizFastPath`) → `:3518-3521` buy-in row inside `#wizDials` `display:none`; phone `WizardSteps.swift:61` (fast path) → `:110-114` inside `dials` gated by `showDials`. **RULED-NOT-BUILT.** |
| **T1-13** (prior plan `plan/t1-organizer-setup-and-invite.md:322-328`) | Same as D113; and "M-005 (the ladder stays; a numeric field is a later polish)". | The $20 gap was seen and deferred as polish. `WizardDials.stakes = [0,25,50,75,100,150,200]` `WizardState.swift:17`. |
| **D206** `:5552-5560` | "The wizard's defaults are the defaults": `create_league` writes points · **squads2** · 3 months · $0; column defaults match. | BUILT + APPLIED: `20260902160000_the_defaults_the_wizard_shows.sql:60-62,141`; prod defaults today `buyin_cents=0 · season_format='points' · season_months=3 · structure='squads2'`. So squads2-for-one is a **ruled** default, while **D205** `:5542-5550` records both real seasons as two-person solo. |
| **D136 Q-29** `:4426-4432` | "First-tee default: UNCHANGED — next Saturday, 1–7 days out … The sketched '≥ 7 days out' is declined for now." | BUILT as ruled: `WizardState.swift:148-154` `defaultStart()` = next Saturday, never today. Ruled the opposite way from SP-3. |
| **D161 / D180** `:4801-4813`, `:5011-5035` | Code window = [lock, first tee); D180 adds a one-week floor **only when lock ≥ first tee**, and a manual "Roster's set" close. D180's parenthetical: "tee-it-up should also start the season, removing the date picker for new leagues — is real and worth its own decision." **D205** `:5546`: "D161's 'tee-it-up starts the season' stays deferred." | BUILT: `20260831190000_roster_door.sql`, `RosterDoor.of()`, `WizardState.swift:414` invite note. The date-less start is a NAMED open decision, not taken up through D221. |
| **D8** `:127-138` · **D48** `:1393-1410` · **D46** `:1333-1349` | Dials only inside Custom; "presets never mention them"; allowance is an internal constant, "no user-facing dial anywhere"; $0 is first-class and the default, "surfaced so it's seen, not buried". | Preset cards print "100% hcp · … / 95% hcp · … / 90% hcp · …" (`WizardState.swift:66-73`; `index.html:3500-3512`) — the presets' own lines are the dials' vocabulary, against D8's letter. The prior audit's M-010/M-008 (preset jargon; structure caption) were **never ruled** (reader-prior-audit-delta.md:179,181,358 → PA-033). |
| **IOS-007 Option 2** `docs/ios/DECISIONS.md:52-77` → **IOS-018** `:179-185` | 2026-08-27: the phone gets "quick-start league creation (name · preset · stake — three fields; the Customize dials stay desk)". The same day IOS-018 supersedes: "The phone builds everything the web has — including the wizard's dials and the lock". | The three-field door was ruled and reversed; the phone wizard is the web's three panes (`WizardScreen.swift:4-9`). |
| **D129 / D136 Q-27** `:4323-4331` | Payment path: `set_buy_in_terms`, note + due date, Home owe line. | BUILT (`contract.psv:258`; D129 "BUILT 2026-09-02"). Prod today: 0 of 13 leagues carry a note. |
| **D131** `:4343-4349` | Noun sweep: "stake = money on a live game only; the Pot tab's pride bets take D64's own noun, forfeits ('Post a forfeit')". | NOT built on the phone for the sheet title: `PotPane.swift:189` `SheetFrame("Post a stake", sub: "Pride, on the books — never money")`. **RULED-NOT-BUILT** — CJ-20/TM-05's "Post a stake · never money" is this. |
| **DL C-1 / C-3** (reader-data-layer.md:274,330) | Challenge and outing objects. | No ruling anywhere (grep "challenge a", "outing", "intent" in the log: nothing after Batch 3's tee-sheet note). Prod `scheduled_rounds` columns: `id, profile_id, play_on, course_label, note, created_at, tagged, tee_time, course_id, league_id` — no game, no stake. `declare_round` `contract.psv:124-125` likewise. |

## What I SAW — the code path at tip (confirms the mechanism the statement describes)

- Mint on a name: phone `WizardScreen.swift:52` (`nameSheet` while `leagueId == nil`) → `:252-268` `create()` → `svc.createLeague(name:)`; web `index.html:19935-19949` ("The name is the first commitment — ask for it, THEN create the row") → `:17293` `sb.rpc('create_league')`. Both cite "My Cup husks", i.e. the 2026-07-22 name gate (`108db9e`, recorded at D96 `:3440-3442`), **not D5**.
- Step 0 asks name + shows the Pro chip (`WizardSteps.swift:11-40`); step 1 presets + collapsed dials (`:56-76`); step 2 review rows + `inviteNote` + Lock (`:195-211`; rows `LeagueCopy.swift:131-155`). No step asks who / when-in-human-terms / what-for. Cancel at step 0 deletes the row (`WizardScreen.swift:296-303`).
- Lock → `WizardLockShare` → `WizardLockShareSheet` with `ShareLink` + "Add golfers" (`WizardLockShareSheet.swift:49-63`). First human-contact surface after Lock — as D40 ruled.
- Doors: `LeaguelessDoors.swift:28-30`; `HomeView.swift:178-190`; `OrientationScreen.swift:137-138`; `ClubhouseView.swift:136-155`.
- Live: `LiveSetupView.swift:24-35` course → foursome → game → nearby → "Tee off"; plan bridge `:64-75` today-only, course + group, no game.
- Money grammars: `WizardState.swift:393` "Buy-in · Per player · $0 = bragging rights"; `LiveModels.swift:71-78` four live labels; `PotPane.swift:189` "Post a stake"; `LeagueCopy.swift:139-143` "BUY-IN · None · bragging rights".

## Prod (read-only, 2026-09-04)

- Leagues: **setup 6 / 6 alone** (all non-sandbox), **season 6 (5 at $75)**, complete 1 (sandbox). 0 payment notes anywhere. 6 of 12 real leagues (13 with the sandbox) are a founder alone.
- **But** every setup league was created 2026-07-17 (4), 07-20 (1), 07-21 (1) — before the name gate (07-22), before D96's hero CTA (08-04), before D113/D151/D206. Since then: `league_create` 2 (07-27, 08-30), `league_created` 1 (08-30, the phone's retired name), `lock_ok` 1, `invite_open` 1, `lock_attempt` 1; the four 08-28 season leagues carry 5–9 members (seed-shaped). The post-remediation wizard has produced ~2 real creations and is **unmeasured**, not proven to produce leagues of one.
- `live_rounds`: 21 abandoned / 5 final. `forfeits`: 0. `crew_step_shown`: no rows (the web crew step has never fired for a real cold arrival; the phone has none).

## Refutation attempts that failed

1. "D119(4)/D151(4) already fixed the doors" — they re-ordered and quieted the same three nouns; the phone's `+` still lists five mixed items (IOS-022 item 1, `HomeView.swift:177`). Reaffirms, doesn't answer.
2. "D113 already fixed money" — the default is fixed (D196); the *placement* half of the same ruling is unbuilt on both clients, and the $20 gap was deferred in T1-13. Ruled-not-built.
3. "D206 already fixed the defaults" — it made the wizard's defaults the row's; it chose squads2 for a founder of one without re-examining fit (D205, same review, says the real seasons are solo). Ruled the other way.
4. "D180 fixed the dying code" — only for lock ≥ first tee; the default first tee (D136 Q-29) still puts the close 1–7 days out for a league locked before it (persona D's Friday-lock case).
5. "IOS-007 ruled a light door" — superseded by IOS-018 within the day.
6. "D41 handles run it back" — D41 itself files the roster-carrying version as a deferred decision.
7. "D151's crew step asks who you play with" — web only, after signup, once; not in creation; not on the phone.

## Corrections to the statement / evidence (holds regardless)

1. **Root cause cites "D5 (no husks)". Wrong entry.** D5 (`:87-99`) is "Receipts exist in schema, not in UI". The mint-on-name rule is the 2026-07-22 name gate (`108db9e`), recorded in D96 `:3440-3442` and in both clients' comments (`WizardScreen.swift:7-9`; `index.html:19935-19936`). Cite those.
2. **"$0 when money was the point" reads as if $0 were the wrong default.** $0 is ruled (D46, D113, D196) and correct. The defect is D113's **unbuilt placement** (the choice sits behind Customize on both clients: `index.html:3518-3521`; `WizardSteps.swift:110-114`) plus the ladder that skips $20 (T1-13 deferred the numeric field). Rephrase to "money is a choice the founder must go looking for; D113's 'seen, not buried' half is unbuilt".
3. **"a code that dies at first tee"** → "closes at first tee for a league locked before it (D161); D180's one-week floor applies only when lock ≥ first tee; D136 Q-29 explicitly kept the next-Saturday default." Persona D's case stands and is untouched by D180.
4. **"two squads for a roster of one"** → "D206 ratified squads2 as the minted default (built, applied) while D205 records both real seasons as solo" — the design phase overturns a ruling, not a stray constant.
5. **"half of all leagues ever created are a founder alone"** — true (6/12 real), but a **July cohort** predating every remediation ruling; post-remediation n≈2. State it as "the July wizard produced six leagues of one; the remediated wizard is unmeasured (2 creations, 1 lock, 1 invite since 07-27; no crew_step event ever)".
6. **Add D97 and D40 as the pair that removed "who is in" from before the lock** — a pre-mint Who step must be designed as *not an invite* (or amend D40), and must not resurrect D97's deleted staging machinery.
7. **Add IOS-007 Option 2 → IOS-018** — the canon's only light creation door was ruled and reversed; a new intent layer amends IOS-018's "everything the web has, in the web's own copy and behaviour".
8. **Add D180's and D205's named-but-deferred "tee-it-up starts the season / no date picker for new leagues"** — the design phase should take that decision up by name.
9. **Add D151 "explicitly NOT done: a foyer" / v23.55** — an intent layer must be a door/sheet, not a room, or amend that line.
10. **"Post a stake · never money" (PotPane.swift:189) is D131 ruled-not-built** — cite D131 rather than counting it as a fourth grammar; the live labels (`LiveModels.swift:71-78`) plus "Buy-in · Per player" are the un-ruled remainder.
11. **Reader-id nit:** "DL C-1/C-3" are section refs inside reader-data-layer.md (DL-01 → C-1 challenge; DL-29 → C-3 outing); cite DL-01 and DL-29, and cite the `scheduled_rounds` column list (no game, no stake) alongside `declare_round`.
12. **Evidence to add:** M-010 / M-008 (preset jargon; structure caption) are OPEN and never ruled (PA-033); D8's "presets never mention them" is violated by the preset lines themselves — the one place SP-3's principle-level claim is already on the record.
