# Verify · SP-7 · lens "already-ruled" — 2026-09-04, tip 3bba87e

**Verdict: HOLDS — but the statement is wrong in three places and under-cites the canon in two.** SP-7 is not "already fixed", and the rulings that exist point the OTHER way: the four-tab bar with a league room as one of its slots is ruled and built as ruled, three times over (D82, D93 "NAV UNCHANGED — and that is the point", D94, IOS-011, IOS-002 §2/§5, D203), and the freeze is exactly what makes the redesign a level-3 change needing a CONFLICT line (CC-01). Two things the statement asks for as NEW are already canon and were simply not built: the blueprint's **Compete** slot IS a list of "your containers … leagues + events, live status chips" with the league room as a drill-in (`ia-blueprint.html:192-202, 226`), and IOS-002 §5 rules that "the standings sentence LEADS" and that the switcher "lists leagues + events" — neither is what the phone shipped. Two evidence lines are false as written: "no recap" (D66 is built on the phone as a re-openable ceremony sheet) and "career shows 0 seasons" is a deliberate definition (`#7` in `20260725190000`) under D67, not a counting bug. The season-as-story remainder (archive, last season on Home, multi-season continuity) is genuinely UNRULED — D41 parked it to "its own decision" on 2026-07-20 and no entry has taken it; prod has never held a second season.

Everything below was read at tip `3bba87e`; prod counts are from read-only `supabase db query --linked` on 2026-09-04.

## 1 · The nav: ruled as a FREEZE, and built as ruled — this is why SP-7 holds and why it needs a level-3 entry

| Ruling | What it says | Built? |
|---|---|---|
| Hierarchy line `spec/decision-log.md:13-15` | "the IA blueprint (four questions; Home / Clubhouse / ⊕ / You — IA P5 retired the Crew tab …)" | — (the log's own frame) |
| D82 `:2856-2870` | ONE orientation teaching "the four places (Home / Clubhouse / ⊕ / You) and the two ways to play" | YES `OrientationScreen.swift:49-56` |
| D93 `:3307-3317` | "**NAV UNCHANGED — and that is the point.** The blueprint's four slots (Home · Clubhouse · ⊕ · You) stay exactly as shipped … The quadrant Home was considered and rejected … the four-tab bar P5 established still holds" | YES |
| D94 `:3328-3374` | rejects the four-quadrant Home (A) and two-quadrant (C); "the feed STAYS WHOLE" | YES (phone: IOS-012 moved the doors to `+`) |
| IOS-011 `docs/ios/DECISIONS.md:127` | "Four tabs Home · Clubhouse · ⊕ · You (D82 canon, unchanged) … Rejected: a fifth tab; the ⊕ as a menu page; the six-segment Clubhouse control" | YES `MainTabView.swift:128, 174, 205, 209` |
| IOS-002 §2 `:47` / §5 `:104-113` | "Tab order and names do not change"; "Root = **Standings** for the current league (switcher in the toolbar lists leagues + events, like `renderClubGroups`)" | root = standings YES (`RoomBits.swift:206`, D218 `:5660-5668`); the switcher half NO (§2 below) |
| D203 `:5511-5530` | the Clubhouse tab pages between league rooms; "IOS-002 §5 — the Clubhouse is *the* league room; the leagues are peers"; swiping writes `preferredLeague` | YES `ClubhouseView.swift:84-114`; CH-52 is D203 as ruled |

Nothing in D111–D221, DECISIONS.md or `plan/t1–t7` proposes renaming the tab, re-slotting it, a HISTORY destination or a season archive. The prior plan ruled *inside* the four tabs (D93's own words). `docs/ux-overhaul-2026-09-04/UX_AUDIT.md` exists at tip but is untracked (CC-32 stands as process).

**On "no entry records the rename" (CC-04): CONFIRMED.** No `### D` heading names Compete → Clubhouse (grep). The trail: the blueprint offers two names — "'Compete' is the clear choice; 'Cups' is the on-brand alternative" (`ia-blueprint.html:175`) — "Clubhouse" is neither; D11 (2026-07-15, `:171-176`) treats **Crew** as the nav tab and retires "clubhouse" *from copy*; D38 (2026-07-17, `:1071`) already says "the calendar tab was retired into the League Room (IA P2)"; D80 `:2748-2755` says IA P5 retired Crew; D82 (2026-07-27, `:2863`) is the first entry to write "Clubhouse" as a place name, as a given. The IA P1–P5 rulings live only in the frozen HTML.

## 2 · Already canon, NOT built — the statement asks for these as new; the design phase must CITE them instead

1. **The COMPETE list.** `ia-blueprint.html:192` "My competitions · leagues + events, live status chips ('2nd · 12 back')"; `:193` "Start something · League · Ryder · Bracket · Major — visible from day one"; `:194` "Join with a code [inline]"; `:196-200` "League Room [drill-in, segmented] Standings · Board · Schedule · Pot · Manage [Pro only]"; `:226` "Compete — Who am I against? — Your containers with a one-line status each, plus the door to start or join one. No content from inside a league leaks up." **IOS-002 §5 `:106` changed the tab's root from that list to one league's Standings** — in a founding artifact that cites the IA as "unchanged" (`:47`) — and D203 built on top of it. The web is closer to the blueprint than the phone: `renderClubGroups` (`index.html:11012-11025`) renders leagues AND events as one chip row with a status word each, above the room. The phone splits them: a toolbar `Menu` of leagues only (`ClubhouseView.swift:66-75`), `EventChips` above the room (`:87-89`; `EventChips.swift:24-43`), and a 5-pt dot row. **So "a COMPETE surface listing leagues, events and challenges as peers" is the blueprint restored, not a new IA proposal; the CONFLICT line is against IOS-002 §5 and D203, and the authority is the blueprint itself.** ("Challenges" alone is new.)
2. **"The standings sentence leads."** IOS-002 §5 `:106`: "The standings sentence leads; the climb; the table; the individual race as a segment." Built: hero → strip → PhaseHero → `NextCard` → on-the-line → climb → *then* `StoryLine` under the table head (`StandingsPane.swift:92-105`; `StandingsTableView.swift:22`), and the individual race as a third stacked list, not a segment (`:110-111`; CH-09). **CH-01 is a ruled-not-built defect against IOS-002 §5**, not only a brief complaint.
3. **Events in the switcher; a Ryder slot on Home.** IOS-002 §5 `:106` (switcher lists leagues + events) — not built on the phone (§2.1). IOS-004 §2 `docs/ios/IOS-004-opportunity-map.md:17` rules a Home slot "your match this week (only when a Ryder session is open …)" — not built: Home never reads `events` / `open_duels` (HM-03; `HomeView.swift` grep = invites banner only). IOS-022(7) `DECISIONS.md:245` ruled the Major flag-closed for v1 — built — and CJ-16's occasion cards selling it are a defect against IOS-022(7).
4. **Two segments should already be gone.** D93's tradeoff `:3323-3325`: "Clubhouse loses two of its six segments, which makes the room more clearly 'this league'." Built half: the routing (`setRoomSeg` `index.html:4493-4502`; `roomTabs` `LeagueRoomScreen.swift:129-141`) — the segments themselves stayed on both clients (`index.html:3642-3649`; `RoomPane.allCases` `RoomBits.swift:224`). CH-05/CH-06 are D93 half-built.
5. **D128** (rules lead the League pane, open, in sentences; every bylaw row a door) — ruled, not built on the phone (`LeaguePane.swift:100-110` `DisclosureGroup("League rules")`, per PA). **D132** ("THE PRO · GALEN · runs the league" chip) — ruled, not built (grep "runs the league" = 0; CH-10). **D139** (a settled season is the final table: no cut row, no IN/OUT, a CUP badge) — built on the web (`index.html:4780-4791`), NOT on the phone: `ClimbMath.note(teams:scenarios:)` (`StandingsMath.swift:410`) has no settled branch and still emits "TOP K ADVANCE TO THE CUP FINAL" (`:425-426`); `ClimbView.swift:35` prints it; persona B (a phone walk, `persona-B:18`) hit exactly this (`:157-160`). **D66(d)**'s second half — "re-viewable forever from the League Room **and the trophy case**" — the trophy case tile opens a round receipt, never the ceremony (`TrophyCaseView.swift:24-25`).

## 3 · Ruled AND built — evidence lines that are rulings working as designed (amend by number, not "fix")

| SP-7 claim | Ruling | Built | Note |
|---|---|---|---|
| The hero is a record (name · phase · code · eyebrow · span · THE PRO · Add golfers · Cancel) | IOS-018 (web parity, `DECISIONS.md:179-189`) + IOS-019 rules 1 and 3 (`:195, :197`) — `#hubHeader` ported as "the room's ONE hero"; D71 (cancel in any phase but complete); D161 (code hidden in setup) | YES `LeagueRoomScreen.swift:145-210` (the comment names IOS-019) | CH-10/CH-11/SV-11 attack IOS-018's port + IOS-019's shape; name both |
| The four-KPI strip | IOS-019 (1) `:195`: "The Clubhouse's 2×2 stat grid becomes one season strip inside the hero"; IOS-003 §4 `:148` forbids "a dashboard grid of KPI tiles (one hero, one lane)" | YES `StandingsPane.swift:173-244` ("Not a grid of tiles (IOS-003 §4)") | SV-12/CH-17: the ruled compromise is still four KPIs; the entry amends IOS-019(1) |
| "Six-segment control IOS-011 rejected" (WB-21) | IOS-011 rejected the segmented **widget**; IOS-019 (4) `:198` replaced it with a tab strip carrying the same six items | YES `CSTabStrip` | Correct WB-21: the widget was restyled, the count was never ruled down except by D93's tradeoff (§2.4) |
| Board/Schedule are doors dressed as tabs (CH-05) | D93 `:3300-3306` "the Clubhouse segments for schedule now route to the destination instead of toggling a pane" | YES both clients | a D93 design, half-built (§2.4) |
| Schedule pushes a global calendar (CH-04) | D93 (`view-schedule` outside the room; cross-league by design); D38 `:1068-1099` (the room's calendar scopes to the league — 2026-07-17, web) | phone: global (`ScheduleScreen.swift:22-29`), reads the *preferred* league at `:336` | D38's scoping is the ruling the phone's push ignores; T3-06/Q-12 gave the web a back link only |
| The room duplicates Home (CH-08) | D93/D94 "one authoritative surface per question"; D121 `:4242-4250` (the row "is a door, not a second standings"); D218 (every Home door lands on STANDINGS) | Home side built; the room never subtracted | the principle is ruled; the room violates it |
| The wrapped room under a gold card; "no recap" | **D66** (a) takeover once (b) per-person money (c) stored columns (d) re-viewable from the room; **D41** run it back; CC-49 says keep the ceremony's shape | **YES on the phone**: `LeagueRoomScreen.swift:93-97` (`ceremonyDue` → sheet once), `SeasonCeremonyView.swift:10-76` (champion, margin, rung, runner-up, king, "You're owed", the ledger line, Run it back), `StandingsPane.swift:127-142` (`wrappedHero`: "See how it ended" + "Run it back — Season 2"), `StandingsTableView.swift:23`; `LeaguelessDoors.swift:23-25` + `MainTabView.swift:381-387` (`RunItBackCard`); web `index.html:11088-11102, 13374` | **"No recap" is FALSE as written.** True: it is a sheet, not a place (no route, no deep link, not on Home, not in the trophy case); it opens over a room that keeps racing (D139 unbuilt on the phone; `NextCard` un-gated — no ruling) |
| ⊕ tab "Post" → cover "Golf" → "Play now" | D110 addendum `:4112-4119` v IOS-011 `:127` (contradiction, unreconciled) | YES | already verified in `verify-SP-5-already-ruled.md` §1 row 2; cite, don't re-argue |
| Orientation teaches the four places + long/short game | D82 `:2856-2870`; D117 (define the four nouns — PROPOSED, owner's call); D116(3) (invitees skip it — not built) | YES `OrientationScreen.swift:49-56` | CJ-26/CC-06 attack D82 as ruled |
| People is a flat list (YP-09) | D177 `:4932-4956` ruled exactly this order (requests lead · one search · buddies · requested · findable); D93 made buddies a place; D154 builds `recent_partners` for the live setup only | YES `PeopleScreen.swift:31-47` | league mates / recent partners / shared history: unruled beyond D177 |
| You is record-first (YP-07) | D177 ruled the spine: buddies door → "Your golf" → "Your seasons" (season strip, rivalries, "Every season"); "the Clubhouse is where you switch rooms" | YES `YouScreen.swift:86-190` | note: **"Every season" (`LeagueRecordView`, `LeagueRecord.swift:20-24`: "SEASON II · FINISHED 3RD OF 12") is the one HISTORY surface that exists** — a per-membership line, not an archive |
| Paging re-lenses Home (CH-52) | D203 `:5522` "Swiping writes `store.preferredLeague`, so the rest of the app follows you out of the room" | YES `ClubhouseView.swift:110-114` | as ruled |
| "Leagues & events · Played in" | D131 `:4347` + D208 `:5572-5580` | YES `Career.swift:25` | — |

## 4 · Genuinely UNRULED — where SP-7's structural weight sits

1. **A season with an arc (last season on Home, an archive, continuity).** D41 `:1192-1199` (2026-07-20): V1 "mints a NEW league id — continuity by *convention* … **True multi-season continuity** (same league id, running back-to-back history, 'defending champs', the grudge/margin line) is … its own decision." Never taken (grep: no later entry; D128 `:4317` lists "multi-season" among three teasers with a ⚑ to the owner, unanswered). `native_home` returns one season per membership — active/cup_final first, else latest (`20260902200000_the_hero_names_the_leader.sql:201-215`) — so a league in its second season would drop its first from every client read. `HomeMode.pool` (`Models.swift:294-302`) hides a wrapped league from Home whenever any other membership is live ("a wrapped league beside a live one is the Clubhouse's, not Home's") — a client rule recorded only in the D121 build note, no entry. **Prod 2026-09-04: 7 seasons, 1 complete, 0 leagues with a second season, 8 members of the one complete league, 1 event.** The arc cannot be checked against real data because the data model has never held one. UNRULED — holds.
2. **A HISTORY destination.** D67 (career record), D62/D61 (event lineage), D19 (rivalries), D177 ("Every season") give it data and rows; nothing rules a place. Unruled — holds.
3. **A wrapped room that is a record page** (CH-33): D139 covers the ladder only; nothing gates `NextCard` ("October is covered"), the strip's counting column or the clash card after `complete`. Unruled beyond D139.
4. **A league room whose identity is not the `leagues` row** — no ruling; every ruling since IOS-002 §5 assumes it.

## 5 · Claims wrong as written

1. **"no recap"** → see §3: D66 built as a sheet; amend to "the recap is a one-time sheet re-openable from the room, not a place".
2. **"`career_record.seasons_done` counts paid seasons so every career shows 0" (DL-14)** — the migration says why: "#7: the denominator under the earnings total is the count of seasons that actually paid this profile — not every complete season they were in" (`20260725190000_payout_penny_fixes.sql:154-158`), under D67(b) "an EXACT sum of recorded payouts, never a re-derivation". It is a label problem (seasons *paid* under a word that reads *played*), not a counting bug; prod: `season_payouts` = 0 rows, `trophies` = 8, so every earnings line is $0 and its denominator 0 — DL-15's "before the ledger" caveat is the real finding.
3. **"the panes are the tables" (root cause, UI)** — the panes are the blueprint's own "League Room [drill-in, segmented] Standings · Board · Schedule · Pot · Manage" (`ia-blueprint.html:196-200`, level 3) + IA P2's calendar (D38 `:1071`) + the photos arc's Album; they coincide with tables. Retiring them needs a CONFLICT line against the blueprint §03 as much as against the DB.
4. **"a COMPETE surface listing leagues, events … as peers" (why_structural, as a new need)** — already the blueprint's Compete (`:192-194, 226`); the phone departed at IOS-002 §5; the web's `renderClubGroups` already does it.
5. **WB-21's "six-segment control IOS-011 explicitly rejected"** — the widget, not the count (IOS-019(4)).
6. **"events absent from Home (HM-03)"** — ruled (IOS-004 §2 `:17`, a Ryder slot when a session is open) and unbuilt; say so.

## 6 · Corrections to the statement and evidence (for the synthesis)

1. Split the evidence into (a) ruled-and-built freezes to CONFLICT against — D82, D93 (`:3307-3317`), D94, IOS-011, IOS-002 §2 `:47` and §5 `:106`, D203, IOS-018, IOS-019 (1)(3)(4), D177 (You/People order), D110 addendum; (b) ruled-and-unbuilt items the redesign can cite as executed — blueprint `:192-202, 226` (the Compete list), IOS-002 §5 (sentence leads; events in the switcher), IOS-004 §2 `:17` (Ryder on Home), D93 `:3323` (two segments go), D38 (a league-scoped calendar), D128, D132, D139 on the phone, D66(d)'s trophy-case door; (c) unruled — the season arc/archive/continuity (D41 parked), a HISTORY place, the wrapped room as a record, the room's identity.
2. Replace "no recap, archive or last season on Home" with: "the recap is D66's sheet (built, re-openable from the room only); there is no archive and no last-season line because D41 parked multi-season continuity to 'its own decision' and none was taken; `HomeMode.pool` hides a wrapped league from Home beside a live one (`Models.swift:294-302`, no entry)."
3. Add the prod frame: 7 seasons / 1 complete / 0 leagues with two seasons / 0 payouts / 8 trophies / 1 event — "season repeat" has never happened in the data, so the cost line is a forecast, not a measurement.
4. `root_cause` IA line: "the blueprint's four questions were built as four containers … by D82/D93/D94/IOS-011/IOS-002 §5" → add "and IOS-002 §5 replaced the blueprint's Compete LIST with one league's Standings as the tab's root, without a CONFLICT line; D203 paged that choice."
5. `why_structural`: the CONFLICT list should name the blueprint §02–§04 (the Compete slot and the League Room segments), IA P2/D38, D93, D94, D203, IOS-002 §2/§5, IOS-011, IOS-019(1)(4), D177, D66/D41/CC-49 (the ceremony's shape — keep), D139; and the entry should state which rulings it *restores* (§2 above) so the log does not read them as new.
6. Keep the persona lines; note persona B's walk is the phone (`persona-B:18`) and that her "TOP 2 ADVANCE" is D139-unbuilt-on-phone, her "Post 2 more rounds" is unruled, and persona E's "SQUADS LOCKED" is D120-retired copy still printing (`LeagueCopy.swift:271`; SP-8).
7. DL-14 line → "seasons_done is seasons PAID by design (`20260725190000:154-158`, D67(b)); with 0 payouts it reads 0 for everyone; split seasons played from seasons paid (DL-15)".

## 7 · What I did not do
No device run; `ClimbMath.note` and `NextCard` gating read from code only; the web's `#room-league` past-seasons absence taken from the CH reader; the IA P1–P5 HTML history not diffed (only the frozen blueprint read); persona quotes checked by grep, not re-walked.
