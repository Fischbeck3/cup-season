# Verify · SP-2 · lens "already-ruled" — 2026-09-04, tip 3bba87e

**Verdict: HOLDS.** SP-2 is neither fixed nor ruled as a whole. But the evidence list mixes three different things and the statement must be amended to separate them: (a) axioms the decision log RULED and BUILT that the brief now attacks (these are the root cause, and the design phase must amend them by number); (b) fixes the log RULED that were built on the web and NEVER built on the phone (the shipping client); (c) items nothing has ruled. Two evidence lines currently read as defects but are deliberate rulings built as ruled (D130's one-producer note; D203's pager coupling) and one is a ruling the statement contradicts without saying so (D82/IOS-011/D177 put "post a round" on the ⊕, not on Home).

## 1 · The axiom is ruled and built — nothing in D111–D221 or IOS-0xx reverses it

| Claim in SP-2 | Ruling | Built? | Evidence |
|---|---|---|---|
| One hero over one membership | D81 `spec/decision-log.md:2803-2855` ("one hero slot + one lane, dispatched on lifecycle"); D94 `:3328-3354` (keeps one lane, rejects the quadrant Home); D121 `:4242-4251` ("keep the single hero (D81/D94 stand)") | yes (phone) | `Models.swift:305-318` `HomeMode.of` picks `pool.first(where: preferredLeague) ?? pool.first` |
| Role × stage matrix | D119 `:4222-4231` | yes, the words only (phone) | `HomeView.swift:956-965` `line` switches on `m.isPro` and `m.phase` |
| Lens = the open league | D123 `:4262-4271` rec (1) "one-league surfaces … use the viewer's open league (D81/D94)"; D130 build note `:4334` "the hero, the lead card and the circle read ONE membership producer (`HomeMode.of(...).membership`)" | yes | `HomeView.swift:39,45,71,79,96` all read `mode.membership` |
| Second league = a compact row | D121 `:4246` ("one compact 44-px row … quiet"); plan T3-P4 / D114 `plan/t3-member-home-and-roles.md:99-109,134` | yes (phone); "the web row is still owed" (D121 header, `:4243`) | `HomeView.swift:1015-1069`; no `HomeLeagueRow` equivalent in `index.html` |

These are the rulings O-08/O-09 (`reader-canon-and-constraints.md:181-182`) already name. SP-2's `root_cause` cites them correctly. Verdict on this half: RULED, BUILT, and exactly the thing the brief overrides — holds at level 3.

## 2 · Ruled, built on the WEB, not built on the PHONE (the shipping client)

| SP-2 evidence | Ruling | Web | Phone |
|---|---|---|---|
| Leagueless hero inert (HM-02) | D81 `:2822-2824` — the league-less hero "climbs a ladder of next-unmet-facts", each rung a move | BUILT: `index.html:11445-11474` rung 7 "Post your first round" (`:11449`), rung 6 "Find your buddies" / "Post a round" (`:11461`), rung 5 "Start a league" | NOT BUILT: `HomeView.swift:810-812` the leagueless card is not a `Button`; `LeaguelessDoors` is rendered in Orientation (`OrientationScreen.swift:138`), the Clubhouse (`ClubhouseView.swift:140`) and Settings (`CardAndSettingsScreen.swift:412,529,633`) — never in `HomeView.swift` (grep: no match) |
| Forming/preseason → empty table (HM-04, HM-05) | D119 rec (1) `:4226` "every cell with a move the viewer can make"; D136 `:4404-4407` "'Plan a round' leads before first tee, 'Post a practice round — it builds your number' second"; plan T3-P2 `plan/t3-member-home-and-roles.md:132` ("HomeHero gains a CTA slot") | BUILT: `index.html:11388-11434` `nextStep` (Pro: name / lock / share / draw / plan; member: plan) + `subStep` "Post a round" (`:11409-11412,11431-11432`) | NOT BUILT: `HomeHero` has no CTA slot; the only door is the whole card → `push(.league(lid))` → STANDINGS (`HomeView.swift:803`) with "See the table →" in every membership mode (`:839-842`). **And D218 `:5660-5669` (2026-09-02) ruled exactly that — "the whole hero is a NavigationLink landing on the Clubhouse STANDINGS pane, with a visible See the table →" — with no stage qualifier.** So HM-05 is D218 as built; D218 silently conflicts with D119(1)/D136 on the phone and neither entry names the other. |
| Wrapped "Run it back." with no door (HM-07) | D81 `:2826` "complete = the record (position + purse, champagne) with run-it-back"; D41 `:1175-1200` (the run-it-back card on Home); D66 `:2063` | BUILT: `index.html:11216-11255` — "Season recap →" (`:11251`) + "Run it back — Season 2" (`:11252-11253`) | NOT BUILT on the hero: `HomeView.swift:981` is a sentence; the card opens STANDINGS (D218). The run-back path exists on the phone — `presenter.runBack` sheet `MainTabView.swift:381-387`, reached only from the Clubhouse (`ClubhouseView.swift:172`) |
| Solo champion never told (HM-06) | D137 `:4444` — the crown reads `seasons.champion_*`, both structures | BUILT: `index.html:11242-11246` (`solo ? S.champion_member_id : S.champion_squad_id`) | NOT BUILT: `HomeView.swift:869,980` test `champion_squad_id` only; `Me.Season.champion_member_id` exists (`Models.swift:50`) and is never compared. A bug, not structural — should move out of SP-2's evidence into a defect list. |
| Events never on Home (HM-03) | IOS-008 `docs/ios/DECISIONS.md:81-99`, option (2) PROCEEDING: "Home leads with the live-round banner, then the standing, then **'your match this week' only when a Ryder session is open**, then 'your next round'" | n/a | NOT BUILT: `open_duels`/`events` decoded (`Models.swift:233-234`) and read only by `PushDev.swift:40`, `OrientationScreen.swift:90`, `YouRepository.swift:141` — no Home reader |
| No ME movement / cause (HM-16, DL-06) | D81's own CONFLICT resolution `:2837-2845`: "**the standing is a verb** — the hero always renders the MOVE **and its cause** ('Danny's 78 Saturday dropped him behind you'), and on quiet days points forward ('beat your number by 3 Saturday and you take 2nd')" | partial (`heroMyRung` move chip; no cause) | partial: the chip is ▲/▼/"— held" from `prev_rank` (`HomeView.swift:928-934`); no cause, no forward sentence. D130 `homeStakeLine()` `:4333-4342` — the forward-pointing line — "remains unbuilt" per its own note (`:4334`) |
| Second league on the web (WB-02) | D121 `:4243` "The web row is still owed" | NOT BUILT | built |

Verdict on this half: RULED, NOT BUILT on the phone. It still holds as a problem, but the design phase must cite D81 (ladder + verb-with-cause), D119(1)/D136 (member verbs), D41/D81 (run-it-back on the wrapped hero), D137 (solo crown), IOS-008(2) (the duel slot), D130 (stake line) and plan T3-P2 — and must resolve D218 against D119 explicitly.

## 3 · Ruled deliberately THE OTHER WAY and built as ruled — the statement reads them as defects

1. **A wrapped league beside a live one disappears (HM-26).** RULED: D130 build note `:4334` — "one rule for the phone: the hero, the lead card and the circle read ONE membership producer … so a stored league that has wrapped falls to the live one everywhere at once". BUILT: `Models.swift:292-303` (`pool` comment: "a wrapped league beside a live one is the Clubhouse's, not Home's"). Not a bug; a ruling to amend.
2. **The lens follows the Clubhouse pager (HM-38, CH-52).** RULED: D203 `:5511-5531` — "Swiping writes `store.preferredLeague`, so the rest of the app follows you out of the room." BUILT: `ClubhouseView.swift:110-113`; `MainTabView.swift:174`. `SessionStore.swift:29-31` defines `preferredLeague` as "The league Home leads with (the web's `cs_last_league`)". Not a bug; a ruling to amend (the coupling is D203's stated intent).
3. **"The one 'what can I do' verb — post a round — is not on the page" (HM-12).** RULED THE OTHER WAY, three times: D82 `:2856-2870` (the four places incl. ⊕); IOS-011 `DECISIONS.md:125-127` ("⊕ is a full-screen cover opening **on the post form**"); IOS-012 `:129-131` (the doors leave Home's lane for the `+`); D177 `:4932-4956` ("**'Post a round' removed** … competing with a ⊕ that is permanently on screen an inch below it"). The post verb is on the tab bar by ruling. The hero carries a post CTA only pre-tee (D119/D136 sub-step, web `:11409-11412`), not in season. Side note for SP-5, not SP-2: the phone's ⊕ opens a three-door cover (`MainTabView.swift:336-339`, `PostCoverView`), not "the post form" IOS-011 ruled — a drift from a ruling.
4. **11-pt row under the league I'm losing (SV-28, O-09).** D121 ruled it "quiet" by design; "2nd of 2" is D130's own "the hero line prints the totals at n = 2" (`:4334`). Ruled and built; holds only as the brief overriding D121.

## 4 · Not ruled anywhere (the true remainder)

- `native_home` carrying `last_round_on`, a streak, `week_no`, `next_up` for rank ≥ 3, `last_season` (DL-05/07/27/31) — no D-entry; the readers' B-3/B-6 are proposals. `20260902200000:144-148` carries `index_current` and `rounds_count` only; no week producer (only `week_no` in the snapshot subquery `:324,336`).
- A WHO layer beyond leader/runner-up (HM-32): D130 named the leader and runner-up (built, v2); nothing rules the row above me.
- A ME strip (HM-31): nothing rules one; D81's verb-with-cause is the nearest ruling and is unbuilt.
- Forming/preseason/wrapped/event/leagueless as NOW items rather than heroes; composing Home across memberships from one ranked read — unruled (this is the level-3 change O-08/O-09 describe).

## 5 · Corrections to the statement and evidence

1. Split the evidence into the three classes above. As written, "every mode but season is a dead end" attributes to the IA what is, on the phone, largely D81/D119/D136/D41/IOS-008 ruled-and-unbuilt plus D218 built-over-D119. The structural claim survives only as the level-3 point: even with every ruled CTA built, the hero is still one membership chosen by `preferredLeague`, dispatched by phase, and the brief's ME → NOW is not reachable inside D81/D94/D121/D123.
2. Replace "wrapped league beside a live one disappears" (bug framing) with "D130's 2026-09-02 one-producer rule (`:4334`, `Models.swift:292-303`) makes the wrap the Clubhouse's; amend D130's note".
3. Replace "the lens follows the Clubhouse pager" (bug framing) with "D203 ruled the coupling (`:5511-5531`); amend D203 or re-scope `preferredLeague` to navigation only (CC-13)".
4. Restate the post-verb line: "the post verb is on the ⊕ by D82/IOS-011/D177; Home's hero carries it only pre-tee by D119/D136, and the phone never built that sub-step (T3-P2)". Do not claim it is absent from the product's first screen; claim it is absent from the page's story.
5. Add the D218-vs-D119 conflict as a named finding: D218's "whole hero → STANDINGS" (2026-09-02) overrode the D119 phone CTA plan (T3-P2) without citing it; HM-04/HM-05/HM-07 are D218 as built.
6. Move HM-06 (solo champion) out of SP-2 into a phone-defect list — D137 is built on the web and the phone compares the wrong id (`HomeView.swift:869,980` vs `Models.swift:50`).
7. Add IOS-008(2) to `root_cause` as the ruled-but-unbuilt event/duel slot; add D81 `:2837-2845` (verb + cause + forward sentence) as the ruled-but-unbuilt ME layer; keep D130's `homeStakeLine()` as ruled-unbuilt.
8. `why_structural` should state the closure test explicitly: building every ruled CTA (T3-P2, D41 on the phone hero, D137 on the phone, IOS-008's duel slot, D130's stake line, D121's web row) leaves a Home that is still one phase-dispatched membership; that is why the level-3 amendment is required and why the fixes above are not a substitute for it.

## 6 · Prod frame (read-only, 2026-09-04)
`leagues`: 1 complete · 6 season · 6 setup — so the wrapped/forming heroes SP-2 describes are the modes six-plus real leagues open on today; the D119/D136 member verbs and the D81 ladder are unbuilt on the client those leagues use.
