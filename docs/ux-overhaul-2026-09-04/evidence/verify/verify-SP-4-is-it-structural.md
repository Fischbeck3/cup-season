# Verify · SP-4 · lens "is-it-structural" · tip 3bba87e · 2026-09-04

**Verdict: DOES NOT HOLD as stated.** SP-4 is filed at the MECHANICS level ("'I want to beat Jake' has no object … no button labelled 'Challenge Jake' can go on the Tour Card until something exists for it to create"). The object exists, is ruled at level 4, is built, and is what both real seasons in prod are. A single new door with defaults — one screen over three existing RPCs — answers all five brief questions for the two personas SP-4 genuinely belongs to (F and B). What survives is (a) an IA absence already owned by SP-3/O-04 (no intent door from a person to that object), (b) one ruled-and-unbuilt small mechanic (D21's Callout, for the one-round async case), (c) a list of single-screen / single-guard defects, and (d) two pending level-4 rulings that change what a record counts, not the IA. None of this needs a mental-model change beyond what SP-3 already asks for.

## 1 · The refutation — the object exists (SAW)

| Claim in SP-4 | What exists at tip | Evidence |
|---|---|---|
| "the cheapest competition — two friends, one week — cannot be created" | A two-golfer solo league IS a season, by ruling. `create_league(p_name)` → `lock_league(p_structure='solo', p_buyin_cents=0, p_starts_on, p_ends_on)` → `add_friend_to_league(jake)`. Three existing RPCs, no new table. | D205 `spec/decision-log.md:5542-5551` (MECHANIC level, 2026-09-01, RULED): "**a solo league is a season at two golfers** … the engine already plays a solo league at two: `lock_league` sends solo straight to `season` at one member … **the clash pairs two, the Final seats two** … Two of the two real seasons are two-person solo leagues." |
| "one week" | First tee may be today; the window is the dates. Server: `v_starts := coalesce(p_starts_on, current_date)`; `v_ends := p_ends_on` when given; months are DERIVED (`greatest(1, …)`). CHECK is `season_months >= 1`. Phone ladder starts at 2 weeks; `canCup` only at ≥ 6, so a short season is a plain points table. | `20260902163000_the_first_tee_horn.sql:156-168`; `20260830180000_cap_default_and_season_window.sql:31-34`; D143 `:4483+` (BUILT); `WizardState.swift:18` `durs = [2,3,4,5,…]`, `:247` `canCup = cup && d.durWeeks >= 6`, `:164` "a 2-week pilot says 1". Prod: shortest real season 42 days. |
| "a friendship … changes nothing competitive" | A buddy is the ONLY person the Pro can seat directly, without a code or a wait: `add_friend_to_league` requires `friendships.status='accepted'`, then `_join_gate(p_league, true)` (open from lock to the halfway turn). | `20260830300000_join_window.sql` (`add_friend_to_league`: "Not golf buddies yet — send a request first"; "D161 · the Pro vouches, so this door stays open to the halfway turn"); `20260831190000_roster_door.sql` `_join_gate`. So friendships change a FIFTH thing SP-4's first evidence line omits: who can be put into a competition in one tap. |
| "no head-to-head" / "who am I competing with" | At n=2 the weekly clash is the pair by construction and the product SAYS so, on the board and on Home: "It's the two of you — every week is the clash." then "You v Jake. Best round of the week takes it." with the christened name as the eyebrow. | D207 `:5560+`; `20260902170000_the_clash_stops_repeating_itself.sql:195-200`; `HomeLead.swift:68,85,205-206,196`; `HomeHeroCopy.swift:33` (two-person hero). Prod: 2 two-member leagues, both `phase='season'`; 8 clashes. |
| "no stake between two golfers" | Three exist for a pair: (1) `create_forfeit(p_league, p_other=jake)` — both are "in the crew" the moment the league exists (`20260724120000_forfeit_ledger.sql:53-70`); (2) a live Match Play round's `$ per side` (L-40, `LiveModels.swift:71-78`, `start_live_round p_config`); (3) the buy-in at n=2 (`lock_league p_buyin_cents`, ledger line L-09). What is true: none is reachable from a PERSON, and (1) needs the shared league first. | as cited |
| "no rematch object" (competition repeat) | Run it back (D41; `StandingsPane.swift:140`, `ClubhouseView.swift:172`) for a league; the Ryder/Major lineage rematch tap (D61/D62; `RyderRoomView.swift:70`, `MajorRoomView.swift:121`, `index.html:14208,14430`). Missing only for a PAIR outside a league — which the door above creates. | as cited |
| "receipts, a link, a push" | `week_clashes.a_best/b_best` + `rivalry_weeks` (§16 receipts); the league code / `share_info` (the link); push kinds `invite` and `system` exist (`push/index.ts:100-103`). | as cited |

**The constructed fix (single screen + defaults, nothing below the UI):** a "Challenge <buddy>" door on the buddy row / Tour Card / rivalry row / feed item that opens ONE sheet ("Jake v Casey · 4 weeks from Saturday · best round of the week takes it · [add a forfeit]") and on Create runs `create_league` → `lock_league(solo, $0, dates)` → `add_friend_to_league`. The five questions, persona F, the morning after: Q1 "Season live · Week 1 of 4 / It's the two of you — every week is the clash." (D207 sentence, `HomeLead.swift:205`); Q2 "You v Tash. Best round of the week takes it." (`:206`) + the last-call day (D176); Q3 post Sunday's round / put it on the tee sheet; Q4 Tash (the clash card, `home_clash` for the two named); Q5 "The clash · closes today" (`clashEyebrow`, `:195-199`). Persona B: the same door on Priya's rivalry row; `my_rivalries` joins ALL shared seasons (`20260716210000:76-83`), so "2–0" carries into the new league. This is exactly canon O-04's prescription for this intent: "the intent doors, each resolving to an existing object (league / event / declared round / callout / stake) — **no new object types**" (`reader-canon-and-constraints.md` O-04) and CC-11's: "'I want to beat Jake' resolves to D21's callout or a named rivalry (D19) — never a new mechanic."

## 2 · What survives, and at what level

1. **IA (SP-3's):** no door from a person to any competition. `TourCardSheet.swift:134,176` (Mute; Add buddy) and `RivalriesSection.swift:26` (→ Tour Card) confirmed; no "challenge"/"rematch" verb for a pair on either client (grep: only Ryder/Major lineage rematches and the standings "waiting on a challenger" line, `StandingsMath.swift:110`). This IS the brief's "I want to beat Jake" intent door — SP-3 owns it; SP-4 should merge into it.
2. **Ruled, unbuilt, small mechanic:** the one-round ASYNC case ("make Sunday count" against a buddy not on your tee) is D21's Callout — ruled level 4 with "CONFLICT check: none", "a settle engine (small) + one more pre-round affordance", three ⚑ for the Pro (`decision-log.md:345-369`). Build status is NOT "unknown" (CC-11): **0 hits** for `callout` in migrations, functions, `contract.psv`, `index.html`, or `apps/ios` outside CSS/font names. The same-tee case exists (L-40 live match). This is SP-2-class (b): ruled, not built.
3. **Single-guard / single-screen defects (not structural):**
   - RSVP for a buddy not tagged: D69 (`:2180+`) deliberately restricted the WRITE to owner-or-tagged ("an RSVP from someone you didn't invite is noise"); `ScheduledRoundSheet.swift:77-78`, `ScheduleModels.swift:511` `canRsvp = mine || taggedMe`. The brief contradicts D69; amending it is one guard in `set_round_rsvp` + one `canRsvp`.
   - Naming a rivalry requires history: D19's own rule (`20260716210000:47-50`), one guard.
   - Tour Card drops `courses`/`shared_courses` (`TourCard.swift:93-126` has no key for them); rivalry row opens the card (YP-04); name lost on the chip (YP-05); the room never reads `my_rivalries` (CH-54 `reader-clubhouse.md:445`). Each is one view.
   - A buddies-by-index board is a client sort over `my_friends().index_current` (`20260715210000:85`; DL A-3) — one screen, pending CC-10/C-9.
4. **Pending level-4 rulings that do NOT change the IA:** C-2 (what a casual meeting is — so `head_to_head` can count `live_rounds.game_result` and same-day rounds; a new RPC, B-2) and C-9/CC-10 (whether friends are ranked at all). Both are "what does the record count", not "what is the product".

## 3 · Immutable-law check
No immutable law blocks either fix. The draft's proposed `challenges` object with "an optional forfeit/stake, a push kind" must respect: T-02 (stake = money on a live game only; pride = forfeits — the draft's word "stake" on a challenge would create the fourth stakes object CC-11 warns against), L-20 (one push per condition, no nag on an unanswered challenge), L-21/D216 (never manufacture stakes — user-initiated only), L-40/D21 ("worth zero points — it never touches scoring"; a challenge beside the engine's clash inside a league week must not score twice — C-1(b), D52/D108/D207's one spotlight per league-week), L-12 (`add_friend_to_league` is D161's Pro-vouch door; a code join still passes the covenant). The draft's `why_structural` conflicts with RULED entries rather than laws: D21 (the challenge already ruled), D205 (the two-golfer season), D64 (forfeit parties are "in the crew"), D19 (naming needs history), D69 (RSVP is for the invited). All are amendable at level 4; none is on the wall.

## 4 · Corrections to the statement and evidence
1. Re-level SP-4 from "mechanics" to "IA (door) + ruled-unbuilt (D21) + defects", or merge it into SP-3 as the "I want to beat Jake" intent door resolving to the D205 two-golfer league (O-04, CC-11). Delete "no object", "cannot be created", "no rematch object", "no stake between two golfers" as absolutes; replace with "no door from a person; the pair's stake needs the shared league first; the one-round async case is D21, unbuilt".
2. Evidence line 1 must add the fifth thing friendships change: `add_friend_to_league` (buddies only, no code, no wait — `20260830300000`).
3. "D21's Callout ruled, build status unknown (CC-11)" → "ruled, UNBUILT (0 code hits at tip)".
4. Replace "Needs a challenges object … level-4 entries plus migrations" with: (a) the door (SP-3), (b) close D21's three ⚑ and build it (new table + RPC + one push kind — a build, not a design question), (c) amend D69 and D19's guard, (d) log C-2 and C-9. Cite D21's own CONFLICT clause ("if a future version ever wants callouts to score, that is a NEW level-4 decision").
5. Move persona C's quote ("one round of X puts you past Dre") to SP-2 — it is the in-league projection, D81's verb-with-cause, computable client-side today (DL A "Win this week and you move to 2nd"). Move persona A's "fifteen taps to the skins scorer" to SP-5/O-07 (live-door discoverability, D134's six placements). SP-4's personas are F and B only.
6. "the calendar hides a buddy's round Home just showed (persona F)" — NOT reproduced from code: `ScheduleModels.swift:396` builds `crew` with `is_friend == true || shared_league == true`, and `my_schedule` returns friends' rounds (`20260718192400:236-241`). Keep as an unreproduced persona observation or drop.
7. Prod frame (read-only, 2026-09-04): friendships 24 rows / 12 accepted; buddies sharing no league 6 of 12 (confirmed); `rivalry_names` 0; `week_clashes` 8 across 6 seasons (not only the two-member ones), 0 winners; `forfeits` 0; `game_results` 0; `event_duels` 9; `live_rounds` final 5, 2 with `game_result`; two-member leagues 2, both in season; shortest real season 42 days; `scheduled_rounds` 4. "65 rivalry pairs across 20 profiles" is reader-reported, not recounted here.
8. Keep DL-09's "three implementations of one rule" as a consolidation debt (B-2), not as structural evidence.
9. `root_cause` should say the Ryder duel engine is NOT the nearest parent of "beat Jake" — the D205 two-golfer league with D52/D207's clash already is; D21 is the ruled shape for the one-round case.
