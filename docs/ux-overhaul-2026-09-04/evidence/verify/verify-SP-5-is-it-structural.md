# Verify · SP-5 · lens "is-it-structural"

*Adversarial verifier, 2026-09-04. Repo `/Users/fischbeck3/cup-season` at tip `3bba87e`. Read-only. SAW = read at the cited line; PROD = `supabase db query --linked` (read-only); INFER marked.*

## Verdict

**holds = TRUE — narrowly.** Five of the statement's seven claims dissolve with a default change, a copy change, or one screen's change each, and I could construct that fix without breaking any immutable law. What survives is one claim — the quick post captures no "who I played with" — and the funnel rung that hangs off it. For the league-less golfer (personas F and A; 14 of 39 prod profiles) no copy, default or single-screen change answers the brief's WHO question, and therefore its NEXT ("challenge Ravi", "make Sunday count"), because the round record has no place for the partner, the only partner sources are live seats and scan claims (0 in prod), and head-to-head outside a league does not exist. That residual is a mechanic-level change (the post's data shape plus what a tagged casual meeting means) with an IA consequence (the act after the ceremony) — structural by the brief's own definition (the user model "who I play with" separated from the data model "a live seat").

The statement is mis-levelled and padded: its hierarchy label says IA (where the composer lives), but where the composer lives is a UI-level default (O-07). The structural core is MECHANIC → IA. The other five claims should be split out as UI fixes that ride the same path.

## The refutation attempt — the maximal non-structural fix

| # | Claim in the statement | SAW at tip | Non-structural fix that dissolves it | Canon check |
|---|---|---|---|---|
| A | The ⊕ always presents the cover; Post is the quiet second row | `MainTabView.swift:338` sets `postOnComposer = false; showPost = true`; `PostCoverView.swift:95-102` ember hero "Play now — score the group", quiet row "Post a round — after you play"; `:5-7` "Post is the 90% case" | **Default change:** flip `:338` to `postOnComposer = true` (the pre-D110-addendum rule, FR-17's fix). The composer already carries "Play now" as an ember header link with the dot (`PostRoundScreen.swift:119-131`) | D110 addendum is UI level and overridable (O-07). L-25 (ember = live) kept. L-40's "first-screen sighting" of the free door kept by the header link. Needs the O-07 entry + an IOS-0xx line superseding IOS-011's ⊕ clause (CC-33). **Not structural.** |
| B | The composer asks course, tee, rating/slope, 18/9 and two nines before it accepts a score; cannot post a hand-typed course; no total-only entry | Order hero → where → card (`PostRoundScreen.swift:90-95`); nines only (`:257-259`); preview needs f9/b9 (`PostCard.swift:238-261`); blank rating → `ratingValue` 0 → nonsense vs, Post stays enabled (`PostRoundModel.swift:201`); the direct insert (`PostService.swift:84`) is refused by `rounds_rating_sane` 25–90 / `rounds_slope_sane` 55–155 (`20260718173100:58-61`). **Hand-typed course DOES post:** `PostCourseSearchField.swift:22` "Search a course, or type your own"; `:33` "No match — type the course, rating and slope by hand."; `PostCard.swift:296-303` sends `course_label`. PROD: 155 of 207 quick rounds have `api_course_id is null` | **Single-screen:** a Score box first with "split into nines" as the fold (amends D34 "two boxes" at UI level — D34's purpose was hiding the grid, so in its spirit; IOS-020's "two boxes default" is also UI); mirror Q-22 (`index.html:6989-6999`) so a blank rating disables Post with a sentence. **Default (mechanic-level entry):** `rounds.rating`/`slope` are NOT NULL (`baseline:1254-1255`) and `score_round` derives the differential from them (`20260716100000:75-78`), so a course-only post needs a server-side default (72/113, badged). Precedent: the live twin already defaults 72/113 (`LiveModels.swift:201-202`) and D107 rounds post with them | L-01 satisfiable: the receipt must print "72.0 / 113 · not rated" — a defaulted number that shows its provenance. L-13/L-15 untouched. The default is a mechanic-level entry (spec §5/§16), allowed. **Not structural.** |
| C | No "who did you play with"; the post captures no relationship | `PostCard.swift:47-55`, `PostPayload :278-289` no partner; `rounds` has no partner column; `last_round_with` reads live seats + scan claims (`20260724110000:24-52`), `scheduled_rounds.tagged` excluded; the only partner path is the scan's claim links (`PostScanSheets.swift:29, 40-53`; `PostRoundModel.swift:229-238`). PROD: `scan_claims` = 0; live rounds 5 final / 21 abandoned. D18 (`:287-291`) already anticipated "if the round's foursome was tagged, the sheet offers a one-tap partner shoutout" — the tag never got a column | **No non-structural fix exists.** A chip row on the composer is one screen, but it has nowhere to write: a relation on the record (a `round_players`-style table — not a column, to keep L-02's "the round's facts never change"), an RPC (L-03: a tag that feeds rivalry is a game consequence), a decision on what a tagged casual meeting means (DL C-2; L-19: a tag is NOT a vouch — "UNCONFIRMED" stays), and the reads (feed row, Tour Card head-to-head — DL-09 says none exists outside a shared season, epilogue) | No immutable law blocks it; three laws shape it (L-02, L-03, L-19). **Structural.** |
| D | The flow ends in a close; no next act | `FinishCeremonyView.swift:55-64` Share / "Back to the board"; `EpilogueSheet.swift:32-42` share / link / revoke; `PostRoundScreen.swift:136-140` `onDone` = `close` (`PostCoverView.swift:111`) → the cover dismisses to the tab underneath (`MainTabView.swift:338` snapped it back); `PostLinks.openReceipt/openPeople` wired (`MainTabView.swift:390-392`), no caller in `Post/*` | **Single-sheet:** the epilogue gains 1–3 rows keyed to membership state (no league: start / join / "put Saturday on the tee sheet" via `presenter.wizard`, `.join`, `.declare`; in league: `openReceipt` / the table). D18 says "dismissible, never blocking" — rows are fine. The live finish (PP-10, `LiveRoundHost.swift:49-57`) is a second single-screen fix | **Not structural — EXCEPT** the rung "challenge the partner", which needs C. |
| E | No post door on Home | `HomeView.swift:123` "Post one, or" is prose; **but** `:165-169` the lead card's clash and floor faces open the composer directly (in season only — `HomeLead.swift:166-175` needs a league); league-less Home has no door; You's empty state has one (`YouScreen.swift:138` → `links.postRound` → `MainTabView.swift:494`) | **Single-screen:** a persistent chip on Home (HM-12's rec) | D94 one-fact-per-tile; D218 precedent for Home doors. **Not structural.** |
| F | Tab "Post" → cover "Golf" → "Play now" first; IOS-011 vs D110 addendum | `MainTabView.swift:209` "Post"; `PostCoverView.swift:90, 95, 99`; `DECISIONS.md:127` vs `decision-log.md:4116-4119` | **Copy + one log entry** (TM-03, CC-33) | **Not structural.** |
| G | Live twin: ~20 concepts, Tee/Rating/Slope exposed with silent 72/113 | `LiveSetupView.swift:24-34, 94-103`; `LiveModels.swift:201-202`. PROD: 21/26 abandoned | **Single-screen:** a "Customize" fold; the D133 seat rule stays visible | L-40 / D107 untouched. **Not structural.** |

Applying A+B+D+E+F+G to the five personas' five questions:

- **E (invited, in a league), C (active), B (between seasons):** every question answerable after the fixes — Home chip answers WHAT CAN I DO; the table answers WHO; D122's phrase before typing fixes B's "on the books" (copy).
- **A (new golfer)** and **F (buddies, no league):** WHAT'S HAPPENING (buddies' rounds), WHAT CAN I DO (the chip), WHAT HAPPENS NEXT (epilogue rows) — answered. **WHO AM I COMPETING WITH — not answered.** F: "can't see myself against a buddy anywhere … the one genuinely exciting door … is never connected to the Sunday it could animate" (persona F `:270`). After the fixes, Tash and Ravi's Sunday still produces three unrelated rounds (or, at 21/26 abandonment, none); no rivalry, clash or challenge can hang off a typed round (PP-05). A's "Close saves nothing, so I can't set up tomorrow tonight" is dissolvable (the live setup's Close could stage a `declare_round` with `p_tagged` — an existing RPC — CJ-48), but A's WHO has the same gap as F's.

So the residual is exactly C (+ the D rung on it), for two of five personas, the league-less majority in prod (14 of 39 profiles; 1 has posted).

## Immutable-law check

- **No fix here breaks a vision/mechanic law.** Three laws SHAPE the structural fix and the entry must name them: L-02 (store the partner as a relation, never mutate the round's scoring facts), L-03 (the tag is an RPC write), L-19 (a tag is not a vouch; "UNCONFIRMED" stays; attested only if the partner's device was in the session).
- **L-40 caveat.** The canon reader files "live leads the ⊕ in ember" under the immutable wall (L-40, citing D110 `:4102-4106`), but O-07 itself classes D110 as UI level and says only the metal (L-25) and a first-screen sighting of the free door are fixed. Fix A ("the ⊕ opens on the composer") contradicts L-40's *order* clause as worded. The O-07 entry must say it supersedes D110's order and keeps its metal and the ember "Play now" link on the first screen (`PostRoundScreen.swift:119-131`) — otherwise the wall will be cited against it.
- **L-01** on the rating default: satisfiable only if the receipt prints the defaulted rating with its provenance; a silent 72/113 (the live twin today, PP-04) would violate it.
- **L-22/L-20** on the partner nudge ("X posted an 84 with you — post yours?"): it must be one of D104's eight emotions and fire once; not verified here — flag for the Social lane.
- **D34** ("two boxes, period") is UI level and amendable by a Score-first box; its purpose (hide the grid) is preserved.

## Corrections to the statement and evidence

1. **"cannot post a hand-typed course at all" is false.** `PostCourseSearchField.swift:22, 33`; `PostCard.swift:296-303`; PROD 155/207 quick rounds carry no `api_course_id`. Amend to: "a hand-typed course posts only with a hand-typed rating (25–90) and slope (55–155); with those blank the UI accepts (`PostRoundModel.swift:201`) and the direct insert (`PostService.swift:84`) is refused by `20260718173100:58-61`." PP-01's body says this; its title overstates.
2. **PP-01's "preview scores 0"** — with a blank rating `ratingValue` is 0, so the differential becomes `gross×113/slope` and the preview prints a nonsense negative vs and 5 pts (`PostCard.swift:238-241`), not 0.
3. **"No post door on Home in any mode (HM-12)" overstates HM-12.** HM-12 says the lead card reaches the composer on the clash and floor faces (`HomeView.swift:165-169`, in season only); league-less Home has none; You's empty state has a direct composer door (`YouScreen.swift:138`). Persona E's "Nothing on Home pointed me at it" is accurate for Home. "3 taps from a leagueless Home" is right; it is 2 from You.
4. **"has no 'who did you play with'"** — add: the scan flow mints claim links for the partner rows off one card (`PostScanSheets.swift:29, 40-53`); PROD `scan_claims` = 0. And D18 (`:287-291`) already ruled a tagged-foursome partner shoutout that never got a column — the fix restores a ruling, not only a brief demand.
5. **Root cause, add the write path:** the quick post is a direct `rounds` insert (`PostService.swift:84`), which is why the server check refuses what the UI accepts. The structural fix's natural shape — a `post_round(gross, course, played_with…)` RPC that derives or defaults rating and writes the relation — is what L-03 already asks for.
6. **Hierarchy level:** relabel to "MECHANIC (the post's shape: who, and what a tagged casual meeting means) → IA (the act after the ceremony)"; list A, B, D, E, F, G as UI-level (5) fixes that ride the same path and are each dissolvable alone.
7. **PROD numbers confirmed:** `post_submit` n = 24, median 39 s, p90 76 s; live rounds 21 abandoned / 5 final; 14 league-less profiles, 1 posted; `scan_claims` 0; rounds 207 quick / 5 live; 0 rounds at 72/113 (so the rating default has no posted-round precedent yet).
8. **Persona A's "Close saves nothing"** is a routing fix (stage a declared round with `p_tagged` from the live setup's Close; `contract.psv` `declare_round`), not part of the structural core.
9. **Evidence line "flow ends in a close … closes to whatever tab was underneath"** — correct; add `PostCoverView.swift:111` (`onDone: close`) and `MainTabView.swift:338` (the tab snaps back on presentation).
