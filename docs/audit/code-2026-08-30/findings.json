[
 {
  "id": "CSS-02",
  "title": "`--panel` is never defined — the Home hero, occasion card, Coming-up rows and RSVP buttons render with no background",
  "file": "index.html",
  "line": "1108 (also 1092, 1147, 1161)",
  "severity": "P2",
  "kind": "bug",
  "category": "correctness",
  "quote": ".hhero{position:relative; overflow:hidden; text-align:left; padding:15px 16px; margin:0 0 12px;\n  background:var(--panel); border:1px solid var(--line); border-radius:var(--r);}",
  "failureScenario": "`--panel` is referenced 4 times (1092 `.rbtn`, 1108 `.hhero`, 1147 `.hocc`, 1161 `.hround`) and defined nowhere. A `var()` naming an undefined custom property with no fallback makes the declaration invalid at computed-value time; for `background` that resolves to transparent. D81's one hero card, the occasion card, every 'Coming up' row and all three RSVP buttons paint no fill — a 1px `--line` outline directly over `--bg0` — while every other card on the same screen paints the `--bg1` face from `.card`.",
  "fix": "Add `--panel` to all three palette blocks as the raised-surface token, or replace the four uses with `var(--bg1)`.",
  "confidence": 9,
  "sessionCode": false,
  "notes": "Same family: `--fs` (1136) and `--bg` (980) are also undefined.",
  "slice": "index.html 1–2493 — head, tokens, stylesheet",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "CSS-03",
  "title": "`.prow` is two different components sharing one class name; the later block strips the earlier's horizontal padding",
  "file": "index.html",
  "line": "723 and 1895",
  "severity": "P2",
  "kind": "bug",
  "category": "correctness",
  "quote": "/* 723 */ .prow{display:flex; align-items:center; gap:10px; background:var(--bg1); border:1px solid var(--line); border-radius:12px; padding:10px 12px; margin-top:8px;}\n/* 1895 */ .prow{display:flex; align-items:center; gap:10px; padding:11px 0; border-top:1px solid var(--line);}",
  "failureScenario": "Both rules are specificity 0-1-0, so the later wins property-by-property while the earlier's unmentioned properties survive. Live-round player rows keep the bordered box but take `padding:11px 0`, so on a 390px phone the colour swatch sits flush against the card border and the −/+ stepper against the other. League Room roster rows inherit the full 1px box and radius, so `.prow:first-child{border-top:none}` cannot flatten the first row.",
  "fix": "Rename one of them; the 1895 block is the newer League Room segment component.",
  "confidence": 9,
  "sessionCode": false,
  "notes": "`#commishChip` carries an inline `margin-top:6px` — a hand-patch against the collision, not a fix.",
  "slice": "index.html 1–2493 — head, tokens, stylesheet",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "CSS-04",
  "title": "Two sticky elements at `top:0` in one scroll container — the live scoreboard paints over the app header",
  "file": "index.html",
  "line": "682 (vs 167)",
  "severity": "P2",
  "kind": "bug",
  "category": "correctness",
  "quote": "/* 167 */ .hdr{position:sticky; top:0; z-index:20; … padding-top:env(safe-area-inset-top);}\n/* 682 */ .scoreboard{position:sticky; top:0; z-index:60;",
  "failureScenario": "Both are descendants of `.main` with no `overflow` ancestor between them, so both stick to the viewport at top:0. A signed-in golfer scoring their own group scrolls past the first screen: `#scoreBoard` rises to y=0 and at z-60 vs z-20 paints over the header for the rest of the round, covering `#hdrSearch` and `#hdrLogo`. The scoreboard also lacks the header's safe-area padding, so on a notched iPhone it runs under the status bar.",
  "fix": "Offset the scoreboard by a header-height token and keep it below the header in the stack, or raise `.hdr` above 60.",
  "confidence": 8,
  "sessionCode": false,
  "notes": "`.view{animation:vin}` creates no stacking context at rest (`to` keyframe is `transform:none`).",
  "slice": "index.html 1–2493 — head, tokens, stylesheet",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "CSS-05",
  "title": "`.mini` / `.f` focus ring measures 1.3:1 — the rule kills the outline and replaces it with an invisible glow",
  "file": "index.html",
  "line": "2031-2034",
  "severity": "P2",
  "kind": "bug",
  "category": "accessibility",
  "quote": ".mini:focus-visible, .f:focus-visible{outline:none; border-color:var(--focus); box-shadow:0 0 0 3px var(--glow);}",
  "failureScenario": "`--glow` is rgba(232,98,44,.20) dark / rgba(201,78,31,.12) light. Composited on the card face the 3px ring measures 1.29:1 dark and 1.17:1 light — no perceptible ring. The rule also sets `outline:none` and, at equal specificity but later source order, defeats `.mini:focus-visible{outline:2px solid var(--focus)}` at 861. A keyboard user tabbing the League Room gets only a 1px border recolour.",
  "fix": "Drop `outline:none`, or raise the ring to `box-shadow:0 0 0 3px var(--focus)` (8.0:1 dark, 4.6:1 light).",
  "confidence": 8,
  "sessionCode": false,
  "notes": "`input.f:focus-visible` (0-2-1) outranks it, so text inputs keep their real outline. Only `.mini` loses it.",
  "slice": "index.html 1–2493 — head, tokens, stylesheet",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "CSS-06",
  "title": "White ink on filled `--pos` / `--brand` fails AA in the default dark theme — the file's own F1 note says why",
  "file": "index.html",
  "line": "1093 (also 1129, 616)",
  "severity": "P2",
  "kind": "bug",
  "category": "accessibility",
  "quote": ".rbtn.on.in{color:#fff; background:var(--pos); border-color:var(--pos);}",
  "failureScenario": "Charcoal is the default theme, so `--pos` is #4EC584 and `--brand` is #E8622C. White on #4EC584 = 2.18:1; white on #E8622C = 3.38:1. Both fail AA for the 12.5–14px text they carry: the selected RSVP pill, the member's single Home action, and the armed Save-card button. The file already solved this one rule away — `.btn` uses `color:#1C1208` at 5.45:1, with an F1 comment saying white ink is for light theme's #C94E1F only.",
  "fix": "Use `color:#1C1208` on the filled ember/mint plus the existing light-theme override pattern.",
  "confidence": 9,
  "sessionCode": false,
  "notes": "Related paper failures: `.rbtn.on.maybe` 3.17:1, `.sccard td.won` and `.pro .pb` 3.81:1.",
  "slice": "index.html 1–2493 — head, tokens, stylesheet",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "CSS-07",
  "title": "`--dim` fails AA as text in BOTH themes and is the file's most-used text colour, including the tab-bar labels",
  "file": "index.html",
  "line": "51 (light: 92)",
  "severity": "P2",
  "kind": "bug",
  "category": "accessibility",
  "quote": "  --dim:#5C646B;\n/* light, line 92 */  --ink:#1A2620; --mut:#52625A; --dim:#8C9992;",
  "failureScenario": "Measured on the surfaces actually painted: dark `--dim` 3.11/2.87/2.84/2.55; light 2.63/2.88/2.93/2.43. Fails AA in every combination in both themes. `--mut` passes comfortably (5.8/6.4). The bottom navigation labels are `--dim` on the tabbar's rgba(12,13,15,.9) = 3.22:1 at 10.5px uppercase — the primary navigation. Same colour carries `.stat .k`, `.chart-note`, `.climb-rung .gap`, every `.eyebrow`, `.hfmeta`.",
  "fix": "Lift `--dim` to ~4.5:1 in both palettes (dark ≈ #7C858C, light ≈ #6F7C75), or reserve it for non-text chrome and move text sites to `--mut`.",
  "confidence": 9,
  "sessionCode": false,
  "notes": "`.room-dusk`'s `--dim:#665F59` measures 2.87 against its own `--bg1`.",
  "slice": "index.html 1–2493 — head, tokens, stylesheet",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "CSS-01",
  "title": "`--fs` is undefined, so D126's endgame sentence renders in mono instead of the sans voice it was written for",
  "file": "index.html",
  "line": "1136-1137",
  "severity": "P3",
  "kind": "bug",
  "category": "correctness",
  "quote": ".chart-note .endgame-line{display:block; margin-top:5px; font-family:var(--fs); font-size:11.5px; …}",
  "failureScenario": "`--fs` is defined nowhere (the three font tokens are `--sans`, `--mono`, `--serif`). An undefined `var()` on an inherited property resolves to inherit, and the parent `.chart-note` is `font-family:var(--mono)`. The D126 sentence that finally states how the title is decided renders in IBM Plex Mono, the same voice as the eyebrow above it — which is exactly what the rule's own `letter-spacing:0; text-transform:none` siblings exist to escape.",
  "fix": "`font-family:var(--sans);`",
  "confidence": 9,
  "sessionCode": true,
  "notes": "Introduced in d41d2b7. One of exactly two CSS rules this session added inside 1–2493.",
  "slice": "index.html 1–2493 — head, tokens, stylesheet",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "CSS-13",
  "title": "`.hhero .hh-sub` is a 38px-tall button — under the 44px floor the file enforces everywhere else",
  "file": "index.html",
  "line": "1138-1140",
  "severity": "P3",
  "kind": "bug",
  "category": "accessibility",
  "quote": ".hhero .hh-sub{display:flex; align-items:center; justify-content:center; min-height:38px; margin-top:8px; width:100%; …}",
  "failureScenario": "Rendered as a real `<button>` at 10545. `min-height:38px` with no padding means a 38px tap target, 6px under the iOS/Material floor, sitting directly under a correctly-44px `.hh-cta`. The file treats 44px as law in three other places with comments explaining why.",
  "fix": "`min-height:44px`.",
  "confidence": 9,
  "sessionCode": true,
  "notes": "The other of the two CSS rules this session added inside 1–2493.",
  "slice": "index.html 1–2493 — head, tokens, stylesheet",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "CSS-09",
  "title": "`.bf-hdr` has no light-theme counterpart — the full-screen board wears a near-black bar on a paper page",
  "file": "index.html",
  "line": "2473-2477",
  "severity": "P3",
  "kind": "bug",
  "category": "correctness",
  "quote": ".bf-hdr{… background:rgba(12,13,15,.9); backdrop-filter:blur(16px);}",
  "failureScenario": "`#boardFull` itself is `background:var(--bg0)` = #EFF2EE in light. Both other hardcoded-dark chrome bars got a light counterpart (`.hdr` at 107, `.tabbar` at 108); `.bf-hdr` was missed. A light-theme user taps 'OPEN ↗' and the board opens as a white page with a #232425 bar welded across the top.",
  "fix": "Add `html[data-theme=\"light\"] .bf-hdr{background:rgba(239,242,238,.9);}` next to 107–108.",
  "confidence": 9,
  "sessionCode": false,
  "notes": "Grepped every `.bf-hdr` reference — no light override exists.",
  "slice": "index.html 1–2493 — head, tokens, stylesheet",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "CSS-10",
  "title": "The retired pre-D76 brand green still leaks into two surfaces",
  "file": "index.html",
  "line": "1331 (also 2168-2171)",
  "severity": "P3",
  "kind": "bug",
  "category": "correctness",
  "quote": "html[data-theme=\"light\"] .momrow{background:rgba(21,116,63,.06);}",
  "failureScenario": "`.momrow` is an ember card (ember gradient, `border-left:3px solid var(--brand)`, ember bold text). Its light override repaints the fill green. Every league 'moment' card shows a green wash inside an orange frame. The same retired green appears literally at 2171: `.finish-share{background:#2FA46A}` — the exact hex the `.room-dusk` comment names as the dead pre-D76 brand.",
  "fix": "`rgba(232,98,44,.06)` for `.momrow`; `#4EC584` for `.finish-share`.",
  "confidence": 8,
  "sessionCode": false,
  "notes": "D76's sweep is documented at 2265–2287 and explicitly lists the leak-hunting rule; these two sites were missed.",
  "slice": "index.html 1–2493 — head, tokens, stylesheet",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "CSS-11",
  "title": "The hole-grid steppers are 19×19px and paint no background (undefined `--bg`)",
  "file": "index.html",
  "line": "980",
  "severity": "P3",
  "kind": "bug",
  "category": "accessibility",
  "quote": ".pcell .prow button{width:19px; height:19px; border:0.5px solid var(--line); background:var(--bg); border-radius:6px; …}",
  "failureScenario": "Two defects in one rule: `--bg` is undefined so the background resolves to transparent, and 19×19px is less than half the 44px floor. This grid is the scorecard-scan confirm surface — a golfer correcting a misread hole must hit a 19px, near-invisible ±, 36 such targets on an 18-hole card.",
  "fix": "`background:var(--bg1)` and grow the hit box to 44px with padding.",
  "confidence": 8,
  "sessionCode": false,
  "notes": "These cells also inherit the `.prow` box from 723 (see CSS-03).",
  "slice": "index.html 1–2493 — head, tokens, stylesheet",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "CSS-12",
  "title": "`.sheet .x` — the close button on every drill-down sheet — is a ~26×26px tap target",
  "file": "index.html",
  "line": "1532",
  "severity": "P3",
  "kind": "bug",
  "category": "accessibility",
  "quote": ".sheet .x{position:absolute; top:14px; right:14px; border:none; background:none; color:var(--dim); font-size:20px; cursor:pointer; padding:4px;}",
  "failureScenario": "18px of SVG + 4px padding each side = 26×26px, 18px short of the floor, in the top-right corner where a right-handed thumb is least accurate. It is the primary dismissal for every §16 receipt sheet, and the panel's own comment records that this ✕ once trapped the user until they rotated. Its colour is also `--dim` (2.55:1).",
  "fix": "`padding:13px; margin:-9px;` or `min-width/min-height:44px` with `display:grid; place-items:center`.",
  "confidence": 8,
  "sessionCode": false,
  "notes": "Same class in descending press frequency: `.step button` 36×36, `.rbtn` ~34px, `.setrow .ctl button` 36×34, `.gchip.add` ~32px, `.hocc .ho-x` 24×24, `.pslot .sx` ~22px, `.ibtn` 18×18, `.echip button` ~14px.",
  "slice": "index.html 1–2493 — head, tokens, stylesheet",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "CSS-08",
  "title": "Four light-theme semantic tokens fail AA at the small sizes they are used at",
  "file": "index.html",
  "line": "90-96",
  "severity": "P3",
  "kind": "bug",
  "category": "accessibility",
  "quote": "  --pos:#0E9B51; --neg:#CC4038; --gold:#9A7418;\n  --warm:#B97A1E; --hot:#C94E1F; --fire:#B03014; --cool:#6E7A84;",
  "failureScenario": "On the light theme's own surfaces: `--pos` 3.19–3.56, `--gold` 3.53–4.25, `--warm` 2.94–3.54, `--cool` 3.60–4.34. All below 4.5:1 at 9.5–15px. Carries the receipt's points figure, the standings 'IN' badge, `.rkmove.up`, `.eyebrow.withgo a`, `.annrow b`. The dark counterparts all pass (7.9–8.6), so this is light-theme-only.",
  "fix": "Darken the four paper values (≈ #0A7E41, #7E5E10, #96620F, #5A646D).",
  "confidence": 8,
  "sessionCode": false,
  "notes": "`--neg` and `--brand` are borderline (pass on the card face, fail on bg0/bg2). `--dawn` passes.",
  "slice": "index.html 1–2493 — head, tokens, stylesheet",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "CSS-17",
  "title": "The Q-13 install-nudge fix moved the banner off the ⊕ tee and onto the sticky header",
  "file": "index.html",
  "line": "167 (collides with 3748)",
  "severity": "P3",
  "kind": "bug",
  "category": "correctness",
  "quote": "/* 167 */ .hdr{position:sticky; top:0; z-index:20;\n/* 3748 */ <div id=\"installNudge\" style=\"… top:calc(env(safe-area-inset-top) + 8px); z-index:24; …\">",
  "failureScenario": "The header's controls occupy roughly safe+14 → safe+59; the banner spans safe+8 → safe+~67 at z-24 > z-20. `busy()` suppresses it only on view-play/record/wizard/draft, so across Home, Clubhouse, You and Schedule the banner sits on the header — `#hdrSearch` and `#hdrLogo` covered until dismissed, and because the header is sticky, scrolling does not reveal them.",
  "fix": "Give the banner `z-index:19`, or offset it below the header.",
  "confidence": 8,
  "sessionCode": true,
  "notes": "Dismissable, which caps the severity. See IDX02-02 / IDX11-03 for the same collision from the markup side.",
  "slice": "index.html 1–2493 — head, tokens, stylesheet",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "CSS-16",
  "title": "`.hfcard.stamped` cancels the 2-line clamp on `.hfid b` for every posted-round card",
  "file": "index.html",
  "line": "623-624 (cancels 555)",
  "severity": "P3",
  "kind": "bug",
  "category": "correctness",
  "quote": "/* 555 */ .hfid b{display:-webkit-box; -webkit-line-clamp:2; …}\n/* 623 */ .hfcard.stamped .hfid > b{display:inline-block; transform:rotate(-1.4deg);}",
  "failureScenario": "`-webkit-line-clamp` only takes effect on `display:-webkit-box`. The 0-3-1 override beats 0-1-1, so the clamp is dead for every posted-round card (both templates emit `.stamped`) and applies only to moments/notices — the inverse of what 555 was written for.",
  "fix": "Move the cant onto a wrapper, or clamp with `max-height` so it survives the `display` change.",
  "confidence": 7,
  "sessionCode": false,
  "notes": "Low blast radius — `.hfid b` usually holds a first name. Filed because it is a rule silently cancelled by another.",
  "slice": "index.html 1–2493 — head, tokens, stylesheet",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "CSS-14",
  "title": "`.optcard.soon` is a latent cascade trap — and the four rules implementing it are already dead",
  "file": "index.html",
  "line": "814-817 (vs 1433)",
  "severity": "P3",
  "kind": "opportunity",
  "category": "simplification",
  "quote": "/* 814 */ .optcard.soon{opacity:.6; cursor:default;}\n/* 1433 */ .soon{font-family:var(--mono); font-size:10px; … border-radius:5px; padding:2.5px 6px; margin-left:8px;}",
  "failureScenario": "`.soon` is both a modifier on `.optcard` and a standalone SOON badge, both at 0-1-0, badge 642 lines later. An element carrying both would collapse from a full-width option card to a 10px mono pill. It cannot fire today only because `gateLiveRound()` removes the class on both branches — which also makes 814–817 unreachable. The trap is that the comment documents `.optcard.soon` as a supported state.",
  "fix": "Delete 814–817 and the comment, or rename the badge to `.soontag`.",
  "confidence": 8,
  "sessionCode": false,
  "notes": "D107 made the live door unconditionally free, so the 'coming soon' state has no product behind it.",
  "slice": "index.html 1–2493 — head, tokens, stylesheet",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "CSS-15",
  "title": "Dead CSS: rules for elements and tokens that no longer exist",
  "file": "index.html",
  "line": "236 (also 184/2039/2045, 232-235, 1363-1369, 55)",
  "severity": "P3",
  "kind": "opportunity",
  "category": "simplification",
  "quote": "#raceChart{width:100%; height:auto; display:block;}",
  "failureScenario": "A maintenance failure, and each entry is a wrong signal. `#raceChart` and `.chart-legend` have no matching element (the race chart was replaced by `.climb`). `.hdr .who`, restyled twice with a 'the chip wears the brand ring' comment, has no matching element either — anyone reading 2039 believes there is a header avatar. `.composer{position:sticky}` never runs (both consumers override it). `--pine` is defined in two palettes and used zero times.",
  "fix": "Delete 232–236, 184–189, 2038–2045, 1363–1369 and the two `--pine` declarations — about 20 lines, no behaviour change.",
  "confidence": 9,
  "sessionCode": false,
  "notes": "`.league-only` is the same shape but already documented as inert at 11395 — leave it.",
  "slice": "index.html 1–2493 — head, tokens, stylesheet",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "IDX02-01",
  "title": "The golfer card's index field has no range guard on either side of the wire — set_index()'s −10..54 rule is bypassed at onboarding",
  "file": "index.html",
  "line": "2682",
  "severity": "P0",
  "kind": "bug",
  "category": "scoring-integrity",
  "quote": "<input class=\"f\" id=\"pfIdx\" type=\"number\" step=\"0.1\" inputmode=\"decimal\" placeholder=\"e.g. 12.4\">",
  "failureScenario": "A new golfer types 124 for 12.4 into 'Handicap index · optional' and taps Save my card. The input has no min/max; #pfSave passes it through unchecked (index.html:13630); set_profile() does zero validation. score_round() snapshots index_at_post := index_current, and v_rounds_ranked scores every round at the maximum band until the auto-engine takes over at three rounds — but the posted rounds are immutable (§16). The identical path through the You-tab editor is refused twice, client-side at 14368 and server-side in set_index.",
  "fix": "Add min/max to #pfIdx, mirror the You-tab check in #pfSave, and add the same −10..54 raise to set_profile() in a NEW migration. Better: have the golfer card call set_index() so one function owns the rule.",
  "confidence": 9,
  "sessionCode": false,
  "notes": "Verifiers corrected severity to P2: 54 (the permitted maximum) already saturates the top band, so the guard buys no scoring integrity; D49 logs the 3-round sandbag window as an accepted tradeoff. The real harm is the typo path — profiles.index_current is unbounded numeric while rounds.index_at_post is numeric(4,1), so 1240 saves fine and then makes the golfer's FIRST round insert fail 22003 with an error that never names the index.",
  "slice": "index.html 2494–3763 — sprite, door, view skeletons, error handlers",
  "verification": {
   "status": "survived",
   "correctedSeverity": "P2",
   "votes": [
    {
     "lens": "control-flow",
     "verdict": "CONFIRMED",
     "refuted": false,
     "correctedSeverity": "P2",
     "reasoning": "Reachability holds end to end: safeBoot gates the card on marker+handle, #pfSave passes the raw value, and prod's live set_profile validates only the photo path with no CHECK on profiles.index_current. But 54 (the permitted max) already saturates the top band, so the guard buys no scoring integrity, and D49 logs the 3-round sandbag window as an accepted tradeoff. Two factual slips: numeric(4,1) is rounds.index_at_post, not profiles.index_current — so 1240 saves fine and the golfer's FIRST round insert then fails 22003 with an error that never names the index. 0 of 33 prod profiles are out of range."
    },
    {
     "lens": "intent-and-precedent",
     "verdict": "CONFIRMED",
     "refuted": false,
     "correctedSeverity": "P2",
     "reasoning": "Nothing exempts the card: git log shows the −10..54 guard arriving for set_index/set_member_index only, after the card was built. But the money-path story is the case D49 already priced and accepted. Real marginal harm is on the surfaces that consume PvI LINEARLY (Major leaderboard, Ryder ordering, feed chip, tour_card avg), plus a second gap: set_profile never writes index_source, so a card-set starter skips round_refresh_index's handoff announcement."
    }
   ]
  }
 },
 {
  "id": "IDX02-02",
  "title": "Q-13 moved #installNudge off the ⊕ and onto the header — it now covers #hdrSearch and #hdrLogo",
  "file": "index.html",
  "line": "3748",
  "severity": "P2",
  "kind": "bug",
  "category": "mobile-overlay",
  "quote": "<div id=\"installNudge\" style=\"display:none; position:fixed; left:12px; right:12px; top:calc(env(safe-area-inset-top) + 8px); bottom:auto; z-index:24; …\">",
  "failureScenario": "After the first posted round csEarnInstallNudge() shows a ~60px banner at safe+8 with z-index 24; the header is sticky z-index 20 with its controls at roughly safe+14 to safe+50. Tapping the search glass or the logo hits the banner. This is the same elementFromPoint failure the comment directly above says it just fixed, relocated from the ⊕ to the header.",
  "fix": "Offset the nudge below the header, or drop its z-index below 20.",
  "confidence": 8,
  "sessionCode": true,
  "notes": "Introduced by commit 1f4eb7d (Q-13). Geometry computed from CSS, not measured.",
  "slice": "index.html 2494–3763",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "IDX02-03",
  "title": "Two stat tiles were relabelled 'across counting rounds' this session, but both averages are computed across ALL rounds",
  "file": "index.html",
  "line": "2895, 2921",
  "severity": "P2",
  "kind": "bug",
  "category": "scoring-correctness",
  "quote": "<div class=\"stat\"><div class=\"k\">Avg vs your number</div><div class=\"v\" id=\"clAvg\">—</div><div class=\"d fl\">across counting rounds</div></div>",
  "failureScenario": "#clAvg comes from loadCareer, which averages index_at_post − differential over every round for the profile with no month_rank filter and no league lens. #msAvg averages every v_rounds_ranked row for the season; the same map builds `hist` with an explicit `counting: r.month_rank<=capN` flag one line later. Both tiles read '…across counting rounds' while the true counting-rounds average can differ sharply, and the Clubhouse standings — which DO respect month_rank — disagree with the You tab for the same golfer.",
  "fix": "Restore honest captions, or filter to counting rounds at both producers.",
  "confidence": 9,
  "sessionCode": true,
  "notes": "Both captions changed in 1f4eb7d. The old captions read 'vs your index' — vague but not false.",
  "slice": "index.html 2494–3763",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "IDX02-04",
  "title": "The Point bands card promises 7 points at exactly −1.0, where cup_points() pays 6 — the surface Q-20 missed",
  "file": "index.html",
  "line": "3216-3220",
  "severity": "P2",
  "kind": "bug",
  "category": "scoring-correctness",
  "quote": "<tr><td>Beat it by 1–3</td><td>9</td></tr>\n<tr><td>Within a stroke either way</td><td>7</td></tr>\n<tr><td>Over by 1–3</td><td>6</td></tr>",
  "failureScenario": "A round landing at pvi −1.0 reads top-down as 'Within a stroke either way' → 7. The server pays 6, and commit 4be974d changed pointsFor()/bandName() from `>= -1` to `> -1` precisely so the screen would stop promising a point the table would not pay. That commit fixed the two JS functions and left this static HTML table — the most literal statement of the rule on the screen — saying the old closed interval. The table is also self-contradictory at −1.0, +3.0 and +1.0, each matching two rows.",
  "fix": "Rewrite the rows as the half-open intervals in bandName()'s vocabulary, or render the table from bandName()/pointsFor().",
  "confidence": 9,
  "sessionCode": false,
  "notes": "The table last changed in f60b26b; what makes it a finding now is that Q-20 unified this rule everywhere else.",
  "slice": "index.html 2494–3763",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "IDX02-05",
  "title": "The post screen hardcodes 'best 4 each month' regardless of the league's counting-cap bylaw",
  "file": "index.html",
  "line": "3222",
  "severity": "P2",
  "kind": "bug",
  "category": "copy-vs-bylaw",
  "quote": "<p class=\"fine\">Every posted round scores. Your best 4 each month count toward your squad — a better round always replaces your lowest, in real time.</p>",
  "failureScenario": "A Pro picks Casual ('unlimited counting') or dials the cap to Best 2 / Best 6. Every member of that league opens ⊕ → Post a round and reads that their best 4 count. Nothing rewrites this paragraph: it carries no id and no selector reaches it; the only cap-aware copy producer is `outside your best ${capN} this month` on a different surface. So the post screen states a fairness rule that contradicts the locked bylaws, on the screen where the golfer decides whether the round is worth posting.",
  "fix": "Give the paragraph an id and set it from the loaded bylaws next to the #calcSeason write.",
  "confidence": 8,
  "sessionCode": false,
  "notes": "Confirmed by grep that no renderer targets this node.",
  "slice": "index.html 2494–3763",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "IDX02-06",
  "title": "The Terms & Privacy consent line on the sign-in door renders at 2.1:1 contrast (links 3.7:1)",
  "file": "index.html",
  "line": "2666",
  "severity": "P2",
  "kind": "bug",
  "category": "accessibility",
  "quote": "<p class=\"fine\" style=\"text-align:center; margin-top:16px; opacity:.7\">By continuing you agree to the <a … style=\"color:var(--mut); text-decoration:underline\">Terms</a> …</p>",
  "failureScenario": "`.fine` is 13px at `--dim`; the inline `opacity:.7` composites it to ≈2.14:1 against `--bg0` in dark and ≈1.89:1 in light. The links land at ≈3.69:1. A golfer on a phone in daylight cannot read the sentence recording their agreement to the Terms, or see that two words in it are tappable — and the .7 multiplier sits on top of a token already reserved for the quietest text in the system.",
  "fix": "Drop the inline opacity and set `color:var(--mut)` on the paragraph, links at `--ink` or `--dawn`.",
  "confidence": 8,
  "sessionCode": false,
  "notes": "Ratios computed by hand from token hexes with the .7 composited; not measured in a browser.",
  "slice": "index.html 2494–3763",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "IDX02-07",
  "title": "The five 'How it works' rows are <div>s with a click handler — no keyboard access, no role, no accessible name",
  "file": "index.html",
  "line": "2936-2940",
  "severity": "P2",
  "kind": "bug",
  "category": "accessibility",
  "quote": "<div class=\"check tap\" data-guide=\"places\" style=\"cursor:pointer\">…</div>",
  "failureScenario": "#youPeopleGo two rows above is a real <button class=\"check tap\"> and receives focus; all five guide rows are plain <div>s with no tabindex, role or keydown handler, so Tab skips them and Enter/Space do nothing. The only handler is a click delegate. A screen reader announces five unlabelled groups with no indication they are actionable. D82 makes this the one permanent home for the orientation, so the entire help system is pointer-only.",
  "fix": "Change the five divs to <button type=\"button\" class=\"check tap\" …> matching #youPeopleGo; `.check` is display:flex so nothing moves. Mark the → span aria-hidden.",
  "confidence": 9,
  "sessionCode": false,
  "notes": "The same div-with-click pattern appears in JS-rendered rows at 16200/16370/16683.",
  "slice": "index.html 2494–3763",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "IDX02-08",
  "title": "#inDate has no max, so a future-dated round is only caught by the server trigger",
  "file": "index.html",
  "line": "3176",
  "severity": "P3",
  "kind": "opportunity",
  "category": "validation",
  "quote": "<label class=\"f\" for=\"inDate\">Date</label><input class=\"f\" id=\"inDate\" type=\"date\">",
  "failureScenario": "A fat-fingered year (2027) is sent and rejected by the UGC trigger, so the golfer gets a generic post failure at the end of the flow instead of a field that cannot hold the value. This is exactly the loop Q-21 fixed for the EMPTY date one line of code away; the future-date twin was left server-only.",
  "fix": "Set `max` on #inDate to today's local ISO date next to the existing default-date write, with the same aria-invalid treatment.",
  "confidence": 8,
  "sessionCode": false,
  "notes": "Filed as an opportunity because the round is correctly refused; the defect is the error's location and legibility.",
  "slice": "index.html 2494–3763",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "IDX02-09",
  "title": "The app's only <h1> lives inside a display:none container — every signed-in view has zero headings",
  "file": "index.html",
  "line": "2785-2786",
  "severity": "P3",
  "kind": "opportunity",
  "category": "accessibility",
  "quote": "<div class=\"title\" style=\"display:none\">\n  <div id=\"lgRow\"><h1 id=\"leagueName\">The Sunday Cup</h1>…",
  "failureScenario": "The four onboarding <h1>s are inside #onboard, hidden once enterApp runs; the header's <h1> is inside a display:none wrapper kept 'so existing setters never null-ref'. Every view section uses <div class=\"eyebrow\"> for its section titles. Heading navigation therefore reports nothing on the four main tabs, and the <h4>s inside the ⊕ cards sit under no h2/h3 ancestor.",
  "fix": "Give each <section class=\"view\"> a visually-hidden <h1>, and promote `.eyebrow` to <h2 class=\"eyebrow\">.",
  "confidence": 7,
  "sessionCode": false,
  "notes": "Could not verify rotor behaviour with a real screen reader.",
  "slice": "index.html 2494–3763",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "IDX02-10",
  "title": "The unhandledrejection handler loses the reason for any falsy rejection value",
  "file": "index.html",
  "line": "3707-3708",
  "severity": "P3",
  "kind": "bug",
  "category": "silent-failure",
  "quote": "var r=e.reason||{};\nvar m='async: '+(r.message||String(r));",
  "failureScenario": "Any rejection with a falsy value — Promise.reject(), reject(null), an aborted fetch — substitutes a bare object, so the telemetry reads `client_error {msg:'async: [object Object]', stack:''}`. That is the exact shape of the ghost the comment eight lines above says this handler exists to prevent.",
  "fix": "`var r = e.reason; var m = 'async: ' + (r && r.message ? r.message : (r === undefined ? 'undefined' : String(r)));` and pass the original r to trace().",
  "confidence": 8,
  "sessionCode": false,
  "notes": "Low frequency by nature, but this is precisely the class of rejection that produced the untraceable ghost.",
  "slice": "index.html 2494–3763",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "IDX02-11",
  "title": "Two SVG sprite symbols (#i-swap, #i-key) have zero references anywhere in the file",
  "file": "index.html",
  "line": "2567-2576",
  "severity": "P3",
  "kind": "opportunity",
  "category": "dead-code",
  "quote": "<symbol id=\"i-swap\" viewBox=\"0 0 24 24\" …>",
  "failureScenario": "Every other symbol in the F6 set has live href uses; these two have 0. They ship in the shell on every cold load and mislead — the F6 comment declares this the sanctioned chrome icon set, so a later session adding a trade or invite-key affordance will assume something already uses them.",
  "fix": "Delete both, or leave a one-line comment naming the surface they are reserved for.",
  "confidence": 9,
  "sessionCode": false,
  "notes": "Verified by counting href=\"#i-<name>\" occurrences for all ten symbols.",
  "slice": "index.html 2494–3763",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "IDX02-12",
  "title": "cs_forge is written on every page load, not on the first door showing, so the forge ceremony can be spent without being seen",
  "file": "index.html",
  "line": "2522-2526",
  "severity": "P3",
  "kind": "opportunity",
  "category": "correctness",
  "quote": "if (location.search.indexOf('forge') < 0 && localStorage.getItem('cs_forge')) document.documentElement.classList.add('ob-fast');\nlocalStorage.setItem('cs_forge', '1');",
  "failureScenario": "The comment states 'first door showing plays the full show', but the setItem is unconditional and runs in the head, before anything renders. A golfer who opens an invite link and closes the tab in half a second has spent the flag; their real first look gets the 1.5s stamp. The `indexOf('forge')` test is a bare substring over the whole query string, so any future param containing 'forge' replays the show.",
  "fix": "Move the setItem to the point where the door is actually revealed, keep only the READ in the pre-paint block, and tighten the replay test to a real param match.",
  "confidence": 7,
  "sessionCode": false,
  "notes": "Cosmetic-only impact.",
  "slice": "index.html 2494–3763",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "idx03-01",
  "title": "switchView's draft→wizard remap runs AFTER the new wizard gate, so a member can be routed into the Pro's bylaws editor",
  "file": "index.html",
  "line": "4217-4234",
  "severity": "P1",
  "kind": "bug",
  "category": "security",
  "sessionCode": true,
  "quote": "  if(v==='wizard'){ const proHere = state.demo || !CS.league || CS.member?.role === 'commissioner'; if(!proHere){ … v = 'home'; } }\n  …\n  if(v==='draft' && state.phase==='setup'){ toast('Lock settings first: the draft opens after setup'); v='wizard'; }",
  "failureScenario": "A golfer joins a league by code while it is still in setup (join_league has no phase check). enterLeague sets state.phase='setup' and role='player'. They open Clubhouse → League and tap the ungated 'Squads · View' (data-go=\"draft\"). switchView('draft') passes the wizard gate (v is still 'draft'), then :4234 rewrites v='wizard' with no re-check → view-wizard renders for a non-commissioner with a live Lock button. That is the D40 breach Q-10 was written this session to close; only the server refuses him.",
  "fix": "Move the wizard gate to just above the view toggle so every remap passes through it: `v = proHere() ? 'wizard' : 'home'`.",
  "confidence": 9,
  "notes": "Not reachable in prod today: 7 setup-phase leagues, all with 0 players. It arms the moment one code-join lands before lock.",
  "slice": "index.html 3764–4577 — demo data, state, helpers, nav, climb",
  "verification": {
   "status": "survived",
   "correctedSeverity": "P2",
   "votes": [
    {
     "lens": "control-flow",
     "verdict": "CONFIRMED",
     "refuted": false,
     "correctedSeverity": "P2",
     "reasoning": "Traced end to end. The reachable tap is the Clubhouse League row (#3570), not the Home button (#homeDraft is display:none outside draft phase). renderWizard has no role test and stamps the viewer 'THE PRO'; #lockBtn has no role guard. Impact ceiling: lock_league and delete_league both check is_commissioner, and #wizCancel has the Q-10 belt — so no privilege escalation, only the D40 guarantee broken on the session that installed it. Prod has zero player-role members in setup-phase leagues, so latent."
    },
    {
     "lens": "intent-and-precedent",
     "verdict": "CONFIRMED",
     "refuted": false,
     "correctedSeverity": "P2",
     "reasoning": "D119 names this line explicitly ('switchView has no wizard gate (:4163 even redirects draft → wizard)') and requires the redirect to be role-aware. Q-10 shipped the gate and the #wizCancel belt and left the remap below it — an incomplete implementation of a logged decision, not an accepted tradeoff. Membership in a setup-phase league is unguarded server-side (join_league and add_friend_to_league have no phase check), so D118's ruling to refuse setup joins is decided but unbuilt."
    }
   ]
  }
 },
 {
  "id": "idx03-02",
  "title": "The climb note tells a two-squad league that only one squad advances, contradicting §14 and the endgame sentence printed beside it",
  "file": "index.html",
  "line": "4570-4574",
  "severity": "P1",
  "kind": "bug",
  "category": "scoring-copy",
  "sessionCode": true,
  "quote": "const seatLine = K>=n ? … : `TOP ${K} ${meta && meta.finish==='points_table' ? '— THE POINTS CROWN' : 'ADVANCE TO THE CUP FINAL'}`;\nnote.innerHTML = `${esc(seatLine)}<span class=\"endgame-line\">${esc(endgameLine(meta||{}))}</span>`",
  "failureScenario": "For structure='squads2' + finish='cup_final' (5 of 18 prod leagues) season_scenarios sets k=1 with the comment 'both reach the Final; the race is the #1 seed (+10)'. climbCut returns {K:1, line:'TOP SEED · +10'}; n=2 so K<n and the note renders 'TOP 1 ADVANCE TO THE CUP FINAL' immediately above endgameLine's 'The top 2 squads seed into a four-week Cup Final'. K is a SEED threshold here, not a seat count. The member on the 2nd rung is told he is out of the Final on the same line that tells him he is in it.",
  "fix": "Give climbCut a third field (`seats`, distinct from `K`) and have both seatLine and endgameLine read it; for squads2 render 'BOTH SQUADS REACH THE FINAL — THE TOP SEED CARRIES +10'.",
  "confidence": 9,
  "notes": "Verified live: squads2/cup_final = 5, squads4/cup_final = 10, solo/cup_final = 3. The demo gets it right because climbCut(null) returns K=2.",
  "slice": "index.html 3764–4577",
  "verification": {
   "status": "survived",
   "correctedSeverity": "P1/P2",
   "votes": [
    {
     "lens": "control-flow",
     "verdict": "CONFIRMED",
     "refuted": false,
     "correctedSeverity": "P1",
     "reasoning": "Path traced hop by hop and verified in prod: all five squads2/cup_final seasons have exactly 2 squads, so K=1 < n=2 in every one of them (one is already at status cup_final). D126 specifies the squads2 sentence verbatim ('Both squads reach the Cup Final … The season decides who starts +10'), so this is a partial implementation of D126, not a behaviour it blesses. endgameLine's `seats` never reads meta.k."
    },
    {
     "lens": "intent-and-precedent",
     "verdict": "CONFIRMED",
     "refuted": false,
     "correctedSeverity": "P2",
     "reasoning": "The decision log convicts the code: spec §14 :178/:246, season_scenarios' own comment, and D126's recommendation all say both squads advance. The same file already distinguishes seed from seat in stakeTxt and scenSeedWord, so the conflation is isolated. Copy-only — seeds, +10 and the fresh-scored window are all server-side and correct — but it is the exact failure D126 existed to fix."
    }
   ]
  }
 },
 {
  "id": "idx03-03",
  "title": "The 'CUP LINE' stake label is dead, so every squads3/4 and solo league calls the last Cup Final seat \"the top seed\"",
  "file": "index.html",
  "line": "4499-4501",
  "severity": "P2",
  "kind": "bug",
  "category": "copy-correctness",
  "sessionCode": false,
  "quote": "  const stakeTxt = cut.line==='CROWN LINE' ? 'the crown'\n                 : cut.line==='CUP LINE'   ? 'the last cup spot'\n                 : 'the top seed';",
  "failureScenario": "climbCut can only return line ∈ {'CROWN LINE','TOP SEED · +10','CUT LINE'} — 'CUP LINE' occurs nowhere else in the file. So the multi-squad case falls through to 'the top seed'. In a squads4 cup_final league (10 in prod) a Pro sitting 3rd reads '12 back of Mudsharks — the top seed', when second place is the LAST Cup Final seat, not the top seed.",
  "fix": "Key stakeTxt off `CUT LINE` → 'the last cup spot' and `TOP SEED · +10` → 'the top seed', and delete the unreachable comparison.",
  "confidence": 9,
  "notes": "For squads2 the fall-through happens to be right, which is why it survived.",
  "slice": "index.html 3764–4577",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "idx03-04",
  "title": "looksLikeOurSentence passes GoTrue's expired-OTP jargon straight to the sign-in screen, un-punctuated",
  "file": "index.html",
  "line": "4156-4170",
  "severity": "P2",
  "kind": "bug",
  "category": "silent-failure",
  "sessionCode": true,
  "quote": "  else if(looksLikeOurSentence(raw)) msg = raw.trim();\n  …\n  if(/[_{}<>]|::|\\bpg_|\\brelation\\b|\\bcolumn\\b|\\bconstraint\\b|\\bschema\\b|\\bsql\\b|\\berror\\b|\\bexception\\b|^[a-z0-9_]+\\(/i.test(s)) return false;",
  "failureScenario": "A stale 8-digit code makes verifyOtp reject with GoTrue's 'Token has expired or is invalid'. The earlier /invalid.*token/ branch requires the words in the other order, so it reaches looksLikeOurSentence: 31 chars, capitalised, has a space, no banned tokens → true → the raw message is shown as 'Code didn't take. Token has expired or is invalid Codes expire with resends, use the newest email.' GoTrue's 'Token' vocabulary is exactly what F-003 exists to suppress, and the missing terminal period makes the sentence run on.",
  "fix": "Stop inferring authorship from prose shape: prefix our own golfer-facing raises with a marker (e.g. `CS: `) and match on that, then delete both looksLikeOurSentence and the allowlist.",
  "confidence": 8,
  "notes": "The header comment claims the test checks a sentence 'ends like prose' — there is no end-of-string check in the function at all.",
  "slice": "index.html 3764–4577",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "idx03-05",
  "title": "humanError maps `permission denied` and `row-level` to \"Please sign in again\", turning the documented missing-grant failure into an unbreakable loop",
  "file": "index.html",
  "line": "4134",
  "severity": "P2",
  "kind": "bug",
  "category": "correctness",
  "sessionCode": false,
  "quote": "  else if(/jwt|not authenticated|auth session|invalid.*token|permission denied|row-level|not logged in/.test(m)) msg = 'Please sign in again.';",
  "failureScenario": "When a new RPC ships without its D37 grant, PostgREST returns 'permission denied for function set_buy_in_terms'. The Pro reads 'Could not save. Please sign in again.', signs out, signs back in, taps again, and gets the identical sentence forever. The same wrong instruction hides the documented `permission denied for table profiles` case. `row-level` is wrong in the other direction: an RLS refusal means the user IS signed in and simply is not allowed.",
  "fix": "Route permission-denied into the deploy-skew sentence and fire a qaEvent so a missing grant shows up in telemetry; route row-level security to 'You don't have access to that.'; keep 'Please sign in again' for jwt/auth session only.",
  "confidence": 8,
  "notes": "Pre-existing, but in the function this session rewrote, and it defeats the deploy-skew branch immediately below it.",
  "slice": "index.html 3764–4577",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "idx03-06",
  "title": "The shape test shrugs away the one server message written to be screenshotted for support",
  "file": "index.html",
  "line": "4168",
  "severity": "P3",
  "kind": "bug",
  "category": "silent-failure",
  "sessionCode": true,
  "quote": "  if(/[_{}<>]|::|\\bpg_|\\brelation\\b|…/i.test(s)) return false;",
  "failureScenario": "delete_account's hard-delete path raises 'Could not delete your account: something still references it (%.%). Nothing was changed — screenshot this and send it in via Feedback.' formatted with the blocking table and constraint. The `_` in every real constraint name trips the `[_{}<>]` gate, so the golfer is toasted the shrug instead and never learns there is anything to screenshot. Its two sibling messages from the same function DO pass, so the behaviour is inconsistent across three messages from one function.",
  "fix": "Match on an author-controlled marker rather than on characters that legitimately appear in our own prose.",
  "confidence": 8,
  "notes": "Rare path — which is precisely when the diagnostic matters.",
  "slice": "index.html 3764–4577",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "idx03-07",
  "title": "Four allowlist alternatives match no message anywhere in the repo, and one is duplicated",
  "file": "index.html",
  "line": "4145-4146",
  "severity": "P3",
  "kind": "bug",
  "category": "dead-code",
  "sessionCode": true,
  "quote": "  else if(/not enough golfers|minimum four|still in the pool|is empty — draw again|is empty — draw again|only the pro|commissioner only|isn't open yet|is still being set up|has wrapped|season is finished/i.test(raw))",
  "failureScenario": "A repo-wide scan of every `raise exception` string finds no message containing 'open yet', 'being set up', 'has wrapped' or 'season is finished' — the only hits are this line itself. `is empty — draw again` is listed twice. Four of eleven alternatives are inert: the branch reads as coverage for the D120/D122 league-stage guards, which were never written server-side.",
  "fix": "Delete the duplicate and the four dead alternatives, or write the stage guards server-side and drop the allowlist for a marker prefix.",
  "confidence": 9,
  "notes": "Verified the four that DO fire match byte-for-byte, including the U+2014 em dash.",
  "slice": "index.html 3764–4577",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "idx03-08",
  "title": "durLabel renders 8 weeks and 10 weeks identically as \"2 mo\", freezing the season-length stepper",
  "file": "index.html",
  "line": "3836",
  "severity": "P3",
  "kind": "bug",
  "category": "ux-correctness",
  "sessionCode": false,
  "quote": "const durLabel = w => w<8 ? `${w} wk` : `${Math.round(w/4.345)} mo`;",
  "failureScenario": "DURS = [2,3,4,5,6,8,10,13,17,22,26,39,52]; round(8/4.345)=2 and round(10/4.345)=2. A Pro on 8 weeks taps '+' and the readout does not change; they tap again and land on 13 weeks, two rungs from where they meant to be. No other pair collides.",
  "fix": "Show weeks alongside months for w>=8, or use the same clamped producer as durMonths.",
  "confidence": 8,
  "notes": "Partly mitigated: #seasonSpan's end date does move.",
  "slice": "index.html 3764–4577",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "idx03-09",
  "title": "D127's hollow-Final warning fires on the demo diorama, the app's showcase surface",
  "file": "index.html",
  "line": "4570-4574",
  "severity": "P3",
  "kind": "opportunity",
  "category": "copy",
  "sessionCode": true,
  "quote": "+ (K>=n && n>1 ? '<span class=\"endgame-line\">Every contender already has a seat, so the Final is a formality until more golfers join.</span>' : '');",
  "failureScenario": "In demo mode window.scenarios is never set, so meta=null and climbCut(null) returns K=2. The diorama has exactly two teams, so the note reads 'EVERYONE ADVANCES — 2 CONTENDERS, 2 SEATS' followed by the formality warning. D127 wrote that sentence to warn a real Pro that his roster is too small; on the diorama it tells a prospect that the product's flagship moment is a formality.",
  "fix": "Suppress the hollow-Final line when state.demo is true, or seed the diorama with meta.",
  "confidence": 7,
  "notes": "Could not confirm from this range how a visitor enters demo mode; state.demo defaults true.",
  "slice": "index.html 3764–4577",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "idx03-10",
  "title": "One producer for the seat/endgame sentence — climbCut, seatLine and endgameLine are three independent statements of one rule",
  "file": "index.html",
  "line": "4376-4384",
  "severity": "P2",
  "kind": "opportunity",
  "category": "simplification",
  "sessionCode": true,
  "quote": "function climbCut(meta){\n  if(meta){ if(meta.finish==='points_table') return {K:1, line:'CROWN LINE'};\n    if(meta.level==='squad' && meta.structure==='squads2') return {K:1, line:'TOP SEED · +10'};\n    return {K:meta.k||2, line:'CUT LINE'}; }\n  return {K:2, line:'CUT LINE'}; }",
  "failureScenario": "Three places answer 'how many advance': climbCut's K (a seed threshold for squads2, a seat count otherwise), seatLine (reads K as seats), and endgameLine's `seats` (a ternary that is always 2 and never reads meta.k, despite D126 §1 specifying it be built from climbCut(meta).K). idx03-02 and idx03-03 are direct consequences, and any future structure has to be taught the rule in three places.",
  "fix": "Replace climbCut with one endgameShape(meta) returning {K, seats, cutLabel, stakeLabel, sentence}; have renderClimb and endgameLine both read it.",
  "confidence": 9,
  "notes": "climbCut predates this session; the contradiction became visible when D126 put seatLine and endgameLine on the same line.",
  "slice": "index.html 3764–4577",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "IDX04-01",
  "title": "endgameLine promises \"+10\" to every Cup Final league; the engine grants it only to 2-squad leagues",
  "file": "index.html",
  "line": "5936 (rendered at 4573)",
  "severity": "P1",
  "kind": "bug",
  "category": "money-scoring-correctness",
  "sessionCode": true,
  "quote": "return `${who} seed into a four-week Cup Final${when ? ` from ${when}` : ''} — scored fresh, so the regular season sets the seeds, not the winner. The leader carries +10 in. Level on points? Months won breaks it.`;",
  "failureScenario": "For a solo league, or any squad league that is not squads2, the climb note tells the leader all season that they carry a 10-point cushion. enter_cup_final inserts the solo finalist with no head_start at all and a non-squads2 squad leader with `case when st.structure = 'squads2' then 10 else 0 end` = 0. Spec §14.3 agrees with the engine — the +10 is the 2-squad compensation, not a general rule. Prod: 10 squads4 and 3 solo leagues on cup_final.",
  "fix": "Gate the clause on `structure === 'squads2'`, or take the head start from cup_final_race's finalists so caption and cup_finalists cannot diverge.",
  "confidence": 9,
  "notes": "Added this session in d41d2b7 (D126). `seats` at 5927 is a three-way ternary that always returns 2.",
  "slice": "index.html 4578–5643 — standings, climb, feed",
  "verification": {
   "status": "survived",
   "correctedSeverity": "P1/P2",
   "votes": [
    {
     "lens": "control-flow",
     "verdict": "CONFIRMED",
     "refuted": false,
     "correctedSeverity": "P2",
     "reasoning": "No guard anywhere: the sentence is unconditional inside the cup_final branch, meta carries a real structure, and the finish dial is independent of structure in both the wizard and the in-season flip. Prod: 10 squads4 + 3 solo on cup_final — 13 of 18 leagues are told something the engine will not do. Three sibling surfaces in the same file gate correctly on squads2, which makes this an omission. Nothing miscomputes, and the D105 Cup Final block eventually renders the real head start."
    },
    {
     "lens": "intent-and-precedent",
     "verdict": "CONFIRMED",
     "refuted": false,
     "correctedSeverity": "P1",
     "reasoning": "D126 prescribes five structure-aware variants and names the +10 only for squads2; the vestigial always-2 `seats` ternary is the fingerprint of branching that was written and then flattened. Spec §14.3 and enter_cup_final agree with each other and against the copy. D126 exists because 8/8 blind testers could not say how the Cup is won, so stating a false rule to the majority of live leagues is worse than the silence it replaced."
    }
   ]
  }
 },
 {
  "id": "IDX04-02",
  "title": "Climb note contradicts itself on every load of a points-table league (K falls back to 2 while the sentence reads state.finish)",
  "file": "index.html",
  "line": "4570-4574 (with climbCut 4376-4384)",
  "severity": "P1",
  "kind": "bug",
  "category": "correctness",
  "sessionCode": true,
  "quote": "note.innerHTML = `${esc(seatLine)}<span class=\"endgame-line\">${esc(endgameLine(meta||{}))}</span>`",
  "failureScenario": "loadScenarios() is fire-and-forget while renderClimb() can run first, so `meta` may be null on the first paint. climbCut(null) returns {K:2,'CUT LINE'} and never consults state.finish, so seatLine renders 'TOP 2 ADVANCE TO THE CUP FINAL' while endgameLine({}) falls back to state.finish and appends 'The points table crowns it on Nov 21…' directly underneath. Permanent if season_scenarios errors. With ≤2 contenders it also appends 'the Final is a formality' to a league that has no Final.",
  "fix": "Make climbCut's no-meta fallback read the bylaws the same way endgameLine does — one producer for the seat count.",
  "confidence": 9,
  "notes": "Verifiers narrowed reachability (scenarios usually lands before the render) and corrected severity to P3; all 18 prod leagues are currently cup_final, so it is dial-gated today.",
  "slice": "index.html 4578–5643",
  "verification": {
   "status": "survived",
   "correctedSeverity": "P3",
   "votes": [
    {
     "lens": "control-flow",
     "verdict": "PLAUSIBLE",
     "refuted": false,
     "correctedSeverity": "P3",
     "reasoning": "The code inconsistency is confirmed and D126 specified one producer, so it is drift not design. But reachability is much narrower than claimed: loadScenarios fires one RPC and the caller then performs 3–4 further SEQUENTIAL round trips before renderClimb, so scenarios usually lands first and re-renders the climb itself. The 'permanent if ungranted' claim is wrong — the function IS granted to authenticated. Realistically a sub-second flash in a new season with no posts."
    },
    {
     "lens": "intent-and-precedent",
     "verdict": "CONFIRMED",
     "refuted": false,
     "correctedSeverity": "P3",
     "reasoning": "Mechanism real and unguarded (also on a league switch, where window.scenarios still holds the previous league's meta), and the standings cut row 400 lines away carries the audit-#12 comment and the finish gate that climbCut's fallback claims to match. But every one of the 18 prod leagues is finish='cup_final' today, where the fallback K=2 happens to agree — so it is dial-gated and transient, with no scoring or money consequence."
    }
   ]
  }
 },
 {
  "id": "IDX04-03",
  "title": "A 2-squad league is told \"TOP 1 ADVANCE TO THE CUP FINAL\" when both squads advance",
  "file": "index.html",
  "line": "4572",
  "severity": "P2",
  "kind": "bug",
  "category": "copy-correctness",
  "sessionCode": true,
  "quote": "      : `TOP ${K} ${meta && meta.finish==='points_table' ? '— THE POINTS CROWN' : 'ADVANCE TO THE CUP FINAL'}`;",
  "failureScenario": "season_scenarios sets k=1 for squads2 with the explicit comment 'both reach the Final; the race is the #1 seed (+10)'. K=1 < n=2, so the note reads 'TOP 1 ADVANCE TO THE CUP FINAL' while the cut separator two rows above reads 'TOP SEED · +10' and endgameLine below reads 'The top 2 squads seed into a four-week Cup Final'. The second-place squad concludes it is eliminated; it is not. Grammar too: 'TOP 1 ADVANCE' should be 'ADVANCES'.",
  "fix": "Branch on the cut label: when climbCut returns 'TOP SEED · +10', render 'BOTH SQUADS ADVANCE — THE RACE IS THE TOP SEED (+10)'.",
  "confidence": 9,
  "notes": "Duplicate of idx03-02 from the adjacent slice.",
  "slice": "index.html 4578–5643",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "IDX04-04",
  "title": "fetchSocial drops {error} after wiping every item's reactions and comments — a failed refresh silently blanks the board",
  "file": "index.html",
  "line": "5024-5043",
  "severity": "P2",
  "kind": "bug",
  "category": "silent-failure",
  "sessionCode": false,
  "quote": "  feed.forEach(f=>{ if(f.post_id){ f.rx = {}; f.cm = []; } });\n  …\n  }catch(e){ console.warn('[social]', e); return; }",
  "failureScenario": "supabase-js resolves with {data:null,error} rather than throwing, so the try/catch never fires; kud/cmt are undefined, the loops iterate nothing, and every item is reassigned {}/[] — but the wipe on 5026 already destroyed the previous values. refreshSocial() calls this on every realtime reaction nudge, so one transient failure erases every reaction count and comment thread on both boards with no toast and nothing in the console, until a full reload. The user reads it as 'my reactions were deleted'.",
  "fix": "Destructure error from both selects and bail before the wipe; run the reads first and only then clear + reassign.",
  "confidence": 9,
  "notes": "",
  "slice": "index.html 4578–5643",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "IDX04-05",
  "title": "The clash card trusts the archived server band, which disagrees with bandName() at pvi = -1.0",
  "file": "index.html",
  "line": "4712",
  "severity": "P2",
  "kind": "bug",
  "category": "two-producers",
  "sessionCode": false,
  "quote": "    const band=b ? (b.band || bandName(Number(b.pvi))) : null;",
  "failureScenario": "cup_points pays 6 at pvi −1.0 and this session's Q-20 fix moved bandName() to the same half-open boundary, but settle_week_clash's inline band CASE still reads `>= -1`. While the week is live the card computes 'A LITTLE LOOSE'; the moment the tick settles it, the same round's row flips to 'PLAYED TO IT' — with the 6-point figure beside it belonging to the other band, and the round receipt still saying 'A little loose'.",
  "fix": "Drop the `b.band ||` preference so the client is the single producer, and bring settle_week_clash's CASE to `> -1` in a NEW migration.",
  "confidence": 8,
  "notes": "See DBE-02 / DB-07 / KIT-03 for the server half.",
  "slice": "index.html 4578–5643",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "IDX04-06",
  "title": "priorRank stores an array index, not a rank — tied squads manufacture ▲/▼ movement arrows",
  "file": "index.html",
  "line": "4751-4757, 4774-4783",
  "severity": "P2",
  "kind": "bug",
  "category": "correctness",
  "sessionCode": false,
  "quote": "      .forEach((row,idx)=>{ priorRank[solo?row.member_id:row.squad_id]=idx; });",
  "failureScenario": "Week 2, nobody has posted, every squad at 0. snapshot_week wrote `order by points desc` — arbitrary among ties — and the client re-sorts by points (a no-op on ties, stable), so priorRank is 0,1,2,3 by DB order. The live `teams` array came from a name-ordered fetch sorted by points, i.e. alphabetically among ties. The two orders differ, so `d = pr - i` is non-zero for at least two rows and the table paints '▲2 this week' on squads that have not scored a point, and fires the SF-6 split-flap animation for ranks that never moved.",
  "fix": "Assign a dense rank on points in both directions and render an arrow only when the two ranks differ.",
  "confidence": 8,
  "notes": "",
  "slice": "index.html 4578–5643",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "IDX04-07",
  "title": "The cut row and rank column break points ties alphabetically; the server seeds by §14.3's ladder",
  "file": "index.html",
  "line": "4784-4795",
  "severity": "P2",
  "kind": "bug",
  "category": "money-scoring-correctness",
  "sessionCode": false,
  "quote": "    if(i===1 && (state.finish||'cup_final')==='cup_final' && teams.length>2)\n      html+=`<tr class=\"cutrow\"><td colspan=\"5\">Cut line · top 2 advance</td></tr>`;",
  "failureScenario": "In a 4-squad cup_final league where 2nd and 3rd are level on points, `teams` is sorted by points only over a name-ordered fetch, so the alphabetically-earlier squad renders 02 with the cut row under it. enter_cup_final actually seeds by score desc, months_won desc, best_month desc, rounds_used asc, coin desc; if the other squad won more months, THEY take seed 2. The standings screen has been telling the wrong squad it is in, all the way to the day the window opens. The cut row also hardcodes 'top 2' instead of reading meta.k.",
  "fix": "Consume season_scenarios' `rank` instead of re-sorting locally, label the cut row from meta.k, and where the ladder cannot be known client-side draw the cut with an explicit 'tie — months won decides' note.",
  "confidence": 7,
  "notes": "The client cannot compute months_won itself; the honest fix is to stop implying an order it cannot know.",
  "slice": "index.html 4578–5643",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "IDX04-08",
  "title": "easeCaps capitalizes the word after \"to\"/\"with\"/\"vs\", so board rows read \"welcome to The league\"",
  "file": "index.html",
  "line": "4881",
  "severity": "P3",
  "kind": "bug",
  "category": "copy-correctness",
  "sessionCode": false,
  "quote": "  t = t.replace(/\\b(with|vs|to)\\s+([a-z])/gi, (m,w,c)=>w+' '+c.toUpperCase());",
  "failureScenario": "The shipped system body '… WAS ADDED BY THE PRO — WELCOME TO THE LEAGUE' renders as 'Casey was added by the pro — Welcome to The league'. Verified by running easeCaps in node: 'JERECHO PLAYED TO IT AT PAPAGO' → 'Jerecho played to It at papago'; 'MUDSHARKS MOVE TO SECOND · 12 BACK' → 'Mudsharks move to Second · 12 back'. The rule fires on any capitalizable word, and the name registry that runs after it cannot undo the damage.",
  "fix": "Skip capitalization when the following word is a function word, or drop the rule and let the multi-word name registry do the restoring it was built for.",
  "confidence": 9,
  "notes": "",
  "slice": "index.html 4578–5643",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "IDX04-09",
  "title": "Solo standings empty state promises a Cup Final regardless of the endgame dial",
  "file": "index.html",
  "line": "4739",
  "severity": "P3",
  "kind": "bug",
  "category": "copy-correctness",
  "sessionCode": false,
  "quote": "    const soloTxt = state.structure==='solo' ? 'INDIVIDUAL RACE — NO SQUADS.<br>STANDINGS START AT THE FIRST POSTED ROUND; TOP 2 MEET IN THE CUP FINAL.' : …",
  "failureScenario": "A solo league whose Pro dialled finish='points_table', before anyone has posted, sees 'TOP 2 MEET IN THE CUP FINAL' for a season that will be crowned off the points table and will never enter a Final. Fifty-five lines down, the session's own #12 guard exists to stop exactly this claim on the cut row.",
  "fix": "Reuse the same finish gate the cut row uses, or render endgameLine({}) here.",
  "confidence": 9,
  "notes": "",
  "slice": "index.html 4578–5643",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "IDX04-10",
  "title": "Optimistic-echo reverts use a bare pop(), which removes the wrong item when a second send or a feed rebuild has intervened",
  "file": "index.html",
  "line": "5016, 5636",
  "severity": "P3",
  "kind": "bug",
  "category": "race",
  "sessionCode": false,
  "quote": "  if(error){ cm.pop(); rerenderFeeds(); toast(humanError(error, 'Comment did not send.')); }",
  "failureScenario": "Type A, send; type B, send (both echo into `cm` as [A,B]). A's insert fails; `cm.pop()` removes B — the comment that succeeded — and leaves A, the one that failed, on screen as if sent. The chat `feed.pop()` has the same shape with a full network round trip as the window, so a realtime rebuild in between makes the pop delete someone else's newest post.",
  "fix": "Hold a reference to the echo object and splice it out by identity.",
  "confidence": 7,
  "notes": "",
  "slice": "index.html 4578–5643",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "IDX04-11",
  "title": "endgameLine's seat ternary is dead code — the caption hardcodes 2 seats instead of reading the server's k",
  "file": "index.html",
  "line": "5927",
  "severity": "P3",
  "kind": "opportunity",
  "category": "dead-code",
  "sessionCode": true,
  "quote": "  const seats = structure === 'solo' ? 2 : (structure === 'squads2' ? 2 : 2);",
  "failureScenario": "Every branch evaluates to 2, so the expression reads as if it varies by structure while it cannot. Correct for every structure shipping today, but the next endgame shape will change season_scenarios' k and this sentence will keep saying 'the top 2' with no signal.",
  "fix": "`const seats = Number(o.k) || 2;` — the caller already has meta.k in hand.",
  "confidence": 9,
  "notes": "",
  "slice": "index.html 4578–5643",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "IDX04-12",
  "title": "roundStreak re-sorts the entire round cache once per rendered card",
  "file": "index.html",
  "line": "5427-5438 (called at 5477)",
  "severity": "P3",
  "kind": "opportunity",
  "category": "efficiency",
  "sessionCode": false,
  "quote": "  const mine=Object.values(window.roundCache||{}).filter(…).sort(…);",
  "failureScenario": "The full board renders up to 120 round cards and calls roundStreak once per card, walking and sorting every entry in the cache each time — 120 × (filter + sort + scan) on the main thread inside a synchronous innerHTML build, re-run on every reaction tap, comment send and realtime nudge.",
  "fix": "Build a per-profile sorted index once at the top of renderFeedFull and have roundStreak read it.",
  "confidence": 8,
  "notes": "",
  "slice": "index.html 4578–5643",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "IDX04-13",
  "title": "The D108 clash system posts are mixed-case by construction, so easeCaps passes them through and they shout beside eased rows",
  "file": "index.html",
  "line": "4875",
  "severity": "P3",
  "kind": "opportunity",
  "category": "copy-consistency",
  "sessionCode": false,
  "quote": "  if(!s || s !== s.toUpperCase()) return s;",
  "failureScenario": "open_week_clash writes `'THIS WEEK: ' || upper(a) || ' v ' || upper(b) || ' — THE CLASH'`; the lowercase ' v ' separator makes the mixed-case guard true, so the board renders it in full caps directly beneath an eased row. The ALL SQUARE post has the same shape; the winner post is half-shouted.",
  "fix": "Normalize those bodies in a NEW migration, or teach easeCaps to treat 'mostly uppercase' as scoreboard voice.",
  "confidence": 8,
  "notes": "",
  "slice": "index.html 4578–5643",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "IDX04-14",
  "title": "Present reaction chips other than the heater are ~22px tall — under the 44px floor the surrounding CSS is written to hold",
  "file": "index.html",
  "line": "4918-4926 (CSS root at 1184-1191)",
  "severity": "P3",
  "kind": "opportunity",
  "category": "accessibility",
  "sessionCode": false,
  "quote": "    const quick=e==='🔥'?' quick':'';",
  "failureScenario": "Only the 🔥 chip gets `.quick`, and only `.rxchip.quick` carries the 44px floor. Once anyone reacts with another emoji, that chip renders as a bare `.rxchip` ≈22px tall, so joining an existing eagle on a phone is a sub-44px target 5px from its neighbours. `.rxpalette button` is ~29px. The CSS comments claim the 44px floor for 'the two tray triggers' — a floor scoped too narrowly.",
  "fix": "Give `.rxchip` and `.rxpalette button` the 44px floor and drop the `.quick`-only scoping.",
  "confidence": 7,
  "notes": "Root is the CSS block at 1184-1231; the emitter in range is where `.quick` is decided.",
  "slice": "index.html 4578–5643",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "idx05-01",
  "title": "pointsFor/bandName band an unrounded PvI; the engine bands round(pvi,1), so the ceremony and the epilogue disagree on the same post",
  "file": "index.html",
  "line": "5794-5798 (and 5802-5808)",
  "severity": "P1",
  "kind": "bug",
  "category": "scoring-correctness",
  "sessionCode": true,
  "quote": "  if(vs>-1)  return [7,'Right on your number. Steady points.'];\n  if(vs>=-3) return [6,'A little loose, still cash in the bank.'];",
  "failureScenario": "Index 12.4, rating 72.6 / slope 130, gross 88 → vs = -0.9862. Client: -0.9862 > -1 → 7 points and 'Played to it'. Server: v_rounds_ranked bands cup_points(round(-0.9862,1)) = cup_points(-1.0) = 6 and 'A little loose'. The post handler hands the client's values straight to the ceremony, so the golfer sees '+7 PTS' flash and the epilogue — reading the server's figures through the SAME bandName — immediately say 'A little loose · 6 pts'. 20 of 1201 sampled PvI values (~1.7%) disagree: the four 0.05-wide half-bands at ±1 and ±3.",
  "fix": "Round the input at the producer (recalc) the way the engine does — round the differential, then round the PvI — before banding.",
  "confidence": 9,
  "notes": "Verifiers corrected severity to P2/P3: prod shows 63 of 289 ranked rounds already disagree, but 62 of those are the missing 95% ALLOWANCE (POST-B-01/D123), not rounding. Rounding alone accounts for 1 of 289. The naive fix inside pointsFor would break tests/app-tests.js:43 and make prod worse (63→71); the fix belongs in recalc alongside the allowance.",
  "slice": "index.html 5644–6500 — draft board, band/stage/season producers, share artifacts, epilogue",
  "verification": {
   "status": "survived",
   "correctedSeverity": "P2/P3",
   "votes": [
    {
     "lens": "control-flow",
     "verdict": "CONFIRMED",
     "refuted": false,
     "correctedSeverity": "P2",
     "reasoning": "No rounding anywhere between recalc's vs and the ceremony, and the epilogue reads the server's rounded figures through the same producers, so the two surfaces are guaranteed to disagree in the four 0.05-wide half-bands. Reproduced the reviewer's numbers in node and confirmed the server's answer in prod. Two corrections: the server rounds the DIFFERENTIAL first, so rounding only vs inside pointsFor is incomplete and would break the pinned test at app-tests.js:43 — the fix belongs in recalc. Nothing is mis-banked; the ledger is correct."
    },
    {
     "lens": "intent-and-precedent",
     "verdict": "CONFIRMED",
     "refuted": false,
     "correctedSeverity": "P3",
     "reasoning": "Swept 289 real prod rounds: today 63 disagree with the server; the finding's fix alone makes it 71 (worse); the ALLOWANCE fix alone makes it 1; both make it 0. So rounding is the second-order term and D123's unbuilt allowance item is the first — and D123 already logs the whole class. The rounding sub-cause is genuinely new. Fix belongs at the producer, folded into D123 (3)."
    }
   ]
  }
 },
 {
  "id": "idx05-02",
  "title": "endgameLine promises the leader a +10 Cup Final head start in every structure; the engine grants it only at squads2",
  "file": "index.html",
  "line": "5936",
  "severity": "P2",
  "kind": "bug",
  "category": "scoring-correctness",
  "sessionCode": true,
  "quote": "  return `${who} seed into a four-week Cup Final${when ? ` from ${when}` : ''} — … The leader carries +10 in. …`;",
  "failureScenario": "A four-squad league (the wizard's highlighted default) gets meta.structure='squads4', so the note tells every member that the regular-season leader starts the Cup Final ten points up. enter_cup_final writes head_start 0 for anything but squads2, and the solo insert has no head_start column at all. index.html's own STRUCT_NOTES mentions +10 only for squads2, so the app contradicts itself between the standings note and the bylaws.",
  "fix": "Make the clause conditional on structure, or read meta from the server.",
  "confidence": 9,
  "notes": "Duplicate of IDX04-01 from the adjacent slice. In demo, meta is null and the fallback state.structure defaults to squads2 — the one case where the sentence is true.",
  "slice": "index.html 5644–6500",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "idx05-03",
  "title": "seasonState()/seasonNote() ignore the round's date, so a backdated round inside a live season is still previewed as scoring league points",
  "file": "index.html",
  "line": "5866-5886",
  "severity": "P2",
  "kind": "bug",
  "category": "correctness",
  "sessionCode": true,
  "quote": "function seasonState(){\n  const st = leagueStage();\n  if(!window.CS || !CS.league) return 'no_league';\n  if(st === 'forming' || st === 'drawing' || st === 'preseason') return 'pre';\n  if(st === 'complete') return 'over';\n  return 'live';\n}",
  "failureScenario": "Season runs May–Nov, today is Aug. The golfer sets #inDate to April 20 (catching up an old card). recalc calls seasonState(), which only looks at the league stage, reports 'live', and the panel promises 9 league points. On Post the handler correctly computes counts from played_on → false, so the ceremony reads 'COUNTS ON YOUR CARD' and the standings never move. That is the D122 complaint relocated from the pre-season case to the backdated case, and the post handler's own comment already says the rule must be keyed on the date.",
  "fix": "seasonState(playedIso) returning 'pre'/'over' when the round's date falls outside the season window, used by both recalc and finishCeremony.",
  "confidence": 8,
  "notes": "The two halves of the D122 fix live 900 lines apart and use different rules; 6893-6894 is the correct one.",
  "slice": "index.html 5644–6500",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "idx05-04",
  "title": "csShareLink claims 'Link copied' when there is no clipboard, and 'Could not make the link' when the link was made — the URL is never shown",
  "file": "index.html",
  "line": "6314-6316",
  "severity": "P2",
  "kind": "bug",
  "category": "silent-failure",
  "sessionCode": false,
  "quote": "    await navigator.clipboard?.writeText((text ? text + ': ' : '') + url);\n    toast('Link copied — no account needed to view it');\n  }catch(e){ toast(humanError(e, 'Could not make the link.')); }",
  "failureScenario": "Two directions, both wrong. If navigator.clipboard is undefined (insecure context, embedded WebView) the `?.` short-circuits, nothing throws, and the toast asserts the link was copied — but the create_share token was minted and the link is unrecoverable. If writeText rejects (routine on Safari/Firefox because the tap's transient activation was spent on the awaited RPC), the golfer is told 'Could not make the link.' about a link that exists and is live.",
  "fix": "Wrap the clipboard write in its own try/catch and present the URL itself in a selectable field on either failure. Never let the clipboard's success decide the share's success.",
  "confidence": 8,
  "notes": "The same function gets the navigator.share half right, which makes the clipboard half look like an oversight.",
  "slice": "index.html 5644–6500",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "idx05-05",
  "title": "A rejected navigator.share() throws away the download fallback and reports 'Could not make the card' for a card that was made",
  "file": "index.html",
  "line": "6092-6095 (and 6237-6240)",
  "severity": "P2",
  "kind": "bug",
  "category": "silent-failure",
  "sessionCode": false,
  "quote": "    if(navigator.canShare && navigator.canShare({files:[file]})){\n      await navigator.share({files:[file], text});\n      return;\n    }",
  "failureScenario": "shareRecapCard awaits a signed-URL fetch, an image decode and canvas.toBlob, which on a slow connection outlives the tap's transient user activation. canShare still returns true, so navigator.share() rejects with NotAllowedError — not AbortError — and control falls to the outer catch: 'Could not make the card.' The PNG is complete in `blob`, the download-plus-clipboard fallback is never reached, and nothing is saved. Identical shape in shareSettlementCard.",
  "fix": "Copy csShareLink's pattern: catch, return only on AbortError, otherwise fall through to the download branch.",
  "confidence": 7,
  "notes": "Three copies of one pattern in this range; only csShareLink is correct.",
  "slice": "index.html 5644–6500",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "idx05-06",
  "title": "A fourth band table in the weekly-clash RPC still uses the closed '>= -1' edge Q-20 removed everywhere else",
  "file": "supabase/migrations/20260829091000_weekly_clash.sql",
  "line": "261-265, 277-281, 321-323",
  "severity": "P2",
  "kind": "bug",
  "category": "duplicated-rule",
  "sessionCode": false,
  "quote": "'band', case when rr.pvi >= 3 then 'Torched it' … when rr.pvi >= -1 then 'Played to it' …",
  "failureScenario": "A best counting round at pvi exactly −1.0 scores 6 but is stamped 'Played to it' (the 7-point band's name) in a_best/b_best, and the client prefers the server's string, so the clash card renders the higher band's name beside the lower band's points while the round card, receipt and ceremony all say 'A little loose'. The board post takes the same wrong turn at :321.",
  "fix": "Change both `>= -1` to `> -1` in a NEW migration, or replace all three inline CASEs with a single `cup_band(numeric)` beside cup_points.",
  "confidence": 8,
  "notes": "Root outside the JS slice; reported because it is the same rule as bandName. Display only — the W is decided on points, despite a comment claiming the band decides.",
  "slice": "index.html 5644–6500",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "idx05-07",
  "title": "leagueStage tolerates a missing m.league on one line and dereferences it two lines later",
  "file": "index.html",
  "line": "5833-5840 (repeated at 5851)",
  "severity": "P3",
  "kind": "bug",
  "category": "correctness",
  "sessionCode": true,
  "quote": "  const lg = m ? m.league : (window.CS && CS.league);\n  …\n  const isCurrent = !m || (window.CS && CS.league && m.league.id === CS.league.id);",
  "failureScenario": "With a membership whose embedded league is null, line 5834 handles it but 5840 throws 'Cannot read properties of null (reading id)'. leagueStage is called for every membership from switcherChip and leaguePhaseLabel, so one bad row takes down the league switcher and the Home hub's group list. The rest of the file already treats m.league as nullable (`m.league?.phase` at four sites).",
  "fix": "`m.league?.id === CS.league.id` in both leagueStage and stageLabel.",
  "confidence": 5,
  "notes": "Could not construct a path that produces a null m.league; filed because the function contradicts itself.",
  "slice": "index.html 5644–6500",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "idx05-08",
  "title": "endgameLine's seats ternary is dead — every branch returns 2",
  "file": "index.html",
  "line": "5927",
  "severity": "P3",
  "kind": "bug",
  "category": "dead-code",
  "sessionCode": true,
  "quote": "  const seats = structure === 'solo' ? 2 : (structure === 'squads2' ? 2 : 2);",
  "failureScenario": "No wrong output today, but the line advertises a rule that varies by structure when it does not. climbCut thirty lines away returns K:1 for squads2, so the next person reconciling the two will 'fix' this arm to 1 and endgameLine will read 'The top 1 squads seed into a four-week Cup Final'.",
  "fix": "Collapse to `const seats = 2;` with a comment citing enter_cup_final.",
  "confidence": 8,
  "notes": "",
  "slice": "index.html 5644–6500",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "idx05-09",
  "title": "canvas.toBlob can hand back null and the File is built from it anyway, shipping a 4-byte 'null' PNG",
  "file": "index.html",
  "line": "6089-6090 (and 6229-6230)",
  "severity": "P3",
  "kind": "bug",
  "category": "correctness",
  "sessionCode": false,
  "quote": "    const blob=await new Promise(res=>drawRecapCard(d).toBlob(res,'image/png'));\n    const file=new File([blob],'cup-season-recap.png',{type:'image/png'});",
  "failureScenario": "toBlob invokes its callback with null when encoding fails (memory pressure on an older iPhone encoding a 1080x1350 canvas with a decoded photo). new File([null], …) coerces null to 'null' and produces a 4-byte file with a PNG MIME type; canShare accepts it, so a corrupt image is shared into the group thread with no error. Same at the settlement card.",
  "fix": "`if(!blob) throw new Error('card encode failed');` after each await — the third copy of this pattern already guards it.",
  "confidence": 7,
  "notes": "",
  "slice": "index.html 5644–6500",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "idx05-10",
  "title": "leagueStage calls every non-current league 'Season live' although leagueSpans already has its dates",
  "file": "index.html",
  "line": "5841",
  "severity": "P2",
  "kind": "opportunity",
  "category": "duplicated-rule",
  "sessionCode": true,
  "quote": "  if(!isCurrent) return 'season';",
  "failureScenario": "A golfer in two leagues sees the second labelled 'Season live' in the switcher and the Home hub row; tapping in flips the same producer to 'Before first tee · Sun Sep 6 · 8 days'. The same league described two ways in ten seconds — the failure D120's comment says this function exists to end.",
  "fix": "window.leagueSpans already holds starts_on/ends_on/status per membership; look the span up by m.league.id and return preseason/final/season accordingly.",
  "confidence": 8,
  "notes": "Also fixes the switcher's silence about a league that has entered its Cup Final.",
  "slice": "index.html 5644–6500",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "idx05-11",
  "title": "leagueStage derives 'Cup Final' from a client date guess rather than the season's server status",
  "file": "index.html",
  "line": "5843",
  "severity": "P3",
  "kind": "opportunity",
  "category": "correctness",
  "sessionCode": true,
  "quote": "  try { if(typeof isCupFinal === 'function' && isCupFinal()) return 'final'; } catch(_){}",
  "failureScenario": "isCupFinal() is `Date.now() >= seasonE() - 27 days`. The server flips seasons.status via the daily tick, and CLAUDE.md warns pg_cron must be confirmed or the tick will not fire. If it has not, every stage chip announces 'Cup Final' while cup_finalists is empty and the standings are still the regular-season table.",
  "fix": "Prefer `CS.season?.status === 'cup_final'` first and keep isCupFinal() only as the pre-tick fallback.",
  "confidence": 7,
  "notes": "",
  "slice": "index.html 5644–6500",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "idx05-12",
  "title": "vsPhrase and bandName lack the null guard vsShort was given for exactly this hazard",
  "file": "index.html",
  "line": "5973-5978 and 5802-5808",
  "severity": "P3",
  "kind": "opportunity",
  "category": "correctness",
  "sessionCode": true,
  "quote": "function vsShort(v){\n  /* null/undefined/'' must NOT become 0 … */\n  if(v===null || v===undefined || v==='') return '—';",
  "failureScenario": "vsPhrase(null) → Number(null) is 0 → 'played to your number'; bandName(null) → null > -1 is true → 'Played to it'. Every current caller gates on != null, but index.html:4712 is one step away: on a pre-D108 database a round whose pvi is null would render 'PLAYED TO IT' on the clash card — an absent number reading as a decent round, the precise failure vsShort's comment describes.",
  "fix": "Add the same null guard to both.",
  "confidence": 8,
  "notes": "Latent, not live — checked all six vsPhrase call sites.",
  "slice": "index.html 5644–6500",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "idx05-13",
  "title": "app-tests pins the band operator but not the band input, which is why finding idx05-01 shipped",
  "file": "tests/app-tests.js",
  "line": "37-52",
  "severity": "P3",
  "kind": "opportunity",
  "category": "test-coverage",
  "sessionCode": true,
  "quote": "  t('bands: -1.0 scores 6, like cup_points', pointsFor(-1.0)[0], 6);",
  "failureScenario": "Every case in the Q-20 block feeds a value already at one-decimal precision, so the suite passes whether or not the client rounds. pointsFor(-0.96) is never tested, so idx05-01 was written, reviewed and shipped under a green suite that claims to pin the client to the engine.",
  "fix": "Add rounding assertions to the same block and step the sweep by 0.01 against a reference that rounds first.",
  "confidence": 8,
  "notes": "The comment on line 5793 already promises 'app-tests pin this'.",
  "slice": "index.html 5644–6500",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "idx05-14",
  "title": "The Q-23 comment is orphaned above the stage vocabulary it does not describe",
  "file": "index.html",
  "line": "5809-5813",
  "severity": "P3",
  "kind": "opportunity",
  "category": "dead-code",
  "sessionCode": true,
  "quote": "/* Q-23 · the compact form of vsPhrase, for tiles and chips. … One vocabulary, no signs. */",
  "failureScenario": "vsShort moved to 5938 when the D120 block landed between the comment and its function, so this paragraph now sits directly above `const STAGE_LABEL` and reads as the rationale for the stage vocabulary. The next reader looking for why STAGE_LABEL exists finds an explanation of golf sign conventions.",
  "fix": "Move the paragraph down to sit above `function vsShort(v)`.",
  "confidence": 9,
  "notes": "Pure hygiene, but this file's comments carry the decision history and a misfiled one is worse than none.",
  "slice": "index.html 5644–6500",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "POST-B-01",
  "title": "The composer previews points at 100% allowance while the engine scores at the league's (default 95%)",
  "file": "index.html",
  "line": "6594, 6606",
  "severity": "P1",
  "kind": "bug",
  "category": "scoring-correctness",
  "sessionCode": false,
  "quote": "    vs=state.myIndex-diff;\n    [pts,msg]=pointsFor(vs);",
  "failureScenario": "League on the default settings (handicap_allowance 95 in every one of the 18 prod leagues). Index 12.4, 84 gross at 71.5/125: client diff 11.3, vs 1.1 → 9 League points promised. Server: playing_index round(12.4*95/100,1)=11.8, pvi 0.5 → cup_points 7. The ceremony shows 9; the board card and receipt render the server's +0.5 and 7 PTS. The gap is 0.05*index (0.62 strokes at 12.4, 1.0 at a 20 index), so it straddles a band constantly. Verified in prod: 288 of 289 ranked rounds differ from the naive figure and 71 straddle a band.",
  "fix": "Apply the league allowance in recalc from CS.settings.handicap_allowance (already loaded), plus a quiet 'Index used 14.2 × 95% = 13.5' row.",
  "confidence": 9,
  "notes": "Logged and decided as D123 (2026-08-29) and NOT yet built. Its sibling half (half-open bands) WAS built as Q-20. Verifiers note home_feed also computes pvi at 100%, so D123 items (2) and (3) must ship together or the mismatch just moves.",
  "slice": "index.html 6500–7458 — the rest of the post composer",
  "verification": {
   "status": "survived",
   "correctedSeverity": "P1",
   "votes": [
    {
     "lens": "control-flow",
     "verdict": "CONFIRMED",
     "refuted": false,
     "correctedSeverity": "P1",
     "reasoning": "Reachable and unguarded on the shipped path: state.myIndex is raw at all four assignment sites, v_rounds_ranked applies the allowance once to a raw index_at_post, every prod league is at the 95% default, and the 100% points are DISPLAYED exactly when the server scores the round. The disagreeing number is one screen away — the board card and the receipt both read the server's pvi. D123 documents the defect and prescribes the fix; it is unbuilt."
    },
    {
     "lens": "intent-and-precedent",
     "verdict": "CONFIRMED",
     "refuted": false,
     "correctedSeverity": "P1",
     "reasoning": "Spec §2.1 defines PvI as Playing Index minus Differential and the engine implements it; the composer has state.preset in hand and ignores it. git log -S shows the line unchanged since the initial commit — never fixed, reverted or knowingly accepted. Two corrections: home_feed also computes pvi at 100%, so the Home card AGREES with the preview and the contradiction appears one tap later in round_card — meaning a client-only patch would trade one mismatch for another; D123 items (2) and (3) must ship together."
    }
   ]
  }
 },
 {
  "id": "POST-B-02",
  "title": "Every post-success step runs inside the try whose catch says \"Post failed.\" — a render throw makes a saved round read as a failure",
  "file": "index.html",
  "line": "6791-6940",
  "severity": "P1",
  "kind": "bug",
  "category": "silent-failure",
  "sessionCode": false,
  "quote": "      if(window.loadStandingsAndFeed) await window.loadStandingsAndFeed();\n…\n    } catch(e){ toast(humanError(e, 'Post failed.')); }",
  "failureScenario": "The insert succeeds and the round is in the database, then a throw inside the awaited loadStandingsAndFeed chain lands in the catch and toasts 'Post failed.' The golfer re-enters the round and posts a genuine duplicate that scores twice.",
  "fix": "Close the try immediately after the insert; run the ceremony, loads and epilogue in a second block whose catch never claims the post failed.",
  "confidence": 8,
  "notes": "DROPPED — both verifiers refuted it. finishCeremony fires BEFORE the awaited load and opens #finish, a fixed inset:0 opaque curtain at z-index 1100 over the z-1000 toast, so the toast cannot be seen; and the DB calls inside loadStandingsAndFeed return {error} rather than rejecting. The structural observation stands; the failure does not.",
  "slice": "index.html 6500–7458",
  "verification": {
   "status": "dropped",
   "correctedSeverity": null,
   "votes": [
    {
     "lens": "control-flow",
     "verdict": "REFUTED",
     "refuted": true,
     "reasoning": "The structural observation is right but the failure is not reachable. finishCeremony fires at 6893, BEFORE the form clear and before the awaited loadStandingsAndFeed, and it opens #finish — a fixed inset:0 opaque curtain at z-index 1100 over the z-1000 toast — so the 'Post failed' toast cannot be seen. Separately the DB calls inside loadStandingsAndFeed return {error} rather than rejecting."
    },
    {
     "lens": "intent-and-precedent",
     "verdict": "ALREADY_HANDLED",
     "refuted": true,
     "reasoning": "Same conclusion: the curtain is already the guard. The golfer sees the ceremony, not the toast, so the duplicate-post scenario cannot start."
    }
   ]
  }
 },
 {
  "id": "POST-B-03",
  "title": "A blank slope previews at 113 and posts as null — the Q-22 guard's slope half is dead code",
  "file": "index.html",
  "line": "6576, 6584, 6787",
  "severity": "P1",
  "kind": "bug",
  "category": "correctness",
  "sessionCode": true,
  "quote": "  const slope=parseFloat($('#inSlope').value)||113;\n…\n  if((f9>0 || b9>0) && (!(rating>0) || !(slope>0))){\n…\n    const rating=parseFloat($('#inRating').value), slope=parseInt($('#inSlope').value);",
  "failureScenario": "Golfer types a course by hand, types Rating 71.4 off the scorecard, leaves Slope blank (the card prints the rating more prominently), enters 41/43. recalc: slope = '' || 113, so `!(slope>0)` is FALSE and the new Q-22 guard does not fire; the panel promises real points against a fabricated 113. Tap Post: parseInt('') = NaN → `\"slope\":null` → rounds.slope is integer NOT NULL → 'Post failed. That didn't go through — please try again.' No field is marked, the panel still shows points, and every retry fails identically. The same unwinnable loop Q-21 just fixed for the date, inside the guard written to prevent it.",
  "fix": "Drop the `||113` so the guard catches a blank slope the way it catches a blank rating, and add a /slope/ branch to humanError.",
  "confidence": 9,
  "notes": "The `||113` predates the session; the new guard is what makes the gap a shipped promise. Verifier note: the server error is a division-by-zero inside score_round's BEFORE trigger, before the range CHECKs — fail-closed twice over.",
  "slice": "index.html 6500–7458",
  "verification": {
   "status": "survived",
   "correctedSeverity": "P1",
   "votes": [
    {
     "lens": "control-flow",
     "verdict": "CONFIRMED",
     "refuted": false,
     "correctedSeverity": "P1",
     "reasoning": "The two halves of the Q-22 guard are asymmetric: blank rating falls to ||0 and fires the guard; blank slope falls to ||113 so `!(slope>0)` is always false. Verified the JSON serialisation (parseInt('') → null) and confirmed against PROD that rounds.slope is NOT NULL with no default. Walked humanError in order and confirmed it lands on the generic constraint sentence with no field marked. One correction: the slope half is not strictly dead (a typed negative fires it) — it is dead for blank and '0', which is where the failure lives."
    },
    {
     "lens": "intent-and-precedent",
     "verdict": "CONFIRMED",
     "refuted": false,
     "correctedSeverity": "P1",
     "reasoning": "113 is the WHS scaling constant, documented nowhere as a fallback for an unknown slope; the ||113 is unannotated and untouched since the initial commit. The guard's own copy ('Pick a tee — or type the rating and slope') states the author's intent, which ||113 three lines above makes unreachable. Hand-typing a course is a first-class supported path the guard itself invites. The preview also fabricates a differential off the invented slope — ~1.6 strokes optimistic on a 130-slope course."
    }
   ]
  }
 },
 {
  "id": "POST-B-04",
  "title": "A 9-hole scorecard scan mints partner claim links as 18-hole rounds",
  "file": "index.html",
  "line": "6868-6872, 7127",
  "severity": "P1",
  "kind": "bug",
  "category": "scoring-correctness",
  "sessionCode": false,
  "quote": "        scanCtx = { others: state.post.scan.others, course_label: payload.course_label, rating: payload.rating, slope: payload.slope, played_on: payload.played_on, holes: 18, };",
  "failureScenario": "Two golfers play a nine and scan the card. scanApply correctly detects the nine and the poster's own payload carries holes_played: 9, but scanCtx hardcodes holes: 18 and scanPartnersSheet passes it into create_scan_claim. When the partner opens /?claim=…, claim_scan_round computes v_nine false and inserts holes_played=18 with nine_rating null: a 9-hole gross of 45 scored as an 18-hole round, differential ≈ −23.9, pvi ≈ +35.7 → the maximum band, plus a −23.9 differential fed into the WHS-lite engine which wrecks their index for 20 rounds. Second defect on the same line: `rating: payload.rating` is already a 9-hole rating when a real 9-hole tee was picked.",
  "fix": "`holes: (nine ? 9 : 18)` — `nine` is already in scope — and send an 18-hole-equivalent rating.",
  "confidence": 8,
  "notes": "Needs an unclaimed partner row on a 9-hole scan. The scan prompt explicitly supports 9-hole cards and the partner filter admits a 9-hole row. Rounds are immutable, so the remedy is delete_round after the victim notices.",
  "slice": "index.html 6500–7458",
  "verification": {
   "status": "survived",
   "correctedSeverity": "P1",
   "votes": [
    {
     "lens": "control-flow",
     "verdict": "CONFIRMED",
     "refuted": false,
     "correctedSeverity": "P1",
     "reasoning": "No guard on the path: the Edge Function prompt explicitly instructs 9-hole cards, normalize() keeps such rows, the client filter admits them, create_scan_claim records the literal 18, and claim_scan_round has no independent hole inference (its gross<18 sanity check passes for any real nine). The poster's own round is correct, which is what makes the divergence silent. The rating half is a genuine contract mismatch, so the naive one-line fix would double-halve a real 9-hole tee — both halves are needed."
    },
    {
     "lens": "intent-and-precedent",
     "verdict": "CONFIRMED",
     "refuted": false,
     "correctedSeverity": "P1",
     "reasoning": "Not by design: create_scan_claim's signature is p_holes int default 18 and claim_scan_round carries an explicit v_nine branch — the database contract was written to accept a nine, and that branch is dead solely because the client hardcodes 18. git shows the hardcode written when the app was 18-only and never revisited when D72/D73 taught the composer and the live sheet about nines. scan_claims is empty in prod — pre-launch timing, not a mitigation."
    }
   ]
  }
 },
 {
  "id": "POST-B-05",
  "title": "Retyping the same course query silently kills the search — lastQ survives hide()",
  "file": "index.html",
  "line": "7305-7310",
  "severity": "P2",
  "kind": "bug",
  "category": "correctness",
  "sessionCode": false,
  "quote": "    if(q.length<3 || state.demo || !window.CS?.user){ hide(); return; }\n    if(q===lastQ){ return; } lastQ=q;",
  "failureScenario": "Type 'tpc' → search runs, lastQ='tpc'. Backspace to 'tp' → the length branch calls hide() and returns WITHOUT clearing lastQ. Type 'c' back → q===lastQ → early return → no search, no dropdown, no spinner, no error. The composer is inert for that query until the golfer types a different string. Same on the more common blur-and-retype shape. This is the silence the audit's competitive golfer hit.",
  "fix": "Reset the memo in hide(), or key the guard on 'already rendered AND visible'; add a focus handler that re-shows the last result set.",
  "confidence": 8,
  "notes": "",
  "slice": "index.html 6500–7458",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "POST-B-06",
  "title": "A late course-search response re-opens the dropdown after the golfer has already picked a tee",
  "file": "index.html",
  "line": "7206, 7274, 7286",
  "severity": "P2",
  "kind": "bug",
  "category": "race",
  "sessionCode": false,
  "quote": "  const hide=()=>{ dd.style.display='none'; dd.innerHTML=''; dd.dataset.stage=''; };\n…\n    const fresh = () => lastQ===q && dd.dataset.stage!=='tees';",
  "failureScenario": "The courses function is slow (the pilot logged 502s). The golfer picks a course (stage='tees', which correctly blocks a late response) then picks a tee: the tee handler calls hide(), resetting stage to ''. Two seconds later the in-flight response resolves; lastQ is unchanged (the tee handler sets input.value programmatically, firing no input event), so fresh() is TRUE and a 230px list inserts itself between the course field and Rating. The golfer reaching for Rating taps a course button, which overwrites the field and discards the tee they just picked.",
  "fix": "Make hide() a terminal state for in-flight requests (a 'closed' stage or a request id compared at render time).",
  "confidence": 8,
  "notes": "",
  "slice": "index.html 6500–7458",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "POST-B-07",
  "title": "Post blames the nines when the real blocker is a missing rating",
  "file": "index.html",
  "line": "6750",
  "severity": "P2",
  "kind": "bug",
  "category": "copy-contradiction",
  "sessionCode": true,
  "quote": "  if(!state.lastPost){toast('Enter at least one nine first');return;}",
  "failureScenario": "Golfer enters 41 and 43, no course and no rating. The Q-22 guard nulls state.lastPost and the panel correctly reads 'Pick a tee — or type the rating and slope to see the points.' They tap Post and are told 'Enter at least one nine first' — pointing at the two boxes they have just filled. state.lastPost is now overloaded: it means both 'no scores' and 'no rating', and one toast can only tell one story.",
  "fix": "Branch on the real state before the toast and mark #inRating, the same treatment Q-21 gave the date.",
  "confidence": 9,
  "notes": "The toast line is old; the new Q-22 guard is what routes a missing-rating case into it.",
  "slice": "index.html 6500–7458",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "POST-B-08",
  "title": "The post-card draft is stored under one global key and survives sign-out — the next account inherits it",
  "file": "index.html",
  "line": "6645-6684",
  "severity": "P2",
  "kind": "bug",
  "category": "data-integrity",
  "sessionCode": false,
  "quote": "const POST_DRAFT_KEY='cs_post_draft';",
  "failureScenario": "Golfer A half-fills the composer on a shared phone and signs out; neither sign-out handler removes cs_post_draft. Golfer B signs in on the same device and restorePostDraft() — gated only on _draftRestored, state.demo and CS.user, none of which is identity — fills B's composer with A's numbers and toasts 'Your unposted round came back'. B taps Post and A's round lands on B's card and in B's leagues. Secondary: delete_account promises PII is scrubbed, but the deleted golfer's gross/course/date stay in localStorage.",
  "fix": "Scope the key to the account and stamp uid inside the payload so restore can refuse a mismatch; clear it on both sign-out handlers.",
  "confidence": 8,
  "notes": "",
  "slice": "index.html 6500–7458",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "POST-B-09",
  "title": "\"Start over\" leaves the composer stuck in 9-hole mode, unlike the post-success reset",
  "file": "index.html",
  "line": "7145-7165",
  "severity": "P2",
  "kind": "bug",
  "category": "correctness",
  "sessionCode": false,
  "quote": "function resetPostComposer(){\n  ['inF9','inB9','inRating','inSlope','inCourse','inDate'].forEach(id=>{ const e=$('#'+id); if(e) e.value=''; });\n…\n  state.post.mode='total';",
  "failureScenario": "Golfer starts a 9-hole card then taps 'Start over — clear this card'. The reset clears values, mode, scores, touched and scan but never state.post.side or state.post.rating9 — while the post-success reset explicitly does. renderPostHoles re-renders with side===9: the back-nine box stays hidden and the label still reads '9-hole gross'. Their next round is 18 holes; they type 84 into the one visible box and it posts as holes_played=9 with nine_rating=rating/2 — differential ≈ +87, five points, and a catastrophic entry into the auto-handicap engine.",
  "fix": "Reset side/rating9/pars in resetPostComposer and re-sync the #postSide control; better, extract one resetPostCard() used by both paths.",
  "confidence": 8,
  "notes": "Related desync: the post-success path sets side=18 but never re-syncs the segmented control.",
  "slice": "index.html 6500–7458",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "POST-B-10",
  "title": "19x19px steppers are the confirmation surface for every scanned scorecard",
  "file": "index.html",
  "line": "6556-6560",
  "severity": "P2",
  "kind": "bug",
  "category": "accessibility",
  "sessionCode": false,
  "quote": "        <div class=\"prow\"><button type=\"button\" data-phm=\"${i}\" aria-label=\"Minus hole ${i+1}\">−</button>…",
  "failureScenario": "index.html:980 sizes these 19×19px with a 3px gap, and scanApply makes this grid THE surface where the golfer corrects the model's misreads (the D36 contract: the receipt stores what the GOLFER confirmed). A golfer fixing a misread 6 to a 5 hits + instead of − and posts a 7; the receipt then records the wrong score as confirmed. Eighteen such pairs per card.",
  "fix": "Grow the hit area with a pseudo-element inset or min-width/min-height 44px, and give the value an aria-live region.",
  "confidence": 7,
  "notes": "The size lives at 980 (see CSS-11); the markup that depends on it is in slice.",
  "slice": "index.html 6500–7458",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "POST-B-11",
  "title": "state.post.rating9 is cleared one input event after recalc has already read it",
  "file": "index.html",
  "line": "6634-6638",
  "severity": "P3",
  "kind": "bug",
  "category": "race",
  "sessionCode": false,
  "quote": "['inRating','inSlope','inF9','inB9'].forEach(id=>$('#'+id)?.addEventListener('input',recalc));\n$('#inRating')?.addEventListener('input',()=>{ state.post.rating9=false; });",
  "failureScenario": "Listeners fire in registration order, so recalc runs BEFORE the flag is cleared on the same event. On a real 9-hole tee, a single terminal paste of a corrected rating makes recalc compute with rating9 still true and show plausible points; THEN the flag flips false and no further input arrives. Post sends nine_rating = rating/2, so preview and posted round disagree by ~30 strokes of differential.",
  "fix": "Merge the two handlers: clear the flag and then recalc.",
  "confidence": 6,
  "notes": "Narrow — needs a single terminal input event on the rating field of a real 9-hole tee. The D72 rule itself is deliberate; the ordering is not.",
  "slice": "index.html 6500–7458",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "POST-B-12",
  "title": "No plausibility bounds on rating or slope — a typo posts as maximum points",
  "file": "index.html",
  "line": "6576, 6584, 6787",
  "severity": "P3",
  "kind": "opportunity",
  "category": "validation",
  "sessionCode": false,
  "quote": "  if((f9>0 || b9>0) && (!(rating>0) || !(slope>0))){",
  "failureScenario": "The only test is > 0. '715' instead of '71.5' fits numeric(4,1) and rounds has no CHECK on rating, so the round inserts with differential −627 and takes the top band, banked in the standings. The panel does say 'You torched your number by 639.0', but nothing stops the tap. Slope has the mirror problem: legal is 55–155, and 0 reaches the server as a divide-by-zero.",
  "fix": "Bound both in recalc's guard (rating 55–80, slope 55–155) and name which one is out of range; a CHECK constraint would make it enforceable.",
  "confidence": 7,
  "notes": "This also closes POST-B-03's blank slope with no extra code.",
  "slice": "index.html 6500–7458",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "POST-B-13",
  "title": "The date is not reset after a successful post — the next round silently inherits the last one's date",
  "file": "index.html",
  "line": "6910-6919",
  "severity": "P3",
  "kind": "opportunity",
  "category": "correctness",
  "sessionCode": false,
  "quote": "      $('#inF9').value=''; $('#inB9').value='';\n      $('#inCourse').value=''; …",
  "failureScenario": "A golfer scans a card from three weeks ago (scanApply overwrites the date with the printed one) and posts. The success reset clears everything but #inDate, and clearPostDraft() removes the only other copy, so nothing restores today. Tonight's round goes in dated three weeks ago, outside the current month's counting window or the season window entirely — and rounds are immutable.",
  "fix": "Add the same two lines resetPostComposer already carries, using isoOf() to avoid the UTC-midnight landmine.",
  "confidence": 8,
  "notes": "",
  "slice": "index.html 6500–7458",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "POST-B-14",
  "title": "The course-cache reads drop {error} — a missing grant is indistinguishable from an uncached course, forever",
  "file": "index.html",
  "line": "7355, 7360, 7396, 7403",
  "severity": "P3",
  "kind": "opportunity",
  "category": "silent-failure",
  "sessionCode": false,
  "quote": "    const { data: tees } = await window.sb.from('api_course_tees').select(…).eq('course_id', course.id);\n    if(!tees || !tees.length) return;",
  "failureScenario": "supabase-js never throws, so a 42501 on a new api_course_tees column returns data=null and the function silently returns. Real per-hole pars and stroke index never load, for every course, for every golfer, permanently — and the symptom is identical to 'this course isn't cached yet', so nobody investigates. The catch never fires because nothing threw.",
  "fix": "Destructure the error and route it to qaEvent with a step tag; same at both call sites. Also note round_epilogue at 6936 destructures only data under a comment claiming the catch handles a missing RPC.",
  "confidence": 8,
  "notes": "",
  "slice": "index.html 6500–7458",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "POST-B-15",
  "title": "A failed claim-link copy says the link is on screen; it never is, and it says the link wasn't made when it was",
  "file": "index.html",
  "line": "7118-7136",
  "severity": "P3",
  "kind": "opportunity",
  "category": "copy-contradiction",
  "sessionCode": false,
  "quote": "      await navigator.clipboard?.writeText(`${location.origin}/?claim=${p._token}`);\n      b.textContent='Copied ✓';\n…\n    }catch(err){ toast(humanError(err, 'Could not make the link.')); b.disabled=false; }",
  "failureScenario": "On a clipboard rejection the golfer reads 'Could not make the link. Copy didn't work — the link is on screen to copy by hand.' Both halves are false: create_scan_claim already succeeded (one of eight daily claims spent) and the sheet never renders the URL. The growth funnel also under-counts, because both events sit after the clipboard await.",
  "fix": "Wrap only the RPC in the 'could not make the link' try; on a clipboard rejection render the literal URL in a selectable field. Move the events to immediately after the token is cached.",
  "confidence": 8,
  "notes": "",
  "slice": "index.html 6500–7458",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "POST-B-16",
  "title": "A failed insert orphans the photo already uploaded to the private media bucket",
  "file": "index.html",
  "line": "6810-6819",
  "severity": "P3",
  "kind": "opportunity",
  "category": "data-integrity",
  "sessionCode": false,
  "quote": "          const { error: upErr } = await sb.storage.from('media').upload(path, state.post.photo, …);",
  "failureScenario": "The photo uploads before the rounds insert. If the insert fails (POST-B-03's blank slope, a gross out of range, a network drop) the object stays in the bucket with no row referencing it, and because the uuid is regenerated each attempt, every retry adds another copy. Nothing in the composer, delete_round or delete_account removes it.",
  "fix": "Upload after the insert succeeds and PATCH photo_path onto the row, or remove the object in the catch.",
  "confidence": 7,
  "notes": "",
  "slice": "index.html 6500–7458",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "IDX07-01",
  "title": "An index of exactly 0 becomes 18 on every live-round resume path",
  "file": "index.html",
  "line": "8041 (also 7899, 7906, 7993, 8033, 8424, 8426)",
  "severity": "P1",
  "kind": "bug",
  "category": "scoring-correctness",
  "sessionCode": false,
  "quote": "return { n:(pl.guest_name||'Guest'), i:Number(pl.guest_index)||18, ci:-1, team:null,",
  "failureScenario": "A scratch golfer is added as a guest with index 0. #gAdd handles this correctly and start_live_round stores guest_index 0. Match play for $20 a side; the strokes ladder is right. Someone's phone reloads mid-round and every resume path rebuilds LIVE with `Number(0) || 18`, so the scratch player is now an 18: recomputeStrokes gives him CH 20 instead of 0, LOWCH flips, and strokeOn hands him a stroke on every hole while taking the strokes the rest of the group was owed. The settlement card and board story pay out on the corrupted ladder. Same falsy-zero for a member profile whose index_current is 0.0.",
  "fix": "One null-safe helper used on every index read (7899, 7906, 7993, 8033, 8041, 8424, 8426 — and 9386, 14965, 15629), plus a preflight grep for `Number(...)||18`.",
  "confidence": 9,
  "notes": "Verifiers extended it: 7899/7906/9386 fire at SETUP too, so a scratch member or picker-added golfer is an 18 from the tee; and the `est` badge goes the wrong way, rendering a confident '18.0 IDX' for a golfer the server recorded as index_source='self'. Season scoring is unaffected (finish_live_round posts gross/rating/slope and score_round resolves the index server-side) — the damage is the side-game strokes ladder and the cash friends settle.",
  "slice": "index.html 7459–8478 — pot, league setup, live round",
  "verification": {
   "status": "survived",
   "correctedSeverity": "P1/P2",
   "votes": [
    {
     "lens": "control-flow",
     "verdict": "CONFIRMED",
     "refuted": false,
     "correctedSeverity": "P1",
     "reasoning": "Every guard I looked for is absent: no min on #gIdx, no clamp at add time, no server normalisation (nullif catches '' not '0'), no early return blocking the resume, no later re-derivation. The zero survives add → tee-off → server → snapshot and is destroyed only on rebuild. p_result is computed client-side from the corrupted ladder and accepted by finish_live_round. Two corrections: 7899/7906 apply the same coercion at SETUP, and the fix list misses 9386, 14965 and 15629."
    },
    {
     "lens": "intent-and-precedent",
     "verdict": "CONFIRMED",
     "refuted": false,
     "correctedSeverity": "P2",
     "reasoning": "Reproduced the ladder flip with the app's own arithmetic. No decision authorises 0→18; the code's own definition of the fallback trigger is NULL (`est: p.index_current==null`), and D133 asks for the estimate to be announced where it decides money. Nothing server-scored is touched — score_round resolves index_at_post from profiles — so season points, differentials, standings and the pot ledger are all correct; what breaks is the side-game ladder and the cash friends settle, at exactly one trigger value."
    }
   ]
  }
 },
 {
  "id": "IDX07-02",
  "title": "Pot tab's champion figure disagrees with the settlement engine by a dollar, and no longer honours payout_champ",
  "file": "index.html",
  "line": "7484, 7497–7501",
  "severity": "P2",
  "kind": "bug",
  "category": "money-correctness",
  "sessionCode": true,
  "quote": "const runnerUp = Math.round(total*pr/100);\n    const king     = Math.round(total*pk/100);\n    $('#pay1').textContent=fmt$(total - runnerUp - king);",
  "failureScenario": "5 members at $50, split 60/25/15 → total 250. runnerUp = round(62.5) = 63; king = round(37.5) = 38; pay1 = 149. The engine settles in CENTS: c_cents = 25000 − 6250 − 3750 = 15000 → the champion is actually paid $150.00. Q-28 traded a trio that summed wrong for a trio that sums right and whose largest tile is wrong. Separately `pc` is destructured and never read, so a payout trio that does not total 100 silently gives the whole difference to the champion.",
  "fix": "Do the split in cents where the engine does it, and format from cents; assert pc+pr+pk === 100.",
  "confidence": 8,
  "notes": "CROSS-RANGE: #lineSplit at 12590 did NOT get the Q-28 fix and still prints three independent rounds, so the League Room now shows two different champion figures on two panes.",
  "slice": "index.html 7459–8478",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "IDX07-03",
  "title": "A live round is destroyed and declared \"already finished\" when the client merely can't see it",
  "file": "index.html",
  "line": "8194–8200, 8205–8212, 8225–8229",
  "severity": "P2",
  "kind": "bug",
  "category": "silent-failure",
  "sessionCode": false,
  "quote": "    clearLiveCache(null); state.live=freshLive();\n    …\n    toast('That round was already finished');",
  "failureScenario": "Two paths. (a) A visitor's my_visitor_rounds call fails: the error is discarded and the catch is empty, RLS blocks the direct select (which is why the RPC exists), so openIds is empty and every cs.live.* key is deleted, state.live is reset, and the user is told the round finished — about a round still status='live' on the server. (b) Tee off Friday, reopen Sunday: LIVE_MAX_AGE filters the still-live round out and the same wipe fires, even though the comment promises 'Scrap this round still closes it if they open it' — which they now cannot, because state.live was reset.",
  "fix": "Re-read the one id's status before clearing and clear only on a non-'live' status; on an aged-out round keep the snapshot and change the banner copy; on an RPC error keep the local resume.",
  "confidence": 7,
  "notes": "Confirmed my_visitor_rounds' return shape matches the client's filter, so a SUCCESSFUL call is handled correctly — the bug is entirely in the failure and age paths.",
  "slice": "index.html 7459–8478",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "IDX07-04",
  "title": "The member's buy-in row still looks tappable, and announces \"check mark\" when unpaid",
  "file": "index.html",
  "line": "7527–7530 (with CSS at 485–498)",
  "severity": "P2",
  "kind": "bug",
  "category": "accessibility",
  "sessionCode": true,
  "quote": "const row=document.createElement(isPro ? 'button' : 'div');\n      row.className='payer'+(paid?' paid':'');\n      row.innerHTML=`<span>…</span><span class=\"tick\">✓</span>`;",
  "failureScenario": "The element changed from button to div but the class did not, and `.payer{cursor:pointer}` plus `.payer:focus-visible` still apply. A member hovers their own name, gets a hand cursor, clicks, and nothing happens — the explanation that used to fire on that click was deleted in the same hunk, so a silent no-op replaced an explained refusal. Second, D129 specified 'the ✓ absent from the DOM when unpaid'; the ✓ is emitted on every row and only hidden by color:transparent, which does not remove text from the accessibility tree — VoiceOver reads an UNPAID member's row as 'Casey, check mark'.",
  "fix": "Give the member row its own class with no pointer affordance, emit the tick only when paid, and give the unpaid row a real status word.",
  "confidence": 8,
  "notes": "`.payer` has min-height:44px, so the tap size is fine; the problem is the affordance and the announced state.",
  "slice": "index.html 7459–8478",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "IDX07-05",
  "title": "The Pro's only route into the payment terms is a ~21px inline link",
  "file": "index.html",
  "line": "7514, 7516",
  "severity": "P3",
  "kind": "bug",
  "category": "accessibility",
  "sessionCode": true,
  "quote": "payBox.innerHTML = `<a href=\"#\" data-payedit style=\"color:var(--brand); text-decoration:underline\">Say how the crew pays you</a> — they can't guess.`;",
  "failureScenario": "#payHow is `<p class=\"fine\">` at 13px/1.6, so both anchors are ≈21px tall inline targets — well under the 44px minimum enforced two rules above on `.payer`. On a phone the single control that answers the audit's number-one question ('who do I pay, how, by when') is the smallest target on the pane, inside a run of body text.",
  "fix": "Render the empty state as a real `.mini` button padded to 44px; keep the inline 'edit' only as the secondary affordance.",
  "confidence": 8,
  "notes": "",
  "slice": "index.html 7459–8478",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "IDX07-06",
  "title": "The demo Pot tab renders a \"How to pay\" heading with nothing under it",
  "file": "index.html",
  "line": "7463–7472 (markup 3550–3552)",
  "severity": "P3",
  "kind": "bug",
  "category": "demo-diorama",
  "sessionCode": true,
  "quote": "const payBox = $('#payHow');\n    if(payBox){",
  "failureScenario": "#payHow is written only inside the `if(!state.demo)` branch, and the stake===0 branch returns before reaching it. The diorama initialises with stake 75 and demo true, so the Pot tab is visible and renderPot takes the demo branch, populating everything except #payHow. A signed-out visitor exploring the demo League Room sees the eyebrow 'How to pay' over an empty paragraph — the only element on that pane with no content, on the surface a prospective Pro judges the product by.",
  "fix": "Write the section explicitly in both non-real branches, or hoist a setPayHow(text|null) that also toggles the eyebrow.",
  "confidence": 9,
  "notes": "",
  "slice": "index.html 7459–8478",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "IDX07-07",
  "title": "The season guard in the buy-in tap handler is duplicated verbatim",
  "file": "index.html",
  "line": "7533–7534",
  "severity": "P3",
  "kind": "bug",
  "category": "dead-code",
  "sessionCode": true,
  "quote": "if(!window.CS?.season?.id){ toast('Buy-ins open once the bylaws lock'); return; }\n        if(!window.CS?.season?.id){ toast('Buy-ins open once the bylaws lock'); return; }",
  "failureScenario": "No runtime effect — the second copy is unreachable. It matters as evidence: the diff shows the first line was written OVER the `if(!isPro)` line without removing the original guard beneath it, which is exactly how the member's explanation (IDX07-04) got lost. A duplicated guard in a money-handling click path reads as deliberate.",
  "fix": "Delete line 7534.",
  "confidence": 10,
  "notes": "",
  "slice": "index.html 7459–8478",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "IDX07-08",
  "title": "The client invents amount_cents on an optimistic buy-in mark; the RPC never re-stamps it",
  "file": "index.html",
  "line": "7540",
  "severity": "P3",
  "kind": "bug",
  "category": "money-correctness",
  "sessionCode": false,
  "quote": "(window.buyIns ||= {})[m.id] = { ...(bi[m.id]||{}), paid: !paid, amount_cents: state.stake*100 };",
  "failureScenario": "mark_buy_in's upsert writes amount_cents on INSERT only. A member first marked paid at a $50 buy-in, later unmarked and re-marked after the Pro raised it to $75, keeps 5000 in the row — while the client asserts 7500 locally. The Pot tab reads '$75 collected' until the next reload and '$50 collected' after it, with no way for the Pro to tell which is true.",
  "fix": "Have mark_buy_in return the stored row (or update amount_cents on conflict) and set the cache from the returned value.",
  "confidence": 6,
  "notes": "Could not determine whether the Pro can raise buyin_cents after a season starts; if that is blocked everywhere the impact is limited to legacy rows.",
  "slice": "index.html 7459–8478",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "IDX07-09",
  "title": "A bragging-rights league reads \"Buy-ins · Bragging rights in\"",
  "file": "index.html",
  "line": "7467",
  "severity": "P3",
  "kind": "bug",
  "category": "copy",
  "sessionCode": false,
  "quote": "['paidChip','paidCount'].forEach(id=>{ const e=$('#'+id); if(e) e.textContent='Bragging rights'; });",
  "failureScenario": "#paidCount is a span inside fixed surrounding copy — `<div class=\"eyebrow\">Buy-ins · <span id=\"paidCount\">5/7</span> in</div>` — so substituting the phrase produces 'Buy-ins · Bragging rights in' on the Pot pane of any $0 league.",
  "fix": "Give the eyebrow its own id and set the whole string.",
  "confidence": 9,
  "notes": "#paidChip is a standalone stat value and reads fine.",
  "slice": "index.html 7459–8478",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "IDX07-10",
  "title": "The pot prices off a wizard state no surface can fill any more",
  "file": "index.html",
  "line": "7477–7478",
  "severity": "P3",
  "kind": "opportunity",
  "category": "dead-code",
  "sessionCode": false,
  "quote": "const invited=[…state.emails…].length;\n    const players=Math.max(ms.length, invited+1, 1);",
  "failureScenario": "A-W2 removed the wizard's email slots, renderEmails() bails because #emailSlots is not in the markup, and the only remaining writer of state.emails is resetWizard. So `invited` is permanently 0 and the expression can only return Math.max(ms.length, 1). It is a loaded gun in the money path: D106 defines the owed number as buy-in × roster and the engine agrees, so restoring any pre-lock invite-staging surface would immediately make the Pro's Pot tab show a bigger pot and a different 'K still owe' than the same league shows a member — and than the settlement will pay.",
  "fix": "Collapse to `Math.max(ms.length, 1)` and delete the invited computation; same at 12583.",
  "confidence": 8,
  "notes": "",
  "slice": "index.html 7459–8478",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "IDX07-11",
  "title": "estimateSI() is called with no hole count on both resume paths, before holes is known",
  "file": "index.html",
  "line": "7998, 8048",
  "severity": "P3",
  "kind": "opportunity",
  "category": "correctness",
  "sessionCode": false,
  "quote": "if(Array.isArray(c.si) && c.si.length===18){ SI=c.si.map(Number); SIEST=!!c.siEst; } else estimateSI();",
  "failureScenario": "estimateSI(n) exists so a nine ranks its SI 1..9. Both resume paths call it with no argument, and before state.live.holes is assigned, so it always ranks 18. A resumed 9-hole round reaching that branch would compute strokeOn against SI values up to 18 on holes 0–8 and hand out fewer strokes than owed.",
  "fix": "Read the hole count first and pass it.",
  "confidence": 5,
  "notes": "Deliberately low confidence: the condition is real and the arity is wrong, but buildLiveSnapshot always writes a full 18-wide si array, so I could not reach the branch with any current input.",
  "slice": "index.html 7459–8478",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "F01",
  "title": "Wolf's ledger stops summing to zero when the comeback wolf is the partner the previous wolf already picked — settleTransfers then silently drops a payment",
  "file": "index.html",
  "line": "8524-8546 (root 8529)",
  "severity": "P1",
  "kind": "bug",
  "category": "money-correctness",
  "sessionCode": false,
  "quote": "const w=wolfAt(h);\n    const side = pick.mode==='lone' ? [w] : [w,pick.partner];\n    const opp=[0,1,2,3].filter(p=>!side.includes(p));",
  "failureScenario": "wolfAt() on holes 17–18 returns whoever is CURRENTLY last, recomputed on every render, so completing or CORRECTING any earlier hole can re-assign them. When the re-derived wolf equals the stored pick's partner, side becomes [B,B] and opp widens to three players: pts[B] moves ±2 while three opponents move ∓1 each, so the hole pays ∓1 net and the ledger stops summing to zero. settleTransfers then walks credit and debit lists of different totals and exits early, so a real debt is never rendered on the settlement card or in transfers[]. Reproduced in node with the file's own functions: 480 of 20,000 simulated games (2.4%) reach the state after a single score correction; one run produced pts [-3,-2,1,3] (sum −1) and a settlement moving $20 of $25 owed.",
  "fix": "Freeze the resolved wolf on the pick (`state.live.wolf[h]={mode,partner,wolf:w}`) and have wolfPointsThrough read it; at minimum guard the degenerate case so a self-partnered hole scores nothing.",
  "confidence": 8,
  "notes": "Verified reachability rests on wolfAt() being live-recomputed rather than stored. The pick UI excludes the current wolf, so the invalid combination can only ever arrive by re-derivation — which is why nothing downstream guards it. Lone-wolf and distinct-partner holes are correctly net-zero. Same defect verbatim in LiveEngines.swift:127–172.",
  "slice": "index.html 8479–9944 — live round engines, play view, finish, recap, home stat strip",
  "verification": {
   "status": "survived",
   "correctedSeverity": "P1",
   "votes": [
    {
     "lens": "control-flow",
     "verdict": "CONFIRMED",
     "refuted": false,
     "correctedSeverity": "P1",
     "reasoning": "Extracted holeDone/netOf/wolfAt/wolfPointsThrough/settleTransfers verbatim into node and simulated 20,000 legal games: with ONE score correction to an earlier hole, 480 (2.4%) end with a stored pick whose partner is now the derived wolf, the ledger sums to −1, and settleTransfers moves $20 of $25 owed. Control run with no correction: 0 in 20,000. So pure sequential play is safe; an edit (or picking ahead) is the trigger — and the trigger is not the elaborate hole-17/18 ordering described, any single ± correction suffices."
    },
    {
     "lens": "intent-and-precedent",
     "verdict": "CONFIRMED",
     "refuted": false,
     "correctedSeverity": "P1",
     "reasoning": "Independently reproduced a concrete 18-hole line ending pts [-1,0,1,-1] where settleTransfers emits one row and D's $5 debit vanishes. The net-zero ledger is a written invariant — spec §7b batch-2 #10 and the D79 comment at index.html:8811 ('the ledger is net-zero by construction. That is exactly the shape settleTransfers() wants'). git shows no attempt ever made at a guard. The same defect is verbatim in LiveEngines.swift:127–172, and LiveEngineTests never covers the degenerate self-partner."
    }
   ]
  }
 },
 {
  "id": "F02",
  "title": "renderLiveBanner names match-play sides by slot order, not MTEAMS — singles show an empty side, a swapped court shows the wrong winner",
  "file": "index.html",
  "line": "9401-9406",
  "severity": "P2",
  "kind": "bug",
  "category": "correctness",
  "sessionCode": false,
  "quote": "const teamA=`${LIVE[0]?.n||''} & ${LIVE[1]?.n||''}`.toUpperCase();\n    const teamB=`${LIVE[2]?.n||''} & ${LIVE[3]?.n||''}`.toUpperCase();",
  "failureScenario": "(a) Singles: MTEAMS is [[0],[1]] and matchCalc nets it correctly, but teamA evaluates to 'JERECHO & LOGAN' (both players) and teamB to ' & '. When Logan goes 2 up the banner reads ' &  2 UP'; when Jerecho does it names his opponent as his partner. (b) 2v2 with a swapped court: matchCalc scores MTEAMS while the banner labels slot order, so the banner announces a pair one of whom is on the losing side.",
  "fix": "Use the same MTEAMS-based builder renderPlay already uses at 8993-8996.",
  "confidence": 9,
  "notes": "The identical bug was already found and fixed inside renderPlay, whose comment says slot order 'announced the wrong side'. The banner is the copy that was missed.",
  "slice": "index.html 8479–9944",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "F03",
  "title": "Home renders two live-round banners from two independent producers, with different (and on an invite, contradictory) copy",
  "file": "index.html",
  "line": "9393-9419",
  "severity": "P2",
  "kind": "bug",
  "category": "dead-or-contradictory",
  "sessionCode": false,
  "quote": "function renderLiveBanner(){\n  const b=$('#liveBanner'); if(!b) return;\n  if(!state.live.active || state.phase!=='season'){ b.style.display='none'; return; }",
  "failureScenario": "renderResumeBanner shows #resumeBanner with no phase gate; renderLiveBanner shows #liveBanner inside #homeSeason, displayed for p==='season'. Both are inside #view-home, so Home shows two 'live round' cards at once. On a D86 invite round they contradict each other — 'Marcus put you on the tee sheet' with a JOIN affordance versus 'Live round in progress' with Continue — and #liveBanner has no sunningdale branch, so a Sunningdale match falls through to 'SCORING LIVE' while the resume banner names the game.",
  "fix": "Retire #liveBanner and renderLiveBanner; renderResumeBanner is the D85/D86 replacement.",
  "confidence": 8,
  "notes": "Read from the markup containers plus renderPhase's display rule rather than observed. #liveBanner's demo-only course fallback (F12) is further evidence it is the pre-D85 banner nobody removed.",
  "slice": "index.html 8479–9944",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "F04",
  "title": "The finish sheet undercounts a league round's app-golfer guests and promises them a claim link the server will never mint",
  "file": "index.html",
  "line": "9675-9679",
  "severity": "P2",
  "kind": "bug",
  "category": "silent-mismatch",
  "sessionCode": false,
  "quote": "const _lg = !!(window.CS?.league);\n  const done  = seats.filter(x=>(_lg ? !x.p.guest : (!x.p.guest || x.p.pid)) && cardHoles(x.i)>0);\n  const guestN = LIVE.filter(p=>p.guest && (_lg || !p.pid)).length;",
  "failureScenario": "A Pro tees off with a buddy found through #rosterFind (guest:true, pid:profile_id). finish_live_round posts ANY seat with guest_profile_id straight to that profile — the branch has no league condition — and returns it in `posted`, never in `guests`. But the client's _lg short-circuit excludes that seat from `done`, so the button reads 'Post 2 cards' when three will post, and guestN counts him so the fine print promises '1 guest gets a recap to claim'. The finish mints zero claim tokens; the golfer who was told he must claim his round is never given a link.",
  "fix": "Drop the _lg branch from both predicates — the server's rule is `!p.guest || p.pid` for posting and `p.guest && !p.pid` for the claim path, league or not.",
  "confidence": 8,
  "notes": "This is the S6-01 class the block above it was written to prevent, reappearing in the opposite direction after D107 widened the server. Verified by reading the live finish_live_round body.",
  "slice": "index.html 8479–9944",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "F05",
  "title": "The recap's guest-link Copy button reports success when nothing was copied, and leaks an unhandled rejection",
  "file": "index.html",
  "line": "9831-9834",
  "severity": "P2",
  "kind": "bug",
  "category": "silent-failure",
  "sessionCode": false,
  "quote": "    try{ navigator.clipboard?.writeText(b.dataset.copylink); toast('Recap link copied'); }\n    catch(_){ toast('Copy failed'); }",
  "failureScenario": "(a) navigator.clipboard undefined — an insecure origin (the repo's own local verify step is python -m http.server), an in-app webview: the optional chain short-circuits, nothing throws, and the toast fires. The guest's /?claim= link — the only way that round reaches their card — is silently lost, and the sheet is the last surface that holds it. (b) writeText REJECTS: the rejection is asynchronous so the synchronous try/catch never runs — the user is told it copied AND an unhandled rejection lands in the global handler. This is the only one of the file's ten clipboard call sites that does not await.",
  "fix": "Make the handler async, test for the API, and await the write. Apply the same guard at 9872-9877 before setting 'Copied ✓' and before growthEvent.",
  "confidence": 9,
  "notes": "humanError learned this lesson this session (Q-08 added a clipboard branch), which is evidence the repo has seen these rejections — but that mapping only helps callers that route the error there, and this one discards it.",
  "slice": "index.html 8479–9944",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "F06",
  "title": "The tee-off strokes preview does not halve the course handicap for a nine, so the first-tee argument is settled with numbers the round then contradicts",
  "file": "index.html",
  "line": "9232-9250",
  "severity": "P2",
  "kind": "bug",
  "category": "scoring-correctness",
  "sessionCode": false,
  "quote": "const chs=ps.map(p=>Math.round(p.i*CSLOPE/113));\n  const low=Math.min(...chs);",
  "failureScenario": "Setup, match play, slope 123, the group taps '9'. recomputeStrokes halves for a nine (round(i*CSLOPE/113/2)); renderMatchPrev does not. Gary 20.0 vs Mitch 14.9: in play Gary gets 3 strokes; the preview prints 'Gary gets 6: holes a,b,c,d,e,f' naming six specific holes. The group agrees the match on that line, tees off, and the dots show three. §2.2's whole point — the first-tee argument settled before anyone hits — is settled wrong, and at larger gaps the copy degrades to 'gets 12 — the 12 hardest holes' on a card with nine.",
  "fix": "Read CHS after a recomputeStrokes over the selection, or at minimum mirror the divisor. One producer for course handicap removes the class.",
  "confidence": 9,
  "notes": "Reachable because the #lrHoles handler ends in renderPlay() → renderRoster() → renderMatchPrev(). D73's comment on recomputeStrokes states the WHS 9-hole rule explicitly; the preview predates it.",
  "slice": "index.html 8479–9944",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "F07",
  "title": "The Wolf settlement card and share page always claim 'THRU 18' regardless of how far the round actually got",
  "file": "index.html",
  "line": "9612",
  "severity": "P2",
  "kind": "bug",
  "category": "correctness",
  "sessionCode": false,
  "quote": "holes: holeLedger('wolf', wolfCells(), 0, false, 'w', \"the wolf's side\"),",
  "failureScenario": "Rain shuts the course after 12 holes and the group finishes the round in the app. wolfCells() pushes a cell for EVERY hole 0..17 (correctly, for index alignment), so c.length is always 18; holeLedger resolves `played: played||c.length` and the caller passes 0, so played becomes 18. renderHoleStrip prints 'THRU 18' on the recap sheet, on the shareSettlementCard PNG, in the game_result jsonb persisted by finish_live_round, and on the public /?share= settlement page which has no engines of its own. matchResult and skinsResult both pass a real thru — Wolf is the only game that lies about it.",
  "fix": "Compute the real thru in wolfResult and pass it.",
  "confidence": 8,
  "notes": "The all-holes push is deliberate per D78, so the fix belongs at the call site.",
  "slice": "index.html 8479–9944",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "F08",
  "title": "#statFinal puts the week close on 'the next Sunday' and labels it 'Sun', contradicting currentWeek() in the same file and §14.0's flexible first-tee weekday",
  "file": "index.html",
  "line": "9989-9992",
  "severity": "P2",
  "kind": "bug",
  "category": "dates-and-timezones",
  "sessionCode": false,
  "quote": "const sun=new Date(today); sun.setDate(sun.getDate()+(7-sun.getDay())%7);\n      …{n:dTo(sun), pri:0, gold:false, t:dTo(sun)===0?'Week closes tonight':`Week closes Sun · ${dTo(sun)}d`},",
  "failureScenario": "A league tees off Thursday. currentWeek() counts from seasonS(), so the week counter in the tile directly above increments on Thursdays, and D108 rolls weeks on the league's real first-tee weekday. This strip reads 'Week closes Sun · 2d' when the week actually closes Wednesday night. A golfer holding a round to land it in 'next week' posts on Sunday and it lands in the week he believed had closed; a golfer racing 'Week closes tonight' is racing nothing. Both the DATE and the LABEL are wrong for any league that does not start on a Sunday.",
  "fix": "Derive the close from the season's own weekday and label it with DOW[].",
  "confidence": 8,
  "notes": "seasonSpanText already carries a fix note for the same regression ('hardcoded Sun/Sat survived the v1.1 flexible-start fix here'). If weeks have since been re-snapped to Sunday this is instead a bug in currentWeek() — the two cannot both be right.",
  "slice": "index.html 8479–9944",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "F09",
  "title": "'Month closes · floors assessed' fires in partial edge months where floors are waived, contradicting #nextTxt thirty lines below in the same function",
  "file": "index.html",
  "line": "9993 (contradicted at 10071-10077)",
  "severity": "P3",
  "kind": "bug",
  "category": "copy-contradiction",
  "sessionCode": false,
  "quote": "{n:dTo(first), pri:1, gold:false, t:`Month closes ${MOS[first.getMonth()]} 1 · floors assessed`},",
  "failureScenario": "A season starting Sep 18 makes September a partial edge month whose floors are waived. On Sep 30 the month line wins the nearest-deadline slot and #statFinal reads 'Month closes Oct 1 · floors assessed', while six lines further down in the same renderStats call #nextTxt reads 'September is a short month — no floor to clear.' Two statements about the same month, on the same screen, from the same function, one false. The `partial` flag that makes the second one true is already in scope.",
  "fix": "Hoist the partial read above the opts array and drop the clause when partial.",
  "confidence": 8,
  "notes": "The #nextTxt branch carries its own comment recording this exact pilot incident — left standing in the sibling line of the same function.",
  "slice": "index.html 8479–9944",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "F10",
  "title": "The group sheet states 'No guests in this round' when the claim tokens simply were not loaded on this device",
  "file": "index.html",
  "line": "9866-9870",
  "severity": "P2",
  "kind": "bug",
  "category": "silent-failure",
  "sessionCode": false,
  "quote": "const gRows = LIVE.map((p,i)=>({p,i})).filter(x=>x.p.guest && L.guestTokens && L.guestTokens[x.i])…join('');\n  openSheet('Group phones', …, `…${gRows || '<p class=\"fine\">No guests in this round.</p>'}`);",
  "failureScenario": "A Pro tees off with two account-less guests; guestTokens is filled from the tee-off response and rides the local snapshot. His phone dies and he signs in on an iPad: rehydrateLiveRound finds no local snapshot and falls to applyServerRound, which hard-sets guestTokens:{} — and the resume query does not select claim_token, so the tokens were never fetched. He taps 'Group phones' and reads 'No guests in this round.' about a round that has two. D85's whole distribution mechanism is dead for the rest of the round.",
  "fix": "Separate 'no guests' from 'no tokens on this device' and say so honestly; better, add claim_token to the resume column list.",
  "confidence": 7,
  "notes": "Root of the missing data is applyServerRound and the resume select; the false statement is in-slice. Did not verify whether live_round_players RLS lets a member SELECT claim_token.",
  "slice": "index.html 8479–9944",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "F11",
  "title": "The demo finish hardcodes 18 holes and can never be satisfied on a demo nine",
  "file": "index.html",
  "line": "9922-9923",
  "severity": "P3",
  "kind": "bug",
  "category": "correctness",
  "sessionCode": false,
  "quote": "const left=18-[...Array(18).keys()].filter(h=>holeDone(h)).length;\n  if(left>0){toast(left+' holes left to score');return;}",
  "failureScenario": "In demo mode, tap '9' (the handler has no demo guard), tee off, score all nine. holeDone is false for 9..17 by design, so `left` computes 9, the toast says '9 holes left to score', and Finish refuses forever. Every other hole loop in the slice reads liveHoles(); this one does not.",
  "fix": "Use liveHoles() in both the count and the range.",
  "confidence": 7,
  "notes": "Demo-only — the real path returns via finishRealLiveRound() before this line. Did not confirm the control is visible in demo mode, only that its handler has no demo guard.",
  "slice": "index.html 8479–9944",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "F12",
  "title": "A real account's live banner falls back to the demo course name 'Encanto GC'",
  "file": "index.html",
  "line": "9418",
  "severity": "P3",
  "kind": "bug",
  "category": "demo-leak",
  "sessionCode": false,
  "quote": "$('#lbHole').textContent=`HOLE ${state.live.hole+1} · ${($('#lrCourse').value||'Encanto GC').toUpperCase()} · THRU ${thru}`;",
  "failureScenario": "primeRealRoster deliberately blanks #lrCourse for a real account ('the demo's Encanto scenery must not greet a real account'). A real member who tees off without typing a course — allowed; the server coalesces to 'Course' — gets a Home banner naming a course in Phoenix they have never played, while #resumeBanner three inches above falls back to 'Your round'.",
  "fix": "Fall back to the same neutral string, or delete the banner with F03.",
  "confidence": 8,
  "notes": "CLAUDE.md's rule is 'gate every real-data path with !state.demo'; this is the inverse — a demo constant reaching a real path.",
  "slice": "index.html 8479–9944",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "F13",
  "title": "Score stepper buttons are 36×36px, 2px apart — under the 44px floor on the most-tapped control in the product",
  "file": "index.html",
  "line": "735 (consumed at 8963-8967)",
  "severity": "P2",
  "kind": "bug",
  "category": "accessibility",
  "sessionCode": false,
  "quote": ".step button{width:36px; height:36px; border:none; background:none; …}",
  "failureScenario": "The − / + pair rendered per player per hole is 36×36 with a 2px gap, inside rows that fill the screen: 8px under WCAG 2.5.5 and Apple's HIG, on the one control a golfer uses one-handed, in sunlight, wearing a glove, four times a hole. A mis-tap does not just miss — the adjacent target is the opposite operation, so it writes a wrong score, and the handler immediately broadcasts it to every other phone on the D85 channel.",
  "fix": "44×44, or keep the 36px ink and add an inset overlay to 44px, and raise the gap.",
  "confidence": 8,
  "notes": "CROSS-RANGE root at CSS line 735 (see CSS-12's family). Assessed from code, not measured on a device.",
  "slice": "index.html 8479–9944",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "F14",
  "title": "renderPlay runs a full Sunningdale engine pass per player row instead of once per repaint",
  "file": "index.html",
  "line": "8955-8961",
  "severity": "P3",
  "kind": "opportunity",
  "category": "efficiency",
  "sessionCode": false,
  "quote": "state.live.game==='sunningdale' ? (liveSolo() ? (sunnSoloStrokesAt(h)[pi]||0) : (sunnStrokesAt(h)[…]||0)) : strokeOn(pi,h)",
  "failureScenario": "The expression is inside `LIVE.forEach`, so sunnStrokesAt(h) — which maps the whole card and re-runs sunnEngine over up to 18 holes × 2 sides — is invoked once per player: four full engine passes per repaint, three discarded, on every ± tap and every incoming realtime score.",
  "fix": "Hoist above the forEach and index inside the loop.",
  "confidence": 9,
  "notes": "Mechanical and behaviour-preserving; the engines are pure and pinned by tests.",
  "slice": "index.html 8479–9944",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "F15",
  "title": "settleRows' WOLF_VAL fallback is an unreachable $2/pt money default hiding behind a != null check",
  "file": "index.html",
  "line": "8874-8876",
  "severity": "P3",
  "kind": "opportunity",
  "category": "dead-code",
  "sessionCode": false,
  "quote": "function settleRows(pts, val){\n  const v = val!=null ? val : WOLF_VAL;",
  "failureScenario": "Both call sites pass state.live.stake and every constructor coerces it to a number, so `val!=null` is always true and WOLF_VAL (=2) is dead. But it is a live $2-per-point default one refactor away from firing: any future path calling settleRows(pts) without an amount would print real dollar transfers for a bragging-rights round.",
  "fix": "Delete the fallback and the constant; make the parameter required.",
  "confidence": 8,
  "notes": "Filed as an opportunity because all three constructors were traced and none can produce a null stake.",
  "slice": "index.html 8479–9944",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "F16",
  "title": "matchResult and sunnResult return the same field in different casing, and showLiveRecap prints both into one template",
  "file": "index.html",
  "line": "9570 / 9557 (rendered at 9760-9763)",
  "severity": "P3",
  "kind": "opportunity",
  "category": "copy-consistency",
  "sessionCode": false,
  "quote": "else if(m.a!==m.b){ winner=m.a>m.b?0:1; status=`${Math.abs(m.a-m.b)} UP THRU ${m.played}`; }",
  "failureScenario": "matchResult uppercases status because the live card shouts; sunnResult lowercases it. showLiveRecap renders `${wSide} def. ${lSide} ${result.status}` for both, so one recap reads '… 2 UP THRU 17' and the other '… 2 up thru 14' — the same sentence in two voices, one shouting mid-clause. matchResult already lowercases the same field for its story/share strings.",
  "fix": "Return sentence case from both and let the live card upcase at render.",
  "confidence": 8,
  "notes": "Cosmetic, but D77 rewrote these builders specifically so the feed row and the text message could not drift.",
  "slice": "index.html 8479–9944",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "F17",
  "title": "The hole strip is eighteen unlabelled spans — a screen reader gets the margin and nothing else",
  "file": "index.html",
  "line": "8580-8607",
  "severity": "P3",
  "kind": "opportunity",
  "category": "accessibility",
  "sessionCode": false,
  "quote": "cells.push(`<span title=\"Hole ${i+1}\" style=\"…\"></span>`);",
  "failureScenario": "D78's argument is that '3&2 states a margin; the strip states the SHAPE'. A screen-reader user gets only the margin: the spans have no text content and no role, `title` on a non-interactive span is announced inconsistently, and the only readable content is the foot. The strip also renders on the public /?share= page.",
  "fix": "Wrap the row in role=\"img\" with an aria-label built from the same cells — holeHighlights already derives most of that sentence.",
  "confidence": 8,
  "notes": "Inline styles are deliberate for the share page; an aria-label costs nothing there.",
  "slice": "index.html 8479–9944",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "F18",
  "title": "holeHighlights credits a streak to 'the wolf's side', which is a different pair every hole",
  "file": "index.html",
  "line": "8613-8626 (rendered from 9612)",
  "severity": "P3",
  "kind": "opportunity",
  "category": "copy-contradiction",
  "sessionCode": false,
  "quote": "if(best>=3) out.push(`WON ${best} STRAIGHT · ${bestEnd-best+2}-${bestEnd+1}`);",
  "failureScenario": "wolfResult passes the legend \"the wolf's side\". If holes 7, 8 and 9 all went to the wolf — three DIFFERENT wolves with three different partners — the recap chip reads 'WON 3 STRAIGHT · 7-9' under an ember legend. Every reader parses that as one party's run; no golfer at that table won three straight.",
  "fix": "Gate the streak on the ledger mode, or word it for wolf.",
  "confidence": 8,
  "notes": "D78's own rule is that highlights are derived at render so they can be re-examined; this claim is re-examinable and wrong.",
  "slice": "index.html 8479–9944",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "F19",
  "title": "drainAbandons() is fired but not awaited at tee-off, and two concurrent drains share one localStorage key",
  "file": "index.html",
  "line": "9482",
  "severity": "P3",
  "kind": "opportunity",
  "category": "race",
  "sessionCode": false,
  "quote": "drainAbandons();   /* clear any scrapped-but-undelivered rounds before opening a new one */",
  "failureScenario": "The comment says 'before opening a new one'; the call is not awaited. drainAbandons does read-all → loop-with-awaits → write-back-the-failures, and has three callers. A scrap landing while a tee-off drain is mid-loop queues an id that the tee-off drain's write-back (computed from a pre-queue snapshot) then erases. The corpse survives and the resume path resurrects it — the pilot bug the queue was built to kill.",
  "fix": "await it, and serialise the function on a module-level in-flight promise.",
  "confidence": 7,
  "notes": "Narrow interleaving, reachable across a slow network.",
  "slice": "index.html 8479–9944",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "F20",
  "title": "D126's endgame sentence shipped to one of its three homes — #statFinal still runs the sort D126 was logged to amend",
  "file": "index.html",
  "line": "9989-10007 (endgameLine defined 5923, used only at 4573)",
  "severity": "P2",
  "kind": "opportunity",
  "category": "decided-not-built",
  "sessionCode": true,
  "quote": "const nx=opts.sort((a,b)=>a.n-b.n || b.pri-a.pri)[0];\n      sf.textContent=nx.t;",
  "failureScenario": "D126 recommends three homes — the hero's season branch, the Season tile pinned with a countdown, and the climb note — and explicitly amends the C4 'nearest deadline wins' rule for #statFinal. What shipped is the climb note alone: endgameLine appears at exactly two lines in index.html (its definition and one call), and the diff has no hunk between 8479 and 10166, so this sort is byte-identical. D126's own 'Current' section computes that this makes the Cup Final line render one day in 155. The decision log now reads as though the endgame is visible everywhere a standing is.",
  "fix": "Build the pin, or amend D126 with a build-state line naming the two homes still open.",
  "confidence": 8,
  "notes": "Verified three ways: grep for endgameLine (2 hits), the diff hunk list, and reading the current #statFinal body.",
  "slice": "index.html 8479–9944",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "idx09-01",
  "title": "The preseason hero tells a golfer their rounds count, under a header that says they don't",
  "file": "index.html",
  "line": "10465, 10522–10524",
  "severity": "P1",
  "kind": "bug",
  "category": "correctness",
  "sessionCode": true,
  "quote": "const starter = state.phase==='season';\n…\nbox.innerHTML = heroCard('', `${esc(lgName||'Your league')} · ${STAGE_LABEL[stage].toLowerCase()}`,\n  dd!=null && !starter ? … : '',\n  starter ? `The season's on. <em>Rounds count from today.</em>`",
  "failureScenario": "League locked, first tee Sun Sep 6, today Sep 2. atStarter() requires state.phase==='season', so block 3 declines and block 4 runs with starter===true and leagueStage()==='preseason'. The single hero card renders header 'Acme Cup · before first tee' over body 'The season's on. Rounds count from today.' Rounds posted today do NOT score for the league (D122); seasonNote() says 'Practice — the season starts Sun Sep 6' on the post form and renderStats() says 'Practice rounds hit your card, not the season' on the same account. Three surfaces, two answers.",
  "fix": "Inside block 4, `starter` and `stage==='preseason'` are the same predicate. Delete `starter` and branch on `stage`.",
  "confidence": 9,
  "notes": "The false sentence is pre-D119; D119 replaced the header with STAGE_LABEL[stage], which turned a wrong-but-consistent card into a self-contradicting one. Verifiers confirmed the server sets leagues.phase='season' at squad formation and at solo lock, before starts_on.",
  "slice": "index.html 9945–11059 — home hero, tiles, occasions, course card, individual stats, feed rows, scorecard",
  "verification": {
   "status": "survived",
   "correctedSeverity": "P1",
   "votes": [
    {
     "lens": "control-flow",
     "verdict": "CONFIRMED",
     "refuted": false,
     "correctedSeverity": "P1",
     "reasoning": "starter ⟺ stage==='preseason' inside block 4, because block 3 returns on both arms whenever inSeason && !atStarter(). The server sets leagues.phase='season' at squad formation and at solo lock while starts_on is still ahead, and v_rounds_ranked's window join makes the sentence false. Under-counted consequence: the CTA guard also suppresses the whole preseason CTA row, and the qaEvent then lies about it."
    },
    {
     "lens": "intent-and-precedent",
     "verdict": "CONFIRMED",
     "refuted": false,
     "correctedSeverity": "P1",
     "reasoning": "D120's Problem paragraph names this pair verbatim — 'Home said Rounds count from today while the Clubhouse said practice rounds hit your card. Logged by all six web personas.' D119 swapped the header to STAGE_LABEL and left the body, so the decided fix landed on the label producer and never on the sentence it was filed to kill."
    }
   ]
  }
 },
 {
  "id": "idx09-02",
  "title": "The whole preseason row of the D119 matrix is computed and then discarded — no CTA for either role",
  "file": "index.html",
  "line": "10514, 10516, 10520–10521, 10544–10547",
  "severity": "P1",
  "kind": "bug",
  "category": "dead-code",
  "sessionCode": true,
  "quote": "      (starter || !nextStep) ? '' : `<button class=\"hh-cta\" data-hform type=\"button\">${nextStep.label}</button>`\n      + (subStep ? `<button class=\"hh-sub\" data-hsub type=\"button\">${subStep.label}</button>` : ''));",
  "failureScenario": "stage==='preseason' implies starter===true, so the guard yields ''. The Pro's planRound CTA and the member's planRound CTA are both built and thrown away, and because `+` binds tighter than `?:`, the subStep button ('Post a practice round — it builds your number') lives inside the else branch and dies with them. Both preseason cells render a card with no action at all — the exact 'no cta' hole D119 was written to close, and D96 documents seven pilots who created a league and stopped because the hero gave them nothing to press.",
  "fix": "Drop `starter` from the CTA guard once idx09-01 is fixed, and split the two buttons into separate expressions.",
  "confidence": 9,
  "notes": "Also makes three copy branches unreachable (10535, 10540, 10542-fallback). Verifier correction: the 10542 fallback IS reachable (Pro / forming / named / n>1). The qaEvent at 10547 still reports a cta label for a button that never rendered.",
  "slice": "index.html 9945–11059",
  "verification": {
   "status": "survived",
   "correctedSeverity": "P1",
   "votes": [
    {
     "lens": "control-flow",
     "verdict": "CONFIRMED",
     "refuted": false,
     "correctedSeverity": "P1",
     "reasoning": "Confirmed the precedence by executing the expression: with starter true the CTA slot is \"\", taking the sub-button with it. leagueStage returns 'preseason' only via atStarter(), which itself requires phase==='season', so there is no escape. git -L shows the `starter ||` guard predates the matrix and D119 added the preseason rows on top of it without revisiting it. wire() is null-safe, which is why this never surfaced as a console error."
    },
    {
     "lens": "intent-and-precedent",
     "verdict": "CONFIRMED",
     "refuted": false,
     "correctedSeverity": "P1",
     "reasoning": "Reproduced in node. D119 and D136 both specify these CTAs; the D119 diff shows the guard line byte-identical while the preseason rows and subStep were appended. One correction: the :10542 fallback IS reachable (Pro / forming / named / n>1). The qaEvent at :10547 reports cta:'Plan a round' for a card that rendered no CTA, so the telemetry meant to measure this will report the fix as shipped."
    }
   ]
  }
 },
 {
  "id": "idx09-03",
  "title": "A finished league renders as \"cup final · 1 week left\" when it is not your only league",
  "file": "index.html",
  "line": "10428–10440",
  "severity": "P1",
  "kind": "bug",
  "category": "correctness",
  "sessionCode": false,
  "quote": "const inSeason = window.CS.league && state.phase==='season';\n  if(force==='cup_final' || (!force && inSeason && typeof isCupFinal==='function' && isCupFinal())){\n    …const wksLeft=Math.max(1, Math.ceil((seasonE()-new Date())/6048e5));",
  "failureScenario": "Two leagues, one finished last month. Tap the finished league's chip in the Clubhouse switcher: enterLeague maps phase 'complete' → state.phase='season', so inSeason is true. Block 1 declines because active.length===1. isCupFinal() returns true (the dial is cup_final and ends_on−27d is in the past), so block 2 paints 'The Grudge · cup final … 1 week left' — wksLeft clamps the negative to 1 — for a season that is over and settled. On a points_table dial, block 3 paints the live standing with a monthly-floor foot on a settled season.",
  "fix": "Gate blocks 1–3 on leagueStage() the way block 4 already does: 'final' for block 2, 'season' for block 3, and let 'complete' reach block 1's copy regardless of active.length.",
  "confidence": 8,
  "notes": "Precisely the drift D120 was written to end — block 4 uses leagueStage(), blocks 1–3 do not. Verified: prod holds one complete league and zero profiles with both a complete and a non-complete membership, so it is latent today.",
  "slice": "index.html 9945–11059",
  "verification": {
   "status": "survived",
   "correctedSeverity": "P2",
   "votes": [
    {
     "lens": "control-flow",
     "verdict": "CONFIRMED",
     "refuted": false,
     "correctedSeverity": "P2",
     "reasoning": "Chain verified: loadMemberships has no phase filter, the Clubhouse chips call enterLeagueById directly (never goClubhouse, the only place CS.season/state are reset), enterLeague maps 'complete' to 'season', and isCupFinal has no upper bound so wksLeft clamps a negative to 1. Prod: exactly one complete league and zero profiles holding both a complete and a non-complete membership, so latent; display-only."
    },
    {
     "lens": "intent-and-precedent",
     "verdict": "CONFIRMED",
     "refuted": false,
     "correctedSeverity": "P2",
     "reasoning": "D120 explicitly names the hero's state.phase keying as one of the drifting sites, and its commit fixed the neighbours and hero block 4 while leaving blocks 1–3 — unfinished conversion, not accepted behaviour. runItBack is the designed flow that produces the two-league shape. Same root shows up one line over in renderClubGroups."
    }
   ]
  }
 },
 {
  "id": "idx09-04",
  "title": "The wrapped-season hero can name — and \"run back\" — a league you are not looking at",
  "file": "index.html",
  "line": "10397, 10401–10403, 10420–10424",
  "severity": "P2",
  "kind": "bug",
  "category": "correctness",
  "sessionCode": false,
  "quote": "const done   = ships.find(m => m.league?.phase==='complete');",
  "failureScenario": "Two finished leagues and nothing running: CS.league is whichever you last entered, but ships.find() returns whichever the memberships query ordered first. The hero reads 'Season wrapped · Sunday Cup', the position figure is suppressed because the ids do not match, the recap tap goes to the Grudge's hub, and Run it back seeds Sunday Cup's bylaws into the wizard. Every element of the card points at a different league than the one you are in.",
  "fix": "Prefer the loaded league when it is the complete one, falling back to the first.",
  "confidence": 8,
  "notes": "renderHomeStart has the identical find() and the same exposure for its runback card.",
  "slice": "index.html 9945–11059",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "idx09-05",
  "title": "Two identical \"Run it back — Season 2\" buttons paint on the same Home",
  "file": "index.html",
  "line": "10286–10300 with 10400 and 10420–10422",
  "severity": "P2",
  "kind": "bug",
  "category": "duplicate-ui",
  "sessionCode": false,
  "quote": "  const done = !state.demo && (window.CS?.memberships||[]).find(m => m.league?.phase === 'complete');\n  const rb = done ? `…<button class=\"btn\" data-runback=\"${esc(done.league.id)}\" type=\"button\">Run it back — Season 2</button>` : '';",
  "failureScenario": "One league, phase complete. Hero block 1 fires and paints an hh-cta 'Run it back — Season 2'; renderHomeStart then paints the .runback card with the same label, and because inALeague is false it also paints the full three-door grid. Home shows the same CTA twice plus three cold-start doors — the exact stacking D119(4)/D136 collapsed the doors to prevent.",
  "fix": "Block 1's comment already says it absorbs D41's runback card; gate renderHomeStart's rb on block 1 not having taken it.",
  "confidence": 8,
  "notes": "data-runback is also dead — the handler closes over done.league and never reads the attribute.",
  "slice": "index.html 9945–11059",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "idx09-06",
  "title": "Home's \"Next\" tile can show a buddy's declared round as your own next round",
  "file": "index.html",
  "line": "10251–10261",
  "severity": "P2",
  "kind": "bug",
  "category": "correctness",
  "sessionCode": false,
  "quote": "  const next=(src||[]).filter(sr=>{ const d=sr.play_on?localDate(sr.play_on):null; return d && !isNaN(d) && d>=today; })…[0];",
  "failureScenario": "my_schedule returns friends' and shared-league rows with mine=false by design, and watchAll holds them. The filter checks only play_on, so a buddy's declared Wednesday round paints 'Next / WED / 08:30 · PAPAGO GC' on your Home, reading as your commitment — and tapping it opens a schedule where nothing of yours exists that day.",
  "fix": "Add the same `sr.mine !== false` guard upcomingFromSchedule carries ten lines above.",
  "confidence": 9,
  "notes": "upcomingFromSchedule's comment records this exact bug being fixed once already; D94's tile reintroduced it.",
  "slice": "index.html 9945–11059",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "idx09-07",
  "title": "home_hero_state writes a client_events row on every hero paint, twice per Home visit",
  "file": "index.html",
  "line": "10548 (with 10389 and 11449–11450)",
  "severity": "P2",
  "kind": "bug",
  "category": "performance",
  "sessionCode": true,
  "quote": "    window.qaEvent?.('home_hero_state', { stage, role: isPro ? 'pro' : 'player', cta: nextStep?.label || null });",
  "failureScenario": "qaEvent is an INSERT into client_events. seen() guards the same event name with __heroSeen so each state emits once per session; this new line has no guard, and renderHomeHub calls renderHomeHero then renderPulse, which calls it again during the whole preseason window. Every tap onto Home writes two rows — a Pro checking Home ten times a day for two weeks writes ~280 rows where the design intended one. The series also now mixes two payload shapes under one event name, so the launch-week baseline the comment is protecting cannot be read.",
  "fix": "Fold the new fields into seen(), or guard with the same key and give it a distinct event name.",
  "confidence": 8,
  "notes": "",
  "slice": "index.html 9945–11059",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "idx09-08",
  "title": "Board moments on Home are dated by their UTC date — evening posts read as tomorrow",
  "file": "index.html",
  "line": "10935",
  "severity": "P2",
  "kind": "bug",
  "category": "dates",
  "sessionCode": false,
  "quote": "<small>…${dfmtShort((p.created_at||'').slice(0,10))}…</small>",
  "failureScenario": "created_at is a timestamptz serialized in UTC, so .slice(0,10) takes the UTC calendar date and dfmtShort routes it through localDate(). A league announcement posted at 18:10 Phoenix carries 01:10Z the next day and renders 'Sep 1' on a card the golfer watched land the previous evening. Every post created between 17:00 and midnight Phoenix is dated one day late.",
  "fix": "Format from the full stamp, or route it through dgTime.",
  "confidence": 8,
  "notes": "dgTime two functions away branches on `str.length<=10` for exactly this trap. The same slice(0,10) is used for the feed's date BUCKETING at 11255.",
  "slice": "index.html 9945–11059",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "idx09-09",
  "title": "The scorecard footer date has the same UTC truncation",
  "file": "index.html",
  "line": "11035 (rendered at 11056)",
  "severity": "P2",
  "kind": "bug",
  "category": "dates",
  "sessionCode": false,
  "quote": "  const when = (R.finished_at||R.started_at||'').slice(0,10);",
  "failureScenario": "A live round finished at 19:30 Phoenix carries 02:30Z the next day, so the card's footer reads 'Aug 30' for a round everyone played on the 29th — on the one surface whose stated job is to show what actually happened that day.",
  "fix": "Keep the timestamp and format it locally, or reuse dgTime.",
  "confidence": 8,
  "notes": "",
  "slice": "index.html 9945–11059",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "idx09-10",
  "title": "The scorecard's front/back-nine divider is one column off between header and body",
  "file": "index.html",
  "line": "10982 vs 11015, 11029, 11030",
  "severity": "P2",
  "kind": "bug",
  "category": "correctness",
  "sessionCode": false,
  "quote": "  const head = h => `<th class=\"${h===9?'nine':''}\">${h+1}</th>`;\n…\n      const cls = [h===8?'nine':'', …]",
  "failureScenario": "`.sccard .nine{border-right:1px solid var(--line2)}` draws the OUT/IN divider. The header applies it at h===9 while par, SI and every player row apply it at h===8, so the head row's vertical rule is one column right of the rule beneath it. On a 9-hole card the header never reaches h===9 so the head gets no divider at all while the body draws one against the last hole column.",
  "fix": "`h===8` in the header, and guard the body's on N===18.",
  "confidence": 8,
  "notes": "Pre-existing since D92 (f263b26).",
  "slice": "index.html 9945–11059",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "idx09-11",
  "title": "The movement chip and the position figure are measured on two different ladders",
  "file": "index.html",
  "line": "10604 vs 10616",
  "severity": "P3",
  "kind": "bug",
  "category": "correctness",
  "sessionCode": false,
  "quote": "  const pos = sc?.rank || (i+1);\n…\n      if(prior[myId]!=null){ moved = prior[myId]-i;",
  "failureScenario": "sc.rank is `rank() over (order by points desc)` — tied rows share a rank and the next skips — while `i` is the index in a points-only sort with no tie handling. Three squads at 61/55/55: the second 55 has i===2 but rank 2, and last Sunday's snapshot also put it at index 2, so the chip says '— HELD' while the big number moved from 3rd to 2nd. The inverse also occurs.",
  "fix": "Derive `moved` in the same currency as `pos` — rank the snapshot with the same tie rule and compare rank to rank.",
  "confidence": 7,
  "notes": "Only observable when a real tie exists; the code disagreement is certain, the frequency is not.",
  "slice": "index.html 9945–11059",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "idx09-12",
  "title": "The wrapped-season hero shows the naive points index as the final position",
  "file": "index.html",
  "line": "10406–10414",
  "severity": "P3",
  "kind": "bug",
  "category": "correctness",
  "sessionCode": false,
  "quote": "      const i = myId!=null ? teams.findIndex(t=>t.id===myId) : -1;\n      if(i>=0){ … fig=`<div class=\"hh-fig\"><span class=\"hh-pos\">${i+1}</span>…`;",
  "failureScenario": "Season ends with squads at 61/55/55. My squad is the second 55, so findIndex gives 2 and the wrapped hero — the last thing the season says to me — prints a 44px '3rd'. Every in-season surface used heroMyRung, which prefers season_scenarios.rank and printed '2nd' the day before. The season appears to have ended by demoting me.",
  "fix": "Call heroMyRung() here — 10406–10411 is a verbatim copy of 10596–10601 and heroMyRung already falls back to i+1.",
  "confidence": 6,
  "notes": "Held at 6 because I could not confirm loadScenarios still populates window.scenarios after status 'complete'.",
  "slice": "index.html 9945–11059",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "idx09-13",
  "title": "The member's second-move button is a 38px tap target",
  "file": "index.html",
  "line": "10545 (CSS at 1138)",
  "severity": "P3",
  "kind": "bug",
  "category": "accessibility",
  "sessionCode": true,
  "quote": "      + (subStep ? `<button class=\"hh-sub\" data-hsub type=\"button\">${subStep.label}</button>` : ''));",
  "failureScenario": "`.hhero .hh-sub` sets min-height:38px, 6px under the 44px its sibling `.hh-cta` observes. It is the member's 'Post a practice round' — one of only two actions on a member's Home during forming and drawing — directly beneath a 44px primary button, so a thumb aiming low hits the CTA instead.",
  "fix": "Raise min-height to 44px; the visual weight is already carried by colour and size.",
  "confidence": 7,
  "notes": "Same as CSS-13 from the stylesheet side.",
  "slice": "index.html 9945–11059",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "idx09-14",
  "title": "The Clubhouse switcher still hand-derives its league status, contradicting D120",
  "file": "index.html",
  "line": "10216–10218",
  "severity": "P2",
  "kind": "opportunity",
  "category": "copy-consistency",
  "sessionCode": false,
  "quote": "  const chips = (CS.memberships||[]).map(m => groupChip(esc(m.league.name), m.league.id===cur?'Here':(m.role==='commissioner'?'Your league':'In season'), …)).join('')",
  "failureScenario": "A member of a setup-phase league and of one that finished last month opens the Clubhouse: both chips read 'IN SEASON' — one has not started, one is over. A Pro's forming league reads 'YOUR LEAGUE', which is a role, not a stage, so the switcher never states the stage for the Pro at all. Verbatim the failure D120 was written to end, on the surface whose whole job is comparing leagues side by side.",
  "fix": "`stageLabel(m, {bare:true})` already exists and takes the membership; keep the Pro's role as a suffix.",
  "confidence": 8,
  "notes": "leaguePhaseLabel at 10168 does this correctly and has zero callers (idx09-15).",
  "slice": "index.html 9945–11059",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "idx09-15",
  "title": "leaguePhaseLabel and hubGroupRow have no callers",
  "file": "index.html",
  "line": "10168–10177",
  "severity": "P3",
  "kind": "opportunity",
  "category": "dead-code",
  "sessionCode": true,
  "quote": "function leaguePhaseLabel(m){\n  /* D120 · one producer; the Pro's badge rides alongside, not instead of */",
  "failureScenario": "Neither name appears anywhere else in the file; both have been dead since 668a7fe. leaguePhaseLabel received a fresh D120 comment this session, so the stage-vocabulary work was documented on a function nobody calls while renderClubGroups — the live surface forty lines below — kept its hand-derived strings. hubGroupRow is also the only unescaped-interpolation site in this range.",
  "fix": "Delete hubGroupRow; either delete leaguePhaseLabel or make renderClubGroups call it, which fixes idx09-14 at the same time.",
  "confidence": 9,
  "notes": "",
  "slice": "index.html 9945–11059",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "idx09-16",
  "title": "Hero tap telemetry reports the same cta label for all six matrix cells",
  "file": "index.html",
  "line": "10388",
  "severity": "P3",
  "kind": "opportunity",
  "category": "observability",
  "sessionCode": true,
  "quote": "  const wire=(sel,fn)=>{ … qaEvent('home_hero_tap',{cta:sel.replace(/[^a-z]/g,'')}); … };",
  "failureScenario": "wire derives the analytics label from the SELECTOR, not the button. Block 4 wires every cell through '[data-hform]', so 'Name your league', 'Lock it in and invite your crew', 'Share the invite link', 'Draw the squads' and 'Plan a round' all log cta:'datahform'. The comment says launch week is the one clean baseline for whether these CTAs incite — and the tap side of that baseline cannot distinguish the Pro's lock button from the member's Plan a round.",
  "fix": "Give wire an explicit label parameter and pass nextStep.label / subStep.label.",
  "confidence": 8,
  "notes": "",
  "slice": "index.html 9945–11059",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "idx09-17",
  "title": "typeof STRUCT_MIN cannot guard what it looks like it guards",
  "file": "index.html",
  "line": "10505",
  "severity": "P3",
  "kind": "opportunity",
  "category": "dead-code",
  "sessionCode": true,
  "quote": "    const need   = Math.max(0, ((typeof STRUCT_MIN!=='undefined' ? STRUCT_MIN : {})[state.structure] || 4) - n);",
  "failureScenario": "STRUCT_MIN is a const at 12450 in the same classic block. typeof on a const in its temporal dead zone throws ReferenceError rather than returning 'undefined', so this guard would throw in the exact case it appears to protect against. No live failure — the binding is initialized long before any render — but the pattern reads as safety it cannot provide, and the `|| 4` fallback silently applies squads2's minimum to a solo league.",
  "fix": "Drop the typeof wrapper and make the fallback explicit, or hoist STRUCT_MIN.",
  "confidence": 7,
  "notes": "Filed as an opportunity because no call path reaches 10505 before 12450 executes.",
  "slice": "index.html 9945–11059",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "IDX10-01",
  "title": "Round receipt prints the raw index but the engine's allowance-adjusted PvI — the card's own arithmetic doesn't close",
  "file": "index.html",
  "line": "11955-11962",
  "severity": "P1",
  "kind": "bug",
  "category": "money-scoring-correctness",
  "sessionCode": false,
  "quote": "if(r.index_at_post!=null){ rows.push(`… <span>${mine?'Your':'Their'} number that day</span><b>${Number(r.index_at_post).toFixed(1)}</b> …`); }\n  if(pvi!=null){ rows.push(`… <span>Against your number</span><b>${vsShort(pvi)} — …`); }",
  "failureScenario": "Standard preset (95% allowance — every one of the 18 prod leagues). index_at_post 12.4, differential 21.5: the engine computes playing_index 11.8 and pvi −9.7. The receipt renders 'Your number that day 12.4', the differential row, then 'Against your number 9.7 over'. 12.4 − 21.5 = −9.1, not −9.7. On the §16 'show your work' card, the work produces a different number than its own verdict — on essentially every round. round_card already returns playing_index and the client never reads it.",
  "fix": "Render playing_index as 'your number that day', with a sub-row showing `12.4 × 95% = 11.8`, and stop synthesising a 100%-allowance PvI for a league-ranked round.",
  "confidence": 9,
  "notes": "Verified on real prod rows: 288 of 289 differ from the naive figure. This is D123 items (1)/(2), logged and unbuilt. The 'instant → enrich' flip is real but comes from home_feed's own 100% pvi, not from the local fallback the reviewer named; the Tour Card path is inert.",
  "slice": "index.html 11060–12371 — Home stream, Tour Card, You table, drill-down receipts",
  "verification": {
   "status": "survived",
   "correctedSeverity": "P1",
   "votes": [
    {
     "lens": "control-flow",
     "verdict": "CONFIRMED",
     "refuted": false,
     "correctedSeverity": "P1",
     "reasoning": "Every prod league_settings row is 95, and every ranked round differs from the naive figure — round_card returns both a raw index_at_post and an allowance-applied pvi and the client prints them adjacent with the differential between. The 'instant → enrich flip' is real but via home_feed's own 100% pvi, not the local fallback; the Tour Card path is inert (its recent rows carry no id). D123 states the defect and prescribes the fix; unbuilt."
    },
    {
     "lens": "intent-and-precedent",
     "verdict": "CONFIRMED",
     "refuted": false,
     "correctedSeverity": "P1",
     "reasoning": "Prod sweep: 288 of 289 ranked rounds differ from the naive figure and 71 straddle a band. The demo receipt still renders the 'Index × allowance' row the real card dropped — the tell that the step was lost rather than withheld. D2 argues for showing allowance inside receipts and D123 resolves it that way. Nothing is mis-scored; the §16 legibility failure is on the surface whose entire job is §16."
    }
   ]
  }
 },
 {
  "id": "IDX10-02",
  "title": "Receipt's differential formula is missing its parentheses (18-hole) and its ×2 (9-hole)",
  "file": "index.html",
  "line": "11950-11953",
  "severity": "P2",
  "kind": "bug",
  "category": "money-scoring-correctness",
  "sessionCode": false,
  "quote": "rows.push(`<div class=\"mathrow sub\"><span>${r.gross} − ${rt.toFixed(1)} × 113 ⁄ ${r.slope}</span><b>${Number(r.differential).toFixed(1)} DIFFERENTIAL</b></div>`);",
  "failureScenario": "Gross 86, rating 64.9, slope 111, stored differential 21.5: the card prints '86 − 64.9 × 113 ⁄ 111 → 21.5'. Read by operator precedence that expression is 19.9. On a nine (43, 32.5, 111 → stored 21.4) even the parenthesised reading gives 10.7, half the shown result, because score_round doubles it. A golfer who checks the maths on the surface built for checking the maths gets a different number both times.",
  "fix": "`(${gross} − ${rt}) × 113 ⁄ ${slope}${nine ? ' × 2' : ''}`.",
  "confidence": 9,
  "notes": "",
  "slice": "index.html 11060–12371",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "IDX10-03",
  "title": "The Q-23 sign sweep landed on the demo renderer only — every real league still shows the signed number under the renamed header",
  "file": "index.html",
  "line": "11882-11905",
  "severity": "P2",
  "kind": "bug",
  "category": "copy-consistency",
  "sessionCode": true,
  "quote": "  $('#msAvg').textContent = me && me.r ? sgn(me.avg) : '—';\n…\n  let html=`… <th class=\"num\">Avg vs your number</th> …`;\n…\n      <td class=\"num dw\" style=\"color:${p.avg>=0?'var(--pos)':'var(--neg)'}\">${p.r?sgn(p.avg):'—'}</td>",
  "failureScenario": "Commit 2ae4d22 replaced the signed number with vsShort() in renderIndStats — the DEMO renderer — and renamed the column header on BOTH tables. renderIndStatsReal, the only renderer a real league reaches, was not touched. A signed-in golfer whose average is −3.7 reads '−3.7' under a column headed 'Avg vs your number', while the same column in the same build reads '3.7 over' in the diorama and the receipt one tap away says '3.7 over'. The colour rules diverged too: −0.5 is green in demo (`avg>-1`) and red in the real table (`avg>=0`).",
  "fix": "Use vsShort at 11882, 11905 and 11786 with the `avg > -1` threshold; replace ' vs index' at 11921 and 12295.",
  "confidence": 9,
  "notes": "The sweep the audit brief asked about: yes, sites kept a raw sign, and they are the ones real users see.",
  "slice": "index.html 11060–12371",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "IDX10-04",
  "title": "Receipt stutters 'played to it — PLAYED TO IT' and drops the number it promises",
  "file": "index.html",
  "line": "11958-11962",
  "severity": "P2",
  "kind": "bug",
  "category": "copy-consistency",
  "sessionCode": true,
  "quote": "rows.push(`… <b>${mine?vsShort(pvi):theirs(vsShort(pvi))} — ${(mine?bandName(pvi):theirs(bandName(pvi))).toUpperCase()}</b></div>`);",
  "failureScenario": "pvi 0.5 (any value in the played-to-it band): vsShort returns 'played to it' and bandName returns 'Played to it', so the row reads 'Against your number: played to it — PLAYED TO IT'. The comment directly above claims the number now explains the phrase, but vsShort emits no number in that band, so the actual PvI is nowhere on the card — the previous code did show it. `theirs()` is also a no-op on vsShort output, which contains no 'your'.",
  "fix": "Render the band once and the signed figure once.",
  "confidence": 9,
  "notes": "Introduced by 2ae4d22 this session.",
  "slice": "index.html 11060–12371",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "IDX10-05",
  "title": "`dgDay` counts elapsed hours, not calendar days — today's round reads 'Yesterday' after noon",
  "file": "index.html",
  "line": "11136-11139",
  "severity": "P2",
  "kind": "bug",
  "category": "dates-timezones",
  "sessionCode": false,
  "quote": "function dgDay(t){\n  const days = Math.round((Date.now() - dgTime(t)) / 864e5);\n  return days<=0 ? 'Today' : days===1 ? 'Yesterday' : dfmtShort(String(t).slice(0,10));\n}",
  "failureScenario": "A round posted at 9:00am viewed at 11:30pm the same day is 14.5h old → 0.60 days → Math.round → 1 → 'Yesterday'. For a row carrying only played_on the flip happens at exactly noon, every day. And the 'Yesterday' branch is never correct: a round actually played yesterday is ≥1.5 days old by any normal viewing hour and rounds to 2. The label fires only when it is wrong.",
  "fix": "Bucket on calendar midnights the way feedBuckets already does.",
  "confidence": 9,
  "notes": "Only caller is the quiet-day digest — the line shown on exactly the days when nothing else is on screen.",
  "slice": "index.html 11060–12371",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "IDX10-06",
  "title": "Season ceremony can publicly name every member as 'Still owed to the pot' when the buy-in read failed",
  "file": "index.html",
  "line": "12138-12140",
  "severity": "P2",
  "kind": "bug",
  "category": "silent-failure",
  "sessionCode": false,
  "quote": "const bi=window.buyIns||{};\n  const owing=fromLedger && collectedCents<potCents ? members.filter(m=>!bi[m.id]?.paid).map(…) : [];",
  "failureScenario": "window.buyIns is built from a select whose {error} is dropped (index.html:15058, the CLAUDE.md landmine), so a failed read leaves {} — as does resetToBlank. On a D106 season where the server reports collected < pot, csSettlement then evaluates `!undefined?.paid` true for every member and the ceremony renders 'Still owed to the pot — Alice, Bob, Carla, Dave, …' naming the entire league, including everyone who paid, in the one takeover the whole crew opens at once.",
  "fix": "Track loaded-ness (null on failure) and render the shortfall amount without a name list; destructure the error at 15058.",
  "confidence": 7,
  "notes": "",
  "slice": "index.html 11060–12371",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "IDX10-07",
  "title": "Every trophy replays the 'fresh mint' engrave on the first real render, because the baseline latches on the demo boot render",
  "file": "index.html",
  "line": "11668-11695",
  "severity": "P3",
  "kind": "bug",
  "category": "correctness",
  "sessionCode": false,
  "quote": "let _trophySeen=null;   /* C4: ids already in the case this session - a tile only engraves on ARRIVAL */\n…\n  _trophySeen = new Set(ids);",
  "failureScenario": "renderIndStats → renderTrophyCase runs at boot with state.demo true, populating _trophySeen with the DEMO ids. After sign-in loadTrophies re-renders with the golfer's real trophies; none of their ids are in the set, so fresh() is true for all and every tile gets the mint animation. A returning user with six trophies watches all six 'arrive' as if just won — the opposite of the C4 comment. The empty-case early return does not help, because the assignment is on the line above it.",
  "fix": "Only take the baseline from a data-bearing render, and reset it when state.demo flips.",
  "confidence": 8,
  "notes": "Visual/narrative, not data. Verified the animation is not reduced-motion-only.",
  "slice": "index.html 11060–12371",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "IDX10-08",
  "title": "Late `round_card` response overwrites whichever receipt sheet is open, title and all",
  "file": "index.html",
  "line": "11984-12008",
  "severity": "P3",
  "kind": "bug",
  "category": "race",
  "sessionCode": false,
  "quote": "    const body=document.getElementById('rcptBody');\n    if(!body) return;                             /* sheet already closed */\n    body.innerHTML = roundCardBody(r, pvi, …);",
  "failureScenario": "On a slow connection: tap card A, close it, tap card B. A's RPC resolves, finds #rcptBody (it now belongs to B) and rewrites the body, #shTitle and #shSub. The golfer is looking at a receipt for round A labelled as round B's card, with no error. The existence check cannot detect this because the id is reused for every round receipt.",
  "fix": "Stamp the sheet with the round id and bail unless it still matches.",
  "confidence": 7,
  "notes": "Covers the case where the second sheet is opened from a different surface too.",
  "slice": "index.html 11060–12371",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "IDX10-09",
  "title": "Feed buckets slice a UTC timestamp to a date string — evening posts sit in the wrong day for a full day",
  "file": "index.html",
  "line": "11254-11255",
  "severity": "P3",
  "kind": "bug",
  "category": "dates-timezones",
  "sessionCode": false,
  "quote": "const items = (window.homeFeedRows||[]).map((r,i) => ({ …, d:((r.played_on||r.created_at||'')+'').slice(0,10), … }))",
  "failureScenario": "A league note posted 7pm Monday Phoenix comes back as Tuesday UTC, so it sits under 'Today' all day Tuesday. Every post created between 5pm and midnight is affected. The documented UTC-midnight landmine applied to created_at instead of played_on — and dgTime two functions away explicitly handles the same trap.",
  "fix": "Derive the bucket date from the local rendering of the timestamp; keep preferring played_on, which is already a bare local date.",
  "confidence": 8,
  "notes": "Cosmetic (a section header), but postRow renders its own dfmtShort of the same slice, so the label and the section agree with each other and both disagree with reality.",
  "slice": "index.html 11060–12371",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "IDX10-10",
  "title": "Digest interpolates `post_kudos.emoji` into innerHTML unescaped — the only unescaped field in the slice",
  "file": "index.html",
  "line": "11213-11214",
  "severity": "P3",
  "kind": "bug",
  "category": "security",
  "sessionCode": false,
  "quote": "bits.push(m.e ? `<b>${esc(m.who)}</b> ${m.e}’d your ${m.gross}` : …);",
  "failureScenario": "m.e is k.emoji straight off post_kudos, written by a DIRECT client insert whose only constraint is `char_length(emoji) <= 8`. A league member can write an 8-character string containing markup and every leaguemate who has a round reacted to gets it injected raw into #homeDigest's innerHTML. Eight characters is too short for a working script payload, so this is markup corruption rather than XSS today — but it is one length-constraint change from worse, and the same value is escaped everywhere else it is rendered.",
  "fix": "`${esc(m.e)}`.",
  "confidence": 7,
  "notes": "Verified the RLS policy on post_kudos does not constrain the emoji value.",
  "slice": "index.html 11060–12371",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "IDX10-11",
  "title": "Squad receipt attributes the entire squad total to 'the ledger' when the stats read failed",
  "file": "index.html",
  "line": "12285-12291",
  "severity": "P3",
  "kind": "bug",
  "category": "silent-failure",
  "sessionCode": false,
  "quote": "const fromRounds=rows.reduce((a,r)=>a+r.pts,0);\n  const adj=t.pts-fromRounds;   /* the ledger's net: bonuses − penalties */",
  "failureScenario": "The reads that build window.indRows sit inside a try whose catch only warns. If they fail, indRows stays [] and tapping a squad renders 'Counting rounds 0 / Bonuses & penalties · the ledger +142 / Total 142' with an empty 'Who built it' list — the receipt asserting, with the confidence of arithmetic, that 142 points came from bonuses in a league that has no bonuses.",
  "fix": "Distinguish 'not loaded' from 'zero': keep indRows null on failure and say the breakdown isn't loaded.",
  "confidence": 7,
  "notes": "",
  "slice": "index.html 11060–12371",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "IDX10-12",
  "title": "'Most Improved · needs 2+ rounds' is printed for a reason that isn't the reason",
  "file": "index.html",
  "line": "11893-11895",
  "severity": "P3",
  "kind": "bug",
  "category": "copy-consistency",
  "sessionCode": false,
  "quote": "const movers=rows.filter(p=>p.d!=null && p.d<-0.04)…;\n  else { … $('#awImpS').textContent='Most Improved · needs 2+ rounds'; }",
  "failureScenario": "p.d is non-null for anyone with ≥2 rounds; the filter also drops everyone whose index rose or held. A mid-season league where all eight golfers have 6+ rounds but nobody's index has fallen renders 'needs 2+ rounds' — telling a league full of posted rounds that it hasn't posted enough, with the R column beside it reading 6, 7, 9.",
  "fix": "Split the two cases: 'nobody has moved yet' vs 'needs 2+ rounds'.",
  "confidence": 8,
  "notes": "Verified the delta direction is right.",
  "slice": "index.html 11060–12371",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "IDX10-13",
  "title": "The floor sentence has no season-state gate on the path that actually runs — the SX-01 guard sits on a dead branch",
  "file": "index.html",
  "line": "11449-11456",
  "severity": "P3",
  "kind": "bug",
  "category": "copy-consistency",
  "sessionCode": false,
  "quote": "    box.innerHTML = (state.floor>0 && !state.demo) ? `<p class=\"fine\">Monthly floor · ${floorSentence()}</p>` : '';\n    return;",
  "failureScenario": "For a league in the season phase but before first tee, this early return fires and Home prints the whole penalty clock for a season that has not begun. The guard that fixed exactly this (`if(state.phase!=='season' || atStarter())`) lives further down on the fallback branch, which the early return makes unreachable — its own comment says '(none today)'. D122 added seasonState()==='live' to the 'Month closes' chip 180 lines above for the same complaint.",
  "fix": "Gate the early-return line on seasonState()==='live' too.",
  "confidence": 6,
  "notes": "Confidence 6 because it is a rule statement rather than a countdown, so it is arguably tolerable pre-season — but it is inconsistent with the guard the same session just added on the adjacent chip.",
  "slice": "index.html 11060–12371",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "IDX10-14",
  "title": "`dgAsOf` latches the seen-mark before the profile id exists, binding the session to the wrong localStorage key",
  "file": "index.html",
  "line": "11110-11112, 11193",
  "severity": "P3",
  "kind": "bug",
  "category": "race",
  "sessionCode": false,
  "quote": "function dgKey(){ return 'cs.seen.' + (window.CS?.profile?.id || (state.demo ? 'demo' : 'anon')); }\n…\n  if(window.dgAsOf===undefined){ window.dgAsOf = dgReadSeen(); dgMarkSeen(); }",
  "failureScenario": "renderHomeHub fires from several independent module promises that race the profile load. If any wins, dgKey resolves to cs.seen.demo or cs.seen.anon, dgAsOf latches from that key and dgMarkSeen writes to it. Because the latch is `=== undefined`, no later render re-reads — so the digest compares against the wrong timestamp for the rest of the session and the profile's own key is never advanced, so the next session's window is also wrong.",
  "fix": "Latch per key and re-read when dgKey changes, or return early while the profile id is unknown.",
  "confidence": 6,
  "notes": "The exact promise ordering varies with network; the demo→real transition case is deterministic.",
  "slice": "index.html 11060–12371",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "IDX10-15",
  "title": "~50 lines of unreachable Home code: `homeRoundCard` / `renderHomeRounds` / `fillHomeWeather` target elements that no longer exist",
  "file": "index.html",
  "line": "11302-11352",
  "severity": "P3",
  "kind": "opportunity",
  "category": "dead-code",
  "sessionCode": false,
  "quote": "function renderHomeRounds(){\n  const box=$('#homeRounds'), head=$('#homeRoundsHead'); if(!box||!head) return;",
  "failureScenario": "Neither id exists anywhere in the document, so all three functions are unreachable — keeping ~50 lines alive that read as live behaviour on review, including a weather edge-function call path and the slice's only bare classic→module reference (fmtTee), currently harmless only because the function can never run.",
  "fix": "Delete the three functions, the export and the call; if kept deliberately, say so and make the fmtTee reference `window.fmtTee?.()`.",
  "confidence": 9,
  "notes": "Verified by grepping every id=\"home*\" in the markup — the retired ids are gone.",
  "slice": "index.html 11060–12371",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "IDX10-16",
  "title": "The digest's only receipt affordance is an `aria-hidden` image with a click handler",
  "file": "index.html",
  "line": "11225-11229",
  "severity": "P3",
  "kind": "opportunity",
  "category": "accessibility",
  "sessionCode": false,
  "quote": "const ph = best.r && best.r.photo_url ? `<img class=\"dgthumb\" src=\"…\" alt=\"\" data-dg-rcpt aria-hidden=\"true\">` : '';",
  "failureScenario": "On a quiet day the resurfaced round's receipt is reachable only by tapping the thumbnail, which is aria-hidden, has no role, no tabindex and no accessible name — so a keyboard or screen-reader user cannot reach or activate it, and the digest reads as static text. Elsewhere in the same file the equivalent affordance is done correctly (feedRow gives its cards role/tabindex/aria-label). The thumb is also exactly 44×44 with no padding.",
  "fix": "Make the whole .digest block the target the way feedRow does, and leave the img decorative.",
  "confidence": 8,
  "notes": "",
  "slice": "index.html 11060–12371",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "IDX11-01",
  "title": "resetToBlank() leaves state.startISO (and durWeeks) behind — a second league inherits the first league's first-tee date",
  "file": "index.html",
  "line": "12376-12380",
  "severity": "P1",
  "kind": "bug",
  "category": "correctness",
  "sessionCode": false,
  "quote": "  state.seasonStart=null; state.seasonEnd=null;\n  state.finish='cup_final';   /* never carry one league's dial into the next */",
  "failureScenario": "Claimed: applyBylaws writes League A's committed starts_on into state.startISO, resetToBlank does not clear it, and lockBylaws then creates the new league's season five months in the past with A's length.",
  "fix": "Add state.startISO='' to resetToBlank and reset the other per-league dials there too.",
  "confidence": 9,
  "notes": "DROPPED — both verifiers refuted the mechanism. applyBylaws:14862 only writes startISO `if(state.seasonStart)`, and every call site is preceded by resetToBlank nulling seasonStart, with the season row loaded afterwards; so startISO can only ever hold the boot default (next Saturday) or the current wizard's own dial. The three prod seasons whose starts_on predates created_at are explained otherwise.",
  "slice": "index.html 12372–13427 — blank slates, phase/wizard/routing, install nudge, init",
  "verification": {
   "status": "dropped",
   "correctedSeverity": null,
   "votes": [
    {
     "lens": "control-flow",
     "verdict": "REFUTED",
     "refuted": true,
     "reasoning": "The causal chain needs applyBylaws to write League A's committed starts_on into state.startISO, and that write is unreachable: every applyBylaws call site is preceded by resetToBlank nulling state.seasonStart, and enterLeague loads the seasons row only afterwards. startISO can only hold the boot default or the current wizard's own dial."
    },
    {
     "lens": "intent-and-precedent",
     "verdict": "REFUTED",
     "refuted": true,
     "reasoning": "Same: line 14862 is dead on every path that reaches it, so the stated mechanism does not exist. The prod seasons whose starts_on predates created_at are explained otherwise."
    }
   ]
  }
 },
 {
  "id": "IDX11-02",
  "title": "Install nudge re-appears on every navigation after the golfer dismisses it (`shown` is never cleared)",
  "file": "index.html",
  "line": "13395-13400",
  "severity": "P1",
  "kind": "bug",
  "category": "correctness",
  "sessionCode": true,
  "quote": "  window.csHideInstallNudge = function(){\n    if(!n) return;\n    if(busy()) n.style.display = 'none';\n    else if(shown) n.style.display = 'flex';\n  };\n  function dismiss(){ try{ localStorage.setItem('cs_nudge_done','1'); }catch(_){}; if(n) n.style.display = 'none'; }",
  "failureScenario": "After the first posted round, show() sets shown=true. The golfer taps ✕ → dismiss() writes cs_nudge_done and hides it but leaves shown===true. Their next tap on any tab runs switchView, whose tail calls csHideInstallNudge; busy() is false on Home, so the `else if(shown)` branch sets display back to flex. The banner they just dismissed returns on every view change for the rest of the session — including after 'Add to Home Screen', which also routes through dismiss(). Only a reload clears it.",
  "fix": "Set `shown = false` in dismiss(), and re-check eligibility in the re-show branch.",
  "confidence": 9,
  "notes": "Introduced by the Q-13 hunk this session; the pre-session code had no re-show path. Verifiers corrected severity to P2 — session-scoped, self-heals on reload, and the Q-13 move already stopped it eating the ⊕'s taps.",
  "slice": "index.html 12372–13427",
  "verification": {
   "status": "survived",
   "correctedSeverity": "P2",
   "votes": [
    {
     "lens": "control-flow",
     "verdict": "CONFIRMED",
     "refuted": false,
     "correctedSeverity": "P2",
     "reasoning": "`shown` has exactly one write site and is never reset; csHideInstallNudge's only caller is the unconditional tail of switchView, and its else-branch never consults eligible(). Reproduced the sequence in an isolated simulation of the block's verbatim logic. Bounded to the session (localStorage persists the dismissal across reloads), no data or auth consequence."
    },
    {
     "lens": "intent-and-precedent",
     "verdict": "CONFIRMED",
     "refuted": false,
     "correctedSeverity": "P2",
     "reasoning": "The IIFE's own header states the contract it violates ('Shows once, ever; no-ops when … already dismissed') and D23 says each nudge fires once. Q-13's commit message and remediation-plan row scope the change to hiding on full-screen surfaces only — no re-show was specified, and git log -S shows the line born in this session. It does not block the ⊕ (Q-13 moved it below the tab bar in the stack); that belongs to IDX11-03."
    }
   ]
  }
 },
 {
  "id": "IDX11-03",
  "title": "The relocated install nudge sits on top of the sticky header and blocks its only two controls",
  "file": "index.html",
  "line": "3748",
  "severity": "P2",
  "kind": "bug",
  "category": "mobile-layering",
  "sessionCode": true,
  "quote": "<div id=\"installNudge\" style=\"… top:calc(env(safe-area-inset-top) + 8px); … z-index:24; …\">",
  "failureScenario": "The header occupies roughly safe+0 to safe+47 and contains exactly two controls; the nudge is fixed at safe+8, ≈45px tall, z-index 24 > 20, and opaque, so it takes the hit-test for both. Once IDX11-02 re-shows it after dismissal, the header stays untappable for the rest of the session. The same defect class Q-13 was fixing on the ⊕ — relocated, not removed.",
  "fix": "z-index below 20 and an offset below the header, or dock it above the tab bar.",
  "confidence": 7,
  "notes": "Same collision as CSS-17 / IDX02-02 from two other slices.",
  "slice": "index.html 12372–13427",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "IDX11-04",
  "title": "The install nudge's BUSY list names `view-record` (a 3-card menu) but not `view-post` (the composer it says it protects)",
  "file": "index.html",
  "line": "13390-13393",
  "severity": "P2",
  "kind": "bug",
  "category": "correctness",
  "sessionCode": true,
  "quote": "  var BUSY = ['view-play','view-record','view-wizard','view-draft'];",
  "failureScenario": "#view-record is the three-option Golf menu, not the composer; the composer is #view-post and is absent from BUSY. A golfer who reaches the composer inside the 3.2s boot timer gets the banner dropped over it, and switchView('post') itself calls csHideInstallNudge, whose `else if(shown)` branch explicitly re-shows it there. Meanwhile the page it does guard needs no guarding.",
  "fix": "Replace 'view-record' with 'view-post' (or list both).",
  "confidence": 8,
  "notes": "Introduced this session. Both ids are real, which is why the wrong one reads plausibly.",
  "slice": "index.html 12372–13427",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "IDX11-05",
  "title": "Danger-zone gate tests `state.phase!=='complete'`, a value state.phase can never hold — a settled league is offered 'Cancel this league · the season is under way'",
  "file": "index.html",
  "line": "13336-13345",
  "severity": "P2",
  "kind": "bug",
  "category": "correctness",
  "sessionCode": false,
  "quote": "    if(dz) dz.style.display = (isPro && state.phase!=='complete') ? '' : 'none';",
  "failureScenario": "enterLeague maps league phase to only setup/draft/season, so a crowned league arrives as 'season' and the gate is always true. The Pro of a finished league sees the red Cancel link with the note '(the season is under way — a pot needs every member to approve)'. Tapping it calls request_league_cancel, which raises 'completed seasons are the record book — they cannot be cancelled'; that sentence starts lowercase so looksLikeOurSentence rejects it and the Pro reads 'Something went wrong — please try again.'",
  "fix": "Test the real signal the rest of the file uses: `CS.league?.phase==='complete' || CS.season?.status==='complete'`.",
  "confidence": 9,
  "notes": "renderStats and #hhPhase already do exactly this; this call site was missed.",
  "slice": "index.html 12372–13427",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "IDX11-06",
  "title": "wizRoster() can only ever return 1 — the wizard dims every squad option and toasts on every tap",
  "file": "index.html",
  "line": "12451-12470",
  "severity": "P2",
  "kind": "bug",
  "category": "dead-state",
  "sessionCode": false,
  "quote": "function wizRoster(){\n  /* D97: the staged-invitee term is gone with the staging itself */\n  return 1 + (state.emails||[]).filter(e=>e.includes('@')).length;\n}",
  "failureScenario": "A-W2 removed the email slots; renderEmails returns at its first line because #emailSlots does not exist, and its input listener is the only writer of an address into state.emails. So wizRoster() returns 1 for every league, forever: renderStructFit dims the 2-, 3- and 4-squad buttons for every Pro and prints '1 golfer staged — solo fits' even for an eight-man league; the #structSeg handler toasts '4 squads plays best at 8+' on every squad tap; and wizPortrait renders roster 1, so a $75 stake draws 'The pot $75 · $75 / player · 1 in so far'.",
  "fix": "Base the wizard's roster on the real membership when one exists, and delete the staged-invitee wording.",
  "confidence": 9,
  "notes": "renderStructFit is live — renderSetup calls it on init and on every dial change. renderEmails and updateInviteNote are now no-ops kept alive only for callers.",
  "slice": "index.html 12372–13427",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "IDX11-07",
  "title": "totalWeeks() disagrees with the server's week engine by exactly one for every season the current client locks",
  "file": "index.html",
  "line": "12432-12440",
  "severity": "P2",
  "kind": "bug",
  "category": "scoring-consistency",
  "sessionCode": false,
  "quote": "function totalWeeks(){ const s=seasonS(), e=seasonE(); return Math.max(1, Math.ceil((e-s)/(7*864e5))); }",
  "failureScenario": "seasonEndDate() returns start + durWeeks*7 days, so ends_on − starts_on is an exact multiple of 7 (prod: 42, 91, 119, 182, 273). The client reports ceil(7N/7)=N; the engine computes ceil((ends_on − starts_on + 1)/7) = N+1. On a live season starting 2026-09-05 and ending 2027-03-06 the server mints week_clashes.week_no 27 of 27 on the last day while the header reads 'Wk 26 / 26', renderClash draws that clash's window six days past the season end, and the settle predicate cannot fire before ensure_week_clash's status gate closes — leaving a dangling unsettled week-27 clash.",
  "fix": "Match the engine (`ceil(((e-s)/864e5 + 1)/7)`), or better fix seasonEndDate to return start + durWeeks*7 − 1 so ends_on is the inclusive last day.",
  "confidence": 7,
  "notes": "Cross-range cause at seasonEndDate:7610. Could not observe a season that has reached its last day, so the dangling-clash half is read from the migration.",
  "slice": "index.html 12372–13427",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "IDX11-08",
  "title": "renderPhase and renderLeagueMeta still print the status vocabulary D120 retired — the five-way contradiction is live one screen apart",
  "file": "index.html",
  "line": "12671-12676",
  "severity": "P2",
  "kind": "bug",
  "category": "contradictory-copy",
  "sessionCode": true,
  "quote": "  if(hd) hd.textContent = state.structure==='solo' ? 'Individual league — no squads'\n    : p==='setup' ? 'OPENS AFTER SETTINGS LOCK'\n    : p==='draft' ? 'LIVE NOW — CAPTAINS READY'\n    : 'Complete · rosters locked';",
  "failureScenario": "D120 requires every status string to read STAGE_LABEL and explicitly retires 'captains', 'LIVE NOW', 'invites open' and 'rosters locked'. This session applied it to exactly one site in this slice — inside a display:none container. For one league in phase 'draft' a golfer today reads 'Squads drawing' on Home, 'Squads drawing' in the Clubhouse group row, 'Squad formation' in the League Room header, and 'LIVE NOW — CAPTAINS READY' on the League tab. In setup they read 'Forming' on Home and 'Setup — invites open' in the room — the string D120 flags as also contradicting D40/D112.",
  "fix": "Route these and #hhPhase through STAGE_LABEL[leagueStage()] / stageLabel(); rewrite #setupInviteSub and #draftPoolSub as 'N in · need M to tee off'.",
  "confidence": 9,
  "notes": "The one-line STAGE_LABEL edit at 12657 is session code; the uncorrected sites are older, but the session's own sweep is what makes them a contradiction rather than a uniform legacy.",
  "slice": "index.html 12372–13427",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "IDX11-09",
  "title": "#phaseSub is inside a display:none container — eleven lines of branch logic render into an invisible node on every call",
  "file": "index.html",
  "line": "12650-12663",
  "severity": "P3",
  "kind": "bug",
  "category": "dead-code",
  "sessionCode": false,
  "quote": "  $('#phaseSub').textContent = p==='setup' ? … : isCupFinal() ? `CUP FINAL · Wk ${currentWeek()} / ${totalWeeks()} · fresh slate · …`",
  "failureScenario": "#phaseSub lives inside `<div class=\"title\" style=\"display:none\">` and nothing un-hides it, so every renderPhase call computes this chain and throws it away. Being invisible is also why nobody caught that it has no 'complete' branch: because enterLeague collapses complete to 'season' and isCupFinal() has no upper bound, a settled league would read 'CUP FINAL · Wk 26 / 26 · fresh slate' here — the exact bug renderStats documents and fixes only for its own strip.",
  "fix": "Delete the block and the markup, or bind it to stageLabel() and un-hide it. Give isCupFinal() its missing upper bound regardless.",
  "confidence": 9,
  "notes": "boot() also writes #phaseSub, equally invisibly.",
  "slice": "index.html 12372–13427",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "IDX11-10",
  "title": "Calendar day cells are divs with click handlers — no keyboard or screen-reader route to any day",
  "file": "index.html",
  "line": "12739-12745",
  "severity": "P3",
  "kind": "bug",
  "category": "accessibility",
  "sessionCode": false,
  "quote": "    html+=`<div class=\"calcell…\" data-cd=\"${d}\">${d}<span class=\"caldots\">${dots}</span></div>`;",
  "failureScenario": "A keyboard or VoiceOver user can reach #calPrev and #calNext (real buttons) but not a single day: .calcell has no role, tabindex or accessible name, so the only route to 'Put your round on this day' is a pointer tap. A day with rounds is announced as a bare number — the meaning is carried by 5px unlabelled dots. The column headers render single letters with two ambiguous pairs and no abbr.",
  "fix": "Emit a <button> with an aria-label for tappable days, and give the headers accessible names.",
  "confidence": 8,
  "notes": "The same div-as-button pattern is widespread, but this instance is the only way to reach a whole feature.",
  "slice": "index.html 12372–13427",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "IDX11-11",
  "title": "The Major's exhibition list sorts a level (0.0) card to the bottom because of `pvi || -99`",
  "file": "index.html",
  "line": "13101",
  "severity": "P3",
  "kind": "bug",
  "category": "correctness",
  "sessionCode": false,
  "quote": "    const ex=cards.filter(…).sort((a,b)=>Number(b.pvi||-99)-Number(a.pvi||-99));",
  "failureScenario": "An exhibition entrant who plays exactly to their number has pvi 0, and `0 || -99` is −99, so that card sorts below every other exhibition card including one at −12. Its own row still renders 'LEVEL', so the board shows a card labelled LEVEL underneath one labelled '12 OVER'.",
  "fix": "`b.pvi ?? -99`.",
  "confidence": 9,
  "notes": "The neighbouring guards in the same function use the correct `!= null` test, so this is an isolated slip.",
  "slice": "index.html 12372–13427",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "IDX11-12",
  "title": "The bylaws card derives allowance / verification / penalty from the preset index rather than the stored settings row",
  "file": "index.html",
  "line": "12533-12537",
  "severity": "P3",
  "kind": "opportunity",
  "category": "single-producer",
  "sessionCode": false,
  "quote": "    <div class=\"byrow\"><span>HANDICAP ALLOWANCE</span><b>${ALLOW[state.preset]}%</b></div>",
  "failureScenario": "league_settings stores handicap_allowance, verification and floor_penalty as their own columns, and lock_league writes them separately. The bylaws card — the league's constitution — reconstructs all three from state.preset, which applyBylaws clamps to 'standard' whenever b.preset is unrecognised. Any league whose preset string and allowance column diverge would print 95% / Attested / −5 sqd pts while the engine scores at whatever the column holds.",
  "fix": "Read CS.settings, which D129 already keeps in memory.",
  "confidence": 6,
  "notes": "Latent — verified against prod that no league currently diverges.",
  "slice": "index.html 12372–13427",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "IDX11-13",
  "title": "lockedPhase() drops the {error} — a transient read failure is reported as 'Lock failed.' on a league the server just locked",
  "file": "index.html",
  "line": "16151-16158",
  "severity": "P2",
  "kind": "bug",
  "category": "silent-failure",
  "sessionCode": true,
  "quote": "    const { data } = await sb.from('leagues').select('phase').eq('id', CS.league.id).maybeSingle();\n    const p = data?.phase;\n    return (p && p !== 'setup') ? p : null;\n  } catch(_){ return null; }",
  "failureScenario": "This is the probe the rewritten #lockBtn handler uses to decide, after an exception, whether the lock committed. supabase-js returns {data:null,error} rather than throwing, and the error is discarded — so a phone that loses signal for that one SELECT yields 'not locked', and the handler paints the persistent #lockErr with 'Lock failed.' on a league whose bylaws the server committed a second earlier. The exact 25-day incident Q-01 was written to end, reachable through the probe instead of the commit.",
  "fix": "Destructure the error and return a third value meaning 'could not confirm', with copy that says so.",
  "confidence": 8,
  "notes": "Out of the assigned range but named in the brief. Also checked and sound: btn.disabled=false sits on the single fall-through path, and lock_league is idempotent on locked_at.",
  "slice": "index.html 12372–13427",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "IDX12-01",
  "title": "The board fetches the OLDEST 120 posts — a league past 120 posts has a frozen board",
  "file": "index.html",
  "line": "15132-15133",
  "severity": "P0",
  "kind": "bug",
  "category": "correctness",
  "sessionCode": false,
  "quote": "let r = await sb.from('posts').select(COLS(true)).eq('league_id', CS.league.id)\n      .order('created_at', { ascending:true }).limit(120);",
  "failureScenario": "`ascending:true` + limit(120) is ORDER BY created_at ASC LIMIT 120 — the first 120 posts the league ever made. Past 120, nothing new is ever fetched: a member sends a chat message, the realtime posts-INSERT handler re-runs loadStandingsAndFeed, which re-fetches the same ancient rows, and the message never renders — worse than invisible, because the optimistic echo is then wiped by the rebuild the code comment relies on. Round posts, moments, announcements and settlement cards all stop appearing. The sibling Home fetch gets this right (descending, limit 20), which is the tell.",
  "fix": "Fetch descending and reverse client-side on both the primary and the skew retry; renderFeed walks the array in order, so the reverse preserves reading order exactly.",
  "confidence": 9,
  "notes": "Verifiers corrected severity to P1: the only prod league past 120 (Ridgeline Cup, 121) is a seeded QA league created 2026-08-28 with no real members; the largest human league has 15 posts. Imminent, not currently biting a real user.",
  "slice": "index.html 13433–15400 — module block first third: client, auth, profile, People, invites, push, memberships, enterLeague, loads, formation",
  "verification": {
   "status": "survived",
   "correctedSeverity": "P1",
   "votes": [
    {
     "lens": "control-flow",
     "verdict": "CONFIRMED",
     "refuted": false,
     "correctedSeverity": "P1",
     "reasoning": "No merge, top-up, cursor or pagination exists anywhere; feed is emptied and rebuilt entirely from the ascending window, and the realtime handler re-runs it with no filter. The Home fetch that IS descending excludes chat and round kinds, so those two render only on the board. Prod: the only league past 120 is a seeded QA league with no real members; the largest human league has 15 posts. Imminent, not currently biting."
    },
    {
     "lens": "intent-and-precedent",
     "verdict": "CONFIRMED",
     "refuted": false,
     "correctedSeverity": "P1",
     "reasoning": "Nothing in the spec, decision log or comments describes an oldest-first board; the only trace is a performance note about render cost. git log -S returns the initial commit and one column-adding commit that copied the ordering verbatim — never fixed, never accepted. The sender's own chat is worse than invisible: the optimistic echo is wiped by the rebuild the code comment relies on."
    }
   ]
  }
 },
 {
  "id": "IDX12-02",
  "title": "CS.season is never cleared on a league switch — a season-less league renders the previous league's standings, squads and pot",
  "file": "index.html",
  "line": "14947-14956",
  "severity": "P1",
  "kind": "bug",
  "category": "correctness",
  "sessionCode": false,
  "quote": "    if(season){\n      CS.season = season;\n      CS.payouts = [];",
  "failureScenario": ".maybeSingle() returns {data:null,error:null} for a league with no seasons row — every pre-lock league. resetToBlank clears state.seasonStart/End but never CS.season, and only goClubhouse() nulls it, while the Clubhouse chips and the switcher sheet call enterLeague directly. So switching from a live League A to a forming League B leaves CS.season = A's season: loadLeagueData loads A's squads and buy_ins, loadStandingsAndFeed loads A's standings, snapshots and ranked rounds and renders them as B's room, the formation screen shows A's squad names with '—' for every player, loadScenarios runs season_scenarios(A), and if A just closed, A's trophy ceremony pops over League B. RLS allows every read because the golfer really is a member of A.",
  "fix": "Clear CS.season and CS.payouts before the seasons query, not conditionally after it — and clear CS.squads/weekClash/cupRace too. Best placed in resetToBlank, since createLeague reaches the same state by a second path.",
  "confidence": 9,
  "notes": "Verified in prod: at least one account is in two leagues where one has no season row. Verifiers found writes on the stale value too — sendChatFrom stores a chat under the other league's season_id, and the quick post stamps season_id onto an immutable round.",
  "slice": "index.html 13433–15400",
  "verification": {
   "status": "survived",
   "correctedSeverity": "P1",
   "votes": [
    {
     "lens": "control-flow",
     "verdict": "CONFIRMED",
     "refuted": false,
     "correctedSeverity": "P1",
     "reasoning": "CS.season has four assignment sites and only goClubhouse nulls it; neither switch path routes through it; resetToBlank does not clear it; create_league inserts no seasons row so maybeSingle legitimately returns null; and prod holds an account in exactly the two-league shape required. Understated: there are persisted WRITES on the stale value — chat stored against the other league's season_id, and the quick post stamping season_id onto an immutable round."
    },
    {
     "lens": "intent-and-precedent",
     "verdict": "CONFIRMED",
     "refuted": false,
     "correctedSeverity": "P1",
     "reasoning": "Unchanged since the initial commit; the only CS.season=null ever added was goClubhouse's, in the switcher commit. resetToBlank's own comment ('never carry one league's dial into the next') states the invariant this violates — CS.season and CS.squads are omissions from that list. The proposed fix is incomplete: CS.squads, window.weekClash and window.cupRace need clearing too, and createLeague reaches the same state by a second path, so the clear belongs in resetToBlank."
    }
   ]
  }
 },
 {
  "id": "IDX12-03",
  "title": "Clearing City or Home course silently does nothing while the sheet reports \"Card saved ✓\"",
  "file": "index.html",
  "line": "14342-14343",
  "severity": "P2",
  "kind": "bug",
  "category": "silent-failure",
  "sessionCode": false,
  "quote": "        p_city: $('#phCity').value.trim() || null,\n        p_home: $('#phHome').value.trim() || null,",
  "failureScenario": "set_profile upserts with `city = coalesce(excluded.city, profiles.city)` — null means KEEP, not clear. A golfer who moves, empties the City field and taps Save gets 'Card saved ✓' while the row is untouched; reopening restores the old value, and no other path in the app can clear those two fields. The same handler gets it right for GHIN five lines down, with a comment saying so — the semantics were understood and applied to one field of three.",
  "fix": "Give city/home the same nullif shape as ghin_number in a new migration and stop sending `|| null`; until then, stop reporting success.",
  "confidence": 9,
  "notes": "",
  "slice": "index.html 13433–15400",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "IDX12-04",
  "title": "applyBylaws stashes CS.settings, so \"run it back\" carries the previous league's payment terms into the new league",
  "file": "index.html",
  "line": "14854",
  "severity": "P2",
  "kind": "bug",
  "category": "correctness",
  "sessionCode": true,
  "quote": "  CS.settings = b;",
  "failureScenario": "D129 made CS.settings the sole reader of the Pro's payment note. Two paths poison it: runItBack stashes the OLD league's whole league_settings row and the create hook re-applies it, so the brand-new league's Pot tab shows last season's 'Venmo @casey · by Fri Mar 6' as configured terms while the DB row is empty and every member correctly sees 'The Pro hasn't posted how to pay yet'; and enterLeague swallows a loadBylaws failure, leaving the previous league's note and league_id in CS.settings for the new room.",
  "fix": "Split prefill from load so runItBack sets only the dials, and null CS.settings before the loadBylaws attempt.",
  "confidence": 8,
  "notes": "`CS.settings = b` is this session's line (D129); runItBack and the prefill hook predate it — the new assignment is what turned an existing prefill into a payment-terms leak.",
  "slice": "index.html 13433–15400",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "IDX12-05",
  "title": "The invite message states a first-tee date the league never agreed to",
  "file": "index.html",
  "line": "14586",
  "severity": "P2",
  "kind": "bug",
  "category": "correctness",
  "sessionCode": true,
  "quote": "    if(state.seasonStart || state.startISO) lines.push(`First tee ${firstTeeText()}.`);",
  "failureScenario": "state.startISO is a wizard-only global that renderSeasonDates self-seeds (next Saturday) and nothing ever clears. So for any league with no seasons row the guard is true regardless, and firstTeeText() returns next Saturday — or the date the Pro dialled in a DIFFERENT league's wizard. The text sent to a forming league's crew reads 'First tee Sat Oct 3.' for a league whose bylaws are not locked and whose Pro set a different day. Structurally the function takes an `L` and then reads three of its five lines from current-league globals.",
  "fix": "Gate the line on real season data only, derive the buy-in and the Pro from L, and clear startISO in resetToBlank.",
  "confidence": 8,
  "notes": "",
  "slice": "index.html 13433–15400",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "IDX12-06",
  "title": "The ball-marker radiogroup is unreachable by keyboard, so a keyboard-only golfer cannot finish onboarding",
  "file": "index.html",
  "line": "13583",
  "severity": "P2",
  "kind": "bug",
  "category": "accessibility",
  "sessionCode": false,
  "quote": "    `<button class=\"mini\" data-mk=\"${k}\" role=\"radio\" aria-checked=\"${k===pfMarker}\" tabindex=\"${k===pfMarker?0:-1}\" …",
  "failureScenario": "pfMarker starts null by design (S1-01: 'the marker is chosen, never inherited'), so every marker renders tabindex=\"-1\". A roving tabindex needs exactly one 0. Tab skips the whole grid; the group element has role=radiogroup but no tabindex either, so the arrow-key handler can never fire. #pfSave refuses to save without a marker, so a keyboard-only user is hard-blocked at the card gate with no way forward.",
  "fix": "Make the first marker the group's tab stop when nothing is picked; aria-checked stays false for all, which is correct ARIA for an unselected radiogroup.",
  "confidence": 9,
  "notes": "",
  "slice": "index.html 13433–15400",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "IDX12-07",
  "title": "\"Avg vs your number · across counting rounds\" averages every round, counting or not",
  "file": "index.html",
  "line": "15243",
  "severity": "P3",
  "kind": "bug",
  "category": "money-scoring",
  "sessionCode": true,
  "quote": "          avg: rs.length ? rs.reduce((a,r)=>a+Number(r.pvi),0)/rs.length : 0,",
  "failureScenario": "`rs` is every v_rounds_ranked row for the season, including rounds above the counting cap, and the very next line computes `hist` with a `counting: r.month_rank<=capN` flag — so the distinction exists two lines away and is not applied. This session changed the tile sub-labels to 'across counting rounds', so a golfer on a cap-2 league who posted six rounds sees a figure over all six under a label promising two. #clAvg is worse: it reads the lifetime cross-league career average, which has no counting cap at all.",
  "fix": "Compute the labelled thing, or revert the two sub-labels.",
  "confidence": 8,
  "notes": "Same defect as IDX02-03 from the markup side.",
  "slice": "index.html 13433–15400",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "IDX12-08",
  "title": "A Tour Card that fails to load is narrated as \"This golfer keeps their card private\"",
  "file": "index.html",
  "line": "13967-13970",
  "severity": "P3",
  "kind": "bug",
  "category": "silent-failure",
  "sessionCode": false,
  "quote": "  }catch(_){ card=null; }\n  if(!card || !card.visible){\n    openSheet('Tour Card','PRIVATE','<p class=\"fine\">This golfer keeps their card private, or you don’t share a league yet.</p>');",
  "failureScenario": "supabase-js never throws on a database error, so the catch is dead for that case: a failing tour_card RPC returns {data:null,error}, card is null, and the sheet asserts a fact about another person's privacy settings that is false. The golfer concludes their buddy has locked them out; retrying looks identical, and the error is never logged so there is no breadcrumb.",
  "fix": "Destructure the error and branch — humanError for a real failure, PRIVATE only for a real {visible:false}.",
  "confidence": 8,
  "notes": "",
  "slice": "index.html 13433–15400",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "IDX12-09",
  "title": "The round-cache skew retry sniffs the error message, against the rule the same function documents twice",
  "file": "index.html",
  "line": "15185-15188",
  "severity": "P3",
  "kind": "bug",
  "category": "silent-failure",
  "sessionCode": false,
  "quote": "    if(rq.error && /photo_path/.test(rq.error.message||''))\n      rq = await sb.from('rounds').select('id, gross, differential, …').in('id', rids);",
  "failureScenario": "Every sibling retry in this file fires on ANY error and says why in a comment — the posts retry ten lines up, the members retry ('missing column grants on a sealed table fail without naming the column'), loadProfile ('error-message sniffing is not skew armor'). This one alone gates on the message, so any error that does not contain 'photo_path' — a 42501 from a column-grant seal, a transient 500, an RLS change — skips the retry and leaves window.roundCache empty, so every round post on the board loses its photo, points, differential and §16 receipt while the row still renders. There is no console.warn here either.",
  "fix": "Drop the regex and warn — the same shape as the two siblings around it.",
  "confidence": 7,
  "notes": "",
  "slice": "index.html 13433–15400",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "IDX12-10",
  "title": "loadProfile's second attempt drops its error — both failures return a signed-in golfer to the card gate with no breadcrumb",
  "file": "index.html",
  "line": "13507-13512",
  "severity": "P3",
  "kind": "bug",
  "category": "silent-failure",
  "sessionCode": false,
  "quote": "    q = await sb.from('profiles').select('id, display_name, …').eq('id', CS.user.id).maybeSingle();\n  }\n  const data = q.data;\n  CS.profile = data || null;",
  "failureScenario": "The first select's error is warned about; the retry's is never inspected. If the legacy shape also fails — a new profiles column added without its column grant, an RLS change, a network blip — CS.profile becomes null and boot treats a fully signed-in golfer as having no card, dropping them back on the profile gate with a pre-filled name and no error anywhere. That is precisely the 2026-07-23 photo_path symptom, and the retry that was added to prevent it has no diagnostic for its own failure.",
  "fix": "Warn and bootTrace on the retry's error so the errbar breadcrumb turns a silent gate-loop into a one-glance diagnosis.",
  "confidence": 7,
  "notes": "",
  "slice": "index.html 13433–15400",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "IDX12-11",
  "title": "Every board INSERT triggers a full eight-query room reload",
  "file": "index.html",
  "line": "15125",
  "severity": "P3",
  "kind": "opportunity",
  "category": "efficiency",
  "sessionCode": false,
  "quote": "async function loadStandingsAndFeed(){",
  "failureScenario": "The realtime posts-INSERT handler calls this for every post, and chat lives in posts. One chat message re-runs v_squad_standings, cup_final_race, week_clashes, 120 posts, standings_snapshots, rounds by id, v_rounds_ranked, v_individual_standings, plus two storage createSignedUrls batches and fetchSocial. A five-person exchange during a live round issues ~50 queries and re-signs every avatar and round photo, on phones on course Wi-Fi.",
  "fix": "Split out a refreshBoard() for chat inserts, route on the payload's kind, and debounce the way nudgeSocial already does.",
  "confidence": 8,
  "notes": "",
  "slice": "index.html 13433–15400",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "B-01",
  "title": "covenantGate never resolves when its sheet is dismissed — boot() hangs forever",
  "file": "index.html",
  "line": "16032-16043",
  "severity": "P1",
  "kind": "bug",
  "category": "correctness",
  "sessionCode": true,
  "confidence": 9,
  "quote": "return new Promise(res=>{\n    openSheet('Before you join '+(info.name||'this league'), …);\n    document.getElementById('covJoin')?.addEventListener('click',()=>res(true));\n    document.getElementById('covNo')?.addEventListener('click',()=>{ closeSheet(); res(false); });\n  });",
  "failureScenario": "A signed-in golfer with a pending cs_code for a buy-in league reloads. boot() reaches `if(!(await covenantGate(pend)))` and the covenant sheet opens ABOVE the still-visible door (z-200 vs z-50). They tap the sheet's ✕, the backdrop, or Escape — all three call closeSheet() and resolve nothing. boot() never returns: bootStep never leaves 'memberships', the 8s watchdog prints 'Boot stalled at [memberships]', boot's finally never runs, and safeBoot's finally never clears `booting`, so every later auth event (including a forced SIGNED_IN re-boot) no-ops. D116 deliberately keeps the pending code, so a reload re-offers the same trap. Only 'Not now' escapes.",
  "fix": "Resolve the promise on any sheet close — a one-shot hook invoked from closeSheet(), or a dismissable wrapper. All five call sites leak a pending promise; boot() and resumeAfterProfile() are the two that wedge.",
  "notes": "16 of 18 prod leagues carry a non-zero buy-in, so the gate fires on essentially every invite. Verifiers corrected severity to P1 (recoverable by reload) rather than P0.",
  "slice": "index.html 15400–18706 — module block second half: realtime, lock, door, joins, switcher, events, tee sheet, URL hatches, boot",
  "verification": {
   "status": "survived",
   "correctedSeverity": "P1",
   "votes": [
    {
     "lens": "control-flow",
     "verdict": "CONFIRMED",
     "refuted": false,
     "correctedSeverity": "P1",
     "reasoning": "No close hook, no MutationObserver, no per-sheet modal flag exists anywhere; closeSheet is pure DOM plus scroll-lock. .sheet is z-200 over .onboard's z-50, so the ✕ is genuinely tappable at boot. Worse than stated: `booting` never clears, so even a SIGNED_IN event with force=true is refused. resumeAfterProfile has the same shape and leaves the invited first-timer on an EMPTY overlay."
    },
    {
     "lens": "intent-and-precedent",
     "verdict": "CONFIRMED",
     "refuted": false,
     "correctedSeverity": "P1",
     "reasoning": "D116 rules on what 'Not now' must do and gives no cover for an unresolved promise; git shows the boot() call site added today, so there is no 'knowingly accepted' history. covenantGate is the only promise-wrapped sheet in the codebase, so it deviates from no idiom and none exists to save it. 16 of 18 prod leagues carry a buy-in. Two corrections: the watchdog DOES paint a visible red error, and a reload plus a button press escapes."
    }
   ]
  }
 },
 {
  "id": "B-02",
  "title": "Stored XSS: the league switcher renders an event name unescaped",
  "file": "index.html",
  "line": "16388",
  "severity": "P1",
  "kind": "bug",
  "category": "security",
  "sessionCode": false,
  "confidence": 9,
  "quote": "const events = (window.myEvents||[]).map(e => row('__event:'+e.id, e.kind==='major'?'🏆':'⚔️', e.name, …)).join('');",
  "failureScenario": "`row` inserts its title argument raw (`<b>${title}</b>`), while every sibling in the same function escapes. create_event stores p_name verbatim. A league member creates a Ryder named with an `<img src=x onerror=…>` payload; loadMyEvents pulls attached events for every league you belong to, so every other member who taps the header league name to open the switcher executes it in the app origin. The CSP is Report-Only and the Supabase session lives in localStorage on that origin — session exfiltration, not defacement.",
  "fix": "`esc(e.name)`, or escape inside `row` itself (its only other callers pass literals).",
  "notes": "Precedent: SEC-C6 was a launch blocker and F-002 filed three residual unescaped sinks as P1 on exactly this reasoning. Requires league membership, which is a join code away.",
  "slice": "index.html 15400–18706",
  "verification": {
   "status": "survived",
   "correctedSeverity": "P1",
   "votes": [
    {
     "lens": "control-flow",
     "verdict": "CONFIRMED",
     "refuted": false,
     "correctedSeverity": "P1",
     "reasoning": "Every link holds: create_event stores p_name verbatim, RLS deliberately fans attached events to all league members, loadMyEvents is awaited by the switcher on every open, and row() is the one helper in that function that does not escape while its two siblings ten lines away do. The CSP is Report-Only by comment, so it is instrumentation not a control. Narrowing: the attacker must be a league member — which costs one join code — and the payload lands in the origin holding the Supabase refresh token."
    },
    {
     "lens": "intent-and-precedent",
     "verdict": "CONFIRMED",
     "refuted": false,
     "correctedSeverity": "P1",
     "reasoning": "Precedent is explicit: SEC-C6 was a launch blocker and F-002 filed three residual sinks as P1 on exactly this reasoning ('sibling renders DO esc() the same values — proves oversight'). git log -S shows the line raw since introduction, never escaped and reverted. The other consumer of the same data (groupChip at 10219) wraps it in esc(), which proves the data is known-unescaped and this is the outlier."
    }
   ]
  }
 },
 {
  "id": "B-03",
  "title": "The growth funnel's link_opened / claim_started nodes can never fire — state.demo is true at module-eval and for every signed-out session",
  "file": "index.html",
  "line": "18326, 18497, 18523, 18640",
  "severity": "P2",
  "kind": "bug",
  "category": "silent-failure",
  "sessionCode": false,
  "confidence": 9,
  "quote": "if(shareParam){ window._csShareView = true; window.growthEvent?.('link_opened', 'share', shareParam.trim()); … }",
  "failureScenario": "growthEvent opens `if(state.demo || !window.sb) return;`. state.demo is initialised true and flipped only by resetToBlank/showWelcome/enterGuestLive. The ?share=/?join=/?claim= blocks all run at module EVALUATION, before any boot, so all three link_opened calls are no-ops for every visitor. A signed-out visitor never flips the flag at all, so claim_started and profile_created are dropped too. Someone opens a shared link, taps through, claims a round and creates a profile — growth_events records zero rows, and the migration that grants log_growth_event to anon precisely for this is unreachable.",
  "fix": "Drop state.demo from growthEvent's guard — log_growth_event is already fail-closed, anon-safe and rate-limited server-side.",
  "notes": "The funnel table will read zeros and be read as 'nobody opens links'.",
  "slice": "index.html 15400–18706",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "B-04",
  "title": "Accepting an invite joins a paid league with no covenant and no disclosure — the sixth join path Q-14 missed",
  "file": "index.html",
  "line": "17249-17259",
  "severity": "P2",
  "kind": "bug",
  "category": "correctness",
  "sessionCode": false,
  "confidence": 8,
  "quote": "window.respondInvite = async (id, accept)=>{\n    const { error } = await sb.rpc('respond_invite', { p_id:id, p_accept:accept });\n    …\n    toast(accept ? 'Joined ✓' : 'Declined');",
  "failureScenario": "respond_invite inserts a real league_members row. The Pro invites Sandy to a $100 league; Sandy taps Accept on the notification row or in the switcher. No covenant sheet runs, and none of the three call sites names a stake — the row reads 'League invite · PIGL', the details sheet says 'A season-long league.', and the success path is a bare toast with no enterLeague, so not even openLeagueWelcome's buy-in line fires. Sandy is on the pot sheet for $100 having never been shown a number. It also walks around D112, which join_league enforces and respond_invite does not.",
  "fix": "Route accept through covenantGate before respond_invite, and call openLeagueWelcome after a successful league accept.",
  "notes": "Q-14/D116 ruled consent belongs on EVERY join path and fixed five of them; this one was not counted.",
  "slice": "index.html 15400–18706",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "B-05",
  "title": "The sign-in door dead-ends: 'I have a league code' hides the email door and #obBack does not exist yet",
  "file": "index.html",
  "line": "16002-16013",
  "severity": "P2",
  "kind": "bug",
  "category": "correctness",
  "sessionCode": true,
  "confidence": 9,
  "quote": "$('#obJoin').addEventListener('click', () => {\n  const em = $('#obEmail'); if(em) em.style.display = 'none';\n  const bk = $('#obBack'); if(bk) bk.style.display = '';\n  …\n});",
  "failureScenario": "#obBack is not in the markup; it is created only inside openEmailBox. On a cold load a first-time visitor taps 'I have a league code': #obEmail is hidden, `bk` is null so nothing is shown in its place, and the door is left showing only the LEAGUE CODE input. backToDoor() only un-hides #onboard and never resets the inline display, and the Back button is minted only by openEmailBox — which on this branch runs only if they type a valid code and submit. A visitor with no code cannot sign in without reloading. Q-18's own comment claims 'the same Back returns, so neither branch is a one-way street'.",
  "fix": "Put #obBack in the static markup (hidden) so both branches reveal the same node, or extract an ensureAuthBack() both call.",
  "notes": "Secondary: openEmailBox re-anchors #emailbox on every call but inserts #obBack only once, so after a re-anchor the Back button is stranded.",
  "slice": "index.html 15400–18706",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "B-09",
  "title": "Every board post — chat included — triggers a full standings + Home reload, uncoalesced",
  "file": "index.html",
  "line": "15455-15462",
  "severity": "P2",
  "kind": "bug",
  "category": "efficiency",
  "sessionCode": false,
  "confidence": 8,
  "quote": "      { event:'INSERT', schema:'public', table:'posts', filter:'league_id=eq.' + CS.league.id },\n      () => { loadStandingsAndFeed(); if(window.loadHome) window.loadHome(); … }",
  "failureScenario": "The filter is league_id only, and chat lives in posts. Twenty messages during a live round produce twenty INSERTs; each one, on every open client, runs loadStandingsAndFeed AND loadHome — roughly 20 × 12 × N-clients round trips for a conversation, on phones. The two lines directly below already solve this for reactions and comments with a 250ms coalescer; the expensive handler is the one that never got it.",
  "fix": "Wrap the posts handler in the same coalescer and skip the standings refetch for kind='chat'.",
  "notes": "",
  "slice": "index.html 15400–18706",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "B-10",
  "title": "liveSync.flush() silently discards queued hole scores on any error message containing 'function'",
  "file": "index.html",
  "line": "15558-15568",
  "severity": "P2",
  "kind": "bug",
  "category": "silent-failure",
  "sessionCode": false,
  "confidence": 6,
  "quote": "            if(/not live|final|No such|not in this|function|schema cache/i.test(r.error.message||'')){\n              q.shift(); this.saveQ(q); continue;\n            }",
  "failureScenario": "The bare token `function` also matches PostgREST's 42501 body, 'permission denied for function live_set_score' — precisely the D37 landmine. If a future migration re-creates live_set_score without re-granting EXECUTE, the group scores 18 holes: the broadcast leg still paints every stroke on every phone so the round looks perfect, while every durable write is 42501'd and shifted off the queue. They tap Finish and the database has no scores.",
  "fix": "Match only the skew shapes and the RPC's explicit refusals; never drop on a bare 'function' or on error.code 42501 — keep the entry and surface a loud breadcrumb.",
  "notes": "",
  "slice": "index.html 15400–18706",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "B-06",
  "title": "A stale cs_code_name names the wrong league on the sign-in door",
  "file": "index.html",
  "line": "18490-18513",
  "severity": "P3",
  "kind": "bug",
  "category": "correctness",
  "sessionCode": true,
  "confidence": 7,
  "quote": "  try { localStorage.setItem('cs_code', jc); } catch(_){}\n  …\n  sb.rpc('league_by_code', { p_code: jc }).then(({ data }) => { … localStorage.setItem('cs_code_name', data); …",
  "failureScenario": "cs_code is written synchronously; cs_code_name only inside the async .then(). A golfer opens an invite for Alpha, declines (D116 now keeps both keys), then opens an invite for Bravo: cs_code becomes BRAVO immediately but cs_code_name is still 'Alpha' when safeBoot reads it, so the door reads \"You're invited to Alpha.\" When the RPC resolves, the warm-up guard matches only the GENERIC line, so the named-but-wrong line is never corrected. The golfer signs in and is seated in Bravo.",
  "fix": "Remove cs_code_name in the same statement that sets cs_code.",
  "notes": "",
  "slice": "index.html 15400–18706",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "B-07",
  "title": "'Resend code' survives the Back button and stays live on the door",
  "file": "index.html",
  "line": "15839-15847",
  "severity": "P3",
  "kind": "bug",
  "category": "correctness",
  "sessionCode": true,
  "confidence": 8,
  "quote": "    back.addEventListener('click', () => {\n      $('#emailbox')?.classList.remove('open'); … back.style.display = 'none'; authStatus('');\n    });",
  "failureScenario": "#obResend ships hidden and startResendCooldown() sets it visible once a code has been sent. The Back handler closes the three boxes and restores both doors but never re-hides it, so the door shows 'Continue with email', 'I have a league code' AND a floating 'Resend code' that still fires a real OTP send to CS.pendingEmail from a screen with no email box open. Supabase's per-hour mailer limit is what that burns. The #obJoin handler one screen over remembers to hide it.",
  "fix": "Hide #obResend inside the Back handler.",
  "notes": "",
  "slice": "index.html 15400–18706",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "B-08",
  "title": "The only escape from an opened door branch is a sub-44px text link",
  "file": "index.html",
  "line": "15837",
  "severity": "P3",
  "kind": "bug",
  "category": "accessibility",
  "sessionCode": true,
  "confidence": 8,
  "quote": "back.id = 'obBack'; back.type = 'button'; back.className = 'cs-tskip';\n    back.textContent = '← Back';",
  "failureScenario": "`.cs-tskip` has no padding, no min-height, 12.5px text in `--dim` — a hit box of roughly 45×16 CSS px, about a fifth of the required area, in the dimmest ink on the palette. On a phone it is the sole control that returns a golfer from the code box or join box to the door (and per B-05 the only one that exists at all once minted).",
  "fix": "Give #obBack real padding and a 44px min-height, or add it to .cs-tskip — its other four uses have the same problem.",
  "notes": "",
  "slice": "index.html 15400–18706",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "B-11",
  "title": "The public share card's browser tab reads 'at null' when the round has no course",
  "file": "index.html",
  "line": "18380, 18424",
  "severity": "P3",
  "kind": "bug",
  "category": "correctness",
  "sessionCode": false,
  "confidence": 7,
  "quote": "    document.title = `${info.name} — ${info.gross} at ${info.course} · Cup Season`;",
  "failureScenario": "info.course comes from a nullable course_label, and the same function guards it twice elsewhere. Post a round with the course blank, share the card, and every recipient's browser tab, iOS share-sheet preview and bookmark title reads 'Casey — 84 at null · Cup Season'. This is the one page people who have never heard of Cup Season see, and 'null' is the most legible tell that a page is broken.",
  "fix": "Reuse the same fallbacks the card bodies already use.",
  "notes": "",
  "slice": "index.html 15400–18706",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "B-12",
  "title": "CS.reviewerMode is sticky — once set, the door never returns to OTP",
  "file": "index.html",
  "line": "15960-15968",
  "severity": "P3",
  "kind": "bug",
  "category": "correctness",
  "sessionCode": false,
  "confidence": 8,
  "quote": "  if(email.toLowerCase() === 'reviewer@cupseason.app'){\n    CS.reviewerMode = true; CS.pendingEmail = email;\n    ci.type = 'password'; ci.removeAttribute('inputmode'); …",
  "failureScenario": "Nothing ever resets CS.reviewerMode or restores the input. An App Review tester types the reviewer address, then tests the normal OTP flow with their own: a real code is sent, but the code box is still a password field labelled 'REVIEW PASSWORD', the 8-digit auto-submit is dead, and #obCodeGo takes the reviewer branch and calls signInWithPassword with the OTP digits — always failing. The door is unusable until reload, on the exact path Apple exercises.",
  "fix": "Reset the flag and the input in the non-reviewer branch and in the Back handler.",
  "notes": "",
  "slice": "index.html 15400–18706",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "B-13",
  "title": "backToDoor() on SIGNED_OUT can reveal an empty onboarding overlay",
  "file": "index.html",
  "line": "18697",
  "severity": "P3",
  "kind": "bug",
  "category": "correctness",
  "sessionCode": false,
  "confidence": 5,
  "quote": "else if(event === 'SIGNED_OUT'){ bootResolved=false; CS.user=null; CS.league=null; … backToDoor(); }",
  "failureScenario": "backToDoor() only un-hides #onboard. showProfileGate() had set #obDoor, #obCaption and .ob-hero to display:none, and the card-save path hides #obProfile without restoring any of them. In a tab that ran the card gate and then entered the app without a reload, a SIGNED_OUT that does not come with its own reload — a cross-tab sign-out, or a refresh-token failure — un-hides an overlay with every child hidden: an opaque screen with only the wordmark and no control.",
  "fix": "Have backToDoor() restore the door explicitly.",
  "notes": "Confidence 5 — could not confirm how often supabase-js emits SIGNED_OUT without an accompanying reload. #wOut and ?exit both reload, so the common paths mask it.",
  "slice": "index.html 15400–18706",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "B-14",
  "title": "Unguarded localStorage.setItem on the signed-out join door",
  "file": "index.html",
  "line": "16057-16058",
  "severity": "P3",
  "kind": "bug",
  "category": "silent-failure",
  "sessionCode": false,
  "confidence": 6,
  "quote": "    localStorage.setItem('cs_code', code);\n    if(name) localStorage.setItem('cs_code_name', name); else localStorage.removeItem('cs_code_name');",
  "failureScenario": "Every other localStorage access in this module is wrapped — the file's own convention says the author knows it throws. These two are bare, inside an async click handler with no surrounding try. In a webview or a browser blocking site data the SecurityError escapes as an unhandled rejection: openEmailBox never runs, no status line appears, and tapping Join on a valid league code does visibly nothing.",
  "fix": "Wrap both in the same try/catch the rest of the module uses and let openEmailBox open regardless.",
  "notes": "safeBoot's getItem has the same gap, though it fires after boot and costs only the invite-door line.",
  "slice": "index.html 15400–18706",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "O-01",
  "title": "The covenant ignores the payment terms this session's own migration added for it",
  "file": "index.html",
  "line": "16033-16040",
  "severity": "P3",
  "kind": "opportunity",
  "category": "incomplete-feature",
  "sessionCode": true,
  "confidence": 9,
  "quote": "      <div class=\"byrow\"><span>BUY-IN</span><b>$${usd} / player · on the pot sheet</b></div>",
  "failureScenario": "20260830040000_buy_in_terms.sql extends join_covenant_info with has_pay_note and buy_in_due_on, and its header says the covenant should learn THAT the Pro has posted terms. `grep -n 'has_pay_note' index.html` returns nothing: covenantGate renders neither field. The server pays for the columns, the round trip and the fail-closed design, and no golfer sees the result.",
  "fix": "Add a DUE row when buy_in_due_on is set (with the manual y,m-1,d parse) and a one-line 'The Pro has posted how to pay' when has_pay_note is true.",
  "notes": "",
  "slice": "index.html 15400–18706",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "O-02",
  "title": "~50 lines of unreachable invite-email machinery in lockBylaws",
  "file": "index.html",
  "line": "15696, 15735-15744",
  "severity": "P3",
  "kind": "opportunity",
  "category": "simplification",
  "sessionCode": false,
  "confidence": 8,
  "quote": "  let emails = state.emails.filter(e => e.includes('@'));\n…\n  if(emails.length){ const { error: eInv } = await sb.from('invites').insert(…); }",
  "failureScenario": "state.emails is written only by renderEmails, which early-returns because the wizard's email slots were deleted. So the filter is permanently [], the invites insert can never run, and the `emails` half of lockBylaws' return value — which openLockShare renders as '(N already invited.)' — is dead. Same family as the D97 residue that produced the 25-day lock_fail incident.",
  "fix": "Delete the local, the insert block and the return key, and drop the clause in openLockShare. That also removes this file's last direct insert into invites.",
  "notes": "",
  "slice": "index.html 15400–18706",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "O-03",
  "title": "hideWelcome() is not the inverse of showWelcome() and mutates an element that is never shown",
  "file": "index.html",
  "line": "18027-18033",
  "severity": "P3",
  "kind": "opportunity",
  "category": "dead-code",
  "sessionCode": false,
  "confidence": 8,
  "quote": "function hideWelcome(){\n  $('#obWelcome').style.display = 'none';\n  $('#obDoor').style.display = 'block'; …",
  "failureScenario": "showWelcome() does something entirely different and never touches #obWelcome, and #obWelcome is never set visible anywhere in the file. So hideWelcome's two call sites restore the display of an element inside an overlay enterApp() has already hidden, for a screen no code path can reach. Two paired names doing unrelated jobs is exactly the shape that makes the next reader assume a relationship that is not there.",
  "fix": "Delete #obWelcome and hideWelcome, or rename it restoreDoor() and call it from backToDoor, where it would fix B-13.",
  "notes": "",
  "slice": "index.html 15400–18706",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "O-04",
  "title": "?join= consumes the query string before ?claim= reads it",
  "file": "index.html",
  "line": "18496, 18519-18523",
  "severity": "P3",
  "kind": "opportunity",
  "category": "simplification",
  "sessionCode": false,
  "confidence": 7,
  "quote": "  history.replaceState(null, '', window.location.pathname);\n…\nconst claimParam = new URLSearchParams(window.location.search).get('claim');",
  "failureScenario": "The join block replaceStates to a bare pathname; the claim block then constructs a fresh URLSearchParams from an already-empty search. Any URL carrying both silently loses the claim, and the ?exit check downstream re-reads location.search after two replaceStates. The coupling is invisible — neither block mentions the other.",
  "fix": "Read all four params once at the top, act, then do a single replaceState.",
  "notes": "",
  "slice": "index.html 15400–18706",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "O-05",
  "title": "openLeagueWelcome's buy-in line can land in a different sheet",
  "file": "index.html",
  "line": "17966-17977",
  "severity": "P3",
  "kind": "opportunity",
  "category": "correctness",
  "sessionCode": false,
  "confidence": 6,
  "quote": "    loadBylaws(CS.league.id).then(b=>{ … document.getElementById('shBody')?.insertAdjacentHTML('afterbegin', `…You're on the pot sheet: $${usd} buy-in.…`); }).catch(()=>{});",
  "failureScenario": "The insert targets whatever #shBody holds when the round trip returns, with no check that the welcome sheet is still open. A golfer who dismisses the welcome and immediately opens another sheet gets a money claim prepended to an unrelated screen.",
  "fix": "Await loadBylaws before openSheet, or stamp the sheet with an owner token and compare it.",
  "notes": "",
  "slice": "index.html 15400–18706",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "O-06",
  "title": "openMembersSheet prints an invite email unescaped",
  "file": "index.html",
  "line": "17832",
  "severity": "P3",
  "kind": "opportunity",
  "category": "security",
  "sessionCode": false,
  "confidence": 7,
  "quote": "        `<div class=\"fine\" style=\"padding:3px 0\">✉ ${i.email} · WAITING</div>`",
  "failureScenario": "i.email is interpolated raw while every other field in the same sheet is escaped. Latent today — only the Pro can read invites for their own league, and per O-02 the file's only writer is unreachable, so the table is effectively frozen empty. It becomes a live sink the moment the invites path is revived.",
  "fix": "`esc(i.email)`.",
  "notes": "",
  "slice": "index.html 15400–18706",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "DB-01",
  "title": "season_email_payload is granted to `authenticated` — every league member's email and unsubscribe token, no caller check",
  "file": "supabase/migrations/20260727240000_name_resolution.sql",
  "line": "123-124",
  "severity": "P0",
  "kind": "bug",
  "category": "security-pii",
  "sessionCode": false,
  "confidence": 9,
  "quote": "revoke all on function public.season_email_payload(uuid) from public, anon;\ngrant execute on function public.season_email_payload(uuid) to authenticated;",
  "failureScenario": "season_email_payload is SECURITY DEFINER with NO caller identity check — its only gate is `status = 'complete'`. Its `recipients` key is a jsonb_agg of email, display_name, email_prefs token and payout cents over every league_members row. Two earlier migrations explicitly revoked it from authenticated; this file re-granted it, apparently by pasting the D37 boilerplate. Verified LIVE: proacl includes authenticated=X and prosecdef is true, and executing it as role `authenticated` in a rolled-back transaction returned a real email address and a non-null token. Any member reads their own season id (RLS allows it) and receives every league-mate's address, payout and unsubscribe token — and email_unsubscribe(uuid) is anon-callable, so harvested tokens unsubscribe league-mates. It walks straight past the profiles.email column seal.",
  "fix": "New migration revoking execute from public, anon, authenticated and re-asserting service_role, plus the self-enforcing DO block pattern of 20260827130000_close_month_revoke.sql, plus a db-checks assertion. Belt and braces: gate the body on is_league_member, or drop the emails from the payload and let the Edge Function join them under service_role.",
  "notes": "Verified against the live database, not inferred. Verifiers corrected severity to P1: anon is correctly revoked, so it needs a live session plus a member-visible season id, and exactly one season is 'complete' in prod today. email_prefs otherwise carries zero grants to authenticated, so this is a genuine escalation.",
  "slice": "supabase/migrations/ (138 files)",
  "verification": {
   "status": "survived",
   "correctedSeverity": "P1",
   "votes": [
    {
     "lens": "control-flow",
     "verdict": "CONFIRMED",
     "refuted": false,
     "correctedSeverity": "P1",
     "reasoning": "Reproduced against prod: executed as role authenticated inside a rolled-back transaction and got back a recipients array with a real email and a non-null token. No caller check exists and no wrapper intervenes. email_unsubscribe is anon-granted and matches on token with no ownership check, so harvested tokens are directly actionable. The precedent revoke migration (20260827130000) covers three other functions and missed this one. Not an unauthenticated leak — anon is correctly revoked — so P1."
    },
    {
     "lens": "intent-and-precedent",
     "verdict": "CONFIRMED",
     "refuted": false,
     "correctedSeverity": "P1",
     "reasoning": "The migration's own header admits the grant line is blanket boilerplate applied to a script-patched pg_dump body, and it reversed two explicit reasoned revokes. No decision, spec or comment endorses client callability; the sole caller is the Edge Function under service_role. email_prefs carries ZERO grants to authenticated and profiles.email is column-sealed, so both the addresses and the tokens are otherwise unreachable — this is a genuine escalation, not redundancy."
    }
   ]
  }
 },
 {
  "id": "DB-02",
  "title": "declare_round accepts any profile ids as tags with no relationship check and no cap — retag_round enforces both",
  "file": "supabase/migrations/20260827210000_push_wave7.sql",
  "line": "131-163",
  "severity": "P1",
  "kind": "bug",
  "category": "security-abuse",
  "sessionCode": false,
  "confidence": 8,
  "quote": "  select array_agg(distinct t.pid) into v_tags from unnest(coalesce(p_tagged,'{}')) t(pid) where t.pid <> auth.uid();\n  v_tags := coalesce(v_tags, '{}');",
  "failureScenario": "retag_round caps the array at seven AND rejects any pid that is not an accepted friend or a league mate. declare_round, which CREATES the same tagged array and fans the SAME push_nudges rows, has neither check, and caps p_note but leaves p_course uncapped with no length CHECK on scheduled_rounds.course_label or push_nudges.body. An authenticated user calls declare_round with a long course string and a large tag array harvested from search_golfers (which returns any discoverable='everyone' profile), and one push_nudges row per tag is inserted whose body carries the attacker's text; the push webhook then delivers them. Repeatable with no rate limit. Tagging also makes can_see_round true, so the attacker-authored round lands in each stranger's schedule.",
  "fix": "Lift retag_round's two guards into declare_round verbatim and cap p_course, or extract one _assert_taggable(uuid[]) helper both call.",
  "notes": "Verifiers found TWO live overloads: the 5-arg (20260715230000) HAS both guards; the 6-arg — the one both clients call, because they send p_course_id — has neither. 20260718192400's header claims 'Same body as 20260715230000 otherwise', which is false. Push bodies are clamped to 140 chars at delivery, and search_golfers is limit 10 per call.",
  "slice": "supabase/migrations/ (138 files)",
  "verification": {
   "status": "survived",
   "correctedSeverity": "P1",
   "votes": [
    {
     "lens": "control-flow",
     "verdict": "CONFIRMED",
     "refuted": false,
     "correctedSeverity": "P1",
     "reasoning": "Prod has TWO live overloads: the 5-arg keeps both guards, the 6-arg — the one both clients call, because they send p_course_id — has neither, and overload resolution is caller-controlled. SECURITY DEFINER so RLS is no backstop; push delivers each nudge and the only mute check requires the recipient to have already muted a stranger. Corrections: the lock-screen text IS clamped to 140 chars at delivery, and id harvesting is 10 per search."
    },
    {
     "lens": "intent-and-precedent",
     "verdict": "CONFIRMED",
     "refuted": false,
     "correctedSeverity": "P1",
     "reasoning": "The rule is stated in the repo's own migration header — 'You can tag accepted buddies and league mates (validated in declare_round, not trusted from the client)' — and was implemented for two consecutive migrations before 20260718192400 rewrote the function, dropped both guards and the board post, and described the rewrite as otherwise identical. No commit removes the guard text, because the regression arrived as a new file. D104 says nothing about tagging validation."
    }
   ]
  }
 },
 {
  "id": "DB-03",
  "title": "A stranger can post an immutable scoring round onto your profile via the league-less live round",
  "file": "supabase/migrations/20260829090000_leagueless_live_rounds.sql",
  "line": "143-146, 325-345",
  "severity": "P1",
  "kind": "bug",
  "category": "data-integrity",
  "sessionCode": false,
  "confidence": 8,
  "quote": "      case when (v_el->>'member_id') is null\n           then nullif(v_el->>'guest_profile','')::uuid end);",
  "failureScenario": "start_live_round takes guest_profile as raw caller text cast to uuid with NO validation — not a friendship, not a shared league, only the FK to profiles. finish_live_round then inserts into rounds with profile_id = that guest_profile_id and attested = true for any complete rated non-casual card. An attacker calls search_golfers, seats any discoverable profile (who also receives a push nudge carrying the attacker's course label), enters an 18-hole card, and finishes. The victim owns a round they never played: immutable per §16, feeding round_refresh_index so it moves their index, and fanned by round_to_board to every league they belong to. D107 §4 decided the auto-post; nothing anywhere enforces its premise that seats are app golfers found by @handle or buddy.",
  "fix": "Gate guest_profile on an accepted friendship, a shared league or an exact @handle match (matching D118's ruling for the invite picker); otherwise seat name-only and degrade to the D88 claim-link path. Alternative: require a live_round_players.seen_at stamp before finish_live_round will post — presence as consent.",
  "notes": "Read D107 and D88 first so as not to report a decided mechanic. Verifiers add: the missing gate is inherited from D88 (20260728220000) — D107 made it consequential — and it is not confined to the league-less door, since the member_id league check does not extend to guest_profile. D125 (unbuilt) covers the attestation half but not who may seat whom.",
  "slice": "supabase/migrations/ (138 files)",
  "verification": {
   "status": "survived",
   "correctedSeverity": "P1",
   "votes": [
    {
     "lens": "control-flow",
     "verdict": "CONFIRMED",
     "refuted": false,
     "correctedSeverity": "P1",
     "reasoning": "start_live_round's only unconditional guard is auth.uid() not null; every league/membership check sits inside `if p_league is not null`, and the one league-less rejection fires on member_id, not guest_profile. finish's starter check passes for the attacker and its post gate tests only attacker-controlled fields. No trigger on rounds validates ownership, gross 18..200 admits the payload, and SECURITY DEFINER means RLS never runs. Worse than stated: there is no delete trigger, so delete_round leaves index_current contaminated."
    },
    {
     "lens": "intent-and-precedent",
     "verdict": "CONFIRMED",
     "refuted": false,
     "correctedSeverity": "P1",
     "reasoning": "D107 §4 decides the auto-post (correctly excluded from the claim) but nothing authorises seating an arbitrary profile — D118's ruling on the invite picker establishes the opposite posture for the same class. The unvalidated passthrough is inherited from D88; D107 escalated the consequence. M-093/D125 already rate the harm P1 and cite these exact lines, but D125's remedy is attestation, is unbuilt, and would still leave a stranger able to push an unattested scoring round onto anyone in the directory."
    }
   ]
  }
 },
 {
  "id": "DB-04",
  "title": "league_pulse buckets the floor by UTC month — the floor surface flips a month 7 hours early in Phoenix",
  "file": "supabase/migrations/20260716040000_league_pulse.sql",
  "line": "31",
  "severity": "P1",
  "kind": "bug",
  "category": "dates-timezones",
  "sessionCode": false,
  "confidence": 8,
  "quote": "  mo as (select date_trunc('month', current_date)::date as m),",
  "failureScenario": "Prod's TimeZone is UTC (verified, from the configuration file, with no role override), so current_date is a day ahead of Phoenix for the last seven hours of each local day. league_pulse is the floor surface — credits, floor, at_floor, partial — and its month and its round join both come from current_date. From 17:00 Phoenix on the last day of a month until midnight UTC every member reads credits 0 and at_floor false, on the exact evening a golfer checks whether they made the floor. The partial flag recomputes against the next month too, so a genuinely partial first month flips from the waiver copy to the nag copy.",
  "fix": "Derive the month from the season's own timezone — the idiom already used in settle_week_clash, open_week_clash and finish_live_round.",
  "notes": "Verifiers corrected severity to P2 and the citation: the live definition is 20260722211500_covenant_pulse_pairings.sql:60, and close_month is NOT affected (run_month_closes passes an explicit p_month and the cron fires at 00:10 Phoenix). Display-only, ~7 hours per month, and the Home stats strip shows a correct figure from a device-local month key on the same screen. The claimed mirror case (00:00–07:00 on the 1st) does not exist for a UTC-behind zone.",
  "slice": "supabase/migrations/ (138 files)",
  "verification": {
   "status": "survived",
   "correctedSeverity": "P2",
   "votes": [
    {
     "lens": "control-flow",
     "verdict": "CONFIRMED",
     "refuted": false,
     "correctedSeverity": "P2",
     "reasoning": "Verified at the server, not the session: TimeZone=UTC from the configuration file with no role override, and current_date measured a day ahead of Phoenix. The failure constructs against real seasons and the partial flag flips too, re-exposing SX-01 for seven hours. Two corrections: the cited file is superseded by 20260722211500 (a fix is a NEW migration), and the claimed mirror case does not exist for a UTC-behind zone. close_month is NOT affected — the cron passes an explicit month at 00:10 Phoenix — so this is display-only."
    },
    {
     "lens": "intent-and-precedent",
     "verdict": "CONFIRMED",
     "refuted": false,
     "correctedSeverity": "P2",
     "reasoning": "No decision, spec or comment makes it deliberate; the precedent runs the other way — 20260828150200 fixed the same class in finish_live_round two days earlier, naming 'in the wrong cap/floor month' as the harm and adopting the exact idiom proposed. git shows the CTE touched once, only to fix the inverted partial test. The Home stats strip shows a correct figure from a device-local month key on the same screen, so the app contradicts itself for ~7 hours per month with no ledger consequence."
    }
   ]
  }
 },
 {
  "id": "DB-05",
  "title": "native_home builds the phone's tee sheet from UTC current_date while the web uses the device's local date",
  "file": "supabase/migrations/20260827130400_native_home.sql",
  "line": "59, 393",
  "severity": "P2",
  "kind": "bug",
  "category": "dates-timezones",
  "sessionCode": false,
  "confidence": 8,
  "quote": "  v_today      date := current_date;\n…\n      from my_schedule(v_today, v_today + 14) s;",
  "failureScenario": "Prod TimeZone is UTC, so current_date is tomorrow from 17:00 Phoenix onward, while index.html builds the identical 14-day window from the browser's own midnight. A Phoenix golfer with a 6pm Saturday tee time opens the phone at 5:30pm: native_home passes p_from = Sunday and the round they are driving to vanishes from Home's upcoming_rounds — while the web, open beside them, still shows it.",
  "fix": "Take the date in the caller's league timezone, or at minimum widen the window to current_date − 1.",
  "notes": "Same root as DB-04 and DB-09.",
  "slice": "supabase/migrations/ (138 files)",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "DB-06",
  "title": "settle_week_clash decides the W on points but labels the card by band — a 9-hole round loses to an 18-hole round with the identical band",
  "file": "supabase/migrations/20260829091000_weekly_clash.sql",
  "line": "290-303",
  "severity": "P1",
  "kind": "bug",
  "category": "scoring-correctness",
  "sessionCode": true,
  "confidence": 8,
  "quote": "  elsif v_b_best is null\n     or (v_a_best is not null and ((v_a_best->>'points')::int > (v_b_best->>'points')::int)) then\n    v_winner := wc.a_member; v_win_b := v_a_best;",
  "failureScenario": "Claimed: the header states twice that the BAND decides the W, but points and band are not the same classification — v_rounds_ranked halves points for a nine and does not halve pvi, so an 18-hole +1.5 (band 'Beat your number', 9 pts) beats a 9-hole +1.5 (same band, 5 pts) and the board declares a winner while both stored receipt cards read the identical band.",
  "fix": "Pick one currency and make the copy match it.",
  "notes": "DROPPED — both verifiers ruled BY_DESIGN. The arithmetic is right but the diagnosis is inverted: spec §2.4 makes half-value points a first-class league bylaw (D72), and every points comparison in the engine is in that currency. The band string is display; the W is correctly decided on points. The residual real defect at these lines is the closed `>= -1` boundary, filed separately as DB-07 / DBE-02.",
  "slice": "supabase/migrations/ (138 files)",
  "verification": {
   "status": "dropped",
   "correctedSeverity": null,
   "votes": [
    {
     "lens": "control-flow",
     "verdict": "BY_DESIGN",
     "refuted": true,
     "reasoning": "Reachable and the arithmetic checks out, but the behaviour is the documented half-value law, not a currency mix-up. spec §2.4 makes 'points ÷ 2 (round up)' a league bylaw for nines, and every points comparison in the engine is in that currency. The reviewer's fix would invert it."
    },
    {
     "lens": "intent-and-precedent",
     "verdict": "BY_DESIGN",
     "refuted": true,
     "reasoning": "Same. D72 made the 9-hole round a first-class post under §2.4's half-value rule; the band string is display and the W is correctly decided on points. The residual real defect at these lines is the closed >= -1 boundary, which is DB-07/DBE-02."
    }
   ]
  }
 },
 {
  "id": "DB-07",
  "title": "The clash's band ladder uses pvi >= -1 where cup_points uses pvi > -1 — the boundary the client fixed this same session",
  "file": "supabase/migrations/20260829091000_weekly_clash.sql",
  "line": "263-268, 279-284",
  "severity": "P2",
  "kind": "bug",
  "category": "scoring-correctness",
  "sessionCode": true,
  "confidence": 9,
  "quote": "           'band', case when rr.pvi >= 3  then 'Torched it'\n                        when rr.pvi >= 1  then 'Beat your number'\n                        when rr.pvi >= -1 then 'Played to it'",
  "failureScenario": "cup_points' third rung is strictly greater than −1. At pvi exactly −1.0 the engine scores 6 and the round card says 'A little loose', while the clash receipt says 'Played to it'. index.html's bandName carries the corrected half-open form with a `/* Q-20: half-open, matching cup_points */` comment, made in THIS session's commit 4be994a; the clash migration, written a day later in the same session, reintroduced the bug in SQL.",
  "fix": "Change both copies (and the v_phrase at :322) to `> -1` in a NEW migration; then extract a `band_name(numeric)` SQL function beside cup_points so the ladder has one server-side producer.",
  "notes": "A direct regression against another change in the same session.",
  "slice": "supabase/migrations/ (138 files)",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "DB-08",
  "title": "finish_live_round lets a caller author unbounded kind='system' board text; the wolf/skins branch beside it caps at 200",
  "file": "supabase/migrations/20260829090000_leagueless_live_rounds.sql",
  "line": "395-406",
  "severity": "P2",
  "kind": "bug",
  "category": "data-integrity",
  "sessionCode": true,
  "confidence": 8,
  "quote": "          v_story := 'Match play: ' || coalesce(case when (p_result->>'winner')='0' then p_result->>'side_a' else p_result->>'side_b' end, 'the winners') || ' def. ' || …",
  "failureScenario": "side_a, side_b and status are raw caller-supplied jsonb text with no length cap and no sanitisation, concatenated into posts.body with kind='system'. The wolf/skins branch eleven lines below does `left(p_result->>'story', 200)` — same function, same table, opposite discipline. A league member can mint a system-voice post attributed to the server, and kind='system' is what the push webhook curates on, so the forged line reaches every crew member's lock screen. Board bodies are escaped client-side, so this is spoofing and unbounded length, not XSS.",
  "fix": "`left(coalesce(…), 40)` on each interpolation and cap the assembled story at 200; better, build the side labels server-side from live_round_players.",
  "notes": "",
  "slice": "supabase/migrations/ (138 files)",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "DB-09",
  "title": "declare_round rejects a round for today after 17:00 Phoenix",
  "file": "supabase/migrations/20260827210000_push_wave7.sql",
  "line": "142",
  "severity": "P2",
  "kind": "bug",
  "category": "dates-timezones",
  "sessionCode": false,
  "confidence": 8,
  "quote": "  if p_play_on is null or p_play_on < current_date then raise exception 'Pick a day that has not happened yet'; end if;",
  "failureScenario": "With prod on UTC, at 17:30 Phoenix current_date is already tomorrow. A golfer opens the tee sheet, taps today, and the client sends its device-local date — which the server refuses with 'Pick a day that has not happened yet', for today. retag_round has the same expression, so a round already on the books cannot be retagged after 17:00 either.",
  "fix": "`p_play_on < current_date - 1`, the slack the ugc_safety trigger already uses with a comment explaining exactly this.",
  "notes": "Third instance of the DB-04/DB-05 root cause; filed separately because this one is a hard user-facing rejection.",
  "slice": "supabase/migrations/ (138 files)",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "DB-10",
  "title": "transfer_pro records the recipient as the actor in commissioner_log",
  "file": "supabase/migrations/20260828150200_transfer_pro_and_live_played_on.sql",
  "line": "56-57",
  "severity": "P3",
  "kind": "bug",
  "category": "audit-integrity",
  "sessionCode": false,
  "confidence": 9,
  "quote": "  insert into commissioner_log (league_id, actor_id, action, detail)\n  values (v_league, p_member, 'transfer_pro', jsonb_build_object('from', v_me, 'to', p_member));",
  "failureScenario": "actor_id is the member who RECEIVED the shop, while the caller is v_me, in scope two lines up. Every other commissioner_log insert in the repo passes the caller. Any query answering 'what did this member do as Pro' attributes the handover to the recipient, and the outgoing Pro's log shows they never transferred anything. The detail json still carries from/to, so the truth is recoverable, but the indexed column is wrong.",
  "fix": "`values (v_league, v_me, …)`, with a one-line backfill from detail->>'from' if existing rows matter.",
  "notes": "Carried verbatim from 20260712150000; this session's migration re-created the body without noticing.",
  "slice": "supabase/migrations/ (138 files)",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "DB-11",
  "title": "lock_league's coalesce makes 'no counting cap' unrepresentable through the RPC",
  "file": "supabase/migrations/20260829220000_lock_league.sql",
  "line": "82",
  "severity": "P3",
  "kind": "bug",
  "category": "scoring-correctness",
  "sessionCode": true,
  "confidence": 7,
  "quote": "    counting_cap        = coalesce(p_counting_cap, counting_cap),",
  "failureScenario": "counting_cap is the one nullable bylaw and NULL is meaningful — every scorer reads coalesce(counting_cap, 999) so NULL means 'no cap'. The client encodes that as CAPVALS[3] = null, which PostgREST delivers as SQL NULL, and coalesce then keeps whatever is stored instead of writing NULL. The direct-write fallback eleven lines further down in the client writes NULL correctly, so the RPC and its own fallback disagree about what 'no cap' means. It does not bite today only because create_league leaves the column NULL.",
  "fix": "Drop the coalesce for this field, or use an explicit sentinel the client sends for 'no cap'.",
  "notes": "Latent — verified every writer and confirmed the initial value is NULL.",
  "slice": "supabase/migrations/ (138 files)",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "DB-12",
  "title": "lock_league's settings UPDATE never checks FOUND — it would report success over a half-lock",
  "file": "supabase/migrations/20260829220000_lock_league.sql",
  "line": "78-96",
  "severity": "P3",
  "kind": "opportunity",
  "category": "silent-failure",
  "sessionCode": true,
  "confidence": 7,
  "quote": "  update league_settings set … locked_at = now() where league_id = p_league returning * into v_settings;",
  "failureScenario": "With no league_settings row the UPDATE matches zero rows, v_settings is all-NULL and nothing raises: locked_at is never written so the idempotency guard cannot catch a retry, `v_settings.structure <> 'solo'` evaluates NULL so form_squads is skipped, the ELSE sets phase='draft', and the function returns already_locked:false with phase 'draft' — announcing a successful lock over a league with no bylaws, no squads and no locked_at. The exact failure class this migration exists to eliminate, reproduced inside the fix.",
  "fix": "`if not found then raise exception 'That league has no bylaws row.'; end if;`",
  "notes": "Not reachable today — all 18 prod leagues have a league_settings row and create_league inserts one.",
  "slice": "supabase/migrations/ (138 files)",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "DB-13",
  "title": "lock_league reads locked_at without FOR UPDATE — a concurrent second call rewrites the bylaws of a locked league",
  "file": "supabase/migrations/20260829220000_lock_league.sql",
  "line": "69-76",
  "severity": "P3",
  "kind": "opportunity",
  "category": "race",
  "sessionCode": true,
  "confidence": 6,
  "quote": "  select * into v_settings from league_settings where league_id = p_league;\n  if v_settings.locked_at is not null then",
  "failureScenario": "Two overlapping calls both observe NULL before either commits; the second then blocks on the row lock and, under READ COMMITTED, re-evaluates and applies its own SET list — writing a second locked_at and a full set of bylaws over a league that is already locked, past the settings_write policy's `locked_at IS NULL` rule. The rest is safe (no duplicate season, form_squads early-returns), so the only casualty is the bylaws themselves.",
  "fix": "`select … for update;` — one keyword, and the second caller serializes behind the first.",
  "notes": "Requires two locks with different payloads inside one request's flight time.",
  "slice": "supabase/migrations/ (138 files)",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "DB-14",
  "title": "native_home swallows a 42501 on profiles and re-onboards the user — the photo_path incident with the error removed",
  "file": "supabase/migrations/20260827130400_native_home.sql",
  "line": "91-109",
  "severity": "P2",
  "kind": "opportunity",
  "category": "silent-failure",
  "sessionCode": false,
  "confidence": 8,
  "quote": "    from profiles p where p.id = v;\n  exception when others then\n    v_profile := null;\n  end;",
  "failureScenario": "native_home wraps 14 sub-reads in `exception when others`. The profile one is consequential: its catch-all sets v_profile := null, and the function's own documented contract is that null means the golfer-card gate. CLAUDE.md records the live incident this reproduces — a profiles column added without its column grant fails 42501 and 'took boot down to the card gate'. native_home already selects photo_path, is_founder, index_source, handle and marker; the next column added and read here without its grant will not error, it will silently return profile: null for EVERY phone user at once.",
  "fix": "Narrow the handler to no_data_found, or re-raise on 42501. The same reasoning applies to the five membership-loop handlers, where a swallowed 42501 drops a whole league off Home.",
  "notes": "Filed as an opportunity because it needs a future migration to trigger — but it is a missing guard against an incident that has already happened once.",
  "slice": "supabase/migrations/ (138 files)",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "DB-15",
  "title": "set_buy_in_terms writes both columns unconditionally from two null-defaulted parameters",
  "file": "supabase/migrations/20260830040000_buy_in_terms.sql",
  "line": "52-56",
  "severity": "P3",
  "kind": "opportunity",
  "category": "api-design",
  "sessionCode": true,
  "confidence": 7,
  "quote": "  update league_settings\n     set buy_in_note   = nullif(btrim(coalesce(p_note, '')), ''),\n         buy_in_due_on = p_due_on",
  "failureScenario": "Both parameters default to null and both columns are written every call, so the function cannot express 'change the note, leave the date'. The only caller today sends both. The moment a second surface saves one field — a phone screen, a quick edit, a clear-the-note affordance — set_buy_in_terms(league, note) silently wipes the due date, and the Pro's stated deadline disappears from every member's Pot tab with no error and no post.",
  "fix": "`buy_in_due_on = coalesce(p_due_on, buy_in_due_on)` and give the note an explicit clearing sentinel, so the two halves agree on what null means.",
  "notes": "Latent — single caller sends both fields.",
  "slice": "supabase/migrations/ (138 files)",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "DB-16",
  "title": "The named-band ladder now has three producers that can disagree",
  "file": "supabase/migrations/20260829091000_weekly_clash.sql",
  "line": "261-268",
  "severity": "P2",
  "kind": "opportunity",
  "category": "duplication",
  "sessionCode": true,
  "confidence": 8,
  "quote": "           'band', case when rr.pvi >= 3  then 'Torched it' …",
  "failureScenario": "The mapping from pvi to a named band exists in three places with no shared source: cup_points owns the points ladder, bandName in index.html owns the label, and settle_week_clash owns a second copy of the label — twice. DB-07 is the disagreement that already shipped. This is the class that recurs: the label ladder was fixed once this session (Q-20) and reintroduced in SQL a day later, because the fix had nowhere central to land. The repo already solves this shape with build-tokens/build-markers/build-db plus preflight checks that fail on a stale artifact.",
  "fix": "Add band_name(numeric) beside cup_points, have settle_week_clash call it, and generate the client's bandName and the Swift equivalent from it — so the three copies become one producer and two artifacts.",
  "notes": "The concrete instance is DB-07/DBE-02/KIT-02/KIT-03.",
  "slice": "supabase/migrations/ (138 files)",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "DB-17",
  "title": "log_growth_event's rate limit only applies when a token is supplied",
  "file": "supabase/migrations/20260828160000_growth_events.sql",
  "line": "140-145",
  "severity": "P3",
  "kind": "opportunity",
  "category": "abuse",
  "sessionCode": false,
  "confidence": 7,
  "quote": "  if v_tok is not null and ( select count(*) from growth_events where token = v_tok and at > now() - interval '1 hour') >= 20 then\n    return;\n  end if;",
  "failureScenario": "The fence is guarded by `v_tok is not null`. Every anon branch requires a token, so anon is covered; a signed-in caller passing no token is unbounded. Rows are small and PII keys are stripped, so this is analytics pollution rather than a breach — but growth_events is the table the funnel is measured from, and a member can trivially skew v_growth_funnel's counts.",
  "fix": "Add a second fence keyed on the actor for the token-less case; the existing index supports the scan.",
  "notes": "The rate limit reads as though it covers everything and does not.",
  "slice": "supabase/migrations/ (138 files)",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "DBE-01",
  "title": "The Cup Final is not \"scored fresh\": the monthly counting cap leaks into the 4-week window",
  "file": "supabase/migrations/20260828170100_cup_final_race.sql",
  "line": "47-56",
  "severity": "P1",
  "kind": "bug",
  "category": "money-scoring-correctness",
  "sessionCode": false,
  "confidence": 8,
  "quote": "   where rr.season_id = p_season\n     and rr.month_rank <= coalesce(ls.counting_cap, 10000)\n     and rr.played_on between se.ends_on - 27 and se.ends_on",
  "failureScenario": "month_rank is ranked over the whole CALENDAR MONTH before this helper filters to the window, so rounds played before the window — worth ZERO in the Final — still consume the cap slots that decide which window rounds count. Season ends 2026-10-31, cap 4: three pre-window 12s take ranks 1–3 and only one of four in-window 9s survives. The golfer contributes 9 Final points instead of 36 while a teammate who skipped the first three days contributes 36. Both close_season (the crown) and cup_final_race (the room's scoreboard) read this helper, so the wrong number is consistent everywhere — which is why nobody would catch it.",
  "fix": "Re-rank inside the window: row_number over rows already filtered to [ends_on−27, ends_on], and cap that.",
  "notes": "Verifiers reproduced it on a prod fixture and corrected two things: it fires whenever ends_on−27 is later than the 1st (nearly every season, not only straddled ones), and when the window DOES straddle, each month gets its own cap, so up to 2N window rounds count — the proposed fix leaves that half in place. 6 of 18 prod leagues carry cap 4; two active cup_final seasons are exposed. The identical expression predates D105 (inline in close_season since 20260716170000).",
  "slice": "DB scoring & settlement engine",
  "verification": {
   "status": "survived",
   "correctedSeverity": "P1",
   "votes": [
    {
     "lens": "control-flow",
     "verdict": "CONFIRMED",
     "refuted": false,
     "correctedSeverity": "P1",
     "reasoning": "Reproduced against prod with a read-only VALUES fixture: with cap 4 and a window opening Oct 4, three pre-window 12s take ranks 1–3 and only one of four in-window 9s survives — 9 Final points instead of 36. Verified month_rank's window clause from prod's own viewdef; no date predicate exists upstream. Two corrections: it fires whenever ends_on−27 is later than the 1st (essentially every season), and when the window straddles, each month gets its own cap so up to 2N rounds count — which the proposed fix does not address. 6 of 18 leagues carry cap 4; two active cup_final seasons exposed."
    },
    {
     "lens": "intent-and-precedent",
     "verdict": "CONFIRMED",
     "refuted": false,
     "correctedSeverity": "P1",
     "reasoning": "D105's 'ONE expression' is about SHARING one expression between crown and race, not a ruling on ranking semantics; it lifts the filter verbatim and flags only the one subtlety it did consider. §14.0/§14.3 say scored fresh. Copy on the client promises 'only rounds inside the four-week window count' and the finalist receipt lists only surviving rounds, so a bumped window round appears nowhere — breaking §16 at the moment D105 was written to serve. The expression is inherited from 20260716170000, so the cited file is the site, not the origin. Also a mirror defect: enter_cup_final's ladder CTEs let window rounds bump pre-window rounds out of months_won/best_month."
    }
   ]
  }
 },
 {
  "id": "DBE-02",
  "title": "settle_week_clash bands PvI with `>= -1` — the clash card calls a 6-point round \"Played to it\"",
  "file": "supabase/migrations/20260829091000_weekly_clash.sql",
  "line": "261-265, 277-281, 320-323",
  "severity": "P1",
  "kind": "bug",
  "category": "scoring-correctness",
  "sessionCode": false,
  "confidence": 9,
  "quote": "'band', case when rr.pvi >= 3  then 'Torched it'\n                        when rr.pvi >= 1  then 'Beat your number'\n                        when rr.pvi >= -1 then 'Played to it'",
  "failureScenario": "cup_points uses `> -1` (verified in prod: cup_points(-1.0) = 6). A round at pvi exactly −1.0 is scored 6 by v_rounds_ranked but labelled 'Played to it' by settle_week_clash, and the same jsonb carries both. Line 322's v_phrase repeats it in the board post ('played to their number') for a round the standings booked as 'A little loose'. Mid-week the phone/web compute the corrected band and the label visibly flips at settle time.",
  "fix": "Change both `>= -1` (and :322's) to `> -1` in a new migration; better, call a shared band_name(). Extend db-checks 17.",
  "notes": "Built in 4b042d0 (D107/D108), one commit before the audit baseline; the session's own Q-20 fix pass should have swept it. Verifiers corrected severity to P2 — the W is decided on points, so no points, standings, ledger or record is wrong; the defect is an archived label plus a permanent board post.",
  "slice": "DB scoring & settlement engine",
  "verification": {
   "status": "survived",
   "correctedSeverity": "P2",
   "votes": [
    {
     "lens": "control-flow",
     "verdict": "CONFIRMED",
     "refuted": false,
     "correctedSeverity": "P2",
     "reasoning": "pvi is stored at one decimal so −1.0 is a real bucket; cup_points is half-open and the deployed settle_week_clash body (read from pg_proc, not the file) is closed. The function is called unconditionally by daily_season_tick under an active cron job with no flag or setting gating it, and both clients consume the stored band. But the W is decided on (points)::int, so no cup points, standings, ledger entry or record is wrong — a label and a copy line at one exact value. Adjacent uncaught: score_round(7 args) has the same closed edge in the POINTS path."
    },
    {
     "lens": "intent-and-precedent",
     "verdict": "CONFIRMED",
     "refuted": false,
     "correctedSeverity": "P2",
     "reasoning": "spec §2.2's own table puts −1.0 in the 6-point band, so cup_points is right and the clash ladder contradicts the spec. Q-20's commit message states the invariant being violated ('the band NAME always matches the band's POINTS — the class of bug, not just its one instance') and added db-checks 17, which pins cup_points and never inspects this ladder. Reproduced end to end in prod. The label is archived permanently in the week_clashes row and the board post, and visibly flips at settle time."
    }
   ]
  }
 },
 {
  "id": "DBE-03",
  "title": "The settlement post's three dollar figures sum to $1 more than the collected total",
  "file": "supabase/migrations/20260828170100_cup_final_race.sql",
  "line": "443-457",
  "severity": "P1",
  "kind": "bug",
  "category": "money-correctness",
  "sessionCode": false,
  "confidence": 9,
  "quote": "v_pot_line := v_pot_line\n      || ' — champs $'      || round((v_money->>'champ')::numeric  / 100.0)\n      || ' · runner-up $'   || round((v_money->>'runner')::numeric / 100.0)\n      || ' · points king $' || round((v_money->>'king')::numeric   / 100.0);",
  "failureScenario": "Default stake $75, six members: collected 45000, and season_payouts is exactly right (270.00 + 112.50 + 67.50). The post then rounds each to whole dollars INDEPENDENTLY — verified in prod: 270 + 113 + 68 = 451 against a stated pot of $450. mark_buy_in's late-payment post has the identical bug. This is the defect the session fixed on the client as Q-28 and did not carry to the server, at the exact moment the room is asked to trust the ledger.",
  "fix": "Apply the champion-absorbs rule the cent math already uses, or print cents so the ledger and the post are the same numbers.",
  "notes": "Arithmetic executed against prod. season_payouts rows are correct — only the narrated line is wrong. Verifiers: 18 of 65 realistic stake × roster combinations mis-sum, including the schema default; D106's own recommendation text prints cents. Severity corrected to P2 (no money moves wrongly).",
  "slice": "DB scoring & settlement engine",
  "verification": {
   "status": "survived",
   "correctedSeverity": "P2",
   "votes": [
    {
     "lens": "control-flow",
     "verdict": "CONFIRMED",
     "refuted": false,
     "correctedSeverity": "P2",
     "reasoning": "On the ordinary cron season-close path with no guard beyond a non-zero pot; the ledger it reads is populated before the read (award_season_trophies calls recompute_season_payouts first); deployed prod source matches the file. Swept the REAL prod stake/split values: mismatches at collected 5000, 15000, 25000, 35000 and 45000. mark_buy_in carries the same defect and is commissioner-callable. The money itself is correct and the client's Pot tab was already fixed, so no payout is wrong."
    },
    {
     "lens": "intent-and-precedent",
     "verdict": "CONFIRMED",
     "refuted": false,
     "correctedSeverity": "P2",
     "reasoning": "D106's own recommendation prints CENTS ($112.50, $67.50); the implementation silently substituted whole-dollar round(), so this is drift FROM the logged decision. The pattern is inherited from 20260724230000 and carried forward twice, never accepted anywhere. The identical defect was treated as a bug on the client this same session (Q-28, with a comment naming the exact failure), which is the strongest available evidence it is not by design. 18 of 65 realistic stake × roster combinations mis-sum, including the schema default."
    }
   ]
  }
 },
 {
  "id": "DBE-04",
  "title": "A league still in phase='draft' gets month-close and weekly-clash board posts, and will auto-crown a champion",
  "file": "supabase/migrations/20260829220000_lock_league.sql",
  "line": "101-104",
  "severity": "P1",
  "kind": "bug",
  "category": "correctness",
  "sessionCode": true,
  "confidence": 9,
  "quote": "    insert into seasons (league_id, number, starts_on, ends_on)\n    values (p_league, 1, coalesce(p_starts_on, current_date), coalesce(p_ends_on, current_date + 182))\n    returning * into v_season;",
  "failureScenario": "seasons.status defaults to 'active' and NOTHING ever sets it — start_season only sets leagues.phase. So a season is 'active' from the moment the bylaws lock, weeks before first tee. run_month_closes iterates status in ('active','cup_final'), so three prod leagues currently at phase='draft' with seasons starting 2026-09-05 each get 'AUGUST CLOSED — LEDGER POSTED' on a board of a league that has not started. A league that locks and never drafts stays 'active' forever, so at ends_on−27 daily_season_tick calls enter_cup_final (seeding from empty squads, all at 0) and at ends_on+grace close_season crowns and posts a pot settlement for a league that never teed off.",
  "fix": "Give seasons a 'scheduled' status set by lock_league and flipped by start_season, and gate the cron loops on it; failing that, gate on leagues.phase='season' and current_date >= starts_on.",
  "notes": "Verifiers: the month-close half has ALREADY fired in prod (a league starting 2026-08-03 carries a JULY CLOSED sentinel and post written 2026-08-01). The weekly-clash half is refuted — open_week_clash returns null before starts_on. is_partial suppresses the floor loop so no points move; the one points write is the hybrid +15, which spec §14.0 already flags as open for v1.1. Severity corrected to P2/P3.",
  "slice": "DB scoring & settlement engine",
  "verification": {
   "status": "survived",
   "correctedSeverity": "P2",
   "votes": [
    {
     "lens": "control-flow",
     "verdict": "CONFIRMED",
     "refuted": false,
     "correctedSeverity": "P2",
     "reasoning": "seasons.status has no pre-season value in its check constraint and start_season writes only leagues.phase; run_month_closes iterates on status with no date or phase predicate and close_month posts unconditionally. enter_cup_final guards only on status and the date and seeds from a LEFT JOIN, so empty squads still yield ranks 1 and 2 separated by random(). Two corrections narrowing it: floors are already waived by is_partial and v_rounds_ranked already restricts rounds to the season window, so no points or penalties are wrong; and close_season's INNER JOIN yields a NULL champion, not 'Squad 1'."
    },
    {
     "lens": "intent-and-precedent",
     "verdict": "CONFIRMED",
     "refuted": false,
     "correctedSeverity": "P3",
     "reasoning": "status='active' from creation is by design — D120/D122 settle the pre-first-tee state as a stage DERIVED from dates, and D122 says so in as many words — so the proposed 'scheduled' status would reopen two decisions. The weekly-clash leg is REFUTED: open_week_clash returns null before starts_on. The month-close leg is real and has ALREADY fired in prod (a league starting 2026-08-03 carries a JULY CLOSED sentinel and post written 2026-08-01), but is_partial suppresses the floor loop so nothing scores, and the one points write (hybrid +15) is logged as open for v1.1 in spec §14.0. The right fix is to gate close_month on month/season overlap and enter_cup_final on phase."
    }
   ]
  }
 },
 {
  "id": "DBE-05",
  "title": "close_month's hybrid +15 goes to an arbitrary squad when nobody played (or on a tie)",
  "file": "supabase/migrations/20260727160000_board_voice.sql",
  "line": "306-327",
  "severity": "P2",
  "kind": "bug",
  "category": "scoring-correctness",
  "sessionCode": false,
  "confidence": 8,
  "quote": "    order by coalesce(sum(rr.points),0) desc\n    limit 1;",
  "failureScenario": "There is no `having sum(...) > 0` and no tie handling. In a 4-squad hybrid league where nobody posted in a month, every squad sums to 0 and `order by 0 desc limit 1` returns a non-deterministic squad, which is then given +15 season points and announced on the board. The same happens on a genuine tie. It is a permanent season_adjustments row that changes the standings and, via v_squad_standings, the Cup Final seeding.",
  "fix": "Add the having clause and detect ties explicitly; better, delete the branch — D48 retired Format C.",
  "notes": "Reachable only for season_format='hybrid' — which is the column DEFAULT and is carried by four LOCKED prod leagues today (DBE-06).",
  "slice": "DB scoring & settlement engine",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "DBE-06",
  "title": "league_settings.season_format defaults to 'hybrid', a format D48 retired — and four locked prod leagues carry it",
  "file": "supabase/migrations/00000000000000_initial_baseline.sql",
  "line": "1081",
  "severity": "P2",
  "kind": "bug",
  "category": "spec-conflict",
  "sessionCode": false,
  "confidence": 9,
  "quote": "    \"season_format\" \"text\" DEFAULT 'hybrid'::\"text\" NOT NULL,",
  "failureScenario": "create_league inserts league_settings with no columns, so every league is born hybrid. Any lock path that does not go through the wizard's lockViaRpc leaves it: four LOCKED prod leagues carry 'hybrid' and one carries 'h2h'. On the 1st, close_month's hybrid branch will award those leagues a +15 monthly bonus that appears in no bylaw, no wizard screen and no spec section still in force. D48's premise — 'no engine code exists for any of these' — is false: the +15 engine is in close_month.",
  "fix": "Set the column default to 'points', update the stragglers, delete close_month's hybrid block, and add a db-check asserting no row diverges.",
  "notes": "The five non-'points' rows may be blind-audit test leagues; the DEFAULT is the durable bug.",
  "slice": "DB scoring & settlement engine",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "DBE-07",
  "title": "score_round(7 args) is a second, disagreeing implementation of §2.2 that is still granted to authenticated",
  "file": "supabase/migrations/00000000000000_initial_baseline.sql",
  "line": "692-714",
  "severity": "P2",
  "kind": "bug",
  "category": "duplicated-rule",
  "sessionCode": false,
  "confidence": 9,
  "quote": "  v_pts := case when v_pvi >= 3 then 12 when v_pvi >= 1 then 9 when v_pvi >= -1 then 7 when v_pvi >= -3 then 6 else 5 end;",
  "failureScenario": "Executed against prod: score_round(82, 71.0, 113, null, 10.0, 100, 18) returns pvi −1.0 and points 7, while cup_points(-1.0) returns 6. It also skips the engine's rounding step. The function is dead in the engine (its trigger was repointed and rounds.points/pvi were dropped) but pg_proc still shows authenticated=X, so it is on the PostgREST surface and build-db.mjs emits a Swift Rpc.score_round for it. Any client reaching for it gets the retired band rule.",
  "fix": "Revoke execute from authenticated and drop the function — nothing in supabase/, index.html or apps/ios calls it — then regenerate Rpc.swift.",
  "notes": "Also grants the 0-arg trigger function to authenticated; harmless but the same sweep should take it.",
  "slice": "DB scoring & settlement engine",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "DBE-08",
  "title": "The post preview bands the unrounded PvI at 100% allowance — 2 points apart from what the engine writes",
  "file": "index.html",
  "line": "6591-6595",
  "severity": "P2",
  "kind": "bug",
  "category": "scoring-correctness",
  "sessionCode": false,
  "confidence": 8,
  "quote": "    diff=(gross-rating)*113/slope;\n    vs=state.myIndex-diff;\n    [pts,msg]=pointsFor(vs);",
  "failureScenario": "The engine rounds TWICE — score_round stores round(differential,1) and v_rounds_ranked computes round(index*allowance/100 − differential, 1) before calling cup_points. The preview rounds neither and uses 100% allowance. Verified against prod: gross 83, rating 70.8, slope 125, index 12.0 → server 9 points, preview 7. Q-20 fixed the −1.0 edge in pointsFor/bandName but not the rounding, so the disagreement survives for any PvI within 0.05 of a band edge.",
  "fix": "Round the way the engine does before banding, and apply the league allowance — D123 item (3) plus the rounding step D123 does not mention.",
  "notes": "Same defect family as POST-B-01 and idx05-01; the allowance is the dominant term and the rounding the residual.",
  "slice": "DB scoring & settlement engine",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "DBE-09",
  "title": "A commissioner can pre-insert the month_closed sentinel and silently disable a whole month's floor penalties",
  "file": "supabase/migrations/00000000000000_initial_baseline.sql",
  "line": "1981",
  "severity": "P2",
  "kind": "bug",
  "category": "data-integrity",
  "sessionCode": false,
  "confidence": 8,
  "quote": "CREATE POLICY \"adj_write\" ON \"public\".\"season_adjustments\" FOR INSERT TO \"authenticated\" WITH CHECK (\"public\".\"is_commissioner\"(…",
  "failureScenario": "close_month's idempotency guard is 'a month_closed row with created_by null'. season_adjustments carries authenticated=arwdDxtm and adj_write restricts only the LEAGUE, not the kind and not created_by. A Pro who knows they will miss September's floor inserts that exact row from the console; on Oct 1 close_month returns immediately — no floor penalties for anyone in the league, no hybrid bonus, no board post, and no trace that a close was skipped. §14.2 says every adjustment carries a reason and is reversible via the log; this one defeats the log.",
  "fix": "Tighten the policy to kind='override' with created_by = my_member_id, or move commissioner adjustments behind a definer RPC and revoke INSERT entirely.",
  "notes": "Requires commissioner rights — an integrity/abuse hole, not a privilege escalation. The same policy also lets a Pro mint 'bye' rows and arbitrary matchup_bonus points.",
  "slice": "DB scoring & settlement engine",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "DBE-10",
  "title": "assign_player never checks that the member belongs to the squad's league",
  "file": "supabase/migrations/20260722210000_squad_formation_integrity.sql",
  "line": "135-153",
  "severity": "P2",
  "kind": "bug",
  "category": "authorization",
  "sessionCode": false,
  "confidence": 7,
  "quote": "  if not is_commissioner(se.league_id) then raise exception 'commissioner only'; end if;\n  …\n  insert into squad_members (squad_id, member_id) values (p_squad, p_member);",
  "failureScenario": "p_member is a global league_members.id and is never checked against se.league_id; squad_members' only FK is to league_members(id). A commissioner of league A holding a league_members.id from league B can seat that person. Points do not leak (the season join fails), but the foreign golfer renders on league A's squad card, and on the next 1st close_month's floor loop publishes their display name to league A's board.",
  "fix": "Add an existence check that p_member is in league_members for se.league_id before the delete.",
  "notes": "Did not attempt the call. What I could not determine is how easily a Pro obtains a foreign league_members.id — CS.members is scoped to the open league, so it likely needs dual membership.",
  "slice": "DB scoring & settlement engine",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "DBE-11",
  "title": "run_month_closes only ever closes the immediately-previous month — the final month of every season is never closed, and a missed cron is lost",
  "file": "supabase/migrations/00000000000000_initial_baseline.sql",
  "line": "630-644",
  "severity": "P2",
  "kind": "bug",
  "category": "correctness",
  "sessionCode": false,
  "confidence": 8,
  "quote": "    perform close_month(se.id, (date_trunc('month', current_date) - interval '1 month')::date);",
  "failureScenario": "Two losses. (1) A season ending 2026-12-19 has close_season flip status to 'complete' on 2026-12-22, so on 2027-01-01 the loop no longer selects it and December is never closed — no floor assessment, no sentinel, no board post, no hybrid bonus. §14.0 snaps seasons to whole weeks, so mid-month endings are the norm: 10 of 11 prod seasons. (2) A cron outage leaves that month permanently unclosed, with no catch-up loop, even though close_month is idempotent and safe to re-run.",
  "fix": "Loop from the last closed month to the month that just ended, and call close_month for the final month inside close_season before the crown.",
  "notes": "Verified the status-flip ordering by reading daily_season_tick; not reproduced end to end.",
  "slice": "DB scoring & settlement engine",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "DBE-12",
  "title": "v_rounds_ranked has no join-date gate: a mid-season joiner retroactively banks every round back to first tee",
  "file": "supabase/migrations/00000000000000_initial_baseline.sql",
  "line": "1370-1372",
  "severity": "P2",
  "kind": "bug",
  "category": "scoring-correctness",
  "sessionCode": false,
  "confidence": 8,
  "quote": "             JOIN \"public\".\"league_members\" \"lm\" ON ((\"lm\".\"profile_id\" = \"r\".\"profile_id\")))",
  "failureScenario": "league_members.joined_at exists and is never referenced. A golfer with 20 posted rounds since July joins on 2026-12-01 and is assigned to the thinnest squad: every one of those rounds fans into the season and v_squad_standings credits that squad with five months of best-4 points — roughly 180 in a race the spec sizes at ~118/squad/month. They pay no floor for any of them, because close_month only assesses the month that just ended and §14.1's 15th rule is unimplemented. §9 also caps mid-season joins at halfway; nothing enforces that either.",
  "fix": "Add `r.played_on >= lm.joined_at::date` to the view's seasons join in a new migration, or make it an explicit bylaw — and enforce §9's cap in the join RPC.",
  "notes": "Whether this is intended is genuinely unclear — nothing in the decision log addresses it. Filed as a bug because a squad total that jumps 180 points on a roster change is not something §16's receipts can explain.",
  "slice": "DB scoring & settlement engine",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "DBE-13",
  "title": "Points King — 15% of the pot — is decided by an untiebroken `limit 1`",
  "file": "supabase/migrations/20260828170100_cup_final_race.sql",
  "line": "385-386",
  "severity": "P2",
  "kind": "bug",
  "category": "money-correctness",
  "sessionCode": false,
  "confidence": 8,
  "quote": "  select member_id into king from v_individual_standings\n   where season_id = p_season order by points desc nulls last limit 1;",
  "failureScenario": "Two golfers finishing level on individual points — ordinary in a bands engine where every round is worth 5, 6, 7, 9 or 12. Postgres returns whichever row it likes; that member gets the trophy and 15% of everything collected ($67.50 on a $450 pot), and the other gets nothing with no rung in the story explaining why. Every other crown in close_season gets the four-rung §14.3 ladder plus a logged coin flip.",
  "fix": "Give the king the same ladder, or at minimum a deterministic explainable order with a stored rung; if a true tie is possible, split k_cents (season_payouts' PK already allows two rows).",
  "notes": "v_individual_standings also excludes season_adjustments, so a member carrying a floor penalty is unaffected in the King race — arguably right per §4, but worth an explicit ruling.",
  "slice": "DB scoring & settlement engine",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "DBE-14",
  "title": "lock_league stores p_season_months and then ignores it — the fallback season is a hardcoded 182 days",
  "file": "supabase/migrations/20260829220000_lock_league.sql",
  "line": "39, 88, 101-104",
  "severity": "P2",
  "kind": "bug",
  "category": "correctness",
  "sessionCode": true,
  "confidence": 8,
  "quote": "            coalesce(p_starts_on, current_date),\n            coalesce(p_ends_on, current_date + 182))",
  "failureScenario": "Three defects in one expression. (1) p_season_months is accepted, written to league_settings and never used for the dates, so a caller that trusts the header's 'every parameter is defaulted' and omits p_ends_on gets a 26-week season for a 12-month league. (2) current_date is the server's UTC date, so a Pro locking after 17:00 Phoenix gets a season starting tomorrow. (3) current_date + 182 is 183 inclusive days = 26 weeks + 1 day, breaking §14.0's whole-week snap and leaving the week engine a 1-day week 27.",
  "fix": "Derive the fallback from the stored months in the league clock, snapped to whole weeks and inclusive; or make p_ends_on required.",
  "notes": "All three are latent — the client always passes both dates today — but the migration advertises the defaults as the skew path, which is exactly the scenario that will exercise them.",
  "slice": "DB scoring & settlement engine",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "DBE-15",
  "title": "The weekly clash applies the MONTHLY counting cap to a weekly contest, so a golfer who posted is settled as idle",
  "file": "supabase/migrations/20260829091000_weekly_clash.sql",
  "line": "269, 285",
  "severity": "P2",
  "kind": "bug",
  "category": "correctness",
  "sessionCode": false,
  "confidence": 8,
  "quote": "   where rr.season_id = p_season and rr.member_id = wc.a_member\n     and rr.month_rank <= coalesce(v_cap, 999)\n     and rr.played_on between v_ws and v_we",
  "failureScenario": "With cap 4, a heavy player's fifth round of the month ranks 5, so v_a_best comes back NULL and the settle ladder reads 'one side idle is a walkover W' — B takes the week by walkover and the board says so, even though A posted that week and B may have posted worse.",
  "fix": "Drop the month_rank filter — the clash is a week-level story about the best round posted that week. If the cap must be honoured, say so in the post rather than rendering it as absence.",
  "notes": "Built in 4b042d0. Settlement-adjacent rather than named in the slice; included because it is the third §2.2 implementation reading the same view.",
  "slice": "DB scoring & settlement engine",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "DBE-16",
  "title": "Month closes and week snapshots use the server's UTC date while the rest of the tick uses the league timezone",
  "file": "supabase/migrations/00000000000000_initial_baseline.sql",
  "line": "638-641",
  "severity": "P3",
  "kind": "bug",
  "category": "timezone",
  "sessionCode": false,
  "confidence": 7,
  "quote": "    perform close_month(se.id, (date_trunc('month', current_date) - interval '1 month')::date);",
  "failureScenario": "The cron fires at 07:10 UTC = 00:10 Phoenix, correct for the default league. For a league on America/Los_Angeles in winter that instant is 23:10 on the LAST day of the previous month, and for Pacific/Honolulu 21:10 — so close_month assesses the floor and writes the permanent sentinel while the league still has 1–3 hours of the month left, and a round posted in that window scores for the season but is invisible to the floor check. daily_season_tick and open_week_clash already do this right.",
  "fix": "Take the local date per season and skip when it is not the 1st; same for snapshot_week.",
  "notes": "Only reachable if a league sets a non-Phoenix timezone; every prod season is America/Phoenix today.",
  "slice": "DB scoring & settlement engine",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "DBE-17",
  "title": "seasons is fully client-writable by the commissioner, including champion_squad_id, champion_score and status",
  "file": "supabase/migrations/00000000000000_initial_baseline.sql",
  "line": "2305",
  "severity": "P3",
  "kind": "opportunity",
  "category": "authorization",
  "sessionCode": false,
  "confidence": 8,
  "quote": "CREATE POLICY \"seasons_write\" ON \"public\".\"seasons\" TO \"authenticated\" USING (\"public\".\"is_commissioner\"(\"league_id\")) …",
  "failureScenario": "seasons carries authenticated=arwdDxtm plus a FOR ALL policy for the Pro, and it holds every §16 deciding number: status, champion/runner-up ids, champion_score, tiebreak_rung, pot_cents, collected_cents. A Pro can set champion_squad_id to their own squad after the close (award_season_trophies and recompute_season_payouts both read those columns), move ends_on mid-season to shift the Cup Final window, or freeze the table by setting status='complete'. 20260828170100 sealed cup_finalists for exactly this reason and 20260828150000 sealed profiles; seasons was not swept.",
  "fix": "Revoke write from authenticated and route the two legitimate Pro edits through a definer RPC that refuses once phase='season'; add a db-check asserting it.",
  "notes": "Needs a commissioner acting deliberately — but the same class as the two holes the last two migrations closed.",
  "slice": "DB scoring & settlement engine",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "DBE-18",
  "title": "cup_points should own its own rounding, and the two 'unlimited cap' sentinels should be one",
  "file": "supabase/migrations/00000000000000_initial_baseline.sql",
  "line": "247-259",
  "severity": "P3",
  "kind": "opportunity",
  "category": "simplification",
  "sessionCode": false,
  "confidence": 8,
  "quote": "  select case when p_pvi >= 3 then 12 when p_pvi >= 1 then 9 when p_pvi > -1 then 7 when p_pvi >= -3 then 6 else 5 end;",
  "failureScenario": "cup_points bands whatever it is handed; the rounding that makes it correct lives in the CALLER. Verified: cup_points(-0.95) = 7 but v_rounds_ranked scores that same round 6. So even a caller that copies the half-open edges correctly still gets a different answer unless it also copies the rounding — which is exactly how the web preview (DBE-08) and settle_week_clash (DBE-02) drifted. Separately 'unlimited counting cap' is spelled 999 in four places and 10000 in three, and counting_cap has no column default so create_league leaves it NULL while the wizard copy promises 'Best 4'.",
  "fix": "Make cup_points round its own input so it is total and self-contained, then have every producer call it. Give counting_cap a default and replace both sentinels with one helper.",
  "notes": "db-checks 17 pins cup_points' four edges — extend it to pin the rounding.",
  "slice": "DB scoring & settlement engine",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "DBE-19",
  "title": "close_month's floor ignores joined_at and rounds the 9-hole half-credit up against the golfer",
  "file": "supabase/migrations/20260727160000_board_voice.sql",
  "line": "236-272",
  "severity": "P3",
  "kind": "bug",
  "category": "spec-conflict",
  "sessionCode": false,
  "confidence": 8,
  "quote": "      short := greatest(0, st.participation_floor - m.credits);\n      …\n        delta := -5 * ceil(short);",
  "failureScenario": "Two spec conflicts in one loop. §14.1 says a late joiner on/after the 15th has the floor waived that month; joined_at exists and the loop never reads it, so a golfer who joins on the 28th and posts nothing has their ONE season bye consumed for a month they were not in — or takes −5 per round short if it is already spent. And §2.4 says a nine counts 0.5 toward floors, but ceil(short) throws the half away: floor 2 with one 9-hole round gives short 1.5, ceil 2, delta −10 — the same deduction as posting nothing.",
  "fix": "Join league_members and skip members who joined on/after the 15th; use floor(short) or a numeric multiply so the half-credit is worth something.",
  "notes": "The auto-bye forgiveness itself (D14) is correct and idempotent. Solo leagues can never be penalised here — deliberate and documented this session in floorSentence.",
  "slice": "DB scoring & settlement engine",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "EF-01",
  "title": "Course cache is emptied before the replacement is known to be non-empty; the background refresh does it unattended",
  "file": "supabase/functions/courses/index.ts",
  "line": "108-131 (trigger path 265-272)",
  "severity": "P1",
  "kind": "bug",
  "category": "data-integrity",
  "sessionCode": false,
  "confidence": 8,
  "quote": "await admin.from(\"api_course_tees\").delete().eq(\"course_id\", cid);\n  for (const te of flattenTees(c)) {",
  "failureScenario": "flattenTees() returns [] for any payload whose tees shape it does not recognise — exactly the upstream drift this file documents surviving once on the search endpoint. The delete has already run, the insert loop body never executes, and fetchAndStore returns cid, so the course's tees AND holes (cascade) are gone and the handler answers ok:true. The dangerous trigger is the background path: a cache HIT older than 180 days calls fetchAndStore after the response is sent with only a console.error behind it, so a previously HEALTHY course is silently emptied, unattended — breaking the 'a course in our dataset NEVER goes stale from the user's view' guarantee by the routine that maintains it. The api_courses upsert also drops {error}.",
  "fix": "Compute flattenTees BEFORE any write and return early when empty; destructure {error} on the upsert and the delete.",
  "notes": "Verifier found the empty-parse outcome LIVE in prod — three courses cached 2026-08-29 with raw.tees = {} and zero tee/hole rows — proving the detail endpoint really does return tee-less courses. But the destructive arm is not yet reachable: the 180-day refresh needs a cached_at older than 40 days (first eligible ~2027-01-16) and no prod course has tees>0 with holes=0. Latent with a dated fuse; severity corrected to P2/P3. Also: once emptied, the state self-perpetuates — every tee pick then burns a foreground API pull.",
  "slice": "supabase/functions/ (6 files, 1765 lines, all read)",
  "verification": {
   "status": "survived",
   "correctedSeverity": "P2",
   "votes": [
    {
     "lens": "control-flow",
     "verdict": "PLAUSIBLE",
     "refuted": false,
     "correctedSeverity": "P3",
     "reasoning": "The shape is real and the empty parse occurs in prod today (three courses cached with raw.tees={} and zero tee/hole rows), and the cascade does take holes with the tees. But the destructive combination needs fetchAndStore to run against a course that HAS rows, and no reachable caller does that: the 180-day refresh has never fired (oldest cached_at is 40 days; first eligible ~2027-01-16), prod has zero courses with tees>0 and holes=0, and the search-enrichment path only calls it for courses whose cached tee list was already empty. A transient empty is also self-healed by the next cache call."
    },
    {
     "lens": "intent-and-precedent",
     "verdict": "CONFIRMED",
     "refuted": false,
     "correctedSeverity": "P2",
     "reasoning": "The live database supplies the missing half: flattenTees really does return [] off a real detail fetch. Not by design — the owner ruling that created this path promises a cached course survives 'any upstream weather', which an empty-parse refresh inverts. The Aug-28 hardening pass that fixed the same upstream drift on the search endpoint, and the Aug-28 silent-write sweep behind the CLAUDE.md landmine, both left this function untouched — never considered, not accepted. One thing understated: once emptied the state self-perpetuates, because the next tee pick takes the foreground fetchAndStore branch and burns a paid pull each time."
    }
   ]
  }
 },
 {
  "id": "EF-02",
  "title": "Every manual-location weather request shares one cache row keyed course_id='manual'",
  "file": "supabase/functions/weather/index.ts",
  "line": "54, 73-77, 99-101",
  "severity": "P2",
  "kind": "bug",
  "category": "correctness",
  "sessionCode": false,
  "confidence": 9,
  "quote": "const courseId = String(body?.course_id ?? \"\") || \"manual\";",
  "failureScenario": "weather_cache's primary key is (course_id, play_on). Golfer A in Phoenix requests a date with no course_id and the answer caches under ('manual', date); Golfer B in Seattle requests the same date within 6 hours and gets A's 104F Phoenix forecast. The row stores lat and lon and never reads them back — evidence the key was meant to include the location.",
  "fix": "Key manual entries by rounded location, or add lat/lon to the lookup and the PK.",
  "notes": "",
  "slice": "supabase/functions/",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "EF-03",
  "title": "courses' per-user daily cap is advisory: the ledger write is neither awaited nor error-checked",
  "file": "supabase/functions/courses/index.ts",
  "line": "153-176",
  "severity": "P2",
  "kind": "bug",
  "category": "cost-control",
  "sessionCode": false,
  "confidence": 8,
  "quote": "admin.from(\"courses_usage\").insert({ profile_id: uid, action: String(action ?? \"\") })\n    .then(() => {}, () => {});",
  "failureScenario": "Two defects in one line. Read-then-write with the write unawaited means N concurrent requests all observe the same `used`, so a burst at used=149 makes 199 paid API calls against a cap of 150. And both then-callbacks are empty, so an insert failure is discarded by construction — if courses_usage ever stops accepting rows, `used` freezes and the cap NEVER fires again: unbounded spend on a paid third party with no signal anywhere. The scan function solves exactly this with a reserve-then-count whose comment names the TOCTOU race.",
  "fix": "Mirror scan's pattern: reserve first, count including the reservation, delete on reject, and refuse if the reservation itself errors.",
  "notes": "",
  "slice": "supabase/functions/",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "EF-04",
  "title": "Course cache HITS burn the paid-lookup cap even though they make zero API calls",
  "file": "supabase/functions/courses/index.ts",
  "line": "175 (with 276)",
  "severity": "P2",
  "kind": "bug",
  "category": "correctness",
  "sessionCode": false,
  "confidence": 8,
  "quote": "admin.from(\"courses_usage\").insert({ profile_id: uid, action: String(action ?? \"\") })",
  "failureScenario": "The ledger row is written before the action branch, so action:'cache' counts against the 150/day paid-API cap even when the handler returns from_cache:true having made no upstream call. index.html fires that action on EVERY tee pick, so a Pro setting tees on 150 already-cached rounds gets HTTP 429 'daily course-lookup limit reached' having cost the business nothing — and the message explains none of it.",
  "fix": "Write the ledger row only on paths that actually call gca().",
  "notes": "",
  "slice": "supabase/functions/",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "EF-05",
  "title": "scan: an API request that spent nothing still consumes one of the golfer's 5 daily slots",
  "file": "supabase/functions/scan/index.ts",
  "line": "131-146, 181-196",
  "severity": "P2",
  "kind": "bug",
  "category": "cost-control",
  "sessionCode": false,
  "confidence": 8,
  "quote": "      console.error(\"[scan] api\", r.status, (await r.text()).slice(0, 400));",
  "failureScenario": "The reservation row is deleted only on cap-reject; any API failure leaves it standing and counted. The comment justifies this as 'it still spent the call', true for 429/5xx but false for every 4xx the API rejected before doing work — and for the no_api_key path. A golfer photographing five scorecards on a phone writing HEIC while the client labels them image/jpeg (media_type is unvalidated and forwarded verbatim) gets five 400s, zero cost to the business, and a 24h lockout with no explanation. r.status is in hand and is never branched on.",
  "fix": "Branch on r.status: delete the reservation for 400/401/403/404 and for no-response; keep it for 402/429/5xx and for a completed-but-unusable response. Validate media_type before spending.",
  "notes": "Verified the Anthropic call shape is correct — model, output_config.format, and the stop_reason check are all right.",
  "slice": "supabase/functions/",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "EF-06",
  "title": "scan's kill switch and caps fail OPEN on a database read error, contradicting the file's own 'fail closed' contract",
  "file": "supabase/functions/scan/index.ts",
  "line": "120-146",
  "severity": "P2",
  "kind": "bug",
  "category": "cost-control",
  "sessionCode": false,
  "confidence": 7,
  "quote": "  const { data: flagRow } = await admin.from(\"app_flags\").select(\"value\").eq(\"key\", \"scan\").maybeSingle();\n  const flag = flagRow?.value ?? {};\n  if (flag.enabled === false) return soft(\"disabled\");",
  "failureScenario": "supabase-js never throws, and {error} is dropped here and at the reservation insert and both count queries. On any app_flags read failure flag is {}, enabled is undefined, and the paid vision call proceeds — the kill switch bypassed by a transient DB error. Same shape downstream: a failed reservation yields resvId null and counts that exclude this call; failed counts yield null and both caps pass. So the single event that most warrants refusing disables all three gates at once, in a file whose header says 'COST DISCIPLINE (fail closed, always before spending)'.",
  "fix": "Destructure {error} on all four reads and return soft('unavailable') when any of them errors.",
  "notes": "Requires a DB read error, which could not be induced read-only.",
  "slice": "supabase/functions/",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "EF-07",
  "title": "push: friend-request email interpolates display_name into HTML with no escaping",
  "file": "supabase/functions/push/index.ts",
  "line": "322-334",
  "severity": "P2",
  "kind": "bug",
  "category": "security",
  "sessionCode": false,
  "confidence": 8,
  "quote": "    <p style=\"font-size:16px;line-height:1.5\"><strong>${who}</strong> wants in your crew on Cup Season.</p>",
  "failureScenario": "fromName/toName are user-controlled display_names, and firstName() only takes the first whitespace-delimited token — so any markup without a space passes through intact. An attacker sets a single-token anchor as their display name and friend-requests targets; sendEmail delivers it as htmlContent from hello@cupseason.app, so every target receives a Cup Season-branded email containing the attacker's link. Not script execution, but a working phishing-link injection inside a trusted-sender email. The sibling function defines esc() and escapes every interpolation; push has no escaper at all.",
  "fix": "Copy season-email's esc() into push and wrap both interpolations.",
  "notes": "",
  "slice": "supabase/functions/",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "EF-08",
  "title": "Cost-control ledgers and push tables grant authenticated=arwdDxtm; only a zero-policy RLS stands between a signed-in user and them",
  "file": "supabase/functions/scan/index.ts",
  "line": "131-144 (scan_usage); courses/index.ts:157-176 (courses_usage)",
  "severity": "P2",
  "kind": "bug",
  "category": "security-hardening",
  "sessionCode": false,
  "confidence": 8,
  "quote": "  const { data: resv } = await admin.from(\"scan_usage\").insert({ profile_id: uid, model: MODEL, ok: false }).select(\"id\").single();",
  "failureScenario": "Measured live: scan_usage, courses_usage, push_nudges, device_tokens, email_queue and cancellation_notices all carry authenticated=arwdDxtm with relrowsecurity true and ZERO policies. RLS denies today — I want to be precise about that. But this is the two-layers-one-working shape CLAUDE.md records from the 2026-07-27 audit, and the D37 sweep removed anon's relation privileges, not authenticated's. Add one SELECT policy to scan_usage for a 'your scan history' screen and DELETE arrives with it under the standing grant — a golfer clearing their own rows and resetting the paid-API cap. TRUNCATE is never governed by RLS under any policy set.",
  "fix": "Revoke all on those six tables from authenticated — no client path touches any of them — and extend db-checks to assert that service-role-only tables hold no authenticated grants.",
  "notes": "Grant facts measured via pg_class.relacl per CLAUDE.md's own guidance.",
  "slice": "supabase/functions/",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "EF-09",
  "title": "season-email's only duplicate-send guard reads a field that is always null on the path that invokes it",
  "file": "supabase/functions/season-email/index.ts",
  "line": "229 (with 199)",
  "severity": "P2",
  "kind": "bug",
  "category": "correctness",
  "sessionCode": false,
  "confidence": 7,
  "quote": "  const row = rec;\n  if (row.sent_at) return new Response('already sent', { status: 200 });",
  "failureScenario": "Verified live: the invoker is an AFTER INSERT trigger on email_queue, so record.sent_at is NULL by construction and this guard can never be true on the only path that reaches it — it is dead. And mark_email_sent is an unconditional update, not a compare-and-set, so nothing in the whole path reads back committed state. Any second delivery of the same row — a manual re-fire, a replayed curl with the secret, or the hook being recreated as AFTER INSERT OR UPDATE (the sibling friendships trigger IS that shape) — re-sends the full recap to every member.",
  "fix": "Re-read sent_at before sending, and make mark_email_sent a claim (`where id = p_id and sent_at is null returning 1`).",
  "notes": "The guard is provably dead; a live double-send additionally requires a duplicate delivery, which pg_net does not do by default.",
  "slice": "supabase/functions/",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "EF-10",
  "title": "season-email's cancellation branch mails whatever addresses the request body names, never reading the row back",
  "file": "supabase/functions/season-email/index.ts",
  "line": "191-215",
  "severity": "P3",
  "kind": "bug",
  "category": "security",
  "sessionCode": false,
  "confidence": 7,
  "quote": "  const rec = (body?.record ?? body?.new ?? body ?? {}) as …\n  if (rec.payload && Array.isArray(rec.payload.recipients)) { … for (const r of rec.payload.recipients) {",
  "failureScenario": "rec falls back to the bare request body and the loop takes email, name and cents straight out of it — the cancellation_notices row is never read back by id. The only gate is the shared x-push-secret, which is also on four other webhooks. Anyone holding that secret POSTs a payload and Brevo sends Cup Season-branded mail from hello@cupseason.app to arbitrary addresses with an attacker-chosen league name and dollar figure. The season-recap path next to it does this correctly — every fact comes from season_email_payload().",
  "fix": "Take only rec.id from the body and read the payload back with the service client; that also fixes this branch's sent_at guard.",
  "notes": "Requires the webhook secret; filed as defence-in-depth on a mail-sending surface.",
  "slice": "supabase/functions/",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "EF-11",
  "title": "test-seed's reset is global, not caller-scoped, contradicting its own docstring",
  "file": "supabase/functions/test-seed/index.ts",
  "line": "59-115 (vs header 12-13, call at 361)",
  "severity": "P3",
  "kind": "bug",
  "category": "destructive-operation",
  "sessionCode": false,
  "confidence": 8,
  "quote": "    for (const u of users) if ((u.email ?? \"\").endsWith(`@${BOT_DOMAIN}`)) ids.push(u.id);",
  "failureScenario": "The header says 'Caller-scoped: only ever seeds the signed-in account.' reset() deletes EVERY auth user whose email ends @cupseason.test and every league those bots commission, regardless of the `me` argument — which narrows only one event lookup. So the documented App Review flow also destroys the founder's own test world mid-session, and two founders can never hold test worlds simultaneously. The response reports counts without saying whose.",
  "fix": "Scope reset to the subject (namespace bot emails per subject), or change the docstring and the response to say plainly that reset is global.",
  "notes": "",
  "slice": "supabase/functions/",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "EF-12",
  "title": "test-seed's buildLeague dereferences every insert result without checking {error}, on hardcoded unique codes",
  "file": "supabase/functions/test-seed/index.ts",
  "line": "163-165 (also 177, 184, 190, 198, 281, 289, 302)",
  "severity": "P3",
  "kind": "bug",
  "category": "silent-failure",
  "sessionCode": false,
  "confidence": 7,
  "quote": "    const { data: lg } = await admin.from(\"leagues\").insert({ … }).select(\"id\").single();\n    const leagueId = lg.id;",
  "failureScenario": "League codes are hardcoded constants and reset() finds leagues ONLY via bot commissioner_id, so a league whose bot auth rows were partially removed is invisible to teardown and its code survives. The next seed's insert then fails the unique constraint, supabase-js returns {error} without throwing, lg is null, and the next line dies with 'Cannot read properties of null' — caught and returned as a 500 with that opaque message, AFTER eight auth users have already been created. The operator sees a null-property TypeError with no indication the real cause is a leftover code.",
  "fix": "Destructure {error} on each insert and throw a named error; a small must(res, what) helper covers all nine sites.",
  "notes": "",
  "slice": "supabase/functions/",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "EF-13",
  "title": "weather: today's forecast disappears at 17:00 Phoenix because the range check parses the date at UTC midnight",
  "file": "supabase/functions/weather/index.ts",
  "line": "68-70",
  "severity": "P3",
  "kind": "bug",
  "category": "dates-timezones",
  "sessionCode": false,
  "confidence": 7,
  "quote": "  const day = new Date(date + \"T00:00:00Z\").getTime();\n  if (day < now - 24 * 3600e3 || day > now + 16 * 24 * 3600e3) return soft(\"out_of_range\");",
  "failureScenario": "day is the play date at UTC midnight, so `day < now - 24h` becomes true once now passes date+24h UTC — 17:00 local ON THE DAY OF THE ROUND in Phoenix, 14:00 in Hawaii. A golfer opening the round sheet for TODAY'S round at 5:30pm gets out_of_range and the client hides the weather line entirely. The 24h grace was clearly meant to keep today available all day; the UTC-midnight parse eats seven hours of it.",
  "fix": "Compare on date strings in the league's timezone, or widen the lower bound to 48h.",
  "notes": "The CLAUDE.md UTC-midnight landmine in its comparison form rather than its rendering form.",
  "slice": "supabase/functions/",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "EF-14",
  "title": "test-seed: a guard whose condition is always true (the selected row cannot carry the field it tests)",
  "file": "supabase/functions/test-seed/index.ts",
  "line": "292 (with 282)",
  "severity": "P3",
  "kind": "bug",
  "category": "dead-code",
  "sessionCode": false,
  "confidence": 9,
  "quote": "  if (tRed.captain_player_id === undefined) await admin.from(\"event_teams\").update({ captain_player_id: epId[me] }).eq(\"id\", tRed.id);",
  "failureScenario": "tRed comes from an insert whose select is only 'id', so captain_player_id is undefined on every run and the condition is unconditionally true. The Blue team on the very next line performs the same update with no guard — which is what this line amounts to. Harmless today, but it reads as a meaningful check and will mislead the next person who tries to make it conditional.",
  "fix": "Delete the condition, matching the Blue line.",
  "notes": "",
  "slice": "supabase/functions/",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "EF-15",
  "title": "courses returns raw internal exception text to the client",
  "file": "supabase/functions/courses/index.ts",
  "line": "286-290",
  "severity": "P3",
  "kind": "opportunity",
  "category": "security",
  "sessionCode": false,
  "confidence": 6,
  "quote": "    const msg = String((e as Error)?.message ?? e);\n    console.error(`[courses] action=… failed :: ${msg}`);\n    return json({ error: msg }, 502);",
  "failureScenario": "Today msg is bounded to 'golfcourseapi <status>' and JS TypeErrors, so the leak is small. But this catch wraps fetchAndStore, which touches three tables; the moment any future code there throws a PostgREST message, a database-internal string is on the wire to any signed-in caller. The log line already captures msg, so the response gains nothing from carrying it.",
  "fix": "Keep the console.error and return a fixed string.",
  "notes": "",
  "slice": "supabase/functions/",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "EF-16",
  "title": "push never asserts the table it was invoked for, so a fifth misrouted webhook reads as a data problem",
  "file": "supabase/functions/push/index.ts",
  "line": "424-426 (with 341-345)",
  "severity": "P3",
  "kind": "opportunity",
  "category": "observability",
  "sessionCode": false,
  "confidence": 8,
  "quote": "  if (!record.league_id) {\n    if (!record.event_id) return reply('no-league-or-event', { post: record.id });",
  "failureScenario": "After the friendships and push_nudges branches, ANY table falls into the posts path — the function trusts the shape of `record` without ever checking `table`. Four webhooks point here. A fifth created against the wrong function — the exact D68 incident this file's own comment memorialises — would land here and answer 'no-league-or-event', which reads as 'that post had no league', not as 'this function was called about the wrong table'. The one exit not covered by the otherwise-complete naming discipline.",
  "fix": "`if (table !== 'posts') return reply('unexpected-table', { table });` before the posts branch.",
  "notes": "",
  "slice": "supabase/functions/",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "EF-17",
  "title": "Both webhook secret comparisons are non-constant-time",
  "file": "supabase/functions/push/index.ts",
  "line": "337 (and season-email/index.ts:182)",
  "severity": "P3",
  "kind": "opportunity",
  "category": "security-hardening",
  "sessionCode": false,
  "confidence": 4,
  "quote": "  if (req.headers.get('x-push-secret') !== Deno.env.get('PUSH_WEBHOOK_SECRET')) {\n    return new Response('forbidden', { status: 403 });\n  }",
  "failureScenario": "JS string !== short-circuits on the first differing byte, so comparison time correlates with the matching prefix. I could not construct a realistic remote attack against a Deno edge isolate — network jitter swamps the signal and there is no oracle for iterating — so this is hardening, not a bug. Worth noting the check does fail closed when the env var is unset, which is the half that matters most.",
  "fix": "Compare with a fixed-time helper over the encoded bytes in both functions.",
  "notes": "Confidence is about exploitability, not about the fact. Listed because the slice brief asked.",
  "slice": "supabase/functions/",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "EF-18",
  "title": "push issues one badge RPC per recipient per notification",
  "file": "supabase/functions/push/index.ts",
  "line": "258-260",
  "severity": "P3",
  "kind": "opportunity",
  "category": "efficiency",
  "sessionCode": false,
  "confidence": 8,
  "quote": "  await Promise.all(owners.map(async (id) => badges.set(id, await actionableCount(id))));",
  "failureScenario": "Every board post fans out to one actionable_count_of RPC per distinct device owner. The comment bounds it honestly at 'a dozen recipients per post' — but that is a property of current league size, not of the code, and a 30-person league makes 30 round trips inside the webhook's window for a single chat message.",
  "fix": "Add an actionable_counts_of(uuid[]) alongside the single-profile function, keeping one SQL definition of the count.",
  "notes": "",
  "slice": "supabase/functions/",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "EF-19",
  "title": "season-email's Payload type omits solo, and nothing type-checks these files",
  "file": "supabase/functions/season-email/index.ts",
  "line": "30-37 (used at 250)",
  "severity": "P3",
  "kind": "opportunity",
  "category": "dead-or-contradictory",
  "sessionCode": false,
  "confidence": 7,
  "quote": "  const champ = p.solo ? String(p.champion ?? '').trim().split(/\\s+/)[0] || p.champion : p.champion;",
  "failureScenario": "`solo` is not declared on Payload, so p.solo is a TS error that never fires: Supabase bundles with esbuild and no typecheck, there is no deno binary in this toolchain, and ship.sh only deploys. Verified against the live database that season_email_payload DOES return solo, so the runtime behaviour is correct — the type is decorative, and it is the only thing standing between a future renamed payload key and a silently wrong subject line.",
  "fix": "Declare solo, and add a `deno check` step to preflight — it would have caught this and costs nothing, in the spirit of checks 10/11 for the generated Swift.",
  "notes": "",
  "slice": "supabase/functions/",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "EF-20",
  "title": "scan can move to claude-opus-5 at identical token pricing",
  "file": "supabase/functions/scan/index.ts",
  "line": "23",
  "severity": "P3",
  "kind": "opportunity",
  "category": "simplification",
  "sessionCode": false,
  "confidence": 8,
  "quote": "const MODEL = \"claude-opus-4-8\";",
  "failureScenario": "Not a defect — claude-opus-4-8 is current and supported and the request shape around it is correct. But claude-opus-5 is the same price per MTok, so a scorecard-OCR accuracy upgrade is available at zero marginal cost, and scan_usage already records `model` per row so an A/B against the existing accuracy dataset is free.",
  "fix": "Change MODEL and compare holes_read / holes_sum agreement against the stored history.",
  "notes": "Verified model ids and pricing against the claude-api reference, not from memory.",
  "slice": "supabase/functions/",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "KIT-01",
  "title": "A blank rating still scores a round on the phone — Q-22 landed web-only",
  "file": "apps/ios/Packages/CupSeasonKit/Sources/CupSeasonKit/Post/PostCard.swift",
  "line": "199-222",
  "severity": "P1",
  "kind": "bug",
  "category": "correctness",
  "sessionCode": false,
  "confidence": 9,
  "quote": "let rating = card.ratingValue\n    let slope = card.slopeValue > 0 ? Double(card.slopeValue) : 113\n    let (f9, b9) = card.inputs\n    if f9 > 0 && b9 > 0 {\n      let gross = f9 + b9\n      let diff = (Double(gross) - rating) * 113 / slope",
  "failureScenario": "Golfer types 45 and 46 with no tee picked (the rating/slope row is optional and collapsed by default, and the course field explicitly invites typing them by hand). ratingValue is 0 and slopeValue 0 so slope falls back to 113: diff = 91, vs = idx − 91, cupPoints = 5. The composer renders '91 · 18 holes', chips '5 pts · PIGL' and a large negative vs, and Post stays enabled because submit guards only `preview != nil`. PostPayload then sends rating 0 / slope 0, and score_round's BEFORE trigger divides by slope — division by zero, three retried inserts, and an opaque 'Post failed.' index.html's recalc was fixed on 2026-08-29 with exactly this comment.",
  "fix": "Return nil (or a .needsTee case) from PostCalc.preview when a nine is entered and rating or slope is missing, carrying the web's sentence, and gate the Post button on the same predicate.",
  "notes": "The phone code predates the session; the DIVERGENCE was created this session when the web half was fixed and the Swift half was not. The remediation plan's Q-22 row explicitly names the iOS files. Verifiers corrected severity to P2 for the unreleased client — nothing lands, the DB refuses.",
  "slice": "apps/ios/Packages/CupSeasonKit/Sources/ — shared logic package",
  "verification": {
   "status": "survived",
   "correctedSeverity": "P2",
   "votes": [
    {
     "lens": "control-flow",
     "verdict": "CONFIRMED",
     "refuted": false,
     "correctedSeverity": "P1",
     "reasoning": "The rating/slope row is optional and collapsed by default and the course field explicitly invites typing them by hand, so a card with two nines and no tee is ordinary. PostCalc.vsIsSane exists but is used only downstream of the insert. The Post button is never disabled and tapPost guards only preview != nil. Server end confirms: division by zero in score_round's BEFORE trigger (or the rating CHECK), three failing insert attempts, and an opaque message. The remediation plan scopes Q-22 as 'client · ios' and names the Swift work."
    },
    {
     "lens": "intent-and-precedent",
     "verdict": "CONFIRMED",
     "refuted": false,
     "correctedSeverity": "P2",
     "reasoning": "The plan wrote the iOS half of Q-22 into the same row as the web half and the ship commit delivered index.html only — the divergence was created this session. No guard was ever written and none reverted. But nothing bad can land: the round is rejected at the database, so no invented differential reaches rounds or the ledger, and the harm is confined to an unreleased client — a lying preview and a dead-end Post."
    }
   ]
  }
 },
 {
  "id": "KIT-02",
  "title": "The round receipt still uses the pre-Q-20 band ladder (RoundCopy is a second producer)",
  "file": "apps/ios/Packages/CupSeasonKit/Sources/CupSeasonKit/Rounds/RoundCopy.swift",
  "line": "19-34",
  "severity": "P1",
  "kind": "bug",
  "category": "scoring-consistency",
  "sessionCode": true,
  "confidence": 10,
  "quote": "if vs >= -1 { return (7, \"Right on your number. Steady points.\") }\n…\n    if vs >= -1 { return \"Played to it\" }",
  "failureScenario": "At pvi exactly −1.0 (a real one-decimal bucket, since v_rounds_ranked rounds it) ReceiptSeed calls RoundCopy.bandName, so the receipt shows '−1.0 — PLAYED TO IT' directly above 'Points 6'. The same round on the board card and in the post epilogue goes through the corrected CSBands.bandName and reads 'A LITTLE LOOSE', as does the web receipt. RoundCopy.pointsFor(-1.0) also returns 7 against the engine's 6, and RoundsYouTests pins both stale values, so the suite defends the bug.",
  "fix": "Delete RoundCopy.pointsFor/bandName/vsPhrase, forward ReceiptSeed to CSBands, and update the tests.",
  "notes": "The Q-20 commit is session code; this file is the half it missed. Verifiers: pointsFor/vsPhrase have no production callers, so only the label reaches a screen; the file's 'ONE KNOWN SEAM' header documents a state that no longer exists. Prod holds 0 of 290 rounds at exactly −1.0, so severity corrected to P2.",
  "slice": "apps/ios/Packages/CupSeasonKit/Sources/",
  "verification": {
   "status": "survived",
   "correctedSeverity": "P2",
   "votes": [
    {
     "lens": "control-flow",
     "verdict": "CONFIRMED",
     "refuted": false,
     "correctedSeverity": "P2",
     "reasoning": "pvi is a rounded one-decimal value so −1.0 is genuine, and the call site is live production code presented as a global sheet. The file's 'ONE KNOWN SEAM' header documents a state that no longer exists — Q-20 moved the thing it mirrors — so BY_DESIGN fails. Two corrections: pointsFor and vsPhrase have NO production callers, so only the label reaches a screen; and RoundCopy.theirs is used at the same line and must be kept or forwarded."
    },
    {
     "lens": "intent-and-precedent",
     "verdict": "CONFIRMED",
     "refuted": false,
     "correctedSeverity": "P2",
     "reasoning": "D123 item 4 rules that pointsFor/bandName adopt cup_points' half-open edges, and 4be994a implemented it in CSBands, BoardTests, index.html and app-tests — RoundCopy was not in the commit and has not been touched since parity wave 1. Prod holds 0 of 290 rounds at exactly −1.0, so it has not yet occurred; what keeps it above P3 is that the receipt is the surface whose job is making the points figure defensible, and the test suite actively defends the old rule."
    }
   ]
  }
 },
 {
  "id": "KIT-03",
  "title": "The weekly clash's band name changes when the server settles it — settle_week_clash carries a third ladder",
  "file": "apps/ios/Packages/CupSeasonKit/Sources/CupSeasonKit/League/WeekClash.swift",
  "line": "71-74",
  "severity": "P1",
  "kind": "bug",
  "category": "scoring-consistency",
  "sessionCode": true,
  "confidence": 9,
  "quote": "    return .init(round_id: top.round_id, played_on: top.played_on, points: top.points, pvi: top.pvi,\n                 band: top.pvi.map(CSBands.bandName))",
  "failureScenario": "Mid-week the card bands the round through the corrected CSBands.bandName → 'A little loose' at pvi −1.0. After the tick runs settle_week_clash, week_clashes.a_best.band is written by the SQL ladder's `>= -1` and the card, now preferring the stored row, says 'Played to it' for the identical round — while the points figure beside it is still 6. The settle board post repeats it. db-checks 17 pins only cup_points, so nothing detects the drift.",
  "fix": "Replace both inline CASE ladders (and the v_phrase) with a cup_band(numeric) built on cup_points' thresholds, and pin it in db-checks.",
  "notes": "Verifiers: the settled card actually AGREES with the iOS receipt sheet, because RoundCopy (KIT-02) is also stale — the mid-week card is the outlier. A cup_band() alone leaves the two Swift halves disagreeing. Display only; severity corrected to P2.",
  "slice": "apps/ios/Packages/CupSeasonKit/Sources/",
  "verification": {
   "status": "survived",
   "correctedSeverity": "P2",
   "votes": [
    {
     "lens": "control-flow",
     "verdict": "CONFIRMED",
     "refuted": false,
     "correctedSeverity": "P2",
     "reasoning": "Verified the deployed prod body still carries `>= -1` and that both clients prefer the stored band once settled. daily_season_tick settles every opened clash whose window has passed, and the migration's self-enforcing DO block asserts the tick calls it. Correction: because RoundCopy (KIT-02) is also stale, the settled card AGREES with the iOS receipt and the mid-week card is the outlier — a cup_band() alone would leave the two Swift halves disagreeing. No winner, settlement or ledger changes."
    },
    {
     "lens": "intent-and-precedent",
     "verdict": "CONFIRMED",
     "refuted": false,
     "correctedSeverity": "P2",
     "reasoning": "Reproduced end to end against the live database. spec §2.2 assigns −1.0 to the 6-point band, so the SQL contradicts the spec; Q-20's commit states the invariant and added db-checks 17, which pins cup_points only. git shows the ladder entered in 4b042d0 and never reverted. Missed by the reviewer: a THIRD closed-edge ladder at :322 writes the immutable settle board post, so a cup_band() replacing only the two CASEs leaves the worst surface untouched."
    }
   ]
  }
 },
 {
  "id": "KIT-04",
  "title": "A sub-6-week cup_final league: the wizard says \"points table\", the payload writes cup_final, and the engine runs a Cup Final on day two",
  "file": "apps/ios/Packages/CupSeasonKit/Sources/CupSeasonKit/Wizard/WizardState.swift",
  "line": "236,250,303",
  "severity": "P1",
  "kind": "bug",
  "category": "money-correctness",
  "sessionCode": false,
  "confidence": 9,
  "quote": "canCup = cup && d.durWeeks >= 6\n…\n  public var seasonTail: String { durLabel + (canCup ? \"\" : \" · POINTS TABLE\") }\n…\n    finish = d.finish",
  "failureScenario": "WizardDials.durs offers 2, 3, 4, 5 weeks and finish defaults to 'cup_final', with no gate on the endgame segment. A Pro picks 4 weeks and leaves the endgame alone: the portrait renders '4 wk · POINTS TABLE' and the bylaws row prints 'Points table crowns it', but the payload writes finish='cup_final' and prod's daily_season_tick has no minimum-length guard, so with ends_on = start + 28 the window opens at start + 1 and the season flips to cup_final on day two. close_season then crowns and settles the pot by the Cup path — paying a champion the bylaws said would not exist. At 2–3 weeks the flip lands before first tee.",
  "fix": "Force finish='points_table' in the payload when durWeeks < 6 and say so on the dial, or add the guard to daily_season_tick — but do not leave the copy as the only statement of the rule.",
  "notes": "Shared with the web's renderBylaws, but the phone is the client that also writes the payload. Verifiers reproduced enter_cup_final on a 4-week fixture in an aborted transaction: two of three squads were locked as finalists with zero rounds played, separated by random(). set_league_finish also refuses to dial back once the window is open, so there is no escape hatch.",
  "slice": "apps/ios/Packages/CupSeasonKit/Sources/",
  "verification": {
   "status": "survived",
   "correctedSeverity": "P1",
   "votes": [
    {
     "lens": "control-flow",
     "verdict": "CONFIRMED",
     "refuted": false,
     "correctedSeverity": "P1",
     "reasoning": "Reproduced against prod in an aborted transaction: a squads3 league with a 4-week season flipped to cup_final and locked two of three squads as finalists with zero rounds played, separated by random(), on day two. No length guard exists in daily_season_tick or enter_cup_final, no CHECK constraint bounds season length against finish, and the wizard renders the full durs array with an ungated finish segment. canCup is consumed only by two cosmetic surfaces."
    },
    {
     "lens": "intent-and-precedent",
     "verdict": "CONFIRMED",
     "refuted": false,
     "correctedSeverity": "P1",
     "reasoning": "The 6-week rule exists ONLY as client copy: two commits implemented it as a display gate in renderBylaws and the kit mirrored it, with no payload or migration change. close_season then scores only the two locked finalists over a window covering the whole season. Additional hardening the reviewer missed: set_league_finish raises 'The finish is locked once the final window opens' from day two, so the Pro cannot dial back — a client-only fix is insufficient."
    }
   ]
  }
 },
 {
  "id": "KIT-05",
  "title": "Two total-weeks formulas on one phone — SeasonPhase.of adds a spurious week",
  "file": "apps/ios/Packages/CupSeasonKit/Sources/CupSeasonKit/Models.swift",
  "line": "185-187",
  "severity": "P2",
  "kind": "bug",
  "category": "dates",
  "sessionCode": false,
  "confidence": 9,
  "quote": "let total = max(1, ((CSDate.days(from: s.starts_on, to: s.ends_on) ?? 0) + 1 + 6) / 7)",
  "failureScenario": "Every season this app creates has ends_on = starts_on + 7·N, so for a 26-week league SeasonPhase.of computes 27 while LeagueDates.totalWeeks (and index.html's totalWeeks) computes 26. HomeView renders 'PIGL · week 4 of 27' in the Home eyebrow while the league room's phaseSub renders 'Wk 4 / 26' for the same league on the same day; on the last day it returns week 27 of 27.",
  "fix": "Delete the local arithmetic and call LeagueDates; update the test, which passes today only because its fixture span is not a multiple of 7.",
  "notes": "",
  "slice": "apps/ios/Packages/CupSeasonKit/Sources/",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "KIT-06",
  "title": "insertHoles swallows the error entirely — the hole-by-hole card vanishes silently",
  "file": "apps/ios/Packages/CupSeasonKit/Sources/CupSeasonKit/Post/PostService.swift",
  "line": "91-94",
  "severity": "P2",
  "kind": "bug",
  "category": "silent-failure",
  "sessionCode": false,
  "confidence": 9,
  "quote": "public func insertHoles(_ rows: [PostHoleRow]) async {\n    guard !rows.isEmpty else { return }\n    _ = try? await db.from(\"round_holes\").insert(rows).execute()\n  }",
  "failureScenario": "A golfer posts an 18-hole grid (scan confirm or the opt-in grid). The rounds insert succeeds; the round_holes insert fails on an RLS change, a constraint or a dropped connection. `try?` discards the error, the model ignores the return, and the ceremony plays as a full success. The strokes are gone, the scorecard strip on that receipt is empty forever, and nothing is recorded — no client_events breadcrumb, no toast. index.html handles the same best-effort write by logging round_holes_fail and toasting 'Round posted — hole detail didn't save'. round_holes is one of the four paths named in CLAUDE.md's 2026-08-28 silent-write sweep.",
  "fix": "Return a Bool, emit CSTelemetry on failure, and toast the same sentence the web does.",
  "notes": "",
  "slice": "apps/ios/Packages/CupSeasonKit/Sources/",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "KIT-07",
  "title": "Q-23's no-sign vocabulary never reached the phone — signed PvI and \"vs index\" survive in four producers",
  "file": "apps/ios/Packages/CupSeasonKit/Sources/CupSeasonKit/Post/PostCard.swift",
  "line": "185",
  "severity": "P2",
  "kind": "bug",
  "category": "copy-divergence",
  "sessionCode": false,
  "confidence": 9,
  "quote": "public var vsText: String { (vs >= 0 ? \"+\" : \"\") + RoundCopy.f1(vs) }",
  "failureScenario": "Same round, two clients, two vocabularies. At pvi −3.7 the web now prints '3.7 over' everywhere; the phone prints '-3.7 vs your index' in the composer chip, '-3.7' in the member-history sheet and individual race, '-3.7' on the You/Tour Card avg tiles and '-3.7 — A LITTLE LOOSE' on the receipt. The web also moved the pos/neg colour split to `vs > -1`; the phone still splits at `>= 0`, so a −0.4 round is red on the phone and green on the web.",
  "fix": "Add CSBands.vsShort mirroring the web and route the composer, standings, tiles and receipt through it; move the colour threshold to > -1.",
  "notes": "The divergence was created this session by commit 2ae4d22 (web only).",
  "slice": "apps/ios/Packages/CupSeasonKit/Sources/",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "KIT-08",
  "title": "D122 stopped at the ceremony — the composer still promises league points before first tee",
  "file": "apps/ios/Packages/CupSeasonKit/Sources/CupSeasonKit/Post/PostCard.swift",
  "line": "174-186",
  "severity": "P2",
  "kind": "bug",
  "category": "copy-divergence",
  "sessionCode": true,
  "confidence": 9,
  "quote": "public struct PostPreview: Sendable, Equatable {\n  public let gross: Int\n  public let holes: Int\n  public let vs: Double\n  public let points: Int",
  "failureScenario": "A golfer in a locked league whose first tee is a week away enters 84. PostCalc.preview returns points 9 with no season awareness and the screen renders '9 pts · PIGL'. After posting, the D122 work added this session makes the ceremony say 'PRACTICE · SEASON STARTS SAT SEP 5' — so the app promises 9 league points on one screen and retracts them on the next. index.html closed the loop at the promise, labelling the tile 'This round' and showing an em dash.",
  "fix": "Give PostPreview the season fact (a nil leaguePoints outside the window using PostSeasonRule.counts), label accordingly, and render the note under the chips.",
  "notes": "D122's phone half wired the note into PostCeremony only.",
  "slice": "apps/ios/Packages/CupSeasonKit/Sources/",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "KIT-09",
  "title": "PostSeasonRule.note announces \"the season is over\" (or that it has not started) while it is running",
  "file": "apps/ios/Packages/CupSeasonKit/Sources/CupSeasonKit/Post/PostCard.swift",
  "line": "441-452",
  "severity": "P2",
  "kind": "bug",
  "category": "copy-correctness",
  "sessionCode": true,
  "confidence": 8,
  "quote": "    let st: LeagueCopy.Stage = played < s.starts_on ? .preseason : .complete",
  "failureScenario": "The stage is picked from the ROUND's date but the sentence it selects speaks about the SEASON. Season Sep 5 – Mar 6, today Oct 10, golfer back-dates to Aug 20: the ceremony reads 'PRACTICE — THE SEASON STARTS SAT SEP 5' — future tense about a season that started five weeks ago. A round dated past ends_on reads 'THE SEASON IS OVER' while 20 weeks remain.",
  "fix": "Select the sentence from the league's stage and describe the ROUND separately ('before/after the season window — this round lands on your card').",
  "notes": "",
  "slice": "apps/ios/Packages/CupSeasonKit/Sources/",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "KIT-10",
  "title": "AuthRules.human maps sign-in messages onto every non-auth failure surface",
  "file": "apps/ios/Packages/CupSeasonKit/Sources/CupSeasonKit/AuthRules.swift",
  "line": "45-72",
  "severity": "P2",
  "kind": "bug",
  "category": "error-handling",
  "sessionCode": false,
  "confidence": 7,
  "quote": "if s.contains(\"banned\") || s.contains(\"deleted\") || s.contains(\"403\") {\n      return \"This account was closed and can't sign in again. Start fresh with a different email.\"\n    }",
  "failureScenario": "human() is called from the card load, the league-room load, PushService, ProfileRepository, settings and feedback — none of which is a code screen. A refresh-token expiry during a room load produces text containing 'expired', so the room renders 'That code has expired… use the newest email.' with no code field on screen. Any transport error whose text carries '403' renders 'This account was closed and can't sign in again' to a golfer whose account is fine — a maximally alarming false statement produced by a substring match.",
  "fix": "Split the mapper: keep the auth branches for the sign-in screens and give data surfaces a network-only fallback; at minimum drop the bare 403/deleted/otp substrings.",
  "notes": "That human() is applied to non-auth paths is proven by grep; which exact substring arrives for a given SDK failure could not be confirmed without running the app.",
  "slice": "apps/ios/Packages/CupSeasonKit/Sources/",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "KIT-11",
  "title": "D129's buy-in payment terms have no Swift shape — iOS members see a pot with no way to pay it",
  "file": "apps/ios/Packages/CupSeasonKit/Sources/CupSeasonKit/League/LeagueRoomRows.swift",
  "line": "30-46",
  "severity": "P2",
  "kind": "opportunity",
  "category": "cross-client-drift",
  "sessionCode": true,
  "confidence": 9,
  "quote": "public struct Settings: Decodable, Sendable {\n    public let league_id: UUID\n    public let preset: String?\n    …",
  "failureScenario": "20260830040000 adds buy_in_note / buy_in_due_on and set_buy_in_terms, and the web reads them off CS.settings for the Pot tab. The phone's Settings struct declares neither and the room's select lists explicit columns without them, so no iOS surface can show them. A Pro sets 'Venmo @casey · in by Sep 30' from the desk and every iOS member sees the 60/25/15 split with no instruction — exactly the friction D129 was written to remove. D126's endgameLine has the same shape of gap.",
  "fix": "Add both columns to Settings and the select, render them on the phone's Pot pane with the web's empty state, and add Rpc.set_buy_in_terms behind the Pro's pane. Port endgameLine at the same time.",
  "notes": "",
  "slice": "apps/ios/Packages/CupSeasonKit/Sources/",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "KIT-12",
  "title": "Two conflicting \"JS Math.round\" helpers in one package",
  "file": "apps/ios/Packages/CupSeasonKit/Sources/CupSeasonKit/League/PotMath.swift",
  "line": "10",
  "severity": "P3",
  "kind": "opportunity",
  "category": "duplication",
  "sessionCode": false,
  "confidence": 8,
  "quote": "static func jsRound(_ v: Double) -> Int { Int(v.rounded()) }",
  "failureScenario": "LiveEngines.swift defines the same helper correctly — `Int((v + 0.5).rounded(.down))`, with a comment noting JS rounds halves toward +∞ unlike Swift. They agree on every value the pot currently sees, so nothing is wrong today; the risk is a future negative (a refund, a credit adjustment) rounding one way in the pot pane and the other in the tee sheet, from two functions with the same name and the same stated contract.",
  "fix": "Hoist one CSMath.jsRound (the LiveEngines form) and delete both copies.",
  "notes": "",
  "slice": "apps/ios/Packages/CupSeasonKit/Sources/",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "KIT-13",
  "title": "A nil month_rank counts in the clash and does not count in the standings",
  "file": "apps/ios/Packages/CupSeasonKit/Sources/CupSeasonKit/League/WeekClash.swift",
  "line": "61-63",
  "severity": "P3",
  "kind": "bug",
  "category": "consistency",
  "sessionCode": false,
  "confidence": 7,
  "quote": "    let top = ranked.filter { $0.member_id == member && ($0.month_rank ?? 1) <= capN",
  "failureScenario": "StandingsMath reads the same field with the opposite default (`?? Int.max`). If a v_rounds_ranked row ever arrives with a null month_rank, the same round is a counting round to the clash card and a bumped round to the receipts sheet on the same screen. Today month_rank is a row_number and is never null, so this is latent duplication rather than a live defect.",
  "fix": "Pick one default and state it once — a RankedRound.isCounting(capN:) helper.",
  "notes": "",
  "slice": "apps/ios/Packages/CupSeasonKit/Sources/",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "KIT-14",
  "title": "CSCopy.ordinal traps on a negative argument",
  "file": "apps/ios/Packages/CupSeasonKit/Sources/CupSeasonKit/Models.swift",
  "line": "233-237",
  "severity": "P3",
  "kind": "bug",
  "category": "correctness",
  "sessionCode": false,
  "confidence": 9,
  "quote": "    let suffix = (s >= 11 && s <= 13) ? \"th\" : [\"th\", \"st\", \"nd\", \"rd\"][min(n % 10, 3) == n % 10 ? (n % 10 < 4 ? n % 10 : 0) : 0]",
  "failureScenario": "ordinal(-1): s is −1 so the 11–13 guard misses; n % 10 is −1; the index expression evaluates to −1; the array subscript traps with 'Index out of range' and the app crashes. Every current caller passes a non-negative value, so no live path reaches it — but the expression is unreadable enough that the next caller cannot tell it has a domain restriction.",
  "fix": "Rewrite as a plain switch guarded by abs(n) % 100 (mirroring RyderMath.nthUp, which is already correct), or delete it and call that.",
  "notes": "",
  "slice": "apps/ios/Packages/CupSeasonKit/Sources/",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "KIT-15",
  "title": "Q-27's one floor rule has no Swift producer — the phone states the penalty a fourth way",
  "file": "apps/ios/Packages/CupSeasonKit/Sources/CupSeasonKit/League/LeagueCopy.swift",
  "line": "117,325-335",
  "severity": "P3",
  "kind": "opportunity",
  "category": "cross-client-drift",
  "sessionCode": false,
  "confidence": 8,
  "quote": "BylawRow(\"PARTICIPATION FLOOR\", \"\\(b.floor) / mo · \\(Bylaws.penalty[b.presetIdx])\"),",
  "failureScenario": "index.html added floorSentence() this session as THE single statement of the floor rule, because it was written three different ways: it carves out solo leagues ('there's no squad to dock'), names the automatic season bye, and says short months are waived. The phone has no equivalent, so a SOLO league's bylaws row shows '2 / mo · −5 sqd pts / round short' with no squad in existence, and nextUp never mentions the bye or the waiver. A golfer who reads the rule on the phone and again on the web gets two different rules.",
  "fix": "Port floorSentence into LeagueCopy with the web's four branches and add it to the preflight cross-client copy check.",
  "notes": "",
  "slice": "apps/ios/Packages/CupSeasonKit/Sources/",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "KIT-16",
  "title": "The phone's lock is still four direct RLS writes, though Rpc.lock_league is already generated",
  "file": "apps/ios/Packages/CupSeasonKit/Sources/CupSeasonKit/Wizard/WizardService.swift",
  "line": "109-146",
  "severity": "P2",
  "kind": "opportunity",
  "category": "cross-client-drift",
  "sessionCode": true,
  "confidence": 9,
  "quote": "do {\n      try await db.from(\"league_settings\").update(payload).eq(\"league_id\", value: leagueId).execute()\n    } catch {\n      try await db.from(\"league_settings\").update(payload.withoutFinish).eq(\"league_id\", value: leagueId).execute()\n    }",
  "failureScenario": "D111 built lock_league precisely because the multi-write lock 'stops lying' — it is atomic, it returns already_locked plus the standing phase and season, and it derives the phase from the STORED structure so a mis-sent structure cannot put a league in a phase its squads do not match. Rpc.swift already carries the generated binding. The phone keeps the old path and therefore the old failure modes: a PostgREST update matching zero rows succeeds silently and the lock reports success with locked_at never set, and the client-side phase choice trusts the caller's structure. CLAUDE.md's 'The phone has no direct writes at all' is broken by exactly this function.",
  "fix": "Call Rpc.lock_league and read already_locked / phase / season from the reply, keeping the direct writes only as a skew fallback.",
  "notes": "The RPC and its snapshot entry are session code; the phone was not moved onto it.",
  "slice": "apps/ios/Packages/CupSeasonKit/Sources/",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "IOS-A01",
  "title": "Tee-off crashes with 1 or 3 players: side_a/side_b are built outside the branch that uses them",
  "file": "apps/ios/CupSeason/Live/LiveRoundStore.swift",
  "line": "256-257",
  "severity": "P0",
  "kind": "bug",
  "category": "correctness",
  "sessionCode": false,
  "confidence": 9,
  "quote": "let sideA: JSONValue = .array(s.teams[0].map { .string(players[$0].n) })\n    let sideB: JSONValue = .array(s.teams[1].map { .string(players[$0].n) })",
  "failureScenario": "Open the app, tap ⊕ → 'Play now' → 'Tee off →' without adding anyone. primeRoster has set sel = [0] and fresh() defaults game .score, so teeOffProblem(players: 1) returns nil. defaultTeams(count:1) returns the 2v2 shape [[0,1],[2,3]], so line 256 evaluates players[1] on a 1-element array → 'Fatal error: Index out of range' and the app terminates. Same crash with three players in Skins (teeOffProblem allows 2…4): line 257 evaluates players[3]. These two lines sit above the `switch g` and are evaluated for every game, though only match and sunningdale consume them. index.html builds side_a INSIDE the match branch, so the web never trips; the Swift port hoisted the lines and lost JS's tolerance for a missing index.",
  "fix": "Move the construction into the .match and .sunningdale cases, and make defaultTeams honest for other counts. Add an app-target test calling teeOff() for each game at its legal minimum count — LiveEngineTests only covers teeOffProblem.",
  "notes": "Reachable on the very first use of the tee sheet by a golfer who wants to score only themselves — the D107 'free door' case. Verifier reproduced the fatal error with the file's own defaultTeams body (exit 133) and confirmed the button has no player-count gate.",
  "slice": "apps/ios/CupSeason/ — the SwiftUI app",
  "verification": {
   "status": "survived",
   "correctedSeverity": "P0",
   "votes": [
    {
     "lens": "control-flow",
     "verdict": "CONFIRMED",
     "refuted": false,
     "correctedSeverity": "P0",
     "reasoning": "The tee-off button has no player-count gate, primeRoster leaves sel=[0], fresh() defaults .score, teeOffProblem admits n=1 for .score and n=3 for .skins, and defaultTeams returns the 2v2 shape for every count except 2. Reproduced the exact expression standing alone in swift: exit 133, Index out of range. The web builds side_a inside the match branch and uses .filter(Boolean) elsewhere, so the port lost both protections. D107 makes 'just me, score only' the intended first-use path — which is the crashing one."
    },
    {
     "lens": "intent-and-precedent",
     "verdict": "CONFIRMED",
     "refuted": false,
     "correctedSeverity": "P0",
     "reasoning": "Compiled and ran the repro (SIGTRAP). Every other consumer of the same teams array defends itself with a bounds-checked compactMap; teeOff is the one site that indexes raw, which is evidence against rather than for. git log -S shows the hoist arrived with the port (8d068cf), never as a considered change. LiveEngineTests covers teeOffProblem only, never teeOff."
    }
   ]
  }
 },
 {
  "id": "IOS-A02",
  "title": "The wizard is a full-screen cover whose only exit deletes the league",
  "file": "apps/ios/CupSeason/Main/MainTabView.swift",
  "line": "222-224",
  "severity": "P1",
  "kind": "bug",
  "category": "navigation-dead-end",
  "sessionCode": false,
  "confidence": 8,
  "quote": ".fullScreenCover(item: $presenter.wizard) { t in\n      WizardScreen(existingLeagueId: t.existingLeagueId, links: wizardLinks, initialStep: t.initialStep)\n    }",
  "failureScenario": "A Pro whose league is in setup taps 'Continue' on the Clubhouse setup checklist. Unlike its two neighbours in the same file — presenter.event and presenter.draft, which each wrap in a NavigationStack and add a Close toolbar item — this cover has neither, and fullScreenCover has no interactive dismiss. WizardScreen shows Cancel only at step 0, and Cancel is discard() → deleteLeague. So a Pro who opened the wizard to re-read the bylaws and changed their mind must lock the league or delete it; force-quitting is the only other way out. The D110 addendum bug repeated, with a destructive escape hatch instead of none.",
  "fix": "Wrap it the way the two neighbours are wrapped, with a Close that just clears presenter.wizard; the model already distinguishes discard() from onCancelled(). Keep Cancel-as-discard only for the scaffold case.",
  "notes": "The league-less doors and the run-it-back card DO wrap in a NavigationStack but still add no Close; those paths start on the name sheet, which has its own Cancel, so they are safe.",
  "slice": "apps/ios/CupSeason/",
  "verification": {
   "status": "survived",
   "correctedSeverity": "P1/P2",
   "votes": [
    {
     "lens": "control-flow",
     "verdict": "CONFIRMED",
     "refuted": false,
     "correctedSeverity": "P2",
     "reasoning": "No escape exists: the phase guard in load() is what keeps the Pro inside the wizard, the non-destructive onCancelled lives only on the nameSheet branch (unreachable once leagueId is set), the two neighbouring covers prove the Close pattern was known, and dismissAll() is push-routing only. The web keeps the tab bar visible so the same user can walk away. But the destructive exit is two-tap armed with explicit copy, nothing is lost by relaunching, and the league row survives — a trap with a recoverable escape."
    },
    {
     "lens": "intent-and-precedent",
     "verdict": "CONFIRMED",
     "refuted": false,
     "correctedSeverity": "P1",
     "reasoning": "Precedent is the commit one before the audit baseline — 'fix(ios): the live setup gets a Close', written from an owner field report — so this is a known bug class, half-fixed. WizardLinks already distinguishes onCancelled() from discard(), and the destructive path is documented as the scaffold case (the D5 'My Cup husks' lesson), which does not apply to a Pro re-reading the bylaws. The identical dead end was treated as ship-blocking one commit earlier, and this variant is worse because the offered escape destroys the league."
    }
   ]
  }
 },
 {
  "id": "IOS-A03",
  "title": "A live round is a full-screen cover with no Close — only the setup branch got the D110 addendum fix",
  "file": "apps/ios/CupSeason/Live/LiveRoundHost.swift",
  "line": "26-36",
  "severity": "P1",
  "kind": "bug",
  "category": "navigation-dead-end",
  "sessionCode": false,
  "confidence": 8,
  "quote": "      if store.state.stage == .live, store.state.active {\n        LivePlayView(store: store, links: links)\n      } else {\n        // D110 addendum: setup had NO exit …\n        NavigationStack { LiveSetupView(store: store).toolbar { … Button(\"Close\") { links.done() } … } }\n      }",
  "failureScenario": "Tee off, then want to check the board or Home mid-round. The live branch renders LivePlayView with no NavigationStack and no toolbar inside a fullScreenCover, so there is no Close, no back bar and no swipe-to-dismiss; its .navigationTitle is inert for the same reason. The three ways off the screen are Finish, the two-tap Scrap, and 'Change setup' — which is destructive (IOS-A17). Meanwhile Home carries a 'Continue your round' resume banner whose whole purpose is to let a golfer leave and come back, so the leave half is missing. The web keeps a '← Golf' backlink visible through both stages of #view-play.",
  "fix": "Give the live branch the same NavigationStack + Close, calling links.done() only — Close must NOT touch store.state.active, or the resume banner cannot bring the round back.",
  "notes": "The session's base commit fixed the else branch and left the if branch. Verifier found it is worse for guests and visitors: isPencilOnly hides Change setup AND the whole Finish/Scrap block, so they have ZERO exits.",
  "slice": "apps/ios/CupSeason/",
  "verification": {
   "status": "survived",
   "correctedSeverity": "P1/P2",
   "votes": [
    {
     "lens": "control-flow",
     "verdict": "CONFIRMED",
     "refuted": false,
     "correctedSeverity": "P2",
     "reasoning": "Confirmed: no NavigationStack at the presentation site, no dismiss environment use, no gesture, and leaveRequested is set only by a REMOTE finish/scrap. The state is trivially reachable (relaunch rehydrates a live round; the invite banner puts a non-host member into the cover). The web keeps '← Golf' visible through both stages and leaving does not clear state.live. Nothing crashes and the primary task still completes — three exits exist, they just all end or abandon the round; what is broken is the leave-and-return loop."
    },
    {
     "lens": "intent-and-precedent",
     "verdict": "CONFIRMED",
     "refuted": false,
     "correctedSeverity": "P1",
     "reasoning": "D110's addendum was written from a live owner report and its 'the live stage keeps its own finish/recap exits' is an assumption made while fixing setup, not a decided rule. D86 built the doorbell and the resume banner precisely so a player can be away and come back. Understated for guests and visitors: isPencilOnly hides Change setup AND the entire Finish/Scrap block, so they have ZERO exits — the exact trap reported for setup."
    }
   ]
  }
 },
 {
  "id": "IOS-A17",
  "title": "\"Change setup\" on a live round silently discards every score and orphans the server round",
  "file": "apps/ios/CupSeason/Live/LiveRoundStore.swift",
  "line": "291-294",
  "severity": "P1",
  "kind": "bug",
  "category": "data-loss",
  "sessionCode": false,
  "confidence": 8,
  "quote": "  func backToSetup() {\n    state.stage = .setup; state.active = false\n    Task { await session.leave() }\n  }",
  "failureScenario": "Nine holes into a Skins round the host taps 'Change setup' — a footnote-sized link and the only non-destructive-looking exit — to fix the stake. Setting active=false while keeping state.lr breaks four things at once: the Home resume banner requires active, persist() is guarded on active, foregrounded()→joinSync() is guarded on active, and rehydrate() runs once per process. The host is locked out of a round still live on the server and still being scored by the group. If they then tap 'Tee off →' again, teeOff() nulls s.lr and rebuilds s.scores — the nine holes gone — and starts a SECOND live_rounds row, leaving the first live forever.",
  "fix": "Make backToSetup non-destructive once state.lr is set (refuse, or route through the scrap confirmation), and have teeOff() refuse to start a second round while state.lr != nil.",
  "notes": "Compounds IOS-A03: with no Close on the live cover, this destructive link is the exit a golfer tries first. Verifiers add that the scores are not annihilated — each stroke landed server-side and the disk snapshot survives, and handleLiveOpen can recover in-process — so the damage is a split group, an empty second round, and an orphan only the starter could settle. daily_season_tick sweeps live rounds to 'abandoned' after 24h.",
  "slice": "apps/ios/CupSeason/",
  "verification": {
   "status": "survived",
   "correctedSeverity": "P1",
   "votes": [
    {
     "lens": "control-flow",
     "verdict": "CONFIRMED",
     "refuted": false,
     "correctedSeverity": "P1",
     "reasoning": "Reachable for the host at any hole with no confirmation. Two corrections narrowing it: 'orphaned in live forever' is false — daily_season_tick sweeps live rounds older than 24h to abandoned — and nothing is deleted at the tap, since handleLiveOpen falls through to rehydrate when active is false and the disk snapshot survives. The actual loss requires the second, deliberate 'Tee off →' tap, which is one tap away on the screen the link lands you on. teeOff has no state.lr guard and start_live_round has no existing-round check, so a duplicate row is minted."
    },
    {
     "lens": "intent-and-precedent",
     "verdict": "CONFIRMED",
     "refuted": false,
     "correctedSeverity": "P1",
     "reasoning": "Not guarded and not deliberate: D110's addendum gave setup a Close on the premise 'before tee-off there is nothing to abandon', which backToSetup falsifies, and no decision sanctions re-configuring a round already teed off. Extra consequence: session.leave() nulls the session's lr, so unflushed queued writes stall for the rest of the process. finish/abandon gate on starter_profile_id, so the group cannot close the orphan."
    }
   ]
  }
 },
 {
  "id": "IOS-A04",
  "title": "\"Start over\" nulls card.date but not the date picker, so the round lands on the server's UTC current_date",
  "file": "apps/ios/CupSeason/Post/PostRoundModel.swift",
  "line": "128",
  "severity": "P1",
  "kind": "bug",
  "category": "dates-timezones",
  "sessionCode": false,
  "confidence": 8,
  "quote": "  func startOver() { card.startOver(); setPhoto(nil); clearDraft(); toast.show(\"Card cleared\") }",
  "failureScenario": "At 18:00 in Tempe: set the date, type something, tap 'Start over'. PostCard.startOver sets date = nil but model.day — the Date the picker and the details pill are bound to, and the ONLY writer of card.date — is untouched, so the pill still shows the old date. Post: PostPayload sends played_on nil, and PostPayload's Encodable OMITS nil optionals by design ('which is what the server defaults need'), so PostgREST applies rounds.played_on's default current_date, evaluated in UTC — the next day. The round is filed a day later than the pill says and a day later than the ceremony. Across a month boundary it also lands in the wrong month's cap and floor.",
  "fix": "`day = Date()` inside startOver so the didSet re-populates card.date — exactly the web's Q-21 fix, which landed this session and skipped the phone.",
  "notes": "Verifiers: prod TimeZone is UTC and current_date was confirmed a day ahead of Phoenix at audit time. The drift is not capped at one day — if a past date was picked before Start over, the pill keeps showing it while the round files under today's UTC date.",
  "slice": "apps/ios/CupSeason/",
  "verification": {
   "status": "survived",
   "correctedSeverity": "P1/P2",
   "votes": [
    {
     "lens": "control-flow",
     "verdict": "CONFIRMED",
     "refuted": false,
     "correctedSeverity": "P2",
     "reasoning": "day's didSet is the only writer of card.date outside init/scan/restore, and startOver mutates the card only; no downstream guard re-fills it and submit gates on preview != nil. Verified against the linked prod DB that TimeZone is UTC and current_date was a day ahead of Phoenix at audit time, and that played_on's default is CURRENT_DATE. Correction: restoreDraft is unreachable today for an unrelated reason (init sets card.date, so card.isBlank is false and drafts never restore) — a separate defect. Recoverable by delete_round + repost, and it needs both Start over and the evening window."
    },
    {
     "lens": "intent-and-precedent",
     "verdict": "CONFIRMED",
     "refuted": false,
     "correctedSeverity": "P1",
     "reasoning": "The remediation plan prescribes this exact iOS fix (Q-21/T4-05, citing PostRoundModel.swift:128 and PostCard.swift:112 and spec §9) and it was never applied; git shows day = Date() only on the post-submit reset. Two corrections in the finding's favour: the iOS failure is SILENT where the web's was loud (the phone omits the key and takes the server default rather than tripping NOT NULL, so the missing played_on error mapping never even fires), and the drift is not capped at one day — a previously picked past date keeps showing in the pill while the round files under today's UTC date."
    }
   ]
  }
 },
 {
  "id": "IOS-A06",
  "title": "Home and the Clubhouse resolve \"your league\" differently — a wrapped league splits the shell in two",
  "file": "apps/ios/CupSeason/Clubhouse/ClubhouseView.swift",
  "line": "23",
  "severity": "P2",
  "kind": "bug",
  "category": "correctness",
  "sessionCode": false,
  "confidence": 7,
  "quote": "    if let me = store.me, !me.memberships.isEmpty,\n       let current = me.memberships.first(where: { $0.league_id == (leagueId ?? store.preferredLeague) }) ?? me.memberships.first {",
  "failureScenario": "A golfer in PIGL 2025 (complete) and PIGL 2026 (live) whose cs_last_league points at 2025. HomeMode.of filters wrapped memberships out BEFORE honouring preferredLeague, so Home's hero and UpNextChips are bound to 2026 while ClubhouseView does no such filtering and, with YouScreen, shows 2025's 'Season complete' room. Two tabs give two answers, and 'THE BOARD ↗' on Home pushes 2026's board while the Clubhouse tab's board is 2025's.",
  "fix": "One producer: a currentMembership computed property applying HomeMode.of's active-else-all rule once, read by HomeView, ClubhouseView, YouScreen and HomeModel.",
  "notes": "Could not confirm the server's ordering of memberships, so the 'reload seeds the wrapped one' half is uncertain; the switcher path is certain.",
  "slice": "apps/ios/CupSeason/",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "IOS-A05",
  "title": "Five call sites dismiss one presentation and raise another in the same transaction; a dropped one leaves presenter state stuck true",
  "file": "apps/ios/CupSeason/Post/PostCoverView.swift",
  "line": "95-98",
  "severity": "P2",
  "kind": "bug",
  "category": "race",
  "sessionCode": false,
  "confidence": 6,
  "quote": "                            sub: \"Hole-by-hole on every phone · …\") {\n              close(); links.openLive()\n            }",
  "failureScenario": "close() sets showPost=false and openLive() sets showLive=true in one SwiftUI update, both driving fullScreenCovers on the same MainTabView. The codebase already treats this as unsafe — apply(_:) sleeps 450ms after dismissAll, and PostRoundScreen comments that 'a sheet presented mid-dismissal is dropped'. If the tee sheet is dropped, showLive stays true, which is worse than a no-op: presenter.anythingUp is then permanently true so the push ask never re-raises, and the resume banner's tap becomes a no-op because the value never changes. Same shape at four other sites.",
  "fix": "One hand-off helper encoding the rule apply(_:) already implements — set the outgoing state, await ~450ms, then set the incoming — routed through by all five.",
  "notes": "Could not run a simulator, so the drop rate under SwiftUI 17/18 is unknown; the evidence it happens here is the codebase's own two mitigations and the explicit comment.",
  "slice": "apps/ios/CupSeason/",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "IOS-A07",
  "title": "D120's shared stage vocabulary was applied to three of the hero's five cases and skipped for cupFinal and wrapped",
  "file": "apps/ios/CupSeason/Home/HomeView.swift",
  "line": "534-535",
  "severity": "P3",
  "kind": "bug",
  "category": "copy-contradiction",
  "sessionCode": true,
  "confidence": 8,
  "quote": "    case .cupFinal(let m): return \"\\(m.name) · cup final\"\n    case .wrapped(let m): return \"\\(m.name) · season wrapped\"",
  "failureScenario": "Home's hero eyebrow reads 'PIGL · season wrapped'; one tap into the Clubhouse, LeagueCopy.phaseHeader returns 'Season complete'. Same for the final: 'cup final' versus 'Cup Final'. The commit that produced these lines converted the three cases immediately above to Stage.label with a comment about 'the same league described five different ways in a single session' and left the two below hand-written. Preflight check 20 cannot catch this — it diffs the two label TABLES, never the call sites that bypass them.",
  "fix": "Use Stage.final.label and Stage.complete.label; consider extending check 20 to fail on a hero-side literal duplicating a Stage label.",
  "notes": "Also dead in the same function: the `return m.name` fallback is unreachable, because reaching case .season means SeasonPhase.of already returned .season.",
  "slice": "apps/ios/CupSeason/",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "IOS-A08",
  "title": "The \"How points work\" tables promise 7 points at exactly one over, contradicting the Q-20 half-open band rule fixed this session",
  "file": "apps/ios/CupSeason/Post/PostRoundScreen.swift",
  "line": "313",
  "severity": "P3",
  "kind": "bug",
  "category": "scoring-copy",
  "sessionCode": false,
  "confidence": 8,
  "quote": "  private static let bands = [(\"Beat your index by 3+\", \"12\"), (\"Beat it by 1–3\", \"9\"), (\"Within a stroke either way\", \"7\"), (\"Over by 1–3\", \"6\"), (\"Rough day, posted anyway\", \"5\")]",
  "failureScenario": "A round exactly 1.0 over is awarded 6 by the engine and by every computed surface since Q-20, but the composer's fold-out table says 'Within a stroke either way → 7' while the row beneath says 'Over by 1–3 → 6' — the same round, two answers, on the same screen. The scoring-help sheet reached from the league welcome repeats it. Q-20's commit claims 'All three now read half-open on both clients'; these hand-written tables are the fourth and fifth surfaces.",
  "fix": "Reword to the half-open rule, or generate both tables from CSBands.",
  "notes": "index.html:3217 carries the same words (IDX02-04), so both clients need the same edit.",
  "slice": "apps/ios/CupSeason/",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "IOS-A09",
  "title": "The delete-account button's label fails WCAG AA contrast in both themes",
  "file": "apps/ios/CupSeason/Settings/CardAndSettingsScreen.swift",
  "line": "385-389",
  "severity": "P3",
  "kind": "bug",
  "category": "accessibility",
  "sessionCode": false,
  "confidence": 7,
  "quote": "            Text(vm.deleting ? \"Deleting…\" : \"Delete permanently\").font(CSFont.button)\n              .foregroundStyle(cs.ink)\n              .background(cs.neg, in: RoundedRectangle(…))",
  "failureScenario": "cs.ink on cs.neg measures 2.66:1 in dark and 3.25:1 in light. CSFont.button is 17pt semibold, which is not WCAG 'large text', so the requirement is 4.5:1 and both themes fail — dark by a wide margin. A user with low vision reading this in sunlight is being asked to confirm the one irreversible action in the app.",
  "fix": "Paint the label in a token that clears 4.5:1 on cs.neg, or invert to neg text on bg2 with a neg border — the treatment the armed scrap button already uses.",
  "notes": "Contrast computed from the generated token hexes, not measured on a device.",
  "slice": "apps/ios/CupSeason/",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "IOS-A10",
  "title": "The handicap-index range is stated two different ways inside the same app",
  "file": "apps/ios/CupSeason/Onboarding/CardGateView.swift",
  "line": "221",
  "severity": "P3",
  "kind": "bug",
  "category": "copy-contradiction",
  "sessionCode": false,
  "confidence": 8,
  "quote": "    if let idx, !(-10...54).contains(idx) { note = (\"An index runs from +10 to 54.\", .neg); return }",
  "failureScenario": "The card gate says 'An index runs from +10 to 54.'; Card & settings says 'Index looks off: expected -10 to 54' for the same validation. Two notations for one bound, and the settings one leaks the internal sign convention: in golf a plus handicap is written +2.4, and both fields accept '+' and substitute it for '-'. A golfer reading '-10' reasonably types a minus sign, which the parser accepts and stores as a scratch-plus index they did not mean.",
  "fix": "Use one sentence in both places, in golf notation.",
  "notes": "index.html carries the '-10 to 54' form at two sites, so parity means fixing all four — but the phone contradicting itself is the defect worth closing first.",
  "slice": "apps/ios/CupSeason/",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "IOS-A11",
  "title": "DoorFlags' header documents a limitation the code no longer has",
  "file": "apps/ios/CupSeason/Door/DoorFlags.swift",
  "line": "9-13",
  "severity": "P3",
  "kind": "opportunity",
  "category": "dead-or-contradictory-code",
  "sessionCode": false,
  "confidence": 9,
  "quote": "// Known gap, on the record: `flags_read` is granted to `authenticated` only … Until an anon-callable read exists … this decodes to closed in prod.",
  "failureScenario": "Not a runtime failure — a reader failure. The comment says the Apple button cannot appear in production, but line 44 of the same file calls Rpc.door_flags(), and migration 20260827190000 grants execute on that function to anon — exactly the SECURITY DEFINER RPC the comment says does not exist yet. The next person debugging 'why is Sign in with Apple not showing' reads this header, concludes the path is known-broken, and stops looking at the actual cause.",
  "fix": "Replace the paragraph with what is now true: door_flags() is the anon-callable read, the table read is a signed-in-only fallback, and every failure still decodes to closed.",
  "notes": "",
  "slice": "apps/ios/CupSeason/",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "IOS-A12",
  "title": "A slow push registration freezes the three unrelated notification toggles",
  "file": "apps/ios/CupSeason/Settings/CardAndSettingsScreen.swift",
  "line": "436",
  "severity": "P3",
  "kind": "bug",
  "category": "correctness",
  "sessionCode": false,
  "confidence": 7,
  "quote": "    .disabled(push.busy)",
  "failureScenario": "PushService.enable() sets push.busy while it awaits the system prompt and APNs registration, which can hang for seconds or indefinitely if the user leaves the alert unanswered. Every pill in the row goes through the same helper, so 'Round pings', 'Chat pings' and 'Season email' — pure profile-column RPCs with nothing to do with APNs — are disabled too. A golfer who opened Settings to mute chat cannot.",
  "fix": "Move .disabled(push.busy) onto the enable/disable pill alone.",
  "notes": "",
  "slice": "apps/ios/CupSeason/",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "IOS-A13",
  "title": "UpNextChips takes a links parameter it never uses and reloads on a key that misses the changes it displays",
  "file": "apps/ios/CupSeason/Schedule/UpNextChips.swift",
  "line": "14-17",
  "severity": "P3",
  "kind": "opportunity",
  "category": "dead-or-contradictory-code",
  "sessionCode": false,
  "confidence": 7,
  "quote": "  let leagueId: UUID?\n  let links: CSLinks",
  "failureScenario": "Two problems in one small view. links is stored and never referenced — every chip is a plain HStack with no Button, so a chip reading 'Invites · 2' is a dead end even though CSLinks was threaded down for exactly that. And the reload key is the membership COUNT, so accepting a buddy request or an invite leaves the count unchanged and vm.load() never re-runs — the chip keeps showing the stale number until relaunch.",
  "fix": "Wire the chips to links (or drop the parameter) and key the task on store.me?.generated_at, the id HomeView already uses.",
  "notes": "Confirmed by grep: links appears only in the declaration and the init.",
  "slice": "apps/ios/CupSeason/",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "IOS-A14",
  "title": "Two fields on the door carry two accessibilityLabels each",
  "file": "apps/ios/CupSeason/Door/DoorView.swift",
  "line": "120-127",
  "severity": "P3",
  "kind": "bug",
  "category": "accessibility",
  "sessionCode": false,
  "confidence": 8,
  "quote": "        .accessibilityLabel(\"The \\(AuthRules.otpLength) digit code\")\n        …\n        .accessibilityLabel(\"The \\(AuthRules.otpLength) digits\")",
  "failureScenario": "The OTP field declares two different labels and the reviewer password field does the same. Only the outer survives, so half of each pair is dead code — and a reader editing the wrong one changes nothing and concludes VoiceOver is broken. On the one screen every user must pass, that is the worst place to leave a modifier that silently does nothing.",
  "fix": "Keep one label per field.",
  "notes": "Verified both labels are on the same view chain, not on sibling views.",
  "slice": "apps/ios/CupSeason/",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "IOS-A15",
  "title": "The tee sheet's people picker reports \"no buddies\" when the buddy read failed",
  "file": "apps/ios/CupSeason/Live/LiveSetupView.swift",
  "line": "597-602",
  "severity": "P3",
  "kind": "bug",
  "category": "silent-failure",
  "sessionCode": false,
  "confidence": 7,
  "quote": "  private func buddies() async {\n    if let l = try? await SupabaseService.shared.call(Rpc.my_friends()) { … }\n    loaded = true\n  }",
  "failureScenario": "my_friends() throws on a flaky network, `try?` swallows it, rows stays empty and loaded is set regardless — so the sheet renders 'Type a name or @handle to search — buddies you add appear here.' A golfer with twelve buddies is told, in the app's own voice, that they have none, on the screen where they are building a foursome. The search path has the same swallow, so an outage renders 'No golfers found' — inventing a fact about the database from a transport error. The course search next door distinguishes these states correctly.",
  "fix": "Keep the error and render a third state.",
  "notes": "Same class, lower stakes, in PeopleScreen and PeoplePickerSheet.",
  "slice": "apps/ios/CupSeason/",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "IOS-A16",
  "title": "SessionStore.reload() drops a concurrent refresh silently, so a lock or a join can leave a stale screen",
  "file": "apps/ios/Packages/CupSeasonKit/Sources/CupSeasonKit/SessionStore.swift",
  "line": "83",
  "severity": "P2",
  "kind": "bug",
  "category": "race",
  "sessionCode": false,
  "confidence": 7,
  "quote": "    guard !loading else { return }",
  "failureScenario": "Every consequential flow ends with an unawaited `Task { await store.reload() }` — the wizard's onLocked, onJoined, the draft's onSeasonStarted, the join sheet, InvitesBanner's accept, HomeView's refreshable, PostRoundModel's trailing reload. If any two overlap, the second hits the guard and returns having done nothing and reported nothing. The user sees the pre-lock or pre-join state until some unrelated event triggers another reload, and the failure is indistinguishable from a slow server.",
  "fix": "Coalesce rather than drop: keep a pending flag and run once more when the in-flight load finishes, or hold the Task and let late callers await it.",
  "notes": "In the packages slice, but every dropped refresh visible in the app originates here.",
  "slice": "apps/ios/CupSeason/",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "SD-01",
  "title": "covenantGate's promise never resolves when the sheet is closed by X / backdrop / Escape — Q-14 put it in boot(), so the app wedges",
  "file": "index.html",
  "line": "16027-16044, 18173-18186",
  "severity": "P0",
  "kind": "bug",
  "category": "correctness",
  "sessionCode": true,
  "confidence": 9,
  "quote": "return new Promise(res=>{ openSheet(…); document.getElementById('covJoin')?.addEventListener('click',()=>res(true)); document.getElementById('covNo')?.addEventListener('click',()=>{ closeSheet(); res(false); }); });",
  "failureScenario": "See B-01: the same defect, filed from the session-diff slice. Q-14 moved the gate into boot(), so dismissing the covenant sheet by ✕, backdrop or Escape leaves boot() awaiting forever — bootStep stuck at 'memberships', the watchdog printing 'Boot stalled', boot's finally never running, and safeBoot's `booting` flag never clearing so no later auth event can re-boot. 16 of 18 prod leagues carry a buy-in, so the gate fires on essentially every invite.",
  "fix": "Resolve on any sheet close; no await in boot() may depend on a button the user can route around.",
  "notes": "Duplicate of B-01. Severity corrected to P1 by both verifiers — recoverable by reload, and 'Not now' always escapes.",
  "slice": "git diff 34d20b6..HEAD — the 2026-08-29 session",
  "verification": {
   "status": "survived",
   "correctedSeverity": "P1",
   "votes": [
    {
     "lens": "control-flow",
     "verdict": "CONFIRMED",
     "refuted": false,
     "correctedSeverity": "P1",
     "reasoning": "Same as B-01, traced independently: no resolve hook, no observer, no modal flag; the sheet paints over the door and its ✕ is tappable; the hang leaves booting true so even a forced SIGNED_IN safeBoot is refused. Escapable only by reload."
    },
    {
     "lens": "intent-and-precedent",
     "verdict": "CONFIRMED",
     "refuted": false,
     "correctedSeverity": "P1",
     "reasoning": "D116 rules only on what a decline must do; git log -S shows the boot() call site added in this session's f96d745, so the dismissal case was neither known nor accepted. Prod: 16 of 18 leagues carry a buy-in, so the gate fires on essentially every invite. The resumeAfterProfile call site is a second wedge for the brand-new invited signup."
    }
   ]
  }
 },
 {
  "id": "SD-02",
  "title": "Formation draw guard compares the unassigned pool to the squad count; one late joiner in a 2-squad league blocks both Draw and Start",
  "file": "index.html",
  "line": "15373-15381",
  "severity": "P1",
  "kind": "bug",
  "category": "correctness",
  "sessionCode": true,
  "confidence": 9,
  "quote": "${isC && pool.length && state.draftType !== 'assign'\n      ? (pool.length < (CS.squads||[]).length\n          ? `…<button class=\"mini\" id=\"fmDraw\" disabled>Draw squads</button>…${pool.length} in · need ${(CS.squads||[]).length}…`\n          : '<button class=\"mini\" id=\"fmDraw\">Draw squads</button>') : ''}",
  "failureScenario": "squads2, 4 golfers, the Pro runs the draw — pool 0, 'Start the season' shows. A fifth golfer joins by code before Start. Now pool 1 < squads 2, so Draw renders DISABLED with 'GOLFER 1 in · need 2 to cover the squads', and `!pool.length` is false so 'Start the season' vanishes at the same instant. The Pro can neither deal the new joiner nor start the season — from a UI the server would have accepted: randomize_squads refuses on total < sq_n where total is every league_member (5), and would deal the joiner into the smallest squad. The message is numerically false too.",
  "fix": "Compare against the roster, matching randomize_squads' own rule.",
  "notes": "Three prod leagues are in phase='draft' today. Verifier correction: keep the `!pool.length` gate on Start — start_season correctly raises while any member is loose; only the Draw gate is wrong. The remediation plan's own Q-08 row specified `CS.members.length < CS.squads.length`; the implementation shipped `pool.length`, and the pre-commit code offered Draw unconditionally, so this is strictly worse than what it replaced.",
  "slice": "git diff 34d20b6..HEAD",
  "verification": {
   "status": "survived",
   "correctedSeverity": "P1",
   "votes": [
    {
     "lens": "control-flow",
     "verdict": "CONFIRMED",
     "refuted": false,
     "correctedSeverity": "P1",
     "reasoning": "The first draw is fine (pool == total); the divergence appears only on the redraw path, which the server was rewritten to support (deal into the smallest squad). No rescue: assign taps are unbound in random mode, start_season has one call site, draft_type is not editable after lock, and join_league has no phase guard so a late join is normal. Correction: hiding Start is CORRECT — start_season raises while any member is loose — so only the Draw gate should change."
    },
    {
     "lens": "intent-and-precedent",
     "verdict": "CONFIRMED",
     "refuted": false,
     "correctedSeverity": "P1",
     "reasoning": "The remediation plan's own Q-08 row specifies `CS.members.length < CS.squads.length`; the implementation shipped pool.length — a slip against the spec of the very fix. The pre-commit code offered Draw unconditionally and the server would have dealt the joiner, so Q-08 turned a one-tap recovery into a dead end. The phone's DraftCopy.startBlocker mirrors the server correctly, which is the pattern the web guard should have followed."
    }
   ]
  }
 },
 {
  "id": "SD-03",
  "title": "Q-23's vsShort landed on the demo standings renderer only; the real table and career tile still print signed numbers under the new 'vs your number' headers",
  "file": "index.html",
  "line": "11882, 11898, 11905, 11786",
  "severity": "P1",
  "kind": "bug",
  "category": "correctness",
  "sessionCode": true,
  "confidence": 9,
  "quote": "  $('#msAvg').textContent = me && me.r ? sgn(me.avg) : '—';\n…\n      <td class=\"num dw\" style=\"color:${p.avg>=0?'var(--pos)':'var(--neg)'}\">${p.r?sgn(p.avg):'—'}</td>",
  "failureScenario": "renderIndStats returns into renderIndStatsReal for every non-demo session, so the vsShort conversions are on the branch a real league never reaches. The session changed the column HEADER to 'Avg vs your number' and the two markup labels, and left the cells as sgn(). A golfer reads a column headed 'Avg vs your number' whose values are '-3.7', taps the round and the receipt says '3.7 over', opens the composer and it says '3.7 over' — the exact three-screens-three-vocabularies defect Q-23 says it removed, now with a header that contradicts its own cells. The colour rules diverged too.",
  "fix": "Use vsShort in renderIndStatsReal and the career tile, with the demo's `avg > -1` colour threshold.",
  "notes": "Duplicate of IDX10-03. The remediation plan assigned the REAL renderer and the career tile to Q-23 phase 1 and explicitly deferred the DEMO table to phase 2; the commit did the inverse and the status ledger recorded it as 'done · standings ×2'. openMemberHist, also a phase-1 target, is byte-identical to the base commit.",
  "slice": "git diff 34d20b6..HEAD",
  "verification": {
   "status": "survived",
   "correctedSeverity": "P1/P2",
   "votes": [
    {
     "lens": "control-flow",
     "verdict": "CONFIRMED",
     "refuted": false,
     "correctedSeverity": "P2",
     "reasoning": "renderIndStatsReal is the branch every real golfer lands on, and renderCareer's sign() runs on that path unconditionally. The pre-session line numbers named in the remediation plan resolve to exactly these sites. Strictly worse than the pre-session state, where header and cell agreed. But the arithmetic is correct and nothing about points, money or eligibility is wrong — a confused reader, not a misinformed one."
    },
    {
     "lens": "intent-and-precedent",
     "verdict": "CONFIRMED",
     "refuted": false,
     "correctedSeverity": "P1",
     "reasoning": "The plan assigned the REAL renderer and the career tile to Q-23 phase 1 and explicitly deferred the DEMO table to phase 2 (C-11); the commit did the exact inverse and the status ledger then recorded it 'done · standings ×2'. openMemberHist, also a phase-1 target, is byte-identical to the base commit. The false 'done' is what would keep it unfixed, and no preflight check covers it (Q-25 is unbuilt)."
    }
   ]
  }
 },
 {
  "id": "SD-04",
  "title": "The dismissed install nudge reappears on the next navigation — dismiss() never resets `shown` and csHideInstallNudge re-shows on it",
  "file": "index.html",
  "line": "13396-13400",
  "severity": "P1",
  "kind": "bug",
  "category": "correctness",
  "sessionCode": true,
  "confidence": 9,
  "quote": "    else if(shown) n.style.display = 'flex';\n  };\n  function dismiss(){ try{ localStorage.setItem('cs_nudge_done','1'); }catch(_){}; if(n) n.style.display = 'none'; }",
  "failureScenario": "See IDX11-02. `shown` carries two meanings — show() sets it as 'has ever been shown', csHideInstallNudge reads it as 'should be visible now' — and after dismiss() they diverge for the session, so the restore branch that exists to bring the banner back when leaving a full-screen surface also resurrects a banner the golfer explicitly killed, on every route change.",
  "fix": "Set shown=false in dismiss(), and gate the re-show on eligible().",
  "notes": "Duplicate of IDX11-02. The IIFE's own header states the contract it violates ('Shows once, ever; no-ops when … already dismissed') and D23 says each nudge fires once. Severity corrected to P2 — session-scoped, self-heals on reload.",
  "slice": "git diff 34d20b6..HEAD",
  "verification": {
   "status": "survived",
   "correctedSeverity": "P2",
   "votes": [
    {
     "lens": "control-flow",
     "verdict": "CONFIRMED",
     "refuted": false,
     "correctedSeverity": "P2",
     "reasoning": "Reproduced in an isolated simulation of the block's verbatim logic: after earn flex, after ✕ none with cs_nudge_done set, after a tab tap flex, and flex again. `shown` has one write site and eligible() is consulted only inside show(). Session-scoped and self-healing on reload; no data, scoring or auth consequence."
    },
    {
     "lens": "intent-and-precedent",
     "verdict": "CONFIRMED",
     "refuted": false,
     "correctedSeverity": "P2",
     "reasoning": "Q-13's commit message and its remediation-plan row specify only hiding on the four full-screen views, and the IIFE's own header states the opposite contract. git log -S shows the line born in 1f4eb7d — never reverted, never accepted. Q-13 did move it below the tab bar in the stack, so it no longer intercepts the ⊕."
    }
   ]
  }
 },
 {
  "id": "SD-05",
  "title": "The D119 hero's preseason row never renders: `starter` short-circuits the body copy and, via + binding tighter than :, the CTA and sub-CTA",
  "file": "index.html",
  "line": "10465, 10521-10546",
  "severity": "P1",
  "kind": "bug",
  "category": "correctness",
  "sessionCode": true,
  "confidence": 9,
  "quote": "    const starter = state.phase==='season';\n…\n      (starter || !nextStep) ? '' : `<button class=\"hh-cta\" data-hform type=\"button\">${nextStep.label}</button>`\n      + (subStep ? `<button class=\"hh-sub\" data-hsub type=\"button\">${subStep.label}</button>` : ''));",
  "failureScenario": "See idx09-01 / idx09-02, verified together: block 3 returns unconditionally when inSeason && !atStarter(), so block 4 sees phase==='season' only when atStarter() is true — which is exactly what leagueStage() calls 'preseason'. The card therefore renders 'before first tee' over 'The season's on. Rounds count from today.', and the whole CTA argument collapses to ''. Two of six matrix cells never draw, four copy branches are unreachable, and the qaEvent still reports a cta label for a button that was never in the DOM.",
  "fix": "Delete `starter` and branch on `stage`, and parenthesise the CTA+sub concatenation.",
  "notes": "The iOS commit dc0ae12 names this exact contradiction, attributes it to the web, and fixes only the phone. Verified by executing the expression: with starter true the slot is \"\".",
  "slice": "git diff 34d20b6..HEAD",
  "verification": {
   "status": "survived",
   "correctedSeverity": "P1",
   "votes": [
    {
     "lens": "control-flow",
     "verdict": "CONFIRMED",
     "refuted": false,
     "correctedSeverity": "P1",
     "reasoning": "Block 3 returns unconditionally on inSeason && !atStarter(), so `starter` and stage==='preseason' are the same predicate under two names, used as if they were opposites. Confirmed the precedence by executing the expression. Reachable in prod: lock_league sets phase='season' at lock for solo and squads reach it after the draw, both with starts_on ahead. D122's seasonState classifies the same stage as 'pre', so Home and the composer actively disagree."
    },
    {
     "lens": "intent-and-precedent",
     "verdict": "CONFIRMED",
     "refuted": false,
     "correctedSeverity": "P1",
     "reasoning": "D119/D136 specify these CTAs, the remediation plan's C-01 instructs deleting 'Rounds count from today', and gameplay-loop.md:88 diagnoses the starter-vs-atStarter split by name. D119 replaced the old (wrong-but-consistent) eyebrow with the stage word and left the body, making the contradiction visible without fixing it; dc0ae12 then fixed the identical bug on the phone and named the web as where it was logged."
    }
   ]
  }
 },
 {
  "id": "SD-06",
  "title": "endgameLine claims 'The leader carries +10 in' for every Cup Final league; enter_cup_final grants the head start only to squads2",
  "file": "index.html",
  "line": "5920-5936",
  "severity": "P2",
  "kind": "bug",
  "category": "money-scoring-correctness",
  "sessionCode": true,
  "confidence": 9,
  "quote": "  return `${who} seed into a four-week Cup Final${when ? ` from ${when}` : ''} — … The leader carries +10 in. …`;",
  "failureScenario": "Duplicate of IDX04-01 / idx05-02, seen from the diff: the one sentence built to tell a golfer how the title is decided states a 10-point advantage that will never be applied, for every structure except squads2.",
  "fix": "Make the head-start clause conditional on structure, or read it from cup_final_race's meta.",
  "notes": "",
  "slice": "git diff 34d20b6..HEAD",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "SD-07",
  "title": "On squads2 — the default structure — the seat line and the new endgame line contradict each other",
  "file": "index.html",
  "line": "4570-4574",
  "severity": "P2",
  "kind": "bug",
  "category": "correctness",
  "sessionCode": true,
  "confidence": 8,
  "quote": "    note.innerHTML = `${esc(seatLine)}<span class=\"endgame-line\">${esc(endgameLine(meta||{}))}</span>`",
  "failureScenario": "Duplicate of idx03-02 / IDX04-03 from the diff side: the session made the first half louder ('ADVANCE' → 'ADVANCE TO THE CUP FINAL') and added the contradicting second half beside it, so a squads2 league reads 'TOP 1 ADVANCE TO THE CUP FINAL' above 'The top 2 squads seed into a four-week Cup Final', with the cut separator saying a third thing.",
  "fix": "Recognise the squads2 shape and say 'BOTH SQUADS ADVANCE · TOP SEED CARRIES +10', or suppress endgameLine when the seat line already states the rule.",
  "notes": "endgameLine's `seats` is always 2 — right for every current structure by accident, so nothing caught the shape.",
  "slice": "git diff 34d20b6..HEAD",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "SD-08",
  "title": "Q-28 fixed the pot tiles but not #lineSplit, the live 'On the line' bar — the two surfaces now disagree on the champion's payout by a dollar",
  "file": "index.html",
  "line": "12590 (vs 7498-7500)",
  "severity": "P2",
  "kind": "bug",
  "category": "money-scoring-correctness",
  "sessionCode": true,
  "confidence": 9,
  "quote": "      $('#lineSplit').textContent=`CHAMPS ${fmt$(Math.round(total*pc/100))} · RUNNER-UP ${fmt$(Math.round(total*pr/100))} · POINTS KING ${fmt$(Math.round(total*pk/100))}`",
  "failureScenario": "5 members, $50, 60/25/15 → total 250. The Pot tab now prints champion 250 − 63 − 38 = $149; the gold 'On the line' bar in the same League Room still prints round(250×0.6) = $150. Same league, same pot, two screens, $150 vs $149. Before the session both said $150 and merely summed to $251; now the app contradicts itself, which is worse on the surface whose whole job is being trustworthy about money.",
  "fix": "Hoist one potSplit(total, payout) producer with the champion absorbing, and call it from renderPot, #lineSplit and the demo branch — matching the server's own cent math.",
  "notes": "Related to IDX07-02.",
  "slice": "git diff 34d20b6..HEAD",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "SD-09",
  "title": "seasonState()/seasonNote() key off today's date, not the round's played_on and not the season end",
  "file": "index.html",
  "line": "5866-5885, 6618-6628",
  "severity": "P2",
  "kind": "bug",
  "category": "money-scoring-correctness",
  "sessionCode": true,
  "confidence": 8,
  "quote": "  const _ss = seasonState();\n  if(_k) _k.textContent = _ss === 'live' ? 'League points this round' : 'This round';\n…\n  $('#calcPts').textContent = _ss === 'live' ? pts : '—';",
  "failureScenario": "(a) Backdating, which D136 kept legitimate: the composer promises 9 league points for a round dated before first tee while the post handler's own `counts` test — computed from played_on, the rule v_rounds_ranked implements — says no, so the ceremony reads 'COUNTS ON YOUR CARD'. That is D122's defect relocated. (b) The mirror: leagueStage() has no season-over branch at all and isCupFinal() stays true forever past ends_on, so after the season ends but before the phase flips, the composer promises points for a round the view will never score. #hhPhase eight hundred lines away does check CS.season.status. The phone got this right — PostSeasonRule.note takes playedOn.",
  "fix": "Give seasonState the round's date and add a season-over branch to leagueStage.",
  "notes": "Same root as idx05-03.",
  "slice": "git diff 34d20b6..HEAD",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "SD-10",
  "title": "lock_league's already-locked early return hands back phase='setup' for a half-locked league, and the client reports 'Bylaws locked' about a league it can never finish locking",
  "file": "supabase/migrations/20260829220000_lock_league.sql",
  "line": "68-76 (with index.html:16108-16120)",
  "severity": "P2",
  "kind": "bug",
  "category": "silent-failure",
  "sessionCode": true,
  "confidence": 7,
  "quote": "  if v_settings.locked_at is not null then\n    …\n    return json_build_object('already_locked', true, 'phase', v_league.phase, 'season', row_to_json(v_season));\n  end if;",
  "failureScenario": "The retained deploy-skew fallback writes locked_at FIRST and leagues.phase LAST. A failure in between leaves locked_at set with phase='setup', no season and no squads. On the Pro's retry lock_league takes the early return and hands back phase 'setup', which lockViaRpc returns as a truthy nextPhase — so the handler fires lock_ok, toasts 'Bylaws locked' and opens the celebration for a league still in setup, and will do so on every future attempt. The 'success on failure' class D111 was written to kill, moved one layer down.",
  "fix": "Treat `already_locked and phase = 'setup'` as not yet locked and fall through — the season/form_squads/phase writes are all idempotent.",
  "notes": "Prod has 0 half-locked leagues today, so it is a live hazard rather than live damage — but the fallback that creates it is retained deliberately.",
  "slice": "git diff 34d20b6..HEAD",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "SD-11",
  "title": "D120's 'one producer' missed two prominent call sites — the Clubhouse header still prints 'Setup — invites open' and 'Squad formation'",
  "file": "index.html",
  "line": "13317-13322, 17511-17512",
  "severity": "P2",
  "kind": "bug",
  "category": "dead-or-contradictory-code",
  "sessionCode": true,
  "confidence": 9,
  "quote": "    $('#hhPhase').textContent =\n      state.phase==='setup' ? 'Setup — invites open'\n      : state.phase==='draft' ? 'Squad formation'\n      : …",
  "failureScenario": "D120 converted the switcher chip, the Home digest label and the room's phase sub — but the only site it touched in this family is inside a display:none container. For a league in phase 'draft' a golfer reads 'Squads drawing' on Home and in the Clubhouse row, and 'Squad formation' in the League Room header and the record line. In setup they read 'Forming' on Home and 'Setup — invites open' in the room — the string D120 flags as also contradicting D40/D112. Preflight check 20 compares the two label tables to each other and cannot see call sites.",
  "fix": "Route #hhPhase and renderLeagueRecord through stageLabel(); teach leagueStage the 'season is over' case #hhPhase already knows.",
  "notes": "Duplicate of IDX11-08 from the diff side. stageLabel(m) also cannot answer preseason/final for a non-current membership and falls back to 'season'.",
  "slice": "git diff 34d20b6..HEAD",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "SD-12",
  "title": "Q-18's Back button is created lazily inside openEmailBox, so on a cold load the league-code door hides the email door with no way back",
  "file": "index.html",
  "line": "15832-15850, 16002-16006",
  "severity": "P2",
  "kind": "bug",
  "category": "correctness",
  "sessionCode": true,
  "confidence": 9,
  "quote": "$('#obJoin').addEventListener('click', () => {\n  const em = $('#obEmail'); if(em) em.style.display = 'none';\n  const bk = $('#obBack'); if(bk) bk.style.display = '';",
  "failureScenario": "Duplicate of B-05: on a cold signed-out load #obBack does not exist, so tapping 'I have a league code' hides the email door and creates nothing in its place. Only a reload restores it. The commit says 'symmetric across the email and league-code doors.'",
  "fix": "Extract an ensureAuthBack() and call it from both.",
  "notes": "",
  "slice": "git diff 34d20b6..HEAD",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "SD-13",
  "title": "Q-27's floor sentence feeds only renderPulse; the wizard's (i) was hand-edited instead, still threatens a squad penalty for solo leagues, and now leaks '(D14)' into user-facing copy",
  "file": "index.html",
  "line": "3331",
  "severity": "P3",
  "kind": "bug",
  "category": "copy-contradiction",
  "sessionCode": true,
  "confidence": 9,
  "quote": "          <div class=\"ihelp\" id=\"ih-floor\">The anti-ghosting rule. … no asking, no approval (D14); from the second miss the squad takes the penalty: −5 points per round short under Standard rules. Short months are waived.</div>",
  "failureScenario": "A Pro configuring a SOLO league (3 in prod) taps the (i) beside Participation floor and reads that the squad takes the penalty — which cannot fire, since close_month drives its loop from squad_members and a solo league has no squads. Q-27 built floorSentence() precisely to stop saying this; the wizard's (i) was edited by hand in the same commit and kept the claim. A casual-preset Pro reads the same false threat. And the string now shows the golfer an internal decision id, '(D14)', three commits after Q-26 removed 'bylaws §4' for exactly that reason. grep shows floorSentence has two call sites, both inside renderPulse, while the commit says 'One floorSentence() now feeds all three.'",
  "fix": "Populate #ih-floor from floorSentence() on every wizard render and strip the citation; the GUIDE sheet and the dial's own <small> are a third and fourth wording.",
  "notes": "floorSentence also derives the penalty from state.preset rather than floor_penalty, the column close_month reads. All 18 prod leagues agree today, and CS.settings now carries the raw row.",
  "slice": "git diff 34d20b6..HEAD",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "SD-14",
  "title": "looksLikeOurSentence passes raw JavaScript TypeError messages through to the golfer — \\berror\\b does not match 'TypeError'",
  "file": "index.html",
  "line": "4163-4169",
  "severity": "P3",
  "kind": "bug",
  "category": "silent-failure",
  "sessionCode": true,
  "confidence": 8,
  "quote": "  if(/[_{}<>]|::|\\bpg_|\\brelation\\b|\\bcolumn\\b|\\bconstraint\\b|\\bschema\\b|\\bsql\\b|\\berror\\b|\\bexception\\b|^[a-z0-9_]+\\(/i.test(s)) return false;",
  "failureScenario": "\"Cannot read properties of undefined (reading 'league')\" is 52 chars, starts with a capital, contains spaces, has no banned character — and `\\berror\\b` does not match 'TypeError' because there is no word boundary inside the word. Every gate passes and the golfer sees 'Could not join. Cannot read properties of undefined (reading 'league')'. Before this session the same input fell to the shrug. The shape test is the last gate before raw text reaches a user and it is not conservative enough to be that.",
  "fix": "Require a positive signal (a marker prefix or an errcode) rather than inferring authorship from prose shape.",
  "notes": "",
  "slice": "git diff 34d20b6..HEAD",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "SD-15",
  "title": "Four of the eleven alternatives in the Q-08 error allowlist match no message anywhere in the schema, and one is duplicated",
  "file": "index.html",
  "line": "4145-4146",
  "severity": "P3",
  "kind": "bug",
  "category": "dead-or-contradictory-code",
  "sessionCode": true,
  "confidence": 9,
  "quote": "  else if(/not enough golfers|minimum four|still in the pool|is empty — draw again|is empty — draw again|only the pro|commissioner only|isn't open yet|is still being set up|has wrapped|season is finished/i.test(raw))",
  "failureScenario": "Four alternatives return zero hits across supabase/migrations and supabase/functions — they were written from memory rather than from the source, so whatever real messages they were meant to allow still fall to the shrug, which is exactly the failure Q-08 set out to fix. One alternative is listed twice.",
  "fix": "Generate the allowlist from the schema, or add a preflight check that every alternative matches at least one raise exception literal.",
  "notes": "Duplicate of idx03-07.",
  "slice": "git diff 34d20b6..HEAD",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "SD-16",
  "title": "Q-22 nulls state.lastPost, so Post now rejects a card with both nines filled by saying 'Enter at least one nine first'",
  "file": "index.html",
  "line": "6578-6587 with 6749",
  "severity": "P3",
  "kind": "bug",
  "category": "copy-contradiction",
  "sessionCode": true,
  "confidence": 8,
  "quote": "    state.lastPost=null; return;\n…\n$('#postBtn').addEventListener('click', async ()=>{\n  if(!state.lastPost){toast('Enter at least one nine first');return;}",
  "failureScenario": "Duplicate of POST-B-07: the panel correctly says 'Pick a tee', and the button contradicts it two inches away. The Q-21 date guard right below sets aria-invalid and focuses the field; this one does neither.",
  "fix": "Track why lastPost is null and toast accordingly, marking #inRating.",
  "notes": "#inSlope defaults to 113 when blank, so `!(slope>0)` can never be true and the guard reduces to a missing rating — the message names a field it does not check (POST-B-03).",
  "slice": "git diff 34d20b6..HEAD",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "SD-17",
  "title": "app-tests' two lock assertions resolve after the PASS/FAIL summary is computed, so a broken share sheet still reports PASS",
  "file": "tests/app-tests.js",
  "line": "85-100 with 111-113",
  "severity": "P2",
  "kind": "bug",
  "category": "test-coverage",
  "sessionCode": true,
  "confidence": 9,
  "quote": "    Promise.resolve(window.openLockShare('draft', 0)).then(() => { … t('lock: share sheet prints the join URL as text', …); });\n  })();\n\n  const fails = R.filter(r => !r.ok);\n  console.log(`\\n${fails.length ? 'FAIL' : 'PASS'} — ${R.length} tests, ${fails.length} failure(s)`);\n  return { total: R.length, failures: fails.map(f => f.name) };",
  "failureScenario": "openLockShare is async; the IIFE returns immediately and the summary is computed on the same tick, so the two t() calls push into R after the return value was produced. Regress openLockShare so it stops printing the join URL and the suite still prints PASS and returns zero failures — the numbers the commits quote. Given this block exists specifically to catch the Q-01 regression that ran 25 days, a silent-pass harness is the wrong failure mode.",
  "fix": "Await all pending assertions before computing fails, or seed R with a failing placeholder the .then replaces.",
  "notes": "The comment acknowledges the ordering and treats it as cosmetic.",
  "slice": "git diff 34d20b6..HEAD",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "SD-18",
  "title": "Zero committed test coverage for any of the session's new client functions, while the commit messages quote ~45 assertions for them",
  "file": "tests/app-tests.js",
  "line": "1-113 (whole file)",
  "severity": "P2",
  "kind": "opportunity",
  "category": "test-coverage",
  "sessionCode": true,
  "confidence": 9,
  "quote": "/* Cup Season in-browser function suite. All targets are classic-block globals … */",
  "failureScenario": "grep over tests/ for vsShort, floorSentence, endgameLine, seasonNote, leagueStage, inviteMessage, openInviteSheet or the pot trio returns nothing. The commits report '12 invite assertions', '8 pot assertions across both roles', '8 endgame assertions across both dials', 'Ten role × stage assertions', '7 stage/state assertions' — 45 assertions that exist only as one-off console drives. Five of the findings in this review live inside exactly those functions, and each is a single assertion away from being caught.",
  "fix": "Add pure-function assertions in the style of the band property test: vsShort over null/''/NaN/-1.0, potSplit summing to the pot across sizes and splits, endgameLine across structures × dials asserting '+10' only for squads2, floorSentence across preset × structure, seasonNote across the four states.",
  "notes": "",
  "slice": "git diff 34d20b6..HEAD",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "SD-19",
  "title": "D122's live-only gate suppresses the points preview for every league-less golfer — the figure the old copy existed to show them",
  "file": "index.html",
  "line": "6618-6628",
  "severity": "P3",
  "kind": "bug",
  "category": "correctness",
  "sessionCode": true,
  "confidence": 7,
  "quote": "  $('#calcPts').textContent = _ss === 'live' ? pts : '—';",
  "failureScenario": "A signed-in golfer with no league (the free-forever identity tier, and every new account before it joins) gets seasonState() === 'no_league', which is not 'live', so the headline figure — the biggest thing on the 'How this round scores' card — renders '—'. The prior copy was deliberately aspirational ('the round still counts on your card — points apply in any league you join') and was the hook toward joining. The commit's stated scope is 'outside the season window'; no_league is not outside a window, it has no window, and the commit never mentions it.",
  "fix": "Gate on `_ss !== 'pre' && _ss !== 'over'` so no_league keeps its figure.",
  "notes": "",
  "slice": "git diff 34d20b6..HEAD",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "SD-20",
  "title": "D126/D127 are web-only: the phone's climb note still prints the string D127 identified as wrong, and preflight 20 does not cover it",
  "file": "apps/ios/Packages/CupSeasonKit/Sources/CupSeasonKit/League/StandingsMath.swift",
  "line": "403-408",
  "severity": "P3",
  "kind": "bug",
  "category": "cross-client-drift",
  "sessionCode": true,
  "confidence": 8,
  "quote": "    if K >= n { return \"EVERYONE ADVANCES — \\(n) CONTENDER\\(n == 1 ? \"\" : \"S\"), \\(K) SEAT\\(K == 1 ? \"\" : \"S\")\" }",
  "failureScenario": "For a one-contender league the web now renders 'NOBODY TO RACE YET' plus the endgame sentence; the phone renders 'EVERYONE ADVANCES — 1 CONTENDER, 2 SEATS' — the exact string D127's commit calls out — and no endgame sentence at all, so the phone still has no answer to 'how is the title decided'. LeagueRoomTests pins the old string, so the drift is asserted rather than caught, and preflight 20 compares only STAGE_LABEL to Stage.label.",
  "fix": "Carry D126's endgameLine and D127's seat-line branches into ClimbMath.note and update the test; extend check 20 to the note strings.",
  "notes": "The IN/OUT badge word did cross to both clients, and the test that pinned 'LOCKED' was correctly updated.",
  "slice": "git diff 34d20b6..HEAD",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "SD-21",
  "title": "Preflight 19 counts covenantGate occurrences rather than proving each join is preceded by one",
  "file": "tests/preflight.mjs",
  "line": "561-570",
  "severity": "P3",
  "kind": "opportunity",
  "category": "test-coverage",
  "sessionCode": true,
  "confidence": 8,
  "quote": "  const joins = [...html.matchAll(/rpc\\(\\s*'join_league'/g)].length;\n  const gates = [...html.matchAll(/covenantGate\\s*\\(/g)].length - 1;   // minus the declaration",
  "failureScenario": "The check is order-blind and association-blind: five joins and five gates pass even if all five gates sit on one path. Any change that adds a sixth join AND a covenantGate reference elsewhere — a retry, a helper, a comment — passes. It also matches single-quoted rpc only, so a double-quoted call drops joins to 0 and downgrades the whole check to a WARN.",
  "fix": "Parse rather than count — acorn is already a dependency — or funnel every join through one joinLeague(code) helper that gates internally and assert the rpc appears exactly once.",
  "notes": "",
  "slice": "git diff 34d20b6..HEAD",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "SD-22",
  "title": "Q-15's 'Enter your email and you're in' survives on the generic invite landing, where it is load-bearing for a string match",
  "file": "index.html",
  "line": "18598 (with 18506-18512)",
  "severity": "P3",
  "kind": "bug",
  "category": "copy-contradiction",
  "sessionCode": true,
  "confidence": 8,
  "quote": "      : `You're invited. Enter your email and you're in.`);\n…\n    if(st && /^You're invited\\. /.test(st.textContent))",
  "failureScenario": "safeBoot shows the generic line — the exact promise Q-15 says it removed from 'all three surfaces that carried the claim' — and only swaps it once league_by_code resolves. If that RPC is slow, fails, or the code is bad, the golfer reads the removed claim for the whole session; and the resolver's regex is anchored on that literal, so the string cannot be corrected without also updating the matcher.",
  "fix": "Mark the element with a data attribute and match on that, then correct the third string.",
  "notes": "",
  "slice": "git diff 34d20b6..HEAD",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "SD-23",
  "title": "renderPot carries a duplicated season guard and leaves cursor:pointer on the non-Pro status row",
  "file": "index.html",
  "line": "7534-7535, 7529 (with 485-490)",
  "severity": "P3",
  "kind": "bug",
  "category": "dead-or-contradictory-code",
  "sessionCode": true,
  "confidence": 9,
  "quote": "        if(!window.CS?.season?.id){ toast('Buy-ins open once the bylaws lock'); return; }\n        if(!window.CS?.season?.id){ toast('Buy-ins open once the bylaws lock'); return; }",
  "failureScenario": "The duplicate is leftover from swapping the removed `if(!isPro)` guard for a season check that already existed on the next line. Separately, D129 changed a member's row from button to div so it stops being a control, but `.payer` still sets cursor:pointer with no element-qualified override, so on any pointer device the row still shows a hand cursor over a div that does nothing — a partial fix to the commit's own complaint that 'the member rows LOOKED tappable'.",
  "fix": "Delete the duplicated line; add div.payer{cursor:default} and mark it as status for assistive tech.",
  "notes": "Duplicates IDX07-07 and IDX07-04.",
  "slice": "git diff 34d20b6..HEAD",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "SD-24",
  "title": "A root package.json + package-lock.json now make every Netlify client deploy depend on an npm install nothing in the build uses",
  "file": "package.json",
  "line": "1-13",
  "severity": "P3",
  "kind": "bug",
  "category": "deploy-skew",
  "sessionCode": true,
  "confidence": 6,
  "quote": "  \"devDependencies\": {\n    \"acorn\": \"^8.14.0\",\n    \"eslint-scope\": \"^8.2.0\"\n  }",
  "failureScenario": "netlify.toml pins the build command and stamp-version.sh's allowlist never copies package.json into dist, so nothing new is served. But Netlify detects a package.json at the base directory and installs dependencies before running the build command; with a lockfile present that is npm ci. From this commit onward, a registry outage or an install failure fails the deploy for two packages only tests/preflight.mjs uses — and preflight never runs on Netlify. The blast radius is 'client deploy blocked', since a failed build keeps the previous deploy.",
  "fix": "Skip the Node install via environment, or move the dev tooling's package.json under tests/ so it is not at the base directory. Confirm on the next deploy's build log.",
  "notes": "Could not read a Netlify build log; the 'install runs' half is from documented behaviour rather than observation.",
  "slice": "git diff 34d20b6..HEAD",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "SD-25",
  "title": "switchView's wizard gate runs before the draft→wizard remap it creates itself, so one route still bypasses it",
  "file": "index.html",
  "line": "4218-4225 with 4234",
  "severity": "P3",
  "kind": "bug",
  "category": "security",
  "sessionCode": true,
  "confidence": 7,
  "quote": "  if(v==='wizard'){ … }\n…\n  if(v==='draft' && state.phase==='setup'){ toast('Lock settings first: the draft opens after setup'); v='wizard'; }",
  "failureScenario": "Duplicate of idx03-01 from the diff side. The commit's claim is 'A guarantee that lives on one route is not a guarantee: it belongs where every route passes' — and the one route switchView invents for itself does not pass it. The server still refuses every write, and #wizCancel has its own belt, so nothing destructive follows.",
  "fix": "Move the gate below the whole remap block.",
  "notes": "Low reachability today: D112 means a member should not be in a setup-phase league at all.",
  "slice": "git diff 34d20b6..HEAD",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "SD-26",
  "title": "seasonNote names an uncommitted default first-tee date for a league whose bylaws are not locked",
  "file": "index.html",
  "line": "5878-5885",
  "severity": "P3",
  "kind": "bug",
  "category": "correctness",
  "sessionCode": true,
  "confidence": 7,
  "quote": "  let when = '';\n  try { when = firstTeeText(); } catch(_){}\n  return kind === 'short' ? (when ? `Practice · season starts ${when}` : 'Practice round') : …",
  "failureScenario": "For a Pro who has created but not locked a league (seven in prod), leagueStage is 'forming' and seasonState is 'pre', but firstTeeText() falls back through seasonStartDate() to defaultStart() — the next upcoming Saturday, already written into state.startISO by renderSeasonDates. So the composer tells the Pro 'the season starts Sat Sep 5' about a date nobody has committed to and that changes the moment they move the dial. The `when ? … : 'Practice round'` fallback exists for exactly this case and never fires.",
  "fix": "Require a committed date: `const when = state.seasonStart ? firstTeeText() : ''`.",
  "notes": "",
  "slice": "git diff 34d20b6..HEAD",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "TT-01",
  "title": "db-checks 16 cannot see a league with zero squads, and never checks the unseated member its own message names",
  "file": "tests/db-checks.sql",
  "line": "354-365",
  "severity": "P1",
  "kind": "bug",
  "category": "false-negative",
  "sessionCode": true,
  "confidence": 9,
  "quote": "    join squads q on q.season_id = s.id\n    where l.phase = 'season'\n    group by l.id\n    having count(*) filter (where not exists (select 1 from squad_members sm where sm.squad_id = q.id)) > 0",
  "failureScenario": "Claimed: the inner join means a league that never formed a squad produces no row and cannot be counted — a worse §15 violation than an empty squad — and the HAVING clause never references league_members even though the failure string promises 'or an unseated member'. Live counts: the check reports 1 league; 3 are in phase='season' with zero squads and 4 have a member seated in no squad.",
  "fix": "Replace the inner join with a three-arm union and report the counts separately.",
  "notes": "DROPPED — both verifiers refuted the numbers. All three zero-squad leagues are structure='solo', where zero squads is the DESIGNED terminal state (lock_league routes solo straight to phase='season' and skips form_squads; start_season wraps its raises in `if st.structure <> 'solo'`), and the unseated members are in those same solo leagues. So none of the seven is a violation. The structural observation about the message promising more than it evaluates stands as a copy nit, not a false negative.",
  "slice": "tests/, tools/, stamp-version.sh, netlify.toml, sw.js, legal.html",
  "verification": {
   "status": "dropped",
   "correctedSeverity": null,
   "votes": [
    {
     "lens": "control-flow",
     "verdict": "REFUTED",
     "refuted": true,
     "reasoning": "The load-bearing claim fails on a column the reviewer did not read: all three zero-squad leagues are structure='solo', where zero squads is the designed terminal state — lock_league routes solo straight to phase='season' and skips form_squads, and start_season wraps its total/loose/empty raises in `if st.structure <> 'solo'`."
    },
    {
     "lens": "intent-and-precedent",
     "verdict": "REFUTED",
     "refuted": true,
     "reasoning": "Same: a solo league is DEFINED as a league with no squads, so the inner join is the correct scope, not blindness. The unseated members are in those same solo leagues. The message promising 'or an unseated member' remains a copy nit."
    }
   ]
  }
 },
 {
  "id": "TT-02",
  "title": "preflight check 8 existence-checks only 5 of the 9 allowlisted files (line-anchored regex stops at the backslash continuation)",
  "file": "tests/preflight.mjs",
  "line": "124-131",
  "severity": "P2",
  "kind": "bug",
  "category": "false-negative",
  "sessionCode": false,
  "confidence": 10,
  "quote": "const names = ((stamp.match(/^cp (?!-r).*$/gm) || []).join(' ').match(/[\\w.-]+\\.(?:html|js|webmanifest|png)/g) || []);",
  "failureScenario": "stamp-version.sh's allowlist cp spans two lines with a backslash continuation; `^cp …$` matches only the first, so the four continuation files (three icons and og-image) are never existence-checked. Confirmed by the live run: 'PASS dist allowlist files exist -- 5 files'. Delete og-image.png and preflight PASSes; the Netlify build then runs stamp-version.sh under set -euo pipefail, cp fails, and the deploy aborts. Check 4, ten lines above, already computes a continuation-aware cpLine and does not share it.",
  "fix": "Flatten the continuations once and drive both checks from it.",
  "notes": "",
  "slice": "tests/, tools/",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "TT-03",
  "title": "preflight check 18 exempts every window.* name globally — so a classic block calling a module-scoped function bare is not reported",
  "file": "tests/preflight.mjs",
  "line": "491-508",
  "severity": "P2",
  "kind": "bug",
  "category": "false-negative",
  "sessionCode": true,
  "confidence": 9,
  "quote": "const bridged = new Set([...html.matchAll(/window\\.([A-Za-z_$][\\w$]*)\\s*=/g)].map(m => m[1]));\n...\n        if (BROWSER.has(name) || bridged.has(name) || sharedGlobals.has(name)) continue;",
  "failureScenario": "Demonstrated by running the check's own scan() with its real 181-name bridged set over a classic snippet containing `function onLockTap(){ return lockBylaws(); }` and `function other(){ return neverDeclaredAnywhere(); }` — only the second is reported. lockBylaws is module-scoped and reachable from classic code only as window.lockBylaws; calling it bare is a guaranteed ReferenceError, and it is one of 181 names silently exempted this way. That is exactly the classic↔module landmine CLAUDE.md leads with, in the check written to catch free identifiers.",
  "fix": "Scope the exemption per block kind: a CLASSIC block may resolve a bare name only from classic top-level declarations, never from the window.X= set.",
  "notes": "",
  "slice": "tests/, tools/",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "TT-04",
  "title": "db-checks 3's hand-typed RPC array has drifted — 8 client RPCs unchecked, 3 of them added this session",
  "file": "tests/db-checks.sql",
  "line": "64-79",
  "severity": "P2",
  "kind": "bug",
  "category": "drift",
  "sessionCode": false,
  "confidence": 10,
  "quote": "select '3 · authenticated RPC grants',\n  case when missing = '' then 'PASS — all 105 granted'\n...\n  from unnest(array[ 'abandon_live_round','add_event_player', ... ])",
  "failureScenario": "The array holds 108 names while the header and the PASS string both say 105, and eight RPCs the web client calls are absent: cup_final_race, guest_live_set_score, guest_live_set_wolf, guest_live_state, lock_league, log_growth_event, set_buy_in_terms, unregister_device_token. Three were added by this session's commits — the newest RPCs, the ones most likely to have missed a grant, are the ones the check cannot see. Ship one without its grant and check 3 still says 'PASS — all 105 granted' while the button 403s in prod.",
  "fix": "Generate the array from the client's rpc call sites (preflight already extracts them) or drive the assertion from packages/db/contract.psv.",
  "notes": "All eight ARE granted today — a missing guard, not a live 403.",
  "slice": "tests/, tools/",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "TT-05",
  "title": "db-checks 3 has no 'extra' half — an accidental grant to authenticated is invisible outside three hardcoded names",
  "file": "tests/db-checks.sql",
  "line": "64-79",
  "severity": "P2",
  "kind": "opportunity",
  "category": "missing-guard",
  "sessionCode": false,
  "confidence": 9,
  "quote": "select coalesce(string_agg(f, ', '), '') as missing\n  from unnest(array[ ... ]) f\n  where not exists (select 1 from pg_proc p ... and has_function_privilege('authenticated', p.oid, 'execute'))",
  "failureScenario": "Check 2 asserts anon can execute EXACTLY twelve functions — missing AND extra. Check 3 computes only missing, so a migration that grants authenticated execute on an engine function is undetected unless it is one of the three names hardcoded in check 12. That is precisely the incident check 12's own comment describes (close_month's grant creeping back). Concretely: `grant execute on function public.score_round() to authenticated;` in a new migration passes every check in this suite — and DB-01 and DBE-07 are two live instances of exactly that shape.",
  "fix": "Give check 3 an `extra` arm mirroring check 2, then delete check 12's hardcoded trio as redundant.",
  "notes": "",
  "slice": "tests/, tools/",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "TT-06",
  "title": "app-tests.js: the two lock assertions are async and never reach the summary or the return value",
  "file": "tests/app-tests.js",
  "line": "92-117",
  "severity": "P2",
  "kind": "bug",
  "category": "silent-pass",
  "sessionCode": true,
  "confidence": 9,
  "quote": "    Promise.resolve(window.openLockShare('draft', 0)).then(() => { … });\n...\n  const fails = R.filter(r => !r.ok);\n  return { total: R.length, failures: fails.map(f => f.name) };",
  "failureScenario": "The IIFE returns the summary synchronously; the t() calls inside .then push into R after it. So if openLockShare renders a sheet with no join URL — the exact Q-01 regression this block was written for — the console prints PASS with zero failures and the returned object reports none. Any driver reading the return value records a clean pass over a live bug.",
  "fix": "Make the suite async and await the pending assertions, or seed R with a failing placeholder the .then replaces.",
  "notes": "Duplicate of SD-17. The comment at line 111 notes the lines print after the summary but not that they cannot fail it.",
  "slice": "tests/, tools/",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "TT-07",
  "title": "preflight check 11 reports a stale contract.psv as an unpushed migration — and contradicts deploy-status in the same ship.sh run",
  "file": "tests/preflight.mjs",
  "line": "232-245",
  "severity": "P2",
  "kind": "bug",
  "category": "wrong-diagnosis",
  "sessionCode": false,
  "confidence": 9,
  "quote": "  const psv = readFileSync(join(root, 'packages', 'db', 'contract.psv'), 'utf8');\n  const inProd = new Set(psv.split('\\n')...map(l => l.split('|')[0]));\n...\n  if (pending.length) warn('rpc pending deploy', `in a migration but not yet in prod — owe a db push: …`);",
  "failureScenario": "Live right now: preflight WARNs 'owe a db push: set_buy_in_terms', but the migration IS applied, the function exists and authenticated can execute it — all verified by query. What is stale is contract.psv, a hand-refreshed snapshot the check reads as if it were prod. So ship.sh prints 'owe a db push' and deploy-status's 'none pending' ten lines apart in one run. The inverse is undetectable: a stale contract.psv also means rpc.ts and Rpc.swift lag prod, and Rpc.swift is the artefact that makes D37 'enforced by the compiler' — the phone currently has no Rpc.set_buy_in_terms for a function that is live and granted.",
  "fix": "Ask deploy-status (or the CLI) which migrations are applied and classify accordingly; when the migration is applied but the function is absent from the snapshot, say the snapshot is stale. Stamp the snapshot with the max applied version so staleness is detectable offline.",
  "notes": "",
  "slice": "tests/, tools/",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "TT-08",
  "title": "deploy-status counts untracked files as an owed deploy, so the Stop hook speaks on nearly every session",
  "file": "tools/deploy-status.mjs",
  "line": "111-118",
  "severity": "P2",
  "kind": "bug",
  "category": "noise",
  "sessionCode": false,
  "confidence": 9,
  "quote": "  const dirty = (run('git', ['status', '--porcelain']) || '').split('\\n').filter(Boolean).length;\n...\n  if (dirty) owed.push(`${dirty} uncommitted change${dirty > 1 ? 's' : ''}`);",
  "failureScenario": "git status --porcelain lists untracked files. Running the hook emitted 'Deploys owed — client: 1 uncommitted change' for a single untracked audit directory. Nothing is owed — untracked files cannot reach Netlify. This repo routinely generates audit and notes directories, so the hook documented as 'silent unless something is genuinely owed' will fire on most sessions, which is the tune-out failure the file's own comment warns about.",
  "fix": "Use --untracked-files=no for the uncommitted-changes arm, or count only untracked files inside the dist allowlist.",
  "notes": "",
  "slice": "tests/, tools/",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "TT-09",
  "title": "ship.sh refuses the client push for any untracked file, and still exits 0",
  "file": "tools/ship.sh",
  "line": "88-90",
  "severity": "P2",
  "kind": "bug",
  "category": "refusal-path",
  "sessionCode": false,
  "confidence": 9,
  "quote": "  if [[ -n \"$(git status --porcelain)\" ]]; then\n    say \"${RED}Uncommitted changes — commit them first; this script does not commit for you.${OFF}\"",
  "failureScenario": "Same root as TT-08 with a harder consequence. With committed, unpushed work AND one untracked scratch file, ship.sh prints RED 'commit them first', never offers the push, falls through to 'done' and exits 0. Untracked files have no bearing on what Netlify builds from the pushed commit, so the refusal is on a non-risk — and the exit code says success, so a wrapper cannot tell the push was skipped. That is the state of this very checkout.",
  "fix": "Scope the gate to tracked changes or to the served files, and exit non-zero when a layer reported as owed is skipped by a refusal rather than a declined confirmation.",
  "notes": "",
  "slice": "tests/, tools/",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "TT-10",
  "title": "preflight check 3 treats classic let/const and NESTED function declarations as window properties",
  "file": "tests/preflight.mjs",
  "line": "63-67",
  "severity": "P2",
  "kind": "bug",
  "category": "false-negative",
  "sessionCode": false,
  "confidence": 8,
  "quote": "    for (const d of m[2].matchAll(/^\\s*(?:async\\s+)?function\\s+([A-Za-z_$][\\w$]*)/gm)) assigned.add(d[1]);\n    for (const v of m[2].matchAll(/^(?:let|var|const)\\s+([A-Za-z_$][\\w$]*)/gm)) assigned.add(v[1]);",
  "failureScenario": "Two over-excusals in the set the check uses to decide a window.X reference is safe. Classic top-level let/const are global LEXICAL bindings, not properties of window, so `const CS = {…}` plus `window.CS.league` elsewhere passes and throws at runtime. And the function regex allows leading whitespace, so it also collects functions declared INSIDE other functions — 34 such names (toast, refresh, start, stop, kill, show, trace…), none of which is a global. A `window.toast(...)` in one of the file's 14 inline onclick strings would pass and be undefined at runtime — and check 3 is the ONLY check that can see inside those handler strings.",
  "fix": "Anchor the function regex to column 0 and admit only var and function declarations; better, reuse check 18's eslint-scope pass.",
  "notes": "No live instance today — all 194 bridged names resolve.",
  "slice": "tests/, tools/",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "TT-11",
  "title": "legal.html ships a different dark palette than the app, and no check covers it",
  "file": "legal.html",
  "line": "13-17",
  "severity": "P3",
  "kind": "bug",
  "category": "design-drift",
  "sessionCode": false,
  "confidence": 8,
  "quote": "  @media (prefers-color-scheme: dark){\n    :root{ --bg:#0C0D0F; --panel:#17191C; … --gold:#FF5A2E; … }\n  }",
  "failureScenario": "tokens.json defines the dark ground as bg0 #0B1410 and the surface as bg1 #131D17, and index.html and the manifest both use #0B1410. legal.html uses a neutral charcoal — a visibly different family — and calls #FF5A2E 'gold' when that is the `hot` heat token. tokens.json's own note on bg0 says it 'converges meta/favicon/legal'; legal did not converge. A golfer on the dark app taps Privacy and crosses a visible seam mid-flow. Preflight check 10 reads only index.html, so the repo's other shipped HTML page is entirely unchecked.",
  "fix": "Point legal.html's custom properties at the tokens.json values and extend check 10 to hold it to the same map. Also drop the stale 'after the light-first app' comment.",
  "notes": "",
  "slice": "tests/, tools/",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "TT-12",
  "title": "stamp-version.sh publishes brand/ wholesale — an internal design sub-project with sources, tests and strategy docs",
  "file": "stamp-version.sh",
  "line": "40",
  "severity": "P3",
  "kind": "bug",
  "category": "over-broad-publish",
  "sessionCode": false,
  "confidence": 9,
  "quote": "cp -r brand \"$DIST/brand\"",
  "failureScenario": "That one line copies 36 files to cupseason.app: brand/README.md (internal brand strategy and its decision history) and all of brand/ember-logo-system/ — package.json, tsconfig, src/*.ts, test/*.test.ts, .gitignore, masters/**/*.svg and a contact-sheet.html that renders as a page. Nothing in index.html or legal.html references brand/ at runtime — the only occurrence is a comment. A miniature of the leak OPS-C7 closed, inside the file whose header promises 'Nothing else can ever be served', and preflight check 8 deliberately skips cp -r lines so nothing validates it.",
  "fix": "Replace with an explicit list of the brand files the site actually serves (today: none), and drop check 8's (?!-r) exclusion.",
  "notes": "",
  "slice": "tests/, tools/",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "TT-13",
  "title": "sw.js chains skipWaiting() after addAll(), so one 404 in the SHELL stops the worker updating",
  "file": "sw.js",
  "line": "17-21",
  "severity": "P3",
  "kind": "bug",
  "category": "silent-failure",
  "sessionCode": false,
  "confidence": 8,
  "quote": "    caches.open(CACHE).then((c) => c.addAll(SHELL)).then(() => self.skipWaiting())",
  "failureScenario": "Cache.addAll is all-or-nothing: a single non-2xx rejects the whole promise, so the install rejects, skipWaiting never runs, and the new service worker never activates — the previously installed worker keeps serving. Navigations stay network-first so the app is not broken, but the SW silently stops updating and its cache is never retired, which is the opposite of the header's stated strategy. Install errors go to the SW console only.",
  "fix": "Cache individually with allSettled and keep the update path alive — only '/' is needed for offline correctness, and the navigate handler re-caches it anyway.",
  "notes": "",
  "slice": "tests/, tools/",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "TT-14",
  "title": "preflight check 20 silently PASSES with 'apps/ios absent' if LeagueCopy.swift is merely renamed",
  "file": "tests/preflight.mjs",
  "line": "572-575",
  "severity": "P3",
  "kind": "bug",
  "category": "false-negative",
  "sessionCode": true,
  "confidence": 9,
  "quote": "  if (!existsSync(copyPath)) pass('stage vocabulary shared', 'apps/ios absent — skipped');",
  "failureScenario": "Checks 12–17 gate on the apps/ios DIRECTORY; check 20 gates on one specific FILE path. Move Stage.label into its own file or reorganise the folder and the check prints 'apps/ios absent — skipped' and PASSes while apps/ios sits right there fully populated — so the two clients can drift apart on the six stage words, the exact D120 drift the check was written for, with a green preflight. The message is also factually false, which makes the miss hard to notice.",
  "fix": "Gate on the directory like its siblings, and FAIL rather than skip when apps/ios exists but the table cannot be located.",
  "notes": "",
  "slice": "tests/, tools/",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "TT-15",
  "title": "preflight check 19 counts gates instead of pairing them to joins",
  "file": "tests/preflight.mjs",
  "line": "555-563",
  "severity": "P3",
  "kind": "opportunity",
  "category": "weak-assertion",
  "sessionCode": true,
  "confidence": 8,
  "quote": "  const joins = [...html.matchAll(/rpc\\(\\s*'join_league'/g)].length;\n  const gates = [...html.matchAll(/covenantGate\\s*\\(/g)].length - 1;\n...\n    : gates >= joins",
  "failureScenario": "The current state is correct — five joins, each preceded by an awaited, result-checked gate. But the assertion is a global count: delete the gate on the invite-link boot path (the one the comment says matters most) and add two covenantGate references anywhere else, and it still passes. The join regex is also single-quote-only, so a double-quoted call drops joins to 0 and downgrades the check to a WARN, and a covenantGate( inside a comment inflates the gate count.",
  "fix": "Pair them structurally — require an awaited gate within the preceding lines of each join — after stripping comments, and accept both quote styles.",
  "notes": "Duplicate of SD-21.",
  "slice": "tests/, tools/",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "TT-16",
  "title": "preflight check 2 is role-blind, revoke-blind and comment-blind",
  "file": "tests/preflight.mjs",
  "line": "44-51",
  "severity": "P3",
  "kind": "bug",
  "category": "false-negative",
  "sessionCode": false,
  "confidence": 8,
  "quote": "  const granted = new Set([...migs.matchAll(/grant\\s+(?:all|execute)\\s+on\\s+function\\s+(?:\"?public\"?\\.)?\"?([a-z0-9_]+)\"?/gi)].map(m => m[1].toLowerCase()));",
  "failureScenario": "The regex stops at the function name: a grant to service_role satisfies it while authenticated holds nothing; a later revoke in a newer migration does not unsatisfy it, and migrations are never edited so the old GRANT text stays in the corpus forever; and a commented-out grant counts. Scanned the corpus and found no live instance of any of the three — but (b) is the shape D37's history says will recur.",
  "fix": "Match the full statement through its semicolon and require the right role; strip comments; process GRANT/REVOKE in filename order so the last statement wins.",
  "notes": "db-checks 3 asks the live database the right question — preflight's job here is only to catch it before the push.",
  "slice": "tests/, tools/",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "TT-17",
  "title": "preflight's <script> regex matches only bare <script> and <script type=\"module\"> — one added attribute removes a block from checks 3, 6 and 18",
  "file": "tests/preflight.mjs",
  "line": "63, 96, 517",
  "severity": "P3",
  "kind": "opportunity",
  "category": "brittleness",
  "sessionCode": false,
  "confidence": 9,
  "quote": "  for (const m of html.matchAll(/<script(\\s+type=\"module\")?\\s*>([\\s\\S]*?)<\\/script>/g)) {",
  "failureScenario": "Three checks share this pattern. Add any attribute — defer, an id, a nonce if the CSP is ever moved off unsafe-inline, or reorder to `<script defer type=\"module\">` — and that block silently disappears from the bridge check, the parse check and the free-identifier check, with no failure and no warning. The `> 100` length filter has the same shape: a short block, such as a future pre-paint shim, is never syntax- or scope-checked at all.",
  "fix": "Match any attribute string and derive isModule from it; assert the block COUNT so a block vanishing is itself a failure; drop the length filter.",
  "notes": "",
  "slice": "tests/, tools/",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "TT-18",
  "title": "preflight check 7 is a single-line regex over multi-line innerHTML templates",
  "file": "tests/preflight.mjs",
  "line": "116-122",
  "severity": "P3",
  "kind": "opportunity",
  "category": "weak-assertion",
  "sessionCode": false,
  "confidence": 8,
  "quote": "  const sinks = [...html.matchAll(/^.*innerHTML[^=]*=[^=][^\\n]*\\$\\{(?!esc\\()[a-z]+\\.(name|title|body|display_name|city|label)\\b[^\\n]*$/gmi)];",
  "failureScenario": "`[^\\n]*$` confines the match to the line innerHTML appears on. Of the file's 188 innerHTML assignments, 24 open a template literal that continues on later lines — everything past line 1 of those is invisible, as is the very common `el.innerHTML = builtString`. The field list also omits note, msg, handle, reason, comment, course. A widened single-line variant found one hit, safely routed through mkr(). No live XSS found — the coverage is the finding, and B-02 and IDX10-10 are two sinks it could not see.",
  "fix": "Walk the actual template literals and whitelist safe wrappers instead of blacklisting field names.",
  "notes": "",
  "slice": "tests/, tools/",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "TT-19",
  "title": "build-tokens silently drops any colour token that is not exactly #rrggbb from the phone's palette",
  "file": "tools/build-tokens.mjs",
  "line": "74, 80",
  "severity": "P3",
  "kind": "bug",
  "category": "silent-omission",
  "sessionCode": false,
  "confidence": 8,
  "quote": "const isHex = (v) => typeof v === 'string' && /^#[0-9a-f]{6}$/i.test(v);\n...\nconst colorTokens = entries.filter(([, , s]) => isHex(s.dark ?? s.light));",
  "failureScenario": "A colour written #fff or #rrggbbaa is excluded from CSPalette and Tokens.swift with no warning. Check 10 compares index.html against tokens.json (both have it, so it passes) and then compares the generated files against themselves (also passes) — so the check whose purpose is 'one palette, not two' reports agreement while the phone silently has no name for the colour.",
  "fix": "Accept the other hex forms and normalise, or keep the strict test and RAISE on any colour-group token that fails it.",
  "notes": "A louder sibling: a hex dark with an rgba light produces invalid Swift, caught at compile time rather than here.",
  "slice": "tests/, tools/",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "TT-20",
  "title": "ship.sh warns loudly on 'database unknown' but says nothing on 'functions unknown', and proceeds either way",
  "file": "tools/ship.sh",
  "line": "68-82",
  "severity": "P3",
  "kind": "bug",
  "category": "inconsistency",
  "sessionCode": false,
  "confidence": 8,
  "quote": "elif have database unknown; then\n  say \"\"; say \"${RED}Database state unknown — the CLI could not answer. Not skipping it silently.${OFF}\"\nfi\n\n# --- edge functions ---\nif have functions maybe; then",
  "failureScenario": "deploy-status returns four states per layer; ship.sh handles database owed and unknown, but for functions only maybe. On any machine where the CLI cannot answer, the run prints a RED database warning, prints NOTHING about edge functions, and walks into the client push — against the header's promise to show all three and ask separately. Separately, have() returns 1 both when the state does not match and when the JSON fails to parse, so a malformed --json would skip all three layers and print 'done'.",
  "fix": "Add the functions-unknown arm, and make have() distinguish a parse failure from a state mismatch.",
  "notes": "",
  "slice": "tests/, tools/",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 },
 {
  "id": "TT-21",
  "title": "db-checks 15 watches only lock_fail, and its ratio arm would have stayed silent through much of the incident it was written for",
  "file": "tests/db-checks.sql",
  "line": "322-343",
  "severity": "P3",
  "kind": "opportunity",
  "category": "coverage",
  "sessionCode": true,
  "confidence": 7,
  "quote": "    when attempts >= 3 and fails > oks then 'FAIL — ' || fails::text || ' lock_fail vs ' || oks::text || ' lock_ok in 7d'",
  "failureScenario": "The js_errs arm is good and is FAILing correctly right now ('11 lock_fail(s) carrying a JavaScript reference error in 7d: staged is not defined'). The ratio arm is weaker than the incident it cites: 1 ok vs 11 fail over 25 days is ~3.4 events per 7-day window, so `attempts >= 3` sits on the threshold and a quiet week PASSes over a live bug. More importantly the check is scoped to ONE flow: the identical post-commit JS throw on start_season, form_squads, create_league or settle_major is unwatched, even though both global error handlers now keep a 4-frame stack in the same table.",
  "fix": "Add a generic sibling: any client_events row in 7d whose msg matches a reference error, grouped by event, FAILs regardless of flow.",
  "notes": "",
  "slice": "tests/, tools/",
  "verification": {
   "status": "unverified",
   "note": "P2/P3 — not put through the two-skeptic verification pass"
  }
 }
]